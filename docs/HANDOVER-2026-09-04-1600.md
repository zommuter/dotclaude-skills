# Handover -- 2026-09-04 ~16:00

Session `cf972527-e10a-4659-bd27-b842d569b075`. Successor to
`docs/HANDOVER-2026-09-03-2130.md`, **whose section 3 this session found WRONG about five of
ten items** -- see section 6.

Everything committed and pushed to private `origin` at **`6db550e4`**. Public `github` NOT
pushed (deliberate, per the `id:7408` ratification gate). 38 commits. Working tree clean.

Checkpoints: `relay-ckpt-20260904-1437` (review, `reviewer (claude-opus-5)`) and
`relay-ckpt-20260904-1518` (handoff, `integrate (claude-opus-5)`).

---

## 1. The one thing to read if you read nothing else

**`id:03a3` was NOT safe to run, and the owner's instinct is what caught it.** He asked, before
the 46-repo migration, whether a relay classifier treats TRIMMED files the same as UNTRIMMED.
It does not. Everything else this week had measured that the trim reduces BYTES; nothing had
tested that consumers DECIDE the same afterwards.

Three defects, all now fixed and verified. The worst, `id:3795`: the shrinker plants the
`-- detail:` pointer BETWEEN the title and the lane tag, so `unpromoted-scan`'s `primary_lane()`
went lane-dark on 18 items and 8 lost an EXECUTABLE lane. Since `promote>0` yields `handoff`
and `surface>0 AND promote==0` yields `human`, **the trim could silently convert
pool-executable work into human questions, across 46 repos.**

The method mattered: a MATCHED PAIR (today's ledgers versus the same ledgers with note prose
inlined back), never a git comparison, which would have confounded the trim with a week of real
content change.

---

## 2. Verified checkbox state -- I checked every one of these

The last two handovers both misreported this. Do not trust the list; the commands are cheap.

| id | box | where |
|---|---|---|
| `9ce0` | `[x]` | `TODO.archive.md` |
| `1447` `a580` `3795` `d4d3` `e96c` `bfd3` | `[x]` | `TODO.md` |
| `cf64` `2654` `60eb` | `[x]` | `ROADMAP.md` |
| `7c82` | **`[ ]`** | `TODO.md` -- deliberately, see below |
| `e567` | **`[ ]`** | `ROADMAP.md` -- migration delivered, gate remains |
| `37ea` | **`[ ]`** | `TODO.md` -- batch 1 only |
| `e047` `0176` `ddcb` `962b` `735f` `c132` `8372` | `[ ]` | filed today, 6 carry red specs |

**`7c82` is NOT ticked on purpose.** Its mechanism is demonstrably live (4 expired carve-outs
now execute), but the relay review found its DONE-CHECK unsatisfiable as written -- both clauses
false at HEAD, naming a file that carries no `# fails-against` lines and never has. Ticking it
would assert a check that cannot pass. **It needs a corrected acceptance, not a tick.**

`bfd3` is `[x]` as DROPPED, not as done. Read its note before citing it.

---

## 3. Owner decisions today

- **`e567` option 2** -- shrink the note header. Delivered: 497 -> 255 B/note, 387,292 ->
  200,005 total, 784 of 784 bodies BYTE-IDENTICAL. `id:f3d2` NOT reopened.
- **`1447` dataflow narrowing** over the cheaper split-the-set option, then **amended so
  `untraced` REFUSES** -- recorded as an explicit amendment quoting the superseded sentence.
- **`37ea` DISSOLVE** the cited-body hazard by re-anchoring consumers, over three build-a-guard
  options.
- **`37ea` orphan-scan: measure first (1+3)**, then fix the consumer.
- **String literals: grepped = SYNTAX, leave; sweep the rest.**
- **`bfd3` DROPPED as proposed**, re-filed on the extraction axis as `ddcb` + `962b`.

---

## 4. What to do first

1. **`e567` is the last thing gating `03a3`.** Its migration is delivered; it stays open only on
   "`id:03a3` cites it". The red spec `tests/test_note_header_cost_cited_e567.sh` demands the
   citation carry a MEASURED per-note byte figure, not just the token.
2. **`ddcb` and `962b`** -- the two extraction fixes that replace `bfd3`. Both mechanical, both
   fail LOUD (an unresolvable case keeps the site IN). `962b` must key escape-decoding on the
   READER LANGUAGE: shell single quotes do not interpolate while Python and JS do, and getting
   that backwards corrupts a shell pattern.
3. **`0176` before any batch shrink.** Its escape is evaluated against the wrong future state
   and already silences `tracker/ledger-map.py:493`, the ONE genuine continuation-body consumer
   on this tree.
4. `735f`, `c132`, `e047`, `8372` all carry red specs authored this session.
5. **`7c82`'s acceptance needs rewriting** before it can close.

---

## 5. THE PATTERN, found seven times today

**A check that cannot fail, or cannot say why it failed.**

1. `9ce0` -- a counter reporting 0 unconditionally after its mechanism was emptied.
2. `1447` -- union-anchoring at FILE level, so a file reading notes in one function and the
   ledger in another was a silent false clear.
3. `37ea` -- TWO tests passing while asserting against text the shrink had moved.
4. The relay review -- **my own `9ce0` negative case was red for the WRONG reason**: its
   mutation raised `KeyError` at setup and never reached its assertion. I had run its green-now
   path and not `verify-negatives`.
5. `735f` -- `|| fail` after a grep cannot distinguish a FALSE assertion from a check that
   COULD NOT RUN. `grep` exits 1 for no-match and 2 for an error. **360 sites.**
6. `c132` -- the hermeticity backstop fires on relay worktrees, so ordinary relay operation
   reports a false breach. A guard that cries wolf trains you to ignore it.
7. `bfd3` -- a proposed filter that would have SILENTLY CLEARED a demonstrated real consumer.

**Three of these were in work I had already checked myself.** The ones caught were caught by
re-running the check, never by reading the report.

---

## 6. Traps paid for -- do not pay them again

- **The previous handover's "What closed" list was wrong about FIVE of ten items** (`cf64`,
  `40c0`, `2654`, `60eb`, `2eba` all unticked). Its section 8 contradicted its own section 3.
  This is the THIRD recurrence. **Verify every checkbox before repeating a status claim.**
- **A green suite from the agent that wrote both code and test is not verification.** Re-derive
  against the ORIGINAL incident.
- **A "green" measured on a MOVING tree is not evidence.** A suite run mid-agent-edit reported
  575/0/4; the settled-tree rerun found a failure.
- **`claim.sh` is MODE-BLIND** -- same key refuses regardless of `--mode`, so handoff and review
  CANNOT run in parallel on one repo. That is correct: both write `ROADMAP.md`. The relay's D3
  invariant settles the order -- review before handoff.
- **Counts over a computed reader set are TREE-DEPENDENT.** The `1447` note's 105 does not
  reproduce; independent runs give 45, 49, 50 depending on tree and slice. The LEDGER tier is
  pinned at 6; only `untraced` moves.
- **`ledger` vs `untraced` is a PROVENANCE boundary, not act-on/ignore.** 3 of 6 ledger-traced
  sites are `\n` artifacts, and the one genuine body consumer scores `untraced`.
- **The extracted `\n` is a TWO-CHARACTER literal, not a newline.** `extract_patterns` does not
  decode escapes. This refuted `bfd3` and is the whole of `962b`.
- **Regenerate a baseline from a PINNED CLONE** when any agent may be editing the script the
  regen runs. A corrupt baseline here is the destructive outcome.
- **A single-ledger baseline regen TRUNCATES the shared file.** Always the two-command
  both-ledgers form, then verify both basenames survive.

---

## 7. Cross-session state -- loderite

- **`id:1447` is CLOSED and pushed.** Their owner gated their shrink run on it LANDING. That
  condition is now met; **the call is his, and I have not represented it as anything else.**
- They filed `id:e6ab` (`BLOCKED on upstream (routed:d4d3)`). **`d4d3` is now fixed** -- the
  notes directory is derived from the pointer, and their corpus was the second test: every
  pointer found, ZERO missing. Their byte figures are theirs and are not reproduced here.
- **Their prompt-size gate was inert and is now not.** Before the fix, `docs/roadmap-notes/`
  (823,644 chars, 209 files) was invisible to `classify-repo.sh` while `ROADMAP+TODO` (447,575)
  was counted -- the invisible half nearly TWICE the visible one, growing with every note.
- They had a real **"Prompt is too long"** executor death the same day. Recorded in `e567` as
  CO-OCCURRENCE, explicitly NOT causation, with their own three caveats attached.
- `2eba` still waits on their `archive-roadmap.mjs` mirror. Deliberate divergence, not an
  oversight.
- Their session repeatedly caught things we had missed, including auditing its OWN clean result
  because "a predicate reporting 0 is the shape of the defect this thread started with". Treat
  their reports as high-quality and their caveats as load-bearing.

---

## 8. Still owed / not done

- **Public `github` not pushed.** Deliberate.
- **`7c82` cannot be ticked** until its acceptance is corrected.
- **2,817 em/en dashes remain repo-wide** across 708 `*.sh`/`*.py` files, mostly Python
  DOCSTRINGS the comment sweep did not treat as comments. Today swept 144 comment + 81 literal.
  This is a migration, not a sweep.
- **`relay-doctor.sh` / `relay-reconcile.sh`** print the same ungrepped "path not found" lines
  that were swept in `unpromoted-scan.sh`; that idiom is now inconsistent across three scripts.
- **`RES_RE` in `mechanical-orphan-scan.sh` matches `[—-]` but not U+2013.** Left deliberately
  to keep that change verdict-preserving.
- **`classify-repo.sh` charges 32,768 B for `docs/ledger-notes/4983.md`, which never existed** --
  the pointer is QUOTED inside a `REVIEW_ME.md` box about that very defect. Conservative
  direction; the "documenting a marker defect corrupts marker-counting tools" class.
- **Three `REVIEW_ME` boxes opened by the review**: `7c82` (unsatisfiable done-check), `2654`
  (baselines were stale -- REGENERATED this session, 4,060 chars recovered), `d119` (closed
  `[x]` with both deliverables absent, pre-existing).
- **`37ea`'s residual stands**: the sweep establishes NO invariant that the next consumer will
  be union-anchored. That is the `cb3e` shape in reverse and the surviving argument for a guard.
