# Handover -- 2026-09-02 ~15:00

Session `019adc92-1869-4df7-a992-54d160c82670`. Successor to `docs/HANDOVER-2026-09-02-0045.md`.

**The shrink LANDED.** Three ledgers 1,684,939 -> 529,015 bytes (**-69%**), `TODO.md` alone
1,340,493 -> ~254,700 (**-81%**). `main` clean at `89fdedd1`+, all pushed to private `origin`;
public `github` deliberately untouched. Suite **558 passed / 0 failed / 4 expected-red**.

Ledgers are short now, so `Read` them freely -- but they are 68% conforming, not 100%, and
`relay/scripts/todo-conformance.sh <file> | grep shape-prose` tells you which lines still
carry prose. Never `Read` `relay-loop.js` (~77k tok).

---

## 1. What landed

| id | what |
|---|---|
| `e95b` | roadmap-lint rule 3(c) follows per-id detail pointers; a MISSING note is its own `DETAIL-POINTER-MISSING` finding |
| `75c8` | gate signal identity is rule + item, measurement stripped |
| `30fe` | **structural SHAPE check** -- reports prose outside lane/gate/id/title/pointer. WARN only |
| `6546` | wave-2/3 shrinker: re-split, closed `[x]` items, `--notes-dir`, case-insensitive `blocked`, lane INVARIANCE |
| `1254` | three prose-laned items given real lanes |
| -- | editable-notes policy + REVIEW_ME archive rule, both in `CLAUDE.md` |

**The lane rule took four attempts and only the fourth is right.** Rounds 1-3 all tried to
answer *"what is this item's lane"*, which a line cannot tell you: round 1 promoted every
body token (moved 3 OPEN loderite items into pool lanes -- reverted), round 2 promoted
"when unambiguous" (invented lanes here), round 3 relocated everything (left items
lane-less). The answer is **INVARIANCE**: `id:4da4` already defines the lane as the FIRST
token on the line, so a token that is not first is prose and one that IS first must stay
first. Asserted, not trusted -- a split that would change the computed lane is REFUSED.

---

## 2. THE OPEN GAPS -- read this before starting anything

All promoted to `ROADMAP.md` with acceptance criteria, three with RED specs already merged.

- **`id:5f34` -- do this first.** `shrink-acceptance.py` scores a LOST violation as an
  improvement, so it structurally cannot tell "a spurious hit disappeared" from "a detector
  went blind". It called wave 1 clean while **40 items' decided-markers had gone dark**. The
  fix is half-built in the same file: `attribute_dispatch_gain()` does exactly this
  attribution in the other direction. RED spec: `tests/test_shrink_acceptance_loss_attribution_5f34.sh`.
- **`id:1608`** -- `orphan-scan --shipped`, the SEVENTH body-reading consumer and the only
  one measured to MANUFACTURE a wrong recommendation (`TICK-READY` 0 -> 1; `id:2b4b`'s sole
  suppressor was the word VERIFY in prose that moved). RED spec merged.
- **`id:4983`** -- the lane grammar is hand-mirrored in three places and they DISAGREE:
  `ledger-shrink.py`'s `_LANE_PATTERNS` admits `[INTENSIVE]` as a lane class,
  `classify-repo.sh`'s `LANE_TAGS` does not. Latent, not realised (re-verified: 0 lane
  changes under classify's exact set). Tagged `[ROUTINE]` -- **probably wrong**, the fix
  changes dispatch grammar and may not belong at executor tier unattended.
- **`id:8524`** -- promote `shape-prose` WARN -> ERROR. Gated on the remaining prose.
- **`id:03a3` -- the biggest one, and NEW at session close (owner directive).** Test and
  migrate the other 46 repos. **3,250,289 bytes** fleet-wide; `it-infra` (621,707, its
  `TODO.md` alone 511k) and `loderite` (527,320) are HALF the total. Per-repo prerequisites,
  learned expensively, are in `docs/ledger-notes/03a3.md`. Gated on `5f34`: migrating 47
  repos with a gate that scores blindness as improvement replicates wave 1's outcome 47
  times and nobody will read each improvements list by hand.
- **`id:1365`** -- sign the owner's authorizations. Filed after the "did the owner really
  say this?" question arose four times in one session and was resolved four times by luck.

---

## 3. loderite -- authorised, unblocked, NOT run

The run did not happen this session. Everything needed is settled, so pick it up without
re-negotiating:

- **Baseline** `e013b748`+ (confirm current with them), clean tree, `docs/ledger-snapshots/before-shrink.txt`.
- **Scope**: `ROADMAP.md` + `TODO.md` ONLY. Not `REVIEW_ME.md`, not the archives.
- **Protocol**: run + commit in loderite, **push NOTHING**. They re-run
  `tools/ledger-detector-snapshot.sh`, diff identity sets, and every id leaving a finding set
  must be explained. Dispatch sets growing by one id is a STOP.
- **Dry-run measured** -232,882 chars (-54%) with the older tool; re-measure, the tool has
  moved three rounds since.
- **Their 86 leading-run old-vocab lane tags are a real prerequisite there.** `lane-convert.sh`
  is the only path for those; the remedy that worked here (give the item a real lane, the
  retired token demotes to prose) applies only where the lane was COMPUTED FROM prose.

**Two decisions are back with loderite's owner**, not ours: whether annotation lines should
own an id (his PROMOTE ruling rested on a premise the FOREIGN-ID guard has since removed),
and moving handoff records to `RELAY_LOG.md`.

---

## 4. Traps, all paid for this session

- **A green gate is not evidence.** "SAFE TO LAND" was true and 40 detectors were blind.
  Read the IMPROVEMENTS list, not just the verdict.
- **`ORPHAN_SCAN_LIMIT` defaults to 10.** Pre/post both return 10 rows with different
  membership, which reads as a composition change and is pure artifact. It produced one
  wrong conclusion here. Uncap before comparing anything.
- **A checker that derives its notion of correctness from the thing it checks cannot fail.**
  Three instances today: the gate above, loderite's hand-maintained token census, and my
  own lane verification borrowing the shrinker's over-broad regex -- so my first "0 lane
  changes" was never an independent check. Ask what a check would be UNABLE to see.
- **Measurement artefacts read exactly like the failure you are hunting.** Six instances
  between the two repos in one day. Neither of us has filed the class yet.
- **The guards are right; cooperate with them.** The vocabulary ratchet blocked this work
  three times -- twice correctly (retired vocab on an added line, which became a shrinker
  refusal) and once on a test fixture that must contain the banned token, resolved by
  composing the token with the reason stated in the file. Never `--no-verify`.
- **NEVER `git checkout --` / `git restore` to discard.** The owner corrected this three
  times before it stuck. Make a fresh worktree at the commit you want, or move files aside.

---

## 5. Still owed

- `tools/ledger-detector-snapshot.sh` -- adopt loderite's, wire in the discovery half
  (theirs is a hand-maintained census that goes stale silently; ours greps the detectors).
- The measurement-artefact hazard class, unfiled by both repos.
- `id:8679` is still OPEN -- "promote the 11" remains UNVERIFIED pending the 11-vs-21
  reconciliation. Do not treat it as settled; loderite is explicitly not doing so either.
- **Five scratch worktrees under `/tmp` are still registered**, all dirty, all abandoned
  generations of the shrink that eventually landed: `/tmp/w3-f2`, `/tmp/w3-final`,
  `/tmp/w3-inv`, `/tmp/w3-redo`, `/tmp/w3-redo2` on branches `w3-final`, `w3-final2`,
  `w3-inv`, `shrink/w3-redo`, `shrink/w3-redo2`. `git worktree remove` refuses them
  (modified files) and I did **not** force -- their content is regenerable and forcing is
  the habit the owner corrected three times today. `/tmp` clears on reboot; run
  `git worktree prune` afterwards and `git branch -D` the five branches once you are
  satisfied nothing in them is wanted. The three `relay/gap*-handoff` branches WERE merged
  and are already deleted force-free.
- `verify-negative-cases.py` **cannot verify a red spec** (green-now-then-red-there is
  structurally impossible before the fix). Both handoff children hit it and verified the
  uniqueness rule by hand. That is a real gap in the runner.
