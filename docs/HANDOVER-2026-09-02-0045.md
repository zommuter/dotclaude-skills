# Handover -- 2026-09-02 ~00:45

Session `e266a9e6-6437-468c-93ea-5b90b1990598`. Successor to `docs/HANDOVER-2026-09-01-2222.md`.

**`TODO.md` is still 1.34 MB.** The shrink was built, ran twice, and was reverted twice on a
gate refusal. Resolve an id with `grep -n 'id:XXXX' TODO.md ROADMAP.md`, then `sed -n` the
slice. Never `Read` it whole, and never `Read` `relay-loop.js` (~77k tok).

State: main clean at `70ea5239`, all pushed to private `origin`. Suite **554 passed / 0 failed
/ 1 expected-red**. Public `github` deliberately NOT pushed.

---

## 1. The headline: the FORMAT is ratified and the tooling is green, but the SHRINK IS NOT LANDED

`id:0d7c`'s format was ratified in `docs/meeting-notes/2026-09-01-2226-ledger-line-shrink-format.md`
(six decisions, then amended in place against 7 forced findings from a `--fabled` pass, then a
post-closure Amendment session on loderite's build evidence). All three tools are built, tested
and pushed. The shrink itself was applied to the live ledgers **twice** and **reverted both
times**, because the acceptance gate refused. **Each refusal found a real unbuilt consumer.**

**Measured when applied:** 1,688,246 -> 583,443 bytes across the three ledgers (**-65%**), with
`TODO.md` alone 1,343,800 -> 268,234 (**-80%**) and its longest item line 34,541 -> 4,279.

### Refusal 1 -- two markers no pattern named
Shrinking `id:78ff`'s TODO line dropped `<!-- children:b466 -->` (a TYPED EDGE, `id:46f6` --
closure is computed from these) and `<!-- xledger-ok: ... -->` (which SUPPRESSES an
`orphan-scan --cross-ledger` report, so losing it made a knowingly-accepted divergence fire as
a new violation). Both are the `id:d35a` silent-no-op class. **Fixed** (`c5d78046`): the keep
rule is now STRUCTURAL -- every `<!-- ... -->` is kept, because in this ecosystem an html
comment IS metadata by construction and item prose never uses one. Enumerating marker NAMES is
precisely how those two were missed.

### Refusal 2 -- a fourth consumer, `id:e95b`, OPEN
`roadmap-lint.sh`'s rule 3(c) NO-ACCEPTANCE-NO-TWIN (`:698`, `id:213a`) asks whether an open
item has an Acceptance/Tests/Done-check clause IN ITS BODY. After the shrink that clause lives
in the detail file, so a workable item reads as structurally un-workable. Measured: the warning
count went **1 -> 2**, the new one `id:6958` (`da55` fires on both sides, pre-existing).
Same shape as `id:2ee1` and `id:f3d2`. **This is the blocker. `0d7c` is now `gated-on:e95b`.**

Also filed: **`id:75c8`** -- the gate itself has a false-positive class. It treats a finding's
full message text as its signal identity, so a detector whose message embeds a MEASUREMENT
reports a false NEW VIOLATION. Of refusal 2's 43 fatals, **42 were this** (37
`length-unshrinkable` + 5 `length-grandfathered`, non-blocking REPORT lines carrying a char
count). Meanwhile that same detector's findings FELL 669 -> 75 and the gate could not see the
improvement. Fix before trusting the gate's count again: normalise signal to rule + item.

**Recommended next step:** close `e95b`, fix `75c8`, re-run. The re-run recipe is in section 5.

---

## 2. What LANDED and is pushed

- **Wave 1** (`f1095e03`): `id:2ee1` ledger-slice follows detail pointers; `id:f3d2` byte gate
  counts them; `routed:71ed` archive block-rule mis-attribution (fixed by DEFER-AND-WARN after
  the filed lint suggestion was checked against the live incident and found to MISS it);
  `id:e8af` derived backlink index (`tools/meeting-backlinks.py` +
  `docs/ledger-notes/BACKLINKS.md`, 667 ids across 218 notes, 0.07 s).
- **Wave 2** (`76bc0e51`): `tools/ledger-shrink.py`, `tools/shrink-acceptance.py`, and the
  length RATCHET in `todo-conformance.sh` + `relay/head-length-baseline.txt`.
- **SECURITY `id:3674`** (`599b9bbe`): `destructive-git-guard.py` ALLOWED `git reset --hard`
  after a safe `git` subcommand + newline or unspaced `;`. Root cause was one layer above
  `_OPERATORS` (which already lists both): `shlex.split()` eats a newline and glues `;`, so the
  splitter saw no boundary and swallowed the second command into an argv whose subcommand read
  `status`. On the DEFAULT path, not an adversarial edge.
- **`id:8679`** -- the 11-vs-21 reconcile. Of 21 indented id-bearing lines, exactly **11 are
  checkbox items** and were promoted; the other 10 are non-checkbox annotation lines that
  cannot be dedented without breaking the TODO grammar. **The original "promote the 11" ruling
  was right and the 21 was my over-count.** Those 10 depend entirely on the
  refuse-to-relocate-a-continuation-carrying-an-id rule.
- **`id:9128`** -- loderite's escalated `routed:d67b`, ingested with both headline claims
  REFUTED (see section 4).
- **`id:5b1f`** -- `orphan-scan --shipped` mislabelled `@container` tracking lines as unblocked.
- A CLAUDE.md design heuristic (owner-approved): a grandfathering mechanism is not a ratchet.

Filed, not fixed, per the owner's ruling: `id:9cac` (`archive-closed.sh` has the same
block-rule defect over three ledgers), `id:5446` (exit-7 caller handling), `id:b7de` (narrow
the pointer grammar), `id:a282` (reference-doc slicing, measurement filed as
`docs/a282-reference-doc-slicing-measurement.md`).

---

## 3. Owner decisions this session -- do NOT re-litigate

- **REVIEW_ME joins the per-item tree**, re-scoped to single-id boxes (7 of 21 live) after the
  closing pass found 14 of 21 are multi-id and `md-merge` refuses multi-marker lines.
- **Keep-set is DERIVED from the detectors**, verified by a DIRECTIONAL round trip -- this
  OVERRODE all six personas' cheaper recommendation, and the e2e/re-extract idea was the
  owner's own.
- **One detail file per id**, sections named by LOGICAL ledger (`## From TODO`), never the
  physical path (it stales at the archive boundary).
- **Ratchet at 500 chars, keyed on LENGTH with monotonic shrink.**
- **`md-merge.py` owns both files**, per-id lock in fixed order; the prose-only-Edit clause was
  STRUCK.
- **Shrink RUN authorised to merge unattended if all gates pass.** They did not. Nothing merged.
- Wave 2 proceeds; push each wave to private origin; file follow-ups rather than fix in place.

---

## 4. Corrections to my OWN claims -- the pattern is the point

Four this session, and the pattern is that a green signal was not evidence:

- **I shipped an INERT fix and the full 546-test suite passed.** `id:5b1f`'s first fix read the
  TODO line; the live marker sits on the ROADMAP TWIN. Caught by running the scan against the
  real ledger and seeing zero matches where I expected one -- AFTER I had already ticked it.
- **I told the owner `destructive-git-guard.py` denies unconditionally.** Its intent is; its
  behaviour was not. loderite pushed back with a measured matrix and was right (`id:3674`).
- **I reported `todo-conformance` as "clean" when it was CRASHING.** Zero output was a silent
  `pipefail` death, not cleanliness: exit 1, **576 real findings reported as none**. Confirmed
  against `git show HEAD:`. Now exit 0 with 680 findings.
- **A regression test of mine passed for the wrong reason** -- its fixture used id `hc01`, which
  is not hex, so the tool skipped the item and nothing was ever shrunk.

Symmetrically, loderite's escalated `routed:d67b` reached the owner on two premises that are
FALSE against this code: `verify-isolation.sh` is not blind to a missing worktree (case (d) at
`:41`, implemented `:73`, exit 2 verified), and `integrate.sh` already calls it with the
CALLER-supplied `--worktree`. loderite re-ran both and retired them (`routed:ec98`).

---

## 5. Re-run recipe for the shrink

```bash
git worktree add --detach /tmp/before-snapshot HEAD        # force-free; guard denies --force
for f in TODO.md ROADMAP.md REVIEW_ME.md; do
  python3 tools/ledger-shrink.py --file "$f" --min-chars 500 --apply
done
python3 tools/shrink-acceptance.py --before /tmp/before-snapshot --after .   # exit 0 = land
tests/run-tests.sh                                          # do NOT pipe to tail; it masks the exit code
```
To revert: `git checkout -- TODO.md ROADMAP.md REVIEW_ME.md`, then delete
`docs/ledger-notes/*.md` **except `BACKLINKS.md`**.

**After a successful land, regenerate the ratchet baseline** -- it was captured at PRE-shrink
lengths, so until it is regenerated the ratchet is ~74% looser than intended:
`relay/scripts/todo-conformance.sh --regen-length-baseline TODO.md > relay/head-length-baseline.txt`

---

## 6. Traps

- **A green suite is not evidence the thing you built works.** Three instances above.
- **Never pipe `run-tests.sh` to `tail`** -- the exit code becomes `tail`'s. I reported a
  1-failure run as exit 0 that way.
- **`~/src/*` globs double-count renamed repos**; use `relay.toml`.
- **Load matters.** Five agents each running the full suite drove load to 58 and made every
  result unreliable. Have agents verify their OWN test; run the suite once, centrally.
- **The destructive-git guard denies `worktree remove --force`** and `rm -rf`. Use the
  force-free forms; do not route around it.
- **`~/.claude` has ONE unpushed commit of mine** (the CLAUDE.md heuristic + user-profile),
  blocked by another session's dirty `settings.json` and `skills/meeting/discoveries.md`. I did
  not commit files that are not mine.

---

## 7. In flight at handover

A fleet-wide `/relay --intensive --quota-7d 20` pool, run id `relay-20260902-004245-29308`,
launched after all preflight gates passed (flags, fable config, mech preflight `proceed`,
mech currency `current`, install drift OK). 7-day quota was at 6.0% against the 20% cap.
The owner asked for a session wrap-up and `systemctl poweroff` once it completes;
`pkaction` confirms `implicit active: yes`, so no password prompt will block it.
