# Handover -- 2026-09-05 ~13:00

Session `14166549-bbc9-4e32-a778-0455ceb39018`. Successor to
`docs/HANDOVER-2026-09-04-1600.md`.

Working tree clean at `0f9b0a90`. Private `origin` has everything. **Public `github` is
UNTOUCHED at `aef97157`** and must stay that way until the item below is resolved.

---

## 1. The one thing to read if you read nothing else

**The loderite censor is HALF DONE, and the unfinished half is a one-command job that is
waiting on the owner.**

loderite's public GitHub repo fronts a public issue tracker; **its code and ledgers are
CONFIDENTIAL**. This repo is public. Owner-directed scope: item titles and internals OUT,
the bare project NAME stays.

- **DONE** -- the working tree at `0f9b0a90` carries none of the censorable internals.
- **NOT DONE** -- three commit messages (`4d9dc0e3`, `7cd3952d`, `c17a7447`) and the
  historical blobs in the unpushed range still contain them. The rewrite was **DENIED by
  the destructive-git guard** and was deliberately NOT routed around. The command is:

```
git filter-repo --replace-text <replace.txt> \
  --refs aef971574e46611d401cfa715b555df1f5b9cf53..HEAD --partial --force
```

The replace-spec is 12 entries: the wave-1 byte counts, the pointer-corpus `+bytes`
figures, their unpushed fix SHA, one item topic, and word-boundary regexes for three of
their ids. If the scratchpad copy is gone, rebuild it from section 6.

**Backup: tag `pre-censor-backup-20260905` = `94767925`, PUSHED TO PRIVATE ORIGIN.** It was
pushed *before* the rewrite attempt on purpose -- `filter-repo` rewrites tags in range, so a
local-only backup tag would have been rewritten along with the thing it was backing up.

**After the rewrite the four `relay-ckpt` SHAs change**, so the four ratification entries
become unresolvable. Close them with `ratify-queue.sh retire <ckpt> --reason ...` (that verb
exists for exactly this: ids landed by another path). Do NOT hand-edit the queue file.

---

## 2. The censor is PARTIAL BY CONSTRUCTION -- do not report it as complete

About **half** the audited internals were **already on the public remote before today**, so
censoring the unpushed range does not remove them:

| already public (needs a PUBLISHED-history rewrite) | count |
|---|---|
| their notes-dir path | 52 files |
| `archive-roadmap.mjs` | 16 |
| `check-ledger-duplicates` | 9 |
| `extract-roadmap-notes` | 8 |
| `ledger-detector-snapshot` | 3 |
| their ids `718c` / `f303` / `d385` / `b276` | 25 / 5 / 5 / 2 |
| their corpus sizes | 3 figures |

Rewriting **published** history is a different decision with a real limit: a force-push
cannot retract what is out. GitHub retains unreferenced objects, and clones and forks keep
the old tree. It reduces discoverability; it does not undo publication. **Owner's call, not
taken.**

`55c7` was deliberately NOT blanket-censored: it anchors a REAL LOCAL item (em-dash
migration S7) and is an adjudicated cross-repo homonym at
`tracker/homonym-allowlist.txt:129`. Censoring the token would have broken our own
provenance. Only the sibling-side description was removed. **187 such homonyms exist -- never
censor a 4-hex token without checking whether it anchors a local item.**

---

## 3. What landed

- **`id:9628`** (`7cd3952d`) -- `ledger-shrink.py`'s `@owner-accepted` keep-pattern dropped
  the DATE, so a dated receipt survived DATELESS. The comment directly above the pattern
  already stated the requirement the pattern failed to implement. Fix: the date is
  **OPTIONAL** `(?::[0-9-]+)?`, deliberately NOT symmetric with `@owner-answered`.
- **`id:4839`** -- filed, promoted, 4 red specs. Three dimensions, and **a fix for any two
  ships the same failure by the third route**: (a) baselines not installed (INERT, but
  ANNOUNCED), (b) baseline key has no repo dimension (WRONG ceiling, unannounced), (c)
  length metric is locale-dependent (`${#r}` = chars under UTF-8, BYTES under `LC_ALL=C`).
  **The manifest fix must NOT land first** -- alone it converts an announced INERT into an
  unannounced WRONG on 46 repos.
- **`id:3bd4` / `id:4f0f`** -- promoted with red specs (md-merge silent-no-op; wrapped-item
  reach).
- **`docs/ledger-notes/03a3.md`** -- wave-1 findings appended, edit DECLARED in the header.
- Pool worked `7408 b890 5355 16a4 b87b 2724 0bb7` besides.

---

## 4. THE `9628` FIX RULE, because the obvious fix is the wrong one

Do not "restore the symmetry" with `@owner-answered` by making the date mandatory. That
drops the dominant **BARE** form out of the keep-set and relocates every bare receipt
fleet-wide -- **1,774 bare vs 222 dated on this tree**, a wider loss than the bug. The spec
pins BOTH directions and was verified to discriminate all three variants: pre-fix fails (A),
mandatory-date fails (B), correct fix passes both. **A dated-only spec ships the regression
GREEN.**

---

## 5. Traps paid for -- do not pay them again

- **A GitHub repo's `private: false` describes the REPO OBJECT, not its contents.** I read
  that flag, concluded a sibling's material was public, and argued from it. Wrong -- the
  public repo is a tracker front for confidential code. **Ask; do not infer publication
  status from an API flag.**
- **A `# fails-against-mutation:` heredoc spread over several comment lines is a SILENT
  NO-OP.** `verify-negative-cases.py` takes only the REMAINDER OF THE MATCHED LINE, so the
  extracted command was the bare `python3 - <<'EOF'`: bash warns, python reads empty stdin,
  exit 0, test runs against the UNMUTATED tree and passes. Use a single-line `python3 -c`
  with an `assert n != s`. Generator gap filed `id:b890`.
- **Declare a substring of the SOURCE line, not of the rendered output.** `FAIL: (A) ...`
  never appears in the file body; `(A) ...` does.
- **The handback's orphan-branch claim was FALSE on both runs today.** It names
  `relay/orphan/<runId>-execute` as parked; the ref did not exist either time. Verify before
  chasing.
- **`Prompt is too long` killed an executor on BOTH runs** -- reproducible, the
  `id:f3d2`/`e567` prompt-size-gate class, not a one-off.
- **Injections are not reliably worked.** Of 5 injected units, 3 were consumed by the pool
  and produced nothing (the run whose executor died); 2 landed. Verify by `workedIds`, never
  by "the pool finished".

---

## 6. The replace-spec, so it can be rebuilt

Literals: `322,886` `144,999` `119,484` `63,802` `583,264` `581,728` `53,623` `cc16d1e1`
and the two-word item topic. Word-boundary regexes for their three newly-introduced ids.
Everything maps to `[redacted]`.

**Do NOT add bare small numbers** (968 / 208 / 86 / 43) as literals -- they collide with
ordinary text everywhere. Phrase-level or word-boundary only.

---

## 7. Cross-session state

The sibling repo's wave 1 is **ratified and pushed on their side**. Their run validated our
`9628` fix end-to-end on a corpus this tree cannot produce (we have zero dated head-line
receipts), against a PINNED hash that matched our committed tool byte-for-byte.

Their findings that are OURS to act on, all verified against our code and recorded in
`docs/ledger-notes/03a3.md`:

1. The shrink **de-grandfathers old-vocab lane tags**, so `pre-commit-lane-vocab.sh` reads a
   mechanical rewrite as a new addition and BLOCKS the commit. Structural: `:151` collects
   ADDED lines, `:6` grandfathers only UNCHANGED ones. Every tail repo with old-vocab tags
   hits this.
2. `[HARD - hands]` is **never** auto-converted (`lane-convert.sh:16-21`), so those repos
   need a human ruling mid-wave and cannot run unattended.
3. Adopting `ledger-detector-snapshot.sh` is **gated on its off-by-one fix**
   (`RSTART+9` -> `RSTART+8`; `<!-- id:` is 8 chars and awk `substr` is 1-indexed). It
   captured a 3-char suffix, 16x fewer distinct keys, so a departure can be MASKED by an
   unrelated arrival while the set diff looks clean.
4. That fix is **not fetchable** -- apply the one-character change by hand.
5. Procedural: take the POST-snapshot on a **CLEAN TREE**. A dirty tree flips the
   `classify-repo` verdict `execute` -> `blocked` with identical id sets (reproduced 3x).

---

## 8. Still owed

- **The history rewrite** (section 1) and then the ratification-queue retire.
- **Public `github` not pushed.** 4 ratification entries pending, none resolved.
- **`id:c4a4` never filed** -- `orphan-scan.sh:337`'s comment says UNMARKED-GATE is
  "unaffected by pointer_missing", true of SUPPRESSION and FALSE of the CORPUS: `:343` sets
  `match_text` to line+note when the pointer resolves and `:475` greps `match_text`. Its
  injection was consumed without work.
- **The backtick-pair defect is UNFILED.** Reproduced here: when a backticked span contains
  a kept marker PLUS other text, the pattern's optional `` `? `` takes one tick and strands
  its partner, leaving an odd backtick count. Not cosmetic --
  `gather-repo-state.sh:321` strips backtick-quoted spans before picking the leftmost lane
  tag (`id:1bbd`), so an unbalanced tick can swallow or expose a lane token. Needs a spec
  case alongside `9628`; the fixture is two lines.
- **`id:03a3` job-B decision** (section 2).
