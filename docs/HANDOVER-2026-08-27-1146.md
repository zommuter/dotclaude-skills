# HANDOVER — 2026-08-27 midday (rule 2c went live; 99b7 escalated to a third-party push)

Session `717457a5-817f-401a-9c62-807bccd0f96a`. **Supersedes `docs/HANDOVER-2026-08-26-2330.md`.**

Do **not** read `TODO.md` whole (the `id:35b7` trap). Resolve any id with
`grep -n 'id:XXXX' TODO.md ROADMAP.md`, then `sed -n` the slice.

---

## 1. READ FIRST — `id:99b7` is NOT a bookkeeping item

It was filed this morning as *"the ratification queue mints entries that can never resolve"* —
queue hygiene. **It is actually: on a non-substantive unit, the relay pushes to every remote it
can reach, including a third party's repository.**

Code-confirmed at `relay/scripts/integrate.sh:845-852`. The only per-remote opt-out is a push URL
set to the literal string `no_push`; otherwise, when `defer_push` is empty (a **non-substantive**
unit), every remote enters `to_push` — the comment says it verbatim: `# non-substantive: push
everything`. For `git-annex` that is `git push upstream` against `git://git-annex.branchable.com/`,
**the upstream project this repo forks**.

Not theorised — **observed**. While authoring the `99b7(a)` RED spec, a fixture used the real
upstream URL and scenario D's non-substantive path actually attempted the push. git-annex's
pre-receive hook rejected it. Nothing was published (`git://` is read-only there), the fixture was
replaced with a local bare repo, and the agent disclosed it plainly.

The **substantive** path is not affected — it defers non-private remotes for ratification, which is
how the four unresolvable entries got minted. Same root cause, two symptoms, very different weight.

**STOPGAP IS APPLIED** (owner-ratified): `git -C ~/src/git-annex remote set-url --push upstream
no_push`. Because `:846` `continue`s *before* `eligible_count` increments, this kills both symptoms
— no push attempt on a non-substantive unit, no `pending_remotes` entry on a substantive one. The
FETCH url is deliberately unchanged, so tracking upstream still works. Revert with
`git -C ~/src/git-annex remote set-url --push upstream git://git-annex.branchable.com/`.

**The lesson, three times in one day:** every real defect today surfaced from *running* something,
never from reading it — rule 2c's total inertness, the multi-line-marker bug, and this. Each looked
well-understood on paper first.

## 2. Hard state (verified 2026-08-27 ~11:46)

| | |
|---|---|
| dotclaude-skills | clean, **0 unpushed**, github at parity; suite **511 passed / 0 failed / 1 expected-red** |
| executor contract | **v16** (v15→v16 today, `id:5eeb` near-done carve-out); `CLAUDE.md` pointer agrees |
| ratification queue | **0 pending** (was 13) — 7 resolved, **6 retired** with reasons |
| parked orphans | **0** across all own repos |
| live pool | `relay-20260827-113009-14765` (`--afk --after 2 --exclude dotclaude-skills`) **still running** at handover time |
| git-annex upstream | pushurl `no_push` (stopgap); fetch url unchanged |

## 3. What landed

**`id:c219` — the big one.** `self-transcript.sh` globbed only `<session>/subagents/agent-*.jsonl`
while Workflow-dispatched children write one level deeper under `subagents/workflows/wf_<id>/` —
a shape that outnumbers the flat one **25,620 to 2,476** on this host, and which four sibling
scripts already hardcoded. Contract v15 rule 2c was therefore **silently inert for every pooled
child**, failing open behind a green 509-test suite. Fixed depth-agnostically (`e7de63b6`).

**`id:413c` — the guard for that class.** `relay/scripts/transcript-shape-preflight.sh`, wired as
SKILL.md **step 0d**, WARN-ONLY. (A) censuses transcripts independently at unbounded depth and
compares against what the resolver reports collecting via a new `--list-candidates` seam — a
cross-check, never a clone, since two copies of "where transcripts live" is what allowed c219.
(C) recovers a real dispatch marker and requires round-trip resolution. Exit 3 = inert, exit 4 =
INDETERMINATE (distinct by design: "could not look" must not read as "all clear").

**`id:5eeb` CLOSED on live evidence, then split.** Rule 2c ran in a real pool for the first time:
**7 verdicts — 5× `ok`, 1× `warn` (265,470 B), 1× `handback` (310,010 B)**, zero resolution
failures. Observation half is `id:5fd1`, **TODO-only and deliberately NOT in ROADMAP**.

**Contract v15 → v16.** The `handback` fired on `leancow` at byte 310,292 of a 335,766-byte
transcript (~92% through); it finished green and integrated. v16 adds a **near-done carve-out**
(land what you have, start nothing new, name the measured bytes). Recorded in-file: v15 was
authored entirely from post-mortem transcripts because the rule could not run; v16 is the first
revision informed by watching it work.

**`id:99b7(b)`** — `ratify-queue.sh retire <key> --reason TEXT` (`182b82aa`). Deliberately NOT
`resolve --force`: that would let a real un-pushed merge be marked ratified, the one thing the
queue exists to prevent. `resolve`'s verification is untouched — verified by re-running it.

## 4. Open decisions — yours

1. **`id:99b7(a)` is spec'd but NOT implemented.** RED spec on branch `work/99b7a-red-spec`
   (worktree `~/.cache/relay/worktrees/dotclaude-skills/spec-99b7a`, commit `8f906c2b`), 16 pass /
   13 fail. Ratified design: an explicit **do-publish allowlist** (owner inverted the offered
   scheme-based exclusion — an allowlist fails CLOSED, which is right for a publishing decision).
   **Migration ratified CONFIG-FIRST**: write `[publish] default_remotes = ["origin"]` plus
   `publish_remotes = ["github"]` for the four public repos (`dotclaude-skills`, `zkm`, `toesnail`,
   `proton-moresync`) BEFORE the code lands, or publication silently stops for exactly those four.
2. **Still owner-decidable before implementing (a)**: whether `undeclared ⇒ never push` is
   unconditional (the spec pins YES, which **narrows** id:4d44 case 4); per-repo `publish_remotes`
   union-vs-override; whether the loud undeclared line is per-unit or deduped per-repo-per-run
   (caller-side in `relay-loop.js`); and how `no_push` and "undeclared" reconcile now that they are
   two ways to say the same thing.
3. **Three existing fixtures will break under (a)** and need `publish_remotes` in their config:
   `test_integrate_per_remote_push_4d44.sh`, `test_diary_push_remote_narrowed_f66e.sh`,
   `test_integrate_ratification_gate_4d44.sh`.
4. **`id:2c2a`** — ratified, scoped, still unbuilt. It rewrites `integrate.sh`'s exit-code contract,
   so it **serializes** behind 99b7(a) and behind any live pool; all three touch that file.
5. **`id:e4e4`** (new) — discovery fan-out granularity: both hops are `MECH_MODEL='bash'` with zero
   agents, so `DISCOVER_SHARDS=6` is inherited from when it might have been an inference-cost
   tradeoff. Candidate (c), one hop forking internally, looks best on cost — but the shard runs
   `reconcile-repo.sh` LIVE (fetch / ff-merge / uv.lock cascade / worktree reap), so N-way forking
   is N concurrent git mutations contending on flock'd shared state. **Measure the discovery phase
   before building** — that cost is still unmeasured.
6. **The privacy tree audit** (`tools/privacy-audit.sh`, run today): **no secrets published.** All 6
   credential-class hits are false positives. 147 are ratified-benign session UUIDs. **68 are a
   0%-true-positive substring match** — a short name pattern with no word boundary, hitting
   "Evidence" 67×, the same defect class as the `gated?` false positive. Genuine exposure is 66 of
   287, all identity-class (home paths in 14 files, name ×1, domain ×3). **The word-boundary fix is
   one line in your PRIVATE pattern file** — I did not touch it.

## 5. `id:5fd1` — what rule 2c still has NOT shown

Four branches remain unexercised. Do **not** tick on a green suite; these are executor *behaviour*,
and that inference is exactly what got `5eeb` wrongly ticked and reverted this morning:

- **ZERO-COMMIT** — budget blown with nothing committed. Added in v15, has **never once fired**, and
  it is the branch the whole livelock guard exists for.
- The **repeat escalation** to `route="hard-split"` on a second ZERO-COMMIT handback for one item.
- The **pre-first-edit NARROW-SCOPE warn** actually narrowing scope — today's single `warn` was
  periodic, not pre-first-edit.
- **v16's own carve-out reporting clause.** leancow never exercised it: the carve-out was written
  *after the fact* to legitimize its behaviour, so "name the measured bytes" has never been tested.

## 6. Gotchas learned today

- **`grep -m1 -o` bounds matching LINES, not matches.** A real dispatch prompt names the worktree
  twice ("Your worktree `<wt>` … on branch relay/`<wt>`"), so the marker came back as a two-line
  string and silently matched nothing. Trim with `${m%%$'\n'*}`, never `| head -1` (the id:81d5
  banned shape).
- **Check WHICH marker failed before reporting a resolver defect.** Three "matched none" hits in a
  run were the literal placeholders `'$marker'` and `'<worktree basename>'` — quoted text from the
  script's own error template and from a TODO item, appearing in transcripts because agents *read*
  those files. Counting failures would have invented a defect.
- **`ratify-queue.sh verify` picks the wrong remote for a fork.** It defaults to the entry's
  recorded `pending` remote; for git-annex that was `upstream`, so `--remote origin` is REFUSED as
  "does not cover this entry's PENDING remotes". The refusal is correct — the entry itself was
  mis-minted.
- **A queue length is not a work count.** The "13 pending, oldest 4 days" backlog was 12 pieces of
  bookkeeping and one void entry. Six were already published and never `resolve`d.
- **`git cherry` reports textual drift, not substance.** All 5 parked orphans showed `+` while every
  one was fully superseded. Verify per-id and per-artifact instead.
- **Nothing reads `salvage/*` tags.** `relay-reconcile.sh` enumerates `refs/heads/relay/*`;
  `death-record.sh`'s salvage probe globs worktrees and branches; the tag readers filter to
  `*-ckpt-*`. A park-then-tag commit is preserved forever and surfaced never.
- **A test in this repo reached the network** (the spec fixture push, §1). Hermeticity is a rule
  here; a fixture holding a real remote URL will act on it.

## 6b. LATE ADDITION — the `--afk --after 2` pool finished after §2 was written

Run `relay-20260827-113009-14765`: **7 units, all pushed, 0 handbacks** — inflownistration
`execute` (dc63) + `review` (3cb5/dc63/8f65/a180), lean4btc `hard` ×2 (de30, 7182), leancow `hard`
×2 (c2d9, 8610), leAIrn2learn `hard` (1ab6). 156 agents, ~42 min, 0 errors.

**Rule 2c got real exercise: 11 verdicts over 26 invocations — 6 `ok`, 4 `warn`, 1 `handback`
(335,952 B).** And **v16's carve-out clause passed its first real test**, which closes one of
`id:5fd1`'s four branches: the `leancow` child measured `handback` at byte 336,200 of a
367,700-byte transcript (~91% through), finished green, and — as v16 newly requires — NAMED the
measurement in its summary: `… suite 10/10 [rule 2c near-done carve-out, measured bytes=335952]`.
Last run's leancow did not, which is why the clause was written. §5's remaining branches are now
three, not four: ZERO-COMMIT, the repeat escalation, and the pre-first-edit NARROW-SCOPE warn.

**NEW BLOCKER — `id:502f` (HIGH PRIORITY), the thing to look at first next session.** The
prompt-size gate refused to dispatch a `review` child for **`loderite` (~520,546 tok)** and
**`it-infra` (~155,393 tok)** against a **100,000 tok** budget — loderite is **5.2× over** — in
BOTH rounds, `repeatHandbackKind: "queue-exhausted"`. **The gate is correct** (it is refusing a
child that would die of `Prompt is too long`, the exact `id:5eeb` class, and it is loud). The
defect is that a correctly-refused review is an **un-reviewed repo, indefinitely**: unaudited
commits accumulate with no anti-gaming pass ever running, and loderite carries the `id:5adb`
salvage work. **Filed UNDIAGNOSED on purpose** — do not assume the cause. Candidates to MEASURE
first are in the item: whether `ledger-slice.sh` runs at all for a `review` unit (cf. the
2026-08-21 `id:35b7` incident where a stale proxy 404'd the slice hop and the gate sized ~315,704
tok of whole ledgers), loderite's own ledger sizes, and whether the diff-since-checkpoint has
grown unbounded — which would be self-reinforcing, since each un-run review enlarges the next
prompt.

**`id:b4dd` closed as STALE.** A 🔴 REVERT-ME line asserted "`github` push is DISABLED (`no_push`)
… the public mirror is NOT receiving pushes". It is not: push url is the real URL, `ls-remote
github` == local `main`, and three pushes landed during this session. Found incidentally. Worth
carrying forward as the derived-doc drift class applied to a **ledger line** — a stale status claim
at the top of the backlog reads as current state and would have sent someone to "fix" a working
remote.

## 7. Next-session shape

The pool from this session may still be finishing — check `~/.config/relay/RELAY_STATUS.md` and
`relay-reconcile.sh --all` before starting anything that touches `integrate.sh`.

**Start with `id:502f`** (§6b) — measure what inflates loderite's review prompt before running
another pool, because two more rounds would just reproduce the same refusal. It is a measurement,
not a build, so it is cheap.

Then: **`99b7(a)` config-first, then implement against the existing RED spec** (it is the
third-party-push fix, and the stopgap only covers git-annex); then **`id:2c2a`** (must follow —
both touch `integrate.sh`); then a longer `--afk` pool, which is the only thing that advances
`id:5fd1`'s three remaining branches.

Cheap and independent: the privacy pattern word-boundary fix (§4.6), which would cut that
auditor's noise by 24% and make its 66 real hits legible.
