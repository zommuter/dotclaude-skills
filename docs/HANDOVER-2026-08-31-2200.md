# Handover -- 2026-08-31 ~22:00

Session `42a3e2fb-066d-41eb-820d-3cbd5755c602`. Successor to
`docs/HANDOVER-2026-08-28-0250.md`.

Do **not** read `TODO.md` whole (the `id:35b7` trap, ~384 KB). Resolve an id with
`grep -n 'id:XXXX' TODO.md ROADMAP.md`, then `sed -n` the slice. Two executor children
died on `Prompt is too long` today for exactly this reason.

---

## 1. READ FIRST -- one decision is blocking, and it is yours

**`id:2065` -- the S9 archive conflict.** The em-dash delimiter migration and the
lane-vocabulary ratchet cannot both be satisfied on `*.archive.md`. Migrating the 85 live
lane tags in `ROADMAP.archive.md` (47) and `TODO.archive.md` (38) makes them ADDED lines
carrying historical venue-keyed vocabulary, so `hooks/pre-commit-lane-vocab.sh` blocks
both files (exit 1, verified with `LANE_VOCAB_ALL_REPOS=1`). **All 85 are closed `- [x]`
entries; zero are open.**

Three ways out, full detail in the ROADMAP item:
- **(a) carve `*.archive.md` out of the ratchet** -- my recommendation. The ratchet exists
  to stop old vocabulary on DISPATCHABLE work, and a closed archived entry is not
  dispatchable. Archives ARE read (`resolve-gates.sh`, `unpromoted-scan.sh`'s twin corpus,
  `lib-typed-edges.sh`) but for ids and checkbox state, not lane tags.
- **(b) rewrite them to capability-keyed vocab** -- satisfies both rules but destroys the
  record of which lane the work was actually done under.
- **(c) exempt archives from S9** -- amends the seam, and leaves 85 tags that S10's
  strictness flip must then carve out too.

The rewritten-but-uncommitted archive content is preserved unstaged in worktree
`.claude/worktrees/agent-a6bb08543e21587fe`. It is deterministically re-derivable if that
worktree is reaped, so reaping it is safe.

**`id:6958` (S9) stays OPEN** until this is ruled. Do not let a pool close it.

---

## 2. What landed

**The em-dash delimiter migration went from S0 to S9-partial in one session.**
`S0, S1, S2, S3, S4, S5, S6, S7, S8` are all closed and pushed. S9's live half is done.

Suite: **532 passed / 0 failed / 1 expected-red**, main clean and pushed at `b921cc33`.

Concretely observable now:

```bash
relay/scripts/lane-delimiter-scan.sh --live-only TODO.md ROADMAP.md   # exit 0, no rows
```

- All three lane readers fail LOUDLY (`rc=2`) on a broken SSOT instead of silently
  substituting a hardcoded vocabulary. Those deleted fallbacks listed **three** INPUT
  lanes while the SSOT defines **four** -- `[INPUT - author]` was invisible to any
  fallback that fired.
- The ratchet blocks `[HARD - pool]` in either delimiter, not just the em-dash spelling.
- `gather-repo-state.sh` now sees a hyphen-spelled `[INTENSIVE - <res>]`. Before S2 it did
  not, so such an item silently lost its run-alone resource claim -- measured 1 match -> 2.

Earlier in the session, before the migration work: three stranded relay branches salvaged
(`feat(3126)` lean4btc, `feat(2790)` git-annex), `linguistic-universals` v0.9.0 landed
with `uv.lock` un-ignored, and `loderite id:d5ff` sent back for re-spec with your ruling
recorded at `ROADMAP.md:1430`.

---

## 3. Open findings from today -- none addressed, all filed

Four are guards that exist and do not fire. That was the session's recurring theme.

| id | what |
|---|---|
| `4e01` | **The lane ratchet no-ops inside a git worktree** -- it compares the worktree toplevel against `relay.toml` own-repo paths. Every agent commit today was unguarded; the same content from the main checkout WAS blocked. Carries two measured constraints: fixing it starts blocking 11 legitimate fixtures, and even fixed it reaches only 3 rename-table lanes, so a green ratchet must never be read as "no old-vocab lines added". |
| `59f2` | A repo whose `[ROUTINE]` items are ALL blocked stays classified `execute` and never advances to `hard`. Measured: S2/S5/S7 were ungated with both hard-gate conditions satisfied and none dispatched. |
| `514f` | `relay-reconcile.sh --integrate` bypasses `integrate.sh`'s `id:4d44` per-remote gate. Integrating git-annex attempted a push to the PUBLIC `git://git-annex.branchable.com/`; only that remote's own hook stopped it. Also prints `(pushed)` on a partial failure. **Publish path -- prioritise.** |
| `4d1c` | Some tests FAIL run directly but PASS under `run-tests.sh` (a hermetic fixture repo exporting `RELAY_TOML` makes the global hooksPath hook fire at test runtime). Pre-existing. **The harness is the only trustworthy signal.** |
| `c442` | `tracker/ledger-map.py`'s three lane regexes are em-dash-only. Flipping its fixtures AND expected JSONs together yields a GREEN suite asserting a DEGRADED mapping (lane, venue, resource and a report row lost, rc=0 throughout). Regexes and goldens must move in ONE commit. |
| `2065` | The S9 archive conflict above. |
| `1ccd`, `6e8d`, `62b4`, `7a5e` | From the pool's own review. `7a5e` is closed. |

Routed out: `routed:7ad4` to **relay-core** (its shadow binary reimplements
`gather-repo-state.sh` semantics; classify parity goes RED until it follows).

---

## 4. Relay run + machine state

Run `relay-20260831-190008-13032` (single-repo, `--afk`, opus, 7d capped 60%) ended
`user-stop` after 2 rounds, 58 agents, 970k subagent tokens. It completed a review and
S6, and produced `id:59f2` by failing to reach the HARD seams.

- **Public `github` is deliberately HELD at `b3f2736e`.** Your ruling: hold until the
  migration is further along. The `relay-ckpt-20260831-1920` ratification entry is still
  pending in the queue -- resolve with `ratify-queue.sh resolve` after pushing, whenever
  you want it published.
- Zero relay refs, zero parked orphans, zero relay worktrees. One agent worktree
  deliberately retained (see §1).
- No pool is live; no leases or heartbeats held.
- `test_privacy_gate_prepush.sh` flaked once under `-j nproc` and passed standalone and on
  re-run. Watch it; one occurrence is not yet a pattern.

---

## 4b. The overnight standby handoff -- filed, not built (`id:8e9a`)

This session ended by handing an unattended pool to a standby session (`dotclaude-skills-1a`)
primed to idle until told, run `relay --intensive --quota-7d 90`, wrap up, and
`systemctl poweroff` while you are afk. The pattern worked but was improvised per-message.

I drafted an `overnight-standby` skill and then **reverted it unimplemented** -- it is
filed as `id:8e9a` with five open decisions instead, because several are yours, not mine:
whether it is a skill at all or a `references/` doc the handoff points at (a
mandatory-load skill costs per-prompt ctx for something used once a night); who owns the
poweroff contract and whether a failed poweroff is simply reported (my view: yes, benign
-- the machine stays on, never escalate); which `--quota-7d` wins when the standby's spawn
prompt and the handoff disagree (tonight: spawn said 60, you told me to convey 90 -- I
conveyed 90 and said so); what the handing-off session must enumerate so a pool cannot
quietly undo owner-reserved state; and whether "push before poweroff, verified across
every repo the pool touched" becomes a hard gate.

One thing to watch tonight regardless: `--quota-7d 90` is close to spending the whole
weekly bucket.

## 5. Next

1. **Rule `id:2065`**, then finish S9's archives and close `6958`.
2. `c442` -- tracker regexes plus goldens, one commit.
3. **S10 (`id:da55`, `[INPUT - meeting]`) is yours.** It drops the dual-vocab tolerance,
   which converts any latent half-migration into a hard fleet-wide failure. It must not go
   to a pool, and it should not run before `2065` and `c442` are closed.
4. `514f` next among the non-migration findings -- it is a publish path.
