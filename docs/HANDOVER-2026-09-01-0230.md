# Handover -- 2026-09-01 ~02:30

Session `be4d4cf2-725d-4a7f-b619-055892e976e0`. Successor to
`docs/HANDOVER-2026-08-31-2200.md`.

Do **not** read `TODO.md` whole (the `id:35b7` trap, ~385 KB). Resolve an id with
`grep -n 'id:XXXX' TODO.md ROADMAP.md`, then `sed -n` the slice. This is not a stylistic
note any more -- see section 1, the same size problem now blocks the reviewer too.

This session did exactly one thing: ran `/relay --intensive --quota-7d 90` to drain, then
wrapped up. It made no design decisions and closed no gated item.

---

## 1. READ FIRST -- `dotclaude-skills` can no longer be reviewed by the pool

**`id:dd59` (filed this session, `TODO.md`).** The assembled REVIEW prompt for this repo is
**~470,285 tok against the 300,000 tok dispatch budget** -- 1.57x over. The `id:4f9b`/`id:b018`
prompt-size gate fired correctly and refused dispatch **3 times in this one run**
(`RELAY_STATUS.md` repeat-handback ALERT, `verdict=review handbacks=3`), so the failure is
graceful but total.

This is not a missing gate and not the execute-side `id:93cc`. The `id:e68f` slicer IS in the
path (`id:b018` made the gate measure the slice), and slicing as currently scoped still does not
bring the repo under budget. **Consequence: executor commits here accumulate with no review
path** -- precisely the blind spot the D3 anti-gaming invariant exists to close.

The same run also produced a raw `[execute:loderite] failed: Prompt is too long` -- an ungated
overflow that reached the API. Both the gated and the ungated form are live in one run.

Directions are in the item; the first step is measuring which component dominates the review
slice (the reviewer sees whole ROADMAP + REVIEW_ME + RELAY_LOG + the diff), not guessing.

---

## 2. Still blocking, still yours -- unchanged from the last handover

**`id:2065` -- the S9 archive conflict.** Untouched this session and still the blocker on
`id:6958` (S9). Full statement of the three options is in
`docs/HANDOVER-2026-08-31-2200.md` section 1 and in the ROADMAP item; nothing new was
learned, so it is not restated here.

**Verified after the run: both are still open.** `id:6958` and `id:2065` are `- [ ]` at
`ROADMAP.md:1666` and `:1667`. No pool child closed either.

**Verified: the archives were not rewritten.** Five `chore(...): archive done items` commits
landed during the run; all five are pure appends to `*.archive.md` (0 deleted lines across all
five, checked with `git show <sha> -- '*.archive.md' | grep -c '^-[^-]'`). No lane-tag
delimiter was touched.

---

## 3. The run

`relay-20260831-220243-21277`, workflow `wf_ae5afaa5-55f`. 22:02 -> 02:12, ~4h 09m.
8 rounds, 5-wide, apex Opus, `--intensive` (so `--afk`; hard units dispatched at apex),
7d quota cap 90%. 780 agents, 7,843,547 subagent tokens, 1,939 tool calls.

**48 units dispatched, 34 completed, 59 blocked, 0 in-flight, 0 agent-failures.**
Drained cleanly -- not a quota stop.

All preflights were green before launch: `proceed` + `current` (proxy pid 2717 held the
on-disk allowlist), no install drift, no pending injections, arg-guard clean.
`transcript-shape-preflight` returned exit 4 INDETERMINATE, which is normal for a fresh
session with no children yet.

Work by repo: `loderite` 9, `it-infra` 8, `dotclaude-skills` 6, `linguistic-universals` 5,
`lean4btc` 5, `git-annex` 2. Six `hard` units landed at apex -- the `--intensive` posture
did what it was for.

In this repo the run closed `c442`, `4ce8`, `32f9`, `4d1c` and filed `ad2a`, `9e86`,
`41d3`, `d3bf`, `bfee`. Suite re-run by hand after the pool drained:
**532 passed / 0 failed / 1 expected-red**.

---

## 4. Repeat-handback ALERTs (id:1432 -- the loop flagged these itself as bug signals)

| repo | verdict | n | what |
|---|---|---|---|
| `dotclaude-skills` | review | 3 | the 470k-tok prompt above -- `id:dd59` |
| `git-annex` | hard | 2 | executor-contract rule 2c fired at 273,449 B vs the 300,000 B threshold, ZERO-COMMIT branch |
| `it-infra` | hard | 2 | SIZE-OUT before starting; `id:1019` is not executable from an `it-infra` worktree |
| `lean4btc` | execute | 2 | classifier claimed 2 open `[ROUTINE]` items that are already `[x]` |
| `loderite` | execute | 2 | child agent failed (the `Prompt is too long` API error) |

The `git-annex` one carries a note worth acting on: rule 2c's ZERO-COMMIT branch commits its
own accumulator line to `RELAY_LOG.md`, and **if the integrator does not merge handback
branches that line is lost**, so the next attempt sees count 0 again and can never escalate to
`route="hard-split"`. That is a real accumulator leak, not filed yet.

---

## 5. Owner decisions surfaced by children (not settled -- yours)

**`loderite` ROADMAP size.** A child ran the item's own acceptance command
(`node tools/extract-roadmap-notes.mjs --min-chars 4000`), landed real progress (committed
`4768bc81`, ROADMAP 400,295 -> 399,491 bytes), then measured that the item's done-check is
**unreachable by that command at any `--min-chars` value**: even `--min-chars 500` only reaches
297,292 chars against a 225,000 ceiling. Four of the six oversized open items have zero
continuation lines -- every word lives on the single head line -- so the extractor's whole
mechanism has nothing to move. Closing them needs restructuring owner-authored decision prose,
which the item's own "Out of scope" clause forbids. The item's scope and its done-check
contradict each other. Needs your call: allow restructuring, accept a staged reduction, or
change the ceiling.

**`lean4btc`** handed back a `hard` unit at a genuine spec/architecture conflict, worktree
left completely clean, no commits -- a decision-gate handback, not a sizing failure.

---

## 6. State I am leaving behind

Main is **clean** at `5c2dc73c`, suite green.

**Public `github` is still HELD at `b3f2736e`** by your ruling. `main` is **178 commits
ahead** of it. Verified untouched: nothing pushed there this session. Every
`dotclaude-skills` close this run recorded `push=partial ratification=pending`, which is the
withholding working as designed. `~/.config/relay/ratification-queue.jsonl` now has **8
pending** entries for this repo (`relay-ckpt-20260831-1920` and `-1933` from the previous
session, plus `-2212`, `-2235`, `-2242`, `-2252`, `-2302`, `-2326` from this run). The
`-1920` entry is still `pending` and was not resolved.

**Two run worktrees left on disk. I did not retire them** -- surfacing rather than acting,
since you were afk:
- `~/.cache/relay/worktrees/lean4btc/relay-20260831-220243-21277-review-repo-0` -- a
  `--no-ff` merge conflicted and aborted; `lean4btc` main is unmoved at `caa0927f`,
  `landed=false`. This one holds real unmerged work.
- `~/.cache/relay/worktrees/dotclaude-skills/relay-20260831-220243-21277-execute-repo-0` --
  at `915e3c8d`, identical to main, from a clean "nothing dispatchable" drain. Safe to reap.

Retiring either goes through `worktree-retire.sh` first (force-free), **not**
`relay-reconcile.sh --integrate`, which cannot take a stranded `relay/<runId>-...` branch and
which `id:514f` says can attempt a push to a public remote. I ran no reconcile.

`.claude/worktrees/agent-a6bb08543e21587fe` (the uncommitted archive rewrite) is untouched and
still uncommitted, as instructed.

**Two stale residuals that predate this run and are not mine** -- left alone, their mtime-TTL
handles them: claim `agent-aca4b63066407fe78.json` (runId `executor-3855...`, claimed
2026-07-21) and heartbeat `discovery-producer.json` (last beat 2026-08-28). This run's own
heartbeat and leases were released -- `claims/` and `heartbeats/` hold nothing from
`relay-20260831-220243-21277`.

Post-drain `backtest-verdict.py --append-log` ran: `agree=2 diverged=59 red=0 expected=59
new=0 crashes=0`. Both gates pass (0 crashes is the hard gate, RED=0 the quality gate).

---

## 7. Filed this session

- **`id:5c05`** -- `--intensive` should PRIORITIZE `[INTENSIVE]`/`[HARD]` over routine
  `execute`, not merely permit them. It only flips `args.allowIntensive` and implies `--afk`;
  scheduling stays the fixed `execute -> review -> hard -> handoff` ladder, so a run launched
  to spend apex capacity on heavy work still drains routine first. Your request, at launch.
  Flagged as a design question: the `review`-before-`hard` rung is the D3 anti-gaming
  invariant and must not be reordered.
- **`id:dd59`** -- section 1 above.

## 8. Wrap-up scans -- surfaced, not acted on

`orphan-scan.sh --shipped` (report-only) flagged 92 candidates, capped at 10 in output. Three
worth your eye, none touched:

- **`id:6217` TICK-READY** -- green `tests/test_make_test_files.sh`, no gate. The suite is
  green, so it likely is done, but I did not tick it: it is a relay ROADMAP item and ticking a
  delegated verdict unattended is the class CLAUDE.md warns about. Re-run and tick if you agree.
- **`id:3ca7` and `id:ebbe` GATE-READY** -- all their gates are now `[x]`, so both are unblocked
  and dispatchable. `id:3ca7` is the round-tail lane-pull item that `id:5c05` interacts with.
- **`id:c4b4` GATE-STALE** (30d) plus five UNMARKED-GATE items (`6c6e`, `c3f6`, `7488`, `d84f`,
  `046a`) that carry gate vocabulary with no typed `gated-on:` edge.

Run `ORPHAN_SCAN_LIMIT=0 ~/.claude/skills/meeting/orphan-scan.sh --shipped .` for the other 82.

`archive-done.sh` moved 3 aged done entries to `TODO.archive.md` (commit `9b626832`).

Machine powered off after this was written. Main clean at `9b626832`, everything pushed to
`origin` (private), `github` still held at `b3f2736e`.
