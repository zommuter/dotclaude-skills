# id:963c

Authored directly by relay review run `relay-20260908-231617-32609` (not relocated by
`tools/ledger-shrink.py`). Conventions and backlinks: `docs/ledger-notes/README.md`.

## From ROADMAP

  - **What happened (2026-09-08)**: the execute unit for `id:64f9` rewrote 4 of 46
    over-budget ledger titles and wrote in `RELAY_LOG.md`: *"Leaving the item open,
    unticked, for a follow-up batch; this session made real, verified progress rather than
    a size-out."* It then returned `64f9` in `worked_ids`, which is what the dispatch
    prompt asks it to do. The integrator ticked the box (`35e8983c`) and the next commit
    (`0f8569de`) swept the item into `ROADMAP.archive.md`. Nothing here was dishonest: the
    executor reported accurately and the driver did exactly what it is written to do.
  - **The mechanism**: `relay/scripts/relay-loop.js:3202` defines the field as *"the item
    id(s) you closed/**advanced**"*. `relay/scripts/roadmap-tick.sh` has no notion of
    "advanced" -- its documented behaviour is to flip the first open box owning each id --
    and `relay/scripts/integrate.sh:803-825` feeds every returned id straight to it. So a
    partial-progress return is indistinguishable from a close, and the honest executor is
    punished for reporting progress at all.
  - **How it surfaced**: ticking the box removed the test's EXPECTED-RED carve-out (see
    `CLAUDE.md` §Testing), so `make test` went from 610 passed / 5 expected-red to 610
    passed / **1 real failure** -- `tests/test_title_rewrite_batch_acceptance_64f9.sh`
    assertion (2). Reopening restored 610 / 0 / 5. That is the whole detection signal, and
    it is already mechanical: the repo's own definition-of-done rule says tick the box,
    then `make test` must be fully green.
  - **Acceptance**: after the integrator ticks a set of `worked_ids`, a tick that turns a
    `# roadmap:<id>`-headed test file from EXPECTED-RED into a REAL failure is REFUSED or
    LOUDLY reverted, naming the id and the test file. An item may not be recorded closed
    while its own spec test is red. The guard re-uses `tests/run-tests.sh`'s existing
    expected-red mapping rather than reimplementing it, and never depends on the child
    self-reporting honestly. A tick whose spec test is green, or which has no `# roadmap:`
    test at all, behaves byte-identically to today; archiving is unaffected except that it
    can no longer sweep away an item the guard refused.
  - **Done-check**: `tests/run-tests.sh tests/test_tick_refuses_red_spec_963c.sh` green --
    hermetic fixture repo with one open `[ROUTINE]` item, one `# roadmap:<id>` test that
    FAILS and one that passes; assert ticking the failing item is refused/reverted and
    LOUD, ticking the passing item succeeds, and an id with no spec test is unaffected.
    Replay the shape of this incident (`id:64f9` at `35e8983c`) and confirm the guard
    fires. Then full `make test` green.
  - **Context**: `relay/scripts/roadmap-tick.sh` (the tick pass and its post-write
    assertion), `relay/scripts/integrate.sh` (~lines 803-825; the `EX_TICK` handback path
    already exists and is the natural place to fail), `tests/run-tests.sh` (the
    `# roadmap:` / expected-red mapping -- read it, do not reimplement it).
  - **SCOPE, stated so it is not quietly widened**: this item asks ONLY for the cheap
    mechanical guard above. Whether the child return contract should instead grow a real
    closed-vs-advanced signal (a separate `closed_ids`, or a `not_closed` field) is a
    DESIGN DECISION that touches every child of every unit type, and it is the owner's --
    deliberately left open by the review that filed this. Do not settle it inside this
    item.
