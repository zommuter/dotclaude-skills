# id:9d12

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

- an it-infra relay review child reported `6 transcripts matched - choosing the most recently modified`, which is a silent wrong-answer rather than a refusal. `id:ff30` makes `self-transcript.sh` THE resolver for 'where is the calling agent's own transcript', and `context-budget.sh --self` is its only consumer, which is what makes executor-contract rule 2c runnable at all. **This matters directly to `id:0a08`**: that item measures rule 2c compliance from transcripts, and a resolver that silently picks the wrong sibling undermines the measurement even for a child that DOES comply. Ambiguity should FAIL LOUD (refuse, name the candidates) rather than pick most-recently-modified. <!-- routed:d359 --> <!-- id:9d12 -->
