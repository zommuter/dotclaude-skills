# id:11a4

AUTHORED note, not relocated by `tools/ledger-shrink.py` -- the verbatim-reproduction claim
does not apply to it (see the "Authored notes" section of `docs/ledger-notes/README.md`).

**EDIT DECLARED 2026-09-09 (review `relay-20260909-205831-5121`)** -- the item has LANDED and is
`[x]` closed. The "Parked work" section below is kept as the record of how it got there, but its
final sentence ("The branch is not merged and nothing here ticks the item") is now FALSE. Read
the RESOLUTION section at the end of this note before acting on anything below.

## Parked work -- READ BEFORE RESTARTING THIS ITEM

An execute unit attempted this item on run `relay-20260909-143257-21736`, DIED of
`Prompt is too long` mid-work (the `id:5295` inert-budget-guard window), and its residue was
commit-and-parked (`id:f272`). The branch is still on disk, unmerged:

    relay/orphan/relay-20260909-143257-21736-execute-11a4-0

It holds 1 commit, +253/-7 across 7 files, including changes to `tests/run-tests.sh`,
`tests/test_ratify_gate_binds_remote_7408.sh` and
`tests/test_relay_doctor_sees_manifest_gap_4839.sh`.

**Verdict 2026-09-09: NOT INTEGRATED -- it breaks a guard test.** Measured, not inferred: the
branch merges onto `main` cleanly, but with it merged
`tests/test_negative_case_syntax_ssot_7c82.sh` FAILS at

    FAIL: (f) run-tests.sh's item_open() pattern changed shape; re-check the twin in

Bisected to this branch specifically -- of the three cleanly-merging orphans from that run,
only this one touches `tests/run-tests.sh`, and only this one changes `item_open()`. With the
other two merged and this one excluded the suite is 623 passed / 0 failed / 4 expected-red;
with this one added it is 623 / 1 failed / 4.

That failure is not incidental damage: `test_negative_case_syntax_ssot_7c82.sh` exists
precisely to notice when `item_open()`'s shape drifts away from its twin. The work here may
well be correct -- but it changes one half of a deliberately-pinned pair and does not update
the other, which is the thing the guard is for.

**To restart**: begin FROM this branch, not from zero. Fix the twin the SSOT test names (the
guard's message tells you which), then re-run
`bash tests/test_negative_case_syntax_ssot_7c82.sh` plus `make test`. Do not "fix" the failure
by relaxing the SSOT test.

**Sibling context**: `ROADMAP.md:195` records this item as the EXPECTED-RED axis of a pair
whose DECLARATION axis is tracked separately -- read that line before assuming scope.

The branch is not merged and nothing here ticks the item.

## Why it was left parked rather than discarded

Its two clean siblings from the same run (`id:799f`, `id:aa5e`) WERE integrated on 2026-09-09
(`relay-ckpt-20260909-2042`, `-2042-2`) after the suite was verified green with them. This one
was held back on evidence, not on caution -- see `id:7f4c` for why leaving orphans parked has
a real cost (a parked orphan suppresses its item, and enough suppressions un-name the execute
verdict and lock the repo out of dispatch entirely). Restart or dispose of this deliberately;
do not let it sit indefinitely.

## RESOLUTION 2026-09-09 -- LANDED, and the branch is now superseded residue

The restart happened exactly as prescribed. An execute unit on run
`relay-20260909-205831-5121` began FROM the parked branch, cherry-picked its commit, and fixed
the one thing the verdict above named: the reshaped `item_open()` had dropped the literal
`grep -qE "^- \[ \] .*<!-- id:${token} -->"` invocation that
`tests/test_negative_case_syntax_ssot_7c82.sh` case (f) pins byte-for-byte. It was restored as an
explicit existence check ahead of the `@container`/`DECOMPOSED` line inspection -- the SSOT test
was NOT relaxed. Merged as `e0af4e8a`; the item is `[x]` in `ROADMAP.archive.md`.

Verified by review `relay-20260909-205831-5121` (checkpoint `relay-ckpt-20260909-2256`..):

- `tests/test_negative_case_syntax_ssot_7c82.sh` PASS, and `make test` is 626 passed / 0 failed /
  0 errored / 3 expected-red.
- The new spec `tests/test_container_never_expected_red_11a4.sh` was replayed against the pre-fix
  `run-tests.sh` from `relay-ckpt-20260909-2245` and reddens at case (B), the assertion the item
  names, with control case (A) still green -- so the close is a real implementation change.
- Zero `tests/test_*.sh` still key an open `@container`/`DECOMPOSED` ROADMAP item.

**Scope note, because it differs from this item's own text**: the landed predicate matches only
`@container|DECOMPOSED`, NOT the `[INPUT - decision]` class the item's body also named. Four
files (`9eb7`, `6217`, `8372`, `dcf3`) were reclassified as legitimately expected-red rather than
retargeted, on the grounds that a decision-gated item eventually ticks while a container never
does. Surfaced for the owner in `REVIEW_ME.md`, Review 2026-09-09f.

**The branch `relay/orphan/relay-20260909-143257-21736-execute-11a4-0` is now SUPERSEDED
residue** -- every line it held is on `main`. It is still parked, so it still suppresses this
item per `id:7f4c`. Disposition is a retire (`worktree-retire.sh`), not a merge; review did not
run it because branch deletion is outside a review child's remit.
