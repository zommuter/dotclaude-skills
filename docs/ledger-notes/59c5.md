# id:59c5

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From REVIEW_ME

Its ratified acceptance had two clauses. (a) *a multi-parent line round-trips both edges* SHIPPED and is re-verified green this review. (b) *"the two spellings resolve to ONE relation, so a parent-side `children:` edge and a child-side `children-of:` edge naming the same pair produce the same fact"* did NOT ship, and the suite stayed green because the RED spec `tests/test_ledger_map_multi_parent_8302.sh` built a fixture for (b) (`aa03 children:bb02`, `bb02`) and then asserted NOTHING about those two lines. MEASURED on the post-8302 tree: `bb01 parents=[aa01,aa02] children=[]`, `aa01 parents=[] children=[]`, `aa03 parents=[] children=[bb02]`, `bb02 parents=[] children=[]`. **My call, and why:** the headline DATA LOSS is genuinely fixed and the archive/CHANGELOG record of that is honest, so I filed the residue as a NEW item `id:59c5` with its own red spec rather than un-archiving `8302` (un-archiving would leave a `[x]` entry in `ROADMAP.archive.md` and an open live entry for one token -- archive-vs-live drift). Reverse it if you would rather the token stay open until its whole acceptance is met. **The reusable lesson is the bigger one:** the RED spec was authored by the previous review in the SAME relay lineage as the acceptance it was meant to pin, so nothing independent checked that its assertions covered every clause -- an under-asserting spec is indistinguishable from a met acceptance once the suite is green. Worth a mechanical check (does each acceptance clause have a matching assertion?) if this recurs. <!-- id:59c5 -->
