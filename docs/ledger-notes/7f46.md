# id:7f46

**EDITED 2026-09-03 (`id:64f9`, title rewrite) -- declared, per the notes-are-editable
convention in `CLAUDE.md`.** This item's ledger TITLE in `TODO.md` was rewritten to fit
`LEDGER_ITEM_TITLE_MAX`. Nothing else on the item line changed: lane tags, typed-edge and
`id:` markers, the detail pointer and the checkbox are all untouched. The ORIGINAL title is
reproduced VERBATIM in the final section of this note, so no wording was deleted -- it moved
here. The relocated prose below is otherwise unchanged.

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

stale skill/command/script-path/doc references in `TODO.md` renamed `fables-turn`→`/relay` (`## relay skill` header; id:ef8a/d0bc/d0da/6a3c/4f21/14c1/045a/62f5/a13c/e0c1; `relay/scripts/`, `relay/references/`, `docs/relay.md`). REMAINING (this item): the config-dir migration DECISION — `~/.config/fables-turn`→`~/.config/relay` symlink is still live; migrate the state dir (with symlink) OR document the cache/state split canonically — plus any code/docs outside `TODO.md` still using the alias. <!-- id:7f46 -->

## Original title (verbatim, before the `id:64f9` rewrite)

Relay fables-turn→relay naming inconsistency: the cache base is canonical `~/.cache/relay/worktrees/` (with `~/.cache/fables-turn` kept as a back-compat symlink), but the state file is still `~/.config/fables-turn/relay.toml` and several docs/usages still reference the `fables-turn` alias. Recommend a consistency pass — either migrate the config dir to `relay` (with symlink) or explicitly document `~/.cache/relay` (cache) + `~/.config/fables-turn` (state) as the canonical split so callers stop using the alias. **PARTIAL 2026-07-07:**
