# id:11a7

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

+ FABLE-REVIEW 2026-07-02: use a SEPARATE relay-doctor-sample.timer (hourly/daily), NOT the single-purpose id:98f0 watchdog; COUNT RISING EDGES not raw rows (persistent-state checks logged per cadence measure duration×cadence, not incidence); log live-pool status per row to filter transient mid-commit false positives; timer runs must NOT notify; define the observe-exit window+threshold now <!-- id:11a7 -->
