# id:3db4

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(measured 2026-08-26 on the full drafts/ population, immediately after `id:391b` wired the drafter and produced its first 13 skeletons). Of the 13, only **4** were fillable (`fadb`/`0ce2`/`8c1d`/`3ee7`, all `id:8bea` ai-codebench judge seams that state their command literally). The other **9 split into three distinct causes, none of which the scanner can currently see**: (a) **6 items whose prerequisite CODE WAS NEVER BUILT** — the five `llm-from-scratch` stage runs (`e868`/`cf0d`/`e617`/`a477`/`9e89`) have no training driver at all (`stages/10_nanogpt/` and `stages/15_nanogpt_ut/` hold only `model.py`, `20_tinystories/` only `config.py`+`tokenizer.py`; the closed seams `eab2`/`0161`/`d3d5` delivered CPU-tested MODULES only — verified by direct `ls`, not inferred), plus `bde7` whose ROADMAP line records its own prereq gap (`id:8bd3` built the HumanEval+ converter on a fixture but never ran it, so the bench has nothing to run against); (b) **2 scanner FALSE POSITIVES** — `11c3` is an `@container` line already decomposed into `47bf`(shipped)/`ac14`(recipe already in `done/`), and `1ae5`'s real lane is `[INPUT — decision] @container`: `mechanical-orphan-scan.sh:108` SUBSTRING-matches `[MECHANICAL]` across the whole line and 1ae5's body QUOTES `hard-lanes.md` verbatim (already filed upstream as `routed:2cf0`, see `~/src/loderite/TODO.md:5`); (c) **1 needing new code** — `3a50`'s `oracle_asymmetry_null` is a library function (`src/traidbtc/validate.py:709`), not a CLI entry point, and its route is deliberately frozen as network-touching. **The (b) class is the SAME word-boundary substring bug as the `gated?` detector false positive (`id:259f`, where *investigate*/*mitigate*/*aggregate* all trip it) — fix them together or at least consistently.** The (a) class is the harder and more valuable one: a drafted skeleton for work whose code does not exist is worse than no skeleton, because it looks actionable. Scope: teach the scanner/drafter a RUNNABILITY precondition (does the named entry point resolve?) and a container/quotation-aware lane match, so `drafts/` means *ready to author* rather than *matched a string*. Do NOT silently drop unrunnable items — surface them as a distinct class, per the no-silent-swallow rule (`id:4347`). <!-- id:3db4 -->
