# id:3c24

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

— owner-ruled 2026-08-20 (`/relay human --all`). Measured: jobAI carried **8** boxes all of the form "confirm this behaves as intended on REAL data" (junior-title penalty, non-spoken-language penalty, LinkedIn staleness cutoff, HTML-snippet stripping, tracking-URL backfill, end-to-end scan, `/scan-jobs` rerank, `prune`+`stats`) — most of which one `uv run jobai scan` session discharges together; yinyang-puzzle carried **8** more that are each "visually verify this tiling's cut output" (PenroseP3, T3636, T3464, T33344, T33434, T33336, T31212, T4612). That is 16 boxes for 2 sittings, and it inflates the human tier by ~17% while making the real backlog harder to read. Neither cluster can be auto-ticked — a human must run or look — so the fix is presentation, not automation: emit ONE box per cluster carrying the individual checks as sub-lines, so the count reflects SITTINGS rather than assertions. **Do not over-apply:** collapse only when the members share one action AND one sitting; 8 independent design judgements that merely look similar stay separate. Owner explicitly approved collapsing BOTH clusters, including the 8 tilings. Re-checkable: `gather-human-backlog.sh jobAI | awk -F'\t' '$3=="review_me"' | wc -l` drops from 8 to 1 with no assertion lost from the box body. <!-- id:3c24 -->
