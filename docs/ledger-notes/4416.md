# id:4416

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

— user 2026-07-17, surfaced while measuring id:3a1c. A workflow's `journal.jsonl` line is only `{"type":"started|result","key":"v2:<sha>","agentId":"<id>"}` — **no `label`, no `model`, no `phase`, no prompt summary** — even though `relay-loop.js` passes `label`/`phase`/`model` to `agent()`. Consequence: answering "what did these 22 agents DO?" required joining `agent-<id>.meta.json` (has `model`, but empty `label`/`phase`) against the first user message inside each `agent-<id>.jsonl` to recover the prompt — a manual reconstruction, per run, to get facts the harness already had at spawn time. The Workflow tool's own diagnostics advertise the journal as the place to look ("one `{"type":"result",...}` line per completed agent with its full return value"), so this is a real gap in the documented path, not just an inconvenience. WANTED: persist `label`, `phase`, `model`, and a prompt digest on the `started` line (and duration/token counts on `result` if available). Note the likely split: `agent-*.meta.json` carrying `model` but EMPTY `label`/`phase` suggests the loss may be harness-side rather than relay-side — **establish which before designing a fix**; if harness-side, the relay-side fallback is to `log()` a label→agentId map per dispatch. Unblocks id:3a1c (can't quantify scaffold wall-time share per category without it) and sharpens `relay-econ.py` (id:08a3), which today re-derives categories heuristically. <!-- id:4416 -->
