# id:32ba

Authored directly, not relocated by `tools/ledger-shrink.py`. Conventions:
`docs/ledger-notes/README.md`.

## From ROADMAP

Found 2026-09-07 by the relay review of run `relay-20260907-100619-27900`, auditing the
`id:2964` close (commit `d7685ee4`).

### The gap

`d7685ee4` fixed `tools/ledger-shrink.py` in TWO halves, and its own commit subject names
the first one:

1. **the SHAPE anchor** -- `_MARKER_RE` replaces the unanchored `<!--[^>]*-->`, refusing a
   value containing `<`, `>` or `--` and bounding it at 400 chars;
2. **the QUOTED-EXAMPLE mask** -- `_MASK_QUOTED_MARKERS` drops a comment-shaped keep whose
   match BEGINS inside an inline-code span.

**No assertion in the suite fails when half (1) alone is reverted.** Every fixture that
exercises an over-match writes its prose `<!--` inside backticks, so the mask suppresses the
over-match before the shape rule is ever consulted. Verified two ways, both without mutating
the tool:

- Every `<!--`-in-prose fixture in `tests/test_ledger_shrink_marker_grammar_2964.sh` (cases
  A and C) and in `tests/test_shrink_example_marker_hoist_8372.sh` (its cases A and C) is
  backticked -- `grep -n '<!--' ` over both files, 5 fixture lines, 5 backticked.
- Replaying case C's body against the OLD regex and the code-span span-list directly: the
  old pattern produces exactly one 366-char over-match, and it starts INSIDE a code span, so
  the mask alone drops it.

Case (E) does not discriminate either: it pins that an unenumerated marker NAME survives,
which the old `<!--[^>]*-->` also satisfied.

Consistent with this, the file's `# fails-against-assertion:` declares `(A) a marker QUOTED
inside an inline-code span was hoisted` -- the MASK half. `make verify-negatives` reports
`red-there OK` against the two-half mutation, and that is honest; it simply cannot see that
one of the two halves is carrying the whole result.

### Why this is not a reopen of `id:2964`

`id:2964`'s acceptance is met and was re-verified independently in this review: `make test`
581 passed / 0 failed / 19 expected-red, `TODO.md` `ee62` is 169 chars and `5817` is 233
(the two measured corruptions), hoisted `id:XXXX` placeholders are down to a single
genuinely-unbackticked prose mention at `TODO.md:668`, and `verify-negatives` is green for
the file. The commit message ALSO declares its own adjacent gap ("NOT verified: that each fix
half reddens independently -- the sandboxed copy-and-mutate probe was denied"). This item is
that gap, measured and narrowed to which half is unpinned.

The shape anchor is not dead weight: `TODO.md:668` carries an UNBACKTICKED
`'<!-- id:XXXX -->'` in prose, so the unmasked class exists in the live corpus. It is
untested, not unnecessary.

- **Acceptance**: an assertion exists that FAILS when `_MARKER_RE` alone is reverted to
  `re.compile(r"<!--[^>]*-->")` with `_MASK_QUOTED_MARKERS` left `True` -- i.e. a fixture
  whose prose `<!--` is NOT inside an inline-code span and whose body would be dragged onto
  the head line by the old pattern. The existing cases keep passing unchanged.

- **Done-check**: add the fixture to `tests/test_ledger_shrink_marker_grammar_2964.sh`, then
  declare the shape-half-only mutation as a SECOND machine-readable case in that file's
  leading comment block (`# fails-against-mutation:` restricted to the `_MARKER_RE` line,
  with its own `# fails-against-assertion:` naming the new fixture's FAIL line), and show
  `python3 tests/verify-negative-cases.py --root . tests/test_ledger_shrink_marker_grammar_2964.sh`
  reporting `green-now OK` / `red-there OK` for BOTH declared cases. `make test` stays
  581-or-better passed / 0 failed.
