# id:cb00

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(found 2026-08-27 during the tracker-pilot re-derivation of `id:90f2`). **MEASURED, not inferred:** `grep -c 'children-of' relay/scripts/lib-typed-edges.sh` returns **0**; the only spelling it extracts is the forward `<!-- children:a,b -->` form written ON THE PARENT. In this repo's `TODO.md` alone the counts are **45 `children-of:` against 12 `children:`**, so about 79% of parent/child edges are invisible to any check built on the shared library. `relay/scripts/ledger-slice.sh:81` already says so in a comment and works around it by defining its OWN local extractor, which is the tell: a second implementation exists precisely because the shared one is incomplete, and the two can now drift. **CONSEQUENCE, observed on a real item:** `id:46f6` made closure computable from typed edges ("closure = child edges by checkbox"), but `id:90f2`'s three blocking children (`143a`, `9453`, `46d5`) are ALL written `children-of:`, so a closure check over the shared library sees `90f2` as having no children and would read it as closable. That is the exact silent-no-op class `id:d35a` is about, one layer down. **WHY BOTH SPELLINGS EXIST:** they are not synonyms and neither should simply be deleted. `children:` is written on the PARENT and enumerates downward; `children-of:` is written on the CHILD and points upward, which is why it dominates (a seam knows its parent at authoring time; a parent does not know its future seams). A fix must read BOTH and agree on one graph. **Fix shape:** teach `lib-typed-edges.sh` the `children-of:` form, have it emit the same edge set regardless of which end declared it, then retire `ledger-slice.sh`'s private extractor so there is one reader. Add a fixture per direction plus one where both spellings describe the same edge, asserting the graph is identical. **Do NOT rewrite existing markers to a single spelling** as part of this: that is a fleet-wide ledger edit touching every repo, and it would land on the same shared non-union files the em-dash delimiter migration is already queued to rewrite. Sequence it after, or skip it entirely once both spellings parse. Sibling: `id:46f6` (typed edges), and the `id:2b4b` family of "the tool reports a clean that the ledger does not support". <!-- id:cb00 -->
