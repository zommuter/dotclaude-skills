# id:1474

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(INBOUND routed:0e9e from loderite). Do NOT fold this into the 5th firing: `id:43c8` was updated earlier today with history `4 (2026-07-26), 4 (2026-07-29), 5 (2026-08-10), 7 (2026-08-13), 3 (2026-08-18 — meeting 1052)`; this is the **1445** meeting, session `495e3576`, note `docs/meeting-notes/2026-08-18-1445-hud-anchor-layout-system.md`, and it is the **6th** firing with **2** forced findings. **Both forcing findings were FALSE FACTUAL PREMISES the closing pass caught inside already-ratified text** — (1) a code fact asserted from a `grep | head -10` that returned exactly 10 lines and truncated line 11, and (2) an enumeration ("all three orphans") that undercounted, colliding with the same decision's own shrink-only ratchet — both confirmed against HEAD before amending. **This is direct evidence for the build design, not just a count bump:** the value came from the subagent **RE-VERIFYING the digest's "verified code facts" against the repo** rather than reasoning from them; a per-decision pass that only critiques *reasoning* would have caught **neither**. That is an argument that the per-decision Fable pass must carry a re-verification step, and it further strengthens the already-recorded owner decision to build the deterministic pre-ratification premise-checker FIRST — finding (1) in particular (a truncating `head` hiding line 11) is exactly the mechanically-checkable class, and is the same `head`-truncation family as `id:da87`. **The BUILD decision remains the owner's** (`SKILL.md` step 0f.6 requires the count be surfaced so the trigger is auditable — this discharges that, nothing more). Relates `id:43c8`, `id:8df5`, `id:da87`. <!-- routed:0e9e --> <!-- id:1474 -->
