# id:b54b

**EDITED 2026-09-03 (`id:64f9`, title rewrite) -- declared, per the notes-are-editable
convention in `CLAUDE.md`.** This item's ledger TITLE in `TODO.md` was rewritten to fit
`LEDGER_ITEM_TITLE_MAX`. Nothing else on the item line changed: lane tags, typed-edge and
`id:` markers, the detail pointer and the checkbox are all untouched. The ORIGINAL title is
reproduced VERBATIM in the final section of this note, so no wording was deleted -- it moved
here. The relocated prose below is otherwise unchanged.

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(found 2026-08-27 during pre-flight for an `--afk` pool; the launch was HELD on it). `git branch --list 'relay/*'` in `~/src/dotclaude-skills` shows **`relay/ok`**, UNMERGED, 1 commit `299b470f` *"child work ok"* adding a single file `g-ok`, author `t`, dated **2026-08-22**. That is a fixture shape, not real work. Its producer is `tests/test_integrate_post_push_handback_5fe2.sh:233` — `git -C "$MAIN_OK" worktree add -q -b relay/ok "$TMP/wt-ok" main` — which is supposed to run against a `mktemp -d` repo; `tests/test_integrate_failed_push_ratification_5155.sh:218` also passes `--branch relay/ok`. **THE HAZARD IS NOT THE JUNK COMMIT, IT IS THE NAMESPACE:** `relay/scripts/relay-reconcile.sh:250` enumerates `refs/heads/relay/*` via `for-each-ref`, which is how orphan relay branches are discovered — so this fixture branch is indistinguishable from a real parked orphan, and the auto-integrate path (`auto-integrate-orphan.sh`; and the `--fabled` bounded auto-integrate) is a candidate to merge `g-ok` into `main`. **This is the SAME CLASS as the `id:99b7` discovery**: a test fixture that reaches real state and ACTS on it — there, a fixture holding the real git-annex upstream URL attempted a third-party push; here, a fixture wrote a branch into the live repo. The repo's hermeticity rule (*"Tests must be hermetic: work in `mktemp -d` … never touch `~/.claude` or the network"*) is stated but NOT ENFORCED. **TWO SEPARABLE PIECES:** (a) decide the disposition of the existing `relay/ok` branch — deletion is the obvious call since the commit is a fixture artifact, but it is a DESTRUCTIVE op on a real repo and is the owner's, not a scanner's; (b) the durable fix — find why the fixture escaped its temp root (a `$MAIN_OK` that fell back to the cwd repo is the prime suspect; check for an unset/empty-var fallback), and add a guard so a test can never create a ref in a repo it did not itself mktemp. A `relay/*` ref in an own repo whose commit is not reachable from any run id is the cheap detector. **PART (a) DONE 2026-08-27 — owner-authorised deletion.** `git branch -D relay/ok` → *"Deleted branch relay/ok (was 299b470f)"*. **Recovery handle recorded before the delete, deliberately, since a branch delete is reflog-only:** the commit was `299b470fa0f23bf514c9d0d27d15a95d803cb555`, *"child work ok"*, 2026-08-22 09:11:11 +0200 — restore with `git branch relay/ok 299b470f` while it survives `git fsck --unreachable`. Post-delete, `git branch --list 'relay/*'` holds only `relay/relay-20260827-102629-23445-execute-5eeb-0`, which is VERIFIED merged into `main` (`git merge-base --is-ancestor` → true), so reconcile has nothing to mistake for a parked orphan and the `--afk` pool launch is unblocked. **PART (b) REMAINS OPEN and is the part that matters** — nothing yet prevents a test writing a ref into a real repo again, and deleting this branch removed the evidence trail, so do NOT treat this item as closed. The prime suspect is still an unset/empty `$MAIN_OK` in `tests/test_integrate_post_push_handback_5fe2.sh` falling back to the cwd repo; the cheap standing detector is a `relay/*` ref in an own repo whose commit is reachable from no run id. **PART (b) MITIGATED, NOT ROOT-CAUSED (2026-08-27, merged 93e6eec5). Do NOT tick.** Three structural guards landed: `tests/run-tests.sh` snapshots `relay/*` refs and the worktree list around the WHOLE run and fails loudly on drift, independent of any individual test's exit code; `relay/scripts/fixture-branch-leak-scan.sh` is the standing detector (a `relay/*` branch that is shape-anomalous or reachable from no run in `RELAY_LOG.md`, excluding the legitimate `relay/orphan/*` namespace); and an `in_tmp()` fail-closed guard sits before every `git worktree add -b relay/...` call site in the two named fixtures. **The ORIGINAL escape mechanism was NOT reproduced** and the investigating agent said so rather than naming a line: `git -C ""` was confirmed to target the caller's cwd, but neither named test could be made to leak via mktemp failure, the same-statement `local` gotcha, or `integrate.sh`, and the leaked commit's content matches no fixture text in any revision. The guards do not depend on that diagnosis, which is why they landed anyway. **A FOURTH defect was found in the guard itself and fixed (40af4ebb):** the snapshot enumerated `git worktree list` unfiltered, including the HARNESS-created agent worktrees under `.claude/worktrees/` that Claude Code adds and removes per subagent. Measured 20 harness entries against 2 real ones, so any agent starting or finishing mid-run would have raised a SPURIOUS breach. It had been verified only against a leak its author created deliberately, never against legitimate concurrent churn: a guard needs both a true-positive AND a false-positive control. Suite 522 passed / 0 failed / 4 expected-red. <!-- id:b54b -->

## Original title (verbatim, before the `id:64f9` rewrite)

**A TEST FIXTURE LEAKED A `relay/ok` BRANCH INTO THIS REAL REPOSITORY, and `relay-reconcile.sh` enumerates exactly that namespace — so a pool can pick it up as an orphan and auto-integrate junk into `main`**
