# id:167f

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

`RUNNER ERROR ... OSError: [Errno 39] Directory not empty: '/tmp/negcase-*/tree/\.git'` raised from `TemporaryDirectory` teardown of the scratch repo. **CONFIRMED INDEPENDENTLY BY TWO AGENTS on 2026-09-01**, which is why it is filed rather than dismissed: the back-fill run hit it twice (`test_integrate_failed_push_ratification_5155.sh`, then `test_gather_dedup_own_id.sh`), and the id:a290 predicate work hit it on `test_negative_case_runner_a73c.sh` at a measured ~1 in 14 full runs. Neither reproduced on an isolated re-run; both then reported `TOTAL: 0`. **MY OWN FIRST HYPOTHESIS WAS WRONG and is recorded so nobody re-derives it:** I attributed the first sighting to contention with a concurrent `git push`, because I had chained the two commands. The back-fill reproduced it with NO push running, so push contention is REFUTED. The shape points at something inside a test still writing into the scratch `.git` as the runner tears it down -- a background `git` process outliving its foreground command is the obvious candidate, but the cause is NOT established and neither agent chased it. **WHY IT MATTERS more than a flaky test usually would:** this tool exists to certify that other tests fail for the right reason, so an intermittent FALSE violation is corrosive in exactly its own domain -- and with 126 declared cases it will now surface regularly enough to be treated as noise, which is how a real violation gets ignored. **Contract:** teardown is robust to a lingering writer (retry, or `ignore_cleanup_errors`, or reap children before cleanup -- pick one and say why); a teardown failure is NEVER reported as a negative-case violation, since those are different things; and a fixture pins that a lingering writer in the scratch tree does not produce a violation. Relates `id:a73c` (the runner). <!-- id:167f -->
