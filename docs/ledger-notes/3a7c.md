# id:3a7c

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

— owner-raised 2026-08-27, the deeper rethink behind `id:502f`. The owner's framing: *"why is RELAY_LOG even READ, let alone WHOLE? It was designed to be an append-only LOG file (or maybe grep-able) and not a functionality mechanism."* `countedLedgersFor` (`relay-loop.js:2891`, id:7c5f) adds REVIEW_ME.md + RELAY_LOG.md to the sized set for a `review` verdict "because only a review child is contractually required to read those two" — so the WHOLE log counts against a 100,000 tok dispatch budget. Measured 2026-08-27: RELAY_LOG is **60% of loderite's 520,546 tok**, and the file is append-only `merge=union` with no archiver, so the share only grows. **This item is the ROLE question, deliberately SEPARATE from the two mechanical fixes** — `id:502f`'s review-shaped slice (A) and the rotator (C) make review RUN again; they do not answer whether a review child should be reading a log wholesale in the first place, nor whether grep/tail-shaped access should replace whole-file reads for LOG-role files generally. Note the same conflation may reach beyond RELAY_LOG: the counted set is the thing to re-derive, per role, not just the one file. **Two premises CORRECTED at filing time, do not reinstate them:** (1) RELAY_LOG is **PER-REPO, not shared across repos** — verified, no shared copy exists at `~/.config/relay/` or `~/.claude/`; per-repo sizes loderite 1,252,525 B / dotclaude-skills 676,801 / it-infra 115,702 / leancow 61,019 / zkm 55,342. (2) `id:502f` says TWO repos are unreviewable; it is at least **THREE** — **dotclaude-skills itself is the WORST at ~550,287 tok** (TODO 1,157,933 + ROADMAP 283,294 + REVIEW_ME 28,648 + RELAY_LOG 676,801 B) and was invisible only because the 2026-08-27 pool ran `--exclude dotclaude-skills`. Any repo crosses the cliff as its ledgers grow — the rest are "not yet", not "not affected". **EMPIRICALLY ANSWERED 2026-08-27 — children ALREADY window-scope it; the GATE is the thing that reads it whole.** Transcript census over **1,050 genuine dotclaude-skills review-child transcripts** (identified by the review-only dispatch string *"Run the full trust-but-verify procedure including the test-integrity audit."*, matched on LINE 1 only — a naive whole-file grep false-positives on transcripts that merely QUOTE relay-loop.js source). `RELAY_LOG.md` accesses: **33 whole-file (~3%), 482 window (`tail -8`, `head -5`, Read with offset/limit), 42 diff-window (`git diff relay-ckpt-…HEAD -- RELAY_LOG.md`), 147 write.** None of the 33 whole-readers died; max observed tool_result was ~28 KB. So the owner's premise is confirmed from BEHAVIOUR, not just from the contract text: the file is used as a log, and the ~161x over-count is charged by `countedLedgersFor` alone. **REVIEW_ME.md is DIFFERENT and must not be lumped in** — 360/1050 (~34%) whole reads; it is the small, actively-pruned file, so counting it is defensible. **CRITICAL CAVEAT, measured: dropping RELAY_LOG from the counted set rescues NOBODY.** At the current 12k overhead the three repos go 550,287→381,087 / 520,546→207,415 / 155,393→126,468 tok — all still above the 100,000 budget; TODO+ROADMAP ALONE are 373,925 / 199,196 / 123,624. The binding constraint is **whole-counted TODO.md + ROADMAP.md**, not the log. Correcting the RELAY_LOG count is therefore honest accounting that stops the gate lying, and is cheap — but it is NOT a fix for `id:502f`, and must not be sold as one. Sampling limit stated by the census: the 33 whole-read cases were inspected in full, the remaining ~2,100 file-touch events were classified by regex with spot-checks. <!-- id:3a7c -->
