# id:0a08

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(measured 2026-08-27, run `relay-20260827-154820-505` round 8). **EVIDENCE, counted from the child's own transcript** (`…/wf_d176e251-2e8/agent-a42ba2332fc0f5ba1.jsonl`, loderite `execute` on `id:551f`): **0 actual `Bash` invocations of `context-budget.sh`** against **31 `Edit`/`Write` tool_uses** (first edit 15:38:17), ending in `Prompt is too long` at **175,479 tok**. The child DID load the contract — the single `context-budget` string in its transcript is the **contract document text itself** (record 9, 15:35:12), i.e. it read the rule and did not execute it. Counting that string as compliance is the trap: grep for a `tool_use` with `name=="Bash"`, never for the script name. **THIS IS NOT `id:c219` AGAIN.** c219 made rule 2c *runnable* (the resolver's glob missed the Workflow-nested transcript shape) and `id:413c` added a launch preflight for that. Runnable ≠ run. The resolver is healthy — step 0d reported `coverage OK, 10/10` at this very run's launch — and OTHER children do comply: the same morning produced **11 verdicts over 26 invocations** across a pool. So compliance is **per-child and unenforced**, which is worse than uniform inertness because it is invisible: the verdict stream looks healthy while individual children silently opt out. **CONSEQUENCE FOR `id:5fd1`:** its finding that the **ZERO-COMMIT branch has never once fired** is now partly explained — a branch cannot fire in a child that never invokes the check. Do NOT keep reading that as 'the branch is untested'; some fraction of it is 'the rule was never run'. **DECISION REQUIRED — how do you enforce a contract rule on a child you do not control?** The repo's own mechanize-first rule says reserve the LLM for what the mechanical layer cannot do and make those cases fail LOUDLY; a budget check that depends on the child *choosing* to run it is the anti-pattern that principle names. Candidates, none evaluated: a host-side periodic sampler that reads the child's transcript out-of-band and kills/hands-back on threshold (no child cooperation needed); a PreToolUse hook on `Edit`/`Write` that refuses the first edit until the check has run; or accepting rule 2c as advisory and sizing executor items so the ceiling is unreachable. Note the first candidate is the only one that works on a child that has stopped following its contract at all. <!-- id:0a08 -->
