# id:2b4b

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

— `relay-reconcile.sh` enumerates ONLY `refs/heads/relay/orphan/*` (`ORPHAN_NS="relay/orphan/"`, `:91`, used by the `--all` sweep at `:361`). A branch enters that namespace only when a run DIES and its worktree is PARKED (id:689c). But a child that hits a ROADMAP.md merge conflict, correctly runs `git merge --abort`, and hands back leaves its branch under the LIVE name `relay/<runId>-<verdict>-repo-N` with the worktree still registered — it is never parked, so reconcile cannot see it in either mode. Measured 2026-08-12 after run `relay-20260812-122721-23819`: `relay-reconcile.sh --all` reported `0 parked orphan(s) across all own repos` and the per-repo form reported `no parked orphans`, while FOUR branches held real unmerged commits (cartulary `execute-repo-1` 297890b, dotclaude-skills `review-repo-0` e28ecb1, escapement `review-repo-0` 931a908, loderite `review-repo-1` 3ad14ec). The handbacks DID name each worktree path, so the information exists in `RELAY_STATUS.md` — but the tool a human is told to run for exactly this ("recover via `/relay reconcile`") answers "nothing here". Note this is NOT a bug in `--all` (which is correctly scoped to parked orphans, id:4e14); the gap is that conflict-handback branches never join the class `--all` enumerates. Fix candidates: (a) have the integrator PARK a conflict-handback branch into `relay/orphan/*` on abort, so the existing consume path finds it — smallest change, reuses id:689c wholesale; (b) widen reconcile to also list live `relay/<runId>-*` branches whose run is no longer in `heartbeat.sh live-runs`, kept distinct from parked orphans in the output; (c) both. (a) is preferred — one namespace, one consume path. Until then a human must read `RELAY_STATUS.md` Blocked worktree paths, which is precisely the manual step reconcile exists to remove. **⚠ THIS ITEM'S PREMISE IS STALE AS OF 2026-08-27 - VERIFY BEFORE ACTING ON IT.** Ran `relay-reconcile.sh --all` live: it printed a dedicated section headed `STRANDED (unmerged, owning run not alive, NOT parked - id:2b4b)` and correctly listed `it-infra relay/relay-20260827-154820-505-review-repo-0`, i.e. exactly the class this item says it cannot see. So fix candidate (b) (widen reconcile to list live `relay/<runId>-*` branches whose run is no longer alive, kept distinct from parked orphans) appears to have LANDED while this item stayed open. **Not ticked** because that is a read of one run's output, not a diff of the implementation against this item's full text, and candidate (a) (park conflict-handbacks into `relay/orphan/*` on abort) may still be unimplemented. **A SECOND correction, and the more useful one:** the it-infra branch did NOT strand via the ROADMAP.md-conflict-then-abort route this item describes. It never touched `ROADMAP.md` at all. It was OVERTAKEN: a later, wider review round started from a fresher base once more owner commits landed, and integrated at 17:17, leaving the earlier attempt's branch behind and un-parked. That is a SUPERSESSION / round-scoping gap, a distinct route to the same symptom, and candidate (a) would NOT have caught it since there was no conflict to abort on. Close or re-scope this item against the code rather than against its own prose. <!-- id:2b4b -->
