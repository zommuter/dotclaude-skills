# id:1ed3

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

**EDITED 2026-09-03 (`id:64f9`, title rewrite) -- declared, per the notes-are-editable
convention in `CLAUDE.md`.** This item's ledger TITLE in `TODO.md` was rewritten to fit
`LEDGER_ITEM_TITLE_MAX`. Nothing else on the item line changed: lane tags, typed-edge and
`id:` markers, the detail pointer and the checkbox are all untouched. The ORIGINAL title is
reproduced VERBATIM in the final section of this note, so no wording was deleted -- it moved
here. The relocated prose below is otherwise unchanged.

## From TODO

`relay-loop.js:494` hardcodes `{ label: "write-relay-status", phase: "Status", model: "haiku" }`, and the prompt (relay-loop.js:484-493) hands a weak model a PRE-RENDERED payload of operational claims (checkpoint tags, item ids, push results) with "Run EXACTLY this one command and nothing else ... Pipe the payload below verbatim ... do not retry or write any file yourself", targeting `RELAY_STATUS.md` — a SHARED coordination file other automated relay sessions read as dispatch ground truth. That is structurally indistinguishable from audit tampering: an agent with ZERO corroborating context ordered to write unverifiable operational state into a file that drives automation. Observed: 6/6 blocked in run relay-20260810-210203-987, 3 blocked in relay-20260810-193031-13271 (loderite). The classifier was right that the AGENT transcript held no evidence for the claims, and wrong that the work had not happened (tags verified present in git). Note the irony: id:0d31 minimized this agent to a single pipe precisely so a weak model could not drift — and that minimization is exactly what strips the context justifying the write. The payload is already deterministic and `relay-status-publish.sh` does ALL the real work (path resolve, claims/burnup sections, atomic flock write, event append), so the agent contributes nothing but the pipe — the same shape as the ~12 existing `model:"bash"` hops (id:4239). CAVEAT for the fix: converting it to `model:"bash"` means MECH_FALLBACK=fallback-haiku would map it BACK to haiku when the proxy is down and re-trigger the block, so that fallback needs an exemption or a direct-write path. Impact today is visibility-only — id:cb50 keeps this off the critical path, so dispatch/integrate/push are unaffected — but the loss is SILENT, and a stale RELAY_STATUS.md is what other sessions read. <!-- id:1ed3 -->

## Original title (verbatim, before the `id:64f9` rewrite)

Make the `write-relay-status` hop MECHANICAL (`model:"bash"`) instead of `haiku` — as an LLM call it is repeatedly BLOCKED by the safety classifier as instruction-poisoning / audit-log tampering, silently losing run visibility.
