# id:d082

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

**This note is EDITABLE** (owner-ratified 2026-09-02). If a fleet rule is violated
in the prose below -- retired vocabulary, a lane delimiter, a banned token -- FIX IT
HERE and amend the line above to say what was changed. Notes are not immutable: an
unfixable violation keeps its guard red forever, and this prose gets copied back out
into new items. An undeclared edit makes the verbatim claim above a lie.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

-- the grammar rule is still right, but adopt it on the SAFETY argument only. Measured in loderite after implementing it: planExtraction(parseBlocks(TODO.md),{minChars:1500,splitHeads:true}) returns extract=[32f7,b56a] both before and after; only skippedIdInCont changes ([9602,e9d3] -> []). Neither parent shrinks by a character. Reason it is unmeetable by any implementation: each parent's ONLY continuation line IS the annotation (536 / 531 chars), so once the rule makes that line its own block the parent's continuation is empty, and head-split then refuses because both heads already carry their detail-note pointer. The claim is self-contradictory -- the material that would make the parent shrinkable is exactly the material the rule protects from moving. <!-- id:d082 -->
