# id:f499

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(found while measuring `id:502f`, 2026-08-27). `relay-loop.js:3962` `if (!item) return null` returns silently; every OTHER failure branch on that path (`throw`, `MECH-ERROR`, unusable path) logs a reason, as the function's own header promises ("FAIL-OPEN and LOUD: any failure — **no item id**, script error, agent throw, MECH-ERROR, empty or non-path stdout — leaves `unit.slice_path` unset, **logs WHY**"). It does not log why for the first of those five. The cost is not cosmetic: `oversizeDispatchReason`'s unsliced-branch REMEDY (`relay-loop.js:3037`) instructs the operator to "find why `ledger-slice.sh` produced no `slice_path` for it (**the relay-loop log records the reason**)" — for a review unit that reason is never recorded, so the remedy is a dead end and the real cause (review units carry no `actionable_routine_ids`, so `dispatchItemFor` yields `''`) stays invisible. This is the id:4347 no-silent-swallow shape applied to a diagnostic path. **FIX IS ONE `log()` CALL** naming the unit's repo + verdict and that no dispatch item exists — deliberately INDEPENDENT of how `id:502f` is resolved, since the silent return also covers any non-review unit that reaches dispatch with no item. Do NOT bundle it into the 502f fix. <!-- id:f499 -->
