# id:4e01

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

fixing this will START blocking legitimate work. Across today's test seams, 11 added lines match the ratchet's exact predicate (`- [ ] … [HARD - pool|hands|meeting|decision gate]`) and ALL 11 are deliberate FIXTURES (ids d001-d004, e001-e006, 2222, 7777) that exist precisely to prove a reader handles old vocab in the new delimiter. They landed only because the worktree bypass let them. So the fix cannot be the membership change alone: either pair it with the `printf -- '- [ ] %s …' '[HARD - pool]'` fixture idiom (already used by `test_lane_vocab_ratchet_delimiter.sh` and, since today, `test_unpromoted_scan_newvocab.sh`) as documented guidance, or give `tests/` a scoped carve-out. Otherwise closing this hands every future fixture author a wall with no signposted way through, and the likely outcome is `--no-verify`, which is worse than the bypass. **SCOPE LIMIT, verified 2026-08-31 against the hook source:** even once the worktree bypass is fixed, this ratchet reaches only a NARROW slice. It fires solely on a checkbox line whose leftmost unmasked lane tag is a key of `lane_rename`, which is exactly three entries -- `pool`, `meeting`, `decision gate` -- plus the `hands` special case. So `[INPUT - meeting]`, `[INTENSIVE - res]`, `[HARD - epic]`, `[HARD - strong model]`, and any line where `[MECHANICAL]` precedes `[HARD - pool]` are ALL outside its reach. Batch D1 measured six of its own conversions producing hyphen lane tags on checkbox lines the hook would never flag even in a properly scoped repo. Consequence: a GREEN ratchet run must NOT be read as "no old-vocab checkbox lines were added" -- it means "none of the three renameable pool/meeting/decision-gate spellings were added". Any future coverage claim built on a clean ratchet is overclaiming. <!-- id:4e01 -->
