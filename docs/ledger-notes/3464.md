# id:3464

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

**TITLE REWRITE REVERTED 2026-09-03.** `id:64f9` rewrote this item's title and the change was
BACKED OUT the same day; the ledger line is byte-identical to its pre-`64f9` form again, so
this note declares no title edit. Reason: the line carries the retired venue-keyed lane tag
`[HARD - decision gate]`, and rewriting the title re-presented it to
`hooks/pre-commit-lane-vocab.sh` as an ADDED line, which correctly BLOCKED the commit.
Converting the tag was not an option -- this item's own subject is that a lane detector
matches `[MECHANICAL]` by bare substring anywhere on a line, so its title enumerates lane
tokens AS THE EVIDENCE, and touching them risks changing the computed lane. That is the same
refusal `64f9` already applied to `id:378e`; this item belonged in it and was missed.

## From TODO

Live false positive: loderite id:1ae5, lane [HARD — decision gate], a DECOMPOSED container whose body quotes hard-lanes.md verbatim ('the vocabulary is [ROUTINE] / [HARD] / [HARD — decision gate|hands|meeting|pool] / [INPUT — …] / [MECHANICAL]') — the scanner matched its own documentation. It surfaced through gather-human-backlog.sh as 'id:1ae5 has no recipe (host=? resource=?) — author a recipe or run mechanical-orphan-draft.sh', i.e. the resolution path invites authoring a recipe for an item that must never run mechanically. Note mechanical-orphan-draft.sh would then write a drafts/1ae5.json skeleton for it (harmless — drafts/ is never consumed — but it is noise pointing the wrong way). Fix: anchor the lane match the way roadmap-lint.sh/hard-lanes.md parse it (leading lane tag after the checkbox), not a substring anywhere in the line. Same class as the emitter-grammar work in routed:2a86/id:378e but a DIFFERENT script. Self-check when fixing: this very inbox line quotes [MECHANICAL] too. <!-- id:3464 -->

## Title-rewrite exemption (`id:64f9`, reverted 2026-09-03)

The `id:64f9` pass rewrote this title and it was reverted; the section that reproduced the
original title is removed with it, because the ledger line carries that title again verbatim
and a second copy here would rot. See the header for why this item is exempt from the pass.

**When `id:14e5` lands** (positional `id:`, visible typed edges) this item becomes rewritable:
the lane tokens in the title stop being ambiguous with real lane tags at that point, so the
substring hazard that forces the exemption disappears. Until then, leave the title alone.
