# id:61dd

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(owner asked this be filed, 2026-08-27). **NOT a runtime defect — verified, do not "fix" it as one.** The headline diagnostic is `relay-loop.js:3616` *Property 'mergedSha' may not exist on type '{ merged: boolean; siblingBranches: never[]; pushRemotes: never[]; pushPending: string; } | …'*. The consumer is guarded by `else if (result && result.landedUnfinished)` (`:3605`), and the ONLY union member setting `landedUnfinished: true` (`:1761-1764`) is exactly the one carrying `mergedSha` — so at runtime the property is always present, and the access is additionally defensive (`result.mergedSha || '?'`). The real problem is that the sibling return shapes do NOT declare `landedUnfinished` at all, so it is not a DISCRIMINANT and TypeScript cannot narrow the union. **FIX:** add an explicit `landedUnfinished: false` (and, where equally absent, `mergedSha: ''`) to the other return shapes of that function so it becomes a genuine discriminated union — a types-only change with no behavioural effect, which is why it needs a test that pins the SHAPES rather than any new behaviour. **WHY IT IS WORTH DOING even though nothing is broken:** six further diagnostics stand alongside it — `PRELUDE_SCHEMA` (`:900`), `SHARD_SCHEMA` (`:931`), `INTEGRATE_SCHEMA` (`:1037`), `DISCOVERY_QUEUE_LATEST` (`:1564`), `DISCOVERY_QUEUE_FRESH_SECS` (`:1565`) and `branch` (`:3270`) all declared-but-never-read. A standing wall of benign diagnostics is how a real one goes unnoticed, and this file is the one whose prompt-template runtime crashes `node --check` + grep already miss (see the loop-crash class, `id:5bac`/`id:aec5`) — i.e. exactly the file where losing a genuine signal is most expensive. **Check each unused declaration before deleting it:** an unused `*_SCHEMA` may be a structured-output contract that was silently orphaned when its agent hop moved to `MECH_MODEL='bash'` (cf. `id:24ec`), which would make it a REAL finding wearing a lint's clothes — not dead code. All line numbers are as of `e55dd474`. **THE CAVEAT FIRED — `INTEGRATE_SCHEMA` IS A REAL FINDING, NOT A LINT. DO NOT DELETE IT AS DEAD CODE.** Confirmed 2026-08-27 while answering an owner question about which dispatch sites specify a model. `INTEGRATE_SCHEMA` is declared at `relay-loop.js:1037` and referenced **only from two COMMENTS** — `:1704` and `:4373` — never from code. And `:4373`'s comment describes machinery that no longer exists: *"integrate() has no try/catch around its `await agent(integrator, {schema: INTEGRATE_SCHEMA})`"*. **There is no such dispatch.** The integrate hop is now `model: MECH_MODEL` (`:3517`) — a fenced `relay-mech` command running `integrate.sh` with ZERO LLM judgment — exactly the orphaned-by-a-move-to-`bash` shape this item warned about (`id:24ec`). **Two consequences beyond the diagnostic:** (a) the `:4373` comment is a stale derived doc reasoning about a failure mode (schema-validation retry exhaustion) that cannot occur on a mechanical hop, so anyone hardening integrate error-handling from it would be defending a phantom; (b) the banked claim that **"the integrate agent is SONNET"** is now **STALE** — integration costs no model spend at all. Fixing this is therefore NOT "delete the unused const": decide whether the schema is still the documented contract for `parseIntegrateResult` (`:1704` says it is) and keep it with a comment saying so, or remove BOTH the const and the two stale comments together. **Verified independently:** all 23 `agent()` sites in this file DO specify a model (execute→`'sonnet'` `:4202`-area, review/hard/handoff→`STRONG_MODEL`, everything else→`MECH_MODEL`, handback-followup→`'haiku'`), so nothing here inherits a parent's model. Line numbers as of `52bffddd`; they shifted from this item's original `e55dd474` refs. <!-- id:61dd -->
