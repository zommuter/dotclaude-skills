# id:d84f

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(owner 2026-07-21) — a research-heavy item (motivating case: id:cae2 Agent-SDK audit — mostly mechanical doc-reading + tabulation, with only the non-obvious KEEP/ADOPT/REFACTOR verdicts truly needing the owner) should not be filed as one monolithic `[INPUT — meeting]` blob that waits on a human. Instead, at FILE time, decompose it into: (a) an **automated-research sub-task** a relay loop / research substrate picks up autonomously (reuse the `deep-research` harness where it fits; gather + tabulate + adversarially verify) → banks findings into the item, and (b) a **human-decision counterpart GATED on (a)** that surfaces ONLY the residual judgment calls the research couldn't settle — **or dissolves entirely if the research is conclusive** ("if needed at all"). Enforces the CLAUDE.md rule that *authorising the investigation is NOT authorising the conclusion* — the research agent RECOMMENDS, the owner RATIFIES only the genuine forks ([[opus-decomp-5fc6]] extractor pattern; mechanize-first [[mechanization-415b]]/[[classifier-4d8e]] — reserve the human for the loud residual). Design Qs: (1) home — a new `/filetodo` skill, or an extension of `todo-update` / the `append.sh` filing path? (2) how the split is triggered — heuristic (item mentions "survey/audit/research/check <external source>") vs an explicit `--research-gate` flag at file time (prefer explicit-by-construction, cf. id:176f `model:"bash"`); (3) the ledger shape of the two halves — a `gated-on:` typed edge (id:46f6) from the human item to the research item, so closure is mechanical; (4) which relay lane the research seam gets ([HARD — pool]? a new [RESEARCH] capability lane per [[relay-substrates]]?) and how its output (a findings block) lands back without a human. Relates id:cae2 (first consumer), the `deep-research` skill, `todo-update`, `meeting/append.sh`, id:46f6 (typed edges), [[mechanization-415b]], [[opus-decomp-5fc6]]. <!-- id:d84f -->
