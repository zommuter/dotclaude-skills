# id:077d

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

*: id:a78b's ENTIRE JOB was to re-pin a wrong test fixture, because the ROADMAP's own prescription ('re-pin to an octant-masked-cell fixture') was FACTUALLY WRONG (octant cut faces are irregular pentagons + right triangles, never rectangles; only octant CUBE PAIRS give literal squares). The Sonnet executor correctly deviated from the spec, modified tests, and was RIGHT — a blanket 'executors may not touch tests' would have BLOCKED a legitimate correct fix. So the rule needs a deliberate escape hatch (an explicit marker commit? a flag that FORCES a review unit? allow-with-loud-surface rather than reject?) — design it, do not just ban. SIMILARLY the ROADMAP rule cannot be a blanket ban: executor contract rule 2 REQUIRES the child to tick its own box, so the hook needs LINE-SCOPED logic (may flip [ ]→[x] on its OWN id's line; may not rewrite the entry or touch another item's line). WHAT A HOOK CANNOT CATCH (say so up front, do not oversell): id:4c84's spec defect (Acceptance vs Done-check disagreeing — a semantic mismatch); and id:e02a's role-collapse, since apex is MECHANICALLY permitted to do everything and the failure was that it did. VS THE SEPARATE-USERS APPROACH (the user's own framing — 'maybe easier'): agreed it is far lighter (no sudo, no user management, works per-worktree, no ownership churn on shared node_modules/caches) — but name the trade honestly: a hook is BYPASSABLE (git commit --no-verify) so it is guarded by prose again at the last inch, whereas Unix file permissions cannot be talked out of. Hook+prose still strictly beats prose alone, and is reversible; consider it the observe-first step (CLAUDE.md observe-before-preventing) and let the separate-users build be gated on the hook's log showing real firings. Relates id:1735 (self-reported summaries), routed:8adf (isolation:c6c8 rescope), routed:6fd5 (orchestrator self-implementation), the D3 anti-gaming invariant, CLAUDE.md mechanize-first + no-silent-swallow. **CONSOLIDATED 2026-07-19 (meeting `docs/meeting-notes/2026-07-19-1212-git-hook-enforcement-substrate-reconciliation.md`):** this is the **SINGLE relay provenance engine** — id:7a05's provenance-of-tick and id:0c86's executor-no-own-RED detector fold in HERE (one branch/tier read, not three parallel hooks). 077d supplies the L2 role-boundary/provenance checks that **plug into the id:7a05 git-hook framework** (L0 install + L1 dispatch), rather than building its own install path. <!-- id:077d -->
