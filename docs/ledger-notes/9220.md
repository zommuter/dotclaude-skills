# id:9220

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

Each inline `python3 -c` heredoc now unquotes the captured name itself (id:02fe); they sit in shell heredocs with no import path, which is why the fix went in place rather than into a module. `lib-repo-section.sh` is the writer-side equivalent that DOES exist. <!-- relates:02fe --> <!-- id:9220 -->
