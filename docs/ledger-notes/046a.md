# id:046a

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(user 2026-07-11, after the `/relay human --all` clutter finding: 1191 closed `[x]` items had accumulated — 830 in ROADMAP, never auto-archived at all; TODO's `archive-done.sh` is age-gated + trailing-`on YYYY-MM-DD`-date-dependent + only-runs-under-interactive-`todo-update`, so most relay-closed items never qualified). GATED on the twin-safe `relay/scripts/archive-closed.sh` (id:046a sibling build) landing + a human review of its `--dry-run` output across repos. Then: (1) wire it as a **`/relay review` sub-step** (and/or a systemd-user timer like `relay-gap-sample`) so closed items don't re-accumulate — explicitly **NOT** a mandatory per-prompt step (per-prompt-ctx-budget + observe-before-preventing heuristics argue against another post-prompt hook); (2) make the auto-archive **less conservative** than `archive-done.sh` — drop the ≥30-day age gate + the trailing-date-format dependency, cover BOTH ledgers, and archive relay-closed items regardless of the interactive-session requirement (keep only the twin-safe rule as the real guard). Decide whether `archive-done.sh` is then subsumed/retired or kept as the TODO-only lightweight path. — **DECIDED 2026-07-11 (meeting 2026-07-11-1239-ledger-hygiene-inbox-dashboard, D6):** ONE `systemd --user` timer regenerates the dashboard + runs less-conservative twin-safe `archive-closed` (drop age-gate + trailing-date, both ledgers); pool-safe via `claim.sh peek` + clean-tree gate **with bounded-deferral escalation** — after K consecutive skips, `claim.sh acquire` so a persistently-claimed repo can't starve. — **⚑ HALF-LAPSED, VERIFIED 2026-08-26.** Gate = *"`archive-closed.sh` (sibling build) landing **AND** a human review of its `--dry-run` output across repos"*. **Build half DISCHARGED:** `relay/scripts/archive-closed.sh` exists, 13751 B, executable, and is still being maintained (`git log -- relay/scripts/archive-closed.sh` → `4e11fcd9`). **Human-review half NOT verifiable** — it is an owner action with no artefact to check, so it remains the sole outstanding condition. Concretely: run `relay/scripts/archive-closed.sh --dry-run` across repos and eyeball it; nothing else blocks this item. — **⚠ THE 2026-08-26 VERDICT RECORDED ABOVE IS REFUTED. Read this before acting on any of it.** The claim *"the twin-safety rule that makes it safe is the same rule that makes it ineffective"* — and the "113 closed / 18 qualified / 16%" figure it rested on — was a **MEASUREMENT ARTEFACT of my own making**, not a property of the tool. A categorisation of every top-level `[x]` item across all 6 repos × 3 ledgers found the twin-open protection fired **ZERO times**, no-id items are archivable by design, heading rules held back nothing, and there are **zero nested `[x]` checkboxes fleet-wide** so no container case exists. Every genuine closed item qualified: a **100% qualification rate**. loderite's 113 were **110 already-archived STUBS + 3 genuine items**, and all 3 were archived. **PART (2) IS THEREFORE CLOSED AS REFUTED — there is nothing to relax.** Both candidate relaxations were examined and rejected on their merits, not merely for lack of instances: *"archive when the twin is closed OR the twin's own line is itself archivable"* is **vacuous by construction** (the guard fires only when the twin is an OPEN bullet, and an open bullet is never archivable, so the added disjunct is unsatisfiable exactly when it could apply); and archiving an open parent with all-closed children would assert a done-ness the ledger does not record — that is `children:` closure (`id:46f6`) and an owner ticking decision, not an archiving one. **WHAT THE INVESTIGATION FOUND INSTEAD — a real defect, now fixed in `cba2e778`:** `archive-closed.sh:77` was a divergent copy of `roadmap-archive.sh:111` that DROPPED the `.*` between the id marker and the stub suffix, the exact character the sibling documents as load-bearing (cartulary 2026-08-14, `routed:4a12`). Any stub with prose after its marker read as un-stubbed and was **re-archived every run, unboundedly**. My own real run duplicated ids in `ROADMAP.archive.md` 3→8 here and 1→16 in loderite — ~24 stubs, ~62 KB. Reverted and re-run corrected (306 items vs 329; the ~23 delta was pure spurious re-archiving). **PART (1) IS RE-SCOPED TO `REVIEW_ME` ONLY.** Measured relief by ledger: **REVIEW_ME 30.3–91.0%**; TODO 2.7–56.1%; **ROADMAP −0.5% to +3.0% — NET-NEGATIVE for two repos** (mathematical-writing −0.2%, yinyang-puzzle −0.5%), because the `id:cd9c` stub reproduces the header line verbatim, so for a single-line item the stub is LONGER than what it replaced. **ROADMAP.md structurally cannot shrink via archiving** — do not re-derive this. And the bloat this item was opened for is not archivable at all: dotclaude-skills' `TODO.md` is **96.8% OPEN items** (1,092,275 of 1,128,256 bytes across 612 of them), loderite's is 90.8% — so `id:35b7`'s prompt-size trap is **not an archiving problem** and no wiring here will fix it. **Open residue:** the id-less stub case (1 live instance, yinyang-puzzle `arc_etch`) is re-archived every run by BOTH scripts because `STUB_LINE_RE` requires an id marker; fixing it needs a coordinated grammar change across both scripts on a surface with an open owner decision (`id:2ab3`) — flagged, deliberately not touched. **SCOPE GREW 2026-08-27: a THIRD archiver now sits unwired behind this same gap.** `relay/scripts/relay-log-archive.sh` landed today (commit `696dec67`, `id:502f` half C) — the RELAY_LOG.md rotator that never existed. It is built, tested (10 assertions) and green in a 512-pass suite, and **`grep -c relay-log-archive` returns 0 in BOTH `relay/scripts/relay-loop.js` and `relay/SKILL.md`** — the only reference anywhere is the Makefile install manifest. That is the built-green-but-unreferenced shape exactly, so it should be wired by THIS item rather than a new one. Note ROADMAP.md:1300 already anticipated the failure mode for the ROADMAP archive — *"the archive is a decaying mitigation (it re-fattens without wiring)"* — and RELAY_LOG re-fattens FASTER, because every relay round appends to it unconditionally whereas ROADMAP only grows when items close. **Measured on a COPY of loderite's real log** (never the live file): 600 of 998 entries rotate, 1,252,525 → 431,343 B (−65.6%, −205,295 tok); recombining archive+live returns exactly 998 entries; a second run moves 0. **Do NOT read that as fixing `id:502f`** — loderite still estimates ~315,251 tok against a 100,000 budget AFTER rotation, so the rotator is necessary-not-sufficient; the review-shaped slice (`docs/design-review-slice-502f.md`) is the other half and is itself gated on an owner re-derivation of `FIXED_OVERHEAD_TOKENS`. **WIRING SURVEY 2026-08-27 — the premise is CONFIRMED with hard evidence, and the asymmetry is now pinned.** Attempted to wire `relay-log-archive.sh` and STOPPED rather than invent a trigger; the survey is the useful output. Verified independently (`grep` over `*.js`/`*.mjs`/`*.sh`, excluding tests, worktrees and each script's own file): **`roadmap-archive.sh` IS wired — exactly one call site, `relay/scripts/integrate.sh` step 6b (`:336` resolve, `:769` executable-guard, `:771` invoke-or-handback, `:778` scoped-commit-or-handback), with its own exit code `EX_ARCHIVE=32` (`:232`). **`archive-closed.sh` has NO call site at all** — its only four code references are two comments (`relay-log-archive.sh:51`, `lib-ledger-only-diff.sh:31`) and two REMEDY STRINGS (the `fix:` field of `countedLedgersFor` in `relay-loop.js:2903` and `prompt-size-gate.mjs:270`), i.e. text telling an operator to run it by hand. So of the three ledger archivers, ONE is wired and TWO are not (`archive-closed.sh` for REVIEW_ME.md, `relay-log-archive.sh` for RELAY_LOG.md). **The single existing wiring pattern lives in `integrate.sh`**, which means this item's build is gated on that file — and `integrate.sh` is currently contended (`id:99b7(a)` and `id:2c2a` both rewrite it, and `id:2c2a` rewrites its EXIT-CODE contract, which is exactly what step 6b's `EX_ARCHIVE` participates in). **Sequence this item AFTER 99b7(a) and 2c2a rather than in parallel.** Note the two unwired archivers cover precisely the two REVIEW-ONLY ledgers (`id:7c5f`'s pair) — the ones only a review unit is charged for, and reviews are the verdict currently blocked by `id:502f`. **WIRED + LANDED 2026-08-27 (`7eb948f3`), suite 518/0/1 — but this item is NOT fully closed, see below.** `archive-closed.sh` → step 6c (`EX_REVIEW_ARCHIVE=36`), `relay-log-archive.sh` → step 6d (`EX_LOG_ARCHIVE=37`), both on the step-6b pattern and both through `handback()` at exit 0 per `id:2c2a`; 19 `EX_*` codes, all distinct. **`archive-closed.sh` was deliberately NOT wired bare:** it archives TODO+ROADMAP+REVIEW_ME, so a bare call would run a SECOND archiver over `ROADMAP.md` one step after `roadmap-archive.sh` rotated it — the `id:fdc4` double-archiver collision — and would take `TODO.md` from its own owner-facing path. It gained an additive `--only todo|roadmap|review_me` (default `all`, every existing caller unchanged; twin state still built from BOTH ledgers so scoping cannot turn a twin-open skip into an archive) and only `review_me` is wired. **`relay-log-archive.sh` wired with no extra gate, after a fleet-wide verification rather than on the wiring argument alone:** a read-only `--dry-run` across all 56 own repos shows **12 rotating**, all proportionate (loderite 600/998 entries, dotclaude-skills 554, the rest 27–142) and **40 below the 500-line floor as byte-exact no-ops**; content conservation was measured earlier on loderite's real log (998 in, 398+600 out, idempotent). The 30-day age gate makes a rotation/concurrent-append race on the SAME entry impossible by construction — which is what the `merge=union` hazard actually is — and under `id:2c2a` an archive failure is now a clean landed-but-unfinished `partial=`, not a fleet-wide ✗. **STILL OPEN:** this item's original scope also covered *relaxing the conservatism* of the archivers, which was NOT done; and `roadmap-archive.sh` remains the only archiver whose wiring predates today. Re-scope or split before ticking. <!-- id:046a -->
