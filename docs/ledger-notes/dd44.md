# id:dd44

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

-- 3 instances in one day (gate scoring a vanished violation as a win; loderite's hand-maintained token census; your lane check borrowing the shrinker's own regex, id:4983); distinct from the measurement-artefact class <!-- id:dd44 -->

## Edit 2026-09-10 (`/relay human`, tier-(a)) -- one instance CORRECTED

**This note has been EDITED, so the header's "reproduced verbatim" no longer holds for the
section below this line.** Nothing above was deleted or reworded; this section is additive.

The third instance named in the "From TODO" claim above -- *"your lane check borrowing the
shrinker's own regex, id:4983"* -- **does not hold for the landed test**, re-derived by reading
the file rather than by adopting the REVIEW_ME box that first reported it:

- `tests/test_lane_grammar_ssot.sh` exports `SSOT_DOC=relay/references/hard-lanes.md` and scrapes
  that SSOT doc for the EXPECTED lane set.
- It imports `_LANE_PATTERNS` from `tools/ledger-shrink.py` only as the SUBJECT under test.

Expectation and subject are therefore different sources, and the test genuinely reddened against
the pre-fix implementation -- which a self-referential check could not have done.

**What survives, and is what `dd44` should be filed on:** the doc SCRAPER's own `DASH_RE` uses the
permissive `[A-Za-z0-9 _./-]+` bracket shape the fix removed from the shrinker, so a lane the SSOT
declares in an unusual spelling would be invisible to BOTH the scraper and the consumer, and
assertion `(a)` only guards against total vacuity. That is a COVERAGE limit, not a tautology --
a materially narrower claim than "a checker that cannot fail".

Re-check: `grep -n 'SSOT_DOC=\|_LANE_PATTERNS' tests/test_lane_grammar_ssot.sh`.
