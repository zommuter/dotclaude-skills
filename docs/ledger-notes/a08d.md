# id:a08d

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(drafted 2026-08-27, `children-of:4a5c`; covers the derived index `id:dcf3` AND Forgejo `id:016e` together, because two separate widenings of one pre-registration is exactly how a fail condition quietly stops meaning anything). **What the pre-registration says today, verbatim** (`docs/meeting-notes/2026-08-10-0906-tracker-substrate-replacing-markdown-ledgers.md`, "Pre-registered fail condition"): *"Window: 4 weeks, starting at the first successful full import. Comparator (control arm): the cheap read-only board over `classify-repo.sh` output is built as part of the experiment, not held as insurance. Without it the measurement is tracker-vs-nothing and reports the value of a dashboard rather than of this substrate. Measure: a concrete usage proxy, not self-report. Fail => the migration is unearned; fall back to the control-arm board + a constrained-SQLite `id:2840` index (`id:659c` / `routed:1e99`, kept open for exactly this reason)."* Its Fable finding 9, also ratified, binds the shape: *"two resident boards halve the chance either becomes the habit"* -- one board is RESIDENT for the behavioural window, the other is a one-week fidelity spot-check. **THE QUESTION, asked once:** does the pilot admit additional candidate arms at all, and if so which. Three dispositions are proposed, and they stand or fall as one ruling. **(i) Derived index (`id:dcf3`) -- RECOMMEND ADMIT, in the narrow form only.** It is not a new candidate: it is the FALLBACK the pre-registration already names in its last clause (cartulary's ratified text-is-truth-plus-derived-index design, its D2 and A14), measured DURING the window instead of only on the fail branch. Narrow form means non-resident and on-demand, with its usage proxy reported as a separate row and never folded into the habit-formation measure the resident board owns. **(ii) Forgejo (`id:016e`) -- RECOMMEND SCOPE ONLY, do NOT admit as a measured arm on this ruling.** An arm under `id:90f2`'s contract is a live instance plus an adapter plus a live-verified run; the owner has explicitly barred building or standing up either, and the three questions that would decide it are claims about what a system ENFORCES, which the fleet's own rule says cannot be produced from documentation. Admitting it as an arm now would open a gate on an assumption. It is admitted as a SCOPING deliverable that may become an arm on a later, separate ruling. **(iii) If the owner wants EITHER addition to be RESIDENT during the behavioural window -- RECOMMEND AGAINST AMENDING AT ALL, and decline both.** Finding 9 is the constraint that a third or fourth resident board genuinely breaks; nothing in this proposal buys it back. **GROUNDS (new evidence, not a new preference):** (1) an ad-hoc derived index built in ~40 lines this session ranked every open human-decision item by transitive gate fan-out across 56 repos with no server, and answered a question neither tracker had been asked, so the fallback's capability is no longer a hypothesis -- it is now `tracker/derived-index.py`, tested, and measured 93 gate edges / 26 stale / 6 unresolved on this repo alone; (2) the pilot CLOCK HAS NOT STARTED (`id:fddb`, gated on `id:695d`/`id:cb22`), so this is a PRE-START amendment and no measurement is invalidated -- had the clock been running, the honest answer would have been no; (3) the two-arm framing was ALREADY amended to three by the owner on 2026-08-11 when Beads was admitted as a full arm (`id:4ac9`), so widening is precedent-following, not novel; (4) Forgejo is a cost axis neither incumbent has -- already-paid-for infrastructure in a live engagement, with a native repo+issue identity that maps onto the schema's `(repo,id)` composite key instead of a synthetic project mapping. **COST:** the derived-index arm's code exists, so its eval cost is one more report under `id:90f2`'s equivalence contract (`id:e1f3` scopes it) plus one more usage-proxy row -- roughly the marginal cost of the Beads arm. Forgejo as scoped is a bounded probe against an instance the owner already runs, and as a full arm would cost a fourth adapter plus a live run, i.e. the same as Beads again. **HONEST WEAKNESSES, stated because this is a recommendation and not a decision:** (a) finding 9 cuts against admitting anything, and the narrow non-resident form is a mitigation, not a refutation -- a reader who thinks habit formation is the whole point should decline; (b) each added arm makes the 4-week window carry more comparisons on the same fixed evidence, which weakens every pairwise claim even when no single arm is unreasonable; (c) the agent drafting this also wrote the derived-index arm it recommends admitting, which is a conflict of interest the owner should price in; (d) FORGEJO'S THREE OPEN QUESTIONS ARE NOT ANSWERED HERE AND WERE NOT GUESSED -- whether its API expresses `blocked_by` dependency edges that survive round-trip, whether it can carry the per-view `todo_status`/`roadmap_status`/`review_status` split that `id:857d` binds or forces one status field, and whether a scoped token writes labels and relations without the JWT fallback that makes `id:46d5` an unattended-timer blocker for Vikunja. A forge issue model is open/closed, i.e. binary, which makes the `id:857d` split the likeliest failure point and the same label-laundering hazard Plane already hit -- but that is a HYPOTHESIS to probe, not a finding, and it is recorded as one. Recorded per the delegated-verdict rule: evidence plus a recommendation, ratification is the owner's. <!-- children-of:4a5c --> **OWNER RATIFIED 2026-08-27: BOTH ADMITTED AS FULL MEASURED ARMS.** This OVERRIDES the drafted recommendation, which proposed disposition (i)+(ii): derived index in the narrow non-resident form, Forgejo as a scoping deliverable only. The owner chose the widest option with the proposal's weaknesses in front of him. Recorded as his ruling, not as the agent's conclusion, per the delegated-verdict rule. **WHAT THIS NOW COMMITS TO:** the derived index (`id:dcf3`) is a measured arm under `id:90f2`'s contract with its own usage-proxy row; Forgejo (`id:016e`) is a measured arm, which by that same contract requires a live instance, an adapter, and a live-verified run, so the earlier bar on building or standing one up is lifted for the pilot's purposes. **TWO THINGS THE RATIFICATION DOES NOT YET SETTLE, and they must not be assumed:** (1) RESIDENCY. Fable finding 9 (*"two resident boards halve the chance either becomes the habit"*) constrains RESIDENT BOARDS, not arms, so admitting four arms is not by itself a finding-9 violation. But if any new arm is made resident during the behavioural window, the drafted disposition (iii) says the honest answer flips to declining. Recorded DEFAULT pending an explicit owner call: **new arms are measured but NOT resident**; the two resident boards stay as finding 9 fixed them. (2) WHICH FORGEJO INSTANCE. The owner runs Forgejo for a paying customer engagement; whether the pilot may point at that instance, or needs its own, is unanswered and is not a detail -- pointing a fleet experiment at customer infrastructure is a decision with consequences beyond this repo. **STILL UNANSWERED, and deliberately not guessed** (they are claims about what a system ENFORCES, unobtainable from a doc read): whether Forgejo's API expresses `blocked_by` edges that survive schema round-trip; whether it carries the per-view `todo_status`/`roadmap_status`/`review_status` split `id:857d` binds, or forces one status field (a forge issue model is binary open/closed, so this is the likeliest failure point); and whether a scoped token writes labels and relations without the JWT fallback that makes `id:46d5` an unattended-timer blocker for Vikunja. The pilot CLOCK has still not started (`id:fddb` open, gated on `695d`/`cb22`), which is what keeps this a PRE-START amendment with no measurement retro-fitted. <!-- id:a08d -->
