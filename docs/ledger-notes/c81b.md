# id:c81b

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(surfaced 2026-09-01; owner deferred the decision pending a measurement). `tracker/SCHEMA.md:262` and `lib-typed-edges.sh` agree that the ONLY cross-repo link form is `routed:`, which resolves by BARE TOKEN; `SCHEMA.md:92` then makes a `routed:` token existing in >=2 repos a class-B **FATAL, "never downgradable, listed or not"**. Consequence: filing the owner-ratified S3 edges for `3346` (dotclaude-skills/meeting-rpg, both OPEN) and `6ab8` (mathematical-writing/toesnail, both OPEN) would MANUFACTURE two new class-B fatals. This also reframes `c3f7` -- not carelessness to patch, but the same well-intentioned link filed on a colliding token (`loderite` and `dotclaude-skills` both genuinely own `id:c3f7`; the `yinyang-puzzle` leg is a phantom). **Four directions, none costed yet:** (A) qualify `routed:` (`routed:meeting-rpg/3346`) with bare tokens grandfathered as "resolve if unique, fatal if not", so NO fleet migration is needed -- touches `append.sh` twin-check, `scan-routed.sh`, `lib-typed-edges.sh`, `ledger-map.py`, the inbox lint; (B) a disambiguation table (guards rather than dissolves, grows forever, same shape as the allow-list); (C) tracker-level links only, keyed by uid (dissolves it, but breaks markdown-is-SSOT for links and the link then lives only in a pilot substrate that may be discarded); (D) mint a fresh non-colliding id whenever a link is needed (no tooling change, but taxes every future link). **BLOCKING INPUT, agreed with the owner:** count how many cross-repo links ACTUALLY collide (existing `routed:` edges whose token is owned in >=2 repos) versus LATENT demand (twins we want to link but cannot), with a denominator and a trend -- collisions are a birthday problem in a 65,536-token space and the fleet grew 5,307→6,370 items in two weeks, so a static count flatters (D). If the population is a handful, (D) wins and no meeting is needed; if it is dozens and growing, (A). Do the measurement FIRST. — **MEASUREMENT DONE 2026-09-01 (the blocking input; run over the relay.toml own-set via `tracker/fleet-import.sh --emit-unvalidated`, i.e. the existing census tooling, not a fresh sweep). NOT a decision -- the numbers land between the two branches the owner named, so the choice is still his.** Denominator: 56 repos, 6,438 items, 4,667 id-bearing, 4,525 distinct tokens, 199 `routed:` edge instances over 142 distinct tokens. **ACTUAL collisions = 1** -- exactly `c3f7` (dotclaude-skills/loderite/yinyang-puzzle), the sole class-B error in the fleet; 0.70% of distinct routed tokens. **LATENT demand = 2** -- the ratified S3 edges `3346` (dotclaude-skills/meeting-rpg) and `6ab8` (mathematical-writing/toesnail). Total live population = **3**. The wider collision universe is 137 colliding tokens (3.03% of tokens), of which the adjudication worksheet put only 15 in section A (`needs a look`) and 63 in section B (`birthday collision, no signal`) -- so most collisions carry no link demand at all. **TREND (this is why the count alone does not settle it):** the process is confirmed birthday-uniform -- observed 137 colliding tokens against a model expectation of 158 at N=4,667 over a 65,536 space -- so collisions grow QUADRATICALLY in fleet size while link demand grows linearly with them. Holding the measured link density (3.14% of tokens carry a `routed:` edge) constant, expected links-on-colliding-tokens goes 5.0 today -> 10.9 at +50% -> 19.0 at 2x -> 40.8 at 3x. The fleet grew 5,307 -> 6,370 -> 6,438 items in ~two weeks (~+550/wk), which puts 2x roughly a quarter out. **So the item's own pre-registered test splits:** `a handful` is true TODAY (3), and `dozens and growing` is true WITHIN ~A QUARTER (~19). Recommendation, weaknesses stated: take **(D) fresh ids now** to unblock the two S3 edges and `c3f7` immediately at zero tooling cost, and treat (A) as the pre-registered successor whose trigger is the measured links-on-colliding count reaching ~15 -- re-run this same census to check. The weakness of that split is real: (D) taxes every future link and each fresh id is itself an id that can collide, so deferring to (A) means paying the migration later on a larger corpus, and a pre-registration that is never re-run is a registration that never fires. **Owner's call; nothing here is settled.** Census artifacts were written outside any repo (they quote private-repo titles). — **OWNER RULED 2026-09-01, on the measurement above: take (D) fresh ids NOW, and PRE-REGISTER (A) with a trigger of ~15 links-on-colliding-tokens, re-run of this same census being the check.** Unblocks the two ratified S3 edges (`3346`, `6ab8`) and `c3f7` immediately at zero tooling cost. **The weaknesses were on the table when this was decided and are not dissolved by it:** (D) taxes every future link; each fresh id can itself collide; deferring (A) means paying its five-consumer migration later on a LARGER corpus; and a pre-registration nobody re-runs never fires -- a failure mode this fleet has hit before, which is exactly why the trigger is a NUMBER with a named command rather than a judgement call. **Whoever re-runs the census: the check is `tracker/fleet-import.sh --emit-unvalidated`, and the trigger quantity is distinct-routed-tokens-that-also-collide, measured 1 on 2026-09-01 against an expected 5.0.** — **RULING (D) EXECUTED 2026-09-01, with the local half DONE and MEASURED.** Re-minted this repo's side of both collisions it could dissolve locally: `3346` -> `113e` (the S3 twin with `meeting-rpg`) and the archived `c3f7` -> `4283`. **The ratified S3 edge is FILED at last** -- `113e` now carries a `routed:3346` edge, which resolves UNIQUELY to meeting-rpg because this side no longer owns the token. MEASURED on the live fleet after committing: class-A errors **7 -> 6** (`3346` gone), and dotclaude-skills is **out of the `c3f7` class-B entirely** (it now reads `['loderite','yinyang-puzzle']`). Verified before re-minting that NO child or gate edge referenced either token and that both fresh ids were free locally AND fleet-wide. **THREE CROSS-REPO HALVES ROUTED, not actioned** (this session edits no other repo): `routed:cef7` meeting-rpg (add the reciprocal edge back to `113e`), `routed:985f` mathematical-writing/toesnail (`6ab8` -- neither side is this repo, so someone must choose which re-mints), `routed:b4d7` loderite/yinyang-puzzle (`c3f7`). **A PREMISE IN THIS ITEM IS VERIFIED FALSE:** it says *the `yinyang-puzzle` leg is a phantom*. It is NOT -- yinyang-puzzle owns an anchored id marker for `c3f7` on an OPEN item at its `TODO.md:25` (the round-1 hosting item). So `c3f7` was a genuine THREE-repo collision, not two-plus-a-phantom, and removing this repo's leg leaves a real two-repo one. **Also noticed, unresolved:** `truncocraft` carries 2 `c3f7` markers yet the fleet census does not flag it -- it is in `relay.toml` but evidently not contributing items (paused, or not classified own); worth a look, because a census that silently omits a repo UNDERSTATES every collision count in this item, including the ones the (A) trigger will be measured against. **The (A) PRE-REGISTRATION IS UNAFFECTED and still stands:** re-run `tracker/fleet-import.sh --emit-unvalidated` and act when distinct-routed-tokens-that-also-collide reaches ~15; it measured 1 on 2026-09-01. — **CORRECTION 2026-09-01 to this item's own `truncocraft` note, which was MINE and was WRONG.** I recorded that *`truncocraft` carries 2 `c3f7` markers yet the fleet census does not flag it ... a census that silently omits a repo UNDERSTATES every collision count*. That concern does not exist. `~/src/truncocraft` is a **SYMLINK to `~/src/loderite`**, created 2026-07-01 when the repo was RENAMED (`id:2849`, recorded in `relay.toml`'s own comment, which maps the entry to the `~/src/loderite` basename). So the 2 markers I counted were loderite's, counted a second time through a compatibility shim -- the census is CORRECT to report one repo, and there is nothing to clean up. The lesson generalises past this token: a `~/src/*` directory glob DOUBLE-COUNTS renamed repos through their compatibility symlinks, which is exactly why `relay.toml` (honouring `# path:`) is the SSOT for the repo set and a glob is banned (`id:7633`). I used a glob for a quick cross-check and it produced a phantom finding within minutes -- the ban is not ceremonial. **No change to the (A) pre-registration**, whose trigger quantity was measured from the census, not from the glob. <!-- id:c81b -->
