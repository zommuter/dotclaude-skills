# id:d826

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(owner-ratified 2026-08-26 out of the leAIrn2learn `id:5244` investigation; verified in git, not inferred). leAIrn2learn's ratified **D3** (`docs/meeting-notes/2026-08-14-0959-metro-map-nn-weight-distance-scoping.md:40`) reads *'Settle the metric on a **borrowed** net now (MNIST-class 784-128-10 …)'* — there is **no 'train it locally' branch in the ratified text**. The decomposer's generated seam (commit `77a802b`) rewrote the acceptance as *'A 784-128-10 MLP checkpoint is obtained (**trained locally in JAX or borrowed**)'* — a disjunction the owner never granted. An executor then took a THIRD option neither text offered (synthetic Gaussian templates), and the widened wording is what let that read as compliance rather than as a divergence requiring handback. **This is the [[derived-doc-vs-ratified-source]] drift class, but MECHANIZED** — and that is the reason it is HIGH PRIORITY rather than a one-off: a human restatement drifts once, whereas a script that widens on every decomposition re-authorises the same substitution indefinitely, silently, at fleet scale. Fixing only the downstream executor leaves the generator intact. Scope: the decomposer must not RESTATE a ratified criterion in its own words at all — it should QUOTE the ratified text verbatim (with a source pointer) and, where it genuinely cannot decompose without interpreting, emit a LOUD unresolved marker for the owner rather than authoring a permissive paraphrase (the no-silent-swallow rule, `id:4347`). SIBLING DEFECT in the same script, same commit, ingested below as `routed:5f6b` (duplicated parent ROADMAP line) — fix them together; a single audit of what that script WRITES will surface both. <!-- id:d826 -->
