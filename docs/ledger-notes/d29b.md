# id:d29b

Authored directly, not relocated by `tools/ledger-shrink.py`. Conventions:
`docs/ledger-notes/README.md`.

**EDITED 2026-09-08 (`/relay human`), declared per the notes-are-editable rule.** The
`## Re-measured` section below was APPENDED; the original `## From TODO` text is unchanged
and still reads as first written, including the parts this session refutes. Nothing was
removed.

## From TODO

(a) The unpushed-range rewrite (`git filter-repo --replace-text ... --refs <pub>..HEAD --partial --force`) was DENIED by the destructive-git guard and deliberately not routed around; backup tag `pre-censor-backup-20260905` is on private origin. After it runs the four relay-ckpt SHAs change, so the ratification entries need `ratify-queue.sh retire --reason`. (b) The already-published half needs a force-push over PUBLISHED history, which cannot retract -- GitHub keeps unreferenced objects and clones keep the old tree. OWNER DECISION, not taken. Detail: `docs/HANDOVER-2026-09-05-1301.md` sections 1, 2 and 6. NEVER censor a 4-hex token without checking whether it anchors a LOCAL item -- 187 adjudicated homonyms exist and `55c7` is one.

## Re-measured 2026-09-08 (`/relay human`)

**Half (a) is MOOT.** The three commit messages the section above names are ON the public
remote -- verified per-sha with `git merge-base --is-ancestor <sha> github/main`, all three
yes. They were pushed in a 43-commit batch AFTER `docs/HANDOVER-2026-09-05-1301.md` recorded
that the remote must stay untouched. An unpushed-range rewrite can no longer reach them, so
the `filter-repo` command above is spent. The four relay-ckpt SHAs it would have changed were
instead RESOLVED normally (all 23 queue entries drained this session), so the
`ratify-queue.sh retire --reason` path it anticipated was never needed.

**Half (b) is unchanged and still the owner's, not taken.** A force-push over published
history reduces discoverability; it cannot retract. GitHub keeps unreferenced objects; clones
and forks keep the old tree.

**The section-2 table UNDER-COUNTS by roughly an order of magnitude.** A fresh sweep of the
public tree found the exposure to be about 10x the per-token figures recorded there, across
about 6.5x as many files. Direction matters for the half-(b) decision: the audit's numbers
make the exposure look small enough to erase, and it is not. **Per-token specifics stay OUT
of this PUBLIC file** (private memory `privacy-scan`); what belongs here is only that the
recorded figures are not the real ones -- re-measure before deciding, do not reuse the table.

**Sensitivity, however, is LOWER than the table implies.** Checked the sibling's own product
vocabulary against the public tree: zero hits, every term. What leaked is process metadata
(ledger sizes, internal tool names and their known bugs, item id tokens, repo topology), not
product or design content and not source. The owner-directed scope was "item titles and
internals OUT, the bare project NAME stays"; the titles half held, the internals half did not.

**A structural argument against a broad scrub**, the same one that spared `55c7`: roughly a
third of the carrying files are `tests/`, `relay/scripts/` and `tools/`, where the sibling is
cited in load-bearing comments as the motivating fleet case. Scrubbing those breaks our own
provenance trail.

**Follow-on:** `id:6740` -- handover docs are now gitignored and untracked (9201732b), which
stops further publication but leaves them with no version control at all.

## From TODO

-- detail: `docs/ledger-notes/d29b.md` -- **HALF (a) IS MOOT, verified 2026-09-08 (/relay human):** the three commit messages this item names (`4d9dc0e3`, `7cd3952d`, `c17a7447`) are ON the public remote -- `merge-base --is-ancestor` says yes for all three -- pushed in a 43-commit batch after `docs/HANDOVER-2026-09-05-1301.md` recorded that github must stay untouched. The unpushed-range rewrite can no longer remove them. Only half (b), the PUBLISHED-history rewrite, remains, and it is unchanged: a force-push cannot retract. Box left OPEN -- owner's call, not taken. <!-- id:d29b -->
