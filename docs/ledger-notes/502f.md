# id:502f

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(observed run `relay-20260827-113009-14765`, 2026-08-27, BOTH rounds). The prompt-size gate (`id:4f9b`/`id:b018`) refused to dispatch a `review` child for **`loderite` at ~520,546 tok** and **`it-infra` at ~155,393 tok**, against a **100,000 tok** budget — loderite is **5.2× over**. Both were counted as REPEAT handbacks with `repeatHandbackKind: "queue-exhausted"`: the pool tried, exhausted its options, and gave up in both rounds. **The gate is behaving CORRECTLY** — it is refusing to dispatch a child that would die of `Prompt is too long`, which is exactly the class `id:5eeb` exists for, and the refusal is loud rather than silent. **The defect is that a correctly-refused review is still an UN-REVIEWED repo, indefinitely** — this is the [[relay-builtgreen-but-unreferenced]] shape's sibling: a guard doing its job while the underlying condition goes unaddressed forever, and it is the third instance of that pattern found on 2026-08-27 alone. Consequence: unaudited commits accumulate in both repos with no anti-gaming pass ever running; loderite in particular carries the `id:5adb` salvage work documented this same day. **NOT diagnosed** — do not assume the cause. Deliberately unmeasured so far: what actually inflates loderite's prompt to 520k. Candidates to check BEFORE choosing a fix: the `ledger-slice.sh` path (is it running at all for a `review` unit, or is the whole ledger being sized as in the 2026-08-21 `id:35b7` incident where a stale proxy made the slice hop 404 and the gate sized ~315,704 tok of whole ledgers); loderite's own `TODO.md`/`ROADMAP.md` size; and whether the review prompt assembles a diff-since-checkpoint that has grown unbounded because the repo has not been reviewed in a long time — which would be self-reinforcing, since each un-run review makes the next prompt larger. Note the same ledger-size theme already bit THIS repo from a different direction (`id:b4dd`: the privacy gate took >9 min on a 927 KB `TODO.md` + 281 KB `ROADMAP.md`). Measure first, then decide between slicing the review input, chunking the review into multiple children, or raising the budget for a review verdict specifically. **MEASURED 2026-08-27 (measurement only — the FIX is undecided and is the owner's call).** The estimator is `est = round((ROADMAP+TODO+REVIEW_ME+RELAY_LOG bytes)/4) + 12000` (`CHARS_PER_TOKEN=4`, `FIXED_OVERHEAD_TOKENS=12000`, `relay-loop.js:2977-2979`; the review-only REVIEW_ME+RELAY_LOG pair is added by `countedLedgersFor` id:7c5f). It REPRODUCES both observed numbers to the token off a common ~6,470-char prompt, so nothing here is inferred: **loderite** RELAY_LOG 1,252,525 B (313,131 tok, **60%**) + TODO 381,105 B (95,276) + ROADMAP 361,208 B (90,302) + REVIEW_ME 32,876 B (8,219) + 13,618 overhead = **520,546 tok** ✓; **it-infra** TODO 410,266 B (102,567 tok, **66%**, over budget ON ITS OWN) + RELAY_LOG 115,702 B (28,926) + ROADMAP 29,757 B (7,439) + REVIEW_ME 11,376 B (2,844) + 13,618 = **155,393 tok** ✓. **ROOT CAUSE — a review unit is STRUCTURALLY unsliceable, so `ledger-slice.sh` never runs for it.** Not the id:35b7 shape (a slice hop that 404'd): the slicer is ITEM-KEYED (`--id <4-hex>`), and `dispatchItemFor = inject_item || actionable_routine_ids[0] || ''` (`relay-loop.js:2853`) is fed by the EXECUTE-lane id list, which a review unit carries none of ⇒ `item=''` ⇒ `sliceLedgerForUnit` returns at `relay-loop.js:3962` `if (!item) return null`. **This is the one early-return on that path that does NOT log** — every other failure branch logs a reason — so the gate's own REMEDY text ("find why ledger-slice.sh produced no `slice_path` for it, the relay-loop log records the reason") sends the operator to a log line that cannot exist. **Two corrections to this item's own hypotheses:** (1) the unbounded-growth guess was right but the driver is NOT the diff-since-checkpoint — it is **RELAY_LOG.md**, append-only `merge=union` with **NO archiver anywhere in `relay/scripts/`** (the code says so verbatim at `relay-loop.js:2901`), 15,188 lines and growing every run, hence genuinely self-reinforcing; (2) this is **NOT loderite/it-infra-specific** — EVERY review unit in EVERY repo is sized on whole ledgers; these two are merely the first to cross 100k, so the rest are "not yet", not "not affected". **DECISIVE: no combination of the offered remedies can fix loderite, even in the limit.** Measured archivable mass is TODO **2.0%** (7,581 of 381,105 B; 184 open / 5 closed) and ROADMAP **24.9%** (88,925 B). Deleting RELAY_LOG.md ENTIRELY and archiving every closed item in all three other ledgers still leaves **~175,070 tok, 1.75x over budget** — and the OPEN-item mass alone (226,258 B ROADMAP + 359,488 B TODO = 146,437 tok) already exceeds the 100,000 budget with zero log and zero closed content. So archiving is a dead end here by arithmetic, not by judgement: only a **review-shaped slice** (a slicer keyed on the review's actual required-read set instead of an item id) or **chunking the review across children** can work; raising the budget cannot. Options + a recommendation are in the session summary — NOT decided here. **COUNT CORRECTION (2026-08-27, same session): this item's own title says TWO repos — it is at least THREE, and the worst is THIS repo.** dotclaude-skills measures **~550,287 tok** (TODO 1,157,933 + ROADMAP 283,294 + REVIEW_ME 28,648 + RELAY_LOG 676,801 B), i.e. **above loderite's 520,546**. It never surfaced in run `relay-20260827-113009-14765` only because that pool ran `--exclude dotclaude-skills`, so absence from the handback list is NOT evidence of health. Title left unedited deliberately (the observed-run facts it records are accurate as of that run); read this correction with it. Owner-raised ROLE question — whether a review child should read an append-only log wholesale at all — is filed SEPARATELY as `id:3a7c`, and the unlogged-early-return defect as `id:f499`; (A) review-shaped slice and (C) RELAY_LOG rotator were owner-ratified 2026-08-27 as the get-it-running pair, with `id:3a7c` as the deferred proper fix. <!-- id:502f -->
