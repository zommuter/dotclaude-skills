# id:5817

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

Observed 2026-08-20 (`/relay reconcile --all`): closing the box for parked orphan `relay/orphan/relay-20260819-134717-30389-execute-repo-0` at `REVIEW_ME.md:802` had to fall back to `Edit` + a follow-up `relay/scripts/commit-ledger.sh` commit, because `md-merge.py update-ids` keys strictly on `_ID_MARKER_RE` (`<!-- id:XXXX -->`) and the auto-generated box has none — `REVIEW_ME.md` holds only 3 id markers across ~800 lines. That is the exact Edit-bypasses-the-flock channel the CLAUDE.md tool-choice rule names (`id:93b4`), reached not by choice but for want of a key. Fix: have `relay-reconcile.sh --auto` MINT an id (`meeting/append.sh new-id`) into every box it surfaces, so the closure path is the helper. Re-checkable: `grep -c '<!-- id:' REVIEW_ME.md` rises as new boxes are surfaced, and closing one needs no `Edit`. <!-- id:5817 -->

## Appended 2026-09-09 (`/relay human`) -- SECOND live confirmation, not a rewrite

Nothing above was edited; this section is additive. The same fallback was forced again
today, on two boxes rather than one: `REVIEW_ME.md`'s auto-reconcile box for parked orphan
`relay/orphan/relay-20260908-231617-32609-execute-repo-0` carries no `<!-- id: -->` marker
at all, so ticking it needed `Edit` + `commit-ledger.sh` exactly as on 2026-08-20.

The second box (`...-execute-c655-0`, reviewer-authored) shows the failure is NOT limited to
`--auto`-generated boxes: it names `id:f272` and `id:c655` in backticked PROSE, which
`_ID_MARKER_RE` correctly refuses, so it is equally unaddressable. **And a mint alone would
not have been enough for either**: both boxes are MULTI-LINE, and `md-merge.py` anchors on
the id-bearing LINE, not the item BLOCK (`id:740a`) -- a marker on the last line still leaves
the `- [ ]` checkbox on the first line out of reach. So the fix as written above (mint an id
per surfaced box) is necessary but not sufficient; it composes with `id:740a`, and closing
`5817` without `740a` would still leave `Edit` as the only path for any wrapped box.

Re-checkable, and correcting a figure this note itself would have led a reader to reuse:
`grep -c '<!-- id:' REVIEW_ME.md` is **40** at this writing, against 56 open boxes -- not the
"only 3 id markers" the 2026-08-20 paragraph above records. That count has grown ~13x while
the DEFECT is unchanged, so the marker count is NOT the metric to close this item on; markers
are accumulating on reviewer-authored boxes while the `--auto`-generated ones still get none.
Assert instead that a NEWLY surfaced `--auto` box carries a marker, and that the marker sits
where `md-merge.py` can reach the checkbox.
