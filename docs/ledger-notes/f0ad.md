# id:f0ad

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(a) **The two tools disagree about the same anomaly.** `discover-repo.sh:132` gates the new STARVED banner on `if n > 0:` alone with NO verdict gate, while the sibling shipped the same day gates it on `run-anomaly-scan.sh:458/536` `POOL_ACTIONABLE = {execute,review,hard,handoff}`. A genuinely diverged repo gets the self-contradicting banner `STARVED (2 actionable items …, verdict=blocked) skipped because — diverged from origin …`, and `diverged` is the MOST COMMON substitutive class, so most production STARVED banners will be false positives — while `run-anomaly-scan.sh` reports nothing for that repo. Two tools disagreeing on the same run is how a signal stops being read. (b) **`run-anomaly-scan.sh` false-positives on five ordinary HEALTHY states**, all reproduced: an `--only`-scoped run (candidates come from the full own-set but `relay-loop.js:1803-1812` drops out-of-scope repos before sharding → STARVED for the whole fleet); a quota-stopped `--afk` run (the parser reads only `Skipped`/`Blocked`, never `## Queued`, where `relay-loop.js:3709/4054` puts classified-but-undispatched units); the handback accumulator (`state.handbacks` is push-only and never reset all run — a repo handed back in round 1 and integrated in round 2 keeps its Blocked row; the banked mixed-temporality class); benign per-round skips (`claimed-elsewhere`/`dirty-worktree`/`intensive`, reducing the class to "repo has >1 open item"); and an IN-FLIGHT run (advertised as supported) reporting ZERO-YIELD + DISPATCHED-BUT-NO-OUTCOME for units still running — it never reads `## In-flight` and never consults `heartbeat.sh live-runs`, which `relay-status-publish.sh:158` already uses for exactly this question. Mitigating: the script is NOT wired (zero references outside the Makefile manifest), so nothing fires today. (c) **`tests/test_park_starvation_e7e4.sh` is PARTLY VACUOUS** — reverting ONLY the `additive_surf` fold (`discover-repo.sh:170,179,203,245`) leaves 5/5 PASS across 17 related test files, because case (2) greps `surf_join` for the worktree BASENAME, which the pre-fix suppress reason also contains. The hunk IS load-bearing in one uncovered state: a planned park whose bound item is CLOSED (`reconcile-repo.sh:233` sets `suppress=false`, so the only surface entry is `parked-orphan (planned):`) — current build emits it, reverted build emits `surfaced=[]`, a dead run's parked worktree with ZERO operator trace (the 2026-07-28 phantom-park class). Assert on the marker, not the basename, and add a closed-item case. (d) **LOW cluster:** `run-anomaly-scan.sh:159-163` reports a false clean when `relay.toml` is ABSENT (only the parse-failure path is guarded; `lib-own-repos.sh:33` returns 0 with no output for a missing file → `own-repos=0 … no anomalies … exit=0`, violating the script's own header); `:240` `EVAP_RE` misses `relay-loop.js:3219`'s `integrate.sh mechanical hop failed to dispatch (…)` signature, so the class built for "the integrator never ran" does not fire on the branch where the hop was REFUSED; the STARVED prefix is uncapped (`discover-repo.sh:133-136`) and overruns `run-anomaly-scan.sh:483`'s `short(reason, n=110)`, truncating away the actual cause (6 ids → 118 chars; dotclaude-skills is at 109, one under); `integrate.sh:601-606` `no-upstream` sets `landed=1` but step 8b is gated on `defer_push`, so a NON-substantive unit in an upstream-less repo takes the success path with `ratification=none` and `relay-loop.js:3243`'s surfacing condition never fires — unpublished work silently counted as completed; and a crash window between `landed=1` (`integrate.sh:571`) and the enqueue (`:610-640`) leaves an unpushed merge with no durable record, the one outcome the design says it cannot have (ordering the enqueue BEFORE the ckpt tag closes it). ALSO from the review's mutation pass on `tests/test_run_anomaly_scan.sh`: **22 single mutations, 10 killed / 11 SURVIVED** — every FP guard survived, notably `touched = len(dispatch)` (`:546-547`) whose adjacent comment documents it as the log-gap FP fix, `start = -1` per-run scoping, and `startswith` → `in` on section headings (the `gated?` substring class). The suite's negative control (`:262-320`) contains NO repo with a live pool-actionable verdict, which is precisely why the five FPs pass it. <!-- id:f0ad -->
