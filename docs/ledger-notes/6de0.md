# id:6de0

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

Three of the four slice shapes are now MULTI-id (`--since-last-review`, and `--ids` for both hard and handoff); only `execute`'s `--id` is genuinely one block. A child told it holds one item's block, handed a 14-item survey slice, may reasonably conclude the slice is malformed and HAND BACK -- the brief also instructs it to hand back rather than open a ledger speculatively when the slice looks insufficient, so a wrong singular is a plausible needless-handback cause, not just untidy prose. Note there is an UNPINNED inline copy at `relay-loop.js:2666` that would silently diverge (the pinned byte-identical twins are `oversizeDispatchReason`/`countedLedgersFor`, not this), so fix BOTH or the copies drift. Surfaced by the a060 background agent, which correctly left it alone rather than widening its own scope. <!-- relates:a060 --> <!-- relates:dd59 --> <!-- relates:f957 --> <!-- id:6de0 -->
