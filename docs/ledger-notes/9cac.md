# id:9cac

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

Its `is_boundary` / block-gathering at `:151`/`:176` is the same rule, but it runs over `TODO.md` and `REVIEW_ME.md` as well as `ROADMAP.md`, so the same mid-block insertion silently re-attributes bodies in all three. The `71ed` guard ports as a near-mirror. Surfaced by the agent that fixed `71ed`, correctly left untouched as out of brief. <!-- id:9cac -->
