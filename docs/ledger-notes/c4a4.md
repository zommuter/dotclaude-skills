# id:c4a4

Authored directly, not relocated by `tools/ledger-shrink.py`. Conventions:
`docs/ledger-notes/README.md`.

## From TODO

`:338` reads the detail path off the line, `:343` sets `match_text` to the line PLUS the whole note body when the file EXISTS, `:346` leaves it line-only when it does not, and `:475` greps `match_text`. So the class's INPUT changes with pointer resolvability: an item gains an UNMARKED-GATE finding with its own line byte-identical, because a note it already pointed at gained content (by creation OR append -- `:343` cats the whole file). Observed via a sibling repo's wave-1 snapshot diff, 9 arrivals. FIX the COMMENT, keep the suppression fact (it is true and is why the comment exists); do NOT change detector behaviour -- reading the union is correct and is what id:1608 fixed. Consider a characterisation test; if a `# fails-against` negative case is inapplicable say why and add it to tests/negative-case-exemptions.txt.
