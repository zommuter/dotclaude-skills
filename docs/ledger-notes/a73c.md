# id:a73c

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(successor id minted 2026-08-14 by owner decision; `id:292b` shipped mechanism (1), the linter, and is `[x]` in BOTH `ROADMAP.archive.md` and `TODO.archive.md`). Re-minted rather than kept under `292b` so `orphan-scan --cross-ledger` stays clean without waiting on the `xledger-ok` suppression-marker convention (routed:42c9). Carries: **(2)** the reached-fixture check, **(3)** the ledger-token-shape check, and **the CI runner that actually EXECUTES the declared negative case** — without which `# fails-against:` is a header nobody verifies, i.e. exactly the id:4347 shape. **Scope decision, owner-decided 2026-08-14:** the lint holds **ALL** headerless `tests/test_*.sh` (fail-closed — an opt-in `# defect-fix:` marker was REJECTED because a genuine defect-fix test that forgets the marker would escape the lint entirely and the blind spot would grow silently), with exemptions for known harness/shape tests living in **ONE reviewable allowlist file**, not scattered `n/a` comments, so what has been excused is auditable in one place. Back-pointer: `id:292b`. — **🔴 PRIORITY RAISED 2026-09-01: the CI negative-case runner is no longer speculative -- THREE live instances in ONE session, all in delegated work, all caught only by an independent adversarial review that a routine session would not have run.** Every one had a `# fails-against:` header or an equivalent red-first claim, so mechanism (1) was SATISFIED and blind: the header was declared and nobody verified it. That is precisely the `id:4347` shape this item's own text predicts. **The three, recorded as the fixture corpus for the runner:** (a) `id:9fa2`'s pair-narrowing test ran against parent `275fec46` and died at assertion (a) -- the parent's collector rejects the new paired file format outright -- so assertions (e) superset and (f) bare-token, the two the file EXISTS to pin, were UNREACHED. The file went red, but not for the claimed reason. Fixed by adding a case written in the ancestor's OWN spelling and ORDERING IT FIRST, plus a per-ancestor REACHABILITY block in the header. (b) `id:a290`'s locale case: the `LC_ALL=C`-stripped mutant was killed by a fixture-SANITY probe rather than by the case written to catch it, because the fixture's own unpinned `git worktree remove` returned German and aborted early -- so that case had zero demonstrated killing power until the fixture pinned the locale and re-ran. (c) `id:8302` (earlier, same class, refiled as `id:59c5`): a RED spec shipped with a fixture carrying NO ASSERTION, so clause (b) shipped unimplemented behind a green suite. **THE GENERALISABLE RULE the runner must encode, which mechanism (1) cannot express:** it is not enough that the test FAILS against the declared revision -- **the assertion that fails must be the one the file claims to pin.** A file that dies at an earlier assertion, or is killed by a sanity probe, or whose fixture never reaches the guarded path, is red for the wrong reason and is exactly as vacuous as one that passes. So the runner needs to compare WHICH assertion failed, not merely the exit status -- and where the fix changes a FORMAT (as in (a)), the negative case must be authored in the ancestor's own spelling or it can never reach the code under test. **Two cheap conventions that fell out of fixing these and are worth building in:** order the ancestor-discriminating case FIRST so an early abort cannot mask it, and require a per-ancestor reachability note stating which assertions do and do not discriminate there, since for a format-changing fix some assertions are STRUCTURALLY unreachable at the parent and that is legitimate -- it just has to be on the record rather than papered over. — **BACK-FILL SIZED 2026-09-01, and it is MOSTLY MECHANICAL -- measured, not estimated.** The owner asked for the 158 undeclared/prose-only files to be back-filled rather than left as a reported gap. Two measurements over the 161 headerless (defect-fix) tests: **(1) DERIVABILITY -- 154 of 161** were ADDED in a commit that ALSO touched non-test files, so both halves of a `fails-against-rev` declaration are derivable from history with no per-file judgement: the rev is the add-commit's PARENT, and the paths to overlay are that commit's non-test files. 7 were added in a tests-only commit (no guarded path to overlay, needs a human or a mutation instead); 0 have no add-commit. **(2) CEILING -- 12 of 161 emit NO `FAIL:`-prefixed line at all** (`test_archive_closed.sh`, `test_archive_done_multiline.sh`, `test_classify_hard_floor.sh`, `test_classify_repo_large.sh`, `test_cross_ledger_guard.sh`, `test_git_lock_push_slash_branch.sh`, `test_git_lock_push_ssh_guard.sh`, `test_lean_toolchain_drift.sh`, `test_no_silent_swallow.sh`, `test_orphan_scan_plugin_aware.sh`, `test_quota_sample.sh`, `test_run_tests_parallel.sh`), so the which-assertion mechanism -- which matches the test's own `FAIL:` line -- CANNOT work for them however the declaration is written. Those need either a `FAIL:` idiom retrofit or an exemption with a stated reason; a declaration for them would be one that can never verify. **SHAPE OF THE BACK-FILL, therefore: mechanize the 154, RUN the runner on each, KEEP only what actually verifies, and report the rest LOUDLY rather than shipping a declaration that has never been exercised** -- which is precisely the failure mode this whole item exists to end, so a back-fill that fabricates unverified declarations would be self-refuting. **GATED on the runner's review** (in flight): 158 declarations written against a runner later found wrong would all look authoritative and all be worthless. <!-- id:a73c -->
