# id:8e9a

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

Dogfooded live 2026-08-31: session A finished the em-dash migration, sent ONE `SendMessage` to a standby session B primed to idle until told, B runs `relay --intensive --quota-7d <N>`, wraps up, updates the handover and `systemctl poweroff`s while the owner is afk. The pattern worked, but was improvised per-message and is not written down anywhere. OPEN DECISIONS, none taken: (1) **Is it a skill at all**, or a section in `relay/SKILL.md`? It is invoked once per night at most, and a mandatory-load skill costs per-prompt ctx (the per-prompt-multiplier rule) — a `references/` doc the handoff message points at may be the better shape. (2) **Who owns the poweroff contract** — `systemctl poweroff` via polkit needs no sudo for a local active session, but an inhibitor, a second logged-in user, or the auto-mode classifier can turn it into a prompt that hangs forever against an absent owner. Decide whether B must pre-check (`systemd-inhibit --list`, `loginctl list-sessions`) and whether a failed poweroff is simply reported (my view: yes, benign — the machine stays on, never escalate). (3) **Quota-cap authority** when B's spawn prompt and A's handoff name different `--quota-7d` values — on 2026-08-31 the spawn said 60 and the owner told A to convey 90. Latest instruction should win, but A must SAY which it is conveying rather than silently pick. (4) **What A must enumerate** so a pool cannot quietly undo owner-reserved state: ids left OPEN pending a ruling, remotes deliberately unpushed, worktrees deliberately retained. (5) Whether "push before poweroff, verified across EVERY repo the pool touched, not just cwd" becomes a hard gate. Write-up basis: the drafted SKILL.md from this session was reverted unimplemented; re-derive from this item plus `docs/HANDOVER-2026-08-31-2200.md`. <!-- id:8e9a -->
