# id:795d

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

Owner flagged 2026-08-12 (loderite /relay --afk --only .): id:2ab3 (confirmed archive corruption blinding orphan-scan) sat behind a single routine item and needed a manual inject. NOT a blanket inversion — HARD is Opus-expensive vs Sonnet-cheap execute, so always preempting would invert the pool's cost model. Proposed shape: a NAMED-FACT override mirroring the existing id:8123 precedent (which already lets review outrank execute on the chain-end fact) — e.g. hard outranks execute when the HARD item has open dependents via gated-on:. Owner decision / meeting, not a snap flip. <!-- id:795d -->
