# id:4ce8

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From ROADMAP

(its "LIVE findings exist" code) when handed a DIRECTORY, so a mis-pathed invocation is indistinguishable from a genuine incomplete migration. MEASURED 2026-08-31: `relay/scripts/lane-delimiter-scan.sh --live-only /tmp` exits 1 after `line 85: read: 0: read error: Is a directory` and `line 85: line: unbound variable`; the same script correctly exits 2 for a MISSING path. Cause: the `[[ -r "$f" ]]` guard at :79 is true for a directory, so the read loop runs and dies under `set -u`. This matters because S10 (`id:da55`) gates on this script exiting 0 fleet-wide, and a usage error currently reads as evidence the migration is unfinished. Acceptance: a non-regular-file argument prints one `lane-delimiter-scan: not a regular file: <path>` line to stderr and exits 2, never a bash `unbound variable` trace; regular-file behaviour (0 = no live findings, 1 = at least one) is unchanged. Done-check: a new case in `tests/test_lane_delimiter_scan.sh` asserting exit 2 plus the message for a directory argument, and `tests/run-tests.sh` green. <!-- id:4ce8 --> (archived — see ROADMAP.archive.md)
