# id:3846

HAND-AUTHORED at filing time, not relocated by `tools/ledger-shrink.py`. Conventions and
backlinks: `docs/ledger-notes/README.md`.

## From TODO

**The trimmed `relay-implementer` executor STILL dies `Prompt is too long`. `id:c3c1` step 4 is
partially effective, not validated.**

**Measured on run `relay-20260909-091623-10249`** (2026-09-09, `--afk
--execute-agent-type relay-implementer`, 215 agents, 3 rounds): child
`agent-a572c958ddbc3a01e`, `agentType: relay-implementer`, `model: sonnet`, failed
`Prompt is too long` while working `id:c655` (a seam of `id:4839`, adding a repo dimension to
the head-length and shape-prose baseline keys). Its 79 insertions to
`relay/scripts/todo-conformance.sh` survive, parked as an explicit WIP-UNVERIFIED commit at
`relay/orphan/relay-20260909-091623-10249-execute-c655-0`.

**What this corrects.** `docs/session-handover-2026-09-08.md` records step 4 as *"validated
end-to-end"* with *"no dispatch failures, agent-failures=0"*, and that reading was reasonable
on the evidence available: dispatch DOES work, and it worked again here -- all four execute
children of this run ran as `relay-implementer`/sonnet, confirmed from their `*.meta.json`.
What was never shown, and is now falsified, is the stronger claim the item rests on: that
capturing ~31,030 tokens (41.6%) of the ~82k default preamble clears the Sonnet wall. For at
least one real unit it does not.

**Rate, not elimination.** Run `relay-20260908-174448-4421` (default agent): 5 of 15 execute
children died this way. This run (trimmed agent): 1 of 4. The trim helps and should stay; it is
not the fix, and the item should not be closed as though it were.

**Method note, because the obvious measurement is wrong.** Grepping this run's transcripts for
the literal `Prompt is too long` returns **21** children -- 205 of the run's agents are
`workflow-subagent`/`bash` mechanical hops whose `RELAY_STATUS` payloads QUOTE the phrase.
Filtering to `isApiErrorMessage:true` / `model:"<synthetic>"` (the `id:10dc` discipline the
2026-09-08 handover already recorded) yields exactly **1**. Any future re-measurement of this
must apply that filter or it will report a 21x overcount.

**Where the remaining budget goes, and why this is not simply "trim harder".** The probe series
in `docs/ledger-notes/c3c1.md` puts the floor at **41,654 tokens (55.9%)** -- unreachable by
ANY agent definition, since it is the harness scaffolding plus `CLAUDE.md` plus the memory
index, which a custom definition does not drop (the `default - wide` delta was 367 tokens,
0.5%). `relay-implementer` already narrows tools to `Bash, Read, Edit, Write`, whose definitions
cost 1,845 tokens combined. So there is very little left to cut on the AGENT side: the residual
lever is the BRIEF, which is `id:35b7`/`id:4f9b` (the prompt-size gate) and `id:e68f`
(ledger slicing) -- the same levers `loderite` needs, where the gate correctly refused dispatch
at ~482k tokens three times this run rather than letting a child die.

**Acceptance for closing `id:c3c1` step 4 honestly:** either a run in which every execute child
under the trimmed definition completes without an `isApiErrorMessage` prompt-size failure, or an
explicit re-scope of the item to "reduces the failure rate" with the residual routed to the
brief-size work. Do NOT close it on dispatch success alone -- that is the claim that was already
true and was mistaken for the whole.

**Relations.** `id:c3c1` (the parent; step 4 is what this corrects). `id:77d9` (the preamble
measurement -- ~82k, Sonnet wall ~176.7k, fixed floor ~95k). `id:35b7` / `id:4f9b` (the
prompt-size gate that refused loderite rather than letting it die -- the mechanism that DID
work). `id:e68f` (ledger slicing, absent for loderite). `id:10dc` (the measurement method).

## Appended 2026-09-09 -- the NEGATIVE CONTROL this item was missing

Nothing above was edited; this section is additive.

The measurement above is the WITH-flag arm (1 of 4 execute children died carrying
`EXECUTE_AGENT_TYPE=relay-implementer`). Run `relay-20260909-143257-21736` supplies the
WITHOUT-flag arm on the same repo, same day, same harness: `EXECUTE_AGENT_TYPE` was NOT
passed, and **7 of 10 dotclaude-skills execute children died `Prompt is too long`** (3
completed: `id:6446`, `id:521b`+`id:9088`, `id:227d`). zom.fi lost 1 more the same way.
Fleet total for the run: 8 errored of 696 agents, every one of them the same error, and
every one on the `execute` lane -- 0 on `review`/`hard`, which run Opus.

So the two arms now read 1/4 (25%) with the trim against 7/10 (70%) without it, same repo,
hours apart. That is the first evidence the trim does ANYTHING, and it is also the first
evidence of how far short it falls: a 25% child-loss rate is not a working remedy.

**Do not let a clean run close this.** The code.lawless session reported 92 agents / 0
errors on one `EXECUTE_AGENT_TYPE=relay-implementer` run and read it as confirmation. At a
25% per-child rate a clean run is unremarkable, and that repo's unit sizes are not this
repo's. This item's own headline already says "do not close on dispatch success"; the
cross-repo clean run is exactly the shape of evidence that sentence is warning about.
