# id:9f3a

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

Note for whoever does it: md-merge.py CANNOT target this line — its _ID_MARKER_RE only matches `id:XXXX`, and 279 carries `ref:aae4` — so either give the line a real id marker first or do it in a dotclaude-skills-rooted session that owns the file. Ironically this is the line that already documents this failure class as id:3947. <!-- id:9f3a -->

## From TODO

— the concept's evidence/narrative (grammar, instance inventory, prior art, provenance) now lives in that repo; the guard's existing claim that it is TRACKED in mathematical-writing under id:aae4 stays TRUE (the ledger line + re-anchored carve gate deliberately stayed in .mw beside the machinery), so this is an ADDITION, not a correction — nothing is broken today. -- detail: `docs/ledger-notes/9f3a.md` <!-- id:9f3a -->
