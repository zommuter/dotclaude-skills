# id:2065

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From ROADMAP

carve `*.archive.md` out of the ratchet** — its purpose is stopping old vocabulary on DISPATCHABLE work, and a closed archived entry is not dispatchable; archives are read by `resolve-gates.sh` / `unpromoted-scan.sh`'s twin corpus / `lib-typed-edges.sh`, but for ids and checkbox state, not lane tags. My recommendation, since it targets the actual mismatch and preserves history. (b) **rewrite them to capability-keyed vocab** (`[HARD - pool]` -> `[HARD]`) — satisfies both rules but REWRITES the semantic record of closed entries, destroying which lane the work was actually done under (hazard 7). (c) **exempt archives from S9** — amends the seam, which currently says archived live tags DO migrate, and leaves 85 em-dash tags that S10's strictness flip must then carve out too. The rewritten-but-uncommitted archive content is preserved in worktree `.claude/worktrees/agent-a6bb08543e21587fe` (unstaged), and is deterministically re-derivable from the anchored leading-lane-run regex if that worktree is reaped. <!-- gated-on:6958 --> **RESOLVED 2026-09-01 (owner ratified option (a)), verified by review run relay-20260901-101120-32404:** `hooks/pre-commit-lane-vocab.sh` now excludes `*.archive.md` from the staged-diff collection (`:(exclude)*.archive.md`) and ANNOUNCES each skipped archive file on stdout, never silently. Scope verified NOT a superset of the ratified option: the carve-out is keyed on the `*.archive.md` pathspec alone, the ratchet still exits 1 for the same old-vocab tag staged into a non-archive ledger, and the S9 migration itself (id:6958) is untouched and stays gated. Spec `tests/test_lane_vocab_archive_carveout_2065.sh` green in the full suite (536 passed / 0 failed / 1 expected-red). Commit a298c634. <!-- id:2065 --> (archived — see ROADMAP.archive.md)

## From ROADMAP

-- detail: `docs/ledger-notes/2065.md` <!-- gated-on:6958 --> <!-- id:2065 --> (archived — see ROADMAP.archive.md)
