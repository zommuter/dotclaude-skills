# id:a1b7

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

— single-id-two-views (D2) means one id spans both ledgers, but nothing at integrate enforces that closing it in one closes it in the other, so an integrate can silently leave the twin open. `orphan-scan.sh --cross-ledger` is the existing BACKSTOP that DETECTS this drift after the fact (id:2840's interim measure) — this item is about preventing it at the write, which is the mechanize-first side. Same mechanism produced the `00e1` drift. **Decide which shape:** integrate ticks both (a write, needs the flock'd `md-merge.py` on two files), or integrate REFUSES on divergence (a gate, cheaper and fail-loud, but leaves the human to reconcile). Source: loderite `id:8e2f`, owner `/relay human` 2026-07-30. Relates id:2840, id:a883 (relay-doctor `--strict`). <!-- id:a1b7 -->
