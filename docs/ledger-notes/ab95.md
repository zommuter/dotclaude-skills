# id:ab95

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

The regex is `' on (\d{4}-\d{2}-\d{2})\.?\s*$'` (archive-done.sh:69), so a date followed by ANY trailing marker yields `own_date = None` and the item falls through to the prior-commit branch. Measured in TODO.archive.md 2026-09-10: only **8** dated `[x]` items have the date at EOL where the guard can see it; **48** carry markers after it and are invisible to it; 61 dated in total. Since every conforming item ends in an id marker, the guard fires on ~13% of the population and is structurally dead for the rest. Observed live: id:1ce0 was closed and archived in the SAME session despite carrying `Done on 2026-09-10`. **SEVERITY IS REDUCED, and honestly so -- do NOT file this as the starvation bug.** id:5355's motivating harm was that premature archiving carries a `routed:` breadcrumb out of the live ledger and starves the cross-repo twin-guard; id:1d83 has since made that twin check search `TODO.archive.md` + `ROADMAP.archive.md`, and it was VERIFIED here that an archived item still satisfies it (`token_marker_in_files 1ce0` over the four files exits 0, over the live pair exits 1). So the residual harm is same-session items leaving the live ledger earlier than intended -- discoverability, not guard starvation. Fix: match the date ANYWHERE on the line, or strip trailing markers before matching. Note CLAUDE.md states the prior-commit branch must never be restored to an unconditional shape, which is effectively what this blindness makes it. <!-- relates:5355 --> <!-- relates:1d83 --> <!-- id:ab95 -->
