# id:e68f

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From ROADMAP

(owner proposal 2026-08-21) — today the prompt embeds/points at the whole `ROADMAP.md` + `TODO.md` and a prose instruction TELLS the child to slice; a prose instruction the child can get wrong is not a guard. <!-- id:e68f --> (archived — see ROADMAP.archive.md)
