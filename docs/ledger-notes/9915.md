# id:9915

**EDITED 2026-09-03 (`id:64f9`, title rewrite) -- declared, per the notes-are-editable
convention in `CLAUDE.md`.** This item's ledger TITLE in `TODO.md` was rewritten to fit
`LEDGER_ITEM_TITLE_MAX`. Nothing else on the item line changed: lane tags, typed-edge and
`id:` markers, the detail pointer and the checkbox are all untouched. The ORIGINAL title is
reproduced VERBATIM in the final section of this note, so no wording was deleted -- it moved
here. The relocated prose below is otherwise unchanged.

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

That is exactly 650d's failure mode with a measured instance. Separately f447: tonight was ebdb's THIRD deferral and it multiplied the gates to three across two repos (D6/cb1c open with its definition contested by id:2b93, parity-green, and a new statement-selection call) with no re-arm wiring, so a fourth deferral would still be silent <!-- id:9915 -->

## Original title (verbatim, before the `id:64f9` rewrite)

Re-prioritize id:650d (relay-doctor check 12 must ESCALATE, not just report) and id:f447 (the D6-settles re-arm breadcrumb) — CONCRETE EVIDENCE from relay-core 2026-08-21: the Lean shadow went live-RED on 2026-08-18 (bash shipped id:2799/id:27b4/id:4a76/id:bc2b+353e/id:7c5f in four days, the port followed none), the loud relay-doctor line fired throughout, and NOBODY INVESTIGATED — relay-core's own ROADMAP/TODO still asserted '416 mismatches, ~20k consecutive clean rounds' eight days later.
