# id:d35a

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(root cause of two Sonnet deaths, run `relay-20260827-154820-505`). **PROVEN, not inferred:** `relay/scripts/lib-roadmap-sections.sh:78` defines `ROADMAP_PARKED_HEADING_WORDS='(gated|deferred|done|icebox|archive|parked)'`. loderite's `ROADMAP.md:1415` reads **`### Visibility-only — human lane, NOT dispatchable`** — which contains NONE of those six words. Ran the real predicate: `is_exempt_heading` returns **false**, so every item under it is LIVE and executor-dispatchable. Control: `### Deferred — human lane` and `### Gated — human lane` both park correctly; only the actual heading does not. **CONSEQUENCE, already realised:** the dd4d seam items sit at `ROADMAP.md:1443-1445`, INSIDE that section. The pool worked them anyway — `id:a0e1` and `id:b5b1` are now `[x]`, i.e. **human-lane work was silently executed by the pool** — and `id:551f` + `id:f272` killed two Sonnet children at 175,479 and 176,277 tok (`id:7829`, `id:0a08`). The round-5 child DIAGNOSED this correctly in its own handback (*"Both items also sit structurally inside ROADMAP.md's terminal '### Visibility-only — human lane, NOT dispatchable' section"*) and the pool dispatched into it twice more regardless — the executor saw it, the classifier could not. **FALSE-NEGATIVE SIBLING of a banked false-POSITIVE:** the same file's docstring records that `@owner-gated` was parked ONLY because the literal string contains the vocab word `gated` — so this vocabulary already mis-fires in BOTH directions, and widening it is not obviously safe. **THE FIX IS THE OWNER'S CALL between two shapes:** **(a)** widen the vocabulary fleet-wide (e.g. `not.dispatchable|visibility.only|human.lane`) — one line, fixes every repo, but substring-widening is precisely the mechanism that produced the `@owner-gated` over-park, so it needs a considered pass, not a reflex; **(b)** reword loderite's heading to include a recognised word (`DEFERRED`) — zero fleet risk, immediate, but leaves every other repo able to write the same trap. Recommend (b) NOW and (a) as a deliberate vocabulary review. **NOT DONE IN-RUN, deliberately:** `ROADMAP.md` is a shared non-union ledger and loderite is IN the live pool — editing it mid-run is the collision the CLAUDE.md "check for in-flight automation before reshaping a shared plan" rule names. Do it after the pool drains. **A THIRD QUESTION worth answering separately:** should a section be the mechanism at all, or should each item carry a typed `gated-on:` edge? The round-5 child argued the seam chain's real defect is missing `gated-on:` edges (S2 after S1, S3 needs S2's artifact) — with those, ordering would hold even if the heading were never recognised. **OWNER RULINGS 2026-08-27 + INTERIM FIX APPLIED, WITH A KNOWN OVER-REACH — read before closing.** **(1) RULED: a SECTION must not be the mechanism.** Section-position is positional, invisible to the item line, and silently inherited by anything appended below — the failure here exactly. The durable carrier is a per-item typed `gated-on:` edge (`id:46f6`), which travels WITH the item and survives moves, re-sorts and appends. Option (a) from this item — widening `ROADMAP_PARKED_HEADING_WORDS` fleet-wide — is therefore NOT the direction to invest in; the heading match should shrink in importance, not grow. **(2) RULED: `[ROUTINE]` must never appear on a human-lane item.** The combination is invalid on its face and is a lint, not a judgement call — a cheap mechanical check (`roadmap-lint.sh`) can reject an executable lane tag under a parked/human heading, which would have caught this at authoring time instead of via two dead Sonnet children. **INTERIM FIX APPLIED to loderite** (`296dae61`): heading reworded to `### Gated / deferred — Visibility-only, human lane, NOT dispatchable`, with an inline note marking `Gated / deferred` load-bearing. Verified end-to-end — `is_exempt_heading` now true, and `classify-repo.sh --repo loderite` returns **verdict=blocked, actionable_routine_ids=[]** (was `execute`, offering `551f`/`f272`). **THE OVER-REACH, stated plainly: that heading now parks MORE than it should.** The section prose says *"These three carry `[INPUT — meeting]`"* and there really are exactly three (`ROADMAP.md:1430,1436,1444`) — but the **dd4d seam chain was later APPENDED into the same section** (`:1452-1458`) carrying EXECUTABLE lanes (`S1 [HARD]`, `S2 [ROUTINE]`, `S3 [ROUTINE]`, `S4 [HARD]`, …). S1 and S2 are `[x]` — they were genuinely executable work the pool completed correctly. So parking the heading also parks **dd4d-S3..S6, which are real work**. That is the conservative direction (nothing dies, no human-lane item is executed) and it is trivially reversible, but it is NOT the end state. **RECOMMENDED FOLLOW-UP, deliberately NOT done unattended:** MOVE the dd4d block out from under the human-lane heading into a live section and give the chain explicit `gated-on:` edges (S2 gated-on S1, S3 gated-on S2, …) per ruling (1) — then the ordering holds regardless of any heading. Moving a block of another repo's ROADMAP is a content-level reshape of its plan and is the owner's call, not a scanner's. **FIX COMPLETED 2026-08-27 (loderite `14e6a4e0`) — the over-reach is resolved.** The dd4d chain now lives under its own LIVE heading `### dd4d — Lodegine engine/game separation seams (S1–S6, strictly ordered)`, with ordering carried by typed `gated-on:` edges per ruling (1): S2←S1, S3←S2, S4←S3, S5←S4 **and** `c2f3`, S6←S5. The parked section now holds EXACTLY the three `[INPUT — meeting]` items it was authored for, and carries an inline note marking its `Gated / deferred` words load-bearing. **Verified, not assumed:** each of the six ids appears exactly once (nothing duplicated or dropped in the move); `resolve-gates.sh` reports `d5ff`/`27f7`/`3b3c` each with **1 unresolved gate** while `551f` has none — so the chain enforces ITSELF, which is the whole point of moving off the heading. **⚠ CONSEQUENCE THE OWNER SHOULD SEE: this deliberately RE-EXPOSES `id:551f`.** It is now correctly actionable (`classify-repo` → `actionable_routine_ids=['551f']`) because its gate `b5b1` is `[x]` — and it is the SAME item that killed a Sonnet executor at 175,479 tok. Its death was work-volume, not blockedness, so **nothing about this fix prevents a repeat**. Two things currently stand between it and another dead child, and BOTH are incidental rather than designed: loderite is `verdict=blocked` pending reconcile of its two parked orphans, and the pool's own repeat-handback suppression. Once those orphans are reconciled, the next pool WILL offer `551f` to a Sonnet again. The real remedies are `id:0a08` (enforce rule 2c), splitting `551f` into smaller seams, or routing it off Sonnet — none of which this fix does or claims to. <!-- id:d35a -->
