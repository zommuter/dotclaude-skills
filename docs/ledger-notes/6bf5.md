# id:6bf5

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From ROADMAP

Work these EIGHT ONLY: attempting all 12 in one session killed two executor children with `Prompt is too long` (2026-08-31, run relay-20260831-190008-13032). Pattern to follow: enumerate BOTH spellings, and change every downstream comparison in the same commit — the batch-1 residue updated `unpromoted-scan.sh`'s tag list but not its promote-disposition regex, which silently re-created the lodelore verdict-invisibility bug (id:4b64). <!-- gated-on:2ee5 --> <!-- id:6bf5 --> (archived — see ROADMAP.archive.md)
