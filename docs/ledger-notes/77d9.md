# id:77d9

Authored directly, not relocated by `tools/ledger-shrink.py`. Conventions:
`docs/ledger-notes/README.md`.

## From TODO

**The delegated-subagent preamble is now ~82,000 tokens, not the 58.6k banked by `id:c3c1`
/ `id:10dc` on 2026-08-21 -- about 40% growth in 18 days.** Measured 2026-09-08 from the
live run `relay-20260908-174448-4421`, as first-API-call input tokens before any tool call:

| Tier | first-call input | brief size |
|---|---|---|
| Sonnet children | 84,039 - 84,813 | 5.9 - 7.9 KB (~2k tok) |
| Opus children | 81,737 - 82,929 | same |

Subtracting the brief leaves ~82k of fixed preamble.

**Why this matters more than the number alone.** The same run measured the Sonnet
`Prompt is too long` wall at **~176.7k** (deaths at 170k-177k, highest survivor 175,349).
So the preamble alone is **46%** of a Sonnet executor's usable window before its brief, and
the full fixed floor is:

| Component | tokens | how measured |
|---|---|---|
| system preamble | ~82,000 | first-call input minus brief |
| dispatch brief | ~2,000 | 5.9-7.9 KB |
| `executor-contract.md` | ~7,800 | 31,074 B, read by 5/5 dying children |
| `conventions.md` | ~3,500 | 14,188 B, read by 4/5 |
| **floor** | **~95,000** | **54% of the wall, before one line of repo work** |

**This RE-PRICES `id:59d1`'s two levers, which the owner ratified as a pair.** Lever (1),
trimming the preamble, is now worth ~82k. Lever (3), splitting units harder, addresses the
~92k working span but cannot touch the floor -- and the briefs are ALREADY only ~2k tokens,
so there is very little left to split. For comparison, deleting `executor-contract.md` and
`conventions.md` outright would recover only ~11.5k. Nothing else on the ledger moves the
number the way lever (1) does.

**Death rate this run: 5 of 15 execute children (33%), every one Sonnet.** All five failed
with the single error string `Prompt is too long`; peak inputs 170,257 - 176,712.

**The Sonnet-only claim from `id:7829` HOLDS and now has a mechanism.** Same run, same
harness, same preamble: three Opus children peaked at 232,309 / 192,569 / 190,027 -- above
the Sonnet wall -- and all 12 Opus children survived. haiku 9/0 deaths, bash 510/0. The
asymmetry is window size, not prompt construction.

**Method caveat worth keeping.** A naive grep for the error string across the run's 546
transcripts returns 61 files, of which 56 are FALSE POSITIVES: `RELAY_STATUS` payloads and
ledger text quote the phrase, and `id:7829`'s own title contains it. Only records carrying
`isApiErrorMessage:true` / `model:"<synthetic>"` are real deaths. Anyone re-measuring this
must filter that way or they will report 61.

## PARENT-SESSION BASELINE RE-MEASURED 2026-09-08 -- 74,529, and the gap to 82k persists

(This note has been edited since authoring, per `docs/ledger-notes/README.md`.)

A corrected four-probe run (see `docs/ledger-notes/c3c1.md`) puts a **default
`general-purpose` sonnet subagent dispatched from a parent session at 74,529** first-request
context, against the **84,039-84,813** this note measured for pool execute children in run
`relay-20260908-174448-4421`.

**This does NOT retract the ~82k figure.** The two numbers are different dispatch paths, and
the pool figure is what matters for the deaths this item is about. What it establishes is
that the ~9-10k gap between them is REAL and REPRODUCIBLE, not a one-run artifact -- it was
first noted as "unexplained" and it has now survived a second, independent measurement with
corrected instruments.

**The gap is worth chasing because it is the same order as the prize.** Whatever the pool
adds on top of a parent dispatch is ~10k that no agent definition can touch, and it lands
squarely on the Sonnet path where the ~176.7k wall binds. Candidates not yet distinguished:
the pool's larger brief, the Workflow dispatch wrapper, or per-child state injected by
`relay-loop.js`. Measuring it needs a pool child, so it cannot be settled from a parent
session.

**The floor is now bounded from below by measurement, not estimate.** A minimal-body,
single-tool sonnet child costs **41,654** and no agent definition can go lower; that is
23.6% of the measured ~176.7k Sonnet wall consumed before any brief, any tool result, or any
work. The ~95k fixed floor this note records for pool children is the same quantity on the
other dispatch path.
