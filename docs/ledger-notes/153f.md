# id:153f

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

This is the id:0d7c relocation escape CLAUDE.md already warns about, observed live rather than predicted: the `decision-brief` collector (id:6fda) hit 9 such items INSIDE its own 157-item candidate set and refused to guess their lane, counting them under a named suppression reason instead. **Scope is WIDER than that 9 and the exact figure is NOT established: a crude repo-wide grep for the same shape returns ~32, but a bare grep over-reports on this corpus (the same class that reported 9 `[MECHANICAL]` items against a true 3 on 2026-09-11), so the real count needs `leading_lane_run` + a populated `lane_vocab_scrape`, NOT a grep.** Fix is at the SOURCE line -- move the lane into the leading run -- never by teaching readers to scan past the pointer, which would re-admit the ambiguity the anchoring exists to remove. Note `tools/ledger-shrink.py` already emits a relocated lane in the LEADING run for exactly this reason; these 32-ish predate it or came from another path. <!-- relates:0d7c --> <!-- relates:6fda --> <!-- relates:d35a --> <!-- id:153f -->
