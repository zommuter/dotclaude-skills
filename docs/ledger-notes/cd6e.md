# id:cd6e

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

Hand-run 2026-07-17: loderite had exactly 1 unaudited commit — a TODO checkbox tick — and dispatching a review for it would have put a review child's ROADMAP.md rewrite in the same run as 4 execute children's box ticks, which is precisely how the 4c02 fix got parked as an orphan. Judged it execute-only by hand and skipped the review. This is the already-filed id:0f1e exemption (ledger-only REVIEW_ME/ROADMAP/TODO/RELAY_LOG diffs) viewed from the SCHEDULING side rather than the false-echo-review side: the exemption should also suppress the review VERDICT when execute work is queued for the same repo, not just avoid the wasted strong unit. Cross-ref id:0f1e, id:b95d (ckpt anchor), id:1735. <!-- id:cd6e -->
