# id:413c

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(follow-on from `id:c219`, owner-ratified 2026-08-27). `id:c219` was a one-line over-specific glob that made contract v15 rule 2c silently inert for EVERY Workflow-dispatched child, and it survived a full green suite for a day because `tests/` only ever fed the resolver the transcript shape its author had seen (flat `subagents/agent-*.jsonl`; the pooled shape `subagents/workflows/wf_<id>/agent-*.jsonl` outnumbers it 25,620 to 2,476 on this host). The measurement it swallowed was not hypothetical: the inflownistration handoff child of run `relay-20260827-084504-10452` measured `handback bytes=349393 est_tokens=87348`, exit 3 — a real handback condition, discarded as `verdict unknown`. **Fail-open stays** (owner call 2026-08-27): a harness layout change must never wedge the pool, so fail-CLOSED is rejected. Instead add a relay PREFLIGHT hop that runs `self-transcript.sh` against the REAL `~/.claude/projects` tree for the current session and asserts it resolves to something, failing loudly at launch when it does not. **It belongs in preflight, not `tests/`** — it is inherently non-hermetic (it must read the live projects tree), and this repo's testing rules require hermetic tests, so a `tests/` file structurally cannot cover it. Sits alongside the existing step-0b/0c preflight family (`mech-preflight.sh`, `mech-currency.sh`, `check-install-drift.sh`), each of which exists for the same reason: a component that reports healthy while being unreachable at runtime. Considered and NOT adopted for now: surfacing a child's `verdict unknown` at the integrator (a `relay-loop.js` change, viable as a cheaper second layer). <!-- children-of:c219 --> <!-- id:413c -->
