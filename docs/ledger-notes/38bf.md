# id:38bf

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

— `--system` unit + ssh-push repo-write model (D5) + file-based push creds; build gated on `af30` authorization. Record as an `af30` sub-note. **Amendment-2 (Fable review): bare `git-shell` forced-command does NO per-repo scoping — the spec needs an `SSH_ORIGINAL_COMMAND`-checking wrapper to make D5's "pins the key to git ops on specific repos" true.** Sandbox meeting D1/D5 + Amendment 2; relates id:af30. <!-- gated-on:af30 --> <!-- id:38bf -->
