# id:4a76

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From ROADMAP

— `classify-verdict.sh:235` emits `human` ONLY when `surface > 0`; a repo whose ENTIRE open backlog is human-lane but whose `surface == 0` falls through to `idle`, and `control-board.sh:162` maps `idle → design-drained`. Measured 2026-08-21: helferli = 18 open, ALL `[INPUT — …]`, zero poolable → `design-drained`, "Waiting on a human: _(none)_"; csgebra = 13 open, all executable items `gated-on:` `940f` `[INPUT — decision]` → same false-clean. <!-- id:4a76 --> (archived — see ROADMAP.archive.md)
