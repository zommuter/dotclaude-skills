# id:5dcd

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

— surface `status:triaged` notes, owner rules valid|noise via **re**, append `status:processed verdict:… review_me:id:…` to the note (never deletes). Pairs with id:80e8. (from toesnail, routed:f42b) <!-- id:5dcd -->
