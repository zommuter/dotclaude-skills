# id:143a

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(observed live, `id:90f2`, cleaned up by hand). The uid anchor lives in the issue description, so once the `[[ledger-views]]` marker is stripped, `apply` cannot match the existing item: it creates a SECOND one and leaves a markerless orphan on the board. Label loss repairs cleanly; marker loss does not. `verify` catches it loudly (exit 4), so this is not silent — but the repair path makes the board worse rather than better, which means the honest operator response today is manual cleanup. Fix: match on a carrier that survives sanitization (or reconcile markerless orphans explicitly) so re-apply heals rather than duplicates. Contract: an item whose marker was stripped server-side is re-adopted by the next apply — one item on the board afterwards, not two. <!-- children-of:90f2 --> <!-- id:143a -->
