# id:ddec

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

-- closing it is false (the item is genuinely open, gated-on:X) and dropping the marker erases owner provenance. inflownistration id:466d took exit (1) reword 2026-09-01 (owner-ruled), which works but costs the uppercase terminal word and is per-item; the generic fix is either a third sanctioned exit (a gated-open carve-out in lib-state-claim.sh when the line also carries a gated-on: edge) or a doctrine ruling that reported-speech deferral is the required house style. Not a per-id baseline entry -- see inflownistration REVIEW_ME.md id:553d. <!-- id:ddec -->
