# id:df66

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

Two independent defects, both hit live 2026-08-20 on leAIrn2learn's owner-ratified park of the id:8048 third (LP/simplex) XOR-bound derivation. **(1) No keep-marker:** `list_orphans` (`relay/scripts/relay-reconcile.sh:320`) keys purely on `refs/heads/relay/orphan/*` with no allowlist, keep-namespace, or decided-flag, so every `--all` sweep and every `/relay health` (`relay-doctor.sh:843`) re-lists a settled park as if unresolved — inviting exactly the re-litigation the CLAUDE.md consumed-pre-registration rule warns about. **(2) No dedup on surfacing:** `surface_judgment` (`:434`) appends its `- [ ]` box with `>>` and never checks whether a box for that branch already exists, while `--auto-restart` (id:c14d) runs `relay-reconcile.sh --all --auto` on EVERY relay-loop restart — so a JUDGMENT-classified park (any code diff; `classify_orphan` correctly refuses to auto-merge those) grows one new open human-review box per restart, unboundedly. Note (2) is the worse half: (1) is listing noise, (2) silently inflates the human backlog the `human` mode exists to drain. Fix both: a keep-marker the lister honours (namespace or sidecar), and a dedup guard keyed on the branch name before appending. Workaround used 2026-08-20 (NOT a fix — it only moves one ref out of reach): convert the park to a pushed annotated tag, `parked/8048-lp-simplex-derivation` in leAIrn2learn. Re-checkable: park a code-diff orphan, run `relay-reconcile.sh --all --auto` twice, and assert REVIEW_ME gains exactly one box, and that a kept-marked park does not appear in `--all`. <!-- id:df66 -->

## Appended 2026-09-09 (`/relay human`) -- the premise is now REAL on this repo

Nothing above was edited; this section is additive.

On 2026-08-20 this was a leAIrn2learn observation. Today the owner ruled, in `/relay human`,
that dotclaude-skills' TWO parked orphans (`...-32609-execute-repo-0` / `id:521b`,
`...-10249-execute-c655-0` / `id:c655`) STAY parked deliberately -- both carry reviewed
NEEDS-WORK verdicts and their ledger notes instruct the next executor to start FROM the
branch. So this repo now holds exactly the shape defect (1) describes: two settled parks
that `list_orphans` will re-list on every `--all` sweep and every `/relay health`, forever,
with no way for the ledger to say "decided".

Defect (2) compounds it here specifically: `gather-human-backlog.sh` emits a `parked_orphan`
row per branch, so every future `/relay human` re-surfaces both as human backlog after the
human has already dispositioned them -- and the boxes closed today will be re-created by the
next `--auto-restart`. The workaround recorded above (convert the park to a pushed annotated
tag) is NOT usable here: the whole point of these two parks is that an executor must be able
to `git diff main...<branch>` and resume from them, which a tag out of the orphan namespace
makes harder to discover, not easier.

Re-checkable: `git for-each-ref 'refs/heads/relay/orphan/*'` returns 2 on this repo today;
after a `relay-reconcile.sh --all --auto` run, assert `REVIEW_ME.md` gains ZERO new boxes for
either branch.
