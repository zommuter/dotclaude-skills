# id:3a7a

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

**EDITED 2026-09-03 (`id:64f9`, title rewrite) -- declared, per the notes-are-editable
convention in `CLAUDE.md`.** This item's ledger TITLE in `TODO.md` was rewritten to fit
`LEDGER_ITEM_TITLE_MAX`. Nothing else on the item line changed: lane tags, typed-edge and
`id:` markers, the detail pointer and the checkbox are all untouched. The ORIGINAL title is
reproduced VERBATIM in the final section of this note, so no wording was deleted -- it moved
here. The relocated prose below is otherwise unchanged.

## From TODO

Suggest resolving the name via the main worktree (git rev-parse --path-format=absolute --git-common-dir) or relay.toml, and/or warn loudly when the scanned path is a linked worktree <!-- id:3a7a -->

## Original title (verbatim, before the `id:64f9` rewrite)

unpromoted-scan.sh derives the repo name from the git-toplevel basename, so running it on a relay WORKTREE path (…/relay-<runid>-handoff) names the repo after the branch dir and silently defeats the per-repo decision-queue exclusion (id:47f1 case-g) — observed 2026-07-22 in yinyang-puzzle: 17 phantom 'surface' rows from the worktree vs 0 from the main checkout, which would re-file already-answered lane questions (the answer-then-re-ask loop the 2026-07-02 fix closed).
