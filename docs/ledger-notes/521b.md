# id:521b

AUTHORED note, not relocated by `tools/ledger-shrink.py` -- the verbatim-reproduction claim
does not apply to it (see the "Authored notes" section of `docs/ledger-notes/README.md`).

This is the batch-acceptance gate seam of `id:64f9`.

## Parked work -- READ BEFORE RESTARTING THIS ITEM

An execute unit already attempted this item and its branch is still on disk, unmerged:

    relay/orphan/relay-20260908-231617-32609-execute-repo-0

It holds +181 lines in `tools/shrink-acceptance.py` implementing "Check 4 -- title-rewrite
batch invariance" (verified 2026-09-09 with
`git diff --stat main...relay/orphan/relay-20260908-231617-32609-execute-repo-0`). It merges
cleanly onto `main` and the review found it gaming-clean.

**Reviewed verdict: NEEDS-WORK.** Two findings, both from the review this session:

1. It breaks two currently-green tests. `check_title_rewrites` fires on ANY item whose head
   line changed, and ordinary relocation shrink changes head lines by design -- so the check
   has to distinguish a title REWRITE from a relocation, which it does not.
2. It compares lanes as a multiset, so de-duplicating a doubled lane tag reads as a re-lane.

**Additional blocker**: `id:9088` -- the `id:64f9` RED spec's case 3 is unsatisfiable (its
fixture title is 194 chars against the 200 it must exceed), so cases 4-7 have never run.
Any restart that only fixes the two findings above still cannot be verified past case 3.

**Why this breadcrumb exists**: an item whose work is parked on an orphan branch is
invisible to the next executor, which would start from scratch and rewrite the +181 lines
without the review findings.

The branch is not merged and nothing here ticks the item.
