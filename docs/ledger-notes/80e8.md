# id:80e8

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

— on-branch un-noted detection + SOFT/LLM pass (incl. b7b1 dangling-symbol) + loose/off-branch recovery via `merge-base --is-ancestor`; draft a REVIEW_ME box + 🚧 callout; mark note `status:triaged`. Reads/writes `refs/notes/verify`. (from toesnail, routed:6cc0) <!-- id:80e8 -->
