# id:f833

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

Before this, such a payload silently DUPLICATED the item: only the marker line is replaced, so the old continuation lines stayed beneath the freshly written block. Exit 0, nothing on stderr, and the marker count stayed correct -- so `grep -c` after the write could not see it. Observed live: kienzler-solutions TODO.md id:9f11 grew 43 duplicated lines carrying SUPERSEDED legal citations, i.e. the stale copies contradicted the fresh item beneath them. The guard is deliberately NARROW: a multi-line payload onto a NEW or single-line item still works, because `relay/scripts/handback-followup.py:172` emits every seam that way (head line plus indented Acceptance/Done-check/Context, owner requirement 2026-07-26) -- an over-broad first cut broke `test_handback_followup.sh` and `test_seam_emitter_acceptance_44a1.sh` and was withdrawn. A whole-block replace op was prototyped and withdrawn too: it introduced a SECOND continuation-boundary definition (the id:4983 class) that id:4f0f exists to prevent. Two findings from that attempt are appended to REVIEW_ME id:b5c1 for its ratification. Test: `tests/test_md_merge_multiline_line_guard_f833.sh`, 11 cases, negative-verified against 5073c6236530. <!-- relates:4f0f --> <!-- id:f833 -->
