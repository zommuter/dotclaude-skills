# id:79fd

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(owner design call 2026-08-26). `relay-loop.js:4478` is `while (round < MAX_ROUNDS) { await runRound() }`, and `runRound()` is documented at `:1203` as *"one re-discover → dispatch wave → **drain**"* — a strict barrier. Observed live in run `relay-20260826-122101-7415`: round 2 dispatched 32 units; 26 completed and 7 blocked within ~45 min, then the ENTIRE pool sat idle for 45+ more minutes behind ONE slow unit (leAIrn2learn handoff, blocked on a long pytest), with 61 other repos unscanned and zero dispatch capacity used. This is precisely the barrier/pipeline anti-pattern the Workflow tool's own guidance names (*"barrier latency is real … a barrier wastes the fast agents' idle time"*) — the pool uses `parallel()`-with-barrier semantics where `pipeline()` fits. **The cost objection does not hold, and the cheap path already exists:** `classify-repo.sh` is a MECHANICAL hop — deterministic, zero agents (the Drain-mode ratification records `ambiguous:false`) — and relay-loop already calls it directly at `:1625` (chain-end re-ask) and `:1820`. Round discovery additionally fans out LLM `discover-shard` classifiers (Sonnet, `meta.phases`), but those are signature-cached per repo (id:c3a6, `discover-sig.sh`), so an unchanged repo re-scans for free. So continuous re-discovery over non-in-flight repos is close to free TODAY. **Design questions for the owner, not settled here:** (a) does a repo become re-scannable the moment ITS unit lands, or only when nothing is in flight for it — the in-flight repo must stay excluded to avoid double-dispatch (the id:d530 `--priority` note records that a repo's own discovered unit must never double-dispatch); (b) how do the round-scoped accumulators behave when rounds stop being discrete — `RELAY_STATUS.md` sections already have MIXED temporality ([[relay-status-accounting-temporality]]), and `round=` becomes meaningless as a counter; (c) the two-consecutive-dry-discoveries termination and `MAX_ROUNDS` seatbelt both key on round identity and would need re-expressing; (d) interaction with the dispatch-scoped `--once`/`--after N` caps (id:a615) and the STOP sentinel, which are already dispatch-scoped rather than round-scoped and so may FIT this model better than the current one. Prior art to reuse, not reinvent: the off-Workflow drain (`drain-driver.mjs`, id:cd7a) already re-classifies per round via `classify-repo.sh` with no Workflow discovery agents at all. **OWNER-RATIFIED 2026-08-26 — SUBORDINATED to `id:80b8`, and gated on a MEASUREMENT rather than a meeting.** Investigation (apex, 2026-08-26) found this item is not a new design question: **`id:80b8`** (`TODO.md:114` / `ROADMAP.md:63`, `[INPUT — meeting]`, OPEN, filed 2026-06-30 by the owner — *'still not happy'*) is the same subject with its shape ALREADY decided at meeting `docs/meeting-notes/2026-06-30-1523-relay-loop-mechanical-classifier.md` DP6, and 79fd's four sub-questions map near one-for-one onto 80b8's five listed non-trivialities. Decisively, **80b8 PRE-REGISTERED this very situation as its own trigger**: *'True mid-flight refill (raw Promise.race picker) = thin follow-on, gated on measurement showing the macro-boundary still idles slots.'* Run `relay-20260826-122101-7415` (32 dispatched, settled in ~45 min, then 45+ min idle behind ONE unit, 61 repos unscanned) IS that measurement — so this item's contribution is recorded as 80b8's trigger evidence, not as a rival item. 80b8 additionally names two constraints 79fd omits entirely: the **D3 anti-gaming verdict-class order** (you cannot stream a handoff for a repo whose review is pending) and **fairness/starvation**, which DP6 called *'the only genuinely new design'*. **NEXT STEP IS `id:87f5`'s PRE-REGISTERED RULE, NOT A MEETING:** promote iff round-tail idle share reaches **>=25% of wall-clock, n = all retained runs**, measured with `relay/scripts/relay-burn.sh` + the `id:4438` ranking. The single live observation is n=1 and cannot carry this decision. **TWO FALSE PREMISES IN THIS ITEM, corrected:** (1) it claims round discovery *'fans out LLM `discover-shard` classifiers (Sonnet)'* — **STALE**: both hops dispatch at `MECH_MODEL` = `'bash'` (`relay-loop.js:137`, prelude `:1944` id:86a2, shard `:2113` id:24ec), so the token objection to continuous re-discovery has FULLY LAPSED; the stale claim was traced to this repo's own `CLAUDE.md` gotcha, corrected in `58d4763c`. The cost that IS real and unmeasured is `reconcile-repo.sh`'s LIVE git side effects per scan. (2) sub-question (d) assumes `--once`/`--after N` are dispatch-scoped and so 'may FIT this model better' — **half wrong**: `id:a615` added a dispatch-scoped budget INSIDE each round but the run-ending cap stays round-keyed (`STOP_AFTER_ROUNDS` at `:182`, compared `round >= …` at `:4631`). Worth fixing independently so no decision rests on it. **WHAT THE BARRIER ACTUALLY PROTECTS** (verified): exactly ONE thing that cannot be re-keyed — the `[INTENSIVE]` run-alone OOM invariant (`:4442`, whose guarantee IS 'nothing else is in flight'). The integrator (a per-repo promise chain) and the discovery sig cache (fail-open) are already round-independent; `id:dc5b`'s one-unit-per-repo and the RELAY_STATUS partition are round-keyed EXPRESSIONS of constraints that are not about rounds, and re-key to an in-flight-set filter (`state.inFlight`, `:4170`/`:4205`). The barrier was never designed — it is the default join semantics of `parallel()`, identified retrospectively on 2026-06-30. <!-- gated-on:80b8 --> <!-- id:79fd -->
