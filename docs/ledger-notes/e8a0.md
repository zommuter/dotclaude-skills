# id:e8a0

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

— OBSERVED 2026-07-23: a loderite executor (worktree `~/.cache/relay/worktrees/loderite/exec-7453`) started `vite preview --port 4199` and never tore it down; the worktree was later reaped (de-registered) but the node process leaked and ran 2h47m+ orphaned (no owning session, no claim, port 4199 held). An executor that spawns a long-lived server (dev/preview/e2e host) for a test must guarantee teardown, and worktree-reap (`worktree-retire.sh` / id:3ac8 clean-reap / id:373e force-free cleanup) should kill any child processes rooted in the worktree path before removing it. DISTINCT from the git-lock-hygiene umbrella id:d2cd (that is `.lock`/index locks, not orphaned child PIDs). Directions: (a) executor contract rule — a spawned server MUST be backgrounded under a process-group the executor kills on exit (trap); (b) reap-time sweep — `pgrep -f <worktree-path>` → kill before `git worktree remove`; (c) a periodic orphan-process reaper (pairs with the outage watchdog id:98f0). Observe-first: FIRST logged instance, but the leak is silent + accumulates (port exhaustion, RAM). Relates id:3ac8, id:373e, id:d2cd, [[oom-local-model-session-kills]]. <!-- id:e8a0 -->
