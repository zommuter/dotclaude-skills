# id:3a6c

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

MEASURED by the filer, not assumed: loderite's `node_modules` is 226 MB / 285 packages, and the repo copy plus one live worktree copy total **449 MB under a single `du`** — i.e. ZERO hardlink sharing, npm made a genuine full copy. Alternative measured on this host: `cp -a --reflink=always node_modules <worktree>/` completed in **1 second** with `btrfs filesystem du -s` reporting **Exclusive: 0.00 B** (146 MiB set-shared) — a 226 MB tree for zero new space, versus a full install. **INDEPENDENTLY VERIFIED HERE 2026-08-14**: `~/src` is btrfs (`/dev/nvme0n1p2[/@home]`), and `~/.cache/relay/worktrees` is **1.8 GB**. **REFLINK, NOT A SYMLINK — and the discriminator is correctness, not speed.** The owner floated both; they are not equivalent. A shared or symlinked `node_modules` is MUTABLE SHARED STATE (vite/vitest write `node_modules/.vite`; npm/postinstall scripts write in-tree), so two concurrent worktrees — the normal relay case, `POOL_WIDTH` defaults to 5 — would race and corrupt each other. Reflink copies cheaply and diverges on first write, so it is concurrency-safe by construction. **SCOPE**: the worktree-creation path (SKILL.md invariant 4's `git worktree add`) should, when the source repo has a `node_modules` and the FS supports CoW, reflink it into the new worktree and otherwise fall back to today's behaviour — `cp --reflink=auto` degrades to a normal copy on non-CoW filesystems, so a probe is only needed to decide whether to skip entirely. Node-only; irrelevant to python/uv repos. **STALE DOC TO FIX ALONGSIDE**: the relay's worktree guidance repeats the claim that `node_modules` is "npm-cache-backed, so mostly fast linking — inherent to worktrees and fine". Measured: it is not linking at all, and not fine. Textbook constraint-archaeology — an assertion that was never measured. **COUNT CORRECTION, verified here**: the filing reports "13 worktree dirs live on disk"; `find ~/.cache/relay/worktrees -maxdepth 1 -type d` returns **61** (0 non-directory entries). So the reap backlog is substantially worse than filed — abandoned worktrees from 2026-07-24/26/31 and 2026-08-12 are named in the source item, but the total is 61, not 13. The reap path needs its own look; do not size that work off the filed number. **GENERALISED 2026-08-14 on owner input — this is NOT an npm-specific item.** The owner notes the same symlink-vs-reflink question applies to the Lean build trees in `lodelore`, `mathematical-writing` and `toesnail`. So the unit of work is the relay worktree-creation path handling **large regenerable artifact trees generically**, not `node_modules` as a special case: at minimum `node_modules` (npm) and `.lake` (Lean), with `.venv`, Rust `target/` and similar as candidates. **The correctness discriminator carries over unchanged and is arguably STRONGER for Lean**: a symlinked or shared tree is mutable shared state, and `lake` writes into `.lake/build` continuously during a build, so two concurrent worktrees at the default `POOL_WIDTH` of 5 would race — whereas reflink copies cheaply and diverges on first write. **PER-ECOSYSTEM VERIFICATION IS REQUIRED, NOT ASSUMED**: the 226 MB / 1 s / `Exclusive: 0.00 B` figures were measured for one npm tree on this host; the equivalent numbers for a `.lake` tree, and whether anything in it is host-absolute or otherwise unsafe to copy, must be measured per ecosystem before extending the mechanism to it. Do not generalise the MEASUREMENT along with the design. **Also check for the same unmeasured "it is fine" assertion** in those repos' own docs — loderite carried one for `node_modules` ("npm-cache-backed, so mostly fast linking") that measurement refuted; a Lean-side equivalent is likely and would be the same constraint-archaeology failure. <!-- routed:ca0f --> <!-- id:3a6c -->
