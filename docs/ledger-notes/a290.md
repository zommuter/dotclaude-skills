# id:a290

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(reopens the half of `roadmap:b02f` that was never built). `git worktree remove` refuses ANY worktree whose tree carries `.gitmodules`, whatever its state; direction (b) of b02f (deinit first) was TESTED AND REFUTED 2026-08-26 -- git keys the check on the file being in the tree, not on checkout state. Only `git worktree remove --force` removes them, and that op is `deny` fleet-wide (`id:221f`(a)), so this is an owner call and NOT something a session may route around (the `[[no-route-around]]` rule: the guard binds the OUTCOME, and `rm -r` + `worktree prune` is the exact dodge the owner has already refused once). **MEASURED 2026-09-01:** five `yinyang-puzzle` worktrees, from runs dated 2026-07-24 / 07-26 / 07-31 / 08-20 / 08-26, **1.8 GB**, every one `git status --porcelain` CLEAN with its HEAD already an ancestor of `main` -- i.e. zero work at risk, pure debris. LATENT FLEET-WIDE: `llm-from-scratch`, `Depth-Anything-3` and `CrispASR` also carry `.gitmodules` and will accumulate identically once they see relay traffic; yinyang-puzzle is simply the one combining submodules with five runs. **The re-attempt loop is FIXED separately (`6195eb47`)** -- reconcile now reports them under an ADDITIVE `unretirable-submodule:` marker instead of planning a disposal every round -- so this item is ONLY about disposal, and nothing is silently accumulating unseen any more. **Two shapes, both needing your ruling:** (a) a ONE-OFF supervised `--force` on these five verified-clean-and-merged dirs, reclaiming the disk but setting no precedent; (b) a NARROW escape hatch in `worktree-retire.sh` that force-removes ONLY after its existing clean-AND-merged checks have both passed and the sole remaining blocker is the submodule refusal -- far safer than a blanket allow because it cannot fire on dirty or unmerged work, and it stops the recurrence permanently. (b) subsumes (a). Note b02f closed on direction (a) (a clearer message) while explicitly calling (c) -- report-not-retry -- *cheap and composing*; (c) is now built, and this is the residue. — **DISPOSAL DONE 2026-09-01 (owner ran it; shape (a), the one-off supervised `--force`).** All five `yinyang-puzzle` worktrees removed and all five `relay/*` branches deleted with the force-free `git branch -d`, which ACCEPTED every one -- so git's own merged-check independently confirmed the clean-and-already-ancestor-of-main claim rather than it resting on the session's `merge-base` reading. Post-state verified: `git worktree list` shows only the main checkout, `~/.cache/relay/worktrees/yinyang-puzzle/` is empty, and the wider cache holds 66 repo directories with ZERO worktrees under any of them -- nothing else is parked fleet-wide. Note the reclaimed figure is the 1.79 GB `du` sum taken BEFORE the removal, not a measured before/after delta: free space was not recorded beforehand, so do not cite it as a measured reclamation. **THIS ITEM STAYS OPEN on shape (b) ONLY** -- the narrow escape hatch in `worktree-retire.sh` that force-removes only after its existing clean-AND-merged checks have both passed and the submodule refusal is the sole remaining blocker. (a) reclaimed the disk and set no precedent, so the RECURRENCE is still unaddressed: `llm-from-scratch`, `Depth-Anything-3` and `CrispASR` all carry `.gitmodules` and will accumulate identically once they see relay traffic. Nothing accumulates UNSEEN in the meantime -- the additive `unretirable-submodule:` marker (`6195eb47`) reports the class every round. Owner's call: build (b), or close this and let the marker re-surface it. — **OWNER RULED 2026-09-01: build shape (b), the narrow escape hatch.** A force-remove inside `worktree-retire.sh` that fires ONLY after its existing clean-AND-merged checks have BOTH passed and the submodule refusal is the sole remaining blocker; it structurally cannot fire on dirty or unmerged work. Ends the recurrence for `llm-from-scratch`, `Depth-Anything-3` and `CrispASR` before they reach the state yinyang-puzzle did. **Carry this weakness into the build, stated at decision time and NOT resolved by the ruling:** it puts a `--force` inside an automated path, and the `b02f` archaeology showed git's refusal is NOT keyed on `.gitmodules` being in the tree as b02f documented -- an uninitialised submodule removes cleanly, and the refusal survives `deinit` even with the gitlink dir removed, so something in `.git/modules/*/config` sustains it. The predicate `the sole blocker is the submodule refusal` therefore rests on a mechanism nobody has fully characterised. The hatch must DETECT that specific refusal rather than assume it, and fail closed (refuse, report) on any refusal it does not positively recognise. — **SHAPE (b) BUILT AND REVIEWED 2026-09-01 — NOT MERGED. The review found a SEVERE data-loss path that all four guards pass, so this must NOT land default-on as written.** Work sits on branch `worktree-agent-a9bf47d80f917bdab`, unmerged. **THE DEFECT:** a linked worktree gets its OWN submodule object store at `.git/worktrees/<wt>/modules/<path>`. If a submodule commit was created inside that worktree and its gitlink bump was merged into main, `worktree remove --force` deletes the admin dir and takes the ONLY copy of those objects -- the merged superproject commit on MAIN then references a gitlink that no longer resolves (`upload-pack: not our ref`). Reproduced by fixture end to end. All four guards pass legitimately in that case (HEAD matches, tree clean, branch merged, refusal verbatim) and the hatch prints `no unmerged commit existed to lose` while destroying it. Reachable from `integrate.sh:1419` step 9 on EVERY integrate, unconditionally -- so this converts today's loud defer into silent unrecoverable repo damage, which is strictly worse than the status quo's leftover directory. Mitigated only if the submodule commit was independently pushed to the submodule's own remote; the relay pushes the superproject, not submodules. **REQUIRED BEFORE ANY MERGE:** (i) a FIFTH guard -- for each gitlink in the worktree's index, verify the SHA is present in the SHARED `.git/modules/<path>` store, refuse otherwise; (ii) tests for the two guard mutants that SURVIVED the author's own suite -- deleting the HEAD-matches guard (detached-HEAD worktree carrying an unreferenced commit) and dropping `--ignore-submodules=none` (a committed `submodule.<path>.ignore = all`, an ordinary real setting, hides submodule dirt from default `status --porcelain` and the mutant destroyed real content); (iii) pin `LC_ALL=C` around the probe and the force -- under a German locale the refusal string does not match, and while it fails CLOSED it falls through to the generic dirty-tree advice this whole exercise exists to stop. **TWO MECHANISM CORRECTIONS, both fixture-proven:** `roadmap:b02f`'s `.gitmodules` premise is REFUTED (an uninitialised submodule worktree removes force-free; the refusal needs the submodule POPULATED), and the owner's `.git/modules/*/config` hypothesis is WRONG -- `.git/modules/<path>` exists in BOTH the populated and uninitialised cases, so it is not the discriminator; the state git actually tests is `.git/worktrees/<wt>/modules/<path>`. Also confirmed: git validates submodules BEFORE the dirty check, so a DIRTY worktree emits the IDENTICAL refusal -- the string carries zero cleanliness information and guard (3) is the only barrier. **SEPARATE OWNER CALL, unchanged by this:** `reconcile-repo.sh:190` short-circuits on `-e <wt>/.gitmodules` before any reap is planned, so the hatch is unreachable from reconcile and the recurrence is only partly closed. That skip rests on the same refuted b02f premise and also skips uninitialised-submodule worktrees that would remove force-free today. **House-rule check (reported, not ruled):** this does NOT conflict with the 2026-08-22 unconditional-deny ruling -- `destructive-git-guard.py` covers only the five tree-wide forms and says so explicitly -- and routing the force through an allowlisted script matches the established `force-push.sh` pattern, so it is not a route-around. The real divergence is DEFAULT-ON: every other destructive capability in `worktree-retire.sh` is opt-in. — **ROUND 2 REVIEW 2026-09-01: the fifth guard closes the ORIGINAL fixture but the SEVERE class is only PARTLY closed. STILL NOT MERGED, still not safe default-on.** Two further variants were independently reproduced, both forcing with all FIVE guards passing and the success message asserting `no worktree-only submodule object existed to lose`: **(F1) NESTED SUBMODULES** -- guard 5 walks only TOP-LEVEL gitlinks, but the private store nests (`.git/worktrees/<wt>/modules/vendor/sub/modules/lib/inner`); with the outer commit pushed to the shared store the top-level check passes while the INNER objects stay worktree-only, and after the force `submodule update --init --recursive` fails with the byte-identical `upload-pack: not our ref` signature one level down. **(F2) INDEX-SCOPED vs HISTORY** -- no nesting needed: make a submodule commit, bump the gitlink, then REMOVE the submodule from the index in a later commit, merge both. The index now has zero gitlinks so guard 5 sees nothing to check, yet merged history still references the object and git STILL refuses the plain removal -- proving the refusal trigger and the guard are keyed off DIFFERENT things. **(F3, HIGH) THREE FAIL-OPEN PATHS** despite the comment claiming conservative-in-both-unknowns: `|| return 0` on the `--git-common-dir` probe and `|| true` on `ls-files --stage` both yield an empty missing-list, which the caller reads as SAFE -- so a corrupt or unreadable index in a crashed relay worktree (exactly this script's target population) silently OPENS the force. `--path-format=absolute` also needs git >= 2.31; on older git the guard is a silent no-op while the rest of the hatch fires. **(F4, MEDIUM)** the `.gitmodules`-absent path-as-name fallback can resolve against an unrelated store that happens to sit at that name and falsely PASS -- also hit unplanted in a two-submodule crossover config. **(F5, MEDIUM)** the tightened source-grep ban is narrowed, not closed: six smuggling channels remain (`msg=`, indented `msg=`, `why=`, `hatch_refused=`, `echo`, `log`), each executing a real force invisibly. **CONFIRMED GOOD, so the work is not wasted:** the original hole IS closed without over-refusing (a fully-shared worktree still force-removes cleanly); name!=path resolution is correct; all 8 guard mutants killed on an independent re-run; case L has genuine behavioural killing power and its German control SKIPs loudly rather than passing vacuously; suite 542/0/1. **THE REVIEWER'S STRUCTURAL FIX, which dissolves F1+F2+F4 TOGETHER rather than patching three instances:** re-scope guard 5 from the INDEX to the PRIVATE STORE -- git's refusal is keyed on `.git/worktrees/<bn>/modules/`, so enumerate THAT recursively and require every object reachable from each private store's refs to exist in the corresponding shared store. That drops the `.gitmodules` name mapping entirely, so path-derived names never enter the picture. Plus: make every nonzero git exit inside the guard REFUSE. **UNSURE and not surveyed: whether this fleet actually has nested submodules today.** — **ROUND 3 (`e42341ef`, worktree, UNMERGED): the structural re-scope is BUILT and closes F1, F2 and the three fail-opens; 20 mutants, 19 killed, M20 (pure prose naming `--force`) correctly SURVIVING as the negative control; suite 542/0/1.** The index-scoped guard and its `.gitmodules` name mapping are GONE, replaced by `private_submodule_stores_unsafe`, which enumerates `.git/worktrees/<bn>/modules/` RECURSIVELY, maps each private store to `<common>/modules/<same relative path>`, and requires every object from `rev-list --objects --all <HEAD>` to exist in the shared store. Both escapes were reproduced as verbatim-red fixtures first (nested: `Failed to recurse into submodule path 'vendor/sub'`; index-vs-history: git still refuses the plain removal with ZERO gitlinks in the index, confirming trigger and guard were keyed on different things). Every nonzero git exit inside the guard now refuses and names the step; git < 2.31 is caught explicitly. The six source-grep smuggling channels are closed by a QUOTE-AWARE filter (`tests/lib-shell-code-only.py`) rather than line anchors, self-tested 6/6 smuggled forces caught, 0 false positives on 8 prose lines. **THREE RESIDUALS, and residual (3) may defeat the item's PURPOSE -- this is the one to read:** **(3) OVER-REFUSAL ON THE TARGET POPULATION.** When the main checkout never initialised a submodule, `submodule update --init` inside a LINKED WORKTREE creates ONLY the private store, with no shared counterpart -- and the new guard refuses whenever a private store has no shared counterpart. That is the realistic relay shape (the executor ran the init), so on exactly the repos this item names the hatch may now refuse ALWAYS, reclaiming nothing while carrying all the risk of an automated force. The author asserted the conservative behaviour deliberately and left the call open; a less strict option is to treat objects reachable from the private store's `refs/remotes/*` as safe, since they demonstrably came from a remote. **UNVERIFIED as of this writing -- under adversarial review now, and it decides whether the work is worth merging at all.** **(1) DANGLING OBJECTS, a genuine remaining variant of the SAME class.** The guard asks *reachable from the private store's own refs*; the question that actually matters is *does merged history still resolve after the force*. An object in the private store but unreachable from its refs (a submodule branch reset past a commit whose bump is already merged) is invisible to `rev-list --all`. The one-line strengthening is `cat-file --batch-all-objects`, but that is strictly WIDER than the owner's ruling and would refuse on ordinary rebase debris inside a submodule -- OWNER'S CALL, deliberately not taken. **(2) EMBEDDED `.git` DIRECTORY** (old-style layout / hand-cloned repo): objects live in the tree, not the admin dir, so enumeration misses them and `rm -rf` takes them. Argued out of scope on the grounds that a non-ignored one makes `status --porcelain` dirty so guard 3 refuses, and a GITIGNORED one is expendable under this script's existing force-free policy -- both halves under review. **Also unverified: git < 2.31 is handled BY CONSTRUCTION, never executed** (everything measured on git 2.55.0). — **RESIDUAL (3) MEASURED BY DUMMY-REPO FIXTURE 2026-09-01, and it is MUCH NARROWER than feared: the hatch DOES end the recurrence on the real fleet shape. Recommendation: MERGE.** Two fixtures, both hermetic (`mktemp -d`, `GIT_CONFIG_GLOBAL` isolated, local file remotes), both clean + merged + git refusing the force-free removal with the verbatim string: **(i) the feared shape** -- fresh clone whose MAIN checkout never initialised the submodule, the executor running `submodule update --init` inside the linked worktree. Confirmed the premise exactly: private store EXISTS, shared store ABSENT, and the hatch **REFUSES** (worktree left on disk, branch untouched). **(ii) the real fleet shape** -- main checkout HAS initialised the submodule, then worktree + init. Shared store exists and the hatch **FIRES**: `submodule-force-hatch ... Force-removed`, worktree gone, merged branch deleted. **WHY (i) DOES NOT BLOCK, measured on the actual repos:** 3 of the 4 this item names ALREADY carry shared stores -- `yinyang-puzzle` 1, `llm-from-scratch` 1, `CrispASR` 2, each with its submodules initialised in the main checkout -- so they are shape (ii) and the hatch works there TODAY. Only `Depth-Anything-3` has 0 shared stores and 0 initialised submodules; a single `git submodule update --init` in its main checkout moves it to shape (ii). **AND the shape-(i) refusal is OVER-CAUTION, not a save:** in that fixture the private store's HEAD is `refs/remotes/origin/main` and the object is present in the submodule's upstream, i.e. fully recoverable by fetch -- so the refusal message's claim `no second copy anywhere` is FALSE for exactly this case. That is precisely the treat-`refs/remotes/*`-as-safe refinement the author named and did not take: it would convert shape (i) from refuse to fire, without weakening the worktree-only-object protection that the F1/F2 fixtures pin. **So the honest summary is: safe, correct, and effective on 3 of 4 target repos now; over-strict on a 4th shape that is both rare here and trivially fixable, with a known and bounded refinement available.** Reproduce with: fresh `git clone` of a superproject carrying a submodule; for (i) do NOT init in the clone, `worktree add`, then `submodule update --init` INSIDE the worktree; for (ii) init in the clone first. Then commit in the worktree, `merge --no-ff` it, and run `worktree-retire.sh <repo> <wt> <branch> --expect-merged`. — **MERGED then IMMEDIATELY DE-FANGED, same session. The hatch is on `main` but OPT-IN and INERT in production (`89b54aa8`); reopening default-on is a live owner decision.** Sequence, recorded because the process lesson is the point: the round-3 review landed AFTER the merge (the owner instructed not to hold the merge for it) and returned a flat *not safe default-on, does not end the recurrence*. **FIXED IMMEDIATELY (`8eff0a58`) -- finding 1, CRITICAL and NEW to round 3: guard 3 FAILED OPEN and destroyed uncommitted work.** `$(git status --porcelain 2>/dev/null)` returns EMPTY when git FAILS, and empty read as CLEAN, so an unreadable or corrupt index -- a CRASHED worktree, which this script's own header calls its target population -- took the force path and deleted uncommitted files. Reproduced (`chmod 000` on the worktree index), fixed by capturing the exit status separately, re-verified: refuses, file survives. Two things about how it survived: the merge commit's claim that *every nonzero git exit inside the guard now refuses* was true only of the NEW function -- the swallow lived on in the guard the code itself calls *the only thing standing between this hatch and that bug* -- and NO mutant caught it, because the 20-mutant table replaces the check with `false` rather than making git FAIL. **STILL OPEN, and the reason it is opt-in -- finding 2, a data-loss path ALL FIVE GUARDS PASS:** an executor bumps a submodule, the bump is MERGED, then it AMENDS the submodule commit; the original object goes DANGLING in the private store, invisible to `rev-list --objects --all <HEAD>`, and the force destroys an object a merged commit on main names. Reproduced end to end. Structural cause: the guard asks *what do the private store's refs REACH*, the question that matters is *what gitlinks does MERGED SUPERPROJECT HISTORY name* -- neither set contains the other, so it misses dangling objects AND includes irrelevant remote-tracking refs. **Three candidate answers, none costed, OWNER'S CALL:** (A) `cat-file --batch-all-objects` -- strictly wider, refuses on ordinary rebase debris; (B) a gitlink-derived predicate driven by merged superproject history, which the reviewer notes is a THIRD option the author never weighed and is the only one that matches the actual question; (C) leave it opt-in permanently. **ALSO STILL TRUE:** the hatch is UNREACHABLE from `reconcile-repo.sh:186` (short-circuits on `.gitmodules` and `continue`s BEFORE populating plan_reap/plan_park), so any worktree surviving integrate is still permanent debris -- the recurrence is NOT closed by this work; four more `--force` smuggling channels survive the quote-aware ban (`eval` structurally converts filter-DATA into CODE, so no quote-aware filter can close it); mutant M14 shows loosening the refusal match to a substring survives the whole suite despite the code saying never to; and `M07` is an EQUIVALENT mutant -- `rev-list --all` already includes HEAD, so the `${head_sha:+...}` term is dead code and its justification comment is factually wrong. **REFUTED / clean:** the embedded-`.git`-directory residual (both halves of the author's argument hold), name!=path mapping (byte-identical layouts, nested included), the `GIT_WORK_TREE` override (necessary and safe), and no over-refusal on uninitialised / no-submodule / fully-shared / nested-shared / German-locale. — **OWNER RULED 2026-09-01 (option B): RE-BASE THE PREDICATE ON MERGED GITLINK HISTORY.** The guard must stop asking *what do the private store's refs REACH* and start asking *what gitlinks does MERGED SUPERPROJECT HISTORY NAME, and do they still resolve after the force*. Those two sets are neither subset nor superset, which is the single root cause of BOTH remaining defects, so this closes them together rather than as two patches: the DANGLING-OBJECT data-loss path (an amended submodule commit whose bump is already merged goes invisible to `rev-list --all` while merged history still names it) and the OVER-REFUSAL (remote-tracking refs are consulted at all, so a worktree-only `submodule update --init` refuses a spotless fully-merged tree whose extra objects are ordinary public upstream commits). Gitlinks named by history must be covered even where the submodule is NO LONGER IN THE INDEX -- a later `git rm <submodule>` on the branch must not hide an earlier bump; that was the round-3 F2 escape. Options (A) `cat-file --batch-all-objects` and (C) permanent-opt-in were considered and NOT taken: (A) trades a data-loss hole for MORE over-refusal on ordinary rebase debris, on a hatch whose purpose is to stop refusing. **Weakness accepted at decision time:** (B) is the most work of the three, nobody has costed it, and a fourth review round on this hatch is not free -- three rounds have each found a new escape. If the predicate still cannot answer the question, the honest outcome is to say so, not to ship a sixth guard. **The OPT-IN default is NOT changed by this ruling** -- it stays inert until the predicate lands and is reviewed. — **OWNER RULED 2026-09-01: REVERSE the `reconcile-repo.sh:186` `.gitmodules` short-circuit.** It `continue`s BEFORE `plan_reap`/`plan_park` are populated, so `worktree-retire.sh` is never invoked for a submodule worktree from the reap path and only `integrate.sh` step 9 reaches the helper -- which is why any worktree surviving integrate (handback, crash, abandoned run) became PERMANENT DEBRIS, and how 1.8 GB accumulated on `yinyang-puzzle`. The skip rests on `roadmap:b02f`'s premise that git keys its refusal on `.gitmodules` being in the tree, which is REFUTED BY FIXTURE TWICE: an uninitialised submodule worktree removes CLEANLY, force-free; the refusal needs the submodule POPULATED, and what git tests is `.git/worktrees/<wt>/modules/<path>`. So the skip also excludes worktrees that would reap force-free TODAY with no hatch involved and no risk. Reconcile's job is to stop pre-empting the helper's decision; the helper is the single place that decides what is safe and it already fails closed. **NOTE the immediate win is bounded:** with the a290 hatch still INERT, populated worktrees will reach the helper and be REFUSED -- better than a blanket skip because the refusal is specific and actionable, but not yet a reclaim. The UNINITIALISED case reclaims immediately. **MUST NOT REGRESS:** the additive `unretirable-submodule:` marker has to keep firing every round -- it is additive precisely because a substitutive one would suppress the repo forever (the `id:e7e4` loderite starvation shape). The reviewer's caveat is on the record: this is a policy reversal beyond the narrow hatch, it widens what reconcile touches every round, and it wants its own review rather than a ride-along. — **RECONCILE REVERSAL BUILT (worktree, UNMERGED, under review), and one number changes how to read it: it reclaims NOTHING TODAY.** Surveyed the live fleet 2026-09-01 -- `llm-from-scratch`, `Depth-Anything-3`, `CrispASR` and `yinyang-puzzle` ALL currently have **ZERO relay worktrees** (yinyang's five were disposed by hand this morning; the others have never accumulated any). So this change is **purely PREVENTIVE**, not a reclaim, and its risk today is correspondingly bounded -- there is nothing live for it to misfire on. It matters the next time any of those repos sees relay traffic. State that plainly rather than letting the work read as a cleanup. **What it does:** the `continue` is gone; `.gitmodules` presence now only gates a PREDICTION of the `unretirable-submodule:` marker, computed from the state git actually tests (read `<wt>/.git`'s `gitdir:` line, require `<admin>/modules/` non-empty), and everything else falls through so `worktree-retire.sh` decides. Measured per case: uninitialised+clean+merged -> REAPED FORCE-FREE with no marker (the behaviour the skip made impossible, and the immediate win); populated -> helper invoked and refuses, worktree kept, marker surfaced; dirty -> untouched; plain worktree -> unchanged; marker still fires on round 2 with the repo still yielding a unit (additive, no `id:e7e4` starvation). The b02f-era test was DELETED as its assertion (1) IS the reversed behaviour, with its other three assertions migrated -- the review is checking that migration for silently dropped coverage. **CARRIED RESIDUAL, author-flagged:** a POPULATED worktree with NO `.gitmodules` in its tree (file removed in a later commit while the private store persists) gets a reap planned with NO marker -- the helper still refuses safely, but it is NOT REPORTED, which is the `id:d35a` silent class and precisely the shape that let debris accumulate unseen. Fixture not constructed by the author; under review. — **RECONCILE REVERSAL REVIEWED: NOT safe as written, two HIGH findings, both being fixed on the branch.** **(1) A STARVATION REGRESSION the author's tests never covered -- owner-ruled 2026-09-01: DO NOT PLAN A PARK THE HELPER CANNOT COMPLETE.** An UNMERGED populated-submodule worktree (the handback/crash case the change exists for) planned a park that can never succeed: `worktree-retire.sh` exits 3 without touching the branch, so `relay/orphan/<bn>` never exists, `PARK VERIFY FAILED (id:1af1)` fires every round, and `suppressed re-dispatch` then suppresses the item PERMANENTLY. Measured: `discover-repo.sh` yields **0 units where the parent revision yielded 1** -- item-scoped, but that item is unreachable for good and a single-item repo goes fully dark. That is the `id:e7e4` class arriving by the PARK route rather than the marker route. The ruling SURFACES such a worktree instead: dispatch preserved, marker still reports it, no phantom orphan ref announced. **Weakness accepted at decision time, recorded not solved:** partial work stays on disk AND the item stays dispatchable, so a future executor may start afresh over an abandoned worktree -- that is today's status quo, chosen over keeping the suppression because a SILENT dispatch reduction is the worse failure. **(2) THE PREDICATE WAS NARROWER THAN GIT'S ACTUAL TRIGGER, and every miss was TOTALLY SILENT.** Probed directly: git refuses on the mere EXISTENCE of `<admin>/modules` -- even EMPTY, and even on a repo with NO submodules at all. So both halves of the prediction (the outer `-e .gitmodules` gate and the non-empty `find`) under-fired, and a populated worktree whose `.gitmodules` is absent from its tree (executor inits it, then `git reset --hard` to a commit predating the submodule -- realistic, reproduced) got a reap planned, the helper refused, and NOTHING was reported anywhere. The reap path swallows a non-zero helper exit (`|| true`), so unlike the park path it has NO verify -- the marker was its only reporting channel. Fix: `[[ -d "$wt_admin/modules" ]]` ALONE, which makes predicate and truth coincide, plus resolving a RELATIVE `gitdir:` (`worktree.useRelativePaths`) against the worktree dir, plus a stderr line on any non-zero reap-helper exit so reporting comes from the OUTCOME rather than a prediction. **CONFIRMED GOOD, independently:** all five claimed behaviours; the helper is genuinely invoked (proven via a retire-log string that exists nowhere else); the deleted b02f test's migration is FAITHFUL AND COMPLETE, assertion by assertion, with two of the three migrated cases now STRONGER; blast radius contained, measured 0.28 s for 13 populated worktrees in a round. **Noted, not a defect:** a dirty-only-gitignored uninitialised worktree is now removed, discarding the ignored files -- the documented force-free policy applied consistently, but newly REACHABLE for submodule repos, so build artifacts in those trees became disposable. — **ROUND 4 REVIEW: the predicate is NOT sound. TWO reproduced false-SAFE paths, end to end, all five guards passing, the hatch printing `no merged gitlink left unresolvable`, and recovery failing with the byte-identical `upload-pack: not our ref`. FOUR ROUNDS, FOUR ESCAPES.** **(1) SEVERE, NEW CLASS -- an embedded-`.git`-DIRECTORY submodule is destroyed UNEXAMINED.** Step (c) enumerates only stores under `$admin/modules`; a submodule whose git dir is an embedded `.git` DIRECTORY in the work tree is held by no enumerated store, so the deliberate *no store holds it, already unresolvable, skip* rule fires -- but it was NOT unresolvable: it lived in `$wt/dep/.git/objects`, which the force `rm -rf`s. **This falsifies two shipped claims:** round 3's *REFUTED / clean: both halves of the embedded-`.git` argument hold* is WRONG -- `status --porcelain --ignore-submodules=none` returns EMPTY on such a worktree, so guard 3 passes legitimately; and the `cc8b0b98` mitigation (`! -d "$mroot"` fail-closed *also covers the old-style embedded layout*) only holds when there is NO private store at all -- ONE ordinary submodule creates `$mroot` and the embedded one sails through. Scope boundary *a gitlink no store holds is skipped as already unresolvable* is REFUTED, via a store the enumeration cannot see. **(2) SEVERE -- the `refs/remotes` inference breaks WITHOUT a force-push, and it reaches the MAIN FLEET SHAPE.** The declared residual said the window needs *a rewritten public submodule history AND a merged gitlink pointing into the rewritten part*. Both halves are wrong: an ordinary upstream BRANCH DELETION plus gc suffices, and it reproduces with the SHARED STORE PRESENT -- the shape 3 of the 4 named repos are in. Structural cause: the guard asks *is it reachable from a `refs/remotes/*` tip in THIS private store*, but recovery goes through a FRESH CLONE from the merged commit's `.gitmodules` URL fetching that clone's refspec. Different questions in at least four ways (stale tracking ref -- proven; a second remote not named by `.gitmodules`; an object under a namespace a clone does not fetch; and `refs/remotes/` being a WRITABLE namespace, not an invariant). **This branch converts a hard LOCAL guarantee into a network- and third-party-dependent one.** **(3) Scope note, within the ruling:** a gitlink named only by an UNMERGED branch CAN live only in this worktree's store -- the hatch can leave a parked `relay/orphan/*` ref intact but GUTTED, which collides with the park design's own premise that *the refs ARE the registry* (`id:a4e9`). **COVERAGE HOLES:** `--root` is UNGUARDED and mutant M17 survives the whole suite while being LETHAL (no fixture puts a gitlink in a root commit), yet M14's equivalence argument rests on `--root`; the `ls-tree` fail-open (M24) is unpinned; M30/M33 mutually mask so the `942e6d47` guard has no test of its own. **CONFIRMED GOOD:** steps (a) and (b) could not be broken at all -- root commits, 3-parent octopuses, evil merges and submodule path CHANGES are all handled; M14 equivalence verified by fixture; case S genuinely FIRES (the fleet shape is reclaimed); case V pins the round-3 dangling hole; no over-refusal anywhere. The break is entirely in step (c)'s two escape clauses. **A THIRD undeclared equivalent (M18):** `rev-list HEAD refs/heads/$br` -> `rev-list HEAD` survives and IS equivalent, because guard 4 already proved the branch is an ancestor -- dead code, same shape as the round-3 `${head_sha:+…}` finding, declare or remove it. — **CLOSED OUT 2026-09-01 by owner ruling: MERGE AS OPT-IN AND INERT, correct the overclaiming prose, and STOP -- do NOT fund a round 5.** Both prose corrections landed: the success message now states what the hatch ACTUALLY proves (every gitlink held by an ENUMERATED private store has a shared-store copy or a remote-tracking ref that was current at some past fetch) and NAMES both gaps inline, and the `mroot` comment's false claim to cover the embedded-`.git`-DIRECTORY layout is corrected with the reason it fails (it fires only when there is NO private store at all, so one ordinary submodule defeats it). **Verified inert:** the only reference to `WORKTREE_RETIRE_SUBMODULE_FORCE` outside the script is a COMMENT in `reconcile-repo.sh`; no caller sets it. Suite 545/0/1. **THE REASONING, so a future session does not restart this:** four rounds, four escapes, every one in step (c)'s escape clauses, while steps (a) and (b) survived attack by root commits, 3-parent octopuses, evil merges and submodule path changes. What was being bought is worth little right now -- fleet exposure is ZERO, hand disposal took one paste on 2026-09-01, and **the reconcile reversal (merged separately, `e9dc2e97`) already converts silent accumulation into a LOUD, specific, per-round refusal**, which is most of the value at none of the risk. Note this CORRECTS the round-4 reviewer on one point: it listed *reconcile still cannot reach the hatch* as a reason not to flip the default, which was true when it started and STALE by the time it reported -- the reversal had merged. That sharpens rather than softens the call: flipping the default WOULD now close the recurrence, and would also arm two silent data-loss paths on exactly the repos it targets. **IF ANYONE REOPENS THIS, the two narrowest strengthenings are already named and are both STRICTLY NARROWER than the owner's ruling** (so neither reopens the over-refusal case S guards): make the *no store holds it* skip FAIL CLOSED unless the sha is also absent from every `.git` directory found inside the worktree, and demote the `refs/remotes` clause from *safe* to *safe only if the tip still exists on the configured `.gitmodules` URL at run time*. Also outstanding and cheap: `--root` is unguarded though mutant M17 is lethal, the `ls-tree` fail-open is unpinned, and the `rev-list HEAD refs/heads/$br` branch term is dead code (guard 4 already proved ancestry). <!-- id:a290 -->
