# id:59ea

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(pilot findings 2026-06-12) — (1) integrator step 1 is judgment where it should be mechanics: one Sonnet integrator treated `?? untracked` as clean and proceeded into the zkWhale merge conflict while two others aborted on ` M uv.lock`; spell out the rule in the integrator prompt ("porcelain output non-empty = dirty = abort, untracked included; exemptions listed explicitly" — coordinate with id:bae5's uv.lock carve-out). (2) RELAY_STATUS Queued section double-lists repos: initial queue snapshot entries are never removed when quota-deferred/not-dispatched entries are re-appended (runUnit pushes a second entry; final concat adds a third form) — dedupe by repo, keep the most specific verdict. Both are small relay-loop.js edits + test updates. <!-- id:59ea -->
