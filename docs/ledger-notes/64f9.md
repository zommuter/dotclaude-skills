# id:64f9

Relocated from the ledger by `tools/ledger-continuations.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

> **EDITED 2026-09-08 by relay review run `relay-20260908-231617-32609`** (declared per the
> notes-are-editable convention in `CLAUDE.md`, which requires an edit to be stated in the
> header because the generated text otherwise asserts nothing was changed). Nothing below
> was removed or reworded; the `## Reopened 2026-09-08` section was APPENDED.

## Reopened 2026-09-08

This item was ticked `- [x]` (`35e8983c`) and archived into `ROADMAP.archive.md`
(`0f8569de`) after an execute unit rewrote **4 of 46** over-budget titles. The unit itself
did not claim completion -- its `RELAY_LOG.md` paragraph reads *"Leaving the item open,
unticked, for a follow-up batch"* -- but it returned `64f9` in `worked_ids`, and the
driver-side tick has no way to express "advanced". Ticking the box removed this item's
EXPECTED-RED carve-out, so `make test` reported a REAL failure in
`tests/test_title_rewrite_batch_acceptance_64f9.sh` at assertion (2): *a batch that DELETED
both original titles instead of preserving them in their notes was ACCEPTED*. The item was
restored to `ROADMAP.md` unticked and the suite returned to 610 passed / 0 failed / 5
expected-red. A plain-comment tombstone (deliberately carrying no `id:` marker, so nothing
double-counts) marks the archive line. The underlying tick-semantics defect is `id:963c`.

**Work genuinely landed and NOT to be redone**: the four titles rewritten were `id:8627`,
`id:0640`, `id:83c2`, `id:0220`, all in `TODO.md`, verified by `roundtrip-validate.py`
(directional verdict clean) and `todo-conformance.sh --grammar-lines` (4 findings removed,
0 gained). 42 items remain, and per the unit's own friction note most of those need real
relocation work (author a new `docs/ledger-notes/<id>.md`, move the prose, then write a
faithful short title), materially more expensive per item than the cheap subset taken.

## From ROADMAP

  - **RE-MEASURED 2026-09-04 (post-`id:60eb`): 36 in TODO.md, 10 in ROADMAP.md, 46 total.**
    This is the write-back `60eb`'s done-check requires; the pre-fix figure below is an UPPER
    BOUND and must not be carried forward. Note the two ledgers moved in OPPOSITE directions:
    TODO fell 252 -> 36 as the chrome strip stopped counting `-- detail:` pointers as title,
    while ROADMAP ROSE 5 -> 10 because `id:a580` (`e6261a21`) later anchored that same strip,
    so a keep-token sitting in mid-prose counts as title again. Carrying the stale row forward
    would therefore have understated one side and overstated the other at the same time.
  - **Context (PRE-FIX, superseded by the line above)**: measured 2026-09-02 by the `id:b048` grammar check: 252 in TODO.md, 5 in ROADMAP.md. Title distribution over the 663 items reaching the check: median 174, p75 240, p90 399, max 4097. This is the residue after every mechanical lever is spent -- `ledger-shrink.py` refuses most of them (45 of 68 over-500 head lines refuse as "under 25 chars would move", because the line IS the title), and it is very likely the same population as the 193-items-with-no-bold-run that the `id:0d7c` D3/D4 composition rule already flagged.
  - **Acceptance**: each item head carries a title at or under `LEDGER_ITEM_TITLE_MAX` that still says what the item IS, with the full original prose preserved verbatim in its `docs/ledger-notes/<id>.md`. Nothing is summarised away: shortening the TITLE is not licence to drop content, and an item whose meaning cannot survive the cut is LEFT and reported rather than mangled. Batchable per item; the id, lane, every marker and the checkbox state are untouched.
  - **Why this is not executor-trivial**: rewriting a title is writing, and a bad rewrite silently changes what an item is understood to be. Work in batches with the round-trip validator (`id:ff7c`) run over each batch, which is why this is gated on it.
  - **Done-check**: `todo-conformance.sh` reports zero `grammar-item-title-long` for the batch, the round-trip validator is clean, and a human spot-check of 10 rewritten titles against their notes agrees they still name the same item.
