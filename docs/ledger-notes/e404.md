# id:e404

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(measured 2026-08-27, session f8e5fddf). **INCIDENT 1:** commit `af45bfae` (the `id:ca14` marker) swept `tracker/derived-index.py`, 254 lines of a DIFFERENT agent's in-progress work, into itself. That file's first appearance in history therefore carries an unrelated commit message. **INCIDENT 2, same session, opposite direction:** commit `585e3f78` swept SIX files belonging to the concurrently-running enforcement agent (`CLAUDE.md`, `executor-contract.md`, `hard-lanes.md`, `review.md`, `gather-human-backlog.sh`, plus a new test) into itself. The authoring agent noticed via the '8 files changed' output, ran `git reset --mixed` twice to unwind, and re-committed only its own two files. That recovery was CORRECT and it reported it plainly, but the second reset also dropped the enforcement agent's own ledger commit `4cfc3ced`, which had landed in between. Nothing was lost (recovered and re-committed as `82cf83ce` + `9440c3bb`), but reconstructing what happened needed the reflog. **THE MECHANISM, and why prompting will not fix it:** git's index is PER-REPOSITORY, not per-agent. Two agents working the same checkout share one staging area, so `git add <my-file>` followed by `git commit` by agent A can pick up whatever agent B staged microseconds earlier -- and will, if B staged between A's add and A's commit. **Both agents had an explicit written instruction not to `git add -A`, and both complied**: incident 2's agent staged its two files BY NAME and still committed eight. So this is a race on a shared index, not a discipline failure, and no amount of prompt hardening closes it. **Existing substrate that already knows this:** `relay/scripts/commit-ledger.sh` (id:2147) exists precisely because ledger writes needed to be atomic and scoped, and it flocks per-repo before staging; `git-lock-push.sh` serializes pushes the same way. Neither covers a plain code commit. **`id:3558`** (worktree-per-session with a flock'd merge to canonical) is the recorded real fix for the file-granular attribution race and is the natural parent here; this item is the COMMIT-granular sibling with two measured instances. **Cheap interim options, none chosen:** a flock'd `commit-scoped.sh` wrapping `git add -- <paths> && git commit -- <paths>` on the same lock `commit-ledger.sh` uses; or dispatching concurrent same-repo agents with `isolation: worktree` so they never share an index (note `id:c6c8`: the Agent tool's worktree isolation is CWD-repo-relative, which is safe here since the repo IS the cwd, but is the wrong tool for a cross-repo relay child). **Do NOT respond by serializing all agents** -- parallel same-repo dispatch is load-bearing for this fleet; the fix is isolation or a scoped-commit lock, not less concurrency. <!-- children-of:3558 --> <!-- id:e404 -->
