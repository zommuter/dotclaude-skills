# id:3a09

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From ROADMAP

— three-way, positive-signal detection: UNATTENDED (`RELAY_RUN_ID`/`RELAY_AFK`/a live `id:e149` heartbeat) ⇒ BLOCK; INTERACTIVE (explicit CLI entrypoint AND no unattended signal) ⇒ defer to the existing `permissions.ask`; EVERYTHING ELSE ⇒ AMBIGUOUS ⇒ BLOCK. An absent heartbeat dir is no signal; a heartbeat probe that ERRORED is ambiguous and blocks. An untokenisable command falls back to a conservative regex scan rather than passing through. 20 blocked tree-wide forms (incl. `git -C . checkout -- .` and `&&`-chained), 18 allowed — path-scoped reverts stay allowed BY DESIGN so the guard never pushes anyone toward the tree-wide form. Refusal teaches three alternatives. **INSTALLED BUT NOT WIRED — activation is the owner's**: `settings.json` untouched (mtime 2026-08-19, independently verified) and a test asserts it is unregistered. Promoted + ticked by the integrator; the executor correctly declined to edit ROADMAP per contract rule 5. <!-- id:3a09 --> (archived — see ROADMAP.archive.md)
