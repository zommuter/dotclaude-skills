# id:f544

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(owner-decided 2026-08-13 via `/relay human`). `tests/test_integrate_label_not_strong_ecce.sh` covers parts 1–3 behaviourally but asserts nothing about part 4, because "refuse" had two defensible meanings. The owner picked **(a) non-zero exit / abort the unit** over (b) a loud surfaced no-op: a vacuous review must become a handback the pool RECORDS and stops re-dispatching, rather than a surface that relies on a human reading `RELAY_STATUS.md` — the id:ecce failure mode itself. Add the assertion; the executor no longer chooses. <!-- id:f544 -->
