# id:0968

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

Found 2026-09-01 while fixing the `id:a290` reconcile reversal; PRE-EXISTING, unrelated to submodules, and deliberately NOT fixed there because the owner's ruling named only the unretirable-submodule case and fixing this would have been an unrequested scope extension. **MEASURED on a fresh fixture against the fixed tree:** a dirty unmerged `relay/deadrun-execute-<id>-0` yields `units: 0` and one `PARK VERIFY FAILED`. **Mechanism, and it is the same one:** `worktree-retire.sh` exits 3 at the WORKTREE step for a dirty tree exactly as it does for a submodule one -- it never reaches the branch step -- so the planned `relay/orphan/<bn>` never comes into existence, `PARK VERIFY FAILED ... (id:1af1)` fires every round, and the orphan-suppress step then suppresses the item PERMANENTLY. Item-scoped, so a repo with other open items keeps dispatching, but THAT item is unreachable for good and a single-item repo goes fully dark. This is the `id:e7e4` starvation class reached by the PARK route, which is the same route `id:a290` just had to fix for submodules. **FLEET EXPOSURE TODAY: ZERO** -- surveyed 2026-09-01, no dirty unmerged relay worktree exists anywhere in `~/src`, so this is LATENT, not live. That bounds urgency but not correctness: a dirty unmerged worktree is an ordinary handback/crash outcome and will recur. **Likely fix, by analogy with the ruling already made:** do not plan a park the helper cannot complete -- surface it instead, preserving dispatch, and let the marker report it. Carry the same accepted cost that ruling did (work stays on disk AND the item stays dispatchable, so a fresh executor may start over an abandoned worktree) and do NOT reintroduce a suppression in another form. Relates `id:a290` (the submodule half, fixed), `id:1af1` (the phantom-park text this makes fire by construction), `id:e7e4` (the starvation class). — **SCOPE WIDENED and the EXPOSURE SURVEY RE-RUN 2026-09-01, after a review showed my first survey COULD NOT HAVE FOUND the shapes this item is about.** The original evidence measured only the DIRTY unmerged case, and the `FLEET EXPOSURE TODAY: ZERO` figure was gathered by looking for dirty unmerged worktrees ONLY -- so it was silent by construction about every non-dirty shape. **THREE FURTHER SHAPES MEASURED, none of them dirty:** (1) **LOCKED, unmerged, no submodules** -- `units 1 -> 0` on every round, PERMANENT. A lock is set by `git worktree lock`, never by dirt, so the dirty-only survey could not see it. (2) **A BARE DIRECTORY in the worktree base with no `.git` file** -- `units: 0`, PERMANENT: the helper hits `[[ -e "$wt" ]]`, `git worktree remove` fails with *is not a working tree*, exits 3, and PARK VERIFY FAILED + suppress fire every round. (3) **branch RENAMED out from under the worktree** -- park planned, verify fails, ONE round of suppression, then self-heals on round 2; TRANSIENT, but it strands a `relay/renamed-*` branch. **RE-SURVEYED with the wider shapes: still ZERO fleet-wide** -- 0 locked worktrees across every repo in `~/src`, and 0 stray directories under `~/.cache/relay/worktrees/`. So the conclusion SURVIVED, but it now rests on evidence that could actually have refuted it, which the first one could not. Recording the distinction because *the answer was right* and *the method was inadequate* are different states, and only the second is a reason to distrust the next survey. **This strengthens the case for the fix by analogy already recorded here:** the shapes are unrelated to each other (dirt, locks, stray dirs, renames) and all reach the same starvation through the same door -- a park planned for a worktree the helper cannot dispose of -- so guarding them one at a time is the wrong shape. Do not plan a park the helper cannot complete, whatever the reason it cannot. <!-- id:0968 -->
