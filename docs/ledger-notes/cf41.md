# id:cf41

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

-- loderite filed `id:bc44` to PORT that shape (leading-run emission plus the `_first_lane(head)` vs `_first_lane(keep)` refusal), so our splitHead tail-append stops being documented-not-patched. No action for you; recorded so the two shrinkers converge rather than diverge <!-- id:cf41 -->
