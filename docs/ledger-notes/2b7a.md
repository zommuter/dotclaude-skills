# id:2b7a

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

The leak is NOT the de4a symlink half this time -- `.git` was a regular FILE. It is the cosmetic-dirt half alone, and the defect is a MISSING CROSS-CHECK AT ONE LINE: `worktree-retire.sh:190` gates removal on `git status --porcelain` with NO `git diff` cross-check, which is the EXACT predicate id:3016 fixed in `verify-isolation.sh:190` (same line number, same shape) and never applied to the retire side. Measured in the live worktree: porcelain 93 lines, ALL ` M` annexed PNGs under docs/research/img/card-match/, `git diff --stat` EMPTY, sample-file `git diff` 0 BYTES, 0 untracked, `rev-list --count main..HEAD` = 0 (the id:8b1f clean sized-out shape -- nothing to lose). So the merge landed+tagged+pushed and only worktree-retire refused, leaving the worktree on disk. **ALSO OBSERVED, and it beats the transient-window guess for the shape distribution:** the smudge filter EMITS `warning: unable to convert .git file to symlink that will work with git-annex: createSymbolicLink '../../annex' to './.git/annex': already exists (File exists)` -- i.e. the symlink conversion ATTEMPTS and FAILS here, which is why 7 of 8 annex worktrees carry a normal `gitdir:` file and only 1 is symlinked. Do NOT treat mtime ordering alone as evidence of a transient rewrite-back; a failed conversion produces the same ordering. Worktree left for supervised reconcile, NOT reaped. See id:26ed for the fleet-change note (3 annex repos now, de4a was exercised against 1). **SCOPE RE-MEASURED 2026-09-10 AND IT IS NOT A ONE-OFF: 8 worktrees, 762 MB, across 3 RUNS, ALL code.lawless, ALL IDENTICAL.** Every one measures porcelain=93 / untracked=0 / `git diff --stat` EMPTY / `rev-list --count main..HEAD`=0 -- the same cosmetic-annex-dirt signature with nothing to lose, so all 8 are safely reapable in principle and every one is blocked by the single missing `git diff` cross-check at `worktree-retire.sh:190`. Per-run: relay-20260910-114832-18641 x6, relay-20260910-143324-11403 x1, relay-20260910-173729-12338 x1. `relay-worktree-retire.log` records **92 DEFER events today** over FOUR runs (074741-25003 x8, 114832-18641 x72, 143324-11403 x9, 173729-12338 x3), all `worktree unremovable ... contains modified or untracked files`. The defers are timestamped **18:14**, not 17:37:59 as first relayed. This has therefore been leaking every code.lawless worktree all day, which is the real cost and the argument for fixing the one line rather than reconciling by hand. **DEFER-COUNT CORRECTION, and the method matters more than the number.** The figure stated above as "92 DEFER events today over FOUR runs (074741-25003 x8, 114832-18641 x72, 143324-11403 x9, 173729-12338 x3)" is WRONG and is superseded by: **30 REAL defer events** -- 074741-25003 x2, 114832-18641 x24, 143324-11403 x3, 173729-12338 x1. Of 225 `DEFER` lines in `relay-worktree-retire.log` today, **195 are `/tmp/tmp.*` TEST-FIXTURE lines** and only 30 name a real `~/.cache/relay/worktrees` path. TWO independent counting errors produced the bad figure, both worth knowing because this log invites them: (1) `grep -oE 'relay-[0-9]{8}-[0-9]{6}-[0-9]+'` emits EVERY token occurrence on a line, and these lines name the run token twice (once in `wt=`, once in the inspect hint), so a per-line count becomes a per-occurrence count; (2) the filter `grep -iE 'defer|unremovable|fail|refus'` is broader than `DEFER` and sweeps in fixture lines. **The durable rule for this log: filter on `\.cache/relay/worktrees` to exclude the harness's own `/tmp` fixtures, and extract at most ONE run token per line (`| head -1`).** A raw `grep -c DEFER` over-reports by ~7.5x today. The 8-worktrees / 762 MB / identical-signature measurement is UNAFFECTED -- it was taken from the filesystem, not the log -- and 24 of 30 belonging to one run still means the leak repeats per round, which is the point. **SHARPENED 2026-09-10 BY AN ATTEMPTED REAP -- it is a TWO-STATE DEADLOCK, not two bugs firing in sequence, and the earlier characterisation on this item understates it.** Attempting the owner-authorised reap of `...-114832-18641-execute-f0de-0` established, by measurement: (1) `--discard-residue --ack` WORKS and cleared all 93 cosmetic entries (residue archived outside any repo at `~/.cache/relay/discarded-residue/`), but the discard itself CONVERTED `.git` from a regular file into a SYMLINK -- so the id:de4a normalization at step 0, which had already run and correctly found nothing to normalize, was rendered stale by a LATER step of the same script. (2) Re-running then normalized symlink->file at step 0 and failed at :190 with `contains modified or untracked files` -- the 93 entries were BACK. (3) The mechanism: **ANY git invocation in this worktree trips `filter.annex.process`, which re-creates the symlink.** Proven directly -- `stat` reported `regular file`, a plain `git worktree remove` then failed `'.git' is not a .git file, error code 10`, and `stat` afterwards reported `symbolic link`. The removal command trips the filter and then refuses its own resulting state. So the two states ALTERNATE and NEITHER is removable: `.git`=file gives porcelain 93 and :190 refuses; `.git`=symlink gives porcelain 0 and git's validation refuses with code 10. **This is why an ORDERING fix alone cannot work** -- there is no point at which the normalization survives to the removal, because the removal itself undoes it. A fix must either make the removal not invoke a filtered git read, or dispose without `git worktree remove`. `--force` does NOT help (de4a's own note: validation precedes force) and was in any case DENIED by the permission guard when tried here. **WHY THIS REPO AND NOT OTHERS:** de4a reclaimed 3 zkWhale worktrees and quovadis logged 6 clean normalize+remove pairs today, so the filter does NOT fire everywhere -- it fires here because code.lawless's index is stale against 93 annexed PNGs, which keeps giving the filter work on every invocation. That difference is the thing a fix has to be tested against, and it means a green run on a non-annex or clean-index repo proves nothing. STATE LEFT BEHIND: 1 of 8 has had its residue discarded (recoverable from the archive path above) and is still on disk with its branch; the other 7 are untouched. 0 commits ahead on all 8, so no work was at risk at any point. NOT REAPED -- the only remaining route is directory removal plus `git worktree prune`, which is the technique the owner prohibited 2026-08-26 ([[no-route-around]]), so it needs his explicit say rather than an inference from "reap them". **CORRECTION AND CLOSE 2026-09-10. THE 'TWO-STATE DEADLOCK' PARAGRAPH ABOVE IS WRONG -- disregard it.** It claimed the two failures ALTERNATE so that neither `.git` state is removable. They do not: with a proper `gitdir:` file the code-10 validation PASSES and only the dirt blocker remains, i.e. the failures are SEQUENTIAL. That claim came from a CONTAMINATED MEASUREMENT -- the diagnostic that reported `.git` as a regular file ran `git status` on the same shell line, which tripped `filter.annex.process` before `git worktree remove` was reached, so the removal appeared to be what re-created the symlink. It is not; ANY filtered git read is. The owner corrected the framing directly ("you can replace the .git symlink by the .git file a worktree usually sets up, we've had this thousands of times before"). **ROOT CAUSE, FIXED as `id:1a5c` (`ecb6c22c`):** `worktree-retire.sh` ran de4a's normalization ONCE at step 0, but the residue steps run `git checkout -- .`, the annex filter re-creates the symlink as a side effect, and the removal then dies on its own validation -- so the normalization was stale by the time it mattered. It is now an idempotent function called twice, the load-bearing call being immediately before the removal with no filtered git read between. **ALL 8 WORKTREES REAPED (762 MB -> 0)**, force-free through the tool, `branch -d` never `-D`, residue archived outside any repo at `~/.cache/relay/discarded-residue/` and recoverable. **IS IT A GIT OR ANNEX BUG? Neither -- an INTEROP gap where both sides are reasonable.** annex deliberately symlinks a linked worktree's `.git` so annexed relative symlinks (`../.git/annex/objects/...`) resolve inside the worktree; git's worktree-remove validation requires a FILE and runs BEFORE `--force`, so force cannot override it, even though a symlink resolving to the correct admin dir is functionally equivalent. annex itself calls the resulting status "only a cosmetic problem affecting git status; git add, git commit, etc won't be affected" -- it knows, and git has no way to hear it. The stricter-than-necessary validation is the half worth an upstream report if anyone wants to file one. **WHY THIS REPO ONLY:** the filter only has work when the index is stale against annexed files (93 PNGs here), which is why zkWhale's 3 worktrees and quovadis's 6 normalize+remove pairs succeeded -- so a green run on a clean-index repo proves nothing about this path. The `:190` porcelain-without-`git diff` predicate this item was originally filed for is a SEPARATE and still-open concern: it is what made these read as "dirty" rather than structurally stuck, and `--discard-residue` was needed to get past it. Reopen a fresh item for that if it recurs; the LEAK this item tracked is closed. Done on 2026-09-10. <!-- id:2b7a -->
