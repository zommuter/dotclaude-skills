# id:4cd4

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

Observed live 2026-09-08 (review run relay-20260908-174448-4421): running two files in one invocation, `test_reconcile_retirable_residue_ba95.sh` emitted `VIOLATION: ... RUNNER ERROR on rev 13445619...: OSError: [Errno 39] Directory not empty: /tmp/negcase-.../tree/.git/objects` and `TOTAL: 1 negative case(s) that do not fail for the declared reason`. Re-run alone, the SAME case verified clean (green-now OK, red-there OK). Mechanism: `sandbox_tree` is a `tempfile.TemporaryDirectory` whose `rmtree` races a git process still writing the scratch repo's object dir; the `except Exception` at line 585 wraps the whole `with` block, so a CLEANUP failure is caught AFTER the verdict was computed and the verdict is discarded. Two costs: a reviewer reading the TOTAL line concludes a sound test is vacuous, and an operator who learns to re-run past VIOLATION lines will eventually re-run past a real one. **Fix**: separate the channels -- a RUNNER ERROR is not a finding about the test; report it distinctly, exclude it from the violation TOTAL, and make cleanup failures non-fatal to an already-computed verdict. **Acceptance**: a forced cleanup failure leaves the case's green-now/red-there verdict reported and the violation count unchanged. **Done-check**: `tests/test_negcase_runner_error_channel_4cd4.sh` green; `make test` green. <!-- id:4cd4 -->
