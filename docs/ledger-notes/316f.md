# id:316f

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

Now ported by relay-core id:587a (parity back to 122/122). CAVEAT: relay-core's DEPLOYED ~/.local/bin/relay-core is still the 8 Jul build, so post-fix mismatches are still accruing from a stale binary and must NOT be counted as un-triaged divergence. This unblocks D6's measurement gate. <!-- id:316f -->
