# id:3af4

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(found 2026-08-27 while promoting the em-dash migration seams). The new item `id:55c7` was reported as HAVING a TODO twin when it does not: the bare token `55c7` happens to appear in `TODO.md` prose as a DIFFERENT, unrelated loderite id, and the check matched it. Consequence is the safe direction here (a WARN is suppressed, not raised), but it is the same unanchored-match class as two defects fixed today: rule 3(g) matching a backtick'd prose mention on the raw line (`7a86cdb3`), and the inbox twin check before `id:3743` anchored it. The fix is the same shape as `id:3743`'s: anchor the twin match to an OWNING marker form (`<!-- id:XXXX -->` / `<!-- routed:XXXX -->`) rather than a bare-token grep. Cheap, mechanical, and it removes a false clean from a lint the relay reads. <!-- id:3af4 -->
