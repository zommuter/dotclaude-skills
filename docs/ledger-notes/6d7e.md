# id:6d7e

AUTHORED note, not relocated by `tools/ledger-shrink.py` -- the verbatim-reproduction claim
does not apply to it (see the "Authored notes" section of `docs/ledger-notes/README.md`).

APPENDED 2026-09-09 by the handoff that promoted this item to `ROADMAP.md` (run
`relay-20260909-205831-5121`): the "Owner ruling" and "Collateral" sections at the end.
Nothing above them was altered.

## The residue `id:5295` closed over

`id:5295` shipped its headline and I verified it independently -- see "What DID land" below.
This item is the clause of its acceptance that was **not** met, recorded so the drop is on
the record rather than absorbed silently.

`id:5295`'s acceptance, as written in `docs/ledger-notes/5295.md` before dispatch, read:

> Verified against a fixture reproducing the nested shape with >1 sibling, so a marker that
> matches several transcripts still fails loudly rather than picking one.

`relay/scripts/self-transcript.sh:239` does the opposite, and did before the fix too:

    echo "self-transcript.sh: ${#candidates[@]} transcripts matched — choosing the most
      recently modified ($winner). Candidates: ${candidates[*]}" >&2

A stderr warning and a pick. Exit status stays 0, so `context-budget.sh --self` accepts the
chosen transcript and returns a real-looking verdict computed from **possibly the wrong
child's byte count**. Only the zero-match branch (`:218`) exits non-zero.

## This is not theoretical -- it fired inside the run that closed the item

Session `88e0cae7`, run `relay-20260909-185356-12943`. Exactly one child emitted the
runtime multi-match line with an EXPANDED path (the un-expanded `$winner` spelling appears
in two other transcripts only because those children were READING the script's source, and I
initially miscounted those as real -- they are not):

    agent-a7e1a9161a4073efc  [handoff:dotclaude-skills id:5295]

The handoff child promoting `id:5295` resolved its own transcript ambiguously, under the
defect the item exists to fix, while writing that item's ROADMAP entry.

CLAUDE.md already names the cause, added by this run's review child: the tilde-form marker is
not unique because `provision-worktree.sh`'s own child prompt (`id:34b7`) carries the same
worktree path, so a per-unit marker matches at least two transcripts. The `id:5295` fix
converted "matches 0" into "matches 2". Both are wrong answers to "which transcript is mine";
the second is merely less visible, because it returns a number instead of an error.

## What DID land, verified independently rather than taken from the self-report

- `639c6ffa` adds a tilde<->absolute normalization anchored to the CALLER'S OWN `$HOME`
  (a different `$HOME` still fails), additive, leaving the exact-marker and `id:c219`
  basename paths untouched. Read the diff, not just the message.
- `make test`: 623 passed, 0 failed, 0 errored, 3 expected-red. Run here, not reported.
- The guard produced its FIRST REAL VERDICT: the run's `id:b437` handback reads
  `context-budget.sh --self returned verdict=handback (est_tokens=155939, bytes=623756)`
  before any edit -- rule 2c working exactly as designed, where hours earlier the same call
  returned `unknown`. That child's resolution was NOT ambiguous (checked per-agent), so those
  numbers are trustworthy.

So the close is defensible on the headline and the item should NOT simply be reopened as if
nothing shipped. What must not happen is the clause disappearing.

## Still-open evidence, after the fix

`transcript-shape-preflight.sh` re-run post-fix, on 752 transcripts:

    (A) coverage OK — resolver sees all 752 transcript(s) ...
    (C) SKIPPED — no uniquely-identifying relay worktree marker found among 752 transcript(s).
    EXIT=0

(C) still cannot find a UNIQUELY-identifying marker -- which is this item -- and still exits
0, which is `id:5f6a`. The two compose: the detector that would catch this is itself reporting
green, so nothing would have surfaced the residue if it had not been looked for by hand.

**Acceptance**: `self-transcript.sh` with a marker matching >1 transcript exits non-zero and
names every candidate, rather than choosing one. Callers that legitimately want the
most-recent behaviour must opt in explicitly (e.g. `--allow-ambiguous`), so the default is an
identity check. `context-budget.sh --self` must surface that non-zero as `unknown` (its
existing fail-open path), never as a verdict computed from a guessed transcript.
Consider ALSO making the marker unique at source -- a per-unit nonce in the dispatch prompt
would make ambiguity impossible rather than merely loud -- but that is a `relay-loop.js`
change and a separate lane call, not assumed here.

**Done-check**: extend `tests/test_self_transcript_tilde_marker_5295.sh` (or a sibling) with a
fixture where TWO transcripts carry the same worktree marker; assert non-zero exit, assert
both candidate paths are named, and assert `context-budget.sh --self` reports `unknown`
rather than a number. Assert the single-match path still exits 0 with the right file, so the
fix cannot be "always fail". `make test` green.

**Context**: `relay/scripts/self-transcript.sh:239` (the pick-one branch) and `:218` (the
zero-match branch that DOES exit non-zero -- the asymmetry is the bug),
`relay/scripts/context-budget.sh` (`--self` fail-open),
`relay/scripts/provision-worktree.sh` (`id:34b7`, the colliding second prompt),
`docs/ledger-notes/5295.md` (the acceptance clause this item carries forward).

## Owner ruling (2026-09-09) -- the `id:5295` REVIEW_ME box is DECIDED, branch (b)

`REVIEW_ME.md`'s open box against `id:5295` put two defensible readings to the owner and
refused to choose: **(a)** keep the newest-mtime tie-break, on the grounds that the id:34b7
provisioner always finishes before the unit child starts; **(b)** exit non-zero on any
multi-match, which fails open to `unknown` and is honest, at the cost of disabling rule 2c
for every pooled child until a genuinely unique marker exists.

The owner ruled **(b)**, by injecting this item as the high-priority unit of run
`relay-20260909-205831-5121` with the substance stated explicitly: the resolver *"must exit
non-zero and name every candidate, with any most-recent behaviour behind an explicit opt-in
flag, so `context-budget.sh --self` reports unknown rather than a number computed from
another child's transcript."* His stated reason is the one the box could not weigh for him:
**a wrong byte count is worse than `unknown` because it looks authoritative**. So the
acceptance above is now a ruling, not a proposal, and (a) is closed -- do not re-litigate the
tie-break as a judgement call.

The cost branch (b) carries is real and unmitigated by this item: with the marker
non-unique, rule 2c yields `unknown` for every pooled child until the marker is made unique
at source. The note's own Acceptance already flags the candidate remedy (a per-unit nonce in
the dispatch prompt, a `relay-loop.js` change) and explicitly does NOT assume it. That
remains a separate lane call.

## Collateral: two landed test cases pin the OLD behaviour and will die, not fail

Measured on this tree 2026-09-09, and the reason this is not a one-file change:

- `tests/test_self_transcript_wiring_ff30.sh` case 6 ("ambiguity policy") and
  `tests/test_self_transcript_workflow_nesting_c219.sh` case 7 ("ambiguity stays LOUD") each
  assert `got == $F_DUP` -- the newest-mtime winner -- via an UNGUARDED `got="$(...)"` under
  `set -euo pipefail`. A non-zero resolver exit kills each file at that line, before any
  `FAIL:` is printed. Neither file carries a `# roadmap:` header (both say so on purpose:
  they are defect-fix tests), so neither can be swallowed as EXPECTED-RED -- they are real
  suite failures.
- The fix for both is to pass `--allow-ambiguous` and keep the assertions as they stand:
  the newest-wins pick and the all-candidates-named report are exactly the guarantees the
  opt-in flag must still provide, so the conversion doubles as that flag's regression cover.
  Deleting the cases would discard the only coverage the opt-in path has.
- `tests/test_self_transcript_tilde_marker_5295.sh` case 8 is already tolerant (its
  newest-wins assertion sits inside `if (( rc == 0 ))`, and its loudness assertions hold
  either way), so it stays green under the fix. Its in-file instruction *"Do not change the
  tie-break to make this pass"* was correct when written and is now superseded; the handoff
  updated that comment in place rather than leaving an instruction that contradicts the
  ruling.
- `relay/scripts/transcript-shape-preflight.sh` is NOT affected: its (C) probe deliberately
  selects a marker with `hits == 1` and skips otherwise, so it never exercises the
  multi-match path.
