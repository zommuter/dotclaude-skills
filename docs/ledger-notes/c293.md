# id:c293

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

The rename table and the 2026-09-09 corrective banner make bare `[HARD]` the canonical capability-keyed POOL lane (`[HARD - pool]` -> `[HARD]`), and `human.md:152` agrees. But the "Canonical marker set (the contract both consumers parse)" block was never updated for that rename and still reads `[HARD]` (no recognized lane) -> untagged -> LOUD reject. Consequence, measured 2026-09-10: `gather-human-backlog.sh:74,:492` buckets bare `[HARD]` as `hard_pool` while `project_manager/scan.py:299,:333` buckets it `untagged`, and a new project_manager guard test now FREEZES the scan.py reading. The SSOT contradiction is the cause and should be fixed regardless of which consumer wins. <!-- relates:b466 --> <!-- id:c293 -->
