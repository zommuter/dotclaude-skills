# id:11b1

AUTHORED note, not relocated by `tools/ledger-shrink.py` -- the verbatim-reproduction claim
does not apply to it (see the "Authored notes" section of `docs/ledger-notes/README.md`).

Routed from the shared inbox as `routed:fa6d` (from code.lawless, 2026-09-09) and ingested into
`TODO.md` by relay review `relay-20260909-205831-5121`.

## The finding

Running Workflow/pool children ARE addressable mid-run. The address is the child's RAW agent id,
read from `subagents/workflows/<wf>/agent-<id>.meta.json`, passed to `SendMessage`.

Three things that do NOT work as addresses, and one trap:

- The child's LABEL, the workflow RUN id, and the TASK id all fail as addresses.
- `ListAgents` does NOT list such a child until you have sent to it -- the registry is populated
  BY the send. So the child's absence from `ListAgents` is not evidence that it is unreachable.
  That is the trap: the obvious probe reports the opposite of the truth.

## Evidence

Verified end-to-end on relay run `relay-20260909-212820-29294`, child `a43636e22d75d9982`
(hard unit `id:f5a2`), which replied in-band.

Confirmed NOT a fork: 11 agent transcripts before and after the send, the same transcript file
grew 617k -> 883k, and no journal result record appeared.

## What is NOT proven

The blast radius of the resume on the Workflow's own `await`. Do not document this channel as
safe to use until that is measured.

## The work

`docs/relay.md`'s new operator section and the `inject.sh --prompt-only` channel both understate
what is reachable. Correct them -- but only as far as the evidence above goes, keeping the
unproven `await` interaction explicitly unproven rather than quietly implying the channel is
supported.

## CORRECTIONS 2026-09-09, supplied by the filing session itself

Both corrections come from `code-lawless-3b`, who filed this item and then retracted parts of
it unprompted. Recorded as provenance, not credit: this is evidence gathered by another
session, and an item reading as though dotclaude-skills measured it would overclaim.

**1. "docs/relay.md understates what is reachable" -- WRONG, and the accurate form is weaker.**
`grep -n -i 'sendmessage|unreachable|reach.*child' docs/relay.md` returns NOTHING. That file's
operator section is SILENT on reachability; it never discusses addressing children in either
direction. So this is an OMISSION in the relay's documentation, not a misstatement in it. The
surviving point: `inject.sh --prompt` is the only channel the relay names anywhere, and it
costs a whole dispatched unit and lands only at the next discovery round.

**2. "confirmed NOT a fork" -- one word carrying TWO questions, which the original line merged.**

  (a) Did a second AGENT get spawned? **NO** -- measured and standing: 11 agent transcripts
      before and after, 11 metas, exactly one `hard:code.lawless id:f5a2` meta, no new file,
      the existing transcript growing in place.
  (b) Did the resume start a second EXECUTION LEG sharing that one agent id, transcript and
      worktree? **UNRESOLVED.**

The original sentence asserts (a) using the word "fork", in a context where every reader takes
it for (b). For a ledger entry the natural reading is the only one that matters, so it read as
settling an open question.

**The reproduction trap, which is the reusable part:** checking for a new agent file comes back
CLEAN and proves NOTHING about (b). Someone re-running this in a month would look for a forked
transcript, find none, and close (b) on evidence that never addressed it. This is the same
class as the other five instances collected on 2026-09-09 -- see the METHOD WARNING in
`docs/ledger-notes/3846.md`.

**Surviving evidence for (b), at its real strength, neither more nor less:** the child found
files in its worktree that it did not author, ALREADY STAGED in an index it never ran
`git add` on, and its own write-up was blocked because the path already existed. No dispatched
second child can account for that, since only one opus meta exists. Unexplained, not proof.

**Retired evidence, so nobody rebuilds on it:** an earlier claim that two differing progress
counts (20/33 vs 41/63) proved two concurrent working sets. They do not -- 20 DISTINCT
signatures covering 41 OCCURRENCES is one execution caught half-finished, in two different
units of count.

**Unresolved and NOT to be assumed either way:** whether the resume reattaches to the live run
or starts a second leg. The settling experiment is a throwaway Workflow with an assertion on
the parent's await -- never on a live pool. Until then this is "works, blast radius unverified",
which is why it is NOT documented as an operator channel.
