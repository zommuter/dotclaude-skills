# id:dcf3

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(2026-08-27, `children-of:4a5c`, gated for EVALUATION purposes on the `id:a08d` amendment). Consumes the `id:2bb1` intermediate JSON that the Vikunja and Plane adapters consume, so it is field-comparable by construction; stdout-only, writes no file and no tracker. Three views: `keystones` (open items ranked by transitive OPEN fan-out over typed `gated-on:` edges, i.e. `id:c3f6`'s ranking over typed edges rather than the prose-grep that item forbids), `stale-gates` (gate already closed, gate token owned by nobody, or gate token ambiguous fleet-wide -- all reported LOUDLY on stderr in every output mode, `id:4347`), and `child-edges` (the normalised parent/child edge set). Deliberately NOT folded into `relay/scripts/control-board.sh`: that is the pre-registered CONTROL arm (`id:8066`) and a control that grows the treatment's capabilities stops being a control; it is also repo-grained over classify verdicts where this is item-grained over typed edges. First real run over this repo alone: 1551 items, 685 open, 93 gate edges, 6 unresolved, 26 stale-gate rows. Test `tests/test_tracker_derived_index.sh`. The ITEM is unticked because the arm's ADMISSION is the owner's call (`id:a08d`), not because the code is unfinished. <!-- children-of:4a5c --> <!-- id:dcf3 -->
