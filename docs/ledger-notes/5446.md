# id:5446

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

Today exit 7 surfaces as `MECH-ERROR exit=7`, `sliceLedgerForUnit` returns null, and the loop falls back to the UNSLICED brief. That is the safe direction (loud, and the unsliced brief still carries the pointer), but it degrades the dispatch to a full-ledger prompt that the prompt-size gate may then refuse -- so a single dangling pointer can stall a repo. Differentiating exit 7 from a genuine mechanical error is a caller-side decision. Surfaced by `id:2ee1`. <!-- gated-on:2ee1 --> <!-- id:5446 -->
