# id:fc01

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(framed 2026-08-27, `children-of:4a5c`; do NOT change any ledger schema to answer it -- this item is the question, not the fix). Cartulary ratified **A26** on 2026-08-14 (`~/src/cartulary/ARCHITECTURE.md` 3d, item `id:74ce`): a `gated_on` edge carries a REASON plus a DATE in an edge-keyed SIBLING field, index-aligned parallel lists explicitly rejected because a re-sort silently re-pairs reasons with the wrong edges. **No fleet markdown ledger writes one.** MEASURED on this repo 2026-08-27 via `tracker/derived-index.py`: 93 typed gate edges, of which 26 are stale or dangling and 6 do not resolve at all, and not one carries a machine-readable reason -- the rationale, where it exists, is prose in the item body and is not attached to the EDGE. So today every arm answers "what is blocked" and no arm answers "why", which makes this the sharpest axis in the pilot: it is the one capability the owner's own capability reframe named (*"better dependency (gated-on) tracking"*) that no arm has yet been asked for. **The real question is which of two things it is**, and the pilot can decide it: (a) a SUBSTRATE gap, if some arm makes an edge-keyed attribute expressible and queryable, or (b) a LEDGER-AUTHORING gap that no tracker fixes, if every arm can only attach the reason to an ITEM and the edge stays bare. Priors, stated so the test can refute them: Plane and Vikunja both model a relation as a bare typed link between two issues with no attribute payload, so both would have to launder the reason into a comment, a label, or the description -- which is precisely the `id:857d` collapse the pilot already forbids; the derived index inherits whatever markdown authors, so it is bounded by (b) as well until a marker grammar exists. That makes a NEGATIVE result the likely one, and a negative result is the valuable one, because it says the migration cannot buy this capability and the fix belongs in the authoring grammar (cartulary `id:74ce` is the ratified reference design, and its own open question -- how an edge-keyed reason keys a CROSS-REPO gate -- is live here too, since 1 of this repo's gate edges already crosses repos). **RECOMMENDED TEST, pre-registered before any arm is run so the outcome cannot be re-read afterwards:** author a fixture with three gate edges each carrying a distinct (reason, date) pair, push it through all three arms, and require each arm to recover the EDGE-to-(reason,date) PAIRING from its own emitted payload under `id:90f2`'s non-vacuity rule. Pass = the pairing survives; fail = the reason is recoverable but not attached to a specific edge, which is the parallel-list failure A26 already rejected on paper and would then be observed. Report the result as a separate row in the pilot report, never folded into the usage proxy. <!-- children-of:4a5c --> <!-- id:fc01 -->
