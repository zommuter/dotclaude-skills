# id:87a7

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

MEASURED 2026-08-21, both modes on the SAME run `relay-20260821-174757-32436`: full mode reports `STARVED: loderite classifies execute with 6 actionable [ROUTINE] item(s) (57d1,6612,084f,c8a7,55a4,5adb), yet this run dispatched NOTHING for it`; `--fast` reports `clean: ZERO-YIELD-RUN, STARVED` — i.e. it affirmatively declares the class clean. CAUSE is structural, not a bug in the flag: `--fast` reads RECORDED verdicts from the event log, and a STARVED repo is BY DEFINITION one the run never recorded a verdict for, so the evidence `--fast` relies on is exactly the evidence a starved repo lacks. **WHY THIS IS DANGEROUS RATHER THAN MERELY LIMITED:** the recommended wiring is a BLOCKING end-of-run hop, and a full 50-repo live re-classification measured **2:45 wall** under load (individual `classify-repo.sh` calls 0.36 s unloaded → 93 s for loderite at load 44) — so `--fast` is precisely what a future wiring will reach for to keep the round tail short, and it will silently disable the detector's primary class while still printing a confident `clean:` line. That is the `id:4347` shape with the failure mode INVERTED: not a detector nobody reads, but a detector that reads clean because it did not look. **FIX OPTIONS (owner's call):** (a) `--fast` must NOT print `clean: STARVED` — it should print `STARVED: NOT CHECKED (--fast)` so absence of evidence never renders as evidence of absence; (b) make STARVED cheap enough to always run — the input it needs is "does this repo have open actionable items", which `unpromoted-scan.sh`/a ROADMAP grep can answer without a full classify; (c) run STARVED only for repos with NO event trace in the run (a small set — 3 of 53 today), which is both the correct population and nearly free. (c)+(a) together look strongest but this is not mine to decide. Related `id:4347`, and the timing-needs-load rule (the 2:45 figure is load-dependent, not intrinsic). <!-- id:87a7 -->
