# id:2e5c

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(observed 2026-07-14) — the no-subject `/meeting` audit adds a synthetic Class-3 candidate for open REVIEW_ME boxes and treats a box's "RUN /meeting on id:X" text as a live request. A box can go STALE: id:9fdb's 2026-07-13 "RUN /meeting" box survived a 2026-07-14 TODO re-lane that settled the item (`[INPUT — meeting]`→`[HARD — pool]`, "meeting already held, mtg 1239 D4"), because nothing reconciles a REVIEW_ME box against the referenced id's CURRENT decision/lane state in TODO/ROADMAP → a redundant-meeting recommendation (caught this session only by reading the ratified source). The id:37ab classify fix closes the CLASSIFIER vector but NOT this REVIEW_ME-box vector. **Directions:** (a) when surfacing an open box that references `id:X`, cross-check X against TODO/ROADMAP — if X is now `[x]`, or re-laned off a meeting lane, or carries a DECIDED/RE-LANED note dated after the box, flag it `possibly-stale` and DON'T recommend a meeting off it; (b) mechanize a box↔ledger freshness lint in `orphan-scan.sh`; (c) accept the reviewer-prune cadence as the only guard + document it. Observe-first: FIRST observed instance — prefer the cheap surface-flag (a) over building (b) now. Instance of the "check a derived doc against its ratified source before a pre-registered rule fires" heuristic. **OWNER RULING 2026-07-16 (relay human): PRIORITIZED — the observe-first gate is now DISCHARGED; this is no longer a single instance.** A 5-classifier `/relay human --all` sweep produced **hard recurrence evidence across 4 repos in one pass**: of 6 tier-(a) auto-answers, **3 were stale-box closures rather than verdicts** — cyclotomic-projection `REVIEW_ME:122` asserted "ROADMAP is DRAINED (0 open items)" when `grep -c '^- \[ \]' ROADMAP.md` = **2** (and "11 un-promoted" when the real count was 13); collaib `REVIEW_ME:254` asserted "14 orphan header stubs" when only **3** headers exist and 4 of its 14 listed ids have zero occurrences; cyclotomic `REVIEW_ME:148` asked to confirm a default-flip the owner had already REVERSED and shipped (id:e52d). Two classifiers independently named the pattern unprompted ("reviews are minting boxes faster than they prune them"). **The measurable shape is that a box asserts COUNTS/STATE that are cheaply re-derivable and go wrong silently** — which is what makes direction (b) mechanizable: a lint that re-checks a box's own asserted counts against the live ledger would have caught all 3. Recommend (a)+(b) together now rather than (a) alone; (c) is refuted — the reviewer-prune cadence is exactly what failed here (these boxes survived multiple review passes). Sibling vector: the 2026-07-16 sweep also found a box's file:line SHIFTS under a concurrent review rewrite (yinyang `3709` moved :45→:75 mid-sweep), so any mechanized check must anchor on the id, never a line number. <!-- id:2e5c -->
