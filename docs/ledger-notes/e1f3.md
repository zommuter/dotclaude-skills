# id:e1f3

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(scoped 2026-08-27, gated on the `id:a08d` amendment being ratified; do not build before that ruling). `id:90f2`'s binding rule is that equivalence is recovered from each adapter's OWN emitted payload, never re-derived from the source, or the check is vacuous. The derived index has no server payload to read back, so the third arm's recovered graph is defined as `tracker/derived-index.py --json` output over the SAME intermediate document, which is a genuine emitted artifact and not the source document. Concretely: (1) `tests/test_tracker_adapter_equivalence.sh` today compares two recovered graphs -- add a third producer and assert the SAME triple-equality, not two pairwise ones, so an arm cannot pass by agreeing with only one sibling; (2) the comparison key must be the per-view triple `todo_status`/`roadmap_status`/`review_status` plus edges, per `id:857d`, since the derived index is the one arm that can carry the triple losslessly and would otherwise flatter itself against two arms that cannot; (3) the honest asymmetry to pre-register: Plane and Vikunja both lose `link_kind` and the derived index does not, so a naive three-way byte-equality would FAIL on the index's extra fidelity -- the contract must compare on the INTERSECTION of expressible fields and report the surplus separately, exactly as the two-arm contract already reports the shared `link_kind` loss. **FIXTURES: the existing ones are sufficient for the mapping half and NOT for the interesting half.** `tracker/fixtures/repo-alpha` + `repo-beta` already carry cross-ledger drift, a collision, and both child-marker spellings, which is what the equivalence key needs. They carry ONE gate edge and no stale, dangling, ambiguous, or cross-repo gate, so they cannot exercise the queries that distinguish the arms; those cases exist today only in `tests/test_tracker_derived_index.sh`'s own mktemp fixtures. Promote those into committed fixtures, or the three-way test measures only the mapping the two-arm test already proved. <!-- children-of:90f2 --> <!-- gated-on:a08d --> <!-- id:e1f3 -->
