# id:d8b3

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From REVIEW_ME

The TODO line you filed named two candidate stores as open ("the durable per-repo state the heartbeat/claim files already use, or `relay.toml`"). Promoting it to ROADMAP this review needed one, so the spec narrows to the `event-append` JSONL substrate on the reasoning that `relay.toml` is OWNER-FACING configuration -- `toml-set` is your durable-judgement surface (`bump_policy` et al.) and an executor writing a runtime escalation counter into it would blur that boundary, and `relay-state-write.sh` already enum-validates specific keys on the writer side. **This is a reviewer RECOMMENDATION, not a settled decision.** If you prefer `relay.toml`, the red spec `tests/test_zero_commit_accumulator_durable_d8b3.sh` still passes unchanged (it asserts an out-of-tree path, not a named store) and only the ROADMAP item's Store bullet needs editing. **UPDATE (review run relay-20260901-101120-32404):** the executor SHIPPED the `event-append` recommendation before you answered -- contract v17 -> v18, counter now at `~/.config/relay/relay-events.jsonl` via `relay-state-write.sh event-append`, `relay.toml` explicitly ruled out at the point of use. Verified the named subcommand actually exists (`relay-state-write.sh:179`), so this is not the prose-that-no-ops class. Your confirmation is therefore RETROACTIVE: if you prefer `relay.toml`, the change is a contract-prose edit plus the ROADMAP Store bullet, and `tests/test_zero_commit_accumulator_durable_d8b3.sh` passes either way. <!-- id:d8b3 -->
