# id:7a9c

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(measured 2026-08-27 while building `id:dcf3`; sibling of `id:cb00`, which is the same blindness one layer up in `relay/scripts/lib-typed-edges.sh`). MEASURED on a purpose-built fixture: a downward `<!-- children:X -->` on the parent fills the parent's `children` array but leaves X's `parent` null, while an upward `<!-- children-of:P -->` on the child fills the child's `parent` but never appears in P's `children`. So in the emitted intermediate document NEITHER field alone is the parent/child graph, and any adapter or consumer that reads just one of them sees a partial tree -- silently, which is the `id:2b4b` class. `tracker/derived-index.py`'s `parent_child_edges()` takes the union of both fields and is the runnable reference reader. Fix shape: make `assemble()` write both directions for either spelling, with a fixture per direction plus one where both spellings describe the same edge. NOTE the cost this item must price: changing the emitted document regenerates `tracker/fixtures/expected/*.json` and is a change to the contract surface the two live adapters consume mid-pilot, so it is NOT a free refactor and should be sequenced deliberately. Do not edit `tracker/SCHEMA.md` or the adapters as a side effect. <!-- id:7a9c -->
