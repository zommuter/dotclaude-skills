# id:55f6

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

D2-A's gate `id:4fb8` is DISCHARGED and this is no longer gated on it. **The owner ruled AGAINST both the delegated recommendation (BUILD-NARROWER, bodies-only) and the measured conflict case** — that is his call as domain expert and is recorded, not re-litigated; the evidence he ruled over is preserved verbatim at `docs/meeting-notes/2026-08-13-1415-interim-ledger-substrate-4fb8-evidence.md` so the trade-off stays auditable. **Design still standing from the meeting:** D4-A gate model (marker = TRUTH, path = DERIVED, mechanical reconcile; path-as-truth and the compound directory were ratified then withdrawn); D5-A ordering (`priority:` in gaps of 100, ties by id ascending, lint `id:5a14`) — **but see the cost amendment below**. **⚠ SIX QUESTIONS BUILD-NARROWER WOULD HAVE SIDESTEPPED AND BUILD FULL MUST NOW ANSWER, all surfaced by `id:4fb8`:** **(1) `in_exempt_section` has no truth source.** Flat derives parked-ness from a running heading scan; a tree must hoist it into frontmatter, creating truth with nothing to reconcile against — a D4-A-shaped ruling is needed for parked-ness, and the same call recurs in ≥4 collectors sharing `lib-roadmap-sections.sh`. **(2) `REVIEW_ME.md` cannot be keyed** — 57 entries, **2 with id markers** (verified), so 55 have no key at all, and it conflicts in 2 of today's 6. Either mint ids for all 55 or rule it out of the tree. **(3) The indented `- [ ]` sub-bullet forces a side.** `classify-repo.sh:164` matches `\s*- \[ \] ` so flat counts an indented open item; a per-item exploder folds it into the parent body and **the item vanishes from dispatch**. ROADMAP has 0 today, TODO has 11 (of which 0 are `[ROUTINE]`, so the hazard is LATENT not live). `todo-conformance.sh` and `classify-repo.sh` already disagree on this construct. **(4) The `_chrome.md` residue reintroduces a shared non-union file** — 100% of `todo-conformance.sh`'s findings (12/12 TODO, 143/143 ROADMAP) land in the leftover, i.e. exactly the malformed content, in exactly the file shape the split exists to remove. **(5) `roadmap-archive.sh` and `meeting/append.sh` do not port, they get REWRITTEN** (heading-emptying semantics; append-by-heading). **(6) The `id:c74e` caveat STILL BINDS — D4-A did not neutralise it.** cartulary stores gates as a sorted list field with a real 3-way set-merge driver (`edits.py:50`, `merge_driver.py:119-133`); D4-A's N inline HTML comments in a prose line is a **THIRD form**, and the interim cannot borrow that driver because registering any driver defeats git's add/add refusal at `basesize=0`. cartulary's `id:09f9` (storage topology) is also still unratified — the owner has chosen to hold that meeting, and **this build should not pre-empt it**. **Carried open from the meeting:** archived-gate-target resolution (folds into `id:47f7`), what renders the human view (a derived `TODO.md` is a cache per `id:2840`, never truth). Relates `id:03a1`, `id:f764`, `id:4a5c`, `id:36f1`, `id:2840`, loderite `id:c3e5`. <!-- routed:207b --> — **🔴 URGENT, owner-raised again 2026-09-01: *"every item should only have a brief title plus a link to a per-item file with the details instead of much prose"*. That is THIS item's ruled design, restated independently by the owner three weeks later, so treat the priority as raised and the direction as re-confirmed. NOT a new decision and NOT a new item -- the 2026-08-13 BUILD FULL ruling stands; what is missing is the BUILD.** **NEW SUPPORTING MEASUREMENT (2026-09-01, from id:dd59):** TODO.md is **1.25 MB** and was **66.4% of a review prompt** at 1,245,034 B, which made this repo un-reviewable by the pool until review-scoped slicing landed; ROADMAP was a further 15.8%. **95.5% of TODO.md's bytes sit on HEAD LINES** across 657 open items. **READ THIS BEFORE CITING id:dd59 AGAINST THIS ITEM:** dd59's text says *"splitting TODO.md is DEAD ON ARRIVAL ... at most 4.5% is movable"*. That measured a DIFFERENT split -- moving CONTINUATION lines out (72.9% of items have none, hence 4.5%). It does NOT evaluate this item's split, which moves the HEAD LINE's prose into a per-item file and therefore targets the 95.5%. The number that kills the continuation split is the number that RECOMMENDS this one. Do not let the DOA phrasing foreclose 55f6; they are not the same proposal. **Also unchanged: the SIX questions above still bind and BUILD FULL must answer them** -- in particular (2) `REVIEW_ME.md` has 57 entries with only 2 id markers, so 55 have no key, and (4) the `_chrome.md` residue reintroduces exactly the shared non-union file shape the split exists to remove. A build that skips those ships the same problem in a new directory. — **FOUR OF THE SIX BLOCKING QUESTIONS ANSWERED BY THE OWNER 2026-09-01. Q5 is work not a decision, and Q6's stated blocker is STALE (corrected below). The build is no longer gated on design input.** **Q1 parked-ness -> FRONTMATTER IS TRUTH, the rendered view DERIVES sections.** Each item file carries an explicit parked/active field and `TODO.md` is generated from it, consistent with the already-ratified position that a rendered ledger is a CACHE (`id:2840`) and with D4-A's marker-is-truth model. Carry the weakness knowingly: this is the ONE place D4-A's reconcile-against-a-second-source cannot apply, because there is no independent copy -- a wrong field is simply wrong until a human notices. Path-as-truth (directory-per-section) was explicitly NOT chosen; it was ratified in the 2026-08-13 meeting and WITHDRAWN IN FULL after the `--fabled` pass, and its rename/rename(1to2) hazard applies to moving an item between sections. **Q2 `REVIEW_ME.md` -> RULED OUT OF THE TREE for now.** 57 entries, only 2 keyed; it is small and its entries are transient, so keying 55 of them buys no prompt-size reduction and spends 55 tokens against a space whose collisions were measured TODAY as quadratic (`id:c81b`). It stays flat, and stays a shared non-union file with its measured 2-in-6 conflict rate -- accepted knowingly; it can join later once entries are keyed. **Q3 indented sub-bullets -> PROMOTE THE 11 to first-class items, THEN LINT the construct out.** Preserves today's dispatch behaviour exactly (`classify-repo.sh:164` counts them; a folding exploder would make them VANISH, the `id:d35a` silent-no-op class) and removes the construct that makes `todo-conformance.sh` and `classify-repo.sh` disagree. Hazard was LATENT not live (0 in ROADMAP, 11 in TODO, none `[ROUTINE]`), so this is migration ahead of the hazard, not a fix after it. **Q4 `_chrome.md` -> ACCEPT IT, but LINT SO IT CANNOT GROW.** A bounded, shrinking leftover: the collision surface drops enormously without going to zero, and the build is not blocked on the unrelated 155-entry conformance cleanup (12/12 TODO + 143/143 ROADMAP findings all live in exactly this residue). The compromise is explicit -- a shared non-union file SURVIVES, and it only shrinks if someone does the fixing. **Q5 is NOT a decision:** `roadmap-archive.sh` and `meeting/append.sh` do not port, they get REWRITTEN (heading-emptying semantics; append-by-heading). That is scoped work. **Q6 PREMISE CORRECTED -- this item said cartulary's `id:09f9` (record storage topology) is *still unratified, the owner has chosen to hold that meeting, and this build should not pre-empt it*. That is FALSE and was verified 2026-09-01: `09f9` is `- [x]` at `cartulary/TODO.archive.md:17`, SETTLED 2026-08-12.** So the do-not-pre-empt reason is gone. What MAY still bind from `id:c74e` is the different question of FORM -- cartulary stores gates as a sorted list field with a real 3-way set-merge driver (`edits.py:50`, `merge_driver.py:119-133`) while D4-A's N inline HTML comments in a prose line is a THIRD form, and the interim cannot borrow that driver because registering any driver defeats git's add/add refusal at `basesize=0`. That is a portability cost to weigh at build time, NOT a gate. (This is the third stale premise found in this item's own text -- check the ratified source before letting any of them decide anything.) <!-- id:55f6 -->
