# id:32c3

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

**EDITED 2026-09-03 (`id:64f9`, title rewrite) -- declared, per the notes-are-editable
convention in `CLAUDE.md`.** This item's ledger TITLE in `TODO.md` was rewritten to fit
`LEDGER_ITEM_TITLE_MAX`. Nothing else on the item line changed: lane tags, typed-edge and
`id:` markers, the detail pointer and the checkbox are all untouched. The ORIGINAL title is
reproduced VERBATIM in the final section of this note, so no wording was deleted -- it moved
here. The relocated prose below is otherwise unchanged.

## From TODO

Router = the /chidilog and no-arg /meeting shape: audit, then RECOMMEND, never act — bare invocation inspects preceding context and offers (b) for a claim, (c) for a described system, (a) otherwise. (b) is REPORT-ONLY, fails LOUD when no ratified source is found rather than guessing one, degrades gracefully when ~/src/inflownistration is absent (public skill, private repo), and STATES ITS COVERAGE LIMIT — it only sees id-anchored claims, so a clean pass is never 'no drift'. (c) is propose→confirm: drafts row + evidence prose, proposes each ●/◐/○ with a one-line reason, shows a diff, writes ONLY on owner confirmation (marks are the owner's — a skill assigning them violates H, a component of the grammar it catalogues), and REFUSES-AND-SURFACES under --afk/pool rather than default-writing. Consumption boundary is FORMAT-PARSING for row insertion ONLY, never semantic consumption of the marks. No /infln alias. The skill's own public source must embed no instance names and no populated table schema. Spec: inflownistration/docs/meeting-notes/2026-08-21-0925-inflownistration-three-input-items.md D3+A3; local tracking id:93d6. <!-- id:32c3 -->

## Original title (verbatim, before the `id:64f9` rewrite)

Build the `/inflownistration` skill — scope RATIFIED 2026-08-21, all three candidates plus a parameter-less router: (a) surface the concept + its four docs; (b) a DRIFT CHECK that locates a claim's ratified source by `id:XXXX` grep and diffs the restatement verbatim, hunting disjunctive→conjunctive inversion, criterion-swapped-under-same-name, and criterion-lifted-from-a-recorded-not-adopted-section; (c) file a new instance or worked example into the right doc.
