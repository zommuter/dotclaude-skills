# id:b437

AUTHORED note, not relocated by `tools/ledger-shrink.py` -- the verbatim-reproduction claim
does not apply to it (see the "Authored notes" section of `docs/ledger-notes/README.md`).

## Parked work -- READ BEFORE RESTARTING THIS ITEM

An execute unit attempted this item on run `relay-20260909-143257-21736`, DIED of
`Prompt is too long` mid-work (the `id:5295` inert-budget-guard window), and its residue was
commit-and-parked (`id:f272`). The branch is still on disk, unmerged:

    relay/orphan/relay-20260909-143257-21736-execute-b437-0

It holds 1 commit, +70/-10 across 11 files -- title rewrites plus the matching
`docs/ledger-notes/*.md` files carrying each original title verbatim, which is this item's
whole shape (rewrite ~10 over-budget `TODO.md` titles, preserving the original in the note).

**Verdict 2026-09-09: NOT INTEGRATED -- it CONFLICTS.** Measured in a throwaway worktree:

    CONFLICT (content): Merge conflict in TODO.md
    CONFLICT (content): Merge conflict in docs/ledger-notes/3846.md

This is a genuine shared-non-union-ledger collision, not a broken branch. The orphan rewrites
`TODO.md` title lines; `TODO.md` moved substantially on `main` the same evening (several new
items filed: `id:5295`, `id:5f6a`, `id:6d7e`, `id:7f4c`, `id:40cf`), and
`docs/ledger-notes/3846.md` gained an appended negative-control section. Both sides are wanted.
Neither can be taken wholesale.

**To restart**: begin FROM this branch. Re-apply its title rewrites onto current `main`
rather than merging -- the item's unit of work is per-title, so the rewrites are individually
portable even though the file-level merge is not. Verify with
`tools/shrink-acceptance.py` (Check 4, title-rewrite batch invariance, landed via `id:521b`)
and the `## Original title (verbatim, id:64f9 title rewrite)` convention that
`tests/test_title_rewrite_batch_acceptance_64f9.sh` pins. Re-check WHICH titles are still over
budget first -- `main` has changed, so the "first ~10 of 42" the branch chose may no longer be
the right ten.

**Gate note**: `id:9088` (the unsatisfiable case-3 fixture that blocked the `id:64f9` family)
is CLOSED as of 2026-09-09, so cases 4-7 of the batch-acceptance spec now run. The branch
predates that close and was authored without them.

The branch is not merged and nothing here ticks the item.

## Why it was left parked rather than discarded

Its two cleanly-merging siblings from the same run (`id:799f`, `id:aa5e`) WERE integrated on
2026-09-09 (`relay-ckpt-20260909-2042`, `-2042-2`) after the suite was verified green. This
one was held back on measured evidence. See `id:7f4c` for the standing cost of a parked
orphan -- it suppresses its item, and enough suppressions un-name the execute verdict and lock
the repo out of dispatch. Restart or dispose deliberately; do not let it sit.
