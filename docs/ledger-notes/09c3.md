# id:09c3

Authored directly, not relocated by `tools/ledger-shrink.py`. Conventions:
`docs/ledger-notes/README.md`.

## From TODO

Reproduced 2026-09-05: `` `@owner-accepted:2026-09-01 and more` `` yields a kept head carrying `` `@owner-accepted:2026-09-01 `` -- opening tick, no close. Cause: the keep-patterns' optional `` `? `` matches one tick of the pair and the partner stays in the moved region. NOT cosmetic: `gather-repo-state.sh:321` strips backtick-quoted spans BEFORE picking the leftmost lane tag (id:1bbd), so an unbalanced tick can swallow or expose a lane token -- latent on the repo where it was found (dispatch sets provably unaffected) but not on one whose lane tag sits near a backticked marker. Found by a sibling repo's 03a3 wave-1 run, 8 lines. Spec case belongs alongside id:9628; the fixture is two lines. Distinct from id:1bbd and from the classify-repo backtick-masking question below.
