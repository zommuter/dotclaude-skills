# id:6740

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

Owner ruled 2026-09-08 (/relay human) that `docs/HANDOVER-*.md` is local-only; all 26 were `git rm --cached`ed (9201732b) and the path is gitignored. That correctly stops publication, but it also means these files now live on ONE disk with no backup and no history -- and their whole purpose is session-to-session continuity, so losing them is a real cost, not a tidy-up. The established fleet pattern for exactly this shape is the cross-project inbox (`~/.claude/projects/todo-inbox.md`, id:9fdb): git-tracked in the PRIVATE `~/.claude/projects` repo, committed hourly to the private `zomni/sessions` branch, versioned but never published. Proposal: move handover docs there and leave a pointer in `docs/`. Decide before the next handover is written, since every one authored in the meantime is unbacked. <!-- relates:d29b --> <!-- relates:7e9a --> <!-- id:6740 -->
