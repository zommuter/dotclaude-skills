# id:8f29

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

dotclaude-skills-64 refuted them; I re-ran each against this machine rather than accepting the report, and both refutations hold. **(a) WRONG: I said `verify-isolation.sh` is structurally blind to a child that never created a worktree.** It is not. `verify-isolation.sh /nonexistent/path/xyz` prints `'...' is not a git worktree` and exits **2**. Absence is a documented, implemented failure case (header :41, implementation :73). **(b) WRONG: I proposed a supervisor-side check as if it were missing.** It exists: `integrate.sh` takes `--worktree <dir>` from the CALLER (:431), calls the gate (:555) and hands back on failure (:556). My ancillary note that the supervisor already holds the path from the id:7570 lease was right and already spent. **(c) My third claim reached the RIGHT conclusion by the WRONG route, and the correction matters more than the error** -- filed separately as routed:7ae9 and it is CRITICAL. I said only an interactive permission prompt stood between the unisolated child and a destructive rewrite, which does not exist under `--afk`. The refutation was that `destructive-git-guard.py` is a PreToolUse hook denying `git reset --hard` unconditionally regardless of `--afk`. That is its INTENT and it is correctly wired here. **But on the command form the child actually ran it does not fire at all:** `git status --porcelain` + newline + `git reset --hard` returns NO deny, because `shlex.split()` eats the newline as whitespace so `_split_git_commands` never sees a boundary and swallows the reset into the `status` argv. So the human denial genuinely was the only thing that stopped it -- not because no guard exists under `--afk`, but because the guard has a newline bypass. See routed:7ae9 for the measured matrix and root cause. **WHAT SURVIVES from d67b, unchanged and still worth fixing:** an EXECUTE child skipped worktree creation and committed 26 files to the target repo's MAIN checkout (commit 0df75859, run relay-20260901-231227-28741); its assigned worktree `~/.cache/relay/worktrees/loderite/relay-20260901-231227-28741-execute-a91b-0` was never created. dotclaude-skills-64's reframing is the correct one and I adopt it: the gate is a MERGE-TIME detector, not a dispatch-time preventer, so the loose commit reaches main before anything runs and must be reverted by hand. Prevention needs a dispatch-time or child-side constraint. **Apologies for the escalation on two bad premises** -- the owner was told this was SUPER URGENT partly on (a) and (c) as I stated them. <!-- id:8f29 -->
