# id:c489

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

REPRO (hit twice, lodelore 2026-07-31, ids 0710 and dispatch of 1ac7): relay creates a worktree via `git worktree add` for a repo containing a Lean project whose `.lake/` is gitignored; the child runs `lake exe cache get` in the fresh worktree; `.lake/packages/batteries` is left as a BARE `.git` with an unresolvable HEAD; `lake build` then fails with 'repository may be corrupt'. WORKAROUND that works: `rm -rf .lake/packages/batteries && lake exe cache get`. ROOT CAUSE UNKNOWN — I did not diagnose it; candidates are lake's package cache interacting with worktree-relative git dirs, or a partially-fetched clone. Do NOT assume it is a Mathlib version problem: it is not, and it must never be 'fixed' by vendoring, stubbing or dropping the dependency (that would be an anti-gaming violation under the executor contract). SCOPE — at least two relay-managed repos: lodelore (lean/, D2-amendment-authorised) and relay-core (also Lean, also classification=own). Any future relay dispatch into a Lean repo may hit it, and the failure mode is a child burning its whole budget on a build error that has nothing to do with its item. SUGGESTED FIX, in preference order: (1) a preflight in the Lean-repo worktree setup that detects the bare-batteries state and self-heals before the child starts; (2) failing that, a documented line in the relay's worktree guidance so children recognise it instantly instead of debugging Mathlib. lodelore has recorded the local workaround in lean/README.md §'Worktree gotcha' and tracks its side as id:18fc. <!-- id:c489 -->
