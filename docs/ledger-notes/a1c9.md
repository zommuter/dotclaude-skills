# id:a1c9

**EDITED 2026-09-03 (`id:64f9`, title rewrite) -- declared, per the notes-are-editable
convention in `CLAUDE.md`.** This item's ledger TITLE in `TODO.md` was rewritten to fit
`LEDGER_ITEM_TITLE_MAX`. Nothing else on the item line changed: lane tags, typed-edge and
`id:` markers, the detail pointer and the checkbox are all untouched. The ORIGINAL title is
reproduced VERBATIM in the final section of this note, so no wording was deleted -- it moved
here. The relocated prose below is otherwise unchanged.

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

Motivation: a chidiai session 2026-06-30 hand-rolled exactly this (2 parallel background general-purpose executors in isolated worktrees → octopus merge → inline review), proving the shape works, BUT the agents ran at the parent session's model tier, not Sonnet — wrong cost profile. Generalize into the relay with correct per-agent model selection + the worktree/lease/integrator machinery it already has. <!-- id:a1c9 -->

## Original title (verbatim, before the `id:64f9` rewrite)

`/relay executor` (+ pool dispatch): add a `-b|--background` switch that runs executor units as parallel-as-possible BACKGROUND agents, each pinned to the PROPER per-role model (executors = Sonnet tier, NOT the parent session's tier; the end-of-run review = apex/Opus), then octopus-merges the disjoint-file worktree results (fall back to sequential --no-ff on conflict) and runs the anti-gaming review at the end — also a background agent of an adequate model.
