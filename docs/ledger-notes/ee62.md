# id:ee62

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

— user 2026-06-22 (matching the `.mw` project convention): an HTML comment like `<!-- xledger-ok: <reason> -->` hides an EXPLANATORY annotation from rendered markdown, but the reason is human-readable prose a reader of the rendered TODO should SEE. **Distinguish two classes:** OPAQUE correlation keys with no standalone human meaning (`<!-- id:XXXX -->`, `<!-- routed:XXXX -->`) MAY stay as comments; MEANINGFUL annotations carrying a human-readable reason/decision should be VISIBLE. `.mw` already uses visible tags (`[id:6ab8]`, `@manual`, `@impl`, `@verify`, `@content-hash`) — adopt that style. **Concretely:** (1) pick a visible syntax (e.g. a `[xledger-ok: <reason>]` bracket tag); (2) update the parser(s) that grep the HTML-comment form — `orphan-scan.sh --cross-ledger` xledger-ok detection (`*"<!-- xledger-ok:"*` at ~L63/72; back-compat or one-shot migrate); (3) update `tests/test_ledger_seam.sh`; (4) migrate the live annotation on **id:78ff**; (5) audit other HTML-comment-as-meaning uses across the skills (gate/route markers) for the same treatment. Small reversible design call on the exact syntax → `[HARD — meeting]`. <!-- id:ee62 -->

## AMENDED 2026-09-03 -- the OPAQUE-KEY CARVE-OUT IS WITHDRAWN (owner)

Section appended; the relocated prose above is untouched.

The prose above draws a two-class distinction and **exempts** the correlation keys:

> OPAQUE correlation keys with no standalone human meaning (`<!-- id:XXXX -->`,
> `<!-- routed:XXXX -->`) MAY stay as comments; MEANINGFUL annotations carrying a
> human-readable reason/decision should be VISIBLE.

**That carve-out is withdrawn by the owner, 2026-09-03.** Recorded as an EXPLICIT AMENDMENT,
not a reinterpretation: the 2026-06-22 text says plainly that `id:` and `routed:` may stay,
and the new instruction is that they may not. The owner's stated position is that HTML
comments in `.md` are undesirable in general, not merely when they hide meaning.

The successor work is **`id:14e5`**, which carries the concrete migration. `ee62` keeps its
original scope (the `xledger-ok:`-class *meaningful* annotations) so the two can land
independently; `14e5` is the larger, fleet-binding half.

**Evidence that accumulated between the two rulings, all measured 2026-09-03:**

- **This very item's line is CORRUPTED by the comment syntax it argues against.**
  `TODO.md:34` carries `<!-- id:XXXX -->` and `<!-- routed:XXXX -->` -- the *examples* from
  the prose above -- hoisted onto the head line by `tools/ledger-shrink.py`, plus ~300 chars
  of prose dragged along by the unterminated `<!-- xledger-ok:` example. The tool cannot tell
  an example marker from a real one. That is `id:8372`, and it is the mechanical argument for
  the backtick-escaping half of `14e5`.
- **30 ledger lines** carry a non-hex placeholder marker of this kind (28 in `TODO.md`, 2 in
  `ROADMAP.md`).
- The fleet carries **58,570 bare `id:XXXX` prose references, 3,484 of them ambiguous**, and
  `dotclaude-skills` alone contributes **23,698 (40%)**. Within this repo the largest surface
  after `tests/` is **`docs/ledger-notes/` at 4,225** -- i.e. the trimming programme's own
  output is now a leading source of unanchored references
  (`cartulary/scripts/prose-tail-scan.py`, re-run today).
