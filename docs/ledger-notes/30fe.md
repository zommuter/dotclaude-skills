# id:30fe

## From TODO

The enforcement half `id:0d7c` D4 never specified. D4 gave a LENGTH ratchet (500 chars,
keyed on length, monotonic shrink) and that shipped. A length budget cannot express the
owner's actual bar, stated 2026-09-02: an item line carries **only** lane tag, gate markers,
`id:` anchor, a short title and a detail pointer, and **no prose**. A 237-char line can be
perfectly conforming or entirely prose; the ratchet sees one number and cannot tell.

**Deliverable:** a structural check, in `relay/scripts/todo-conformance.sh` alongside the
length rules, that parses an item line into its allowed components and reports whatever text
survives. The measurement harness already exists in prototype form -- it produced the 460/840
figure in `id:6546` -- and its stripping order is the grammar: leading checkbox, lane tag(s),
first bold run (the title), gate markers, every `<!-- ... -->`, the `-- detail:` pointer.

**Grammar decisions this item must make, not inherit:**

- **Whether the detail pointer stays.** `docs/ledger-notes/<id>.md` is a pure function of the
  id, so it costs ~40 chars per shrunk line for no information -- the owner flagged it as
  possibly "canonically redundant". Three consumers currently key on the PATH being present
  (`ledger-slice.sh`'s `detail_collect`, `roadmap-lint`'s `item_detail_path`,
  `ledger-shrink.py`'s `has_pointer` idempotence guard), and "a file exists at that path" is a
  DIFFERENT predicate from "this item has a relocated body" -- the note file could exist for
  another reason. Decide it here, because the grammar has to name the pointer either way.
- **What counts as a title.** The first bold run is the current de-facto answer, but 183 open
  items have no bold run at all (`id:6546`). The check must have a verdict for those that is
  not "unparseable".
- **Severity.** The length rules split WARN (`length-unshrinkable`, `length-grandfathered`)
  from ERROR (`length-over-budget`, `length-regrowth`), and `tools/shrink-acceptance.py`
  now keys on exactly that split -- its `TODO_CONFORMANCE_NONBLOCKING` set. A new rule must
  land on one side of that line deliberately, or the acceptance gate inherits it by accident.

**Lane vocabulary must match BOTH dash spellings.** The fleet is mid-migration from
`[HARD — pool]` to `[HARD - pool]`, and loderite measured its own ledgers carrying both live
on 2026-09-02 (`[INPUT - decision]` 19 vs `[INPUT — decision]` 58; `[HARD — pool]` 7 with no
hyphen twin). A checker built from one spelling reports a quarter of the `[INPUT]` tags as
malformed, or worse, a shrinker built from one deletes them -- the `id:d35a` silent-no-op
class the global rule names by name. Take the marker set from `relay/references/hard-lanes.md`,
the SSOT, never from either repo's observed strings, and never normalise a delimiter in
passing.

**Cross-repo consumer:** loderite's owner ratified this same check for loderite on 2026-09-02
and its session is explicitly WAITING on this id rather than building a second mechanism.
Route the id when it lands. Their marker vocabulary is a superset of ours in places
(`<!-- lint-ok: -->` x30, `<!-- xgate: -->` x2, `<!-- children-of: -->`), so the grammar
should treat "an html comment" structurally rather than enumerate comment names -- which is
the rule commit `c5d78046` already learned here the hard way, after enumerating names dropped
a `children:` typed edge and an `xledger-ok:` suppression.
