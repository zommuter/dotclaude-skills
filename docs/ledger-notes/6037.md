# id:6037

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

— shard in inject.done/, zero commits and no relay-ckpt in leAIrn2learn, prompt text lost; inject.sh's own header documents `--repo` as the guard for exactly this, routed:a923) <!-- id:6037 -->
