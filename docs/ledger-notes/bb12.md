# id:bb12

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(user 2026-07-01, motivated by the truncocraft→loderite **rebrand** done semi-manually). "Rebrand" (not just "rename") is the better frame: truncocraft→loderite was a public-NAME/brand change ([[nomi]] naming meeting), so the skill covers ALL brand surfaces — repo name, package/module name, in-app/UI strings, docs & README, domain/handle — not only the filesystem path. A project rename is a multi-surface manual chore that easily strands dangling links; mechanize it. Given `<old> <new>`: (1) rename the git REMOTE (fievel bare `src/<old>.git`→`src/<new>.git`, and GitHub via `gh repo rename`) and update the local remote URL(s); (2) rename the local dir `~/src/<old>`→`~/src/<new>` and fix worktree/ref paths; (3) MOVE the Claude project state so memories + sessions stay linked to the NEW location — the per-project dir is keyed by a path-slug (`~/.claude/projects/-home-tobias-src-<old>` → `-home-tobias-src-<new>`), so rename `projects/<slug>/` (memory/, MEMORY.md, sessions/transcripts) to the new slug and fix absolute-path refs inside; (4) update `relay.toml` `[repos.<old>]`→`[repos.<new>]` (+ any `# path:` override) and cross-repo inbox `routed:`/`[<old>]` targets; (5) grep the wider tree for stale references (diary `-p` path pointers, meeting-note project paths, ROADMAP/TODO cross-links). Do it TRANSACTIONALLY: dry-run/preview + rollback plan (a half-renamed project is itself an invalid state — cf. the relay state-machine item id:4da4). Use the loderite (ex-truncocraft) rename as the worked example of every surface that had to be touched. <!-- id:bb12 -->
