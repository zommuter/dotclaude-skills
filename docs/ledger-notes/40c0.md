# id:40c0

Relocated from the ledger by `tools/ledger-continuations.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From ROADMAP

  - **Context**: the tool recovers 216 chars from ROADMAP.md against 178,299 continuation bytes. `id:dd59` ruled continuation-moving "dead on arrival, 4.5% movable" -- that verdict was measured on TODO and must NOT be carried to ROADMAP, where the ratio is inverted. Bears directly on `id:03a3`: migrating 46 repos with a head-keyed shrinker and a head-keyed gate reports success on every ROADMAP while leaving the real bloat untouched.
  - **Acceptance**: a DECISION with its measurement -- the per-repo recoverable fraction under each option, since a dry-run is free and needs no LLM. Whatever is chosen must keep the existing refusal set (never orphan another item id, never plant retired vocabulary, never change an item computed lane).
  - **Done-check**: the decision is recorded with the fleet-wide dry-run numbers behind it, and `id:03a3` cites it.

## STATUS 2026-09-03 -- NOT DONE; residue measured

Section APPENDED, not an edit to the relocated prose above. The header's "reproduced
verbatim" claim is unaffected: nothing above was altered or deleted.

`docs/HANDOVER-2026-09-02-2230.md` lists this item under "What landed" ("37 of 38 ROADMAP
blocks moved, -86,363 bytes"). Its checkbox was never ticked, and verification 2026-09-03
shows that was correct -- **the item is incomplete and stays open.**

Measured residue in `ROADMAP.md`:

- **28 continuation lines / 7,472 bytes** still present, in two runs: lines 28-45 and 84-95.
- Owning items: **`id:6b35`** (6,679 bytes, the `[INPUT - meeting]` pool-launch proxy
  coupling item) and **`id:0e56`**.
- **Not a duplication.** Checked explicitly: the `Hop label` table appears once in
  `ROADMAP.md` and zero times in `docs/ledger-notes/6b35.md`. So `ledger-shrink.py`
  relocated the head-line prose and wrote the note, and `ledger-continuations.py` then did
  not move the block. The content exists in exactly one place, which is the wrong one --
  there is nothing to de-duplicate, only something to move.

Whoever finishes this should confirm whether the two blocks were REFUSED for a stated reason
(the refusal set above forbids orphaning an id, planting retired vocabulary, or changing a
computed lane) or simply missed. `6b35` carries a `children-of:` fan of items (`e405`,
`540f`, `c179` all cite it), so check the block does not carry structure those children
resolve against before relocating it.

## STATUS 2026-09-04 -- STAYS OPEN; local migration finished, the ITEM is not

Re-verified by running the item's own done-check. The verdict is **NOT DONE**, and it is
worth being precise about which half is missing, because the local work IS finished and that
is what keeps getting mistaken for closure.

**Finished:** this repo's `ROADMAP.md` migration. The 28-line / 7,472-byte residue measured
above is GONE. `ROADMAP.md` now holds 6 non-item lines, all of them the `# Roadmap` file
header prose, and `ledger-continuations.py --dry-run` refuses nothing on either ledger
(`no-id`, `foreign-id`, `cited-body`, `unowned` all 0; 0 pointers to plant).

**Missing, both clauses of the written done-check:**

1. **No recorded DECISION carrying fleet-wide dry-run numbers.** The acceptance asks for the
   per-repo recoverable fraction under each option. Only THIS repo has ever been measured.
2. **`id:03a3` does not cite `40c0`.** Verified by reading `docs/ledger-notes/03a3.md` in
   full: zero occurrences.

The item's stated terminus is a fleet-wide decision feeding `03a3`, not this repo's line
count, so reaching a local fixed point does not satisfy either clause. If the intent was only
the local migration, the acceptance text needs an EXPLICIT amendment -- not a reinterpretation
of its words to reach the answer we would now prefer.

**Correcting `HANDOVER-2026-09-03-2130.md`, which is wrong about this item in BOTH directions.**
Section 3 lists `40c0` under "What closed"; it is open. Section 8 correctly says it is not
done but misstates its own figure: it claims "10 `unowned` lines remain in ROADMAP, standalone
comment blocks with no owning id". There are **6** lines, they are the file header rather than
comment blocks (`grep -c '<!--' ROADMAP.md` returns 0), the two standalone provenance blocks
were moved to `RELAY_LOG.md:3128`, and the tool reports `REFUSED unowned: 0`. Section 8 also
contradicts section 1 of the same file, which already says "Section-preamble lines: 152 -> 6
(the `# Roadmap` file header only)".
