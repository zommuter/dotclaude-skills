# id:82ec

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

ACCEPTANCE CRITERION (non-negotiable, from the id:ca14 adequacy standard): the spec must map EACH of the three loderite id:ed3a re-asks to a specific NAMED CALL SITE that would have retrieved the answer -- retrieval-on-demand alone re-ships ca14's failure, because none of the three authors knew a recorded answer existed. Call sites are PLURAL and MANDATORY hooks at the venues where re-asks happen: ROADMAP line edits touching an id, meeting agenda assembly, apex option-framing. A contradiction detector is layered as a CONSUMER of a returned chain, never a corpus scanner; that rejection is conditional on the acceptance criterion being met, not absolute. GATED ON id:cb00 + id:7a9c, READER-SIDE reconciliation ONLY -- cb00 explicitly forbids rewriting existing markers to a single spelling (fleet-wide ledger edit, collides with the queued em-dash migration). PENDING TRIPWIRE QUESTION, must be surfaced for owner ratification and never decided by shipping: this walker consumes inflownistration's ledgers AND its docs/meeting-notes Decisions sections, and its detector reads answer CONTENT semantically -- whether that fires inflownistration's carve-drift tripwire is deliberately open, with both sides recorded at equal weight. Read inflownistration docs/meeting-notes/2026-09-01-0746-meta-blame-and-ledger-chain-scoping.md (D2', D4') before specing. <!-- id:82ec -->
