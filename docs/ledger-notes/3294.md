# id:3294

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

-- zom.fi 2026-09-08: 13 ROADMAP items carry typed gated-on edges pointing at `[HARD - meeting]` items (f9e3, d82e, f36a, 59da) that live only in `TODO.md`. The gates FUNCTION -- `resolve-gates.sh` returns block=1 with an EMPTY dangling column and `open_hard_pool` correctly drops 18 to 5 -- but the lint emits 10 DEAD-GATE warnings saying "nothing in ROADMAP.md can ever clear the gate; promote it", while handoff C2 explicitly forbids promoting a surface/meeting-lane item with a guessed lane (`id:5eb3`). Suggested fix: suppress DEAD-GATE when the target resolves OPEN in `TODO.md` on a meeting/decision lane, which the resolver can already see. **BLAST RADIUS SHARPENED HERE 2026-09-08 (review run relay-20260908-174448-4421) -- the suggested fix covers HALF of this repo's DEAD-GATE warnings, not all of them, so do not size it off the raw warning count.** `roadmap-lint.sh` emitted 4 DEAD-GATE WARNs on dotclaude-skills at HEAD, and their gate targets split two ways: `b0b1` (the target of 540f and c179) IS `[INPUT - meeting]` in TODO.md and so IS the described false-positive class, but `09e4` (target of d4ca and e405) is `[ROUTINE]` in TODO.md -- a genuinely promotable item, so those two WARNs are TRUE POSITIVES saying exactly the right thing. A suppression keyed on "the target resolves OPEN in TODO.md on a meeting/decision lane" correctly leaves them firing. This review PROMOTED `09e4` to ROADMAP.md (reusing its id), which clears those two at the source and leaves 2 for this item. <!-- id:3294 -->
