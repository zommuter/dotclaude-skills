# id:d1fb

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(meeting 2026-07-24, the explicitly-accepted trade) — the amended design is forward-looking: its anchored markers exist only in notes authored after it lands, so it scores ZERO on the existing 203-note corpus (of which only 124 even carry a `## Decisions` heading). The instances the loderite audit found — lane tag left at `meeting`, gate text stale, visible prose still asking the settled question, resolution parked in a comment or a sibling item — need eyes, not a tool. Scope: loderite's audit findings (routed separately, `routed:b81c`) plus this repo's equivalents. Not pool-dispatchable: every instance is a judgment about which side is right. **LODERITE HALF DONE 2026-07-24 (routed:fb6e, owner-ratified in loderite).** The manual pass ran there the same day (Opus + a background Fable cross-audit, 16 findings, every one ratified via AskUserQuestion; evidence: loderite commits `7d571c6` `fbe5690` `fe72fd5` + its `RELAY_LOG.md` 2026-07-24 entry). Swept: stale-open decisions 280c/b8b8/f7c7/2b24 + the retired gallery item; contradictions 9a6b-vs-e4f9/D5 (the only code-risk one — `refine()` proves HALF_CORE `0b0010` and ANTIPRISM `0b0110` unreachable in the dyadic closure, so e4f9's keep-rationale was FALSE and was corrected) and b9fd twin-vs-rescope; stale gates c8ad (a D4 correction missed when applied to sibling c2f3) and e5fe (held on the already-discharged a1fd ruling); 13 cross-ledger drifts, ALL one direction, now 0 (cause filed as loderite id:8e2f); 4 stale meeting/decision lanes re-laned, 27 more swept clean. **loderite no longer needs this item** — d1fb stays OPEN for the remaining repos. It also surfaced a NEW instance class for id:5533 — see that item. <!-- routed:fb6e --> <!-- children-of:968c --> **OWNER-DECIDED 2026-08-14 (`/relay human .`): "Run the loderite recipe here".** Repeat the shape that demonstrably worked in loderite on 2026-07-24 — an Opus pass plus a **background Fable cross-audit**, every finding brought back for owner ratification via AskUserQuestion (there: 16 findings, all ratified; commits `7d571c6`/`fbe5690`/`fe72fd5`). Two alternatives were offered and declined: waiting for `id:f346`'s deterministic premise-check (rejected — do not sequence a known-good manual pass behind an unbuilt tool), and scoping down to only decisions still referenced by an OPEN item (rejected — that accepts leaving unreferenced stale notes in place). So the scope stays this repo's full equivalent of loderite's corpus sweep. Findings are RATIFIED, never self-settled — the delegated-verdict rule applies to the Fable half in particular. <!-- id:d1fb --> — **RE-LANED 2026-08-18 (`/relay human .`, OWNER-DECIDED): `[INPUT — access]` → `[INPUT — decision]`.** `hard-lanes.md` defines `[INPUT — access]` as needing a live credential / physical hardware / on-device action **with no open judgment call**; this item needs none of those — it is reading ledgers and notes and ruling per row, and its own text says "every instance is a judgment about which side is right". Same ground on which `id:87f5` was re-laned from hands on 2026-08-14. **PREMISE CORRECTED: the "~9-10 known instances" were LODERITE's**, and that half is DONE (2026-07-24, 16 findings, all owner-ratified). What stays open here is THIS repo's equivalents, whose count is **unknown** — do not carry the 9-10 figure forward as if it were this repo's backlog. **Mechanical enumeration was ATTEMPTED 2026-08-18 and mostly FAILED, recorded so it is not re-attempted blind:** the only defensible signal is cross-ledger drift — exactly **3** rows today (`id:cd9c`, `id:d119`, `id:ec3c`, all TODO:[ ] / ROADMAP:[x]). Discharged-gate scanning yields ~16 rows at low precision, and prose-vocabulary matching ("OWNER-DECIDED"/"DECIDED" in an open item) ran at roughly **1 in 9** — which is the already-REFUTED `id:010c` design, so it must not be used as evidence. The forward-only premise is confirmed in code, not prose: `lib-typed-edges.sh:53` matches the anchored `<!-- settles:XXXX -->` form only, and there are **0 anchored `settles:` edges across all 214 notes**. Sizing with a pre-built worksheet: **~25-30 min** of owner attention, batched.
