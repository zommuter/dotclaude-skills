# id:684b

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

— found while tidying leftover `relay/*` refs. `relay/ok` carries ONE unmerged commit `94f780b`, message **`child work id:test`**, adding a file literally named `g` with one line. The message and the fixture-shaped filename make it unambiguous test residue, not real work — but it was written into `~/src/dotclaude-skills`, not a `mktemp -d`. **This violates the repo's own stated testing contract** (`CLAUDE.md` §Testing: *"Tests must be hermetic: work in `mktemp -d`, override `HOME`/`DEST_DIR`/roots via args or env, never touch `~/.claude` or the network"*). Consequences beyond the litter: a stray branch is invisible to `/relay reconcile` (it only surfaces `relay/orphan/*`, and there are currently zero of those), it inflates the `relay/*` namespace the relay enumerates, and a test that can create a branch here can also create a COMMIT here — which is one step from a test mutating a real ledger. Note it is dated TODAY, so it was produced by one of this session's runs, meaning the leak is live rather than historical. FIX: find the test that creates a branch named `ok` (or a fixture that omits `-C "$TMP"`/`git init` isolation) and make it hermetic; then add a guard — the suite should FAIL if a test run leaves any new ref, commit, or working-tree change in the canonical repo. Cross-check `relay/handoff-cadence-20260731` (0 unmerged commits) for the same origin. Evidence recorded here so the branch itself can be deleted without losing it. <!-- id:684b -->
