# id:f91a

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

**EDITED 2026-09-03 (`id:800f`, section-preamble removal) -- declared, per the
notes-are-editable convention in `CLAUDE.md`. The verbatim claim above is amended for the
`## Section preamble (from ROADMAP)` section appended at the end of this note, and for
nothing else.** The owner ruled 2026-09-03 that `ROADMAP.md` carries NO section-preamble
prose; the preamble that stood over this item was moved here rather than deleted. No word,
figure or marker of it was altered -- em dashes and retired lane vocabulary are preserved,
because a historical quotation rewritten is a historical quotation falsified.

**EDITED ON RELOCATION (2026-09-02, `id:40c0`) -- the verbatim claim above is amended
for the PROSE THIS PASS APPENDED to `## From ROADMAP`, and for nothing else. Any
prose already in that section arrived by an earlier pass and is untouched here.** Fleet-rule violations found in the relocated
prose were FIXED here rather than parked: 7 punctuation em dashes became `--`. Nothing else changed: no word, figure, marker or line break was
altered, and the appended text is otherwise the ROADMAP.md block verbatim, indentation
included.

DELIBERATELY LEFT, declared rather than silently kept. 2 em dashes inside BACKTICK code spans are NOT converted. A backticked span here
quotes something whose spelling is the point -- a lane tag, a heading that still
carries that character, a `path:line-range`, or live tool output -- so rewriting it
would make the quotation false.
The lane tag `[INPUT — meeting]` is NOT converted, neither delimiter nor name. Every lane tag in
this section is REFERENTIAL -- prose MENTIONING the vocabulary, inside backticks --
not the DECLARATIVE lane of any item, which stays on the ledger line. Rewriting a
referential tag corrupts a record of what a past run classified or of which
spelling a migration is about.

**EDITED ON RELOCATION (2026-09-02, `id:40c0`) -- the verbatim claim above is amended
for the PROSE THIS PASS APPENDED to `## From ROADMAP`, and for nothing else. Any
prose already in that section arrived by an earlier pass and is untouched here.** Fleet-rule violations found in the relocated
prose were FIXED here rather than parked: 13 punctuation em dashes became `--`. Nothing else changed: no word, figure, marker or line break was
altered, and the appended text is otherwise the ROADMAP.md block verbatim, indentation
included.

## From TODO

(INBOUND routed:f91a from loderite 2026-07-30; 4th+ occurrence) — **verified here**: `relay-loop.js:2413` is `const opts = { label, phase, schema }` with only `opts.model` added (`:2414-2415`); across every `agent()` call in `relay/scripts/*.js|mjs` the option keys are 16×label, 16×model, 15×phase, 2×schema — **zero `cwd`, zero `isolation`**. The child is handed `main checkout: ${unit.path}` (`:1862`) with no warning, then told `Work EXCLUSIVELY in that worktree` (`:1867`). **Damage trail**: 2026-06-30 jobAI (id:c6c8); 2026-07-14 loderite (id:f682/7612); 2026-07-30 loderite run `relay-20260730-000539-18583` execute/8aea wrote non-compiling TypeScript into `src/editor-core.ts` + `src/editor.ts` of the LIVE main checkout while its worktree stayed clean at `bd48b07` (leak parked as `relay/orphan/main-leak-20260730-000539` in `~/src/loderite`). **Amplification**: main is left dirty, which trips `clean-tree-gate.sh` (id:aa93) at the next run's integrate and defers it — surfacing as an *unattributed* "main checkout dirty" in a later, innocent run. <!-- routed:f91a --> <!-- children:d464,34b7 --> <!-- id:f91a -->

  - **CORRECTIONS to the inbound report and to this item's first filing -- do not build on the retracted text.** (a) The report's FIX A ("the Agent tool supports an explicit cwd") is **wrong for both APIs**: neither the Workflow `agent()` surface nor the interactive Agent tool exposes `cwd`, and `grep -rn "cwd:"` finds no precedent in this repo. (b) BUT my first filing over-broadened that into "FIX A is not buildable" -- **only the cwd-pinning half is dead; "parent creates the worktree before dispatch" needs no API at all** and is id:34b7 below. (c) `EnterWorktree --path` -- which I floated as the structural candidate -- is **probably non-viable for the pool**: it requires the path to be in `git worktree list` for the *current* repo, but relay worktrees are at `~/.cache/relay/worktrees/<repo>/…` while targets are siblings under `~/src`, so a cross-repo child would be rejected. Likely works only when cwd == target. (d) I claimed enforcement was available **only** below the harness via OS DAC -- **that was wrong and it conflated two layers**: id:5937 established that `permissionMode=auto` approves everything, but that is about *permission prompts*; **hooks deny regardless of permission mode**, and this repo already ships PreToolUse/PostToolUse guards (`hooks/pathspec-drop-guard.py`, `hooks/parallel-edit-detector.py`). That is what makes id:d464 possible. (e) The report's "15+ `main checkout dirty` handbacks" does **not** reproduce: `~/.config/relay/relay-events.jsonl` has **29 total across ALL repos, 2 for loderite**, and ends 2026-07-29 -- before the cited run. Mechanism confirmed, count unverified. (f) A "the worktree is unusable so children flee to main" hypothesis I advanced is **refuted** -- loderite's `RELAY_LOG.md` (`:764`, `:768`, `:1802`, `:2626`, `:2681`, `:10389`) shows children hitting absent `node_modules` and trivially solving it with `npm ci` or a symlink from main, with zero handbacks citing unusability. The breaches were path-targeting slips by children whose worktrees were fine.
  - **Existing work to reuse, NOT re-derive**: id:b659 (routed:8adf) already owns the single-repo `isolation:'worktree'` carve-out for when cwd == target; id:077d already owns the consolidated commit-provenance hook engine (which catches *committed* main-writes but never the uncommitted ones this bug produces).
  - **Status 2026-07-30: NOTHING IS QUEUED. Both children are filed for discussion only, by owner directive -- do not promote either to ROADMAP.** Detection-only is nonetheless refused as the answer: this is the 4th occurrence and `verify-isolation.sh` already detects it (too late, unattributed, after the damage). The two candidate directions are id:d464 (deny the write -- **blocked on an afk-compatibility hazard**, see it) and id:34b7 (remove the child's reason and means to reach main -- unaffected by that hazard, but still unqueued). The open question for the discussion is whether *any* cheap enforcement survives the automation constraint, or whether enforcement genuinely starts at the OS-user layer (id:13ae/02c7/8e7a) with everything above it being containment at best.
  - **AMENDED 2026-08-01 (owner-scoped `/relay handoff`): the hold is LIFTED for `id:34b7` ONLY -- **@owner-accepted:2026-08-01**, ratified directly at integrate** -- it is now promoted to `ROADMAP.md` as `[ROUTINE]` with a RED spec (`tests/test_parent_creates_worktree_34b7.sh`), and `id:f91a` itself is promoted as an `@container` epic (non-dispatchable). **`id:d464`'s "DISCUSSION ONLY / DO NOT BUILD" directive STANDS, unamended.** New evidence for 34b7 part (1): this handoff was dispatched into a worktree the ORCHESTRATOR created pre-dispatch, and the child never needed the main-checkout path -- an existence proof for the handoff verdict, by hand, once. The amendment is recorded in `REVIEW_ME.md` for owner confirmation rather than treated as settled.

## From ROADMAP

(4th+ occurrence, INBOUND `routed:f91a` from loderite 2026-07-30) <!-- children:d464,34b7 --> <!-- routed:f91a --> <!-- id:f91a -->

  **RE-LANED 2026-08-14 (`/relay human .`, owner-directed inspection): `[ROUTINE]` → `[INPUT — meeting]`, gated on `id:d464`.** A review box flagged it as an open `[ROUTINE]` with NO RED spec (`grep -rl 'roadmap:f91a' tests/` empty), i.e. not executor-ready. Inspection settles WHY: this is a **pure container** -- it carries `<!-- children:d464,34b7 -->` and no work of its own, so there is nothing for a spec to specify. Child `id:34b7` (the DISSOLUTION half -- parent creates the worktree before dispatch) is `[x]` DONE; child `id:d464` (a PreToolUse hook denying writes into a claimed repo's main checkout) is `[INPUT — meeting]`, explicitly "DISCUSSION ONLY, deliberately NOT queued", blocked on an unresolved afk-compat question. So f91a closes by CHILD CLOSURE (`id:46f6`), not by execution, and its only open dependency is a meeting-lane item. Leaving it `[ROUTINE]` left a specless trap in the dispatchable pool. Do NOT author a RED spec for it. **No `gated-on:d464` marker is written here, deliberately**: `id:d464` lives only in `TODO.md` and is explicitly never queued, so a typed gate pointing at it is a PERMANENT one and `roadmap-lint` correctly reports it as a DEAD-GATE (verified -- adding it took the repo from 9 to 10 warnings). This is precisely the deliberately-held-gate class the `<!-- owner-hold:REASON -->` marker is being built for (`id:d119` ships the grammar + linter recognition; `id:b8e8` applies it). Give this item an `owner-hold:` marker once `id:b8e8` lands; until then the gate is recorded in prose only.
  - **This is a CONTAINER, not a dispatchable unit** -- marked `@container` so no executor picks it up (`classify-repo.sh`'s `is_human` excludes the marker, id:0cf5). It carries the problem statement and the damage trail; the work lives in its two children. Promoted for VISIBILITY of the epic alongside its queued child, per the owner-scoped 2026-08-01 handoff.
  - **Verified defect (see `TODO.md` id:f91a for the full evidence)**: `relay-loop.js:2413` builds agent options as `{ label, phase, schema }` plus `opts.model` -- across every `agent()` call in `relay/scripts/*.js|mjs` the keys are 16×label, 16×model, 15×phase, 2×schema, **zero `cwd`, zero `isolation`**. The child is handed `main checkout: ${unit.path}` (`relay-loop.js:1958`, and again in the resume prompt at `:1988`) with no warning, then told `Work EXCLUSIVELY in that worktree` (`:1963`). Detection (`verify-isolation.sh`, id:f682) fires only AFTER the write, and the residue surfaces as an *unattributed* "main checkout dirty" in a later, innocent run.
  - **Children and their status** -- `id:34b7` (dissolution: parent creates + provisions the worktree pre-dispatch) is the queued executor work, promoted below. `id:d464` (a PreToolUse deny hook) carries a standing owner directive **"DISCUSSION ONLY -- DO NOT PROMOTE TO ROADMAP AND DO NOT BUILD"** and is deliberately absent from this roadmap; do not build it from this entry.
  - **Close condition**: tick when `id:34b7` is closed AND the owner has ruled on `id:d464`. It has no test of its own; `tests/test_parent_creates_worktree_34b7.sh` is the spec for the queued half.
  - **SUPERSESSION NOTE**: `TODO.md` id:f91a still carries a 2026-07-30 status line reading "NOTHING IS QUEUED … do not promote either to ROADMAP". That hold is superseded for `id:34b7` only, by the owner-scoped 2026-08-01 handoff that produced this section. `id:d464`'s hold STANDS. Flagged in `REVIEW_ME.md` for owner confirmation.


## Section preamble (from ROADMAP), relocated 2026-09-03 (`id:800f`)

This is the SECTION preamble that stood under the ROADMAP.md heading
`## Handoff C2 2026-08-01 (relay handoff, owner-scoped 5-item promotion)`
-- it was never part of this item's own line. Reproduced VERBATIM below, em dashes
and retired lane vocabulary included: it is a historical quotation, so the
DECLARATIVE-vs-REFERENTIAL rule forbids "fixing" it here.

Other members of that section carry a `relates:f91a` edge back to this note: none -- this section has a single member.

> Owner-scoped promotion of exactly five already-filed TODO items (ids REUSED —
> single-id-two-views). The scan found 13 promote- and 90 surface-disposition items; the
> surface set is deliberately NOT touched here (id:5eb3 — the `human` verdict's mechanical
> filer owns it). Rationale and full evidence for each item stay in `TODO.md`; these entries
> are the execution spec only.
