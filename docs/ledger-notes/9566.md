# id:9566

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

The fix names "the repo's no-gallery-ack line" but that ack format is a LODERITE contract this repo cannot read — an executor here would have to invent it, and inventing a ratified consumer's format is exactly the guess §5b says not to force. Needs either the literal ack line from loderite's `check-gallery-gate.mjs`, or an owner ruling that the bump should SURFACE rather than auto-ack. `/meeting` or `/relay human` candidate, not a pool item. <!-- id:9566 -->
