# id:fe67 -- fault (a) of `routed:71c6`/`id:4e84` is MITIGATED, not fixed: the classifier still cannot see the dispatch brief

Filed 2026-09-10 by review `relay-20260910-114832-18641` at the moment `id:4e84` was ticked, so
that the half which did NOT ship stays visible. Do not read this as a reopening of `id:4e84`: its
ROADMAP line, detail note and RED spec all scope the work to the LOOP-side widening, and that
shipped and is green.

## What `routed:71c6` filed, and which half landed

The inbound report named **two distinct faults**:

* **(b) strict rank order starves apex and handoff indefinitely** -- FIXED. The chain-end re-ask
  now accepts `review|execute|hard|handoff` and passes `--exclude` naming this run's dispatches
  for this repo (`27a5216b`, four further defects fixed in `b726a1bd`, spec cases A-G green with
  its declared mutation verified).
* **(a) an excluded-but-open item pins the verdict** -- NOT fixed. On `code.lawless` 2026-09-09
  `id:b77b` was the only actionable routine item and the **dispatch brief forbade working it**, so
  `execute` won the branch and the round dispatched nothing useful.

## Why (a) survives the widening

`classify-verdict.sh` has no notion of a dispatch brief -- verified, and already recorded in
`docs/ledger-notes/4e84.md` as the false premise a previous attempt burned on. The widening does
not give it one. What the widening changes is the CONSEQUENCE: the round's second pass now reaches
`hard`/`handoff`, so a poisoned item no longer starves promotion. The first-pass verdict is still
pinned by an item nobody is allowed to work, and that dispatch is still spent.

## The decision this item carries

Either the brief's exclusion set reaches the classifier (a new input, with the usual question of
who owns it and whether a brief is even durable enough to key a verdict on), or the second-pass
relief is declared sufficient and fault (a) is closed as WONTFIX with that reasoning on the
record. Both are defensible; neither is a reviewer's call to settle silently, which is why this is
`[INPUT - decision]` and not `[ROUTINE]`.

## If the answer is "reaches the classifier"

Note the shape before building: `--exclude` (`id:bc2b`) already walks the cascade demote-only and
is the existing lever, so this would be a new SOURCE for an existing mechanism, not a new
mechanism. Measured ladder, for reference: no flag -> `execute`; `--exclude execute` -> `hard`;
`--exclude execute,hard` -> `handoff`; `+handoff` -> `idle`.
