# id:9fa2

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(owner ratified 2026-09-01: "one recorded convention, no per-token edges"). Deliberate same-id mirrors across a parent and its plugin repo (`zkm`/`zkm-whatsapp`) are EXPECTED and must not be adjudicated per token nor allow-listed as "unrelated": `1c7d`, `4b8e`, `d058`, `f5b7`. **THE TRAP, found by a first attempt and reverted:** a purely structural `<parent>`/`<parent>-<suffix>` rule ALSO captured `5e19` and `cfd1` -- the two S4 doctrine-vs-instance tokens the owner separately ruled must get a FRESH ID on the zkm-whatsapp side (`routed:4ede`) -- silently removing them from the decision queue, and captured `df4e` (`zkm`/`zkm-notmuch`) which was never in the needs-a-look set at all. So the rule must exclude explicitly-adjudicated tokens, or be driven by an adjudication list rather than by repo-name shape alone. It must also REPORT each recognised mirror (visible, counted), never silently downgrade -- an invisible downgrade hides real collisions inside a growing plugin family. **NOT structurally derivable:** the S2 repo-MOVE case (`dbe0`, `loderite`→`lodelore`, id carried verbatim) -- `lodelore` is not `loderite-<suffix>`; propose the smallest honest mechanism for it separately rather than stretching the prefix rule. Contract: the four S1 tokens stop erroring, `5e19`/`cfd1`/`df4e` are NOT auto-downgraded, and two genuinely independent repos sharing a token still error. <!-- children-of:cb22 --> — **MERGED 2026-09-01 (suite 543/0/1), pair-scoped per the owner's narrowing ruling. THREE review rounds, each finding a real defect.** Mechanism: `tracker/mirror-tokens.txt` declares `<token> <repo> <repo>`; `validate` requires SET EQUALITY between the declared pair and the repos observed to carry the token, so the same token minted independently in a THIRD family repo stays a class-A ERROR naming both sets. Repo-name shape only GUARDS a declared mirror, never drives one -- the purely structural rule was tried and reverted for swallowing `5e19`/`cfd1` and `df4e`, which is the trap this item was filed about. **What the three rounds caught, recorded because the pattern is the point:** (1) the original family-scoped predicate was satisfied by any SUPERSET, so a collision minted in `zkm-ner`/`zkm-stt` on a declared token was silently absorbed -- the item's own stated hazard, displaced from the token list to the token's repo SET; (2) the homonym allow-list SILENTLY CANCELLED a refusal (the elif chain reached `allowed_homonyms` before the refuse branch) while the tool's own ERROR text told the operator to `--allow-homonym` it -- code contradicting message, with the wrong one being the actionable one. A refused mirror now OUTRANKS the allow-list and the remediation text says fix-or-remove-the-declaration; (3) `fleet-import.sh`'s non-empty probe still matched only the BARE spelling, so under the new format it would have silently dropped `--mirror-file` and reverted the whole convention to strict with ZERO diagnostic -- the driver now matches the bare form and lets exit 2 propagate loudly. **Verified independently of every authoring agent:** live fleet class-A ERROR 7, class-B 1, the same four downgrades (`1c7d 4b8e d058 f5b7`), `5e19`/`cfd1` still erroring, `df4e` unaffected, 0 refusals -- and both negative controls re-run by hand against their respective ancestors, failing for the CLAIMED reasons (case (g) vs `275fec46` on the superset absorption, case (h) vs `66ed9f9a` on the allow-list cancellation). That last check mattered: the first negative control was NON-DISCRIMINATING -- it died at its first assertion against the parent, leaving the superset and bare-token cases as UNREACHED fixtures presented as passing controls. **RESIDUE, not blocking:** the S2 repo-MOVE case (`dbe0`, loderite -> lodelore) is still unhandled and deliberately out of scope -- `lodelore` is not `loderite-<suffix>`, so the prefix rule must not be stretched to it. The proposal on the table is a recorded repo-rename map (`tracker/repo-renames.txt`) consumed by `validate`, which would also serve dangling `blocked_by` targets and `routed:` resolution across a rename; it wants its own item rather than being bolted onto the mirror file. <!-- id:9fa2 -->

## From REVIEW_ME

You tagged it `[ROUTINE]`, and its Contract is precise (the four S1 tokens `1c7d`/`4b8e`/`d058`/`f5b7` stop erroring, `5e19`/`cfd1`/`df4e` are NOT auto-downgraded, two genuinely independent repos sharing a token still error). But the item itself offers two different mechanisms -- "exclude explicitly-adjudicated tokens" from a repo-name-shape rule, versus "be driven by an adjudication list rather than by repo-name shape alone" -- and they differ in what happens to the NEXT plugin pair nobody has adjudicated yet: the shape rule auto-accepts it, the list rule makes it error until a human looks. That is the property the reverted first attempt got wrong (it silently swallowed `5e19`/`cfd1`), so picking between them is the whole safety question, not a coding choice. The item also asks for the S2 repo-MOVE case (`dbe0`, `loderite`->`lodelore`) to have "the smallest honest mechanism proposed separately" -- a proposal, not an implementation. Per review.md §5b that is design-judgment work: it stays in TODO as a `/meeting` candidate. Verified this review: all four S1 tokens are still `# UNCONFIRMED` in `tracker/homonym-allowlist.txt`, so nothing regressed by leaving it. <!-- id:9fa2 -->
