# id:75c8

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

MEASURED 2026-09-02: a live gate run produced 43 fatals, of which 42 were `todo-conformance:length-unshrinkable` (37) and `length-grandfathered` (5) -- non-blocking REPORT lines whose text carries a char count, e.g. `(1367 chars > budget 500, within baseline 3004)`. The item was over budget before and after; only the number moved. Meanwhile the same detector's total findings FELL 669 -> 75, a large improvement the gate could not see. This is exactly the false-red class the meeting warned would erode the gate's authority on day one. Fix: normalise the signal to rule + item, stripping numeric payload, before set comparison. <!-- gated-on:0d7c --> <!-- id:75c8 -->
