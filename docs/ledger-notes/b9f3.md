# id:b9f3

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

`mapfile` (line 174) is a bash builtin; under zsh `is_private_remote_url` dies on `mapfile: command not found` and returns 1, which every caller reads as "public". Verified 2026-09-10: `zsh -c 'source ...; is_private_remote_url fievel:src/x.git'` exits 1 where bash exits 0. Scope honestly: the script normally EXECUTES under its bash shebang, so the exposure is callers that SOURCE it from a zsh context -- a relay human triage agent hit it live and nearly classified `fievel` as public. This is the fail-toward-auto-publish direction the shared-predicate doctrine exists to prevent. Fix: replace `mapfile` with a POSIX read loop, and add a test that sources the lib under zsh. <!-- relates:4d44 --> <!-- id:b9f3 -->
