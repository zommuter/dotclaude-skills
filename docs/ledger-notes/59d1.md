# id:59d1

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

The owner picked two levers, not one, and they attack the same budget from opposite ends: **(1) TRIM THE PREAMBLE** -- a bounded pilot stripping agent-irrelevant sections from delegated subagents' system prompt (`id:c3c1`/`id:10dc` measured a 58.6k-token preamble consuming ~34% of a Sonnet subagent's usable window BEFORE its first tool call), and **(3) SPLIT UNITS HARDER** before dispatch so each brief fits the shrunken window. Rejected explicitly: moving executor work to Opus (cost, and it abandons the Sonnet-as-executor design) and doing nothing. **Evidence:** `id:7829` records Sonnet's usable context dropping ~2.1x between July and August 2026 with 50 measured `Prompt is too long` child deaths, ALL on Sonnet, ZERO on Opus. **Fresh instance, this session:** two of the six `/relay human` triage subagents died of exactly this failure while triaging the backlog that contains this item. **The pilot must watch QUALITY, not just survival** -- the full preamble may be supplying behaviour whose loss is only visible in worse output, which a death-rate metric cannot see; that is the honest weakness of lever (1). Lever (3) depends on the slicer following `-- detail:` pointers, a known open weakness (`id:2ee1` class), so do not assume splitting alone lands. <!-- relates:7829 --> <!-- relates:c3c1 --> <!-- relates:10dc --> <!-- id:59d1 -->
