# id:d464

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

— **⛔ DO NOT PROMOTE TO ROADMAP AND DO NOT BUILD (owner directive 2026-07-30).** The sketch: matcher `Write|Edit|NotebookEdit`, resolve the target path, and if it lies inside a repo holding an active relay claim (`~/.config/relay/claims/*.json`) but outside that repo's relay worktrees, refuse — with the correct worktree path in the message so the child self-corrects. <!-- children-of:f91a --> <!-- id:d464 -->
