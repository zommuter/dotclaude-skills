# id:bd2d

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

— no child dispatched' emitted TWICE as a handback with handbackInvariantViolations direction:reverse, and both name the SAME worktree path as a live hard-repo-1 child; run also flagged repeatHandbackKind:bug-signal (execute x2, hard x2) <!-- id:bd2d -->
