# id:6a5d

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

-- the `id:4263` fix withholds public remotes correctly, but the queue's producer is `integrate.sh` step 8b and the reconcile path has no equivalent; an unattended `--auto` withhold is therefore announced once and never again, where an integrate.sh withhold is durably resolvable. <!-- relates:4263 --> <!-- relates:4d44 --> <!-- id:6a5d -->
