# id:2da8

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

**EDITED 2026-09-03 (`id:64f9`, title rewrite) -- declared, per the notes-are-editable
convention in `CLAUDE.md`.** This item's ledger TITLE in `TODO.md` was rewritten to fit
`LEDGER_ITEM_TITLE_MAX`. Nothing else on the item line changed: lane tags, typed-edge and
`id:` markers, the detail pointer and the checkbox are all untouched. The ORIGINAL title is
reproduced VERBATIM in the final section of this note, so no wording was deleted -- it moved
here. The relocated prose below is otherwise unchanged.

## From TODO

It should re-classify each round and dispatch review + handoff rounds too, matching the Workflow pool's execute→review→hard→handoff verdict order, looping until GENUINELY drained or human-blocked. Pairs with routed:6472 (delegate integrate-verify to a review agent). (owner directive 2026-07-22, loderite drain) <!-- id:2da8 -->

## Original title (verbatim, before the `id:64f9` rewrite)

relay: the off-Workflow DRAIN (drain-driver.mjs / bare `/relay .`) should handle ALL verdict classes — REVIEW (unaudited commits since last ckpt) and HANDOFF (effectively-drained ROADMAP with open TODO backlog / specless items) — not just EXECUTE. Today when execute-work is drained but review/handoff work remains, the drain terminates and the apex must hand-drive review/handoff.
