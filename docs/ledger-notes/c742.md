# id:c742

AUTHORED note (not relocated by `tools/ledger-shrink.py`) -- the verbatim-reproduction
claim in `docs/ledger-notes/README.md` does not apply here. Conventions and backlinks:
`docs/ledger-notes/README.md`.

## From TODO

Filed from the cross-repo inbox, `routed:86e1` (from project_manager, 2026-09-08). The
report named TWO defects. They landed differently, so they are recorded separately.

**Defect (a) -- the missing install -- is FIXED, incidentally.** The report was that
`relay/scripts/lib-repo-section.sh` existed canonically but was absent from the installed
`~/.claude/skills/relay/scripts/`, so `ckpt-tag.sh` died at line 37 with "No such file or
directory" and no relay checkpoint could be written on this machine. Verified 2026-09-08
after the fact: commit `f6fa91d1` ("a repo name is not the string-matcher's to choose",
`id:02fe`) committed the file and added it to the `Makefile` manifest at `:85`, and the
symlink now resolves (`~/.claude/skills/relay/scripts/lib-repo-section.sh` ->
`~/src/dotclaude-skills/relay/scripts/lib-repo-section.sh`, created 17:16). The fix was a
side effect of a different item, not a response to this report. The window was real: the
file was created untracked at 17:05 and installed at 17:16.

**Defect (b) -- the drift checker's stated MECHANISM is REFUTED; the OBSERVATION is not.**
The report hypothesised that `check-install-drift.sh` "enumerates entrypoints and skips
`lib-*` sourced helpers". That is false on both halves, verified by reading the script and
by running it:

- It globs `"$CANON"/*.sh` (`:71`), which includes every `lib-*.sh`.
- It ALSO parses each script's `source` / `.` directives (`source_targets()`, `:55-66`) and
  requires each `.sh` sibling to resolve in the install (`:78-81`). Sourced helpers are
  explicitly covered, by design.
- Hermetic probe run 2026-09-08: a `mktemp -d` install dir symlinking every canonical
  script EXCEPT `lib-repo-section.sh`, checked with the exact `--canonical` / `--installed`
  form from the report, printed `install drift -- 1 script(s) missing` / `MISSING:
  lib-repo-section.sh` and exited 1. It does NOT report clean over that state.

**So the residual is a real unexplained observation with a wrong diagnosis attached.** The
reporter saw "OK -- fully mirrored (scripts + source targets)" with the file demonstrably
absent, and that output cannot be produced by the state they described. Candidate
explanations, none confirmed: the check ran BEFORE the canonical file existed (created
17:05), in which case both loops correctly saw nothing to mirror and the clean report was
accurate for that instant; or a different `--canonical` / `--installed` pair than the one
quoted; or a stale non-symlink copy of the checker.

**Next step is evidence, not a code change.** Get the exact invocation and its timestamp
from the reporting session before touching the checker. Changing a guard on a refuted
mechanism is how a working guard acquires a bug. If the pre-creation-timing explanation
holds, the real gap is different and narrower: the checker cannot flag a script that a
`SKILL.md` or sibling invokes but that does not yet exist canonically -- which is the
`id:18ed` shape (a repo script a `SKILL.md` invokes by installed path that the `Makefile`
never links), not this one.
