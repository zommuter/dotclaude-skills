# id:c442

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

**This note is EDITABLE** (owner-ratified 2026-09-02). If a fleet rule is violated
in the prose below -- retired vocabulary, a lane delimiter, a banned token -- FIX IT
HERE and amend the line above to say what was changed. Notes are not immutable: an
unfixable violation keeps its guard red forever, and this prose gets copied back out
into new items. An undeclared edit makes the verbatim claim above a lie.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From ROADMAP

MEASURED 2026-08-31 by flipping only the input fixture: `input:meeting` -> `lane:untagged`, the `[INTENSIVE]` resource lost, `venue:pool` lost, and one `legacy-hands-unresolved` report row silently gone — `ledger-map.py import` exits 0 throughout. So `tracker/fixtures/repo-alpha/TODO.md` and `tracker/fixtures/expected/{repo-alpha,fleet-collision}.json` CANNOT be migrated on their own: flipping input and expected together would yield a GREEN suite asserting a degraded mapping. Blocks the tracker slice of S8 (`id:ee31`) and therefore S10. Note `tracker/fixtures/repo-alpha/TODO.md:16-17` are deliberate legacy-venue controls exercising the no-1:1-successor and 1:1-rename paths — preserve that pairing. <!-- gated-on:ee31 --> <!-- id:c442 --> (archived — see ROADMAP.archive.md)
