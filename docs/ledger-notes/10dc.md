# id:10dc

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

THREE Sonnet subagents died `Prompt is too long` in one session; a Sonnet control and three full-tier controls survived. Diagnosed by a dedicated agent parsing the JSONL transcripts (deliberately delegated so the bulk never entered the parent's context — that method worked and is worth reusing). **NUMBERS (from API `usage`, authoritative):** effective ceiling **~175k**, not 200k — last successful requests 172,450 / 171,959 / 174,207, consistent with a 200k budget minus a ~24k `max_tokens` output reserve. A full-tier control ran **200,534 input tokens and SUCCEEDED**, so full-tier subagents are not on a 200k window (they inherit the parent's 1M model). **BASELINE PREAMBLE = 58.6k tokens before the first tool call** (system prompt + CLAUDE.md + memory index + tool definitions) — 34% of the usable window, the single largest line item and the only one that is free to cut. **TWO REFUTED HYPOTHESES, both stated confidently by the parent before measuring:** (1) "big-file reads caused it, slicing discipline fixes it" — the agent that OBEYED the discipline (19 bounded `sed -n`/`grep` slices, never a whole-file read) spent 48,793 chars on the same file family versus 50,345 for the undisciplined one, and died at the HIGHEST peak of the three. Discipline changed the SHAPE (19 small slices vs 4 big) not the VOLUME, and each extra slice bought another full thinking turn. No casualty ever read `relay-loop.js` whole. (2) "narrow the brief and Sonnet is fine" — the narrowest brief (2 files, ~12k tok, explicit discipline) still died. **ACTUAL CAUSE — baseline + ACCUMULATION, dominated by the agent's OWN OUTPUT:** for the crux case the agent's cumulative `output_tokens` was **50,607 vs ~33k of tool results** — extended thinking, not reading. Growth was strictly monotonic, no single result above 13.3k tokens. The SURVIVOR made MORE tool calls (86 vs 27/53/42) with a mean result of 1,130 chars (vs 7,112/2,656/3,148), and is the ONLY transcript showing a **microcompaction** (141,876 → 133,256) — the casualties never received one. **CONCRETELY PREVENTIVE, in order of leverage:** (a) **shrink the delegated-agent preamble** — 58.6k is the biggest and cheapest lever, and it is fleet-wide, not repo-specific; does a subagent need the full memory index and every tool definition? (b) scope briefs so no in-scope file exceeds ~15k chars, and PRE-EXTRACT the relevant TODO item text into the brief instead of having the agent grep a 927 KB ledger; (c) route WRITE-HEAVY work (many Edit/Write turns, which drove the two worst output totals) to full tier, where 200k+ demonstrably passes; (d) cap `Read`/`grep` output at the TOOL layer rather than by instruction — instruction-level discipline is now empirically shown not to work. NOTE (c) is a cost decision: full tier per delegated implementation task vs. losing ~130-200k tokens of uncommitted work per casualty (three times today; all three had to be salvaged and committed by hand because agents that die never commit). Related `id:9397` (relay-loop.js size — a real but SECONDARY factor), `id:b59f` (LSP). **RE-MEASURED 2026-08-22 ON A LIVE POOL RUN — the tax is WORSE than the 2026-08-21 figure, and it is UNIFORM ACROSS TIERS.** Source: run `relay-20260822-102233-32252`, per-agent first-message input (input + cache_read + cache_creation) from the workflow's agent transcripts. **Median first prompt, before any tool call: Opus 64,880 · Sonnet 66,580 · Haiku 48,118.** The 2026-08-21 estimate was 58.6k; the measured figure is ~65k. **The uniformity is the finding:** Opus and Sonnet pay essentially the same preamble, so this is NOT model behaviour — it is the harness system prompt plus tool definitions, which is exactly what a custom agent definition replaces. Per-agent peaks in that run: Sonnet 87,129 / 92,362 / 113,793 (n=3); Opus up to 158,847 (n=6). **So for the three Sonnet agents, preamble was 59-77% of their ENTIRE context footprint**, and across the run's ten real agents (103 of the 114 were `model:"bash"` mechanical hops with a 1-token prompt) that is roughly **650k tokens of pure preamble in a single run**. This discharges the "measurement still owed" note attached to this item — what remains unproven is `id:c3c1`'s mechanism, not the size of the prize. **RE-CONFIRMED INDEPENDENTLY 2026-08-27 (fresh sample, and the CODE is the stale copy).** Ordered by `id:502f` to check the `~58,600` figure before anything was built on it. Result: **58,600 is TOO LOW AND ALREADY SUPERSEDED — by THIS ITEM's own 2026-08-22 re-measurement**, which the code never picked up. `prompt-size-gate.mjs:168-171` still reads *"the measured preamble is ~58,600 tok, not 12,000"*, citing id:299c(c) → the 2026-08-21 n=7 sample of ordinary delegated **Agent-tool Sonnet** dispatches — NOT relay-pool children. This line already records the correction (Opus 64,880 · Sonnet 66,580 · Haiku 48,118, live pool run `relay-20260822-102233-32252`, API `usage`, authoritative). A fresh independent sample today agrees: **n=36 real dotclaude-skills relay-pool children** (18 discovery-shard classifiers + 18 review dispatches, 2 runs, 2026-08-26), first-assistant-request total context (`input`+`cache_creation`+`cache_read`) **min 68,288 / median 72,182 / max 72,254**; subtracting the measured dispatch-prompt content (675–3,093 tok) gives an **implied preamble ≈65,200–71,500 tok**. So three independent measurements now cluster at **~65–71k**, and BOTH constants in the code are wrong in the same direction. **This is a DERIVED-DOC DRIFT instance of the exact class the CLAUDE.md rule names** — a ratified measurement restated in a code comment, then left behind when the source was re-measured one day later; the drift favoured the incumbent constant. **NOTE FOR THE OWNER'S PAIRING RULING (not decided here): at a real ~65k preamble a 100,000 budget leaves only ~35k for the dispatch prompt AND all ledger/slice content combined** — which is why `id:502f`'s projected review slices (94.1k/83.3k/30.9k, computed at the 12k overhead) do not survive re-basing. Do NOT read 100,000 as the "Prompt is too long" threshold: children measured here START at ~72k and run fine, so the budget is a POLICY choice about how much room a child needs to still do its work, not a physical limit — the two numbers encode different things and that is precisely why the code says they must move together. MEASUREMENT LIMITS stated: the harness-system-prompt vs tool-defs vs CLAUDE.md split could not be separated (bundled in `cache_creation`); no execute/hard/haiku-tier relay child transcripts survive locally (reaped — this item's own `id:7b73` complaint); and which contract revision id:9eb7's "~5.5k" measured could not be pinned (`executor-contract.md` is 26,603 B today at v16, ≈6,651 tok at the repo's own CHARS_PER_TOKEN=4 and ≈10,001 at `context-budget.sh`'s denser 2.66 ratio — either way above the "~5.5k" the gate comment still cites). **MODELLED 2026-08-27 (owner asked for a table before ruling). A PAIRING EXISTS THAT MAKES BOTH CONSTANTS TRUE WITH ZERO BEHAVIOUR CHANGE: overhead 65,000 + budget 150,000.** Computed over all **56 own repos** (real `ROADMAP/TODO/REVIEW_ME/RELAY_LOG` bytes via `lib-own-repos.sh`, the shipped estimator, the 32,768 B RELAY_LOG window, and a 6,470-char dispatch prompt). Review-verdict repos REFUSED, of 56: **@budget 100,000** — oh 12,000 → **3**; oh 58,600 → 6; oh 65,000 → **8**; oh 72,000 → **16** (nearly a third of the fleet). **@budget 150,000** — oh 58,600 / 65,000 / 72,000 ALL → **3**. So above ~150k the overhead choice stops mattering: the curve is flat because the blocked set is dominated by three outliers, not by the constant. **Identity check, not just counts:** today's blocked set (oh 12,000 / bu 100,000) is exactly `dotclaude-skills, it-infra, loderite`; oh 65,000 / bu 150,000 yields **the same three**, and so does 140,000, 130,000 and even oh 72,000 / bu 150,000 — so the recommendation has real margin. At bu **120,000** it DIFFERS (`lodelore` joins) — that is the regression floor. **RECOMMENDATION (owner's call, not taken here): set `FIXED_OVERHEAD_TOKENS = 65000` and `DISPATCH_TOKEN_BUDGET = 150000` together.** Both numbers become defensible — the first matches three independent measurements, the second is chosen so the refusal set is provably unchanged — and the current knowingly-false 12,000 goes away. **WEAKNESSES, stated:** (a) it is calibrated to PRESERVE today's refusal set, which assumes that set is correct; it does NOT independently validate that 100,000 was ever the right working-room figure, and that question stays open. (b) It does NOT fix `id:502f` — the same three repos stay blocked, by design. (c) The 6,470-char prompt is a single measured sample, not a distribution. (d) Raising the budget raises the ceiling a child may GROW into during work; the model sizes the START only, and nothing here measures peak-during-work. **WINDOWS MEASURED 2026-08-27 — the per-tier premise at `prompt-size-gate.mjs:29-38` is FALSIFIED, and that comment PRE-REGISTERED this exact trigger.** It reads *"ALL FOUR carry a 200,000-token context window — no dispatched tier has a different one … so a tier-keyed budget table would hold four identical rows and buy nothing. … If a tier with a different window is ever dispatched, THIS is the line to split per tier."* The condition has now FIRED on measurement (28,365 transcripts, API `usage`): `claude-opus-5` reached **378,108** direct and **234,302** in-pool with **zero** `Prompt is too long` failures ever, while `claude-sonnet-5` is capped ~**176k** with 50 genuine deaths (see `id:7829`). The tiers differ by **>2x**, so the one-flat-number justification is spent. **This is a legitimate pre-registration firing, NOT a reinterpretation** — the comment named the artifact and the condition, and the condition is met. **RECOMMENDED PAIRING, given the owner's ruling that a 500,000 budget is "okayish but highly undesirable" (wasteful):** `FIXED_OVERHEAD_TOKENS = 65000` (ratified) with a **per-tier** budget — **Sonnet/execute 100,000** and **Opus/review+hard+handoff 300,000**. Effects at oh 65,000, computed over all 56 own repos: EXECUTE refuses **4** (`lodelore` 111k, `it-infra` 176k, `loderite` 252k, `dotclaude-skills` 430k) vs 3 today — the extra refusal is the gate becoming HONEST, since a Sonnet child starting at >100k of a ~176k window has little working room. REVIEW at 300,000 refuses **1** — only `dotclaude-skills` (445k); `loderite` (268k) and `it-infra` (187k) are unblocked, resolving most of `id:502f` WITHOUT the review-shaped slice. 300,000 sits below both demonstrated-safe Opus points, so it is inside measured territory rather than extrapolated. **WEAKNESSES:** Opus's true ceiling is UNKNOWN (378,108 is the largest ever demanded, not a limit); `dotclaude-skills` stays blocked and genuinely needs ledger work (1,157,933 B TODO.md), which is honest signal, not a false refusal; and raising a budget raises what a child may GROW into — all of this sizes the START only. **FIGURES CORRECTED 2026-08-27 (same day, from a live run) — this item's EXECUTE refusal counts OVER-PREDICT and must not be quoted as-is.** The modelled *"EXECUTE refuses 4 — `lodelore` 111k, `it-infra` 176k, `loderite` 252k, `dotclaude-skills` 430k"* was computed on the **UNSLICED** path (whole `ROADMAP.md`+`TODO.md`). Real `execute` units carry an `id:e68f` item-keyed slice, and `oversizeDispatchReason` sizes on the SLICE when one exists (`id:35b7`) — measured **1,473–1,504 bytes** in run `relay-20260827-154820-505`. So a sliced execute unit is sized at roughly 65,000 overhead + ~1.5 KB ≈ **66.5k against the 100,000 Sonnet budget** and dispatches REGARDLESS of ledger size. **`loderite` execute was never going to be refused, and it was in fact dispatched twice that run** (round 7 integrated `id:b5b1` cleanly; round 8 died on `id:551f`, see `id:7829`). **The REVIEW figures are UNAFFECTED and stand** — a review unit is structurally unsliceable (`id:502f`), so it really is sized on whole ledgers. Treat the execute column as an upper bound on refusals that only binds when slicing FAILS; the review column is the real one. **Second-order consequence, worth stating because the pairing ruling rested partly on this:** the per-tier split does NOT protect Sonnet children — the gate's Sonnet budget is nearly irrelevant for sliced units, and what actually bounds them is the ~176k ceiling reached DURING work, which no dispatch-time gate can see (`id:0a08`). <!-- id:10dc -->
