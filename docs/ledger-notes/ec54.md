# id:ec54

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

An investigation 2026-09-01 found compliance never carries forward WITHIN a meeting: 17 of 26 blocks are repeats in the same session 30-90 min apart, so each block IS obeyed and the next decision point fails identically. Mechanism, with positions: the rule is stated FIVE times (`format.md:163`, `SKILL.md:104`, `:94`, `:98`/`format.md:42`, `:153`) and ALL FIVE sit at meeting SETUP, read once before a meeting that then runs for many turns; no hook re-injects it, the guard is `Stop`-only and purely reactive. RULED OUT: the emit-then-ask loop shape (`format.md:163` already bundles them atomically) -- the failure is the default present-then-pause chat prior, which looks exactly like a compliant decision point right up until the tool call is added. `4b8ff616` shipped the cheap half (forward-scoping `BLOCK_MESSAGE` + a per-session block counter), whose own honest weakness is that it is still prose through the reactive channel and leaves the dominant cause untouched. **The structural option, NOT built:** a hook that re-injects a short same-turn reminder before each new meeting turn, changing DELIVERY POSITION rather than wording. **Decide on evidence, not now:** if block counts still reach 2-3 per session after `4b8ff616`, that is the predicted signal to build it. **Do NOT read the block count as a compliance rate** -- the guard log has ZERO `OK` events in 95 lines ever, because a compliant turn never reaches the check (`AskUserQuestion` does not yield to `Stop`), so the log is structurally blind to successes and can only ever record failures. <!-- children-of:2419 --> <!-- id:ec54 -->
