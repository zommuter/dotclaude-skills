# id:37cb

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

Records are ['ts','input_hash','result','bash','lean'] and classify-repo.sh mktemp -d + rm -rf on EXIT, so today's diverging inputs are already unrecoverable. MUST land BEFORE relay-core units aefd/5c76/7690/d6fa/9d6e close, else post-fix divergence is unattributable by record <!-- id:37cb -->
