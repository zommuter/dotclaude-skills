# id:95a3

Authored directly, not relocated by `tools/ledger-shrink.py`. Conventions:
`docs/ledger-notes/README.md`.

## From ROADMAP

**The parked `id:4d65` attempt is RED in two ways, and one of them is a live defect in
`ratify-queue.sh` rather than in its test.** Found 2026-09-08 during a `/relay reconcile
--all` sweep, on the parked branch
`relay/orphan/relay-20260907-100619-27900-execute-repo-0` (commit `2c45374c`, an `id:f272`
commit-and-park "WIP UNVERIFIED residue" auto-commit: `relay/scripts/ratify-queue.sh` +77
lines, new `tests/test_ratify_queue_self_verify.sh` +193).

**The measurement.** Full `tests/run-tests.sh` in a detached worktree of that branch:
`593 passed, 2 failed, 0 errored, 8 expected-red`. Both failing files were then run on
`main` and both PASS there:

| Test | on the parked branch | on `main` |
|---|---|---|
| `test_ratify_queue_consumer.sh` | `FAIL: a malformed line suppressed the valid entries -- silent truncation` | `PASS` |
| `test_negative_case_runner_a73c.sh` | `FAIL: (i) --list over the real repo must succeed (allowlist + headers parse), got rc=2` | `PASS` |

The branch lacks 3 commits from `main` (`8d7e44bc`, `664b5065`, `2c00599d`); none of the
three touches either test, `relay/scripts/ratify-queue.sh`, or
`tests/verify-negative-cases.py`, so the delta is the branch's own diff and not a stale
base. That check matters -- the sibling `id:be51` branch was 131 commits behind, where the
same reasoning would not have held.

**Failure 2 is localized and is a one-token typo in the new test's own declaration.** The
file declares `# fails-against-assertion: a self-verified-landed entry still shows in the
default (pending) listing`, but its actual line-130 message reads `... still shows in the
default pending listing:` -- **without the parentheses**. So the declared substring matches
zero `FAIL:` lines in the body, and `verify-negative-cases.py` exits 2 as a CONFIG ERROR,
exactly as the CLAUDE.md rule specifies ("0 or 2+ is a CONFIG ERROR, exit 2"). The guard is
working; the declaration is wrong. Fix is to make the two strings agree.

**Failure 1 is NOT localized and is the more serious of the two.**
`test_ratify_queue_consumer.sh` reports that a malformed line in the queue suppresses the
valid entries around it -- silent truncation of a human-backlog surface. That is the
`id:da87` failure class (a reader that quietly drops the rows it exists to show), reached
here through the `list` rewrite rather than through a truncating pipe. No bisect was run
and no responsible hunk within the +77 lines was identified.

**Why this is worth a ledger line rather than only a commit message.** The parked branch is
the only artifact carrying either finding, and its commit is stamped "do not treat as
reviewed" -- so nothing in the ledger would otherwise record that the `id:4d65` seam has
been attempted and came back red. The next executor to pick up `id:4d65` would re-run the
same 193-line test and re-derive both failures. The older twin branch
`relay-20260905-113859-5807-execute-repo-0` (identical but for a weaker
`grep -q "$M2"` where this one has `"${M2:0:12}"`) was discarded in the same sweep, so this
branch is now the sole copy.

**Relationship to `id:ceca`, which covers the same seam.** `ceca` measured the *other* twin
(`e3762d03`, discarded in this same sweep) by running only the branch's own test file, and
reported 11 PASS / 1 FAIL. That finding is REFUTED -- see the CORRECTION section of
`docs/ledger-notes/ceca.md`: its failing assertion grepped a 40-char sha against a field
`main` has always rendered as `${merged:0:12}`, so `--tsv` never dropped anything. The
practical consequence for this item: the surviving branch's `grep -q "${M2:0:12}"` is the
correct repair of that assertion, **not** a weakened test, and the two failures recorded here
are entirely distinct from `ceca`'s. They were invisible to `ceca` because running the seam's
own test file alone cannot surface a regression in a *neighbouring* test -- which is why the
full suite is the measurement that counts.

**What this does NOT establish.** That the branch's approach to `id:4d65` is unsalvageable
-- only that it is red today. The `list` self-verification logic may well be sound with the
truncation bug fixed; nobody has looked. Nor is it established that the two failures share
a cause; they are independent on their face.

**Acceptance**: whoever next works `id:4d65` either repairs this branch -- both failures
green, with the truncation cause stated rather than merely no longer firing -- or records a
decision to start fresh. In either case `test_ratify_queue_consumer.sh` and
`test_negative_case_runner_a73c.sh` pass alongside a green
`tests/test_ratify_queue_self_verify.sh`. **Done-check**: full `make test` green with the
seam's own test present and `verify-negative-cases.py --list` exiting 0.

**Context**: `relay/scripts/ratify-queue.sh` (`list`, and `_verify_remote`/`_verify_pending`
~lines 267-360), `tests/verify-negative-cases.py`; the parked branch above, if it still
exists (`git branch -a --list 'relay/orphan/*'`).
