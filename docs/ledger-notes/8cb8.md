# id:8cb8

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

— carved out of id:de31 (C7 `decision-queue.sh`, already `[x]` in ROADMAP.archive) on owner sign-off 2026-07-23 (relay human). REVIEW_ME flagged that `tests/test_decision_queue.sh`'s declared scope ("record format + flock'd add/list/resolve helper") is a SUBSET of C7's acceptance: this half — the loop-side forced write so a surface-item lane-triage can NEVER silently no-op — is unspecced/unexercised. Needs its own RED spec (a case-g/h path that MUST write to the decision-queue, asserted, not a no-op). Split so C7 isn't frozen "done" on a green-but-narrow test. Relates id:de31, id:dda6 (sibling half). <!-- id:8cb8 -->
