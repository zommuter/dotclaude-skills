# id:b048

Relocated from the ledger by `tools/ledger-continuations.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From ROADMAP

  - **Context**: owner-ratified 2026-09-02 as an amendment to the `id:0d7c` format. Measured: TODO.md head-line bytes 217,931 vs continuation 36,693 (continuations 14%); ROADMAP.md head-line 44,754 vs continuation 178,299 (**continuations 79%**). The two ledgers are mirror images, so a head-line budget is right for TODO and near-vacuous for ROADMAP -- a `--min-chars 500` dry-run recovers 216 chars from ROADMAP (0.09%). This is the loderite `id:0b70` vacuous-check class: green and proving nothing. It ALSO corrects the standing claim that head-line prose is 95.5% of the bytes, which is a TODO-only figure.
> **SUPERSEDED 2026-09-02, BEFORE IMPLEMENTATION -- the three clauses below are the
> ORIGINAL block-size scoping and are NOT what shipped.** The owner replaced the whole
> premise the same day with a LINE GRAMMAR: a ledger line is valid iff it is blank, a
> heading, or an item, and there are NO continuation lines at all. So there is no block to
> budget -- a continuation line is simply invalid. Recorded here rather than deleted
> because a reviewer checking `b048` against its original acceptance would correctly judge
> it not done, and that mismatch was itself a review finding: the supersession had been
> stated only inside a test comment, which is not where a ledger reader looks.
>
> **The clauses that actually govern**, matching what shipped in `bbdfa31d`:
> - **Acceptance**: `todo-conformance.sh` validates every line of TODO.md and ROADMAP.md
>   against the three-shape grammar and reports each violation by class. Report-only: every
>   `grammar-*` class counts in `findings` and NEVER in `strict_findings`.
> - **Tests**: `tests/test_todo_conformance_grammar_b048.sh`, hermetic, one case per class
>   plus the report-only guarantee and an anti-vacuity assertion.
> - **Done-check**: `relay/scripts/todo-conformance.sh ROADMAP.md` reports the continuation
>   lines and the other structural classes; the id token pattern is derived from
>   `lib-anchored-id.sh` rather than re-spelled.
>
> The one clause that SURVIVES the supersession, because it was never about blocks: the
> baseline stays LENGTH-keyed and never ID-keyed (the `id:cb3e` re-grandfathering trap).

  - **Acceptance (ORIGINAL, superseded)**: the conformance budget measures the item BLOCK (head line plus indented continuations, terminated by the next head line, a blank line, or a heading -- the rule verified against `id:401c`). Both ledgers use the same rule; the 500-char figure is RE-DERIVED for blocks rather than assumed, since a block legitimately carries Acceptance/Tests/Done-check. Monotonic-shrink semantics from `id:0d7c` are preserved and the baseline stays LENGTH-keyed, never ID-keyed (the `id:cb3e` re-grandfathering trap).
  - **Tests (ORIGINAL, superseded)**: a case proving a ROADMAP item with a short head and a multi-thousand-char continuation block is REPORTED -- the exact shape (`id:6176`, 191-char head / 6,825-char block) that passes today.
  - **Done-check (ORIGINAL, superseded)**: `relay/scripts/todo-conformance.sh ROADMAP.md` reports the >3,000-char blocks; the 14 measured today are visible to it.
