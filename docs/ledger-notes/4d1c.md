# id:4d1c

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

**This note is EDITABLE** (owner-ratified 2026-09-02). If a fleet rule is violated
in the prose below -- retired vocabulary, a lane delimiter, a banned token -- FIX IT
HERE and amend the line above to say what was changed. Notes are not immutable: an
unfixable violation keeps its guard red forever, and this prose gets copied back out
into new items. An undeclared edit makes the verbatim claim above a lie.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From ROADMAP

-- "run the one test" is therefore an unreliable verification method in this repo. MEASURED 2026-08-31: `bash tests/test_backtest_fidelity.sh` exits 1 with `lane-vocab: BLOCKED` on the CURRENT tree AND on the pre-session baseline `8c8b40a2`, while `tests/run-tests.sh tests/test_backtest_fidelity.sh` reports 1 passed. PRE-EXISTING, not introduced by the delimiter migration. Fix: build hermetic fixture repos with the hook path neutralised (`git -c core.hooksPath=/dev/null init`, or unset it in the fixture's own config) so a fixture never inherits the caller's global hooks. Acceptance: `bash tests/test_backtest_fidelity.sh` run DIRECTLY exits 0, and so does every other test that builds a fixture repo; the ratchet still fires on the REAL repo (covered by `tests/test_lane_vocab_ratchet_hook.sh`). Done-check: a loop running every `tests/test_*.sh` directly reports the same pass set as `tests/run-tests.sh`, and `tests/run-tests.sh` stays green. TODO twin carries the full measurement. <!-- id:4d1c --> (archived — see ROADMAP.archive.md)
