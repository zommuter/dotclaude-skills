# id:e4e4

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(owner question 2026-08-27: *"since bash is 'free', would it make sense for the classify task to spawn parallel classifiers for each repo individually instead of the six shards currently used? Or instead a single bash script that internally forks/parallelizes adequately?"*) **The premise is CORRECT and worth restating, because a stale CLAUDE.md line said otherwise until 2026-08-26**: both hops dispatch at `MECH_MODEL` (`relay-loop.js:137`) = `'bash'` unless `MECH_FALLBACK === 'fallback-haiku'` — the prelude (`:1944`, id:86a2) and the shard (`:2113`, id:24ec, where `discover-chunk.sh` is ONE fenced `relay-mech` command with ZERO agents and no LLM judgment). So sharding is NOT an inference-cost tradeoff; the 6 is inherited from when it might have been. **Three candidate designs**: (a) status quo — 6 shards, round-robin chunked; (b) one `agent()` per repo — maximal isolation and per-repo failure attribution, but each is still a Workflow agent, and the harness caps concurrency at `min(16, cpu_cores-2)`, so ~62 own repos would queue ~14-wide and pay 62× agent-dispatch overhead for work that is pure bash; (c) ONE mechanical hop whose script forks internally (`xargs -P`, GNU parallel) — no per-repo agent overhead at all, one hop, N forks, and the parallelism degree becomes a tunable rather than an agent count. **(c) looks strictly better on cost and is the shape the mechanize-first rule points at — but it is NOT obviously safe, and that is the actual question to answer**: the shard runs `reconcile-repo.sh` LIVE with bounded git side effects (fetch, ff-merge when behind origin, `uv.lock` cascade commit, worktree reap+park), so N-way forking means N concurrent git mutations. Cross-repo they are independent, but they contend on SHARED state — the flock'd `relay-state-write.sh`, `relay.toml`, the inbox — which may serialize the very thing being parallelized, and (b)'s per-agent isolation is what currently keeps one repo's git failure from taking out a batch. **Measure before building** (observe-before-preventing + the id:415b determinism gate): the per-scan git side-effect cost is UNMEASURED (CLAUDE.md names this explicitly as the thing to price in any continuous-dispatch design), and the id:c3a6 signature cache already elides unchanged repos, so the marginal win may be small on a steady-state fleet and large only on a cold/churny one. Get a `relay-burn.sh`-style wall-clock breakdown of the discovery phase first. **Relationship to [[79fd]]**: that item is about the round BARRIER (when re-discovery may start); this is about the fan-out GRANULARITY within a discovery pass. They compose — continuous re-discovery makes granularity matter more, not less — but either can land alone. <!-- gated-on:79fd --> <!-- id:e4e4 -->
