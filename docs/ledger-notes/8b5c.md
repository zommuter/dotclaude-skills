# id:8b5c

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(surfaced 2026-08-26, run `relay-20260826-162405-7522`). Verbatim from `RELAY_STATUS.md`: *"lean4btc execute claimed nothing was dispatchable while the deterministic open_hard_pool count is 7, and it returned 1 considered id(s) [5e0b] of which NOT ONE is in the resolved pool set [de86, de30, 7182, 1d24, d9fa, a63e, c4c3] — ZERO overlap. The child looked somewhere OTHER than the queue — exactly the id:7517/routed:2d94 failure (it swept for the retired …)"*. The detector fired correctly and is doing exactly its job — this item is about the underlying CHILD behaviour it caught, not the detector. Why it matters: a child that reports 'nothing dispatchable' while 7 real items sit in its pool produces SILENT STARVATION — the repo reads as drained while work is queued, which is the id:907e/id:cc90 family. Cross-reference the named prior instance `id:7517` / `routed:2d94` BEFORE designing a fix: if it is genuinely the same failure recurring after a fix, that is a regression and the earlier fix's test is inadequate; if the earlier fix never landed, this is simply its second sighting. Determine which — do not assume. Same run also SIZE-OUT'd `id:9ee4` (`Btc.Core.Codec`) pre-start with no commit and a clean worktree, which is the size-guard working, not a defect. <!-- id:8b5c -->
