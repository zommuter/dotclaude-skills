# id:535d

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

**EDITED 2026-09-03 (`id:64f9`, title rewrite) -- declared, per the notes-are-editable
convention in `CLAUDE.md`.** This item's ledger TITLE in `TODO.md` was rewritten to fit
`LEDGER_ITEM_TITLE_MAX`. Nothing else on the item line changed: lane tags, typed-edge and
`id:` markers, the detail pointer and the checkbox are all untouched. The ORIGINAL title is
reproduced VERBATIM in the final section of this note, so no wording was deleted -- it moved
here. The relocated prose below is otherwise unchanged.

## From TODO

Found 2026-08-26 by a verification pass over the full advisory list; **verified independently, every claim.** **The mechanism**, `meeting/orphan-scan.sh:424`: it resolves an item's test with `grep -rlE "# roadmap:$token([^0-9a-f]|$)" … | head -1`. That pattern is NOT anchored to a file HEADER, so an **inline trailing comment** in an unrelated test claims ownership — and `head -1` then throws away the genuine owner. **Measured on id 6217:** two files match `# roadmap:6217`. The one it picked, `tests/test_make_test_files.sh`, is headed `# roadmap:d3f8` and matches only at line 14, a trailing `# roadmap:6217, currently open+RED` on a fixture-path assignment. It ran that file, saw green, and reported the item TICK-READY. The REAL spec, `tests/test_dryround_single_definition_6217.sh` (properly headed `# roadmap:6217` on line 2), is **RED**: `isDryRound` is still defined twice — `relay/scripts/drain.mjs:102` and `relay/scripts/relay-loop.js:1593`. **Why this is 🔴 and not noise: acting on the advisory would have broken the suite.** `tests/test_make_test_files.sh:18-19` greps ROADMAP.md for that id still being an OPEN checkbox and exits 1 with *"fixture precondition failed: id 6217 is no longer an open ROADMAP item"* — that test **requires it to stay unticked**, because it uses it as its known-RED fixture. So the detector recommended an action that breaks the very file it consulted. The advisory also claimed "no gate": false — `ROADMAP.md:1458` carries `[INPUT — decision]` + `🚧 GATED (auto, id:3801; route:decision-gate)` with two unresolved blockers. **Fix:** anchor the ownership lookup to a header (first ~3 lines), and when several files match, either surface ALL of them or refuse rather than `head -1` — a silent pick among candidates is what turned a RED item green. Same family as the banked "grep the ASSERTIONS, not the filename" lesson, one level up: here it grepped the right token in the wrong file. Relates id:b3ee, id:259f, id:e1bb. — **THE MECHANISM HALF OF THIS IS NOW FIXED (2026-09-01) as part of `id:4425`, which re-discovered defect (2) independently a week later; see the note there.** Both halves this item measured are addressed: the unanchored match no longer claims ownership when the matching LINE names a foreign test file, and the `head -1` discard is gone -- the lookup now iterates all matches and takes the first surviving one, so the genuine owner is selected rather than thrown away. Verified on this item's own worked example: `6217`'s two matches now resolve to `test_dryround_single_definition_6217.sh` (the real owner, exits 1) instead of `test_make_test_files.sh`. **NOT ticked, and deliberately so:** this item ALSO proposed anchoring the link to a file HEADER, which is strictly stronger than the foreign-file rule that `4425` ratified and would reject link shapes this fix still accepts. Whether the header anchor is still wanted on top is the owner's call, not a session's -- close it or keep the residue open explicitly. — **OWNER RULED 2026-09-01: KEEP OPEN for the header anchor.** The mechanism half is fixed (see the id:4425 note) and is NOT the residue. What remains is the stronger rule this item proposed: anchoring the test-owns-item link to a file HEADER rather than merely rejecting a line that names a foreign test file. The concrete gap that leaves today: a trailing `# roadmap:XXXX` on a line naming NO file at all still counts as a valid link. No case is known where that under-catches -- it is a speculative residue, deliberately kept. <!-- id:535d -->

## Original title (verbatim, before the `id:64f9` rewrite)

🔴 [ROUTINE] **`orphan-scan.sh --shipped` reported TICK-READY for an item that is RED, GATED, and whose ticking would BREAK the suite — the owner-linked-test lookup is unanchored and then silently discards the real owner.**
