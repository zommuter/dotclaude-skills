# id:32f9

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From ROADMAP

and every **stored** tag is rewritten to the hyphen"). MEASURED 2026-08-31 by review: `relay/scripts/handback-followup.py:60` (`GATE_TAG`, substituted into a ROADMAP line at :114/:116 and wired from `relay-loop.js` in 13 places) and `relay/scripts/lane-convert.sh:185-186` (its two `repl=` strings) both still WRITE the U+2014 spelling, so the next handback gate or vocabulary conversion re-dirties the live ledgers S9 just brought to 0 live U+2014 lane tags and breaks S10's closing condition. id:1a03's OWN declared done-check (`git grep -nE '\[(HARD|INPUT|INTENSIVE) U+2014 ' relay/scripts/*.js *.mjs *.py` must return nothing) still returns 48 hits. Flip both emitters and, IN THE SAME COMMIT, the three tests that pin the emitted spelling (`tests/test_handback_followup.sh:41,58`, `tests/test_lane_vocab_both_sides_4b64.sh:181`, `tests/test_lane_convert.sh:101-102`) -- flipping either half alone turns the suite red. Acceptance: handback-followup and lane-convert EMIT `[INPUT - decision]` / `[INPUT - meeting]`, while every reader still ACCEPTS both spellings (tolerant-read / canonical-emit is unchanged). Done-check: `tests/run-tests.sh` green AND `grep -n "GATE_TAG = " relay/scripts/handback-followup.py` plus `grep -n "repl='\[INPUT" relay/scripts/lane-convert.sh` show only hyphen spellings. <!-- id:32f9 --> (archived — see ROADMAP.archive.md)
