# id:1cb8

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(Fable review 2026-07-08) — verified against `mechanical-daemon.sh`: (1) daemon death between `:109` (mv→running) and `:111` strands the recipe in `running/` FOREVER — no code anywhere scans running/ (recipe-manifest.md promises it's a recoverable marker; nothing delivers); add a startup-tick reaper (stale running/ → surfaced/requeued LOUDLY); (2) the only trigger is edge-based (`mechanical-daemon.path` PathModified on pending/) — a DEFERRED recipe doesn't modify pending/, so the documented "retried next tick" is actually "stalls until an unrelated write"; add a `.timer` companion; (3) no flock — a manual `run` racing the `.path`-fired service makes the loser's `mv` fail and abort the whole tick mid-batch under `set -e`; (4) `est_wall` gates launch but `bash -c` at `:110` has NO `timeout` — a hung cmd holds the oneshot active and blocks every future tick; wrap in `timeout <est_wall×k>`; (5) `:102` only PROBES the resource claim, never ACQUIRES it — two recipes (or recipe + concurrent pool job) both see "free" and launch together on one GPU; acquire via claim.sh for the run duration (relates the id:33d3 claim-liveness work); (6) `relay-intensity.sh:49` and `resource-probe.sh:35` disagree on the resource vocabulary and `recipe-validate.sh` checks against neither → a plausible `resource:"llama"` recipe defers FOREVER as resource-unavailable — a silent-forever-defer (id:4347 class); one vocab source, validate at intake; (7) inject failure after a successful run is a log-line swallow (`:114-116`) — a completed mechanical run never gets reviewed; escalate via the decision-queue (id:de31), never log-and-continue. TRUST BOUNDARY (meeting lane — folded into id:d0c0/ec23): `bash -c` runs recipe cmds verbatim as the user and the "only relay writes pending/" invariant is documented, not enforced (plain user-writable dir + auto-fire `.path`). <!-- id:1cb8 -->
