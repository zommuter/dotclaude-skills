# id:9d24

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

— found by the control-flow review 2026-08-21. `id:353e`'s demote loop calls `breakerAllows()` once per demote ATTEMPT, and each call increments `redispatchGuard[key].count`. A class the breaker REJECTS is still counted, so a later round trips that class's breaker earlier than its real dispatch history warrants. The in-code comment claiming the extraction is *"byte-for-byte what the inline loop did before"* holds for the FIRST call only — the pre-change code called it once per round, the new code can call it up to `DEMOTE_MAX_CLASSES` times. Bounded (8 per round) and fails in the SAFE direction (over-suppression, never under), so it is a nit rather than a defect — but it silently biases a mechanism whose whole job is counting, and the comment is now wrong. FIX: count only on an ACCEPTED dispatch, or split the predicate from the counter so the demote path can ask without incrementing. Relates id:353e, id:365b, id:1432. <!-- id:9d24 -->
