# id:e2c3

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

Same shape as the existing hooks/pre-commit-lane-vocab.sh ratchet. Without it the blind spot any gate detector has is licensed to GROW: loderite's 3 stale gates used 3 different encodings, and only reading English found all 3. Whole-ledger normalization is NOT in scope (that is cartulary corpus-encoding). <!-- id:e2c3 -->
