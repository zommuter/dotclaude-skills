# id:ab98

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

The loop is 'for t in ob["children_of"]: it["parent"] = uid_of(repo, t)' -- it["parent"] is a SCALAR overwritten on every iteration, so an item declaring more than one parent via children-of: keeps only the LAST one, with no warning. Meanwhile :643-646 appends children: to it["children"], a list, so the two halves of one relation are stored in structurally different shapes and nothing reconciles them. id:7a9c currently records the non-normalisation but NOT this overwrite; the data-loss detail is what makes it a correctness bug rather than an ergonomics gap, and it matters directly to inflownistration id:2b63 (routed:3100), whose chain-walker would traverse exactly this structure. Verified by reading the file 2026-09-01, not inferred. <!-- id:ab98 -->
