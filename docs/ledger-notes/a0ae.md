# id:a0ae

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

-- whose body lists the fleet's existing typed edges as backticked literals including 'gated-on:' -- was flagged GATED with no gate anywhere in the item. Observed live 2026-09-01; the item was correctly judged pick-eligible only because a human checked WHY the flag fired. This is a live instance of the marker-contagion class loderite already memorialised (ledger-is-the-control-plane-markers-are-contagious): ed3a's own body deliberately avoids writing the literal marker token for exactly this reason, which means authors are already routing around the tooling. CONTRACT: the gate check distinguishes a real marker from a prose mention (anchor on the HTML-comment marker form, or exclude backticked spans) without suppressing genuine gate vocabulary. Note this cuts both ways for inflownistration id:2b63 (routed:3100): the edge graph a chain-walker consumes is simultaneously UNDER-declared (authors dodge literal markers) and OVER-matched (prose mentions create phantom edges). <!-- id:a0ae -->
