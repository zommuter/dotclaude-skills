# id:a192 -- the `expires-on:` edge and its guard (SHIPPED 2026-09-10), plus the owner rulings around it

No open ledger line: the work landed the day it was filed. This note exists because two OWNER
RULINGS attach to it and had nowhere else durable to live.

## What shipped

`relay/scripts/expires-on-scan.sh` plus the `<!-- expires-on:XXXX -->` marker. A clause that is
only correct until an item closes declares its own expiry; the scanner reports it once that item
closes. Report-only, never edits. Registered in the Makefile (the install-manifest test caught that
`make install` would otherwise never symlink it). Spec:
`tests/test_expires_on_edge_guard_a192.sh`.

Deliberately NOT a prose detector: of 17 already-stale clauses measured in live files, about 13 are
test headers reading "RED until id:XXXX lands", which is legitimate provenance rather than a broken
promise. A detector would be wrong 13 times in 17 and get muted.

**Known limit, stated because it is the motivating case:** the clause that prompted this lives in
`~/.claude/CLAUDE.md`, outside this repo, so the hermetic repo test cannot scan it. The scanner takes
`EXPIRES_ON_EXTRA_PATHS` for exactly that, but nothing yet WIRES a non-test entry point that names
that file -- so the motivating clause is still unguarded. Tagging it with an `expires-on:0246`
marker is a pending, owner-approved edit to a protected path.

## OWNER RULING 1 (2026-09-10): the inflownistration carve STAYS PARKED

The owner was initially unsure and ruled after the evidence came in. The tripwire has ALREADY fired
once and was ratified 2026-08-21 for the `/inflownistration` skill's mode (c), scoped precisely to
format-parsing for row insertion, never semantic consumption of the `●◐○` marks, with
`mathematical-writing` `id:aae4` (the standalone engine) left untouched.

This guard lives in `dotclaude-skills`, reads no inventory and consumes no marks, so it sits
FURTHER from the carve than something already approved. The ruling is therefore that it does not
fire the re-check.

Recorded weakness, which the ruling accepts rather than denies: the accretion gradient is real. A
future session could generalise this guard fleet-wide by reading the instance row, and that is the
drift the tripwire names. If that happens, it fires the re-check then.

## OWNER RULING 2 (2026-09-10): an instance row in `inflownistration/docs/instances.md` is APPROVED

Prose only, no executable -- which is what that repo is for, and the row-insertion boundary is the
already-ratified one. **Not yet written**, deliberately: the inventory grades components
(T/M/C/H/S/G/R) and carries a Status column, so filing a row before the guard ran would have meant
grading something that did not exist. It has run now, so the row can be graded against the built
thing.

Expected grading, to be verified rather than asserted when the row is written: **S** is the core
(this IS staleness propagation), **T** is the declared `expires-on:` link, **M** is the scanner
computing the obligation, **H** is the human deleting the clause -- and the owner's own observation
sharpened that last one: pre-approval at WRITE time is still H, and better informed than approval
weeks later. That makes **H** reachable as `●` via pre-ratification and **G** as `●` via the
report-only fallback for an undelimited marker, IF the auto-apply half is ever built.

Auto-apply is NOT built and was not part of this item. Its design, from the owner's question: a
delimited region (`begin`/`end`) plus a declared `action:delete` or `action:replace` can be
pre-approved and applied automatically; an undelimited marker stays report-only. Precedent for how
to ship it is `id:1048` -- wired, default OFF, owner flips it after watching it report correctly.

## Related

`id:0246` (the clause this guard's first real marker will point at), `id:3f59` (the
observability-surfaces mechanism this is a cousin of), `id:1048` / `id:0b6c` (the
build-it-default-off precedent), `id:32c3` (the `/inflownistration` skill and its own
propose-then-confirm discipline).
