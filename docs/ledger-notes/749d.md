# id:749d

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(meeting D5-split + D11), modelled on the `id:cece` prior art (`~/src/mattpocock-skils/skills/productivity/handoff/SKILL.md`, ~10 lines, user-invoked only). Borrowings ratified: **'reference, don't duplicate'** — never restate what specs/plans/ADRs/issues/commits/diffs already hold — WITH a precision clause, cite `id:XXXX` first (churn-proof) and `file:line` only SHA-pinned or as fallback, because a bare `TODO.md` costs a 563-box scan and a bare `file:line` rots within a session (measured: a cite in this very meeting went stale between reading and ratification); an OPTIONAL tailoring argument ('what will the next session be used for?') defaulting to 'a fresh session continues exactly this work'; a 'suggested skills' section; explicit redaction of secrets/PII. MUST state in-skill which situation it does NOT cover (D9): live background work is already gone at session death, and 'handoff → fresh' needs a snapshot written BEFORE death. <!-- id:749d -->
