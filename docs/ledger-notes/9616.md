# id:9616

AUTHORED note (not relocated by `tools/ledger-shrink.py`) -- the verbatim-reproduction
claim in `docs/ledger-notes/README.md` does not apply here. Conventions and backlinks:
`docs/ledger-notes/README.md`.

## From TODO

Filed from the cross-repo inbox, `routed:7f74` (from project_manager, observed live
2026-09-08 during the ideas-breeding-ground meeting). It SHARPENS `routed:956c` /
`id:6d77`, which reported the same incident with understated severity; this line carries
the corrected account and is the one to work.

**Observation.** `meeting-question-guard` (`routed:29bc` / `id:2419`) fires but does not
CORRECT. In one project_manager meeting the same Opus session ended on bare transcript
prose at D1 (6951 chars, BLOCK #1), again at D3 (2395 chars, BLOCK #2), and a third
identical prose-only turn at D4. The model re-offended at the very next decision point
after each block, three times, despite the hook's own text saying "from here until the
meeting note is written, EVERY remaining decision point works the same way". The owner had
to intervene manually twice ("AGAIN Come on").

**What the evidence supports.** A post-hoc Stop block is empirically NOT a durable
behaviour change within one session. The intervention that worked was the human, not the
hook. Two structural problems, not one: the block is not durable across the remaining
decision points, and it lands AFTER the prose is generated, so the tokens are already
spent and the model is told "do NOT re-emit".

**Candidate directions (none decided).**

- (a) Escalate wording and format on BLOCK #2+. The counter already exists -- the hook
  printed "BLOCK #1" -- so this is the cheapest change.
- (b) A PRE-emptive reminder injected at each decision point (a `UserPromptSubmit` or
  `PostToolUse` hook while the meeting-guard marker is armed) rather than only a post-hoc
  Stop block. This is the direction the incident actually argues for.
- (c) Surface the repeat-rate from `~/.claude/logs` to see whether repeat-after-block is
  the common case fleet-wide, before committing to (b)'s cost.

Note the ordering: (c) is the "observe before preventing" step and is cheap, but this
incident already gives n=3 within a single session, which is a stronger signal than a
fleet-wide rate would be for the durability question specifically.

Relates to `id:2419` (the guard itself) and to the `feedback-skill-protocol-is-binding`
memory, which records the rule the guard exists to enforce.
