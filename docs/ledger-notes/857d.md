# id:857d

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

This is a GATE on the adapter work, not advisory: `id:90f2` cannot close until each adapter demonstrably carries the per-view pair (or a visible drift marker) into its target. Decided together with keeping `derived_status` — the field stays because adapters need one column, so the laundering risk moves entirely to this gate. <!-- id:857d --> <!-- gated-on:2bb1 -->
