# id:9fa5

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

Design: wire retrieve-top-k.py for BOTH files (it is file-generic) or add a compact digest tier; consume/demote (local) entries; add recurring compaction (evidence cap ~3 once high-confidence, merge trait-clusters: 14 scope-*, 12 evidence/pilot-*, 6 dissolve-*); ALSO extend profile-active.sh awk to accept field-format variants (colon-inside-bold etc.) — 2026-07-02 normalization fixed the data locally but append-time drift will recur <!-- id:9fa5 -->
