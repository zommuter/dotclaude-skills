# id:2e8a

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

— found by adversarial probing in the transform review 2026-08-21 (the lint itself is otherwise sound: 15 positive + 13 negative controls, three suppression-hatch probes confirming there IS no hatch, and the repo is clean under it). **FALSE NEGATIVES (verified with probe files, neither shape present in the repo today):** (1) a trailing-pipe line continuation — `producer |` then newline then `grep -q x` — is unflagged, because the lint is line-based and a trailing `|` leaves stage-2 empty; (2) `producer |& grep -q x` is unflagged, because `|&` splits into a stage starting with `&`. A future author writing either shape gets a green gate on a live instance of the exact bug this lint exists to prevent. **FALSE POSITIVES, and the lint advertises NO exemptions:** `cat f | sed 's/q//'` and `cat f | awk '{print "exit"}'` are both flagged, because the `sed`/`awk` detectors search the RAW script text for a `q` command or the word `exit` rather than parsing the program — so a `q` inside a replacement or `exit` inside a string trips them. With no hatch by design, an author hitting this must contort safe code, which is how a ban decays into a workaround. **DRIFT:** the module docstring promises a `--heredoc` flag, but `main()` has no argv handling, so `--heredoc` is parsed as a PATH and silently no-ops. Fix all four; keep the zero-exemption posture (the fix is a better parser, not a suppression list). Relates id:81d5, id:7518, id:f211. <!-- id:2e8a -->
