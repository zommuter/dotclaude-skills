# id:9128

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

Both of the inbound report's headline claims are REFUTED against this repo's code, verified 2026-09-01: (a) *`verify-isolation.sh` is structurally blind to a child that never created a worktree* is FALSE -- case (d) is documented at its `:41` and implemented at `:73` (`[ ! -d "$worktree" ]` => exit 2), confirmed empirically, and `integrate.sh:553-556` calls it with the CALLER-supplied `--worktree` (the assigned path, not a child self-report) and hands back on failure, which is exactly the supervisor-side fix the report proposes; (b) *only an interactive permission prompt stood between it and a destructive rewrite, which does not exist under `--afk`* is FALSE here -- `hooks/destructive-git-guard.py` is WIRED as PreToolUse and unconditionally denies the five tree-wide forms including `git reset --hard` (`:259`), a hook that fires regardless of `--afk`. WHAT SURVIVES, and is genuinely unaddressed: the gate is a MERGE-TIME detector, so a child can still write and commit to the target's main checkout BEFORE any gate runs -- the breach is caught rather than prevented, and the loose commit must still be reverted by hand. Prevention would need a dispatch-time or child-side constraint (OS-level cwd containment, or asserting main's HEAD is unmoved before/after dispatch). Related `id:c6c8` (the 2026-06-30 jobAI instance), `id:f682`/`id:7612` (the gate), `id:3a09` (the guard). <!-- id:9128 -->
