# id:8a76

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

They are neither `relay/orphan/*` nor under `~/.cache/relay/worktrees`, so `--all` reports them as nothing and 415 MB accumulated invisibly for weeks (id:5d91). **The recipe is PROVEN, not speculative -- 28 were retired with it 2026-09-10, force-free, and 29 merged worktree-less branches deleted with `branch -d`:** surface is `.claude/worktrees/agent-<id>/` paired with branch `worktree-agent-<id>`; retire with `worktree-retire.sh <repo> <repo>/.claude/worktrees/agent-<id> worktree-agent-<id> --expect-merged`, which needs NO change to work on them. **FOUR things the implementation must get right, each learned the hard way here:** (1) the eligibility test is `rev-list --count main..HEAD` == 0 AND `status --porcelain` empty AND branch merged -- 1 of 29 was dirty (an uncommitted lane-delimiter migration of the archive files) and MUST be surfaced, never swept; (2) `worktree-retire.sh`'s residue flags `--commit-residue`/`--discard-residue` REFUSE a non-`relay/*` branch by design, so there is no force-free residue path for these and a dirty one can only be surfaced -- do not add one without an owner decision; (3) WORKTREE-LESS MERGED BRANCHES are a SECOND surface that accumulates independently (29 of the 58 here had no worktree at all) and must be enumerated separately, or the ref clutter that hid this survives the fix; (4) never retire a worktree whose agent is LIVE -- gate on liveness the way the relay path does, and note the shell must not be CWD-inside a worktree it is removing. Also decide whether `/relay health` / `relay-doctor.sh` should REPORT this surface, since today a human has no way to notice it except by looking (id:5d91 part (b)). <!-- relates:5d91 --> <!-- relates:1a5c --> <!-- id:8a76 -->
