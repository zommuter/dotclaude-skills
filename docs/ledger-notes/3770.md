# id:3770

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

Measured 2026-09-08 from a live failure: the id:c057 cap bound a 378 MB HTTP client while `llama-swap` (root service, `system.slice`, `MemoryMax=infinity`, `MemorySwapMax=infinity`) loaded ~17.3 GiB in its OWN cgroup -- `/proc/<llama-server>/cgroup` is `0::/system.slice/llama-swap.service`. RAM and swap filled and the machine stalled. Two aggravators, both physical: the Intel Arc 140V is an **iGPU with SHARED system memory**, so `-ngl 99` does NOT move weights off the same 30 GB it is competing for; and the 30B entry runs `--ctx-size 32768` where every other model uses 8192. `llama-swap` v255 has NO memory limits -- probed the binary, `groups`/`exclusive`/`swap`/`members` govern which models may be CO-RESIDENT, not usage -- so no config-only fix exists. **The four options, none free:** (a) `systemd-run --scope -p MemoryMax -p MemorySwapMax=0` wrapped inside the model's `cmd:` in `/etc/llama-swap/config.yaml` -- caps exactly where the memory is spent and is per-model, but edits a root file and llama-swap would spawn scopes as root; (b) a `MemoryMax`/`MemorySwapMax` drop-in on `llama-swap.service` -- simplest, but caps ALL models collectively for EVERY consumer (zkm and Open WebUI use it too), and `MemorySwapMax=0` there means the OOM killer takes the model server rather than thrashing, which may be the right trade or may break interactive use; (c) `groups` with `exclusive: true` so the 30B can never be resident beside the always-on trio (qwen3.5-0.8b, bge-m3, gemma-4-E4B) -- reduces peak, bounds nothing; (d) drop the 30B's `--ctx-size` to 16384 -- the config author already wrote exactly that advice for the 35B entry at line 170 ("Consider --ctx-size 16384 if OOM or excessive swapping occurs"), so the failure mode was known. **Acceptance: a 30B judge run completes, or is killed cleanly, WITHOUT the machine stalling -- proven by running it, never by reading a config.** Until this lands, all four `local-llm` drafts carry an `_uncapped_warning` field and must not be promoted unattended. <!-- relates:c057 --> INBOUND routed:4887 from ai-codebench FOLDED IN 2026-09-10 rather than opened as a second item (its note docs/ledger-notes/9635.md describes the SAME defect, same mechanism, same measurement). Its one extra fact, kept: the uncapped state also blocks the 22 GB sibling recipes 8c1d/3ee7 (ai-codebench ids). <!-- routed:4887 --> <!-- id:3770 -->
