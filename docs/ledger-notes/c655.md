# id:c655

AUTHORED note, not relocated by `tools/ledger-shrink.py` -- the verbatim-reproduction claim
does not apply to it (see the "Authored notes" section of `docs/ledger-notes/README.md`).

## Parked work -- READ BEFORE RESTARTING THIS ITEM

An execute unit already attempted this seam and its branch is still on disk, unmerged:

    relay/orphan/relay-20260909-091623-10249-execute-c655-0

It holds +79/-15 in `relay/scripts/todo-conformance.sh` (verified 2026-09-09 with
`git diff --stat main...relay/orphan/relay-20260909-091623-10249-execute-c655-0`).

**Reviewed verdict: NEEDS-WORK.** It moves its own RED spec green but breaks 10 tests. Two
findings, both from the review this session:

1. It adds a hard `exit 2` on legacy 3-column baseline rows **without regenerating the
   committed baselines**, so the tool can no longer lint its own repo. A migration path (or
   an accept-both-shapes reader) is needed, not a hard reject.
2. Only the shape-prose half of the seam was done. `length_baseline_load` and the staleness
   detector are untouched, while an added comment falsely claims all three baseline readers
   share the new parser. That comment is wrong as written.

**Why this breadcrumb exists**: an item whose work is parked on an orphan branch is
invisible to the next executor, which would start from scratch and rediscover -- or repeat --
both findings. Start from the branch, or from these findings; do not start from zero.

The branch is not merged and nothing here ticks the item.
