# id:8dbf

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

— handback-followup.py is wired only to the child->orchestrator boundary, so a supervised session that is both roles sizes an item out and files nothing, and the pool then burns an Opus dispatch to rediscover it <!-- id:8dbf -->
