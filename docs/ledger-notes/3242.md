# id:3242

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(root-caused from transcripts 2026-08-22, run `relay-20260822-154630-17003`, loderite `id:6612`). **Verbatim error:** `Prompt is too long`, `isApiErrorMessage: true`, at `2026-08-22T14:06:33.070Z` (last record of that child's `agent-*.jsonl` in the run's workflow transcript dir). **The gate was NOT at fault and worked correctly:** `ledger-slice.sh --repo loderite --id 6612` produced a **2,703-byte** slice; the same round shows the gate genuinely firing for it-infra (`~132015 tok, over the 100000 tok dispatch budget`) which was never dispatched. **What actually happened:** the child's prompt grew **67,283 → 176,311 tokens over ~18 minutes** of tool results and thinking (123k chars of thinking, 171k of tool_result, with single results of 25.8k and 23.4k chars). **The expensive part is WHERE it died:** it had already finished the work, run the full suite (37 failed / 3182 passed, matching its own stated baseline) and a clean typecheck — the last successful call was a `RELAY_LOG.md` heredoc, and the NEXT request was rejected. So a completed unit was lost at the commit step and survives only as `id:f272` WIP residue on `relay/orphan/relay-20260822-154630-17003-execute-6612-0`. **Not fleet-wide:** exactly 1 of 138 agent transcripts in that run carried an API error; the next-highest peak prompts (147k / 134k / 133k) all completed. **Caveat, do not overstate:** 176,311 is under a nominal 200k window and no `max_tokens` override exists in settings or env, so the exact ceiling is UNESTABLISHED — only the error CLASS (context-length rejection, not rate-limit) is proven. **FIX SHAPE (not decided):** the cheapest real mitigation is to make the child COMMIT EARLIER — commit-before-suite rather than commit-after — so a context death costs the verification, not the work. A context-headroom check before the wrap-up sequence is the alternative. Relates to the long-standing executor prompt-size item, but is a DIFFERENT mechanism: that one is dispatch-time, this one is in-session growth. <!-- id:3242 -->
