# id:0d7c

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

FORMAT RATIFIED, TOOLING BUILT AND GREEN, SHRINK NOT LANDED. Applied twice against the live ledgers 2026-09-02 (-65%, 1,688,246 -> 583,443) and REVERTED both times on an acceptance-gate refusal; each refusal found a real unbuilt consumer. Prereqs `71ed`/`2ee1`/`f3d2` are closed; `e95b` is not. Spec + both refusals: `docs/meeting-notes/2026-09-01-2226-ledger-line-shrink-format.md`. gated-on:e95b <!-- id:0d7c -->

## From TODO

-- detail: `docs/ledger-notes/0d7c.md` gated-on:e95b **SCOPE LIMIT MEASURED 2026-09-11: the line-shrink CANNOT reduce the id:4f9b/b018 prompt-size gate, and nobody should schedule it as that mitigation.** The gate charges `file size + every pointed-to note` (`classify-repo.sh:275-296`, `_ledger_note_bytes` at :231), so relocating prose from a ledger line into `docs/ledger-notes/<id>.md` moves bytes from the file column into the note column and the SUM is unchanged -- net zero, plus a per-note header. Measured on this repo: notes are 2,036,546 B, 78.3% of the 650,104 tok estimate, and they got there by exactly this mechanism. This is the id:f3d2 optimism trap seen from the other side: f3d2 correctly refused to let the shrink hide bytes from the gate, and the necessary consequence is that the shrink cannot lower the number either. It is also irrelevant on the SLICE path, since id:2ee1 inlining means a shrunk item slices to the same size as an unshrunk one. id:0d7c remains worthwhile on its own merits (line-length ratchet, readability); it is simply not a prompt-size lever. <!-- id:0d7c -->
