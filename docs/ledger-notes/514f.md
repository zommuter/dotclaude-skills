# id:514f

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

`integrate_branch()` calls `git-lock-push.sh --ff-only --all`, deliberately "must still reach every eligible remote", but carries none of integrate.sh public-remote withholding. MEASURED 2026-08-31: integrating the stranded `git-annex` branch attempted a push to `upstream` = `git://git-annex.branchable.com/`, the PUBLIC git-annex project; main plus 11 `relay-ckpt-*` tags were stopped only by that remote OWN pre-receive hook declining them. Nothing was published, but by the remote guard, not ours. SECOND, same root: the printed status is the AGGREGATE -- it reported `integrated ... (pushed)` while stderr carried `push to upstream failed (rc=1)`, exactly the trap integrate.sh:184 already names ("`push=` is the AGGREGATE; the per-remote truth is in the per-remote keys"). Reconcile never learned it. Fix: SHARE the id:4d44 predicate and per-remote reporting, do not reimplement the recipe. <!-- id:514f -->
