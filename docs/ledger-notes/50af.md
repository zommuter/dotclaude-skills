# id:50af

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(pool-safe: read `RELAY_STATUS.md`/`relay-events.jsonl`/`claim.sh peek`, push only clean + un-claimed) + a **dup-id scan** across ledgers for `md-merge` silent appends from today's ~24 ticks — the acceptance check for id:1b1a. (meeting 2026-07-11-1239, D7) <!-- id:50af -->
