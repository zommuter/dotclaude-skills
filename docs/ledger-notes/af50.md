# id:af50

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

Measured 2026-09-01 against the live allow-list (74 errors / 72 warnings before the ratification, now 10 class-A + 1 class-B). **(1)** `--allow-homonym 8f21 is stale: '8f21' is not a class-A cross-repo homonym in this document` -- the `ledger-map.py:1009-1011` stale-entry WARN firing exactly as designed; `id:e977` predicted it and it is still unactioned. Retire the entry. **(2)** NINE `blocked_by ... is dangling (unresolvable in this document)` warnings: 6 in dotclaude-skills, 2 in it-infra, 1 in puzzle-pwa. Two are MALFORMED rather than merely stale -- `dotclaude-skills/fcdf: blocked_by dotclaude-skills/a` and `.../b`, where `a` and `b` are not 4-hex tokens at all, so something WROTE a broken edge and the writer is worth finding rather than just patching the pointer. The rest are ordinary dangling targets (archived or never-existed). Note the archived-target case is the `id:47f7` SATISFIED-GATE-vs-DEAD-GATE distinction, so resolve ids across the live+archive PAIR rather than assuming a dangling target is dead. Contract: `fleet-import.sh` reports zero stale allow-list entries, the two malformed edges are traced to their writer, and each remaining dangling edge is either repaired or explicitly recorded as satisfied. <!-- id:af50 -->
