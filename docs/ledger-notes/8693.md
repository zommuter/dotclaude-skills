# id:8693

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

-- all 142 of 142 closed items ALREADY carry the archived-suffix and still hold full prose; the archiver appends the suffix and never trims, so the sweep moved 1 item and the file GREW 36 chars. The RISK argument survives (closed items blind no detector) but the tool is the SHRINKER not the archiver -- do NOT pass --open-only, and take the closed half as the low-risk proving pass; for 03a3 check per repo whether closed items are suffix-stubbed but un-trimmed or every 'archive first' step looks like a no-op <!-- id:8693 -->
