# id:46d5

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

— the minted token (projects/tasks/labels) **401s** on `/tasks/{id}/labels` and `/tasks/{id}/relations`, so `id:90f2`'s adapter falls back to a JWT from `VIKUNJA_USER`/`VIKUNJA_PASSWORD`. A JWT expires; that is fine for a supervised run and wrong for `id:f116`'s timer. Either widen the token's permissions in the Vikunja UI or document the JWT-refresh path. Credentials live in `~/.config/relay/tracker-secrets.env` (0600, injection-only, never committed). <!-- children-of:90f2 --> <!-- id:46d5 -->
