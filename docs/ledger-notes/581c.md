# id:581c

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(owner 2026-06-16). For each `zommuter/*` repo NOT cloned locally, review online first (`gh repo view`, README, recent activity, open issues) before any clone; clone only if active triage/work is warranted. Per-repo verdict: keep-as-is / clone+work / archive / delete (gtnsd just went archive+drop as the template). **Uncloned snapshot 2026-06-16 (26, excl. archived gtnsd):** ahk-scripts, ama, createxig, customize, desktopini.py, EdNui, gedcom-reaper-py, gnucash, hash2identicon.js, markeast, newtonfractal, numintropy, poe, pygeon, pyki, rasperrypi-userland-aarch64-git, solidity-experiments, spectral-xray, ssh-agent-sign, Tests, timefolder, vain39, win10helpers, Zommium, zommuter.github.io, zomtems. (`zomtems` = the gtnsd-referenced "share idea fragments without losing ownership" project — triage with the rest.) Recompute the gap via `gh repo list zommuter` vs `~/src/*` + `~/src/zkm/plugins/*`. Likely a `/projects`-style grooming pass. <!-- id:581c -->
