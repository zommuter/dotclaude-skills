# id:aa83

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(owner-decided 2026-08-23). **DECOUPLED from the ASCII-lane question — the filed dependency does not bind.** The inbox item declared the lane spelling its blocker on the grounds that "(b)/(c) cannot reach 100% until an ASCII lane spelling exists" — but the item itself EXEMPTS lane tags, and with them exempt the WARN class reaches 100% of the non-exempt set with no lane change at all. Ship: (a) the global CLAUDE.md rule that TODO/ROADMAP/REVIEW_ME/todo-inbox are ASCII-only with a substitution table (em/en dash -> '-', arrows -> '->', ellipsis -> '...', '>=', 'x', 'sec.', '[!]'), lane tags exempt; (b) the WARN class; optionally (c) a pre-commit ratchet grandfathering existing lines and refusing newly-ADDED non-ASCII, same shape as `hooks/pre-commit-lane-vocab.sh`. **Owner ruled: NO retroactive sweep** — it-infra's TODO.md alone carries 1055 em dashes + 494 other non-ASCII from many prior sessions; only NEW writes conform. Filed from inbox routed:2240. <!-- id:aa83 -->
