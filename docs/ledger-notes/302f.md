# id:302f

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

`lib-typed-edges.sh` joins them and refuses the result. Since `id:aa0d` this fails in the SAFE direction -- both read as GATED, nothing is mis-dispatched -- but the item is then permanently blocked with no path to ungating, and the refusal message names a payload that appears in no ledger. Observed 2026-09-10 in FOUR repos, so it is a grammar gap and not a local typo: lean4btc `id:191a` (`'9ee4\nde86'`) and `id:abc7` (`'9ee4\n7182'`), linguistic-universals `id:cc76` (`'58ca / 1d40 / 58ca'`, three markers), loderite `id:27f7` (`'d5ff\nc2f3'`). Decide: teach the lib to accept multiple `gated-on:` markers per line (AND-ing them, which is what an author writing two plainly means), or make the refusal name the offending line so the ledger-side collapse is mechanical. Note `id:abc7` is inside lean4btc's live pinned-statement cluster, so this is blocking real work. <!-- relates:aa0d --> <!-- id:302f -->
