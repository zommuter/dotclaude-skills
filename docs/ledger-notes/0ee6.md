# id:0ee6

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(user 2026-06-30) — the self-feeding pool loop ends on (among other conditions) **two consecutive empty discoveries** = it re-shards/re-classifies every own repo a final time just to confirm "nothing new", which is wasted classifier work (and on a `/loop` cadence, repeated every tick over a drained backlog). **Idea:** watch the inputs that a discovery actually reads — the own-repos' ledger files (`ROADMAP.md`/`TODO.md`/`REVIEW_ME.md`), the relevant git refs/`HEAD`, `inject.sh` queue, the shared inbox — with `inotifywait` (Linux; `fswatch` as the portable fallback) so the loop **blocks until something a discovery cares about actually changes**, instead of spinning a confirm-empty round. Turns the drained-pool tail from "poll N repos to find nothing" into "sleep until a real edit/commit/inject arrives". Ties to the **discovery signature cache** (id:c3a6, `discover-sig.sh` already hashes the superset of discovery inputs) — the inotify watch-set should be derived from the SAME input set the sig hashes, so the two can't disagree (a change that flips the sig must wake the watcher, and vice-versa). (NB: unrelated to "inflownistration" — that is a project-domain term, see glossary id:2a3d, not a watcher concept.) Design Qs: which exact paths to watch per repo (reuse the sig's input list), debounce coalescing, how it composes with the existing two-dry-discovery exit (replace it, or gate it), and whether it belongs in `relay-loop.js` (in-session) vs the outage-watchdog/`/loop` layer. Note the [[babysitter-durable-cron-no-op]] constraint: an in-session inotify wait dies with the session — it optimizes a *live* loop's idle tail, it is not an outage-survival mechanism (that's id:98f0). Likely `[HARD — meeting]`. **MEETING 2026-06-30-1523 (id:4d8e) decided + DE-PRIORITIZED:** inotify REPLACES the sleep-poll, NOT the two-dry exit (which stays the correctness condition); watch-set derives from `discover-sig.sh`'s input superset + the b444 decision-queue watchfile (id:de31/b444); fail-safe toward re-checking (overflow/doubt → poll). **GATED behind the classifier (id:4d8e) and de-prioritized — DP1 going mechanical already delivered the headline payoff (dry re-discovery is now near-zero-token bash).** Per user note: `git status` incl. worktree heads is the dominant change signal (already hashed by discover-sig), so it works with or without inotify — inotify is optional polish. Decide if the inotifywait dep is worth it only AFTER the classifier lands. <!-- id:0ee6 -->
