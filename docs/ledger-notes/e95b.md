# id:e95b

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

Its rule 3(c) NO-ACCEPTANCE-NO-TWIN (`:698`, `id:213a`) asks whether an open item has an Acceptance/Tests/Done-check clause IN ITS BODY. After the shrink that clause lives in `docs/ledger-notes/<id>.md`, so a perfectly workable item reads as structurally un-workable. MEASURED on the real ledgers 2026-09-02: the warning count went 1 -> 2, the new one being `id:6958` (`da55` fires on both sides and is pre-existing). Same shape as `id:2ee1` (ledger-slice) and `id:f3d2` (byte gate); this is a HARD PREREQUISITE of `id:0d7c` on the same footing. Caught by the `id:0d7c` acceptance gate on a live run, not by the suite. <!-- id:e95b -->
