# id:379e

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

— no assistant message anywhere under `~/.claude/projects/` carries a `relay/worktrees` cwd, so executor sessions sit in NEITHER the target repo's project dir NOR the supervising session's transcript (the 189 relay-run transcripts are 91% opus-4-8, only 135 sonnet-4-6 calls). Executors DO run and DO run Sonnet — loderite's `RELAY_LOG.md` has 253 `executor (sonnet)` checkpoints vs 169 reviewer/strong — but their spend lands somewhere unlocated. Verified 2026-07-29 by making exactly that mistake: a loderite-only slice showed 87% opus-4-8 / 6 sonnet calls and was read as "executors aren't Sonnet" — wrong twice over (the tier is `claude-sonnet-4-6`, not `claude-sonnet-5`, and the dir cannot see executor sessions at all). **Wanted:** locate where executor transcripts actually land (`relay-loop.js:1804` `worktreePathFor` = `~/.cache/relay/worktrees/<repo>/<runId>-<verdict>`), then either emit a repo tag into `relay-events`/`relay-burn` or DOCUMENT the gap so nobody re-derives a false model-mix. Reuse `relay/scripts/relay-burn.sh`, `tools/quota-sample.sh`, `docs/relay-economics-2026-06-16.md`. Blocks the coverage-boundary statement id:3a89 must print. <!-- id:379e -->
