# id:480c

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

— its non-canonical-inline-id guard (`relay/scripts/todo-conformance.sh:194`) greps `\bid:[0-9a-f]{4}\b` over the WHOLE line, so a backticked reference like `` `id:3278` `` inside an item's prose reads as that line's own non-canonical id and minting is skipped. In routed ledgers the citation form is extremely common, so the auto-fix no-ops on precisely the `missing-id` lines it exists to serve. **Silent-swallow shape (id:4347)**: the refusal is warned on stderr ONLY, which is lost in the findings dump — the same anti-pattern as `unpromoted-scan`'s loud-detection-with-no-op-resolution. **Repro**: loderite `TODO.md` lines 13-14 on 2026-08-10, both minted by hand after the auto-fix declined. **Fix direction (reporter's, preserved)**: treat an inline id as the line's OWN only when it is not in a citation context — e.g. require the bare id to be the line's trailing token, or ignore ids preceded by `from <repo>` / a repo name. <!-- routed:d160 --> <!-- id:480c -->
