# id:8266

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(INBOUND from leAIrn2learn relay review). leAIrn2learn commit `77a802b` re-tagged `id:598a` in place AND emitted a byte-identical second copy above the seam block, so one open item double-counted in every collector until the 2026-08-26 review deleted it. Reproduce: `git show 77a802b -- ROADMAP.md`. Sibling of `id:d826` (the SAME script widening a ratified criterion in the SAME commit) — audit what this script writes once and fix both. <!-- id:8266 -->
