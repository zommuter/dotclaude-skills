# id:6958

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From ROADMAP

(possible for the first time, via S0's `lane-delimiter-scan.sh --live-only`): **222 LIVE em-dash lane tags** across the five ledgers — TODO.md 89, ROADMAP.md 48, ROADMAP.archive.md 47, TODO.archive.md 38, REVIEW_ME.md 0. The pre-S0 naive grep counted 612; the 390-line gap is backtick'd/prose mentions the stand-in could not separate from live tags, so size S9 off 222, not 612. Verified independently of the authoring agent. S3 has LANDED, so the ratchet is rearmed and this rewrite is now safe to run. — **PARTIAL 2026-08-31: live ledgers DONE, archives BLOCKED pending an owner ruling (`id:2065`).** `TODO.md` (89 tags) and `ROADMAP.md` (48) migrated and pushed; conservation verified exactly (raw 208->119 and 109->61, i.e. -89/-48, no prose touched). `REVIEW_ME.md` needed nothing (its 2 occurrences are both prose, 0 live). `ROADMAP.archive.md` (47) and `TODO.archive.md` (38) are NOT migrated: the lane-vocab ratchet BLOCKS all 85 lines because they carry historical VENUE-keyed vocabulary, so changing their delimiter makes them ADDED old-vocab lines. Every one is a closed `- [x]` entry. Do NOT close this item until `id:2065` is ruled. -- **BLOCKER CLEARED 2026-09-01 (review run relay-20260901-101120-32404):** the `id:2065` ruling landed (owner chose option (a)) and `hooks/pre-commit-lane-vocab.sh` now excludes `*.archive.md` from its staged-diff collection, verified green by `tests/test_lane_vocab_archive_carveout_2065.sh`. The 85 archive tags (`ROADMAP.archive.md` 47 + `TODO.archive.md` 38) are therefore no longer ratchet-blocked and the remaining archive half of S9 is now runnable. This clears the stated hold; it does NOT tick the item, since the archive rewrite itself has not been performed. — **ARCHIVE HALF DONE 2026-09-01, commit `d8793312`; S9 is mechanically COMPLETE by its own closing condition.** `ROADMAP.archive.md` 47 tags and `TODO.archive.md` 38 tags migrated. CONSERVATION EXACT, measured before and after with `lane-delimiter-scan.sh --live-only` (the existing tool, not an improvised sweep): raw U+2014 counts 2749->2702 (-47) and 3099->3061 (-38), diff exactly 85 insertions / 85 deletions, and every changed line pair differs SOLELY in its delimiter -- zero lines with any other change, re-checked programmatically. Vocabulary untouched as the 2065 ruling requires (`[HARD - pool]`, never `[HARD]`). Written through `md-merge.py update-ids` with per-id `regex_sub` under the flock (TOCTOU-free, and keying per id is what kept the delta exact) then `commit-ledger.sh` -- no Edit/Write/sed -i anywhere. The pre-commit hook PASSED CLEANLY with no `--no-verify`, printing the id:2065 carve-out line for both files, so that ruling is confirmed working in production. Suite 542/0/1. Verified independently of the authoring agent: `lane-delimiter-scan.sh --live-only` over all five ledgers now returns NOTHING. **RESIDUE, filed as `id:cce9`, NOT part of this item:** the closing-condition detector is structurally blind to 7 lines carrying genuine old-delimiter lane tags, 3 of them real -- so `scan returns nothing` is a weaker statement than it appears. <!-- id:6958 -->
