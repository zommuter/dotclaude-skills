# id:8de9

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

— found 2026-08-10 reconciling this repo's lint output against a parallel session's `id:1f8e` work. **The disagreement, both verified by command:** `resolve-gates.sh:36` resolves gate tokens over `ROADMAP.md ∪ TODO.md ∪ TODO.archive.md` (its `:31` comment states this deliberately — "the executor gate must see a resolution in the archive"), so a gate whose id is `- [x]` in the archive is **SATISFIED**. `roadmap-lint.sh` instead reports `DEAD-GATE … it is RETIRED — archived in TODO.archive.md and not a ROADMAP item, so the gate is PERMANENT and can never open; drop or re-target the marker (id:49e0)`. **Live case**: open item `id:2b49` is `gated-on:ac7f`; `ac7f` is `- [x]` at `TODO.archive.md:428` (the single `id:ac7f` hit in `TODO.md` is not an ac7f item — it is 2b49's own gate marker on line 264). So 2b49's gate is **already open** and the item is a ready-to-run `[HARD — meeting]`, while lint has been reporting it as permanently blocked. **Why this is worse than a noisy warning:** the remedy it prescribes is to DROP the marker. Following it deletes a satisfied, correct provenance edge and converts a resolvable dependency into no dependency at all — the lint actively recommends data loss. It also inflates the DEAD-GATE count that `relay-doctor` surfaces, training the reader to ignore the class. **The same false positive already produced a wrong recommendation in this repo's own REVIEW_ME** (Review 2026-08-10 box, since corrected). **Fix**: make `roadmap-lint` use the SAME resolution span as `resolve-gates.sh` — ideally by calling it rather than reimplementing the lookup (two implementations of one predicate is the drift that caused this). Distinguish three states, never conflating them: gate **satisfied** (ticked anywhere in the span → not a warning at all, and the item may be READY — worth surfacing positively); gate **open** (untricked and present → normal); gate **unresolvable** (id exists nowhere in the span → the only genuine DEAD-GATE). The `id:540f`/`id:c179` warnings are a DIFFERENT and possibly-legitimate case (gate `b0b1` lives only in `TODO.md`, never promoted) — do not fold them into this fix without re-checking them separately. Relates id:49e0 (the DEAD-GATE check), id:46f6 (typed edges), id:eb63 (sibling: done-but-unticked items are not detected either — same "ledger state is not what the tooling believes" family). — **SCOPE SET 2026-08-18 (meeting `docs/meeting-notes/2026-08-18-1052-meeting-backlog-selector-948c-788f.md`, D3 as AMENDED).** This item now carries the narrow gate fix, and the meeting's first ratification ("roadmap-lint consumes resolve-gates") was **WITHDRAWN** as broader than this defect and contradicting it. **Do NOT merge the two tools:** `roadmap-lint.sh:348-354` documents the three-map split deliberately and names `resolve-gates.sh` as "correctly" merging for "its own, different question" — resolve-gates answers *is this dispatchable now* (satisfaction), the DEAD-GATE rule answers *can this gate ever open through the execution queue* (location). Merging erases the never-promoted class, which is LIVE (`d4ca`→`09e4`, `e405`→`09e4`, `540f`→`b0b1`, `c179`→`b0b1`). **The actual root cause, verified:** `roadmap-lint.sh:532` tests archive-map EXISTENCE (`${RL_GATE_ARCHIVE[$_dg_t]+x}`) and never the stored checkbox STATE — and since `archive-done.sh` archives only `[x]` items, essentially EVERY archive hit is a false PERMANENT verdict (`2b49`, `540f`, `c179` today, not just the one). **Fix:** share the satisfaction predicate/state-map (ticked-anywhere-in-span ⇒ SATISFIED, never a warning); KEEP the three-way location taxonomy. **Acceptance:** a fixture whose gate target is `[x]` in `TODO.archive.md` yields SATISFIED with no warning, AND a fixture whose target lives only in `TODO.md` still yields the never-promoted warning — assert BOTH, since a positive-only test passes against a lint that warns on nothing. **Also files here (not under id:7877):** the resolve-gates-vs-roadmap-lint disagreement is "two persistent implementations of one predicate drifting", which this item already names. **AMENDED 2026-08-18 (`/relay human .`, action item 2 of meeting `2026-08-18-1052`; carries D3 as AMENDED, which SUPERSEDES that meeting's first ratification).** The withdrawn wording made `resolve-gates.sh` the source of truth with `roadmap-lint` consuming it; that is **not** the decision — the two tools answer different questions by documented design (resolve-gates: *is this dispatchable now* / satisfaction; DEAD-GATE: *can this gate ever open through the execution queue* / location), and consuming one from the other would erase the never-promoted class that is live today (`d4ca`→`09e4`, `e405`→`09e4`, `540f`→`b0b1`, `c179`→`b0b1`). **The amended fix is NARROW:** share the satisfaction predicate/state-map (ticked-anywhere-in-span ⇒ SATISFIED, never a warning) and KEEP roadmap-lint's three-way location taxonomy. **Root cause, re-verified in code 2026-08-18:** `roadmap-lint.sh:532` tests archive-map *existence* via a `+x` parameter-expansion test, never the stored checkbox *state* — and since `archive-done.sh` only ever archives `[x]` items, essentially every archive hit yields a false PERMANENT verdict. **Acceptance (BOTH directions, or the test is vacuous):** a fixture whose gate target is `[x]` in `TODO.archive.md` yields SATISFIED with NO warning, AND a target living only in `TODO.md` still yields the never-promoted warning — a positive-only test passes against a lint that warns on nothing. **OWNER DECISION 2026-08-22 (`/relay human`, REVIEW_ME.md tier (b)) — this item now ALSO owns the DEAD-GATE check's ledger-scope bug, and the chosen fix is to change the CHECK, not the ledger.** `roadmap-lint.sh` reports DEAD-GATE for an open ROADMAP item whose `gated-on:` target lives only in `TODO.md`, reasoning that "nothing in ROADMAP.md can ever clear the gate". But `resolve-gates.sh:36` already resolves gates against `ROADMAP.md ∪ TODO.md ∪ TODO.archive.md` — so such a gate IS resolvable, and the two tools simply disagree about what the resolution set is. Make lint agree with the resolver. That clears all four live WARNs (`d4ca`, `e405`, `540f`, `c179` — all gated on `09e4`/`b0b1`) without touching a single gate marker. The alternative (promote `09e4` + `b0b1` into ROADMAP) was explicitly REJECTED: `b0b1` is a deliberate OWNER gate, and promoting it would put it in the execution queue's line of sight. **Routing correction made while filing this:** the REVIEW_ME box AND `roadmap-lint.sh`'s own WARN text both tell the reader to resolve this via `id:49e0` — but `id:49e0` is `- [x]` CLOSED and archived (`TODO.archive.md:515`, `ROADMAP.archive.md:3488`), so that pointer is dead. Fix the WARN string's `(id:49e0)` reference to name this item in the same change, or the next reader is sent to an archived item again. <!-- id:8de9 -->
