# id:8679

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

21 indented lines in `TODO.md` carry their own id anchor (ROADMAP: 0); the 2026-09-01 ruling sized the promote pass at 11. Owner ruled 11 UNVERIFIED until the two scopes are reconciled. Load-bearing because loderite MEASURED four ids silently orphaned by exactly this shape (body relocated, address lost, counts unchanged, round-trip green). Contract: promote pass and measurement agree on one population before it runs. Spec: `docs/meeting-notes/2026-09-01-2226-ledger-line-shrink-format.md` <!-- id:8679 -->

## From ROADMAP

  - **Acceptance**: ONE agreed population of TODO.md indented lines carrying their own `<!-- id:XXXX -->`, with the discrepancy EXPLAINED rather than averaged. Three figures are now on record and all three are load-bearing: 11 (the 2026-09-01 ruling, owner-marked UNVERIFIED), 21 (measured the same day), and 10 (measured 2026-09-02 post-wave-2/3 shrink, ROADMAP 0). The shrink itself moved the population, so the reconciliation must state the counting rule and the AS-OF commit, not just a number. Load-bearing because loderite MEASURED four ids (89f9, a5b6, ba07, ed26) silently orphaned by exactly this shape: body relocated, address lost, counts unchanged, round-trip green.
  - **Tests**: a committed counting script both the promote pass and any future ruling call, so the population cannot drift again unobserved.
  - **Done-check**: the agreed count is reproducible from a committed command at a named commit, and `docs/meeting-notes/2026-09-01-2226-ledger-line-shrink-format.md` cites it.
