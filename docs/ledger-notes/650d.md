# id:650d

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

Evidence it is the escalation layer and not the statistic that is broken: the loud line 'MISMATCHES present — investigate before the flip' already fired from 2026-07-20T20:49:08 continuously, ~6300 runs across 22 days, plus 9 SHADOW MISMATCH lines from classify-repo, and produced zero investigation. Lifetime grep -c at :682-683 also dilutes a step function to 0.35%. <!-- id:650d -->
