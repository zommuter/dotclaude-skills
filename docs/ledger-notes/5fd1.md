# id:5fd1

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(successor to [[5eeb]], split off 2026-08-27 by owner ruling). **Deliberately TODO-only — do NOT promote this to ROADMAP.md.** Its deliverable is an OBSERVATION, and no executor can produce one; filing exactly this kind of item in the execute lane is what made `dotclaude-skills` hand back on the `id:bfbf` unevidenced-zero-dispatchable guard in run `relay-20260827-102629-23445` (the child was dispatched `execute-5eeb`, correctly found nothing implementable, and failed to enumerate what it had considered). The lane is `[INPUT — decision]` because the item TERMINATES in an owner call; note honestly that none of the four INPUT lanes fits its actual blocker, which is *more pool runs happening* — it is gated on evidence accrual, not on access/authorship/decision/meeting. **What IS now observed** (run `relay-20260827-102629-23445`, the first in which rule 2c ever executed — it was inert until [[c219]]): the check resolves against real children, sizes them, and crosses both thresholds — 5× `ok`, 1× `warn` at 265,470 B, 1× `handback` at 310,010 B, zero resolution failures. **What is NOT yet observed, and is the content of this item:** (a) the **ZERO-COMMIT branch** — budget blown with nothing committed for the item; added in v15 and never once fired, and it is the branch the whole livelock guard exists for; (b) the **repeat escalation** to the existing `route="hard-split"` enum on a second ZERO-COMMIT handback for the same item; (c) the **pre-first-edit NARROW-SCOPE warn** actually causing a child to narrow scope rather than being read as advisory — the 08-27 run's single `warn` was periodic, not pre-first-edit; (d) **v16's own near-done carve-out** being obeyed as written, i.e. a child that finishes under it NAMES the measured bytes in its report (leancow did not — the carve-out was written after the fact to legitimize its behaviour, so its reporting clause has never been tested). **Do not tick on a green suite.** Every one of these is executor BEHAVIOUR; the suite can only show the code paths exist, which is precisely the inference that got 5eeb wrongly ticked and reverted on 2026-08-27. Close only when a real pool has exercised the branch, with the run id and verdict quoted. <!-- children-of:5eeb --> **REINTERPRETED 2026-08-27 — see `id:0a08` before drawing any conclusion from the unfired branches.** This item reads its unexercised branches (ZERO-COMMIT above all) as *"rule 2c has not yet met that situation"*. A live run now shows a THIRD possibility that changes what the absence means: in run `relay-20260827-154820-505` round 8 a loderite `execute` child made **0 actual invocations of `context-budget.sh`** across **31 `Edit`/`Write` operations** and died at 175,479 tok — it read the contract and never ran the check. **A branch cannot fire in a child that never invokes the rule**, so some unknown fraction of "never fired" is non-invocation, not absence-of-the-situation. That distinction decides whether ZERO-COMMIT needs TESTING or needs ENFORCEMENT, and they are different builds. **Do not tick any branch here on a quiet verdict stream** — the stream stays healthy-looking precisely because complying children keep reporting (11 verdicts over 26 invocations the same morning) while non-complying ones emit nothing at all. Before treating a branch as merely untested, measure the INVOCATION rate: count `tool_use` records with `name=="Bash"` and `context-budget` in the input, per child, against that child's edit count — never grep the transcript for the script NAME, which also matches the contract document the child loads. <!-- id:5fd1 -->
