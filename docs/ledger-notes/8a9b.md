# id:8a9b

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

Build a detector for stale item-status lines (`post-MVP` / `SCHEDULED` / `decomposed into N seams` / `RED specs authored` / `first playable slice`) appearing in `CLAUDE.md` / `ARCHITECTURE.md`, enforcing the doc-hygiene rule that architecture/context docs carry durable conventions, NOT mutable item status or scheduling — status lives in `TODO.md`/`ROADMAP.md`; link, don't restate. **The CONVENTION half has ALREADY SHIPPED and is not the remaining work:** it is encoded verbatim as a global-CLAUDE convention at `~/.claude/CLAUDE.md:138` ("**Architecture/context docs (`CLAUDE.md`, `ARCHITECTURE.md`) carry durable conventions, NOT mutable item status or scheduling** — never \"post-MVP / SCHEDULED / decomposed into N seams / RED specs authored / first playable slice\""), verified 2026-07-31. The original ask was disjunctive ("as a global-CLAUDE convention **and/or** a detector"); the convention shipped, the detector never did, so the item is re-scoped to the detector only rather than closed. Natural home: alongside the existing doc/ledger lints (`roadmap-lint.sh` / `todo-conformance.sh` family) or the id:7a05 git-hook substrate — pick after that substrate is chosen. <!-- id:8a9b -->
