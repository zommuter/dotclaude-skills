# id:b8e8

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(successor to the re-scoped `id:d119`, owner-decided 2026-08-14). `id:d119` ships only the marker GRAMMAR + `roadmap-lint`'s recognition of it; this item is the half that was deliberately left out. **The two sub-parts must land TOGETHER and must never be split across items or commits**: migrating `id:540f`/`id:c179` off `gated-on:e62c,b0b1` onto the new marker BEFORE the dispatch gate honours it would leave both items *dispatchable* in the gap — precisely what the 2026-07-31 owner gate forbids (the refusal they encode makes the relay pool unrunnable in any `/remote-control` session, id:b0b1). Marker spelling is CONFIRMED, do not re-open it: `<!-- owner-hold:REASON -->`. **Acceptance**: after the change, `roadmap-lint` emits no DEAD-GATE for 540f/c179, AND `classify-repo.sh` still refuses to dispatch either — assert BOTH, since either alone is the failure mode. Until this lands, the false DEAD-GATE on 540f/c179 is EXPECTED. <!-- id:b8e8 -->
