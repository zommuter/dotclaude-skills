# id:5c05

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

the ladder's `review`-before-`hard` rung is the D3 anti-gaming invariant (unaudited work is always reviewed before fresh strong work) and must NOT be reordered; what is in scope is demoting `execute` below `hard`/intensive *when and only when* `--intensive` is set, or a per-class weighting the flag tunes. Interacts with id:3ca7 (round-tail lane-pull, which hard-fences during the intensive serial phase) and id:c7b6 (resource-budget co-scheduling). Needs a decision on whether the reorder is `--intensive`-scoped or a new explicit flag. — **MEASURED 2026-09-01: do NOT reorder. Recommendation is to CLOSE this as won't-fix unless you disagree.** There is no sequential ladder to reorder: `PRIORITY = { execute: 0, review: 1, hard: 2, handoff: 3, human: 5, mechanical: 6 }` (`relay-loop.js:277`) is a tiebreaker in ONE comparator (`:2505`) over a pool of one-unit-per-repo, so it allocates slots ACROSS repos rather than sequencing phases. `[INTENSIVE]` units are partitioned OUT of that sort entirely (`:2582`) and run serially-alone AFTER the wave, so a PRIORITY change would not touch them at all -- the item conflates `[INTENSIVE]` with `[HARD]`, which are orthogonal axes. Measured on run `relay-20260831-220243-21277` from `~/.claude/logs/relay-claim.log` (48 dispatches, cross-checks exactly): the 5-wide wave was saturated ONLY in round 1 for a few minutes; rounds 2-8 never exceeded 4 concurrent and trended to 0-1. Exactly ONE plausible review-behind-execute deferral (`linguistic-universals`, ~2 min 16 s, round 1). The 59 blocked units were NOT slot-blocked -- only 6 of 56 scanned repos ever got a claim; traced causes are the prompt-size gate (`id:dd59`), context-budget handbacks in loderite/lean4btc, and an unmet human-lane gate (`open_human_lane: 49`). Reordering would have saved ~2 minutes once in eight rounds. Note also `relay-loop.js:266-270` documents review-above-hard as the D3 anti-gaming window, asymmetrically (review beats FRESH STRONG work, deliberately not Sonnet execute) -- so the swap is an invariant change, not a scheduling tweak. The real bottleneck is gates and size-outs. — **OWNER RULED 2026-09-01: IMPLEMENT THE PRIORITIZATION. This OVERRIDES the close-won't-fix recommendation above; the measurement stands as fact but did not carry the decision.** `--intensive` must PRIORITIZE `[HARD]` and `[INTENSIVE]` over routine execute work, not merely permit it. **What the measurement changes about the IMPLEMENTATION (read before touching anything):** (i) there is no sequential ladder to reorder -- `PRIORITY` (`relay-loop.js:277`) is a tiebreaker in ONE comparator (`:2505`) over a one-unit-per-repo pool, so it allocates slots ACROSS repos; demoting `execute` below `hard` there IS the `[HARD]` half and is compatible with D3, since it does not touch the review-beats-fresh-strong-work rung. (ii) The `[INTENSIVE]` half is NOT a PRIORITY change at all -- intensive units are partitioned OUT of that sort entirely (`:2582`) and run serially-alone AFTER the wave, so prioritizing them means moving that serial phase EARLIER, which is a different and riskier edit that delays all parallel work behind it. (iii) The D3 anti-gaming invariant (`relay-loop.js:266-270`) is NOT in scope and must not be reordered. Scope both halves to `--intensive` being set. <!-- id:5c05 -->
