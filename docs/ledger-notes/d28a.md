# id:d28a

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

**EDITED 2026-09-03 (`id:64f9`, title rewrite) -- declared, per the notes-are-editable
convention in `CLAUDE.md`.** This item's ledger TITLE in `TODO.md` was rewritten to fit
`LEDGER_ITEM_TITLE_MAX`. Nothing else on the item line changed: lane tags, typed-edge and
`id:` markers, the detail pointer and the checkbox are all untouched. The ORIGINAL title is
reproduced VERBATIM in the final section of this note, so no wording was deleted -- it moved
here. The relocated prose below is otherwise unchanged.

## From TODO

requiring source lines to just cite `[INBOUND ... from loderite]` in the TARGET repo only, with no marker needed here) is dotclaude-skills' to make, not loderite's. <!-- id:d28a -->

## Original title (verbatim, before the `id:64f9` rewrite)

Teach `lib-anchored-id.sh`'s `_own_marker_re` an `[OUTBOUND routed:TOK to <repo>]` leading-bracket alternative (today it recognizes only `[INBOUND`) — loderite's executor invented `[OUTBOUND` for the 3 lines where loderite is the routing SOURCE (TODO.md:100/381/383, tokens dea3/0d2c/e4a7), correctly leading-positioned but unrecognized by `token_marker_in_files`. Lower stakes than the INBOUND case (the real twin lives in the TARGET repo's ledger, not loderite's), but it is an unsanctioned vocabulary fork and the decision of whether to extend the regex (vs.
