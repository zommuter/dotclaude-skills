# id:7829

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

Measured 2026-08-27 over **28,365 transcripts** using API `usage` sums (`input`+`cache_creation`+`cache_read`), not a bytes proxy. **`claude-sonnet-5` max observed context by month: 2026-07 = 379,086 (n=77,114) → 2026-08 = 178,310 (n=37,512).** A hard ceiling, not a demand artefact — over the SAME window `claude-opus-5` went 237,147 → **378,108**, so the cut is **Sonnet-specific**. Genuine failures (filtered to the harness's `isApiErrorMessage` envelope, excluding the ~1,085 files that merely QUOTE the string in commit messages / `git log` / ledger prose — the census was nearly fooled by exactly that): **50 total — sonnet-5 pool 34 @ ctx 170,875–178,310; sonnet-5 direct 12 @ 171,959–177,011; sonnet-4-6 pool 3; fable-5 direct 1. 26 of the 50 are in August.** The band matches a 200k window minus a ~24k output reserve, exactly the arithmetic `prompt-size-gate.mjs:62` already documents. **CONSEQUENCE: `relay-loop.js:4202` pins `execute` units to Sonnet, so relay executors are capped ~176k TODAY while review/hard (Opus, `:4203`) are not — the two tiers now differ by >2x and the gate charges them ONE flat budget.** **PROXY EXONERATED, do not chase it:** `relay/scripts/mechanical-proxy.py` intercepts ONLY `model=='bash'` (`:322` bails otherwise) and is byte-for-byte passthrough for every LLM path (`:928-935` header forward, `:945` unmodified body) — no model rewrite, no cap, no `anthropic-beta` injection. The owner's "claude-relay downgraded the ctx" recollection is CORRECT that a downgrade happened, but it is **Sonnet, not Opus, and not the proxy**. **UNDETERMINED (do not assume):** WHY Sonnet dropped (server-side model change vs harness change — outside this repo's evidence); and Opus's true ceiling — 378,108 is the largest ever DEMANDED, not a measured limit, since no Opus failure exists to bound it. Decide: whether executor work needs re-sizing (smaller items / tighter slices) now that its window halved, and whether to pin an explicit wide-context model id for any tier. **TWO CORRECTIONS 2026-08-27, both to THIS entry's own overclaims — owner-raised.** **(1) "PROXY EXONERATED, do not chase it" IS TOO STRONG — narrow it to what was actually measured.** What was verified is that `mechanical-proxy.py` does not REWRITE THE REQUEST BODY: it bails on any non-`bash` model and forwards bytes unchanged. That is a claim about the proxy's CODE, not about the harness's reaction to a proxy being configured at all. **Setting `ANTHROPIC_BASE_URL` demonstrably changes Claude Code's own behaviour independently of what the proxy does** — the known instance is that an in-path proxy DISABLES `/remote-control`. So harness-level effects of BEING proxied are real, were NOT measured here, and remain a live hypothesis for any capability difference. Correct reading: **"the proxy does not rewrite model or context on the wire"** — NOT "the proxy cannot affect anything". **(2) The owner asked whether children showed Opus only because the PARENT session was Opus — i.e. model/window INHERITANCE rather than a working `opts.model` override. The already-gathered data largely REFUTES that, and the refutation is worth keeping:** the same pools contain **84,294 `claude-sonnet-5` child transcripts** (execute units, pinned `opts.model='sonnet'` at `relay-loop.js:4202`). If children merely inherited the parent's model they would ALL be Opus — they are not, so the override demonstrably takes effect. Stronger still on the WINDOW question: those Sonnet children **failed at ~176k INSIDE Opus-parent pools**, so a child does NOT inherit the parent's wider entitlement; the window tracks the CHILD's model. **RESIDUAL, honestly unresolved:** this does not fully exclude that a wide-context entitlement is account- or parent-scoped in a way that happens to favour Opus children, and **no relay pool has ever knowingly been started from a Sonnet parent** (owner's own recollection), so the Opus-parent case is the only one observed. If a pool is ever run from a non-Opus parent, RE-MEASURE before trusting the 300,000 Opus budget — and that residual is the strongest argument for pinning an explicit wide-context model id rather than relying on the plain `claude-opus-5` default. **LIVE INSTANCE 2026-08-27, run `relay-20260827-154820-505` round 8 — the FIRST one caught in the act, and it exposes the structural gap.** loderite `execute` on `id:551f` dispatched 15:30:56, died 15:42:21. Cause CONFIRMED from the child's own transcript (`agent-a42ba2332fc0f5ba1.jsonl`), not inferred: final assistant text is literally **`Prompt is too long`**, `stop_reason: stop_sequence`, at a **MAX observed context of 175,479 tok** after ~9.5 min of work — dead centre of this item's measured death band (170,875–178,310). **The pool handled it correctly** (handback, worktree parked/reaped, no auto-resume for execute) — nothing was lost and nothing is wedged. **WHY THE PREVIOUS ONE SURVIVED:** round 7 ran the SAME repo on a different item (`id:b5b1`) and integrated cleanly at `relay-ckpt-20260827-1728`. The difference is the ITEM's work, not the repo or any config. **THE GAP — the prompt-size gate cannot prevent this class, by construction:** it sizes a child's START, and when an `id:e68f` slice exists it sizes on the SLICE, which here measured **1,473–1,504 bytes**. So the unit started at roughly 65k overhead + ~1.5k slice ≈ 66.5k against a 100,000 budget — passing trivially, with ~110k of headroom to the ~176k wall, all of which the child consumed reading and editing during work. **NOT caused by the 2026-08-27 constants change:** the Sonnet budget was left at 100,000 and the overhead correction (12,000→65,000) makes the gate strictly TIGHTER, never looser. **CORRECTION to `id:10dc`'s modelled figures:** that model's "EXECUTE refuses 4 — lodelore, it-infra, loderite, dotclaude-skills" was computed on the UNSLICED path (whole ledgers). Real execute units carry an item-keyed slice, so they are sized on ~1.5 KB and dispatch regardless of ledger size — **loderite execute was never going to be refused**, and the modelled execute-refusal counts over-predict for any repo whose units get sliced. The review figures are unaffected (a review unit is structurally unsliceable — `id:502f`). **WHAT WOULD ACTUALLY HELP** (none of it decided here): a mid-work budget check that hands back BEFORE the wall — which is exactly what executor-contract rule 2c is for, and `id:5fd1` records that its ZERO-COMMIT branch has still never fired; or smaller executor items; or moving execute off Sonnet. <!-- id:7829 -->
