# id:f269

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

`--max-usd N` / `RELAY_MAX_USD`) that stops a run on measured spend, not quota utilization — today only `--quota-7d`/`RELAY_QUOTA_THRESHOLD` exist and they gate on % of quota, so a pre-registered dollar ceiling binds nothing <!-- id:f269 -->
