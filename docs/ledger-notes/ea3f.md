# id:ea3f

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(owner-suggested methodology 2026-08-26, proven the same session — file it before it is lost). The paradox this solves: the artifact you must inspect to diagnose a context death is precisely the artifact that kills whoever reads it. A 428 KB / 477 KB agent transcript overflows the investigator, so the ONLY safe approach is measurement, never ingestion. **The method, in order:** (1) SIZE CENSUS — `ls -S`, `wc -c`, `wc -l` per `agent-*.jsonl`; outliers alone often answer it. (2) IDENTIFY the suspect via the tiny sibling `agent-<id>.meta.json` and `grep -c` (COUNT only, never print). (3) TERMINAL ERROR ONLY — `tail -c 2000` and extract just the final error; the verbatim message is the single most valuable line. (4) LENGTH-ONLY CULPRIT ANALYSIS — `awk '{print NR, length($0)}' f.jsonl | sort -k2 -rn | head -20`, then `jq` out ONLY metadata (`.type`, role, tool `.name`, ~120-char preview) for the top offenders. (5) CUMULATIVE GROWTH CURVE — running byte total vs line number; **this is the discriminator** between one giant blob and death-by-a-thousand-cuts, and it changes the fix entirely. **The interpretation that must accompany it:** distinguish (a) oversized INITIAL prompt — the id:35b7/id:4f9b dispatch-gate's domain, which should have REFUSED dispatch — from (b) MID-RUN accumulation, which no dispatch-time gate can ever see and which needs a different fix (id:5eeb). Getting (a) vs (b) wrong means fixing the wrong layer. **Recommended SHAPE — a script plus a thin skill, NOT a pure prose skill** (mechanize-first, id:415b determinism gate): steps 1, 4 and 5 are pure mechanical computation over byte lengths and belong in `tools/transcript-forensics.sh <transcript-dir|file>` emitting the census, the culprit table and the growth curve; only step 3's reading of the terminal error and the (a)/(b) judgement genuinely need an LLM. A prose-only skill would re-derive the same awk by hand every time and invite exactly the whole-file Read the method exists to forbid. Gate is met — ≥2 consumers this session alone: the id:391b wiring agent died on relay-loop.js, and two loderite executors died in run `relay-20260826-162405-7522`; the fleet's context deaths are recurring, not a one-off. The script should also refuse/warn if asked to emit more than N lines of transcript CONTENT, so the tool itself enforces the discipline rather than merely documenting it. <!-- id:ea3f -->
