# id:08f2

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

-- 'garbage collection for an allocator nobody is fixing'; the durable fix is at the WRITE site (md-merge/append.sh/relay-human write-backs should write the body into <id>.md and touch the head line minimally) -- judged the most important item and it is upstream, not loderite <!-- id:08f2 -->
