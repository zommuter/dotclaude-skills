# id:016e

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(owner 2026-08-27: *"since for a customer project I'm using forgejo for git hosting, consider its issue tracker (and featureset in general) for the Trackers as well -- maybe worth directly adding to this session somehow?"*). **Why it is a materially different candidate, not a third me-too:** Vikunja and Plane are both STANDALONE trackers that would sit beside the git hosting; Forgejo would be the tracker INSIDE the forge the owner is already running for a customer project, so issues, code, refs and CI live in one system and the `(repo,id)` composite key the intermediate schema already uses maps onto its native repo+issue identity rather than needing a synthetic project mapping. It is also already-paid-for infrastructure in at least one real engagement, which is a cost axis neither incumbent has. **Open questions the evaluation must answer, none assumed:** does its API express the `blocked_by` / dependency edges the intermediate schema needs (Forgejo has issue dependencies; whether they survive round-trip through `tracker/schema/ledger-intermediate.schema.json` is untested), does it support the per-view `todo_status`/`roadmap_status`/`review_status` split (`id:857d`'s binding gate) or force one status field, and can a scoped token do label and relation writes without the JWT fallback that makes `id:46d5` an unattended-timer blocker for Vikunja. **SEQUENCING, and this is the load-bearing part:** adding an arm mid-pilot is an amendment to a pre-registration, exactly the class the global CLAUDE.md warns about. It must ride the SAME owner-ratified amendment that the derived-index third arm rides, not a second one -- two separate widenings of one pre-registration is how a fail condition quietly stops meaning anything. If the owner declines that amendment, this item is declined with it. **Do NOT build a Forgejo adapter before the amendment is ratified.** Sibling of the derived-index arm; parent pilot is `id:4a5c`, control arm `id:8066`, adapter contract `id:90f2`. — **FOLDED INTO THE SINGLE ARM AMENDMENT 2026-08-27** (`id:a08d`), as this item's own sequencing clause requires: one amendment covers every candidate arm, and if it is declined this item is declined with it. The amendment's RECOMMENDATION for Forgejo is SCOPE-ONLY, not admission as a measured arm on that ruling, because an arm under `id:90f2` needs a live instance plus an adapter plus a live-verified run and the owner barred building or standing up either. The three open questions above were deliberately NOT answered from documentation: each is a claim about what the system ENFORCES, and the fleet's own observability rule says such a claim can only be produced by exercising it. Recorded hypothesis, to be probed and not assumed: a forge issue model is binary open/closed, so `id:857d`'s per-view triple is the likeliest failure point and would force the same label-laundering Plane already hit. <!-- id:016e -->
