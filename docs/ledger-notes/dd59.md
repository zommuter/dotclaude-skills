# id:dd59

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

-- the REVIEW-side sibling of id:93cc (which is execute-side), and it is not a missing gate: the `id:4f9b`/`id:b018` prompt-size gate fired correctly and refused dispatch 3 times in one run, so the failure is graceful but total. MEASURED 2026-09-01, run `relay-20260831-220243-21277`: `RELAY_STATUS.md` repeat-handback ALERT `dotclaude-skills verdict=review handbacks=3`, each `NOT dispatched -- the assembled review prompt is ~470285 tok, over the 300000 tok dispatch budget`. The `id:e68f` slicer IS in the path (id:b018 made the gate measure the slice), so slicing as currently scoped does not bring this repo under budget -- 1.57x over. Consequence: unaudited executor commits accumulate with no review path, which is exactly the D3 anti-gaming invariant's blind spot. Note the same run ALSO produced a raw `[execute:loderite] failed: Prompt is too long` (an ungated overflow that reached the API), so both the gated and ungated forms are live in one run. **Directions:** (a) find what dominates the review slice -- the reviewer sees more than an executor (whole ROADMAP + REVIEW_ME + RELAY_LOG + the diff), so measure the per-component bytes before choosing; (b) review-scoped slicing (only the units this round actually worked, not the whole ledger); (c) a per-repo review that chunks across rounds. Relates id:93cc (execute half), id:4f9b/id:b018 (the gate), id:e68f (the slicer), id:cd9c/id:689-stub-pruning (headroom). — **REVIEW PATH RESTORED 2026-09-01 (`cf0384f5`), item stays OPEN for the residue.** Directions (a) and (b) are DONE: measured (TODO.md is 66.4% of the prompt at 1,245,034 B, ROADMAP 15.8%, fixed overhead 65,000 tok), and review-scoped slicing is built AND wired. `ledger-slice.sh` gained `--ids a,b,c` and `--since-ckpt <ref>` (derives its own set: keyword-prefixed `id:`/`routed:` in commit messages over `<ref>..HEAD` UNIONed with anchored `<!-- id:X -->` markers on lines ADDED to TODO/ROADMAP, the review.md §5b reverse-handoff source); `sliceLedgerForUnit` calls it for review units off `unit.lastCkpt`. MEASURED: 18 ids → 66,673 B (~23x), the live 6-id review set → 15,443 B, and the real `oversizeDispatchReason` flips from REFUSE (~468,932 tok) to dispatch. Fail-open preserved on every path (no lastCkpt / bad ref exit 2 / empty set exit 5 / throw), so it can only ADD a slice, never suppress a dispatch. **Three premises in this item were FALSIFIED and are corrected here:** the slicer was NOT in the path (the error text that fired is the UNSLICED branch's, `relay-loop.js:3109`, not the sliced branch's `:3079`); archiving is a DEAD END, not merely insufficient (657 open items, ZERO `[x]` lines, 95.5% of bytes on head lines, so `archive-done.sh` has nothing to move); and splitting TODO.md is DEAD ON ARRIVAL (479/657 items = 72.9% have zero continuation lines, so at most 4.5% is movable -- the same failure that made loderite's `id:8d2c` unreachable). **RESIDUE:** the gate still never counts the DIFF (`prompt-size-gate.mjs:317` sums prompt+roadmap+todo only), so a long `--afk` run's accumulated diff is uncounted burden that can still overflow ungated -- measure it before declaring this closed. Also unvalidated in a live run: this has only been exercised by hand, never by an actual pool review dispatch. — **CROSS-REFERENCE 2026-09-01: this item's "splitting TODO.md is DEAD ON ARRIVAL" finding does NOT apply to `id:55f6`.** What was measured here is moving CONTINUATION lines out (72.9% of items have zero, so at most 4.5% is movable). `id:55f6`'s owner-ruled per-item split moves the HEAD LINE's prose, which is where this same measurement found **95.5% of the bytes**. Two different splits; this finding refutes only the first. The owner independently restated the 55f6 design on 2026-09-01, so the misreading risk is live -- stated here so nobody cites this line to close that item. — **RESIDUE MEASURED 2026-09-01, and THIS ITEM'S OWN STATEMENT OF IT IS FALSE. Correcting it here rather than closing on it.** The mechanical half is confirmed exactly as written: `estimateDispatchTokens` sums only the assembled prompt string + ROADMAP.md + TODO.md, and for a review unit also REVIEW_ME.md plus a 32,768 B window of RELAY_LOG.md, at 4 chars/token with a flat 65,000-token overhead. No diff term exists anywhere -- an exhaustive grep for any `diffBytes`/`diffTokens` identifier across the gate and loop scripts returns nothing. **But the stated CONSEQUENCE -- `can still overflow the dispatch budget UNGATED` -- cannot happen, because the diff NEVER ENTERS THE DISPATCHED PROMPT AT ALL.** `unitPrompt()` is pure instruction text: it interpolates verdict/repo/worktree/branch/lastCkpt and per-verdict instruction blocks, and embeds no file contents. `relay/references/review.md` has the CHILD run `git log --stat` and `git diff -U0 <LAST>..HEAD` ITSELF, during its live turn, AFTER dispatch was approved. So the pre-dispatch gate is structurally incapable of being overflowed by the diff -- the diff is never one of its inputs. What the diff actually consumes is the CHILD'S OWN RUNTIME CONTEXT mid-turn, which nothing gates, and `prompt-size-gate.mjs:308-312` ALREADY documents that as the acknowledged `id:9663` residual (*this budgets what the child is REQUIRED to read, not everything it MAY pull*). **So this residue is a special case of a known gap, not a distinct hole, and the failure surface is `child degrades mid-turn`, not `the gate passed something it should not`.** MEASURED magnitude, 5 checkpoints spanning 5.7 h to 6.1 days: diff churn is a consistent 4,500-6,300 tokens/hour; the 6-day whole-repo diff is 898,421 tokens, ~3x the 300,000 dispatch budget. **The caveat that shrinks the worst case:** `unit.lastCkpt` is the most recent `relay-ckpt-*` of ANY verdict and `ckpt-tag.sh` fires at EVERY integrate, so while anything keeps integrating the window stays in the tens of thousands of tokens over hours; the multi-hundred-thousand case needs a repo going DAYS with no successful integrate of any kind. How often that happens is UNMEASURED. **If a fix is ever wanted** it is a host-measured `diffBytes` folded into `countedLedgersFor`'s review-only set -- but note two costs: it is a real behaviour change that will REFUSE review dispatches currently allowed (the same trade `id:10dc`'s overhead correction made), and windowing it without narrowing `review.md`'s READ contract would make the gate LIE, since review.md requires the WHOLE diff across several steps -- that is exactly how `id:b018`'s original ROADMAP-only gate under-counted TODO.md. <!-- id:dd59 -->
