# id:51d8

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(user 2026-07-11, prompted by the `/relay human --all` wall-of-text output) — a "fancy" always-available dashboard that surfaces the human-backlog tiers the relay already produces (`gather-human-backlog.sh` TSV: **"you run these"** = hard_hands + manual + run-classified REVIEW_ME; **"needs a /meeting"** = hard_meeting + chewy tier-c; **hard_pool FYI**), kept current by the `/relay` loop as a mechanical side-effect (no LLM needed to *launch or refresh* it — it reads the committed relay artifacts + `RELAY_STATUS.md`, cf. the id:c8b6 observability artifacts and the `projects` dashboard skill). Each "you run these" item is **prepared as fully as possible ahead of time**: verbatim recorded steps/commands + `<file>:<line>` jump refs (the human.md §4 verbose expansion, but *precomputed*, not LLM-generated per view), and — for pure tool-call/CLI items — a **"run now"** affordance that fires the prepared command (with a confirm gate). Meetings still need an LLM to *run* (route out to `/meeting --cross`, and maybe later `meeting-rpg`), but everything else is preparable. **Design Qs for the meeting:** (1) surface — a **TUI** dashboard (needed for interactive "run now" of CLI/tool calls; framework: stdlib curses vs textual vs a Go/bubbletea sidecar) vs a static/served web page (like the id:36f1 graph) that can only *show* prepared instructions, not launch them; likely both — a read-only web view + a TUI launcher. (2) how the verbose "you run these" steps get **precomputed mechanically** (today they're produced by the LLM triage turn) — a mechanical expander that pulls the ROADMAP/REVIEW_ME item body + cited file lines, or a cached artifact the last `/relay human`/`review` writes. (3) the **"run now" safety model** — which item classes are safe to auto-fire (idempotent CLI like `make install-relay`, `append.sh inbox-done`) vs which need a hard confirm or are never auto-runnable (anything hands/sudo/irreversible); reuse `host-gate.sh` for host-bound items. (4) refresh trigger — a systemd-user timer (like `relay-gap-sample`) reading the artifacts vs a hook on each relay round. (5) relationship to `RELAY_STATUS.md` (which already rolls up Queued/Blocked/Skipped) — extend it vs a new artifact. Feeds and is fed by the human-backlog gather; keep it LLM-free to *view* so it works even when no session is running. — **DECIDED 2026-07-11 (meeting 1239, D5):** FULL build delivered via the relay loop (decompose into ROADMAP items: mechanical precompute → LLM-free renderer as a **separate artifact** (not an extension of RELAY_STATUS.md) → TUI launcher + "run now" → timer). **Run-now safety:** never-auto-fire, always-confirm, only idempotent/safe classes one-click, reuse `host-gate.sh` for host-bound, irreversible/hands = show-steps-only (precompute tags each item's class). Not pilot-gated — build it all, but as relay work. <!-- id:51d8 -->
