# id:99b7

Detail relocated out of the ledger by `tools/ledger-shrink.py`. The item line keeps
its title, lane tag, `id:` anchor, every gate marker and a pointer back here.
**Nothing was deleted** -- the prose below is reproduced verbatim.

See `docs/ledger-notes/BACKLINKS.md` for meetings that cite this id.

## From TODO

(found 2026-08-27 walking the backlog; owner-ratified to file). TWO halves, one queue. **(a) PRODUCER — a fork's third-party upstream is treated as a publish target.** `integrate.sh`'s per-remote narrowing (id:4d44) classifies any non-private remote as public-and-awaiting-ratification. For `git-annex` that is `upstream` = `git://git-annex.branchable.com/` — the real project, which we only ever FETCH from and which a `git://` URL makes unpushable anyway. Four entries (`relay-ckpt-20260826-1309/1403/1454/1508`) each record `pending: upstream` and therefore can NEVER resolve; every future git-annex integrate mints another. `lib-private-remote.sh` answers *is this remote PRIVATE?* — correctly — but the ratification producer needs a different predicate: *is this a remote WE PUBLISH TO?* A read-only scheme (`git://`), or a remote that is a declared fork upstream, must be excluded from the pending-set rather than held. **(b) CONSUMER — no retire path.** Subcommands are `list|show|verify|resolve` only; `resolve` verifies the remote carries the merge sha and refuses otherwise, which is CORRECT and must not be weakened. But that leaves no way to close an entry whose merge sha can never land — (a)'s four, plus two whose commits no longer exist at all (`a9fa3917` dangling, `d47ccf34` absent from the object store; both id-sets landed by other paths, `7518`/`372a` `[x]` on main and `d51f`/`f66e` `[x]` in TODO.archive.md + CHANGELOG.md). Clearing them today would mean hand-editing the flock'd `ratification-queue.jsonl` behind the verifying tool — the route-around-a-guard anti-pattern, explicitly refused. Needs a `retire <key> --reason TEXT` that records WHY it can never land and is auditable, NOT a `--force` on resolve (which would let a real un-pushed merge be marked ratified — the one thing the queue exists to prevent). **Why it matters beyond tidiness**: a queue that reports 6 pending when 0 are actionable trains the reader to ignore it, and this backlog was mis-read as 13 pieces of blocked work for exactly that reason. **HALF (a) DIRECTION RATIFIED 2026-08-27 — an explicit do-publish ALLOWLIST, not a scheme-based exclusion.** The owner chose this over the offered mechanical default (exclude `git://`/read-only push URLs), and INVERTED its polarity: rather than "exclude what looks unpushable", declare the remotes we DO publish to and treat everything else as not-a-publish-target. **Why this is the better direction, explicitly:** the scheme check fails OPEN (include when unsure), which for a *publishing* decision is the wrong way round; an allowlist fails CLOSED — an undeclared remote is never published to and never enters the pending-set — matching the already-ratified posture that publishing to a public remote is never automatic (`id:f66e`). **Shape**: a GLOBAL default plus per-repo additions. Measured 2026-08-22, 41 of 46 own-repo remotes are the private `fievel` LAN host and all are named `origin`, while exactly four repos (`dotclaude-skills`, `zkm`, `toesnail`, `proton-moresync`) carry a public GitHub remote — so a global `origin` default covers the bulk and those four declare `github` explicitly. This yields a clean THREE-WAY semantic that the current two-way private/public split cannot express: private+declared ⇒ push immediately; public+declared ⇒ push gated by ratification; **undeclared ⇒ never push, never track** (the git-annex `upstream` case that mints today's unresolvable entries). **HAZARD to handle in the same change, or this trades one silent failure for its mirror:** a pure allowlist silently swallows a remote you genuinely want to publish to — add a GitHub remote, forget to declare it, and nothing publishes and nothing complains. The change MUST surface an undeclared remote loudly (one line per repo per run: "remote X not in the publish set — not publishing, not tracking"), never skip it in silence. **Interaction with existing machinery:** `lib-private-remote.sh` stays THE private-host predicate (pre-push gate + integrate's per-remote narrowing, id:4d44); the allowlist is a SECOND, orthogonal question ("do we publish here at all?") and the two must not be conflated — do not extend the private predicate to answer it. **Blast radius — why this was NOT handed to an executor blind:** half (a) edits `integrate.sh`'s push path, where a wrong allowlist means work stops being published fleet-wide, which is worse and quieter than the junk queue entries it fixes. Wants a RED spec covering the three-way semantic AND the undeclared-remote surfacing before implementation. Half (b) (the `retire` path) is independent and was built first. **SEVERITY ESCALATED 2026-08-27 — this is not only a queue-hygiene defect: the relay ATTEMPTS TO PUSH TO A THIRD PARTY'S REPOSITORY.** Code-confirmed at `relay/scripts/integrate.sh:845-852`: the ONLY per-remote opt-out is a push URL set to the literal string `no_push`; otherwise, for a **non-substantive** unit (`defer_push` empty) EVERY remote enters `to_push` — the comment says so verbatim (`# non-substantive: push everything`). For git-annex that means `git push upstream` against `git://git-annex.branchable.com/`, the upstream project this repo forks. Observed LIVE, not theorised: while authoring the RED spec a fixture used the real upstream URL and scenario D's non-substantive path actually attempted the push, which git-annex's pre-receive hook rejected. Nothing was published (git:// is read-only there) and the fixture was replaced with a local bare repo — but the attempt is real and would recur on any non-substantive git-annex unit. The substantive path is NOT affected (it defers non-private remotes for ratification, which is how the four unresolvable entries got minted). **STOPGAP APPLIED 2026-08-27** (owner-ratified), ahead of the real fix: `git -C ~/src/git-annex remote set-url --push upstream no_push`. Because `:846` `continue`s BEFORE `eligible_count` is incremented, this kills BOTH symptoms — no push attempt on a non-substantive unit, and no `pending_remotes` entry on a substantive one. The FETCH url is deliberately unchanged (`git://git-annex.branchable.com/`), so tracking upstream still works. Revert with `git -C ~/src/git-annex remote set-url --push upstream git://git-annex.branchable.com/`. NOTE this also means `no_push` and "undeclared" become two ways to say the same thing — reconciling them is an open question for the implementation (the RED spec leaves `no_push` alone). **MIGRATION ORDERING RATIFIED (owner 2026-08-27): CONFIG FIRST, THEN CODE.** Write `[publish] default_remotes = ["origin"]` plus `publish_remotes = ["github"]` for the four public repos (`dotclaude-skills`, `zkm`, `toesnail`, `proton-moresync`) BEFORE the code lands, so the new predicate is a no-op on day one and any surprise surfaces as the loud undeclared line rather than a silent publication stop. Landing code-first would quietly halt GitHub publication for exactly those four. **RED SPEC AUTHORED**: `tests/test_publish_allowlist_99b7.sh` on branch `work/99b7a-red-spec` (worktree `~/.cache/relay/worktrees/dotclaude-skills/spec-99b7a`, commit `8f906c2b`) — 16 pass / 13 fail, red for the right reasons, reproducing the live defect record verbatim. Every scenario asserts the LOUD SURFACING of an undeclared remote, not merely its exclusion; without that the spec would ratify the mirror bug (a real remote silently never published to). Still owner-decidable before implementation: whether `undeclared ⇒ never push` is unconditional (the spec pins YES, which NARROWS id:4d44 case 4); per-repo `publish_remotes` union-vs-override; whether the loud line is per-unit (where `integrate.sh` runs) or deduped per-repo-per-run (which would be caller-side in `relay-loop.js`); and that three existing fixtures (`test_integrate_per_remote_push_4d44.sh`, `test_diary_push_remote_narrowed_f66e.sh`, `test_integrate_ratification_gate_4d44.sh`) will need `publish_remotes` added to their fixture config. **LANDED + STOPGAP DROPPED 2026-08-27.** Half (b) shipped earlier (`182b82aa`, `retire`). Half (a) MERGED to main (`ca037a8c`, merging `16d61164` on `work/99b7a-red-spec`) after the owner ratified all four open semantics: (1) `undeclared ⇒ never push` UNCONDITIONAL — this NARROWS `id:4d44` case 4 from "non-substantive pushes every remote" to "every DECLARED remote", which is the defect, not collateral; (2) per-repo `publish_remotes` UNIONs with the defaults; (3) the loud undeclared line is PER-UNIT; (4) `no_push` stays checked first. **The `git -C ~/src/git-annex remote set-url --push upstream no_push` stopgap has been REVERTED** — push url is back to `git://git-annex.branchable.com/` — because it was SUPPRESSING the new loud line for the one repo the whole item exists for. **Verified end-to-end through the shipped `lib-publish-remote.sh`, not by reasoning:** `git-annex` declares `origin` only ⇒ `origin` PUSH, **`upstream` REFUSED (undeclared)**; `dotclaude-skills` and `zkm-vcard` declare `origin`+`github` ⇒ both push, so the ten plugins keep publishing. Suite **515 passed / 0 failed / 1 expected-red** post-merge. **CORRECTION to this item's own earlier note:** the `origin` floor is NOT unconditional. `lib-publish-remote.sh:111-113` applies it *only when no `[publish] default_remotes` is declared at all* — an explicitly declared non-empty list is honoured verbatim, **including one that omits `origin`**. So the global set IS opt-out-able; what a single repo cannot do is SUBTRACT from it, because per-repo entries union. **OPEN FOLLOW-UP, deliberately not in the merge:** gate the floor on `lib-private-remote.sh`. All 56 own-repo origins are the private LAN host TODAY, so it is safe — but `git clone https://github.com/…` sets `origin` to a public remote, and the floor would then publish there straight through this allowlist. That is a fleet property, not an invariant. <!-- id:99b7 -->
