# id:c471

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

(user 2026-07-10) — `seven_day.utilization` reads **0.0 on a fresh cache** while `.limits[]` `weekly_all.is_active=false`, so `--quota-7d`/`RELAY_QUOTA_THRESHOLD_SEVEN_DAY` cannot bind — only `five_hour` actually guards a pool. Evidence (`~/src/claude-diary/quota/quota-samples.jsonl`): `seven_day` climbed **27%→33% across 2026-07-08**, then reads **0.0 on 2026-07-10 within the SAME weekly window** (resets 2026-07-14) — utilization cannot fall mid-window, so the 0.0 is a non-report, not a real drop. Consequence: a run that passes `--quota-7d 45` gets NO 7-day protection yet `quota-stop.sh` treats the absent/zeroed bucket as "well under cap" and proceeds silently. **FIX:** at the quota gate, detect a non-reporting / inactive weekly bucket (`seven_day.utilization` null-or-0 **and** the corresponding `.limits[]` `weekly_all.is_active=false`) and, **only when a 7-day cap was explicitly requested** (`--quota-7d` / `RELAY_QUOTA_THRESHOLD_SEVEN_DAY` set), LOUDLY surface "requested 7-day cap is currently unenforceable (weekly bucket not reporting)" rather than silently gating on the zero (id:4347 no-silent-swallow). Do NOT hard-stop on it (observe-first) and do NOT touch the tested absent-bucket skip fail-safes (id:c5ba/id:0175) — this is an additive LOUD notice on the *requested-but-dead* path. Cross-ref memory `usage-api-per-model-weekly-null-2026-06-30`; relates the id:d267 sampler (evidence source) and the front-door `--quota-7d` mapping (SKILL.md). <!-- id:c471 -->
