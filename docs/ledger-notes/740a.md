# id:740a

Relocated from the ledger by `tools/ledger-shrink.py`. Conventions, the verbatim
claim and backlinks: `docs/ledger-notes/README.md`.

## From TODO

Hit LIVE this review (run relay-20260908-174448-4421) promoting `id:09e4`: `insert_after` on `id:cb9a` put the new head plus its three `Acceptance`/`Done-check`/`Context` lines BETWEEN cb9a's head and cb9a's own three body lines. Result: cb9a lost its acceptance criteria to the new item's block and the new item appeared to own six continuation lines. Exit 0, nothing on stderr, the marker count stayed correct -- invisible to `grep -c`, and `_item_block_range` (`id:4f0f`) would then report the wrong boundary for BOTH items. `id:4f0f` gave `regex_sub` block reach but left the INSERT ops line-anchored; this is the remaining half. **Fix**: an insert anchored on an id must resolve the anchor's BLOCK via `_item_block_range` and place the payload after the block's last line (`insert_after`) or before its head line (`insert_before`). **Acceptance**: inserting after a wrapped item leaves the anchor's continuation lines attached to the anchor, byte-identical. **Done-check**: a new `tests/test_md_merge_insert_block_anchor_740a.sh` fixture with a wrapped anchor; assert the anchor keeps all its continuation lines and the new item owns only its own. Relates 4f0f, f833, e166, 4983. <!-- relates:4f0f --> <!-- id:740a -->
