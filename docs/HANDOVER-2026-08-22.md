# Handover — 2026-08-21/22 session (integrate.sh battle-test → context budget → guard hardening)

**Session:** `a19755d6-a869-4edb-b5bc-6ed91e168120` · started from `docs/HANDOVER-2026-08-21.md`
**Suite:** **484 passed, 0 FAILED, 1 expected-red** · `main` = `9ae6aed`, pushed to `origin`
**Quota:** 7d 78% (owner authorised **85%**), 7d resets 2026-08-25T12:00Z
**`github` is deliberately behind** — publishing is the owner's ratification act.

> **The ledger is the source of truth, not this file.** Every finding is a `TODO.md` item.
> If this disagrees with the ledger, the ledger wins.

## THE ONE THING TO DO FIRST

**The pool is unblocked.** Both blockers the owner named are fixed, merged and verified:
`id:5155` (a landed merge on zero remotes reported `ratification=none` and was never queued) and
`id:a726`(c) (the private-remote ERE false-positived in the unsafe direction). Suite fully green.

Launch: **`/relay --afk --exclude it-infra --quota-7d 85`**
— or **`/relay human` first** (numbers below); the owner named both as options.

**Check `uptime` before launching.** This session ran at load 31–47 from concurrent agents; a pool
dispatched into that crawls and runs its suites under the `id:7518` flake conditions.

`lean4btc` **will** be picked up — onboarded (`own`, 2026-08-21), classifies **`review`** at
priority rank 2. Apex-only/everything-`[HARD]` lane, so no `[ROUTINE]` dispatch.

## `/relay human` first? — the numbers

- **6 open `REVIEW_ME.md` boxes.** One is now moot: `id:3a09`'s close boundary asked whether the tick
  read right while the guard was ticked-but-unwired. It is wired, tested, and has blocked a real
  destructive command — that box can close.
- **35 open `[INPUT — …]` ROADMAP items.**
- **Ratification queue is EMPTY** (`ratify-queue.sh list`) — expected; no substantive pool integrate
  has run under the new gate. Emptiness is not evidence of a problem.
- Caveat: `id:9d06` (relay human under-reports, zero `review_me` rows) is still open. Cross-check any
  count against `grep -c '^- \[ \]' REVIEW_ME.md`.

## What shipped (all merged, verified centrally, pushed)

| id | what |
|---|---|
| `8739` | `integrate.sh` passes an explicit isolation base. `id:4d44` froze `origin/main`, silently disabling the gate that catches a child writing to the MAIN CHECKOUT — it reported `ok` forever. Re-verified: breach caught (exit 21) with `origin/main` frozen. |
| `4d44` | Per-remote push: private/LAN remotes push automatically, public ones defer. Shared predicate `lib-private-remote.sh` (one definition, used by both the hook and `integrate.sh`); `git-lock-push.sh --remote`. |
| `5155` | 🔴 A landed merge on ZERO remotes reported `ratification=none` with no queue entry. Both causes fixed; `none` is now emitted in exactly one case — every eligible remote verified. |
| `a726`(a)(b)(c) | Land marker moved after the ckpt tag (ratified supersession, below); `remaining=` gained a real `git-lock-push` arm; the private ERE is HOST-anchored. |
| `3a09` | Destructive-git guard **wired and unconditional**. All five ops deny in every context; the `ENTRYPOINT=cli ⇒ interactive` verdict is REMOVED. |
| `b4dd` | Privacy gate >9 min → 4.76 s (it was re-scanning all history once per annotated tag). |
| `5171` | Privacy gate now scans merge-commit conflict resolution (`--diff-merges=dense-combined`). |
| `e7e4` | Park starvation — loderite had 6 actionable items and was skipped across two runs. Skips are now LOUD. |
| `b818` | Sigpipe lint prunes nested checkouts — the suite was not hermetic w.r.t. sibling agents' WIP. |
| — | `run-anomaly-scan.sh` (16/16 mutations killed) · `death-record.sh` (12/12) · `ratify-queue.sh` · `agents/echo-runner.md` repo-managed with `install-agents`/`status-agents`. |

## Owner rulings this session — do not re-litigate

- **`id:4d44`**: pool merges locally, human ratifies and pushes. Amended: the gate is **PER-REMOTE** —
  private auto-pushes, only public defers.
- **`id:299c`(c)**: fix the remedy TEXT; the 100k cap stays **deliberately**. A **COMPOSE TRAP** is
  recorded: `FIXED_OVERHEAD_TOKENS` is 12k against a measured 58.6k, and the cap is 100k against a
  derived 116.4k — **fixing either constant alone makes the gate worse.**
- **`id:3a09`**: all five destructive git ops deny unconditionally. The argument, now in the module
  docstring: this hook governs the AGENT, not the human — a human who needs the command types it in
  their own terminal.
- **`id:5155` supersession**: the land marker moved after the ckpt tag, superseding `id:f5d9(a)` and
  `id:5fe2` acceptance 4 (both asserted `landed=false` on push failure). Ratified — that premise is
  false as of `id:4d44`: step 7 tags before step 8 pushes.

## Open, shaped for the next session

- **`id:6a6b`** — the owner proposed **RE-PERMITTING** `restore .` / `checkout -- .` when there is no
  `-C`/`--git-dir`/`--work-tree` redirect and the cwd is a disposable worktree. The guard already
  parses those globals (`_GIT_GLOBAL_WITH_VALUE`), so it is implementable. **Gap: the ORCHESTRATOR
  works in the main checkout and has no worktree**, so "worktrees eliminate the cross-session risk"
  holds for subagents only. Blast radii differ — `clean -f/-d` is the outlier: it kills UNTRACKED files.
- **`id:9979`** — narrowed: the prose false-positive is really "**heredocs (`<<`) disable
  tokenisation**, and only then does the raw fallback match prose". Much smaller fix than first filed.
- **`id:b818`** — RECURRED the same day in a new test; the fix was file-local. Wants a **shared
  enumerator** so "prune nested checkouts" is inherited, not remembered.
- **`id:7511`** — `relay-events.jsonl` is INCOMPLETE (terminal handbacks with no `dispatch` event; a
  completed unit with no `integrate` event). **Never infer "this did not happen" from an absent event.**
  It already produced two wrong classifiers before anyone noticed.
- **`id:10dc` / `c3c1`** — delegated agents die at ~175k with **58.6k of preamble before the first tool
  call** (34%). Custom agent definitions replace the system prompt AND narrow tool defs; infrastructure
  landed, the measurement is still owed.
- **`id:a726`(d)** — `git-lock-push.sh:241` resolves the pull target from `@{upstream}` regardless of
  `--remote`. Not live here; needs its own fix.
- **`id:299c`(b)** wiring is REPORTED, not made: `relay-loop.js` between `:4224` and `:4225`. **The
  `mechanical-proxy.py` allowlist entry must land in the SAME change as the fence**, then restart the
  proxy (the frozenset binds at import).

## Traps this session paid for

1. **`$?` after a pipe is the LAST command's status** — produced a confidently wrong "exit 0" claim.
2. **`git config --get-all credential.helper` misses the per-URL key** `credential.https://github.com.helper`.
3. **`git diff m^1 m` overstates merge exposure** — use `git diff-tree --cc` for lines in neither parent.
4. **A too-literal `grep` looks exactly like a dropped fix.** Twice reported `0` for a change that was present.
5. **Agents stall waiting on their own background jobs.** Brief them: *commit before running the suite;
   never end a turn waiting on your own background job.*
6. **Worktrees prevent clobbering, NOT merge conflicts.** Partition by FILE OWNERSHIP — one owner per
   file. Held across 12 branches; the single conflict came from a branch that forked before a merge.
7. **A dead agent never commits.** Check `git -C .claude/worktrees/agent-<id> status --porcelain`
   before assuming an agent produced nothing.

## Method notes that earned their keep

- **Every confident causal story was wrong** — stale proxy, credentials, ledger bloat, the
  execute/review split, "briefs not tier". Each was settled only by running a command.
- **Delegate bulky evidence.** The transcript analysis behind `id:10dc` kept ~500 KB of JSONL out of
  the parent and produced better numbers than reading it would have.
- **Run against real data, not structure.** Both `id:7511` instances, and the two classifiers they
  broke, surfaced only because agents tested against the real event log.
- **An unreached fixture is not a passing negative control.** Two agents caught their own contaminated
  controls — one where a flat copy made a library unresolvable, so a probe errored and rendered as a
  false DENY.
- **Before filing "X was never verified", grep the ledger.** `id:299c` was filed on a false premise;
  the finding existed and had even predicted its own neglect.
