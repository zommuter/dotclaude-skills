# Handover — 2026-08-22 afternoon session

**Session:** `e6a00aeb-2a37-4b22-8c4f-3ed9fa9c5213` · started from `docs/HANDOVER-2026-08-22.md` (the morning one — this file does NOT replace it)
**`main`:** `eec50b2` · **suite 489 passed / 0 failed / 1 expected-red** · pushed to `origin` (fievel), ~~**`github` is 72 commits behind by design**~~

> **CORRECTION 2026-08-22 15:xx (id:f66e) — the struck claim above was FALSE, and acting on it
> would mislead.** There was no "by design" hold and no mechanism capable of holding: at 14:40
> that same day a parallel session's routine post-prompt commit pushed **77 commits** to public
> GitHub, because `git-lock-push.sh` pushes to EVERY remote when `--remote` is absent and
> `git-diary-workflow` — which runs after every prompt in every session — never passed it. The
> "hold" existed only as this line and a 2026-07-21 `RELAY_STATUS.md` note ("awaiting owner OK to
> publish to public GitHub") that was a one-time gate for one batch, never a standing policy.
> Nothing read either. **Now fixed:** the owner ruled publication DELIBERATE; the diary skill
> narrows to `--remote origin` and `tests/test_diary_push_remote_narrowed_f66e.sh` enforces it.
> Do not restore the struck claim.
**Load context:** an unrelated `duperemove` saturated the disk all session (load 17–22). Every timing observation here carries that caveat.

> **This file follows `id:749d`/D11 deliberately: REFERENCE, DON'T DUPLICATE.** Every item below cites an `id:XXXX` and stops. The ledger is the source of truth; if this disagrees with it, the ledger wins. Resolve an id with `grep -n 'id:XXXX' TODO.md ROADMAP.md` — do NOT read either file whole (`TODO.md` is 563+ open boxes; that is `id:35b7`'s prompt-size trap).

## Suggested skills for the next session

- `/relay --afk` — the pool is unblocked and loderite is verified dispatchable. Read the caveats below first.
- `/relay human` — 1 open `REVIEW_ME` box, 4 inbox items (below).
- `/meeting --fabled` — **two** passes are now the demonstrated norm on a substantive design; see "what the second pass bought".
- NOT `/relay handoff` — every open item below is already specified.

## State you can verify in one command each

| | |
|---|---|
| inbox | **4 open** (refilled after I drained it; 3 are a Lean-toolchain chain — see below) |
| `REVIEW_ME.md` | 1 open box (`id:8e7a`, device work, correctly human-gated) |
| worktrees | **23** leftover agent/relay worktrees — `id:b818` hermeticity hazard, prune before trusting a sweep |
| live runs | `discovery-producer` only (no pool) |
| `roadmap-lint` | 6 WARNs = the known baseline, not new |

## The one thing to know before dispatching a pool

**The bump-policy fleet default is LIVE.** The next pool run bumps ~46 manifest repos on integrate rather than handing back three. That is ratified (`id:65ad`), and the owner's reasoning is recorded in `relay/references/conventions.md` §Semver bump trigger — he *contests* the 2026-07-17-1541 D1 premise rather than merely overriding it: a refactor asserts a functional identity it cannot guarantee, so a bump is the honest signal; formally-verified code is the carve-out. **Do not "helpfully" restore the old rule.** The unautomated residue is `id:0832` (the `zkm` parent-bump → 16 plugin `uv.lock` cascade).

**`id:d51f(a)` — the writer-side enum validation — is NOT built**, and the reader-side half shipped without it. `relay-state-write.sh toml-set` still accepts any `bump_policy` value silently. That was ratified as the *primary* guard; it is the highest-value small item open.

## What shipped (all merged, verified centrally, pushed to fievel)

| id | what |
|---|---|
| `a360` | loderite starvation — orphan bound to its item by COMMIT MESSAGE, not branch name. Fixed + independently reviewed. **Verified: loderite now dispatches `6612`.** |
| `1171` | the residual half — commit-message fallback still ran a bare-token grep, so a prose mention of an id decided a real suppression. Anchored to the marker via the shared `typed_edges_own_id_of_line`. |
| `65ad` + `d51f(b)` | fleet `bump_policy = minor`; three-state reader (absent / parsed / present-but-unparsed); warn-and-default on an unrecognised value. |
| — | loderite-shaped end-to-end regression test; `human.md` per-remote push fix; 3 inbox items ingested. |

## Open, shaped, and NOT slipping

All filed with full context — read the id, do not re-derive:

- `id:d51f(a)` writer-side enum validation · `id:0832` no-bump / zkm cascade · `id:915b` a vacuous assertion + unpinned gate ordering
- `id:3412` transcript analyzer (**gap distribution FIRST**, per owner) · `id:22f2` quota probe for `w` · `id:c3c1` prove-the-shrink probe · `id:a026` tier mix · `id:10dc` (measurement discharged)
- `id:749d` handoff skill · `id:6279` resume · `id:002a` SessionEnd sentinel · `id:5284`/`id:7e9a` the gated snapshot writer
- `id:5beb` meeting-guard defect · `id:9c4c` `relay-burn.sh` reset mis-detection · `id:0842` SIGPIPE noise on the privacy gate
- `id:cece` prior art — **partially** discharged (handoff only; `wayfinder`, `grill-me` and the two-philosophy install model unexamined)

Design record: `docs/meeting-notes/2026-08-22-1048-handover-resume-and-cache-keepwarm-ping.md` (D1–D14, both `--fabled` passes, 23 findings).

## Inbox — 4 open, and 3 are one chain

`routed:4f85` and its siblings form a **Lean-toolchain derivation chain** spanning toesnail → relay-core → lodelore, with an owner decision that **lodelore is becoming the fleet's Lean derivation source**. That is cross-repo and sequenced; do not file the pieces independently without reading all four. The fourth (`relay-doctor` check 13 is blind beyond two repos) is standalone.

## Method notes that earned their keep — these are the reusable part

1. **The second `--fabled` pass refuted the first amendment round's centrepiece.** Pass 1 gave 12 findings; the owner amended; pass 2 gave 11 more and killed the amended `D1`. If a decision set changes materially after a critique, **the critique no longer applies to it**. Delta-scope the second pass; it is cheaper and sharper.
2. **Five doc-vs-code drifts in one session**, each a restatement that rotted because nothing tests prose: `roadmap-lint` pointing at a closed `id:49e0`; `human.md`'s blanket no-push (which I obeyed, wrongly); `conventions.md`'s stale bump contract; `id:10dc`'s "measurement still owed"; and a project memory asserting a FIRST-vs-LAST own-id rule that `id:6059` had already dissolved. **Verify a claim about code against the code.**
3. **A green suite is not verification when one agent wrote both the code and the test.** Every fix here was independently reviewed, and every review found something — including three *vacuous assertions* (one `grep -qi 'lean'` matching "internal-c**lean**up"). Ask "could this assertion have failed for the reason it names?"
4. **The measurement you need is often already on disk.** The prompt-cache TTL question had an elaborate proxy-capture design ratified against it; the answer was one field in every transcript (`cache_creation.ephemeral_*`). Look before building an instrument.
5. **An agent that corrects its brief is doing its job.** Two did. Brief them to report contradictions, not to comply.

## Traps this session paid for

- `md-merge.py` validates **all** insert anchors up front — chaining item N+1 onto item N in one payload always fails. Anchor them all on an existing id.
- Quoting a guarded command (or a marker like an `id:` comment) inside a heredoc trips two separate guards: `id:9979` and the `id:6059` two-marker refusal. Build payloads with the Write tool.
- `git commit -- <file> -m "msg"` parses `-m` as a pathspec. Order matters.
- The full suite exceeds the default 2-minute Bash timeout (~3 min at this load). Pass a longer timeout or it dies silently mid-run.
- `refs/heads/relay/*` does **not** match `relay/orphan/…`. Use `relay-reconcile.sh` — hand-rolled sweeps are the `id:4e14` false-clean bug.
- `discover-repo.sh` without `--no-reconcile` is **side-effecting** (`git worktree remove --force`). Check `git worktree list` on the target first; use `--dry-run`.
