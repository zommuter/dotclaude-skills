# Handover -- 2026-09-01 ~22:22

Session `68b7cea1-c90d-4fdf-b60f-1d2b079e14fe`. Successor to `docs/HANDOVER-2026-09-01-1257.md`.

Do **not** read `TODO.md` whole -- it is **1.25 MB**. Resolve an id with
`grep -n 'id:XXXX' TODO.md ROADMAP.md`, then `sed -n` the slice.

State: main clean at `44091a23`, suite **545 passed / 0 failed / 1 expected-red**, everything
pushed to `origin` (private). Public `github` was **published this session** (245 commits,
`b3f2736e..c0138298`, owner-authorised on a measured delta) and is now 41 behind again.

---

## 1. TOP PRIORITY FOR THE NEXT SESSION: hold the ledger line-shrink `/meeting`

**Ratify the FORMAT before anything is built.** This is `id:0d7c` (filed this session), which
consolidates loderite's `routed:04cf` with `id:55f6`. The direction is already owner-ratified
(BUILD FULL per-item, 2026-08-13, restated 2026-09-01); what is missing is the format, and the
build must not start without it.

**Why a meeting rather than a build ticket:** merging an id's TODO + ROADMAP text into ONE detail
file collapses the single-id-TWO-views model (meeting D2), and **~48 files under `relay/scripts` +
`meeting/` read these ledgers**. That is a fleet-wide contract change, not a formatting one.

**Settle at least these, none of which the 2026-08-13 ruling covers:**

1. **The REVIEW_ME conflict, which is a live contradiction between two owner rulings made the same
   day.** `04cf` proposes merging an id's ROADMAP + TODO + **REVIEW_ME** text into one detail file.
   On 2026-09-01 the owner ruled `REVIEW_ME.md` **OUT** of the per-item tree (57 entries, only 2
   keyed). Both cannot stand as written -- either REVIEW_ME joins after all, or the merge covers
   TODO+ROADMAP only. **Do not let a build pick one silently.**
2. **The hard constraint** (non-negotiable, already cost two loderite executors): the LANE TAG, the
   anchored ID and EVERY gate marker (`gated-on:`, the construction-sign glyph, `@manual`,
   `@owner-gated`) MUST stay on the item LINE. Every detector greps the line, so a marker that moves
   into the detail file makes the item invisible to dispatch -- the `id:d35a` silent-no-op class.
   ONLY PROSE MOVES.
3. **The RATCHET** (owner requirement, 2026-09-01): the TODO/ROADMAP validator must **REJECT any
   modification that re-bloats the ledgers**. This is not a follow-up, it is half the value -- a
   one-time compression regrows. Natural home is `relay/scripts/todo-conformance.sh` + a pre-commit
   hook shaped like `hooks/pre-commit-lane-vocab.sh`: over-budget staged line BLOCKS, pre-existing
   long lines grandfathered so it lands without a migration, `*.archive.md` carved out per `id:2065`.
4. **Open, from `04cf`:** what happens to a CLOSED item's detail file (archive alongside, or leave);
   how `md-merge.py update-ids` addresses a body no longer on the line (it is the flock'd write path
   for EVERY ledger edit, so this is load-bearing); whether per-item files improve concurrency
   (per-file flocks instead of one huge shared ledger -- probably yes, and it would partly dissolve
   `id:dc5b`).

**Do not greenfield it.** loderite already carries `tools/extract-roadmap-notes.mjs` (16 KB) and 109
files in `docs/roadmap-notes/`, and that tool performs exactly the relocate-body-leave-pointer move.
Pushing its `--min-chars` down is the cheap 90%.

**The measurement, two repos:** loderite ROADMAP+TODO ~767k chars, a Sonnet child dead with
`Prompt is too long`, FOUR such failures in `RELAY_LOG`; 133+193 open items at ~160 chars would be
~52k, about **94% smaller**. Here: `TODO.md` 1.25 MB, 66.4% of a review prompt, and **95.5% of bytes
on HEAD LINES** -- which is exactly what line-shrink moves.

**Trap:** `id:dd59` records *"splitting TODO.md is DEAD ON ARRIVAL ... at most 4.5% is movable"*.
That measured a DIFFERENT split (moving CONTINUATION lines; 72.9% of items have none). It does NOT
evaluate this one, which moves head-line prose. The number that kills one recommends the other.
Both items now carry the cross-reference; do not let the DOA phrasing close `0d7c` or `55f6`.

`id:55f6`'s other four sub-questions were answered 2026-09-01 and are recorded there: parked-ness ->
frontmatter is truth with the rendered view deriving sections; REVIEW_ME -> out of the tree for now;
indented sub-bullets -> promote the 11 then lint the construct out; `_chrome.md` -> accept but lint
so it cannot grow.

---

## 2. In flight at handover time (two background agents)

- **`worktree-agent-a55f1852cf6a622fd`** -- reconcile park/reap symmetry fix. Round-2 review found
  the "report from the outcome" fix had been applied to the REAP loop only; the PARK loop still
  swallows the helper's exit and its existence proxy was DEFEATED via an `ORPHAN-COLLISION`. Fix in
  progress; **check it made the two loops symmetric rather than patching one.**
- **`worktree-agent-a77e325feeb8be74a`** -- round-4 adversarial review of the `id:a290`
  merged-gitlink predicate.

Neither is merged. Both branches exist; verify state before assuming either landed.

---

## 3. Owner decisions this session -- do NOT re-litigate

- **`id:5c05`** -- IMPLEMENT `--intensive` prioritisation. **This OVERRODE my close-won't-fix
  recommendation**; the measurement stands as fact but did not carry the decision. Merged.
- **`id:c81b`** -- (D) fresh ids now, (A) pre-registered at ~15 links-on-colliding. EXECUTED: `3346`
  -> `113e`, archived `c3f7` -> `4283`, the S3 edge FILED, class-A 7 -> 6.
- **`id:9fa2`** -- ratify the mirror convention but NARROW to the repo PAIR. Merged.
- **`id:a290`** -- shape (b) hatch built; then **merged and immediately de-fanged to OPT-IN** after
  review; then predicate re-based on option (B), merged gitlink history. Reconcile `.gitmodules`
  skip: REVERSE. Park path: **do not plan a park the helper cannot complete** (surface instead).
- **`id:535d`** -- keep open for the header anchor.
- **Publishing** -- push on a measured delta (the push added only ruled-provenance UUIDs plus one
  known-noise hit; all genuine identity exposure was already public and unchanged).
- **Ratifications** -- batch-review a sample, then bulk-resolve. **STILL OUTSTANDING**: 13 pending,
  all verified LANDED. The four sample entries are named in `TODO.md`; if only two are checked, make
  them the two REVIEW rows, because one of them ratifies `8302` **clause (a)** while clause (b)
  shipped unimplemented behind a green suite (refiled as `59c5`).

---

## 4. Corrections made to my own claims -- the pattern is the point

Five stale or false premises were found and fixed this session, three of them mine:

- **My `4425` test's own failures were being SWALLOWED as EXPECTED-RED.** `run-tests.sh:170` takes
  `head -1` of a WHOLE-FILE grep for a roadmap token; the literal in my docstring made run-tests read
  it as the red spec of unticked `6217`. A test written to stop a false TICK-READY claim had its own
  failures silently swallowed. Fixed by constructing every marker at runtime. **Two other files still
  carry this misdetection** (`test_orphan_scan_shipped.sh`,
  `test_orphan_scan_language_dispatch_15f3.sh`), and the deeper defect -- whole-file roadmap
  detection -- is unfixed; header-scoping it means changing `run-tests.sh`, the lint and the runner
  together.
- **My `truncocraft` "census omits a repo" finding was WRONG.** `~/src/truncocraft` is a SYMLINK to
  loderite from the 2026-07-01 rename; I counted loderite twice through a `~/src/*` glob -- the exact
  glob the repo bans in favour of `relay.toml` (`id:7633`). The ban is not ceremonial.
- **My `0968` "zero fleet exposure" survey could not have found the shapes it claimed to clear.** It
  looked only for DIRTY unmerged worktrees, and the review measured three more permanent shapes
  (locked, stray directory, renamed branch). Re-surveyed properly: still zero. The answer survived;
  the method was inadequate. Those are different states.
- **`c81b`'s "the yinyang-puzzle leg is a phantom"** -- FALSE. yinyang-puzzle owns `c3f7` on an OPEN
  item at its `TODO.md:25`.
- **`55f6`'s "cartulary `id:09f9` is still unratified, do not pre-empt"** -- FALSE. `09f9` is `[x]`
  at `cartulary/TODO.archive.md:17`, settled 2026-08-12. That was the THIRD stale premise inside
  `55f6`'s own text.

---

## 5. Landed this session

`4425` (orphan-scan TICK-READY: 2 -> 0 false positives) - `5c05` - `6958` (S9 archive migration
complete; `lane-delimiter-scan --live-only` now returns nothing across all five ledgers) - `9fa2` -
`a73c` (negative-case runner + **123 back-filled declarations**, coverage 3 -> 126 verifiable) -
`c81b` execution - `a290` hatch (opt-in, inert).

Filed: `cce9` (lane-scan blind to tags behind a provenance bracket), `0968` (park path starves ANY
unretirable worktree), `167f` (verify-negative-cases teardown race), `0d7c` (the consolidation
above).

---

## 6. Traps for the next session

- **The recurring blind spot on `reconcile-repo.sh`: a fix gets applied to the path the tests cover,
  and the review finds the same defect on the path they do not.** Three rounds running. When fixing
  anything there, ask which loop you did NOT touch.
- **`make verify-negatives` has a ~1-in-14 teardown race (`id:167f`)** -- `OSError: Directory not
  empty` on the scratch `.git`. RE-RUN any violation before believing it. My first hypothesis (push
  contention) is REFUTED; cause unestablished.
- **A `# fails-against-assertion:` must match exactly ONE assertion site** or the runner rejects it
  as a CONFIG ERROR. That guard exists because a substring appearing in two FAIL messages produced a
  false pass -- `id:a73c` instance (a) reported green.
- **Never Read `relay-loop.js`** (~77,400 tok). Targeted `grep -n` + `sed -n` only. Four agents died
  on it before this session.
- **`~/src/*` globs double-count renamed repos through their compatibility symlinks.** Use
  `relay.toml`.
- **`meeting/md-merge.py`: do not batch a rename with an append keyed on the NEW id** -- the append
  target does not exist at read time, the call fails, and you get one view renamed and the other not.
- **Ledger prose must not quote marker syntax literally** -- the `id:6059` guard refuses a line
  carrying two anchored ids, and a literal `# roadmap:XXXX` anywhere in a test file makes
  `run-tests.sh` treat it as that item's red spec.
