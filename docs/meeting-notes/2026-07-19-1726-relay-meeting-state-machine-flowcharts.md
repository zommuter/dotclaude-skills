# 2026-07-19 — State-machine / flowchart set for `/relay` and `/meeting` (id:a17a)

**Started:** 2026-07-19 17:26
**Session:** 81468158-ecce-4daf-880a-9b5513f5d713
**Attendees:** 🏗️ Archie (architect), 😈 Riku (devil's advocate), ✂️ Petra (productivity), 🎛️ Orla (multi-agent orchestration — relay fleet, new), ⚙️ Sage (skill-runtime — diagram-sync mechanics, new)
**Fable consultation:** 4 Fable-5 agents consulted (per user directive "consult a Fable agent at every meaningful step"): (1) feedback on the owner's summary + meeting framing; (2) dc5b architecture second opinion (code-read); (3) `/relay parallel` design safety; (4) `[MECHANICAL]` substrate impact on prior decisions (16-tool-use deep read).
**Topic:** Author + keep-in-sync a state-machine / flowchart set for `/meeting` and `/relay` (id:a17a), correcting the owner's mental model along the way.

## Surfaced context
- **This meeting IS the a17a meeting** — id:a17a ("State-diagrams for `/relay` and `/meeting`, kept in sync") is `[HARD — meeting]` and the designated UML venue (id:a6e1). Not greenfield.
- **id:4da4 pt2 already produced a machine-readable source** — a file:line artifact×actor×transition matrix + invariants I1–I9 (`docs/meeting-notes/2026-07-01-2142-relay-state-machine-invalid-state-detector.md`). Diagram (b) reconciles with it, not redraw.
- **id:dc5b** (`[INPUT — meeting]`, gated-on:owner-ratification) was literally waiting for this meeting.

## Agenda
1. What is the artifact — one flowchart or a split set? And the sync strategy (the "hard part").
2. Ratify id:dc5b (execute+review same-repo ledger collision).
3. `/relay parallel` — single-repo drain-loop with parallel executors (owner wish → file it).
4. Corrections to the owner's mental model (semver/changelog at integrate; the `[MECHANICAL]` substrate).
5. (added) Does `[MECHANICAL]` change the earlier decisions? `--drain` stopping contract; REVIEW_ME generation-side (2e5c).
6. (added) Connect the REVIEW_ME generation gate to the git pre-commit substrate (7a05) — amendment.

## Owner's summary — verified & corrected
The owner's mental model was ~80% right but a year stale on the `/meeting`↔relay seam and omitted the whole mechanical substrate. Load-bearing corrections (Fable independently reached the same):

- **"handoff lanes all unlaned items automatically"** → the *opposite* is enforced. Auto-laning with a guessed lane is banned; `unpromoted-scan.sh` labels untagged items `surface` ("never auto-promote with a guessed lane"); an unlaned `[HARD]` is a LOUD nonzero reject (id:78ff). Lane assignment is surfaced judgment.
- **"meeting↔relay pooling/lanes not fleshed out"** → it is the *most-specified* part of meeting/SKILL.md (advisory claim id:672b; peek-and-warn flock'd `md-merge --commit` write-back id:c144/148b; POOL/HANDS excluded from meeting buckets; relay-ready nudge D4; REVIEW_ME→synthetic C3 D5; C1-under-executor-contract D7).
- **"need to track what works where / blocks/claims"** → already built (claim.sh leases id:0902/ebfb; verify-isolation.sh; worktree-retire.sh id:373e; orphan-park to `relay/orphan/*` + `/relay reconcile`).
- **"review maybe up-semvers and tags"** → correct *for semver repos*, with three refinements: it's the **reviewer/integrator** (never the executor), keyed to a **user-observable close** by judgment (not per-commit), and includes a **CHANGELOG** entry (id:e647/b8fa, landed 2026-07-18). THIS repo is the exception — constitutionally versionless (id:8ef3, git is SSOT). Diagram (b) needs a conditional version/changelog side-branch on the integrate transition, gated on repo-class.
- **"reconcile, doctor — housekeeping"** → subcommand is `health` not doctor; `reconcile` is the deliberate human consume-side of orphan-park ("refs ARE the registry"), not housekeeping. Summary also omitted five real entry points: `next`, `inject`, `stop`/`--once`, single-repo `/relay .`.
- **OMITTED ENTIRELY: the `[MECHANICAL]` substrate.** The relay has **three execution substrates**, not one:

| Substrate | Runs where | Handles | Components |
|---|---|---|---|
| LLM Workflow pool | `relay-loop.js` (Workflow) | execute(Sonnet) / review·hard·handoff(Opus apex) verdict classes; self-feeding discover→dispatch→integrate loop | claim.sh, worktrees, ckpt-tag, integrator |
| Mechanical daemon | **systemd timer, OUTSIDE the Workflow** (2ec4: no non-LLM dispatch in-sandbox) | `[MECHANICAL]` lane — deterministic no-LLM work | `mechanical-daemon.sh`, `discover-repos-mechanical.sh`, `classify-verdict.sh`, `mechanical-orphan-scan/draft.sh`, `recipe-validate.sh`; `.timer` units |
| Human | interactive `/relay human` | `[INPUT]`/`@manual`/REVIEW_ME judgment | gather-human-backlog.sh, tiers a/b/c |

Lane model is **capability-keyed: `[ROUTINE]` / `[HARD]` / `[INPUT]` / `[MECHANICAL]`, each × `[INTENSIVE]`** (not `[ROUTINE]/[MEETING]`). `[HARD]` sub-lanes `— pool | meeting | hands`.

**Got right:** TODO regex/lane/id model (`append.sh new-id`); `[x]` archive (`archive-done.sh`); execute-in-worktree + handback; executor gaming-check; C1/C2/C3 classification; broker.py for meeting-rpg; end-of-meeting memory/discovery/CLAUDE.md harvest. And the instincts that `--afk` autonomy "leaves much to be desired" and REVIEW_ME gets spammed are both real, tracked (fa5c/9d06; 2e5c).

## Discussion (condensed)

**Item 1 (artifact + sync).** Archie/Orla: `/relay` and `/meeting` share *data* not control-flow → three linked diagrams: (a) id/ledger lifecycle (TODO↔ROADMAP↔REVIEW_ME↔inbox, single-id-two-views), (b) relay dispatch state machine (all three substrates + `[MECHANICAL]` + conditional version/changelog branch), (c) meeting classification tree (broker as a conditional sub-branch, not its own diagram). Connect at two seams: meeting *writes* ledgers (a); meeting C1/relay-nudge *feeds* (b). Riku: the real risk is drift — name the sync mechanism or it's gold-plating. Sage: full generation from relay-loop.js is a research project (2ec4 sandbox un-runnable from bash); the tractable option is a **guard test** that the diagram's named states/modes/verdicts match a machine-readable set (SKILL.md mode list + `classify-verdict.sh` enum + the 4da4 matrix), failing `make test` on divergence.

**Item 2 (dc5b).** Fable code-read: the re-chain `queue.push` fires at child-settle (`relay-loop.js:2019`) while `integrate()` is fire-and-forget debt (`:2047`) — a *deterministic* ~1–2 min race, so C4 "gather rate evidence" is wrong. C1's headline risk is smaller than drafted (`:1836` discoverCache invalidation re-dispatches next round → C1 degrades into C2 anyway) BUT C1's real cost is making push-or-release **exhaustive across every `integrate()` exit path** (the id:7570 lease-hold is coupled to the settle-path push; a naive move leaks the lease until TTL) — surgery on the loop's most incident-dense function. Under the owner's m.o. (reliability over latency; dissolve-over-guard; don't add guards to fragile code), **C2 is the better primary**, C3 the eventual dissolution.

**Item 3 (`/relay parallel`).** Fable/Orla: not a new subcommand — **`/relay . --drain [--parallel N]`** on the existing single-repo pool (id:7633), which already bypasses universe-enumeration + 40× discover fan-out (owner's "wastes tokens crawling" premise is partly stale; the residual is one `classify-repo.sh` reconcile = the mechanical predicate reuse he wants). Collision safety is NOT via C3 (correction of an apex over-reach mid-meeting) but via **one-writer-to-main (id:5a39)**: parallel executors produce code+reports only; the single driver holds the lease, merges `--no-ff` serially, ticks checkboxes itself. Stopping contract is **already solved by `drain.mjs` (id:d58f)** — its substantive / confirmation-not-progress / blocked distinction *is* "drained or fully human-blocked"; reuse it, don't reinvent. "Trivial overlap" = predict-then-verify fail-closed: greenlight two executors only if declared file-sets (RED-spec / `# roadmap:XXXX` / item text) are disjoint+non-empty, then enforce at merge (diff second worktree's touched paths vs first's merged diff; intersection → handback).

**Item 5 (`[MECHANICAL]` impact).** Fable 16-read verdicts: **dc5b/C2 STANDING** — every mechanical script writes only under `~/.config/relay` (never repo ledgers), purity-tested read-only; not a fourth ledger-writer. **`--drain` NEEDS-AMENDMENT** — real hole: `mechanical-daemon.sh:110` runs `bash -c "$cmd_str"` where a recipe `cd <repo> && … > results/latest.json` writes an artifact **into the repo main checkout**, and the daemon holds **no repo lease** (`:31`), so a timer firing mid-drain dirties the tree the driver is merging (fails safe — `drain.mjs:65` parks on dirty main — but stalls). Fix: add `claim.sh peek <repo>` → leave-recipe-in-`pending/` to the daemon's existing host/intensity defer gate. Latent sibling: `recipe-validate.sh` checks shape only, not that the cmd stays off tracked/ledger paths (trust, not enforcement).

**Item 6 (REVIEW_ME generation gate + pre-commit amendment).** Grep found the gate is NOT greenfield: id:07dc (box hygiene — prune/shape/"green core creates NO box", `children-of:7a05`, "some checks land as pre-commit hooks"), id:2e5c (semantic freshness — box counts vs live ledger), id:af48 D6 (executor-no-own-RED + reviewer-read-only, "provenance detector rides id:7a05"). Genuinely new = one **clause** with two halves: judgment half → `review.md` contract text; mechanical half (re-derivable-count / lint-resolvable box ban) → registers as a pre-commit check under the existing 7a05 substrate via 2e5c/07dc. Minting a fresh enforcement item would be the NIH pattern id:0e56 exists to catch.

## Decisions

- **D1 (a17a scope):** THREE linked diagrams — (a) id/ledger lifecycle, (b) relay dispatch state machine, (c) meeting classification tree (broker = conditional sub-branch). Diagram (b) MUST show all three execution substrates + the `[MECHANICAL]` lane + the conditional version/changelog side-branch (semver → bump+tag+changelog; versionless → changelog-only), and reconcile with the id:4da4 pt2 matrix (I1–I9) as source-of-truth. *Out of scope:* diagrams for git-diary-workflow/todo-update/discovery-shard this session (a17a lists them "where non-trivial" — judgment per skill, later).
- **D2 (a17a sync):** GUARD TEST — a `make test` check that fails when a diagram's named states/modes/verdicts diverge from the machine-readable set. NOT full generation (2ec4), NOT hand-sync. Candidate consumer of the inflownistration / info-flow idea (id:aae4) — design the guard so it can feed that, don't build it in a way that fights it. *Out of scope:* generating the Mermaid from relay-loop.js.
- **D3 (dc5b ratified):** **C2 now** (hard invariant: never dispatch two units for the same repo in one round; the re-chained execute waits for round N+1 discovery — the class-fix, cheap, durable) + **C3 via id:2840 later** (move checkbox/verdict state out of the non-union ROADMAP.md into the derived index — the structural dissolution). **NOT C1** — its integrate()-surgery becomes vestigial once C3 lands (constraint-archaeology) and it degrades into C2 anyway. C4 (log-and-wait) rejected — deterministic defect, nothing to wait for. *Out of scope:* implementing C3 now (gated on id:2840/dc60/659c).
- **D3b (fa1d amended — false-premise correction):** id:fa1d pre-registered (2026-07-17) that dc5b be ratified *together with* id:d03d because *"d03d's real-remotes mechanism may DISSOLVE dc5b."* Fable's 4th consultation showed that premise is **analytically false for ledger state**: dc5b's collision is a *merge-time* collision on the non-union `ROADMAP.md` (the observed incident already had the execute in its own worktree conflicting at `integrate()`), and the integrator's three-way merge is **byte-identical under real-remotes** — d03d dissolves *unmediated writes to the same checkout* (a different, source-tree class), and an execute+review pair usually conflicts on the **same item's line** (semantic, unmergeable in any topology). Only C3 dissolves it. Per the owner's own pre-registration corollary (*"a false factual premise is amended explicitly, not reinterpreted"*), **fa1d is amended: dc5b is un-blocked from the joint meeting and ratified here (C2).** fa1d's remaining scope = d03d write-scoping + the parallel-executor wish (D4) + C3 sequencing, which keep their own joint meeting. Fable's stated weaknesses (preempts the id:1735 n=1 window; C2 fixes the class but not the underlying re-chain-before-integrate ordering bug C1 addresses) were weighed and accepted by the owner.
- **D4 (`/relay . --drain [--parallel N]`, id:93fe):** file as `[HARD — meeting]` design item — a flag on the existing single-repo pool (id:7633), NOT a new subcommand. Reuses `drain.mjs` (id:d58f) for the stopping contract and one-writer-to-main (id:5a39) for parallel-executor safety. Three mandatory guards: (i) no-progress via drain.mjs + MAX_ROUNDS; (ii) REVIEW_ME anti-spam baked into the loop's review-agent contract (the owner's "don't you dare spam" instruction, mechanized — see D7); (iii) mechanical fail-closed disjoint-path greenlight + merge-time path-intersection enforcement. *Out of scope:* building it now (dedicated design meeting).
- **D5 (daemon peek-and-defer, id:0534):** file companion item — `mechanical-daemon.sh` must `claim.sh peek <repo>` → leave recipe in `pending/` before running a recipe that writes into a repo checkout (closes the D4 dirty-tree hole). Note the `recipe-validate.sh` shape-only-not-path-scope latent gap. Lane `[HARD — pool]`. *Out of scope:* the broader recipe-cmd path-sandbox enforcement (latent, separate).
- **D6 (`/meeting --fabled`, id:7e87):** file `[HARD — meeting]` — mechanize this session's Fable-assisted flow (consult a Fable second-opinion agent at meaningful decision points; apex remains the decision-maker, Fable advisory per [[feedback-fable-optional-not-gate]]). Weigh the per-prompt ctx-budget multiplier (mandatory-step rule) and whether it's a flag, a mode, or opt-in.
- **D7 (REVIEW_ME generation gate, id:c17c + amendment):** ADOPT SPLIT — (a) fail-closed *contract clause* added to `review.md` + the D4 review-agent brief (judgment half): file a REVIEW_ME box ONLY for genuine human-judgment; NEVER for mechanically-re-derivable counts/state, NEVER to re-question an already-decided design without first diffing the ratified source, NEVER for anything an auto-answer/lint resolves. (b) mechanical half FOLDS into id:2e5c + id:07dc under the id:7a05 pre-commit substrate — **no new enforcement item minted** (would duplicate 7a05 children; NIH per 0e56). Amendment (supersedes the in-flight "prose-only" framing): per enforce-not-document, the re-derivable-count ban must be a hook check, not a hope.

## Action items
- [ ] Update id:a17a with D1+D2 (3 diagrams, guard-test sync, mechanical substrate + version/changelog conditional in diagram b, reconcile with 4da4 matrix, inflownistration link) — this note. <!-- id:a17a -->
- [ ] Ratify+update id:dc5b to DECIDED (C2 now, C3 via 2840 later; remove gated-on:owner-ratification) in TODO.md AND ROADMAP.md (single-id-two-views) — this note. <!-- id:dc5b -->
- [ ] `/relay . --drain [--parallel N]` single-repo drain-loop with parallel executors — `[HARD — meeting]`; reuse drain.mjs(d58f)+one-writer-to-main(5a39); 3 guards; cross-link 7633/dc5b/93fe home. Contract: what a future design meeting must produce (flag shape, disjoint-path greenlight mechanism, review-agent anti-spam brief). <!-- id:93fe -->
- [ ] `mechanical-daemon.sh` repo-lease peek-and-defer before writing a recipe artifact into a repo checkout (`claim.sh peek <repo>` → leave in `pending/`) — `[HARD — pool]`; note recipe-validate.sh path-scope gap; closes the D4 dirty-tree hole. <!-- id:0534 -->
- [ ] `/meeting --fabled` — mechanize the Fable-assisted meeting flow (advisory second-opinion at decision points); weigh ctx-budget + flag-vs-mode. `[HARD — meeting]`. <!-- id:7e87 -->
- [ ] REVIEW_ME generation gate — add the fail-closed clause to `review.md` (judgment half); mechanical half folds into id:2e5c/id:07dc under id:7a05 (no new enforcement item). <!-- id:c17c -->
