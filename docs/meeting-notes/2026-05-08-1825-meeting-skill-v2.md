# 2026-05-08 — Meeting skill v2: filename, identity, personas, portability

**Started:** 2026-05-08 18:25
**Session:** c4c95d83-7c3c-4466-90d3-74b52d3d49f2
**Attendees:** Archie (architect), Riku (devil's advocate), Petra (productivity), **Sage (skill-runtime, re-onboarded)**
**Topic:** Evolve `~/.claude/skills/meeting/` based on five concerns raised after four same-day meetings: filename collision/traceability, skill identity (rename/split), fixed-vs-dynamic personas, the "Tobias" persona's existence as a category error, and name portability for sharing.

## Agenda

1. **Filename + session reference** — add HHMM to filename; embed session/start metadata in note header.
2. **Tobias persona + name portability** — is "Tobias" a redundant persona given AskUserQuestion is the user's real voice? How do we make the skill shareable?
3. **Fixed vs dynamic standing personas** — should all four be fixed? How reliable is onboarding optional personas (Mira, Flora, Sage) across independent meetings?
4. **Skill identity** — rename `/meeting` to `/standup`? Split the audit-and-recommend mode from the run-a-meeting mode?

## Discussion

### Onboarding (re-)Sage

**Sage (re-onboarded — skill-runtime lens):** Skills are prompt directories; SKILL.md governs discovery via frontmatter `description`. The slash form maps directly to skill name. Subcommands inside a skill are arg-based dispatching, not separate registrations.

### Decision 1 — Filename + session reference

**Petra:** Current `YYYY-MM-DD-<slug>.md` collides on the same day for related slugs — four notes from today prove the cluster is real.

**Archie:** Adding HHMM is cheap and unambiguous. Existing notes don't need renaming — date-only is grandfathered.

**Riku:** Session reference was unclear — assumed we'd need to scrape `.jsonl` mtime, which is fragile.

**Sage:** Three layers: HHMM in filename, `**Started:**` header line, `**Session:**` header line.

**Zommuter (correction):** Check the diary skill — it already gets the session ID reliably.

**Sage (revised):** Confirmed. `git-diary-workflow/SKILL.md:23` uses `$CLAUDE_SESSION_ID` directly — plain env var, bash expands it inline. No scraping needed.

**Riku (withdrawn):** Objection was based on file-mtime scraping. With `$CLAUDE_SESSION_ID` it's exact.

**Petra (closing):** Adopt all three layers. Capture via `echo "$CLAUDE_SESSION_ID"` once; embed the literal in Write calls to avoid permission-prompt issues with in-arg expansion.

### Decision 2 — "Tobias" persona + name portability

**Petra:** "Tobias" in prior transcripts records what the real user said. Archie/Riku/Petra are model-voiced; Tobias is the human at the keyboard — a category error.

**Archie:** Two separate questions: (1) label in transcripts; (2) membership in persona table.

**Sage:** Portability collapses them. Options: hardcode, git-config-derived, role-based "User", drop entirely.

**Zommuter:** Drop from list, but derive name from `git config user.name` for transcript attribution.

**Petra (closing):** Standing personas table = Archie/Riku/Petra. Human attribution in transcripts and Attendees line uses `git config user.name`. Document human role separately in `format.md`. Existing notes not retroactively edited.

### Decision 3 — Onboarding reliability for registry personas

**Petra (empirical audit):**

| Meeting | Sage relevant? | Sage onboarded? |
|---|---|---|
| meeting-skill | yes (introduced) | ✓ |
| claude-config-repo-split | yes | ✓ |
| parallel-edit-safety | borderline | ✗ |
| whitelist-skill-permissions | clearly yes | ✗ |

**Sage:** Current SKILL.md is passive — tells the model the option exists but doesn't force a relevance check.

**Archie / Riku:** Options: status quo, explicit relevance pass, topic-keyword index. Keywords go stale; explicit pass is right.

**Zommuter:** Add Sage as a fixed member in the `.claude` project override.

**Petra (closing):** Project-override mechanism is the lever. Create `~/.claude/docs/meeting-notes/meeting-style.md` listing Sage as project-standing. This directly fixes the empirical miss (`whitelist-skill-permissions` was a `.claude` meeting). Format spec already supports adding personas via natural language — no spec change needed; just document it explicitly.

### Decision 4 — Skill identity (rename / split)

**Sage:** "Standup" misdescribes — connotes daily quick syncs, not deep design reviews. `/meeting` is accurate.

**Petra:** Three options: keep merged, split into `/triage` + `/meeting`, rename only.

**Archie:** N=2 for split fails — audit always feeds into a meeting today. Split would break the seamless audit→meeting flow.

**Riku / Archie:** Keep merged. Don't split prophylactically. Current SKILL.md already has clear arg-based conditional structure.

**Zommuter:** Keep `/meeting`, merged.

## Decisions

- **D1 — Filename + session.** New format `YYYY-MM-DD-HHMM-<slug>.md`. Header lines `**Started:** YYYY-MM-DD HH:MM` and `**Session:** <literal-session-id>`. Capture both via Bash once at meeting start; embed literals in Write. Existing date-only notes grandfathered. *Out of scope:* retroactive renames; Stop-hook post-write.
- **D2 — Human attribution.** Drop from standing persona table. Table = Archie/Riku/Petra. Attribution uses `git config user.name`. Human role documented separately in `format.md`. *Out of scope:* retroactively editing existing notes.
- **D3 — Persona surfacing.** No global SKILL.md tightening. Project-override files can add standing personas (symmetric with exclusion). Created `~/.claude/docs/meeting-notes/meeting-style.md` adding Sage as `.claude`-project-standing. *Out of scope:* keyword index, mandatory relevance pass.
- **D4 — Skill identity.** No rename, no split. `/meeting` stays merged. *Out of scope:* `/standup`, `/triage`.

## Action items

- [x] `~/.claude/skills/meeting/format.md` — filename pattern, header lines, persona table, human role section, symmetric override note
- [x] `~/.claude/skills/meeting/personas.md` — clarify onboarding trigger
- [x] `~/.claude/skills/meeting/SKILL.md` — capture step, filename pattern, remove "Tobias" inline references
- [x] `~/.claude/docs/meeting-notes/meeting-style.md` (new) — Sage as project-standing
- [x] `~/.claude/skills/meeting/discoveries.md` — append `$CLAUDE_SESSION_ID` finding
- [x] `~/.claude/TODO.md` — add session-ID permission-prompt investigation item
- [x] `~/.claude/settings.json` — whitelist `echo "$CLAUDE_SESSION_ID"`, `date '+%Y-%m-%d %H:%M'`, `date '+%H%M'`, `git config user.name`
