# 2026-05-14 — Class 1 sweep: stale closures + Write allowlist

**Started:** 2026-05-14 17:13
**Session:** 85112ebc-51ad-4b73-956f-e3de8996e157
**Mode:** Class 2 planning record (no meeting was held — plan-mode output)
**Topic:** Sweep all Class 1 impl-ready TODO items; verify stale entries; implement Write allowlist

## Context

`/meeting` no-arg dispatch. Classified all unchecked TODO items:
- Class 1 (impl-ready): 5 items
- Class 2 (planning-worthy): 2 items
- Class 3 (meeting-worthy): 2 items (`.claude cwd` guard, `/meeting --cross-project`)

Date-triggered and event-triggered items skipped per spec.

Orphan-scan returned 32 cand4 candidates; all verified against TODO.md — no true orphans among action items. One "Optional, deferred" item intentionally untracked.

User selected: "Check all Class 1 items in a planning session and proceed accordingly."

## Plan

For each Class 1 item: read current state → verify implemented/done → close if stale → implement if genuinely open.

Items reviewed:
1. **Add Write allowlist for project meeting notes** — genuinely open; spec clear
2. **Whitelist orphan from 2026-05-10-1545** — stale; diary-workflow confirmed working (AI-8 2026-05-14)
3. **Amend discoveries.md γ-class entry** — stale; correction entry already at line 133 of discoveries.md
4. **Add user-profile entry: "proper-sample-first measurement"** — stale; entry already at user-profile.md lines 443–451
5. **S3 — proj grep subcommand** — stale; already implemented in `~/src/project_manager/src/project_manager/cli.py:145–168`; verified via live run

## Implementation findings

- Items 2–5 were all stale TODOs pointing to work that had already been done in prior sessions without the TODO being closed
- `proj grep` implementation at cli.py:145–168 exactly matches spec: TODO.md + TODO.archive.md, case-insensitive substring, `[ ]` default, `--all` flag, `<project>:<line>` output format
- discoveries.md correction entry at line 133 cites the same meeting note as the stale TODO — append.sh was called during that session
- user-profile.md lines 443–451 have the proper-sample-first measurement entry
- For the Write allowlist: per `2026-05-11-1024-allowlist-path-resolution` discovery, cwd-relative form is the only reliably matched pattern; added `Write(docs/meeting-notes/*.md)` to `~/.claude/settings.json`

## Decisions

- `Write(docs/meeting-notes/*.md)` added to `~/.claude/settings.json` global allowlist (cwd-relative, matches when writing meeting notes from any project root)
- 4 stale TODO items closed as already-done; 1 new closed entry added for S3
- Stale items root cause: work was done mid-session or in prior sessions; `todo-update` skill was not run or did not capture the closure

## Action items

- [ ] **Verify Write allowlist live**: next time `/meeting` runs from a project other than dotclaude-skills (e.g., helferli, zkm), confirm no permission prompt fires on the final `Write(docs/meeting-notes/...)` call. Contract: meeting note written without prompt.
