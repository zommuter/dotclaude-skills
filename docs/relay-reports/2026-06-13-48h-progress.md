# 48h progress — 2026-06-11 → 2026-06-13

What the fables-turn relay accomplished across the repo fleet in the last two days.
**~670 commits across 21 repos.** Generated as a Fable-standin (Opus) report on
2026-06-13 ~11:05.

## Headline

The fables-turn relay went from a validated pilot to a **working autonomous
reviewer/executor pipeline**: the strong turn handed off ~21 repos (docs → roadmap →
red-test specs → BDD), Sonnet executors worked the `[ROUTINE]` items, and the strong
turn audited the result. Today it ran end-to-end **unattended on Opus-as-Fable-standin**,
including two checkpoint+review cycles and a full review pass.

## The relay engine itself — dotclaude-skills (124 commits)

- **Autonomous priority-mixed pool** shipped and pilot-validated (id:1ad7): 5-wide,
  serialized integrator, quota-guarded, verdict-class scheduling.
- **`--fable-down` / `-d` executor-only mode** + **review→execute demotion** (a `review`
  repo with open `[ROUTINE]` work runs on Sonnet when the strong model is down, instead
  of idling).
- **Today's hardening (this session):**
  - Fixed the **stringified-args bug** — the harness delivers Workflow `args` as a JSON
    string, so `-d`/`fableDown` was silently a no-op and dispatched doomed Fable reviews.
  - **Deterministic zkm-plugin path overrides** — discovery was non-deterministic because
    the 12 plugins live at `~/src/zkm/plugins/zkm-*`, not `~/src/<name>`; added `# path:`
    overrides (id:f5d8 hardens it at confirm-time).
  - **`id:c34a` $HOME-leak root cause** — the write-status agent created a literal
    `$HOME/` dir in cwd every run; prompt now resolves via `os.path.expanduser` and
    refuses literal `$HOME`/`~` paths.
  - Captured **id:f649** (unattended self-model-guard default = executor-only) and the
    **review→execute demotion** feature.
- Manually integrated 3 dirty-tree HANDBACKs (zkm-pdf, zkm-social, zkm-scan) and produced
  the cross-repo **REVIEW_ME digest** (sibling file).

## Income repos

- **zkWhale (34)** — gamification-first wave: degen-score curve (log-scale, 1-BTC cap,
  txid dedup), share-badge (tier label, **no BTC amount**), total-attested-balance
  celebration, P2PKH `bip137` signature kind, external-signing paste flow, BitInfoCharts
  percentile tile, snapshot-deploy integrity check. *Blocked: real `DONATION_ADDRESS`
  needed before deploy; Matt's veto (TODO id:613e) pending.*
- **helferli (23)** — OTA-disable spec, opt-in RMS energy gate, connection-eviction
  (one live WS per agent_id), default-on session capture (D6 discipline), replay-diff
  exit-code contract, Tier-3/4 reconsideration rubric.
- **trAIdBTC (54)** — **roadmap fully closed** (id:e4a6 done). Next: the real-data v0 run
  (needs network + hours of compute) — schedule deliberately.

## Core + apps

- **zkm (41)** — gamemode lock guard for convert/scrub/index, amender-sweep scoped to
  created files (+ skip on zero-created for the no-op-mbsync speed win), `zkm doctor`
  `--entities` census + amendment-queue visibility, conformance WARN for amenders missing
  `created`, shared `zkm.session` module, self-scope indexing. Phase-2 "done" = 14-day
  zero-intervention window.
- **isochrone (86)** — 2026-06-12 REVIEW_ME batch ratified (7 confirmed, 1 re-specced);
  best-effort honest-exit generation, directed-outbound POI-matrix distances, chronometro
  web tests.
- **meeting-rpg (64)** — live backlog replay (additive `replay: true` flag, lands on
  tail), desktop typography threshold (≥22px), broker ring + frontend backlog render,
  phone-LAN single-column layout, broker idle self-shutdown.
- **zelegator (19)** — LLM-fallback screening rubric (32 cases, 16 DE / 16 EN), coarse-
  and-auditable by design for the id:462c judge-tier eval.

## zkm plugins (~200 commits across 12 repos — all handed off + executed)

- **zkm-pdf** — encrypted-PDF handling (empty-password imported), `/Keywords`→`tags`,
  text-char threshold (markers excluded), skip-log dedup by (sha, reason, threshold).
- **zkm-scan** — text-layer PDFs skipped even without zkm-pdf, DPI 200→300, observe-only
  `ocr_confidence`, HEIC-without-pillow-heif notice, tz-aware EXIF, pillow-heif locked.
- **zkm-social** — GitHub + LinkedIn entity extraction, source-scoped dedup index,
  rendered-subset refresh change-detection, LinkedIn PDF-save + update-in-place.
- **zkm-calendar** — inclusive all-day display, `status:` for non-confirmed events,
  VALARM reminder phrasing, EXDATE/recurrence-override rendering, calendar-name fallback.
- **zkm-vcard** — FN-from-N fallback, `tel:` entity stripping, X-ABLabel unwrap, PGP-key
  CAS pointer, REV-gated in-place updates.
- **zkm-eml** — reprocess preserves foreign frontmatter keys, In-Reply-To thread fallback,
  `slug_ascii` parameterization, data-URI detach on reprocess.
- **zkm-ner** — scrub type-awareness, ISO-4217 ∪ {BTC,ETH} currency allowlist, lowercase
  IBAN (checksum-gated), invoice-separator requirement, derived plugin version.
- **zkm-claude-ai** — ValueError error contract, corrupt-state graceful degradation, date
  fallback order, alternate-branch annotation, `project:` frontmatter.
- **zkm-claude-code** — compaction-summary skip, latest ai-title wins, image-paste = human
  turn, queued-message rendering, slash-command rendering. (Also: v0.1.1 source_dir fix.)
- **zkm-whatsapp** — lossless rewrite (text duplicated into manifest), owner-jid fallback,
  number-change rendering, `keyring:` keysource via secret-tool.
- **zkm-notmuch** — `RuntimeError` contract (core catches → WARN), `tags_exclude: []`
  semantics, created-scoped batch amendments.
- **zkm-claude-ai / zkm-photo** — PNG IHDR dimensions (no Pillow), HEIC mtime-fallback,
  local-tz EXIF, accepted flat-symlink layout deviation.

## Cross-fleet patterns that solidified

- **Observe-before-preventing** counters/gates landed inert pending evidence (ocr
  confidence, valid:false census, RMS gate) — matches the project design heuristic.
- **FP-minimization** in entity extraction ("wrong entity worse than no entity").
- **Plugin error contract**: raise `RuntimeError`, core's amender loop catches and WARNs.
- **Skip-ledger dedup** keyed by (sha, reason, threshold) across pdf/scan.

## Open / needs you

- **~94 REVIEW_ME boxes** awaiting confirm/correct — see the digest; the genuine forks are
  flagged there (helferli session-log privacy default, zkm Phase-2 definition, etc.).
- **ai-codebench** — live-GPU items (id:244b OOM-interrupted, id:8bea) hardware-gated on
  zomni; local main checkout still needs `pull --ff-only`.
- **trAIdBTC** real-data v0 run; **zkWhale** donation address + Matt veto.
