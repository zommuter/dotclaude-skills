# Handover — 2026-08-28 02:50 (overnight relay pool)

Successor to `HANDOVER-2026-08-27-2330.md` (read that one first for the S1 deferral,
owner-held items, and fleet state at handoff). This file covers ONLY the overnight run.

## 1. What ran

    /relay --intensive --quota-7d 60 --exclude dotclaude-skills

Run id `relay-20260827-233813-24295`. Launched 23:38, drained 02:43 (~3.2 h).
5 rounds, 21 units dispatched, 15 completed + pushed, 46 blocked, 0 queued, 0 in-flight.
Ended on backlog drain, NOT on quota: 7d finished at **39%** against the 60% cap
(5h at 14%). 420 agents, 3.51 M subagent tokens, $0.00 extra credits.

`--exclude dotclaude-skills` was honoured — nothing in this repo was dispatched, so the
em-dash migration seams S0..S10 (`id:70bc` / `id:71d6`) are untouched and S1 remains
yours for an attended session.

## 2. What landed (all pushed)

| repo | modes | ids |
|---|---|---|
| relay-core | hard ×4 | aefd, 5c76, 7690, 9d6e |
| lean4btc | hard ×3 | c4c3, 5472, 9512 |
| git-annex | handoff, hard ×2 | 37da, 2790, 28a9, 49c9, 3d5c |
| it-infra | review ×3, execute | cf06, 7c72, f882, f6d3, 38cf |
| loderite | review, handoff | 551f, ed3a, b5a2, 42b4, d5e5 |

Checkpoint tags `relay-ckpt-20260827-2346` … `relay-ckpt-20260828-0243`.

Pre-launch, the inbox auto-filer wrote and committed 4 dead-letter stubs:
`routed:f4cd` → loderite `id:b5a2`; `routed:6308` → inflownistration `id:9549`;
`routed:55c6` → inflownistration `id:2b63`; `routed:0590` → it-infra `id:04e9`.
Inbox is otherwise clean.

## 3. Needs you — two real handbacks

**(a) `linguistic-universals` — version bump blocked by a gitignored lockfile.**
`integrate.sh` handed back with code 24. `version-bump.sh` bumped
`pyproject.toml` 0.8.0 → 0.9.0 and `uv lock` updated `exp1-universals`, but the commit
failed: **`uv.lock` is listed in that repo's `.gitignore`**, so `git add` refused it and
nothing landed (`landed=false`). This collides head-on with the fleet
bump-includes-lockfile rule — the lock must be tracked for a bump to be committable.
The repo was ALSO reported blocked earlier in the run for a dirty non-lock-only main tree,
so clean that first. Decision is yours: un-ignore `uv.lock` in that repo (my
recommendation, it is what every other manifest repo does) or record a deliberate
exemption. Worktree held at
`~/.cache/relay/worktrees/linguistic-universals/relay-20260827-233813-24295-hard-repo-0`.

**(b) `loderite id:d5ff` (dd4d-S4) — decision gate, and one measured refutation.**
The hard child refused to proceed and handed back with evidence rather than guessing:

1. The Acceptance requires the column set be *demo/stable/preview*, but `stable` is not a
   root in `buildMatrix()` — it arrives with `id:466d` (release channels), which
   `ROADMAP.md:725` records as DEFERRED, trigger not fired. Satisfying S4 literally means
   firing 466d. Your call whether it fires or whether a `channel`-attribute substitution
   is acceptable.
2. S4's INHERITS paragraph assumes exactly ONE `engine → game` import exception
   (`world → game/block-types`). The child measured the real graph off disk: **38 such
   edges, not 1** (full list in the run output / RELAY_STATUS handback text). So the rule
   cannot be switched on with one dated exception. The three ways forward — snapshot all
   38 as a dated ratchet, reclassify offenders, or narrow the rule to reachable subsets —
   are all design decisions, and option 1 would launder the 260 `MODULE_ROLES`
   assignments that `REVIEW_ME.md` currently flags as UNREVIEWED into an enforced
   contract. Do not let a pool pick one of these.

Also from loderite: `id:9d77` (celestial spin) was rejected as already-implemented at
commit `486683b6` with a green 10-case test; what remains is human-only (an owner eyeball
on a regenerated capture, plus an LOD/perf check a relay child structurally cannot make —
`gpu-launch.mjs` fails closed on a software rasteriser). Box left open at
`REVIEW_ME.md:13`. Two prior children reached the same conclusion independently.

## 4. Residue you should know about

- **2 stranded branches** (unmerged, owning run no longer alive, NOT parked). I inspected
  both and left them — disposal is not mine to make:
  - `lean4btc` `relay/relay-20260827-233813-24295-hard-repo-0` @ `bd52e5d` —
    "pin pure_native_agree in the proofs spec + wire proofs->native"
  - `git-annex` `relay/relay-20260827-233813-24295-hard-repo-0` @ `7849a609b2` —
    2 commits ahead of `annex-dotgit`, including `feat(2790)` mechanical audit of
    per-item ok emitters
  Salvage with `relay-reconcile.sh --integrate <branch>` per repo, or park. **0 parked
  orphans** otherwise.
- **Install drift — FIXED after the run.** At launch, `fixture-branch-leak-scan.sh` was
  committed in `relay/scripts/` but missing from `~/.claude/skills/relay/scripts/` (it was
  already listed in the Makefile, just never installed). Warned, launched anyway per the
  id:83c3 amendment, then `make install-relay` after the drain;
  `check-install-drift.sh` now reports OK. Nothing to do.
- **2 agent failures with no handback** (`agent-failures=2`); one is named:
  `[release:relay-core:resource]` failed with "issue with the selected model (bash)".
  Mechanical-tier hop, non-fatal to the run.
- **46 blocked repos**, nearly all `id:365b` circuit-breaker trips (`human`/`mechanical`
  verdict dispatched >3× with an unchanged work_sig) — zkm and its 6 plugins, zkWhale,
  helferli, zelegator, meeting-rpg, isochrone, ai-codebench, trAIdBTC. These are waiting
  on a human, not broken. `lean4btc` also carries an `id:1432` no-work suppression.
- `~/.config/relay/relay.toml` checkpoint watermarks were committed and pushed to
  `fievel:src/relay-config.git` (`20bc060`) — the tidy-up the previous session flagged as
  optional. That repo is clean now.

## 5. Machine state

Shut down with `systemctl poweroff` at the end of this session, per the owner's
standing authorization for this run. `dotclaude-skills` is clean and pushed; no relay
pool is live; no leases or heartbeats are held (teardown ran clean).
