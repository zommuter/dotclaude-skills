# TODO

## Relay

- [ ] Relay: 3 open ROADMAP items, all HARD (id:401c HARD — strong-model audit, recurring; id:3346 HARD gated — sub-agent meeting simulation; id:10c0 HARD gated — rename fables-turn state dirs, promoted to ROADMAP review 2026-06-16 1900). ZERO open ROUTINE items (review 2026-06-16 1900 re-derivation; D2/D3 id:3313/id:1f53 closed + cross-ledger synced). id:414a (Tier B gaming-canary harness) DONE 2026-06-16. DONE 2026-06-15: id:9ed4 (parallel-shard discovery — once-only prelude + N shard classifiers), id:cb50 (RELAY_STATUS write off the pool critical path), id:3ac8 (relay-loop auto-reaps dead-run stale worktrees, cf. id:d187), id:fb75 (resolver pushes unblocked work to the pool via inject.sh), id:6e9d (freed lane pulls injections mid-round, poll-once-on-drain), id:fa05 (gaming-scan.sh mechanical detector), id:dfaf (review.md §2 delegate rewrite), id:3826 (supervisor flag-rate logger + pool-crashing Date fix), id:c8db (relay-state-write.sh F1/F2 input-robustness), id:8c35 (real quota-stop reason / visual feedback). Plus the dispatch-safety cluster (ebfb/c3f7/8d52/d748/baf1/bc9d/5f09/be62/d53c/f9ad/de51) and the anti-gaming meeting batch (id:2909 → fa05/dfaf/3826/414a all shipped). <!-- id:d5e0 -->
- [ ] **Auto-surface prior ratified decisions on a known `id:` before `/relay human` + `/meeting` present it as a fresh choice** — investigate tooling so a relay/meeting prompt that references an existing `id:` first greps `docs/meeting-notes/` + ROADMAP/REVIEW_ME for a recent ratified decision on that id and surfaces it in the options ("this revises/overrides prior decision X"), instead of offering a clean slate. Motivating near-miss 2026-06-17: `/relay human` offered "Playwright harness first" for `mathematical-writing` b490 blind to that *same morning's* 0857-meeting `D-b490-7` (Playwright observe-first); only caught manually at the follow-up `/meeting`'s write-back. Candidate: a pre-prompt hook in `human.md`/`meeting` that runs an id-scoped meeting-note/ROADMAP grep. See `mathematical-writing/docs/meeting-notes/2026-06-17-2114-b490-webview-render-harness.md` (D5) + toesnail memory `surface-prior-decisions-before-offering`. <!-- id:0a1e -->
- [ ] **Close scoop window (ii): ban `git add -A` in relay integrator** — `relay-loop.js` integrator must use scoped `git add` on its own worktree-merge paths only, never `git add -A`. Contract: a concurrent uncommitted ledger edit (from `/meeting` or `/relay human`) is never captured in a pool checkpoint commit. Ships with or before id:c144 (the lease exemption). See `docs/meeting-notes/2026-06-17-0953-k3s-parallelity-coordination-design.md` D2. <!-- id:debf -->
- [ ] **Narrow `hard` lease scope + document the invariant** — update `relay/scripts/claim.sh` header and dispatch-safety reference docs: `hard` lease = code/worktree integration only; ledger-only writes = flock + `orphan-scan --cross-ledger`. Contract: doc change + `make test` green. See `docs/meeting-notes/2026-06-17-0953-k3s-parallelity-coordination-design.md` D2. <!-- id:179e -->
- [ ] **Cross-repo routed action items never land in the target repo's tracking — `/meeting`'s "→ routed to <repo> inbox" is a dead letter** — observed 2026-06-18: toesnail's `/meeting` `docs/meeting-notes/2026-06-18-0729-veq-macro-verify-carrier.md` action item `routed:8587` ("→ routed to `mathematical-writing` inbox: design the `.mw` side of the carrier", fulfilling the earlier `routed:e4df` park) was written ONLY in the toesnail meeting note. **`.mw` had no `8587`/`e4df`/`veq`/`carrier` reference ANYWHERE** (TODO/ROADMAP/REVIEW_ME), and `.mw` has no `inbox` file at all — so the routed item was a dead letter for the gap between the meeting (2026-06-18) and a human noticing it. The `/meeting` skill writes the `routed:<id>` token into the SOURCE repo's note but has no mechanism to (a) create the item in the TARGET repo's ROADMAP/TODO, nor (b) surface "you have N inbound routed items pending ingestion" when the target repo is next worked. Same risk for every `routed:<id>` / `ref:<id>` cross-repo handoff (cf. `dotclaude-skills/TODO.md` already uses `ref:aae4` as a manual guard — that pattern is the workaround, not a fix). **Fix directions:** (1) `/meeting` (and `/relay`), when emitting a `routed:<id>` to another repo, should ALSO append a stub `- [ ] [INBOUND — routed:<id> from <repo>/<note>] <title>` into the target repo's TODO.md (flock'd `md-merge.py`), so the item exists in the target's tracking immediately; OR (2) a cross-repo "inbound routed" scanner (a `scripts/scan-routed.sh` greppable over all `own` repos' meeting-notes for `routed:<id>` whose target repo lacks a matching `id:`/`routed:` line) wired into `/relay` discovery / `/projects` dashboard so dead letters surface. (3) At minimum document the manual ingestion step in `/meeting`'s SKILL.md so the router doesn't assume "routed" = "tracked". This near-miss cost a full design item (`.mw` id:358f) sitting untracked; only caught because the owner manually asked "what about .mw's ROADMAP?". Relates to id:0a1e (auto-surface prior decisions on a known id) — both are "the ledger doesn't know about a decision recorded elsewhere". <!-- id:3947 -->
- [ ] **`loop-hint.sh` swallows the `--afk` nudge after a recent prior run (time-gap heuristic too aggressive)** — observed 2026-06-18: a manual `/relay . .mw --afk all \veq and related` printed NOTHING from `loop-hint.sh` (step 0a), so the "wrap this in `/loop` for unattended resilience" tip — the single most useful hint for someone who *just typed `--afk`* (= "I'm walking away") — was suppressed. Root cause: `loop-hint.sh` decides standalone-vs-loop-tick PURELY by time since the last run (`$STAMP` ≤ `GAP`=2700s → assume loop tick → silent). A manual re-invocation within 45 min of any prior `/relay` (very common during an interactive session) trips the suppression even though `--afk` is an EXPLICIT away-signal that should make the nudge fire. The script never even sees argv — the front door calls it with no args. **Fix direction:** pass the invocation's flags into `loop-hint.sh` (or set an env marker) so an explicit `--afk`/`--allow-intensive` **forces** the tip regardless of the time gap. **Nuance (the hard part, already noted in the script header):** a `/loop 20m /relay --afk` would then re-nag every tick, because a manual `--afk` and a loop-driven `--afk` are otherwise indistinguishable (the built-in `/loop` sets nothing observable). So the real fix needs a reliable in-`/loop` marker (env var the loop runner sets, or a sentinel the front door can detect) to separate "manual --afk" (print) from "loop tick of --afk" (suppress). Until that exists, weigh: (a) force-print on `--afk` and accept re-nag on afk-loops (the script already self-suppresses on plain `/relay` loop ticks, so only afk-loops re-nag), vs (b) shorten/condition `GAP` only when `--afk` is present. Low-severity (cosmetic — the nudge is advisory) but it defeats the entire purpose of the 2026-06-18 "surface it FIRST" directive. See `relay/scripts/loop-hint.sh` + SKILL.md step 0a. <!-- id:888a -->
- [ ] **discover-sig.sh: `upstream` field read without a fetch → origin-behind under-invalidation** — audit id:401c Run 11 (`docs/meeting-notes/2026-06-17-1102-strong-model-audit.md`, finding 3.1a). The sig hashes `HEAD...@{upstream}` WITHOUT fetching, but the shard's SYNC-WITH-ORIGIN guard (id:c3f7) fetches+FF. If origin advances between rounds with the local tracking ref untouched, the sig is byte-identical → cached verdict reused, no fetch/FF until the next sig-moving event. Bounded (single pool run, self-healing) but a genuine under-invalidation — the one hazard class the CLAUDE.md gotcha warns about ("new signal → add to discover-sig.sh's blob"). LOW severity. **Needs a measured decision, not an inline edit** (a fetch-per-sig adds network cost to every discovery round) — weigh fetch-in-sig vs accept-the-bounded-staleness. <!-- id:4348 -->
- [ ] **model-probe.sh: grade arm uses `echo "$output"` (flag/escape swallowing)** — audit id:401c Run 11 (Pass 1 finding). `echo "$output" | grep -qP "$regex"` mis-handles output starting with `-n`/`-e`/`-E` or containing backslashes; `printf '%s\n' "$output"` is robust. Zero impact today (battery goldens are numbers/words) but behaviour-adjacent forward-robustness. LOW severity, [ROUTINE]-sized (one-line change + a battery case with a leading-dash/backslash golden). <!-- id:b9b5 -->

- [ ] **[HARD — strong model] Rename relay state dirs `~/.{config,cache}/fables-turn` → relay naming convention** — legacy "fable's turn" naming; the relay is now Opus-apex/Fable-bonus, so the live state dirs should follow the relay name (`~/.config/relay/` + `~/.cache/relay/`). Scope (measured 2026-06-16): **33 `fables-turn` path refs across 13 executable files** + `.gitignore`, `Makefile`, `.claude/settings.local.json`, and ~25 doc files. **Constraints:** (a) git tags `fable-ckpt-*` stay HISTORICAL — scope is the DIRECTORIES only. (b) live dirs an in-flight pool reads/writes — needs back-compat. (c) run only when NO relay pool is active. **BACK-COMPAT DESIGN RESOLVED 2026-06-17 (Opus apex, via /relay human): symlink-old→new + run-when-idle, symlink kept permanently as the safety net; `relay/scripts/migrate-state-dirs.sh` does idle-precondition guard → mkdir new → mv → replace old dirs with symlinks. UNGATED — now a bounded Opus hard-execute; full acceptance in ROADMAP id:10c0.** <!-- id:10c0 -->
- [ ] **Self-model guard: default to executor-only after timeout in unattended mode** — **SUPERSEDED 2026-06-15 by the Opus-apex pivot (id:da26, f64c28b):** the self-guard (warn+sleep) is removed; the front door now probes Fable once/2h and defaults to Opus apex (Fable a bonus, never a gate). Resolves the item's intent (never dispatch a doomed strong unit; the long-requested auto-probe shipped at spec level) — kept open only until the probe is a tested helper rather than a front-door instruction. Original: user directive 2026-06-13: when on Opus without `-d` AND the strong tier (Fable) is unavailable, the SKILL.md step-0 guard currently does "warn + `sleep 10` + proceed" — but that "proceed" dispatches strong-model review/handoff units on `claude-fable-5`, which fail when Fable is down (observed thrice 2026-06-13; the 3rd was an **attended `handoff` run** — all 5 fable children returned "Fable 5 unavailable" at launch, operator asked the user → Opus pilot of 2/5). The attended case worked correctly (asked), but reinforces two design facts: (i) **handoff has NO executor fallback** — creating the roadmap IS the strong work — so unattended fable-down must DEFER handoff units entirely (the `-d` semantics already say this; verify the autonomous front door honors it), not merely demote review→execute; (ii) auto-probe would have avoided spawning 5 doomed children. The guard should, **after the timeout with no human abort (i.e. unattended), DEFAULT TO EXECUTOR-ONLY** (`fableDown=true`) rather than launching doomed strong-model units OR auto-escalating to expensive Opus reviews (which must stay explicit `--strong-tier opus` opt-in). Design qs to resolve: (a) how does the front door detect "unattended" vs an interactive operator who'd Ctrl-C? (b) does this fold into the deferred Fable auto-probe ([[fable-down-flag]] notes auto-probe deferred — demand now confirmed thrice)? (c) interaction with `--interactive` flag. Conservative default everywhere: never auto-spend Opus, never dispatch a strong unit you expect to fail. <!-- id:f649 -->
- [ ] **Relay discovery: record canonical repo path at confirm-time (deterministic, not default-path-assumed)** — relay.toml assumes each repo lives at `~/src/<name>`, but the 12 zkm-* plugin repos actually live at `~/src/zkm/plugins/zkm-<name>`. With no `# path:` override the discovery LLM agent was NON-DETERMINISTIC: the 2026-06-13 09:38 `-d` run reported them "missing at ~/src/zkm-*" and deferred them; the 09:54 run's agent improvised, found them under `plugins/`, and executed 7 of them. Same prompt, same config, different agent initiative. **Immediate fix applied 2026-06-13:** added explicit `# path: ~/src/zkm/plugins/zkm-<name>` overrides to all 12 plugin blocks in relay.toml. **Durable fix (this item):** when the orchestrator first confirms a repo (handoff/discover), resolve and persist its canonical path via `git -C <candidate> rev-parse --show-toplevel` into the relay.toml block, rather than relying on the `~/src/<name>` default + manual override. Removes the improvisation surface entirely. Also: discover-repos.sh should emit canonical paths, and the discover agent should treat a default-path miss as a hard "surface for path confirmation", never silently improvise OR silently defer. <!-- id:f5d8 -->
- [ ] **Relay write-status agent leaks a literal `${HOME}/` dir into cwd** — the `writeRelayStatus` agent in `relay-loop.js` is told to write to `~/.config/fables-turn/RELAY_STATUS.md` "expand ~ to $HOME", but on the 2026-06-13 `-d` run it created a literal `${HOME}/.config/fables-turn/RELAY_STATUS.md` under the dotclaude-skills repo root (single-quoted `${HOME}` never expanded), polluting the working tree (showed as untracked `${HOME}/`). Same risk for any agent given a `~`/`${HOME}` path while cwd is a repo. Fix: have the write agent resolve `$HOME` itself (or pass an already-expanded absolute path from the script) and assert the target is absolute before writing. Low-severity (cosmetic + dirty-tree noise) but recurs every run whose cwd is a repo. **FIX SHIPPED 2026-06-13 (322ab59):** writeRelayStatus prompt now resolves via `os.path.expanduser`, forbids literal `$HOME`/`${HOME}`/`~` path components, aborts unless resolved path starts with `/`. Verified once (Opus review run wpgjvis34 produced no leak dir). Leave open until a second clean run confirms, then tick. <!-- id:c34a -->
- [ ] **Relay integrator leaves orphan worktrees when a child leaves untracked files** — the serialized integrator's step 5 ran `git worktree remove` (no `--force`), which fails on ANY untracked file in the worktree (e.g. a `uv.lock` a review/execute child generated by running `uv`/tests). Because it's `&&`-chained with `branch -d` and runs after a successful merge+tag+push, the failure silently orphans the worktree AND branch under `~/.cache/fables-turn/worktrees/` (found a helferli review orphan 2026-06-13). **FIX SHIPPED 2026-06-13:** step 5 now uses `worktree remove --force` (safe — the committed branch work is already integrated by that point; --force only discards incidental untracked build artifacts). Verify next multi-review run leaves no orphans, then tick. <!-- id:d187 -->
- [ ] **relay-burn.sh: harden the `report` `date -d "$reset"` awk-shellout against a crafted `resets_at`** — strong-model audit Run 9 (2026-06-16, `docs/meeting-notes/2026-06-16-0928-strong-model-audit.md`, Pass 2) found that `relay-burn.sh report`'s human-table path interpolates the JSONL `reset` field (sourced from `seven_day.resets_at`/`five_hour.resets_at` in `/tmp/claude-usage-cache.json`) directly into a shell command run via awk `getline` (`cmd = "date -d \"" reset "\" +%s …"`). A crafted `resets_at` would be a command-injection seam. **Risk accepted LOW** for now: `resets_at` is provider-controlled API data written by the statusline from `/api/oauth/usage`, and an attacker who can write the cache already has local code execution; the seam is also unexercised by any non-`--json` caller in the relay path. **Fix when convenient:** validate `resets_at` (e.g. `^[0-9T:+\-]+$` guard, or parse the epoch in jq inside `report`'s main jq pass) before any `date -d` shellout. Forward-robustness only. <!-- id:287b -->
- [ ] **Intra-repo parallelism — dispatch >1 unit in the same repo when writes are disjoint** — user 2026-06-15: today the pool parallelizes only ACROSS repos (`POOL_WIDTH` = distinct repos, one unit/repo; the lease `claim.sh` is whole-repo, and the integrator does one `--no-ff` merge + one push per repo per turn). The guardrail already *permits* "within a repo on disjoint paths" but nothing implements it. Motivating use case: **`/relay` scoped to the relay (meta) repo alone, at maximum parallelism** — when dotclaude-skills is the only confirmed repo there's currently nothing to fan out across, so the meta-work serializes. **User's write-disjointness model to validate:** the three relay actors touch shared ledgers in mostly-non-conflicting ways — `REVIEW_ME.md` is *append* by executors, *existing-box edits* by `/relay human`, *read + light tidy* by review; ROADMAP similarly (executors tick `[ROUTINE]` boxes, review re-derives, human/handoff define). So "clearly parallelizable" units (touching disjoint files, or disjoint code paths within one repo) should be safe to run concurrently in the same repo NOW. **Nuance/caveats to design around:** (i) these ledgers are NOT `merge=union` (checkbox `[ ]`↔`[x]` toggles can't union) — concurrent edits still need line-scoped flock'd writes (`meeting/md-merge.py`) and git surfaces residual conflicts (`orphan-scan.sh --cross-ledger`); RELAY_LOG.md *is* union (append-only), so it's the easy case. (ii) integration ordering — N worktrees in one repo all merging to the same branch needs a single-owner serialized merge (the D5/D6 "never two children pushing to the same remote" rule), so the parallelism is in the *work*, not the *push*. (iii) claim granularity must drop from whole-repo to sub-repo (path-set or unit-id) so two same-repo units don't refuse each other's lease. **Phasing:** start with the trivially-disjoint case (units whose file/path sets provably don't intersect — e.g. a doc edit + a test-only change + a script in different dirs), gated by a static path-overlap check; defer less-trivial in-repo parallelism (same-file disjoint hunks, semantic coupling) to a follow-up. **Likely wants a `/meeting`** — non-obvious tradeoffs (worktree-per-unit-within-repo vs single worktree + serialized stages, claim re-granularity, merge ordering, how the discovery classifier emits multiple units per repo and proves their disjointness). Relates to [[parallel-session-state-coordination]] (worktrees scoped to same-repo code edits), id:3558 (worktree-per-session flock'd merge), id:6a3c Axis B (write-contention coupling), and the dispatch-safety cluster. **User comment for the meeting (2026-06-15):** "Why does REVIEW_ME need flock when we're supposedly using worktrees?" — valid challenge: among worktree children flock IS redundant (each child has an isolated working copy; the single-owner serialized merge + git conflict-surfacing handle ordering). The flock'd `md-merge.py` exists for MAIN-CHECKOUT writers that do NOT use a worktree — `/relay human` and `/meeting`/manual edits write REVIEW_ME/ROADMAP/TODO directly in the main checkout, and a live `/meeting` racing a `/relay human` (or the integrator touching main) is a same-directory concurrency hazard. So the rule is: worktree work → serialized merge, NO flock; same-directory work → flock. The earlier framing of this item conflated the two. If intra-repo parallel units ALL ran in their own worktrees, flock is moot for them — which argues for "worktree-per-unit + serialized merge" as the clean design and reserves flock strictly for the non-worktree main-checkout actors. Also: the whole-repo lease is the obvious thing to re-granularize (sub-repo path-set / unit-id leases). <!-- id:ca87 -->
- [ ] **Periodic [HARD] test-integrity sanity-check on a cadence (not just on unaudited commits)** — user 2026-06-16: occasionally a repo should get a dedicated [HARD] sanity check verifying its commits carry red→green justification (test added red → impl change → green), independent of whether there are *new* unaudited commits to review. **Distinct from existing machinery:** `gaming-scan.sh` (mechanical: deleted tests / added skips / removed asserts) and `review.md §2` (resurrection check) only fire inside a `review` unit, i.e. when there ARE unaudited commits since the last ckpt — so a repo that's been quiet (or whose gaming slipped through earlier) is never re-swept. **Likely wants a `/meeting`** (user flagged it). Design qs: (a) cadence — what is "occasionally"? every N reviews / time-since-last-deep-audit (new relay.toml field, distinct from `last_review`) / probabilistic per round / weighted by repo income? (b) scope/depth — re-audit full history, or commits since a `last_deep_audit` marker? how far back? (c) reuse vs new checks — does it re-run gaming-scan §2 over a wider window, or add new checks (each commit pairs a test-change with an impl-change; commit message narrates red→green)? (d) verdict class — a new `audit` verdict or a flavor of `hard`; where it ranks in the scheduler (after review, like `hard`?). (e) anti-gaming the auditor — what's the done-check so the audit itself can't be rubber-stamped. Relates to id:401c (recurring strong-model audit), id:414a (Tier B gaming-canary harness), gaming-scan.sh (id:fa05), review.md §2 (id:dfaf). <!-- id:7b4d -->
- [ ] **Pool busy-loops re-dispatching un-doable HARD units → never drains, burns an Opus child + an inject-take per round** — observed 2026-06-16 (run relay-20260616-063457-22124): after the [ROUTINE] backlog drained, ~15 repos' only open work is [HARD] items that are decision-gated (need a /meeting: isochrone id:b104, zkm-claude-ai id:107, zkm-photo, meeting-rpg) or too-large/cross-repo (zkm-scan id:02bd, zkm-eml id:ff0f). The `hard` classifier emits a unit for each, an Opus child sizes it and correctly HANDS BACK (C5 "don't half-do it"). **Bug:** `runRound()` returns `actionable: actionable.length` (relay-loop.js:1135) = units *dispatched*, not units that *did work*; a handed-back hard unit still counts as actionable, so `dry` resets to 0 every round (line 1159) and the drain-detector (`dry >= 2`) never fires. The loop re-discovers the SAME gated-hard set every round and spins to the `MAX_ROUNDS=30` seatbelt (or quota), re-spawning an Opus child per gated repo per round to re-derive the identical handback, and running one `inject.sh take` per round in the discovery prelude (id:9ed4) on an empty queue — the "mostly nothing + many inject-take dispatches" symptom. Real work still ships (7 checkpoints that run), but most rounds are pure churn. **Fix options:** (a) count only units that produced a checkpoint (or did NOT hand back) toward `actionable`, so an all-handback round is a dry round; (b) per-run memo: once a repo's top HARD item hands back as gated/too-large, suppress it from re-dispatch for the rest of the run (mark the repo "hard-exhausted this run"); (c) durably mark a decision-gated HARD item (e.g. relay.toml `hard_blocked_until` or a ROADMAP tag) so the classifier skips it until a /meeting clears it — surface in RELAY_STATUS Queued instead. (a)+(b) make the loop drain after ONE pass over the gated-hard set instead of 30; (c) stops re-deriving the gate every run. Likely (a) for the immediate bleed + (c) for the durable fix. **PARTIAL FIX SHIPPED 2026-06-16 (branch relay-observability):** (a) `runRound` now returns `produced` (checkpoints integrated this round) and the outer loop's drain counter keys on `produced===0`, so an all-handback round counts as no-progress → drains after 2 rounds instead of spinning to MAX_ROUNDS=30 (test_relay_loop_structure id:2d20 checks); AND the discovery classifier now applies an EXECUTABLE-HARD test — `[HARD — decision gate]` / under-a-Gated-section / acceptance-needs-/meeting / multi-session items are EXCLUDED from the `hard` verdict + openHard and SURFACED ("HARD backlog is gated — needs /meeting") instead of dispatched, so already-gated repos stop spawning an Opus child per round. **STILL OPEN (durable auto-closure, → meeting id:719e):** option (c) — when a strong child hands back a FRESHLY-discovered `[HARD — strong model]` item as too-large/gated, AUTO-re-tag it to `[HARD — decision gate]` (durable, in the ROADMAP via the integrator's main-checkout write path) so it never gets re-dispatched in a LATER run either. Forks: where the durable mark lives (ROADMAP re-tag vs relay.toml field vs sidecar), how it's invalidated when the item is re-scoped, REPORT-schema field for the child to signal it, and trusting the child's one-shot "too-large" verdict. Until (c) lands, a human (or the /meeting) re-tagging the item to `[HARD — decision gate]` durably stops re-dispatch today via the new classifier exclusion. <!-- id:2d20 -->
- [ ] **MEETING: external rigor frameworks for the relay's test-integrity / quality bar** — user 2026-06-16: mine established quality frameworks for the red→green audit bar that [[id:7b4d]] (periodic sanity-check), review.md §2, and gaming-scan.sh enforce. Sources to evaluate: (1) **GAMP5** — pharma computerized-system validation (risk-based V-model, requirements→spec→verification traceability, "test what matters by risk category"); what maps onto relay's executor-work verification and what's overkill ceremony. (2) **The SQLite testing rules** — among the most rigorous in OSS: 100% MC/DC branch coverage, the TH3 proprietary harness, anomaly/OOM/IO-error fault injection, "every bug becomes a test"; which of these are adoptable as relay audit criteria vs aspirational. (3) **Code coverage** — should relay measure/require coverage on executor work (line/branch/MC/DC), enforce a per-repo threshold or a no-regression ratchet, and feed it into the review verdict / a [HARD] audit? where does the number live (relay.toml? RELAY_LOG?). Output: a concrete, tiered audit bar (cheap-mechanical → judgment → aspirational) feeding 7b4d's done-check and review.md §2. **Tangential-but-feeds:** id:7b4d, id:414a (gaming-canary), id:401c. Likely ONE meeting; split coverage out only if it grows its own tooling sub-thread. <!-- id:80d7 -->
- [ ] **MEETING: Lean Six Sigma / DMAIC lens on the relay's OWN process (waste + defect reduction)** — user 2026-06-16: apply a process-improvement framework to the relay *workflow itself* (distinct from [[id:80d7]] which is about auditing TARGET-repo code quality). Motivating defects already in hand: the busy-loop churn [[id:2d20]] (re-dispatching un-doable HARD units = pure waste/"overprocessing"), under-saturation from the profiling pass [[id:08a3]] (mean 14.7% time-at-cap → idle capacity), gated-HARD handbacks that re-derive the same verdict every run ("rework"). DMAIC fit: Define the value-add unit (a merged checkpoint) vs the 8 wastes (re-dispatch, idle lanes, handback rework, status/quota Haiku overhead = 46% of agents); Measure via the existing profiler + new burnup/event-log (this session's id:219b/id:c8b6 give the data substrate); Analyze where wall-clock + tokens are actually lost; Improve (drain fix, intra-repo parallelism id:ca87, suppress un-doable units); Control via the burnup time-series + RELAY_STATUS run-progress counters. Decide scope: a focused "cut the churn" session vs a full DMAIC pass. Relates to id:2d20, id:08a3, id:ca87, id:219b, id:c8b6. **Recommend SEPARATE from 80d7** — quality-of-audited-code vs efficiency-of-the-auditor are orthogonal lenses. **IN SCOPE (user 2026-06-16): the wave-barrier under-saturation** — a freed lane idles while one long unit holds the round open (observed live: a `collaib hard` straggler holding round 4 while 4 lanes sat idle). This is the explicit "known residual" deliberately deferred by id:6e9d (which closed only the *injection* case): fully closing it = continuous mid-round re-discovery / work-stealing, but the naive fix taxes EVERY round's tail with discovery agents even when no new work exists — a non-obvious cost/benefit the DMAIC pass should settle (continuous re-discovery vs standing tail cost vs the id:ca87 intra-repo-parallelism lever). Note: in a gated-backlog tail (remaining work all gated/claimed) the barrier costs nothing — it only bites when actionable work waits behind a long straggler. <!-- id:719e -->
- [ ] **Orphaned dead-run worktrees with UNMERGED commits accumulate — id:3ac8 surfaces but never resolves them** — user 2026-06-16: "you mentioned dangling worktrees, but /relay didn't pick them up?" Correct — by design the id:3ac8 auto-reap only removes CLEAN worktrees (HEAD is an ancestor of the repo's main → nothing to lose); worktrees carrying unmerged commits are deliberately NOT reaped (never delete unmerged work) and are only SURFACED as "stale worktree from a dead run with N unmerged commit(s) — needs manual integration." But nothing ever integrates them: the serialized integrator only merges worktrees from children IT dispatched this run, not orphans from dead/killed runs, so they pile up and get re-surfaced as HANDBACKs every run. Snapshot 2026-06-16 after stopping run 22124: 10 orphaned worktrees with 1 unmerged commit each — claude-organizer, collaib, linguistic-unversals, mathematical-writing, puzzle-pwa, rawrora (0615), toesnail ×2 (incl. an `ed7fc8ad-handoff`), zkm-ner, zkm-pdf — spanning dead runs from 0615 + this run. **Why they exist:** a child committed in its worktree, then the run died/was-killed BEFORE the integrator merged+pushed (or integration aborted), stranding the commit. **Wanted: an orphan-reconcile path** the pool (or a `/relay reconcile` mode) runs at discovery: for each orphaned worktree with unmerged commits, attempt the standard verify→`--no-ff` merge→ckpt-tag→push→prune IF it applies cleanly to the current main and passes the repo's suite; on conflict/red, leave it and surface ONE actionable line (not silently re-surface every run). Decide: auto-integrate clean-applying orphans vs always require human review; how to avoid re-attempting a known-conflicting orphan every round (a marker, cf. id:2d20's gated-suppression). Relates to id:3ac8 (clean-reap), the dispatch-safety cluster, id:2d20 (don't-retry-in-vain). **ROOT-CAUSE FIX SHIPPED 2026-06-16 (id:8b1f):** the dominant orphan source was identified — a HARD child SIZING OUT an item (refuse-before-start) was committing a RELAY_LOG/ROADMAP "handback note" (over-applying handoff.md C5's mid-cutoff RELAY_LOG-commit step), but the integrator never merges a handback, so that commit stranded forever. 8 of the 9 inspected orphans (claude-organizer, linguistic-unversals, mathematical-writing, puzzle-pwa, rawrora, zkm-ner, zkm-pdf, toesnail) were exactly these handback-note commits; 1 (collaib) was a superseded-but-DIFFERENT a37f E2E impl (an accidental A/B vs the integrated `e2e/` version — kept for a possible best-of-both port); the empty `toesnail/ed7fc8ad-handoff` self-cleaned. Fix: hard unitPrompt + handoff.md C5 now state a size-out/gated refusal must leave the worktree COMPLETELY CLEAN (no commit, don't touch RELAY_LOG/ROADMAP/REVIEW_ME) — rationale goes ONLY in the returned handback field — so future refusals leave auto-reapable (id:3ac8) clean worktrees and never strand. STILL OPEN here: the reconcile path for orphans that carry REAL partial work (mid-cutoff with committed checkpoints), where merge-or-discard genuinely needs judgment; plus disposing the 9 existing bug-artifact orphans (await user go on discard — flagged as the bug's output, no unique work). **DESIGN SETTLED 2026-06-16** — /meeting (Archie/Riku/Petra/Gil/Orla): NO auto-integration; park unmerged orphans on discovery (`worktree remove` + rename branch to `relay/orphan/*`, commit stays reachable, ONE summary line) + scripted human-invoked `/relay reconcile` + suppress-redispatch of still-open items that have parked partial work (cost hint via `relay-burn --run`); the 9 existing orphans are ALREADY REAPED (moot), collaib's A/B impl lost. Decomposed into id:689c (D1 park), id:3313 (D2 reconcile mode), id:1f53 (D3 suppress-redispatch), id:a692 (D4 forward-flag ↔ id:4267), id:122f (D6 fsck ADVISORY follow-on). See `docs/meeting-notes/2026-06-16-0938-relay-orphan-reconcile.md`. <!-- id:a4e9 -->
- [ ] **D4: forward-flag id:a4e9 ↔ id:4267** at the quota-gate/dispatch locus — "don't BEGIN an item you can't finish under remaining quota" tracked where dispatch decisions live (note only, no build; cost-prediction-before-dispatch out of scope). (see 2026-06-16-0938) <!-- id:a692 -->
- [ ] **D6 (follow-on): filtered ADVISORY `git fsck` dangling-relay-commit check in `/relay review`** — occasional `git fsck --no-reflogs --unreachable | grep commit`, filtered to relay-shaped commits (touches ROADMAP/RELAY_LOG/REVIEW_ME or msg `^(merge\(relay\)|handoff|review|todo|roadmap)`); ONE ADVISORY line, never auto-acts; kill-if-noisy after ~20 runs w/ 0 real hits. Ships after D1–D3. (see 2026-06-16-0938) <!-- id:122f -->
- [ ] **Relay profiler must read git-tracked transcripts, not only the live `~/.claude/projects/` dir — so a shorter `cleanupPeriodDays` doesn't starve it** — user 2026-06-16. Context: the claude-diary plan is to version raw session JSONLs in a per-host `<host>/sessions` git worktree, then LOWER `cleanupPeriodDays` (currently 999999) to reclaim uncompressed live-dir space (git keeps the compressed history). But `profile-run.sh`/`profile-runs-batch.sh` (id:a59e/id:08a3) discover + parse `wf_*` Workflow transcript dirs UNDER the live `~/.claude/projects/.../` tree; the id:08a3 finding already noted "transcripts are pruned at cleanupPeriodDays → 'all runs' = last ~30 days." If the cleanup window drops (e.g. 999999→14), the profiler's retention window drops with it and historical-run profiling breaks. ACTION: before lowering cleanupPeriodDays, point the profiler at the git-tracked transcript branch (or make it fall back to the worktree/branch when a live dir is absent) so its history survives independent of the live-dir cleanup. Sequencing gate: this must land BEFORE the cleanup-period decrease. Relates to claude-diary sessions-worktree DESIGN item, id:a59e, id:08a3. <!-- id:3e89 -->
- [ ] **Act on + sharpen the relay-economics takeaways** (next session) — see `docs/relay-economics-2026-06-16.md` (built with the new `relay-econ.py`, id:08a3). The 3-lens analysis (cost $ cache-accurate / time Σdur / time parallelity-weighted) over 36 runs gives a prioritized roadmap: **(1)** coalesce RELAY_STATUS writes — now justified on BOTH axes (20% of cost AND 37% of wall-clock; ran at only 1.7× concurrency = on the critical path; cache_create at 1.25× is its biggest cost component) → inbox `routed:3e5a`; **(2)** speed the serial discovery prelude (scaffold conc 1.2×, 16% of wall-clock vs 8.6% of cost); **(3)** gate+Haiku the inject-take poll → inbox `routed:26f0`; **(4)** re-measure with `relay-econ.py` after a few post-id:2d20 runs (the 36-run aggregate is contaminated by old spinning runs that inflated status — 06-15 alone was $133 of status). SHARPEN THE TOOL: reconcile list-rate cost vs `relay-burn.sh` actual `used_credits` (Max-plan-sunk vs overage); add a `--trend` mode (status share over time, to SHOW the id:2d20 effect); surface a one-line "$X · Yh wall · Z% overhead" into RELAY_STATUS/statusline (extends id:15bd/c8b6); validate the union-of-intervals parallelity model against the profiler's `concurrency_curve`. <!-- id:1a75 -->
- [ ] **`/relay status` mode — at-a-glance overview (read-only, no dispatch)** — user 2026-06-16. A quick command that summarizes relay state WITHOUT launching the pool: render `RELAY_STATUS.md` (run progress, in-flight, completed, queued, blocked/handbacks) + a backlog-composition line (ROUTINE/HARD/REVIEW/HUMAN ratios, cf. inbox routed:01ac done) + the burnup one-liner ("$X · Yh wall · Z% overhead", reuses `relay-burn.sh report` + id:1a75) + live-claim/orphan summary (`claim.sh peek`, `relay/orphan/*` count). Distinct from `/relay next` (which ACTS on the cwd repo) and the autonomous pool (which dispatches) — this is pure observability. Open qs: cwd-repo vs cross-repo (`--all`) default; freshness when no pool is live (last-run snapshot vs recompute); whether it's a thin SKILL front-door section or a script (`relay-status.sh`). **Relates to the project_manager ↔ relay coupling:** the `/projects` dashboard skill + id:62f5(b) (upstream an `ownership` field into project_manager `scan.py` — relay currently classifies own-vs-clone in a sibling `discover-repos.sh`); a `/relay status` overview is the relay-side analog of `/projects` and could share the classification/ownership substrate rather than duplicate it. Also feeds the statusline runway/backlog work (id:15bd, routed:01ac) and the observability artifacts (id:c8b6). **MUST include the pool's gated-HARD-needs-/meeting list** (the `id:2d20` Blocked entries in RELAY_STATUS) so the human can see what's blocking the pool from one place — pairs with id:f6c9. <!-- id:ec9a -->
- [ ] **Injected unit + same-repo discovery unit in one round → concurrent same-repo units race the integrator → spurious dirty-tree handbacks (wasted strong children)** — observed live 2026-06-16 (run relay-20260616-112222-29307, round 5): an `inject.sh add dotclaude-skills --verdict hard` (id:f6c9) was dispatched in the SAME round dotclaude-skills also had a normally-discovered `review` unit (and an `execute` re-enqueue). All three ran concurrently in separate worktrees; the `hard` child won the integrator, merged + checkpointed (eaf95c6/2571e3f), but its mid-merge writes to the shared MAIN checkout (`~/src/dotclaude-skills`) made the OTHER two units' integration see a dirty tree → both handed back ("Working tree is dirty: relay/references/human.md … Cannot merge"). No corruption (the dirty-tree guard worked), no work lost (they re-dispatch next round) — but two strong/sonnet children were spent for nothing + confusing handback noise. **Root cause (relay-loop.js):** the stated invariant is "pool of distinct repos, one unit per repo" (L71), enforced via the per-repo lease `claim.sh acquire` (L808). BUT (a) the lease is keyed cross-SESSION by runId and `claim.sh acquire` is RE-ENTRANT for the same runId, so two units of the SAME run targeting one repo both acquire it (no sibling-lane serialization); AND (b) the injection merge step (`units.push(injectedUnits)`, ~L617) adds the injected unit WITHOUT deduping against a discovery unit already present for that repo this round. So injection is a backdoor to UNGUARDED intra-repo parallelism — the very thing id:ca87 says needs sub-repo claim granularity + single-owner serialized integration before it's safe. **Fix options:** (a) cheap/correct — dedupe units per repo per round at the merge step: if an injected unit's repo already has a discovery unit this round, either MERGE them into one unit (prefer the injection's verdict/prompt) or DEFER the injection to a later round (honors the one-unit-per-repo invariant; the injection is already consumed from inject.d so it must be re-enqueued, not dropped); (b) make `claim.sh acquire` NON-re-entrant within a run for distinct units (unit-id-keyed sub-lease) so sibling lanes serialize; (c) full id:ca87 intra-repo parallelism (sub-repo path-set leases + serialized single-owner merge) so same-repo units are genuinely safe to run concurrently. (a) is the immediate bleed-stop; (c) is the durable design. Severity moderate (token waste + handback noise, not data loss). Relates to id:ca87 (intra-repo parallelism safety), id:baf1 (injection path), id:ebfb (per-repo lease — note the same-run re-entrancy), id:6e9d (mid-round inject), id:719e (DMAIC: handback rework = waste). <!-- id:0547 -->
- [ ] **Light/incremental handoff for already-onboarded repos — full C1–C5 handoff is overkill** — user 2026-06-16. The `handoff` verdict (discovery classifier) fires when ROADMAP.md is missing OR every item is ticked while untracked new work exists; `references/handoff.md` then runs the full onboarding: C1 docs (CLAUDE.md/README) → C2 roadmap → C3 red tests → C4 BDD → C5 optional HARD. For a repo that's ALREADY onboarded (has CLAUDE.md, a ROADMAP.md with the marker, a test suite + BDD), re-running C1/C4 is redundant re-scaffolding — and handoff children are STRONG-model (Opus), so it's wasted Opus $ (cf. the relay-econ cost lens, id:1a75: ~71% of spend is Opus work). The common onboarded-repo trigger is the "all items ticked + untracked new work" branch — that repo doesn't need docs/BDD rebuilt, it needs the NEW work specced. WANTED: a LIGHT handoff path that detects existing scaffolding (CLAUDE.md present, ROADMAP has the marker, tests/ dir exists) and does only the INCREMENTAL work — qualify+spec the untracked new work as C2 roadmap items + C3 red tests, skip C1 docs and C4 BDD that already exist (verify, don't rewrite). Decide shape: (a) a branch inside handoff.md ("if already onboarded → incremental: C2/C3 for new work only, verify C1/C4 exist"), or (b) a distinct verdict between `handoff` and `execute` (e.g. `respec`). Note this overlaps `review` §5b reverse-handoff (which qualifies+sizes unqualified TODO/ROADMAP additions) — maybe the onboarded-repo "all-ticked + new work" case should route to a review-like reverse-handoff rather than full handoff. Relates to the discovery classifier handoff trigger, review.md §5b, id:1a75 (handoff cost). <!-- id:b14a -->

- [ ] **[HUMAN] Encode "ROUTINE requires the test to gate the REAL goal" in the executor scope guard** — surfaced 2026-06-16 (toesnail /relay human, mirror fidelity id:0e63). The relay's `[ROUTINE]` (Sonnet-safe) vs `[HARD]`/human split implicitly assumes **tests-are-the-spec**; that breaks when part of the success criterion lives OUTSIDE the test (semantic fidelity, faithfulness, meaning, owner judgment). Concrete case: a `.mw` mirror shipped an UNFAITHFUL transcription yet its test was GREEN, because `.mw` keys DAG edges on regex symbol refs — green for faithful AND unfaithful RHS alike. An executor gated on "test passes" would re-ship green-but-wrong work. **Rule to add** to `references/conventions.md` (executor-contract block) and the handoff/review classification step: *before tagging a tooling item `[ROUTINE]`, ask "could this test be GREEN while the actual goal is wrong?" If yes (regex/structural/schema-shape/"it parses"/"it renders" checks guarding a semantic goal) → `[HARD]` + owner/strong re-confirm.* **Tagged [HUMAN] not [ROUTINE] — self-consistently:** this is a CONTRACT change governing ~36 repos with broad behavioral consequence and NO test gating its wording; the owner ratifies it (likely a `/meeting` — overlaps the rigor-bar meeting id:80d7 and the test-integrity sanity-check id:7b4d). Relates to id:80d7, id:7b4d, review.md §2 (anti-gaming). <!-- id:33c2 -->

- [ ] **MEETING-candidate: git-notes flag for un-runnable/sanity-violated commits → `git bisect` auto-skip** — user 2026-06-16 (side-note off toesnail mirror-fidelity thread). Idea: record a per-commit signal in git notes so a `git bisect run` wrapper auto-`125`s (skips) commits that can't be meaningfully tested, and so a stale-test/sanity-violated commit is marked. **Crucial nuance the user raised (the design hinge):** bisect runs the CURRENT/uncommitted test (the bug repro) against OLD source — it preserves untracked working-tree files across checkouts and `git bisect run <script>` lives outside the tree — so a "commit X gamed its OWN committed tests" flag is largely ORTHOGONAL to bisectability (bisect may never run X's tests). The signal that genuinely pays off is **runnability**: "this commit doesn't build / the harness can't execute here" → skip. The sanity-violation flag feeds bisect only insofar as a violation made the commit un-testable. **Substrate already exists:** the verify commit-hook writes ephemeral notes on `refs/notes/verify` (`status:pending findings:…`, toesnail id:8757) and `gaming-scan.sh` (id:fa05) is the mechanical sanity detector — a bisect-skip consumer is a natural extension. **Design qs for the meeting:** (a) carrier — extend `refs/notes/verify` with a `runnable:false`/`bisect_skip:true` field vs a dedicated `refs/notes/bisect-skip` ref (a new field bumps the just-FROZEN verify schema `{status,findings}`+`{triaged,processed,verdict,review_me}` — toesnail /relay human 2026-06-16, so it's a deliberate extension not a free add); (b) who writes it (the post-commit HARD tier? gaming-scan? a separate build-probe?) and when a commit is deemed un-runnable; (c) the `git bisect run` wrapper that reads the note and returns 125; (d) notes are local-only + lossy-on-rebase (toesnail D-decisions) — acceptable for a bisect aid, or does it need durability; (e) cross-repo vs per-repo. Likely ONE `/meeting`. Relates to toesnail verify-hook cluster (id:8757/d5f9/0e63), gaming-scan.sh (id:fa05), review.md §2, id:7b4d (periodic sanity-check), id:80d7 (rigor bar). <!-- id:872e -->

- [ ] **[skeleton L4] unitPrompt guardrail trim: de-duplicate inline rules against the loaded contract** — **EVALUATED 2026-06-18, DEFERRED as low-ROI + high-blast-radius.** `unitPrompt` fires on WORK dispatches (opus review/hard, sonnet execute) whose token cost is dominated by the actual work (reading code, editing, testing) — the ~40-line prompt skeleton is a negligible fraction (unlike the glue/shard agents whose whole job IS the skeleton), so trimming it saves little. AND it touches the UNIVERSAL child contract: the inline 'NEVER push/tag/diary' + lease rules apply to ALL verdicts, but review/handoff/hard load review.md/handoff.md (NOT executor-contract.md) — moving a rule into the wrong loaded file silently drops it for some verdict (catastrophic: pushing/double-working children). 'Contract rules must remain intact to the agent' would require per-verdict verification that every moved rule lands in a file that verdict's child actually reads → wants the canary/before-after + a focused session, not an AFK trim. Re-prioritize behind shard-prompt work. Not a haiku/target-drift concern (work agents are sonnet/opus). See 2026-06-18-0727-relay-skeleton-token-reduction.md. <!-- id:2895 -->

- [ ] **Re-point proxy spike (id:e905) trigger to chidiAI model-degradation test** — throwaway localhost pass-through proxy (originally from 2026-06-03 meeting, living in `docs/meeting-notes/2026-06-03-1452-llm-proxy-token-ctx-and-persona-split.md`) is deferred and now explicitly gated on the chidiAI model-degradation test (WIP). Reuse that harness as the spike vehicle if/when it fires. The proxy is a **measurement/observation instrument — it reduces ZERO billed skeleton tokens** (can't beat Anthropic 0.1× server-side caching; can't cache across distinct subagent conversations; shared `ANTHROPIC_BASE_URL` blocks per-subagent routing; mutation proxy trips ToS). Must NOT be counted as a token-reduction lever. When updating id:e905: embed the chidiAI cross-link. See `docs/meeting-notes/2026-06-18-0727-relay-skeleton-token-reduction.md`. <!-- id:e905 -->

- [ ] **[skeleton] Collapse discover-shard TURN COUNT — batch per-repo git gather (prompt-thinning REFUTED)** — **MEASURED 2026-06-18 (reconciles to $80.62 exactly).** Per shard: prompt = 85 uncached tokens ≈ 0.7% of $ (thinning REFUTED, like L1/L4). Cost = cache_read ~1.9M (46%) + cache_create ~116K (46%) + out (8%). **One-shard dissection (agent-a039f98, 6 repos):** 121 assistant turns, 104 Bash calls = ~17 git/grep invocations PER REPO (git tag/log/status/fetch/rev-list/for-each-ref + cat ROADMAP + greps). cache_read starts at 0 and grows to only ~45K (small context — NOT inherited workflow ctx), but is RE-READ every one of the 121 turns → 1.9M total = small-ctx × turn-count. **So the driver is TURN COUNT, not context size or prompt.** LEVER: a single `gather-repo-state.sh <repo>` (or extend discover-sig.sh — it ALREADY runs these exact git commands to hash them; make it EMIT the raw state too) the shard calls ONCE per repo instead of ~17 calls → ~10× fewer turns → cache_read drops ~proportionally → est. ~3× shard-cost cut (~$55/heavy-day-run). Behavior-preserving (same data, same verdict rules) → GATED by the shard-canary (id:3ea3, calibrated): change the shard prompt to 'run gather-repo-state.sh once then classify', keep `make shard-canary` all-green. Build-ready. See `docs/meeting-notes/2026-06-18-0727-relay-skeleton-token-reduction.md`. <!-- id:11ad -->

- [ ] **Executor: queue interactive-auth needs (askpass/sudo/polkit/login) into a global `/relay human` queue instead of handing back mid-session** — user 2026-06-17. Today when a `[ROUTINE]` executor (or any relay child) hits an action that needs a human-interactive privilege — `sudo`/`SUDO_ASKPASS`, polkit (`pamac` — never `sudo pamac`, see [[feedback-no-sudo-pamac]]), an SSH/login/credential prompt, a GUI askpass — the contract is "missing dep → handback, not sudo", which fails/aborts that unit mid-session and strands the rest of its work. WANTED: instead of failing the unit, the child RECORDS the blocking interactive-auth action (repo, what it needed, the exact command it would have run, why) into a **global queue** (cross-repo, like the shared inbox / `REVIEW_ME` human-backlog) that `/relay human` surfaces and batches — so the human can run all accumulated askpass/sudo/auth steps in one interactive triage pass, and the executor continues with the non-blocked remainder of its work rather than aborting. Design qs: (a) carrier — a new global queue file (`~/.config/fables-turn/auth-queue.md`?) vs the existing shared inbox vs a per-repo REVIEW_ME `[ ]` box flagged `@needs-auth`; (b) how `/relay human` consumes it (a dedicated tier alongside the (a)/(b)/(c) classification, akin to the `@manual` "you run these" checklist); (c) can the child meaningfully CONTINUE after deferring the auth step, or does the auth gate the rest of its unit (if gated, queue + clean-handback the rest; if not, queue + proceed); (d) what counts as interactive-auth (enumerate: sudo/askpass, polkit/pamac, ssh/login, gpg/credential, browser-OAuth). Relates to [[hermes-deferral-contract]] (record-and-defer / ToS tripwire on out-of-harness billing), the executor-contract missing-dep rule (`references/conventions.md`), `references/human.md` (human as 3rd actor), and the `@manual` "human runs these" surface. <!-- id:a505 -->
- [ ] **Collect failure reasons across relay children into a structured, queryable record (general failure ledger)** — user 2026-06-17. Today a unit's failure/handback reason lives only in the transient REPORT field and the RELAY_STATUS HANDBACK line for that run; there's no durable, aggregated record of WHY relay work fails over time. WANTED: a structured failure ledger that captures, per failed/handed-back unit, a categorized reason (e.g. `dirty-tree-race` id:0547, `gated-HARD-too-large` id:2d20, `interactive-auth-needed` id:a505, `Fable-unavailable`, `test-red`, `merge-conflict`, `quota-deferred`, `missing-dep`, `size-out`) plus repo/mode/model/run-id/timestamp — so recurring failure classes are visible and prioritizable (the DMAIC "Measure" substrate, cf. id:719e) instead of rediscovered run-by-run. Substrate already exists: the append-only `~/.config/fables-turn/relay-events.jsonl` (id:c8b6) records dispatch/integrate/handback events — this likely extends the handback event with a `reason_category` + free-text, and adds a small reporter (`relay-failures.sh report` akin to `relay-burn.sh`/`relay-econ.py`) to roll them up by category/repo/time. Design qs: (a) closed enumeration of categories vs free-text + post-hoc clustering; (b) who classifies (the child self-reports a category in its REPORT schema vs the integrator infers from the handback reason); (c) reporter output + whether it feeds RELAY_STATUS / `/relay status` (id:ec9a) / the statusline. Pairs with id:a505 (auth-queue is one failure class) and id:719e (DMAIC waste/defect reduction). Relates to id:c8b6 (event log), id:08a3 (econ), id:2d20/id:0547 (specific failure modes). <!-- id:194e -->
- [ ] **Handbacks must DIRECTLY create durable follow-up TODOs (auto-split `[HARD]` seams OR `[MEETING/HUMAN]`) + gate the parent against re-dispatch** — user 2026-06-17. Today a size-out / gated / blocked handback returns a rich REPORT (the zkWhale id:f14f handback even enumerated a clean 3-seam split: signPsbt+OP_RETURN PSBT builder / amount-nonce poll+localStorage / funnel UI state-machine; the dotclaude-skills id:dba3 handback identified the OS-user+meeting gate id:d0c0) — but that judgment **evaporates** into RELAY_STATUS for one run and nothing durable is created. WANTED: on a handback, the integrator (or a cheap follow-up agent) DURABLY records adequate follow-up items in the repo's TODO/ROADMAP under the **same id-ecosystem** (single-id-two-views), choosing per the handback's own classification: **(i) too-large-but-decomposable** → mint child `[HARD — strong model]` seam items from the child's recommended split (each executor/strong-sized) + leave the parent OPEN; **(ii) decision-gated / needs-design** → mint or re-tag a `[HARD — decision gate]` / `[MEETING]` / `[HUMAN]` item so a `/meeting` or `/relay human` picks it up; **(iii)** in BOTH cases ADD THE GATE so the pool stops re-dispatching the parent every round. **Gate partially exists already** (id:2d20: the classifier's EXECUTABLE-HARD test excludes `[HARD — decision gate]` / Gated-section items and surfaces them; id:2d20 option (c) — durable auto-re-tag of a freshly-handed-back HARD to `[HARD — decision gate]` — is the OPEN durable half). This item GENERALIZES id:2d20(c): not just re-tag-to-gate, but **create the right follow-up work** (split-into-seams vs route-to-meeting/human) so the backlog moves forward instead of stalling. **Design forks (likely a `/meeting`, heavy overlap with id:719e/id:2d20/id:194e):** (a) who authors the split — trust the handing-back child's one-shot proposed-seams (add a `proposed_split: [...]` / `route: hard-split|meeting|human` field to the REPORT schema) vs a dedicated splitter agent re-reading the item; (b) where the items are written + dedup so a re-run doesn't mint duplicate seams (idempotency key on parent-id + seam-title; reuse existing ids per single-id-two-views, never mint a duplicate for already-tracked work); (c) routing heuristic hard-split vs meeting/human (size + decision-gated + dependency-blocked signals); (d) how the parent's gate is invalidated when the seams are done / the meeting clears it; (e) trusting a one-shot "too-large" verdict enough to spawn durable items (anti-gaming: the next review re-checks). Builds on id:2d20 (drain + classifier exclusion), feeds id:719e (DMAIC: handback rework→durable forward progress), uses id:194e (failure-reason category as the routing signal) + id:c8b6 (event log). Concrete first targets to validate against: zkWhale id:f14f (3-seam split), dotclaude-skills id:dba3 (gate on id:d0c0). <!-- id:3801 -->
- [ ] **Flaky test: `tests/test_relay_claim_liveness.sh` (roadmap:7570) intermittently red under suite load** — surfaced by the id:401c Run 14 audit 2026-06-17: a full `tests/run-tests.sh` showed `65 passed, 1 failed` on this file, then two reruns were clean 66/66; it passes in isolation. Timing/mtime-sensitive (worktree-mtime heartbeat liveness, id:7570) — non-deterministic under load. NOT a feature defect; the test itself is racy. Fix: widen the mtime/TTL margins or stub the clock so the suite is deterministically green. Until then a lone red here is likely a flake — rerun before trusting. Unrelated to id:bbd2/id:10c0. <!-- id:16e9 -->

## meeting skill

- [ ] **BUG: /meeting (Opus) skipped two flow steps when invoked with a pre-formed subject arg** — observed 2026-06-16 (trAIdBTC id:abbf promotion meeting, routed in from `/relay human` which called the Skill tool with a long fully-formed subject string). Two omissions on the **Opus** path (the Fable inline-prose split is a SEPARATE known issue — not this): **(1) Adequate lenses not onboarded proactively.** Setup step 4 loads `personas.md`, but the subject-mode agenda ran with only the 3 standing personas (Archie/Riku/Petra) until the *user* asked "is there no additional lens to onboard?" — at which point 📈 Milo (the project's own quant-trading persona, registered from trAIdBTC) was obviously apt and materially changed the outcome (the n-dependence-of-the-firing-threshold argument). The skill should, for a subject-mode meeting, proactively scan the registry (esp. personas previously introduced *in this project*) and onboard topic-relevant ad-hoc lenses in the opening exchange, not wait to be prompted. **(2) Decision options not emitted in the SAME turn as the transcript.** `format.md` §Interactive default protocol (Sonnet/Opus/Haiku) is explicit: emit the transcript chunk AND call `AskUserQuestion` in the same response — "never end a turn on bare prose and send the question in a subsequent turn." At the first decision point I ended on bare prose (closing persona line) and the user had to type "Options?" to get the `AskUserQuestion`. That's the Fable inline-prose shape leaking onto the Opus path. **Hypothesised cause (user's):** invoking `/meeting` via the Skill tool with a big pre-formed `args` payload (vs an interactive `/meeting <subject>` typed by the user) led the model to compress/skip setup+protocol steps. WANTED: (a) make proactive project-persona onboarding an explicit, hard-to-skip step in the subject-mode flow (e.g. setup step 4 emits the candidate ad-hoc personas to onboard before the agenda); (b) reinforce the same-turn transcript+AskUserQuestion invariant so it holds regardless of how the skill was invoked (Skill-tool-with-args vs typed); (c) consider why a large args payload degrades step adherence. Relates to id:a6cb (other /meeting flow-omission bug). <!-- id:1d01 -->

- [ ] **BUG: /meeting can end without committing the implementation it just made** — observed 2026-06-16. A parallel `/meeting` session implemented id:689c (relay-loop.js orphan-park, D1) — it committed the RED spec (`tests/test_relay_orphan_park.sh`, 8044d2a) but ENDED leaving the matching `relay-loop.js` impl change UNCOMMITTED in the working tree. A sibling session found it dirty and committed it on its behalf (796e275) after verifying spec-green + structure-green. So /meeting's Class 1 dispatch (D7, "work as a /relay executor: append RELAY_LOG + commit") and/or the global post-prompt git-diary-workflow obligation did NOT commit the impl — only the spec landed. Possible causes: (a) Class 1 inline implementation didn't run git-diary-workflow before the session ended; (b) the session was interrupted between the spec-commit and the impl-commit; (c) the end-of-meeting steps don't assert a clean tree. WANTED: an end-of-meeting / end-of-turn guard that refuses to finish with un-committed working-tree changes the session itself made (or at least surfaces them loudly). Especially important for SHARED repos — a /meeting leaving dotclaude-skills dirty strands work for sibling sessions and risks a relay pool committing a half-finished spec file. Relates to the git-diary-workflow parallelity bug (claude-diary TODO) and the shared-repo attribution discipline (global CLAUDE.md Step 1c). <!-- id:a6cb -->

- [ ] **honcho** — evaluation **complete** (record-and-defer, 2026-06-05). Current file-based memory (discoveries.md+RAG / user-profile.md+awk-filter / persona-state.yml / auto-memory MEMORY.md) is sufficient for the current corpus (71 profile sections / 162 discovery lines). Honcho (AGPL-3.0, Postgres+pgvector+Docker+FastAPI+deriver+LLM-key, or managed api.honcho.dev) not warranted now. **Reopen gate:** primary = scale/loss (`user-profile.md` outgrows `profile-active.sh --filter` ctx budget, OR a logged save/discard miss); secondary (opportunistic): (a) piggyback if Honcho Claude Code MCP installs for unrelated reasons; (b) fold into meeting-rpg agent-infra wave with `hermes` + agent-persona-separation. **Privacy invariant: self-host only** if ever piloted — managed api.honcho.dev ships user-profile.md to a third party. See `docs/meeting-notes/2026-06-05-1213-honcho-memory-eval.md`. <!-- id:45ac -->

- [ ] **hermes** — record-and-defer (2026-06-05). 2-axis taxonomy: Axis A [transport: in-harness CC-subagent/`claude -p` vs out-of-harness external framework] × Axis B [authority: observer→unattended]; all cells live, none pre-blocked. Broker transport already shipped; deferred = (1) the *driver* process that subscribes+decides, (2) the *authority policy* (which prompts it may answer vs must escalate). **ToS tripwire:** out-of-harness + subscription-billed cell requires a citation from the then-current Anthropic subscription terms confirming programmatic/agent use is permitted — MUST verify BEFORE any code in that cell. **Reopen trigger (standalone, demand-pull, decoupled from honcho):** a concrete non-interactive driver needs to participate — shapes: (a) queue of unattended Class-3 meetings; (b) meeting-rpg multiplayer needs a non-human participant; (c) external agent framework (OpenClaw/Hermes/etc.) already in stack and asking it to observe/answer is near-free. Home = meeting-rpg; record = dotclaude-skills. See `docs/meeting-notes/2026-06-05-1236-hermes-agent-bridge-triage.md`. <!-- id:5c31 -->

- [ ] **Deferred — agent-persona-separation trigger** — revisit when (a) a logged meeting shows persona convergence weakening a decision, OR (b) meeting-rpg agent/broker infra is consumable by `/meeting`. Natural home: meeting-rpg. See `docs/meeting-notes/2026-06-03-1452-llm-proxy-token-ctx-and-persona-split.md`. <!-- id:36be -->

- [ ] **Persona duplication vs. name-reuse: investigation + prevention** — `append.sh -t personas` is append-only with no collision detection; re-onboarding an existing persona creates a duplicate entry (observed 2026-06-18: two Dex entries after toesnail/veq-macro-verify-carrier meeting). Design: (a) **detection** — at meeting end step 5 (save to registry), grep personas.md for the exact name before calling `append.sh` and surface a "this name already exists — merge or new name?" prompt; (b) **merge path** — a separate `append.sh update-persona <name> <new-text>` subcommand (or `Edit` on the resolved symlink path) for extending an existing entry in-place; (c) **name-collision check at meeting start** — when an ad-hoc persona is introduced, warn if their name already exists in the registry so the facilitator can re-onboard the prior persona instead of creating a fresh one. Convention: extend (same name, append lens) when the core role/domain is the same; split (new name) when the lens would confuse a future reader. <!-- id:d44d -->
- [ ] **Delete `/meeting-cross` after 2 successful `/meeting --cross` runs** — remove dir, P2 symlink, any meeting-cross-specific allowlist entries. Contract: registry no longer lists `meeting-cross`; `/meeting --cross` is sole cross entry. See `docs/meeting-notes/2026-06-11-0950-meeting-cross-as-cross-switch.md`. <!-- id:4f5f -->
- [ ] **Citation-logging hook design** (citation-rate-triggered): discoveries.md IS being used — audit confirmed 7 citations across 17 notes (~0.4/meeting). Design a Stop hook that reads transcript, matches surfaced entries, appends to `~/.claude/logs/discovery-citations.log`. **Sole gate (revised 2026-06-03):** open `/meeting citation-logging-hook` when an eye-audit finds **>5 surfaced/cited discoveries per meeting on average**. Measurement: count `## Surfaced discoveries` section entries + inline recall references across recent notes, divide by notes scanned. Re-audit checkpoint: 2026-12-01 (or sooner if citations visibly spike). Entry-count trigger (100 entries) removed — payoff-blind proxy fired at 140 entries while citation rate stayed at 0.4/meeting. Current state: 140 entries / 259 lines / ~0.4 citations per meeting. Pre-gate action (standardize `## Surfaced discoveries` in `format.md` template) shipped 2026-06-01. See `docs/meeting-notes/2026-06-01-1331-discoveries-citation-audit.md` and `docs/meeting-notes/2026-06-03-1402-citation-logging-gate-respec.md`. <!-- id:ab70 -->
- [ ] **Revise effort-estimate table in `format.md`** — now that `was_meeting` detection is fixed, re-run `meeting-cost.log` analysis after ≥10 genuinely-detected meeting sessions accumulate; update the v0 table in `format.md` with real data. Blocked on: `was_meeting=true` log entries accumulating post-fix.
- [ ] **Revisit S1+S2 and S3 adoption on 2026-08-13**: check S2 dedup adoption rate + `proj grep` invocation count; retire S3 if zero uses; re-tune S1 threshold if any `TODO.archive.md` >200 lines. (Orphan from `docs/meeting-notes/2026-05-13-1950-global-todo-skill.md`.)
- [ ] **Meeting-note archival skill** (deferred — gate re-armed 2026-06-06): open `/meeting meeting-note-archival` when `docs/meeting-notes/` has **≥50 notes AND oldest note >3 months old** (count ≥50 fired 2026-06-06 but all notes <1mo → 0 files qualify → build deferred). Design: move notes >3 months old to `docs/meeting-notes/archive/YYYY-QN/`; orphan-scan MUST **recurse into `archive/**` — never skip** (see §Decisions). Real build-warrant: dotclaude-skills orphan-scan runtime over 1s gate (cross-link: see "Caching for orphan-scan.sh" TODO). See `docs/meeting-notes/2026-05-14-1015-skill-ctx-bloat-audit.md` and `docs/meeting-notes/2026-06-06-1510-meeting-note-archival.md`.
- [ ] **`git-diary-workflow` SKILL.md size audit** — pre-emptive trim done 2026-06-11 (see `docs/meeting-notes/2026-06-11-0921-git-diary-skillmd-trim.md`). Current gate: `~(wc -c)/4 ≈ N tokens vs. 2k gate` (chars/4 estimate, reuses `cost-of.sh` SIZE_KB/4 convention). Trigger: post-trim token estimate exceeds 2k OR a prompt-bloat episode traced to this file. See `docs/meeting-notes/2026-05-14-1015-skill-ctx-bloat-audit.md`.
- [ ] **At next helferli meeting**: note whether `Write(docs/meeting-notes/...)` prompt fires — **observation in auto mode is inconclusive** (auto mode silently approves prompts; can't distinguish allowlisted vs auto-approved). Must test in a non-auto session or check settings.json allowlist directly. Contract: result noted in next helferli meeting note. Orphan from `docs/meeting-notes/2026-05-14-1739-class1-cross-repo-verify.md:28`.
- [ ] **Sub-agent meeting simulation for main-ctx isolation** — in broker mode, persona lines are generated *in the main Claude Code context* before being POSTed; broker mode saves the chat round-trip but not the in-context generation (~50–100k tokens/meeting). Additional motivation: parallel slow meetings each accumulate cold-cache penalties (5-min TTL is not user-configurable); sub-agent isolation means the main session never holds meeting token mass and TTL constraint becomes irrelevant. Design: spawn the meeting as a sub-agent (Agent tool or Workflow) that receives topic + user profile + discoveries and returns decisions + meeting-note text; main session processes only the thin return value. Pre-conditions: (1) `broker-mode.md` already has the full decision-routing contract; (2) 2026-06-03 discovery: "defer persona-separation to meeting-rpg-consumable trigger." Gate: opencode port validated (proves broker contract is stable) + ≥1 meeting with ctx > 200k. See: `~/src/dotclaude-skills/docs/meeting-notes/2026-06-03-1452-llm-proxy-token-ctx-and-persona-split.md`. Surfaced 2026-06-11. <!-- id:3346 -->
- [ ] **Caching for orphan-scan.sh** (condition-triggered): `~/.claude/logs/meeting-orphan-scan.log` shows helferli ~1200ms, zkm ~2600ms median — both ≥1s gate. Open `/meeting orphan-scan-caching` when zkm/helferli runtime grows further OR the scans cause perceptible friction. See `docs/meeting-notes/2026-05-14-1803-orphan-scan-accumulation.md` gate-(a) analysis.
- [ ] **Exempt ledger-only writes from the `hard` relay lease** — `/meeting` SKILL.md step 2a + `/relay human` spec: ledger write-backs (`md-merge.py`, `TODO.md`/`ROADMAP.md`/`REVIEW_ME.md`) skip `claim.sh acquire`; change step 2a from DEFER-on-held-lease to peek-and-warn only. `hard` lease narrows to code/worktree integration. Contract: a `/meeting` ledger write-back completes while a multi-repo pool holds the repo `hard` lease, without DEFER. See `docs/meeting-notes/2026-06-17-0953-k3s-parallelity-coordination-design.md` D2. <!-- id:c144 -->
- [ ] **Close scoop window (i): atomic ledger write+commit in `md-merge.py`** — `meeting/md-merge.py` commits just the edited ledger file under the same flock (scoped `git add <ledger-file> && git commit`). Contract: after any ledger write-back there is no modified-but-uncommitted ledger file left in the main checkout. Ships with or before id:c144 (the lease exemption). See `docs/meeting-notes/2026-06-17-0953-k3s-parallelity-coordination-design.md` D2. <!-- id:148b -->

## Caude statusbar

- [ ] **Re-verify quota peak-pricing window (~weekly)** — bump `PRICING_VERIFIED` in `statusline/statusline-command.sh` after confirming the schedule against current Anthropic docs/behaviour. Community window = weekday 05:00–11:00 PT (peak/regular burn); last changed 2026-06-10 (peak throttling reportedly dropped for Pro/Max). Triggered in-band by the statusline staleness marker (dim `?` appears on 💸/🪙 once the date is >7 days old).
- [ ] **Statusbar: other cost-saving indicators** — ~~current session input-token running total~~ done (id:2520 — shows `<pct>%(<tokens>)` in context segment); remaining candidates: cache_read vs uncached ratio (shows caching effectiveness), model currently active.

## worktrees / D-series

- [ ] **AI-4 (worktree TODO.md scope)** — after ≥1 live `/meeting` run from a `meeting-rpg` worktree, log whether autonomous upward traversal of TODO.md recurs. If yes, escalate to mechanical guard design (D5 follow-up). See: `docs/meeting-notes/2026-05-20-2105-todo-md-root-resolution-worktrees.md`.

- [ ] **D6 — sub-agent parallel Class 1 dispatcher (build-gated)** — design recorded 2026-06-04. Gate: **(1) D5 ratified ✓ 2026-06-04**, (2) ≥3 independent disjoint-footprint items logged at once, **(3) Haiku-vs-Sonnet pilot ✓ 2026-06-04** — Sonnet-default confirmed. Design: Workflow tool, worktree-per-item, baseRef=`fresh`, `--no-ff` merge, Sonnet builds + verifies vs `Contract:`, children return `{branch, diary_fragment, todo_item_id, done_summary, contract_met}`, main owns shared-ledger + push; naming `.claude/worktrees/<item-id>`; fail→don't-merge, surface to user. See `docs/meeting-notes/2026-06-04-1048-subagent-parallel-class1.md` and `docs/meeting-notes/2026-06-04-1144-worktree-per-session-d5.md`. <!-- id:cbb5 -->
- [ ] **Independent-session flock'd merge-to-canonical — BUILD OPENED 2026-06-12 (recurrence recorded, contract met)** — originally deferred pending a second cross-session race after the flock+manifest fixes. Recurrences 2026-06-12: (1) same-file collision on uncommitted fables-executor contract-v2 edits (see id:ebfb); (2) git-diary-workflow Step 1c nearly committed a parallel relay session's in-flight relay-loop.js WIP from the trAIdBTC reviewer session; (3) same session's push autostashed over yet another session's fresh WIP. Step 1c now attributes dirty files per session (44a4820), but attribution is file-granular — concurrent edits to the SAME file remain unguarded (manifest `git add` scoops foreign hunks; autostash pops over a moving tree). Real fix per D5.6: each session edits in its own worktree, flock'd merge to canonical. See `docs/meeting-notes/2026-06-04-1144-worktree-per-session-d5.md` (D5.6). **Recurrence 2026-06-16:** `diary-append.sh`'s internal `git pull --rebase` on the SHARED `claude-diary` main checkout aborted ("cannot pull with rebase: You have unstaged changes") because a concurrent session had the claude-diary tree dirty (a sessions-worktree-v0 refactor in flight); the failed run consumed the `-f` temp entry and it only succeeded on retry. Same root-cause class: a shared main-checkout writer with no per-session worktree. xref id:3e89 (claude-diary sessions-worktree). <!-- id:3558 -->

## local-llama-swap subagent delegation

- [ ] Conditional on bounded-B passing: commit a thin `curl`+`jq` local-delegation wrapper with outcome logging (task/model/outcome, zelegator-consumable) and loud-fail on n_ctx overflow. Contract: routes a bounded prompt to localhost:8080/v1, appends one log line per call, no silent truncation. See `docs/meeting-notes/2026-06-11-2235-subagents-local-llama-swap.md`. <!-- id:85d2 -->
- [ ] Forward-flags: (a) auto-routing local-vs-remote → zelegator; (b) opencode agentic-local escape hatch — **C-agentic still untested (deferred); run with 30b model in a clean-memory session before closing this flag.** See `docs/meeting-notes/2026-06-11-2300-subagents-local-llama-spike-finding.md`. <!-- id:f579 -->

## /batch — built-in parallel worktree command

- [ ] **Audit `/batch` for parallel-processing applications** — `/batch` is a built-in Claude Code command (no skill needed): decomposes changes into independent worktree agents, each opens a PR. Candidate applications: (a) parallel ROADMAP `[ROUTINE]` item dispatch as an alternative to the relay for repos where anti-gaming verification isn't critical; (b) parallel meeting-note or TODO migration; (c) multi-repo refactors that are genuinely independent (no review-checkpoint dependency). Also check: whether a unified `fables-turn`/`fables-executor` skill (model-adaptive, detects Fable vs Sonnet via harness-class gate) would be simpler than the current split, now that `/batch` covers the "fan-out parallel" use case. Trigger: open `/meeting batch-applications` after ≥1 `/batch` run in any project. See discovery 2026-06-12. <!-- id:7b23 -->

## fables-turn relay skill

- [ ] **Make `--all` explicit across ALL relay subcommands — bare invocation = cwd repo only** (user 2026-06-15) — `review` and `next` already default to the cwd repo (`git rev-parse --show-toplevel`) and require `--all`/a repo-list for the cross-repo sweep (the historical default-flip), but `human` and `handoff` still default to "all confirmed own repos". Make the cwd-only default UNIFORM: `/relay human` with no arg = the local repo's human-backlog only; `/relay human --all` = the cross-repo sweep. Same for `handoff` (bare = cwd repo handoff; `--all`/repo-list = wave). The autonomous pool (bare `/relay`, no keyword) is the deliberate exception — it stays all-repos by design (it IS the fleet runner). Rationale: least-surprise consistency — every keyworded subcommand should mean "here" unless you ask for "everywhere". Touch points: SKILL.md invocation table + per-mode "Default scope" prose (human-mode §1 currently says "With no argument, scope is all confirmed own repos" — flip to cwd-repo), references/human.md §1, references/handoff.md scope note, and the front-door arg parsing. Update `docs/relay.md` user guide. Surfaced while running `/relay human .` (had to pass an explicit scope to avoid the cross-repo sweep). <!-- id:53f7 -->

- [ ] **Relay defect: TERMINAL/archived repos get re-handed-off forever** — voicebot (archived GO repo; its CLAUDE.md says "No further development happens here") was re-handed-off 5× (fable-ckpt 1617/1749/1823/1843) because the classifier rule "no `fable-ckpt`... no, *no ROADMAP* → handoff" fires every pool for an archived repo whose CORRECT state is no-ROADMAP. Each re-handoff was a wasted ~150k-token Opus child that just re-recorded "TERMINAL disposition, no provisioning" in RELAY_LOG/CLAUDE.md. **Stopgap applied 2026-06-13:** voicebot set `classification = "excluded"` in relay.toml (loop stopped). **Durable fix (this item):** a handoff child that concludes TERMINAL (no executor work, archive-respecting — it already writes a "Relay status: TERMINAL" note to CLAUDE.md + RELAY_LOG) must cause the orchestrator to mark the repo `excluded` (or a new `terminal` classification) so discovery never re-hands-it-off. Options: (a) handoff REPORT_SCHEMA gains `terminal: bool`; integrate() sets relay.toml classification=excluded when true; (b) discovery rule: a repo with a `fable-ckpt-*` tag (already processed) + no ROADMAP + a "Relay status: TERMINAL" marker in CLAUDE.md → classify `idle`, not `handoff`. (b) is self-healing without schema change. Either way the marker the handoff child already writes must be HONORED by discovery. <!-- id:b4f7 -->
- [ ] **Review→execute chaining within a pool** — SHIPPED 2026-06-13 (relay-loop.js), verify on next pool. User asked why review + execute take two pool-runs per repo. Now: a REVIEW child reports `routine_open` (open [ROUTINE] count after re-deriving the roadmap); if >0 and under cap, `runUnit` re-enqueues an `execute` unit for that repo onto the live queue SAME pool (the pushing lane re-checks `queue.length`, so it's never lost). Single-hop only (`!unit.rechained` guard + `rechained:true` mark) — an execute never re-chains, so its commits are reviewed next pool (no intra-pool ping-pong; preserves D3 review-after-execute). NB: execute is per-REPO not per-item — one execute turn works as many open [ROUTINE] items as it can finish. REPORT_SCHEMA.routine_open + review.md/unitPrompt updated; test_relay_loop_structure.sh checks 13–14. VERIFY: next pool with a review that leaves [ROUTINE] open shows a `review→execute re-enqueue` log line + the execute runs same pool. <!-- id:b8ae -->
- [ ] **API-error failsafe: handoff auto-resume + recoverable handbacks** — SHIPPED 2026-06-13 (relay-loop.js), verify on recurrence. Trigger: a linguistic-unversals handoff died on "API Error: Stream idle timeout - partial response received" (n=1) after committing C1+C2; the pool logged it under `<failures>` and ORPHANED the worktree (not in handbacks, worktreePath dropped). Fix: `runUnit` wraps the child `agent()` in try/catch; on throw/null for a HANDOFF it spawns ONE auto-resume child (`resumePrompt`) that continues from the last committed checkpoint; if that also fails (or for review/execute), `integrate()`'s null-guard now records a RECOVERABLE handback with the deterministic `worktreePathFor(unit)` + resume hint (never `worktreePath:'-'`). handoff.md gained a §Resuming. Covered by test_relay_loop_structure.sh checks 11–12. Pre-existing failsafes that already worked: harness retries `agent()` on transient API errors; per-checkpoint commits preserved C1+C2. Per the observe-before-preventing heuristic this is n=1 — do NOT add heavier auto-retry/everywhere-failsafe until more instances; just confirm the auto-resume actually fires + recovers on the next real interruption, then tick. <!-- id:738c -->
- [ ] **Autonomous budget-governed relay campaign (timed cross-window relaunch)** — SUGGESTION, design before build. User wants the pool to relaunch at each 5h-window reset until a quota cap, unattended. First attempt 2026-06-13 (a self-chaining `systemd --user` timer running a governor that relaunched `claude -p --dangerously-skip-permissions "/fables-turn --strong-tier opus"`, refreshing `/tmp/claude-usage-cache.json` itself via the statusline's `/api/oauth/usage` curl to enforce a 50% 7d/Sonnet cap authoritatively, self-terminating at the cap) was **denied by the auto-mode classifier** — correctly: it's a persistent permission-bypassing agent loop the user hadn't authorized at that level. The reusable half DID ship: `quota-stop.sh` per-bucket threshold overrides (`RELAY_QUOTA_THRESHOLD_SEVEN_DAY` / `_SEVEN_DAY_SONNET`, default = general THRESHOLD) so any run can cap long-window buckets tighter than the 5h bucket (covered by tests/test_quota_stop.sh). The governor script body is preserved in this session's transcript (~14:5x) for revival. **Safeguards to design before building**: (a) explicit user-authorized settings.json Bash rule scoping exactly the governor unit + its `claude -p` invocation, not blanket skip-permissions; (b) a hard wall-clock/agent-count campaign budget independent of quota; (c) a visible kill switch (the id:d0bc STOP sentinel should also halt the governor); (d) loud surfacing of each headless relaunch (RELAY_STATUS + notification) so it's never silent; (e) confirm headless `claude -p` actually hosts the background Workflow to completion (push/integration) before trusting it; (f) Fable auto-probe ([[fable-down-flag]], id:f649) so the relaunch picks the right STRONG_TIER instead of hard-coding opus. Interim: in-session loop (this session relaunches the Workflow at each reset, enforcing the cap manually). <!-- id:af30 -->
- [ ] **Fable-return: surface `fable-standin` checkpoints as priority review units** — **SURFACING SHIPPED 2026-06-13** (relay-loop.js): classifier now reports `standin` per repo (latest `fable-ckpt-*` tag message contains literal `fable-standin`); on a real-Fable session (`SESSION_IS_FABLE`, not `--fable-down`) repos whose standin checkpoint left no unaudited commits are ELEVATED execute/idle→`review` so they get an independent Fable audit, and the `standInRank` tiebreaker sorts standin repos first within the review class (and last within execute/handoff — "prefer Fable-vetted" per the 2026-06-13 balanced-scheduler directive). Dormant on Opus/`--fable-down` (Opus never re-reviews its own standin work). Covered by tests/test_fable_standin_marker.sh (checks 9–17). Five repos carry the marker now (proton-moresync, project_manager, cyclotomic-projection, llm-from-scratch, puzzle-pwa — all Opus-standin handoffs 2026-06-13). **Gate met: code is dormant-until-Fable, so it ships ahead of access restore.** REMAINING: verify on the FIRST real Fable-class pool run that the 5 elevate + sort correctly, then tick. Priority-vs-income fine ordering intentionally left as income-dominates. See `docs/meeting-notes/2026-06-13-0859-fable-standin-checkpoint-marker.md` D4. <!-- id:9821 -->

- [ ] **Consider moving relay pointer to global `~/.claude/CLAUDE.md`** [Fable] — currently the `## Relay contract` pointer is written into each managed repo's own `CLAUDE.md` by handoff C1. Alternative: a single entry in `~/.claude/CLAUDE.md` that fires for all sessions, with repo-local opt-in/opt-out markers. Required failsafes before doing this: (a) non-personal repos (upstream forks, checked-out third-party code) must not silently inherit relay rules; (b) repos not yet handed over (no `fable-ckpt-*` tag, no ROADMAP.md) must not confuse executors with a dangling pointer; (c) the global entry must not add noise to sessions where no relay work is happening. Candidate design: global CLAUDE.md carries a conditional ("if this repo has a `RELAY_LOG.md` or `fable-ckpt-*` tag, load `fables-executor`") rather than an unconditional pointer — keeps the passive guarantee without per-repo embedding. Requires Fable judgment: global CLAUDE.md changes affect every session's context budget and trust surface. **Gate met 2026-06-12** (≥3 relay repos active: 7 handed-off/active in relay.toml after this wave). <!-- id:ef8a -->
- [ ] **Strong-model audit** (code review + security + design coherence) — ROADMAP id:401c. Three passes: (1) correctness bugs / shell quoting / race conditions in scripts + Python helpers; (2) injection/path/secrets/permission security; (3) design coherence for unreviewed decisions (sensibility, feasibility, contradictions, gates that can never fire). Every finding fixed inline, tracked, or explicitly accepted. **First run ✓ 2026-06-12** (16 findings — see `docs/meeting-notes/2026-06-12-1811-strong-model-audit.md`). **Latest ✓ Run 13 2026-06-17-1102** (5 files since last audit: discover-sig.sh/relay-loop.js-cache/model-probe.sh/settings-env.py/battery; clean, 0 inline fixes, 2 LOW tracked = id:4348 + id:b9b5, 3 accepted — see `docs/meeting-notes/2026-06-17-1102-strong-model-audit.md`). Recurring: reviewer re-opens the ROADMAP checkbox after each significant Sonnet batch. <!-- id:401c -->
- [ ] **Pilot fables-turn handoff** on confirmed wave (kienzler-homepage, zelegator, helferli, dotclaude-skills, zkWhale; +meeting-rpg as 2nd mini-wave; **income repos: zkWhale, helferli — flagged by user 2026-06-12, registered in relay.toml**) — **dotclaude-skills pilot ✓ 2026-06-12** (C1–C5 merged, tag fable-ckpt-20260612-1328, contract_met, suite 1 pass/5 expected-red; integration defects → id:821c); **review pilot ✓ 2026-06-12** (tag fable-ckpt-20260612-1618, integrity audit clean, no gaming flags); **templates/conventions/handoff refs verified post-pilot-current ✓ 2026-06-12**; `make install-fables-executor` run ✓; **zelegator/zkm/isochrone/meeting-rpg handoff wave ✓ 2026-06-12** (tags fable-ckpt-20260612-1816/-1817/-1818/-1820, all C1–C5 contract_met, 26 REVIEW_ME judgment boxes pending user); **zkWhale/helferli handoff wave ✓ 2026-06-12** (tags fable-ckpt-20260612-1847/-1813, both C1–C5 contract_met incl. C5 HARD execution — zkWhale paste-sig /external id:9356 + expected-red CI guard added by orchestrator, helferli session capture id:0aaa + baseline e2e/dep fixes; 14 REVIEW_ME boxes pending user). NEXT: kienzler-homepage. See `~/.claude/plans/i-want-to-optimize-quizzical-creek.md`. <!-- id:1107 -->
- [ ] **Audit repos for unintended local-only annotated tags** — `git-lock-push.sh` now pushes `--follow-tags` everywhere, so any repo using the diary workflow will auto-publish reachable annotated tags (vX.Y.Z, fable-ckpt-*) on its next push. Aligned with the bump-and-tag rule, but check (`git -C <repo> push --dry-run --follow-tags` or `git tag -l` vs remote) that no repo carries local-only annotated tags it didn't intend to publish. Surfaced by 2026-06-12 review turn. <!-- id:c3c8 -->
- [ ] **Observe `test_git_lock_push_slash_branch.sh` flake** — two one-off full-suite failures on 2026-06-12 ("pull was skipped"); passes in isolation and in 15 consecutive looped full-suite runs. Both failures coincided with heavy parallel relay/git activity on the machine; suspect transient `git ls-remote --exit-code` failure that git-lock-push.sh treats as "branch absent" and silently skips the pull. Test now captures lock-push output and dumps diagnostics on failure (shipped 2026-06-12) — next natural occurrence self-diagnoses; design retry/fail-loud fix only after that evidence lands. <!-- id:456a -->
- [ ] **Manual graceful-drain knob for the relay pool** — sentinel check at the top of `fables-turn/scripts/quota-stop.sh`: `[[ -f ~/.config/fables-turn/STOP ]] && { echo "quota-stop: manual STOP sentinel" >&2; exit 1; }`. Because the gate re-invokes the script before *every* unit dispatch, `touch ~/.config/fables-turn/STOP` makes a running pool stop filling at the next gate check and drain gracefully (in-flight children finish, integration debt drains, undispatched repos listed in RELAY_STATUS) — same path the automatic 90% threshold uses. Remove the sentinel after the drain (or have relay-loop's front door warn if it pre-exists, so a stale STOP can't silently no-op a future run). Document in SKILL.md config-knobs table + `docs/fables-relay.md`. Add a scratch test alongside the existing quota-stop coverage. Surfaced 2026-06-12 during live pool run (five_hour hit the 90% gate mid-run; user asked for a pre-threshold manual stop). <!-- id:d0bc -->
- [ ] **Auto-handle uv.lock drift in the relay pool** (user request 2026-06-12) — pilot run relay-20260612-2304: a parent-zkm version bump (0.14.0) left ` M uv.lock` in plugin checkouts, blocking 2 integrations (zkm-calendar, zkm-eml) + 1 dispatch (zkm-scan); resolution was mechanical (`git add uv.lock && commit "chore: refresh uv.lock"`). Levers, combine as needed: (a) integrator prompt: if porcelain shows ONLY `uv.lock` modified, auto-commit `chore: refresh uv.lock — dependency re-resolution` before the merge instead of aborting; (b) discovery: same uv.lock-only exemption so the repo still dispatches (child regenerates in its worktree); (c) prevention: executor conventions.md mandates `uv run --frozen` / `uv sync --locked` so test runs can't silently mutate the lock — drift then only enters via deliberate dep changes; (d) zkm release flow: bump plugin lockfiles as part of the parent version-bump commit. Guard: the exemption must match uv.lock ONLY — any other dirty path still aborts. <!-- id:bae5 -->
- [ ] **relay-loop.js integrator + status hygiene** (pilot findings 2026-06-12) — (1) integrator step 1 is judgment where it should be mechanics: one Sonnet integrator treated `?? untracked` as clean and proceeded into the zkWhale merge conflict while two others aborted on ` M uv.lock`; spell out the rule in the integrator prompt ("porcelain output non-empty = dirty = abort, untracked included; exemptions listed explicitly" — coordinate with id:bae5's uv.lock carve-out). (2) RELAY_STATUS Queued section double-lists repos: initial queue snapshot entries are never removed when quota-deferred/not-dispatched entries are re-appended (runUnit pushes a second entry; final concat adds a third form) — dedupe by repo, keep the most specific verdict. Both are small relay-loop.js edits + test updates. <!-- id:59ea -->
- [ ] **Review starvation in pool scheduling** — pilot run relay-20260612-2304: 13 execute units consumed the entire quota budget; all 7 review units were quota-deferred, so the anti-gaming window (D3's whole point: review ranks above fresh handoff to keep unaudited work short-lived) stays open across the run. Execute-first is correct *within* abundant quota, but under a binding quota the last units dispatched should not all be executes. Candidate designs: (a) reserve a review quota slice (e.g. stop dispatching executes at threshold−N pp so reviews fit); (b) interleave classes round-robin after the first pool fill; (c) cap executes per run at POOL_WIDTH×k when reviews are queued. Judgment call on D3 semantics — design before the next fleet-wide run, ideally via `/meeting`. <!-- id:1dff -->
- [ ] **Fix pyright nit in `tests/fixtures/mock-broker.py`** — `log_message` override renames the base param (`fmt` vs `format`), reportIncompatibleMethodOverride. <!-- id:e956 -->
- [ ] **Ledger-discipline shared reference (dedupe single-id contract prose)** — D8's recommended extraction (id:6ba4): the single-id-two-views / flock'd-ledger-write / cross-repo-routing invariants are restated in ~5 places (handoff.md C2, review.md §5, relay-loop.js review prompt, meeting/SKILL.md 2b/2e, todo-update step 3); collapse to ONE cited reference + thin pointers (mirror the executor-contract vN pointer). Pure doc-dedup, no behaviour change. <!-- id:962a -->
- [ ] **Cross-PROJECT bidirectional TODO sync — same item mirrored across SEPARATE repos under a shared `id:` token, kept in lockstep** — gap surfaced 2026-06-16 (toesnail `/meeting`). Today's cross-repo machinery covers only two of three cases: **one-way routing** (inbox: born-in-A → routed-to-B's-inbox, `append.sh -t inbox`, `docs/meeting-notes/2026-06-11-1400-cross-project-todo-routing.md`) and **intra-repo bidirectional** (`orphan-scan.sh --cross-ledger`, single-id-two-views D2 — but ONLY TODO↔ROADMAP within ONE repo). The uncovered case: when one logical item is intentionally duplicated across *separate project repos* under the same `id:` (e.g. toesnail+mathematical-writing `id:6ab8`; toesnail+zkm `id:4159`), nothing keeps the copies' checkbox state consistent — closing it in one repo silently leaves the twin `[ ]`. **Proposal:** a cross-repo `orphan-scan --cross-project` that, over a set of repos, greps each for `id:` tokens and flags any id present in >1 repo with disagreeing `[ ]`/`[x]` state (optionally divergent body text). **Open Qs:** which repos to scan (a registry / the relay.toml set / a manifest of mirrored ids?); surface-only vs auto-tick the twin; how it composes with `/todo-update` and `/meeting` write-back. Until built, mirrored items carry manual-sync instructions inline. Relates to [[962a]] + the relay `--cross-ledger` code. <!-- id:69f4 -->
- **(pointer — do not re-file)** "Inflownistration" / general information-dependency propagation (update one place → auto-trigger follow-ups in linked READMEs, examples, call-sites; a renamed constant stales the prose citing it) is TRACKED in `mathematical-writing/TODO.md` under `id:aae4` — it's the generalization of `.mw`'s `stale_after_edit` DAG, and the cross-project TODO-sync above ([[69f4]]) is one concrete instance of it. Recorded here so a future session doesn't re-TODO the idea. <!-- ref:aae4 -->
- [ ] **Triage all uncloned GitHub repos — online-first, clone only if sensible** (owner 2026-06-16). For each `zommuter/*` repo NOT cloned locally, review online first (`gh repo view`, README, recent activity, open issues) before any clone; clone only if active triage/work is warranted. Per-repo verdict: keep-as-is / clone+work / archive / delete (gtnsd just went archive+drop as the template). **Uncloned snapshot 2026-06-16 (26, excl. archived gtnsd):** ahk-scripts, ama, createxig, customize, desktopini.py, EdNui, gedcom-reaper-py, gnucash, hash2identicon.js, markeast, newtonfractal, numintropy, poe, pygeon, pyki, rasperrypi-userland-aarch64-git, solidity-experiments, spectral-xray, ssh-agent-sign, Tests, timefolder, vain39, win10helpers, Zommium, zommuter.github.io, zomtems. (`zomtems` = the gtnsd-referenced "share idea fragments without losing ownership" project — triage with the rest.) Recompute the gap via `gh repo list zommuter` vs `~/src/*` + `~/src/zkm/plugins/*`. Likely a `/projects`-style grooming pass. <!-- id:581c -->
- [ ] **(gated) `ledger-close.sh` heredoc wrapper around `md-merge.py update-ids`** — build ONLY if id:962a's audit shows the JSON heredoc is the residual drift source. <!-- id:3103 -->
- [ ] **`[HUMAN]`/`[MEETING]` tag-taxonomy completion** (user 2026-06-15) — add `[HUMAN]` (trivial human input: confirm / yes-no / run an @manual scenario) and `[MEETING]` (non-trivial design judgment) to the `[ROUTINE]`/`[HARD]` family, completing the actor↔tag↔surface map (HUMAN→`/fables-turn human` tier-A/B, MEETING→`/meeting --cross` tier-C). Closes the untagged-open-item-invisible-to-classifier gap (untagged → lint / reverse-handoff trigger; a repo whose only open work is HUMAN/MEETING stops reading `idle`). **HINTS not gates**: `/fables-turn human` answerability triage still refines (a stale `[HUMAN]` may auto-resolve; a `[MEETING]` may demote to a batch yes/no). `@manual` ⊂ `[HUMAN]` (run-flavored, never auto-tick); REVIEW_ME membership is already an implicit HUMAN signal, so tags matter mainly on ROADMAP/TODO. Multi-file contract change: classify.sh, relay-loop.js discovery, review.md §5b reverse-handoff (assign HUMAN/MEETING, not force-HARD), executor-contract docs, `/fables-turn human`. Sequence AFTER id:72cc (/fables-turn human) + id:962a land. <!-- id:d0da -->
- [ ] **[HARD] `/fables-turn review` cross-project dependency audit** — design done 2026-06-12 (see `docs/meeting-notes/2026-06-12-2312-fables-turn-cross-project-dep-audit.md`). Build tracked by id:4f21 + id:14c1 + id:045a; Axis-B detection forward-flag = id:6b81 (gated on id:ebfb/id:3558). Original spec: review mode audits single repo; should also detect conflicting/coupled changes (broker API, persona format) across related repos; surface in RELAY_LOG paragraph. Architecture: post-barrier audit agent in relay-loop.js; [[coupling]] table in relay.toml; two-tier check (T1 asymmetry, T2 LLM impact note); findings to RELAY_LOG both repos + routed inbox only on real drift. <!-- id:6a3c -->
- [ ] Add `[[coupling]]` schema + one entry to `~/.config/fables-turn/relay.toml` (meeting-rpg ↔ dotclaude-skills, surface globs = `meeting/broker.py`, `meeting/broker-mode.md`, `meeting/personas.md` + meeting-rpg `launcher`/`web/config.json`, note). Contract: relay.toml parses; `[[coupling]]` entry has `repos`/`surface`/`note`. See `docs/meeting-notes/2026-06-12-2312-fables-turn-cross-project-dep-audit.md`. <!-- id:4f21 -->
- [ ] Add post-barrier coupling-audit step to `fables-turn/scripts/relay-loop.js`: after the review barrier, for each `[[coupling]]` whose both repos were touched this wave, spawn ONE audit agent (two-tier: T1 deterministic asymmetry, T2 LLM impact note); else skip. Document in `fables-turn/references/review.md` + SKILL.md. Contract: no agent when <2 coupled repos touched; symmetric RELAY_LOG one-liner + `routed:` inbox entry only on asymmetric drift. See `docs/meeting-notes/2026-06-12-2312-fables-turn-cross-project-dep-audit.md`. <!-- id:14c1 -->
- [ ] Add scratch-repo test `tests/test_coupling_audit.sh`: (1) asymmetric surface change → one flag + one inbox entry; (2) symmetric change → low-priority note, no inbox token; (3) only one coupled repo touched → audit no-ops. Hermetic (`mktemp`, no `~/.config/`/`~/.claude/`). See `docs/meeting-notes/2026-06-12-2312-fables-turn-cross-project-dep-audit.md`. <!-- id:045a -->
- [ ] **Forward-flag (Axis B — write-contention detection):** gated on `id:ebfb` (cross-session reservation) + `id:3558` (flock'd merge-to-canonical) landing; once those ship, extend the coupling audit to detect checkpoint hunks not matching the committing session's declared work, using the worktree-reservation signal as the comparison anchor. Do NOT reabsorb into `id:6a3c`/Axis A before those prerequisites land. See `docs/meeting-notes/2026-06-12-2312-fables-turn-cross-project-dep-audit.md`. <!-- id:6b81 -->
- [ ] **User-facing documentation for the fables-* relay** — **shipped 2026-06-12**: `docs/fables-relay.md` (modes table, artifact glossary, between-turns duties, knobs, safety properties), linked from both SKILL.md files. Awaiting user read-through before ticking. Requested by user 2026-06-12. <!-- id:a354 -->
- [ ] **fables-turn deferred follow-ups** — (a) ~~extend `meeting/orphan-scan.sh` + `classify.sh` + `append.sh new-id` scan globs to include `ROADMAP.md`~~ **shipped 2026-06-12** (roadmap de9c: `append.sh scan-ids` ledger incl. ROADMAP.md, orphan-scan union read, classify RELAY class; spec = `tests/test_id_ecosystem.sh`); (b) upstream an `ownership` field into `project_manager` scan.py (currently fables-turn classifies own-vs-clone in a sibling script); (c) promote the review-mode anti-gaming recipe from prose to a script after 2 review turns prove the pattern (N=2). <!-- id:62f5 -->
- [ ] **fables-turn relay improvement meeting** — agenda prepared 2026-06-14, to run in a separate `/meeting` session: `docs/meeting-notes/2026-06-14-0930-relay-improvement-agenda-PREP.md`. Items: adversarial/plural review verification (id:2909, highest — anti-gaming is the relay's reason to exist over plain Ralph); hybrid mechanical+model discover (id:23fe); review→handoff meta-learning + cross-repo batching (id:4678); tests-as-spec anti-hardcode gate via mutation/property testing (id:2a94); HANDBACK/orphan-worktree GC + value-floor stop condition (id:5081); hot-update/live-reconfigure a running relay-loop + Fable auto-probe (id:5879).
  - **Reconciled 2026-06-15 (overtaken-by-events; meeting still pending for the rest):** (a) id:5081 — orphan/stale-worktree GC HALF SHIPPED as id:3ac8 (discovery auto-reaps dead-run empty worktrees, surfaces commit-bearing ones; first-run-verified on puzzle-pwa); the **value-floor stop condition** half of 5081 remains for the meeting. (b) id:5879 — the **Fable auto-probe** half SHIPPED (`scripts/probe-fable.sh`, front-door once/2h cache); the **hot-update/live-reconfigure-a-running-loop** half remains. (c) Adjacent: the HARD-execute verdict + Opus-apex posture (id:da26) shipped, partially covering the "what does the pool do while Fable is out" concern behind id:2909's framing. (d) New low-latency-pickup work (id:fb75 resolver→inject, id:6e9d freed-lane mid-round inject) shipped this session — not on the original agenda but the same Ralph-vs-relay loop-tightening theme. STILL FULLY OPEN for the meeting: id:2909 (adversarial/plural review — the top differentiator, unbuilt), id:23fe (hybrid discover; interim cheap-pin = id:22ef), id:4678 (meta-learning batching), id:2a94 (anti-hardcode mutation gate), + the two residual halves above. <!-- id:2909 --> <!-- id:23fe --> <!-- id:4678 --> <!-- id:2a94 --> <!-- id:5081 --> <!-- id:5879 -->
  - **id:2909 MEETING HELD 2026-06-15** (`docs/meeting-notes/2026-06-15-1610-adversarial-review-anti-gaming.md`). Decisions: defer the plural refuter fleet (zero recorded gaming incidents — observe-before-preventing); build gaming-canary corpus (two tiers: Tier A bash + Tier B model harness); extract mechanical checks to `gaming-scan.sh`; flag-rate logger to `~/.claude/logs/relay-gaming-flags.log`. Four action items minted below (id:fa05/dfaf/414a/3826).
- [ ] **Discovery step inherits the session model (Opus) — pin it cheap** — relay-loop.js the per-round discovery `agent()` (classifier) has NO `model:` override, so it runs on the session model (Opus on a `--strong-tier opus` run), scanning 30+ repos every round; write-status/quota/integrate are already pinned haiku/sonnet. Classification is purely mechanical (4 deterministic rules over `git tag/log`, ROADMAP `[ROUTINE]` grep, `status --porcelain`, standin grep) with only the "acceptable-dirt" relay.toml-comment check needing any nuance. Cheap interim: add `model: 'sonnet'` (haiku if the acceptable-dirt judgment proves robust) to the discover opts — cuts per-round cost immediately. Full fix = the hybrid mechanical+model discover (id:23fe) / deterministic `discover-repos.sh` emitting DISCOVER_SCHEMA JSON. Do NOT cache verdicts in relay.toml (staleness — git is the source of truth; re-running cheap deterministic discovery each round is both correct and fast). Surfaced 2026-06-15. **DEPRIORITIZED 2026-06-15:** id:3ac8 added a *consequential* `worktree remove --force` decision to the discover agent, so a blind cheap-model pin is now risky (don't put reaping on haiku). Prefer parallel-shard (id:9ed4) + deterministic scanner (id:23fe), which remove the model from the hot path entirely rather than just cheapening it. <!-- id:22ef -->

- [ ] **Discover-speedup track (user-prioritized 2026-06-15: "discover always takes so long")** — the per-round discovery is ONE Opus agent (no `model:` override, relay-loop.js:425) sequentially scanning 30+ repos (fetch + sync-guard + worktree-reconcile/reap + classify + 8 per-repo fields) plus a global prelude (runId, `inject.sh take`, `claim.sh peek`). LLM-in-the-loop = the latency AND cost. Three composable levers, in build order:
  - **(1) ✅ SHIPPED 2026-06-15 — parallel-shard the LLM scan** <!-- id:9ed4 --> (relay-loop.js: a once-only `discover-prelude` agent does runId + the consuming `inject.sh take` + `claim.sh peek` + own-repo list + non-own skipped; then N round-robin `discover-shard` classifiers fan out via `parallel(chunks.map(...))` and merge into the same discovery object. `DISCOVER_SHARDS` knob, default 6. `tests/test_relay_discover_shard.sh`, suite 44 green.) — split the per-repo classification across N cheaper agents instead of one sequential Opus pass. REQUIRES first extracting the **global prelude** (generate runId once; `inject.sh take` is *consuming* so it must run EXACTLY once; `claim.sh peek` once) out of the per-shard work, then fanning out shard agents (each given the shared runId + peek result + its repo subset), then merging `{units, surfaced, skipped}`. Biggest latency cut for least rewrite; ship first. Red test: assert the prelude (inject take / runId) runs once and discovery fans out over repo shards.
  - **(2) Incremental HEAD-keyed cache (the SAFE caching)** <!-- id:bc39 --> — cache per-repo `{HEAD, dirty-hash, claim-state}` from last round; a repo whose tuple is byte-identical reuses its cached verdict, only CHANGED repos are re-classified. Invalidation-keyed on actual git state, NOT time (this is why it's safe where id:22ef's reject-verdict-caching warning does not apply — that warned against *time/blind* caching). Skips the bulk of repeat work in steady state. Red test: a repo with unchanged HEAD/dirty/claim is not re-scanned; any change invalidates.
  - **(3) Deterministic scanner = the real fix** <!-- id:23fe --> — extend the EXISTING `scripts/discover-repos.sh` (already emits ownership/dirty/last-ckpt TSV in seconds) to emit the full verdict JSON (DISCOVER_SCHEMA): the 4 classification rules + the 3ac8 worktree reconciliation become tested shell, the LLM is left only the one irreducible NL bit (acceptable-dirt per relay.toml comment) or dropped. Removes the model from the hot path AND de-risks 3ac8's reaping into tested code. Same id as the Wiggum-agenda item. Red test: discover-repos.sh emits schema-valid verdict JSON matching the LLM classifier on a fixture repo set.
  - **(also ✅ SHIPPED 2026-06-15) status-write off the critical path** <!-- id:cb50 --> — the Haiku `write-relay-status` agent was `await`ed between discover→dispatch and at round end, so the next discover/dispatch blocked on a pure visibility side-effect (user caught the pool sitting on just a write-relay-status when discovery could already proceed). Now `scheduleStatusWrite` snapshots the content and queues the write on a serialized `statusTail`; the pool proceeds immediately and the run flushes the tail once before returning. `tests/test_relay_status_offcrit.sh`.
  - **Build deliberately, NOT into a running pool** — discovery is the single most critical step; each lever lands with its red test, relay-loop.js edits affect only the NEXT launch, and the current pool finishes on the old code. Sequence 1→2→3 (cb50 done). <!-- id:9ed4 --> <!-- id:bc39 -->

- [ ] **[DESIGN/meeting] Connectivity-resilient relay — craft a deterministic workflow per session, then run mostly WITHOUT live cloud dependency** — **Motivation (user 2026-06-15): "that incident shows how volatile cloud-AI dependency is."** A 2-hour pool run = hundreds of live `agent()` cloud calls; a single network drop killed discover-shard:6 + a status write mid-run, and a deterministic `new Date()` bug crashed the whole pool. Every per-step cloud round-trip is a failure point. **Direction:** have the STRONG turn *craft a deterministic workflow per session* (plan/decide once, cloud-side), then *execute* it with as few live cloud `agent()` calls as possible:
  - **Mechanize the mechanical steps** → tested shell, no LLM in the hot path: the deterministic `discover-repos.sh` emitting verdict JSON (id:23fe), a deterministic `relay-integrate.sh` (merge→tag→push→prune as shell, frees a slot + removes a cloud call per integration), the HEAD-keyed discovery cache (id:bc39).
  - **Local-model fallback for the irreducibly-LLM steps** when cloud is down/volatile — review/handoff judgment via a local model ([[local-llama-subagent-architecture]] B-spine; spike id:95e3 gates the wrapper). Degrade-not-die: cloud blip → fall back to local, not crash.
  - **Lean on resume** for crash/drop recovery: `Workflow({scriptPath, resumeFromRunId})` replays the cached agent prefix; pair with the relay's external-state mutation (resume-safety — fresh-relaunch vs resume tradeoff, noted 2026-06-15).
  - **Open questions** (route to a /meeting): which steps are *irreducibly* cloud-LLM vs mechanizable; the local-model quality bar for discover/review; how "craft once, run deterministically" reconciles with the pool's side-effecting git/worktree mutation; whether a session crafts ONE big workflow or the strong turn keeps emitting smaller deterministic units. Connects: id:23fe, id:95e3, the Date-crash guard (test_relay_no_date_api.sh), the failed-shard surfacing (953cf2e). <!-- id:cfa9 -->

- [ ] **Patient stop for the autonomous pool — "finish current batch, drop the queue, don't re-discover"** — gap confirmed by user 2026-06-15: the pool has only (a) NATURAL termination (2 dry discoveries / quota cap / MAX_ROUNDS) and (b) `TaskStop` HARD-kill, which aborts mid-round and abandons in-flight work → leftover handback worktrees (the "I had to quit it" + reap-debt UX). "Graceful" in the code only ever meant *clean termination on drain/quota/failure*, NOT an operator-requested wind-down. We LACK a **patient stop**: finish the current round's dispatched wave + its integration drain (the "current batch"), DROP the queued-but-not-dispatched units, and exit WITHOUT re-discovering. Design: a sentinel the `discover-prelude` (it runs shell each round) checks and returns `stopRequested` — e.g. `~/.config/fables-turn/relay-stop` or a relay.toml flag; the outer loop exits after the current `runRound()` drains instead of looping. Surface as a front-door `--once` (single round then stop) + a `/relay stop` that touches the sentinel for a live pool. Resolves the self-feeding-vs-operator-intent mismatch. <!-- id:3b1e -->

- [ ] **Guard relay-loop.js against the pool's own Workflow-sandbox bugs** — 2026-06-15 the autonomous pool crashed/degraded THREE times from its executor/review children editing the Workflow engine (`relay-loop.js`) with code the static tests can't validate against the LIVE Workflow runtime: a partial id:8c35 (unwired), `new Date()` (id:3826, db5ec43), and `process.env.HOME` (id:3826, a8fe504) — the last two threw synchronously out of `integrate()` and killed the whole pool at the first REVIEW integration. `tests/test_relay_no_date_api.sh` now statically flags Date/process/require/fs/Math.random, but that's per-class whack-a-mole. Deeper fix: either a **runtime smoke check** ("does relay-loop.js start + run one no-op round under the Workflow shim?") gating any merge that touches it, OR treat `relay-loop.js` as a **protected file** only attended sessions edit (executors hand back relay-engine changes instead of committing them). Connects id:cfa9. <!-- id:e37b -->

- [ ] **Verify the `id:da26` remainder after the next dotclaude-skills review/executor loop** (user 2026-06-15) — the da26 "REMAINING" work (deep `-d`/flag-matrix cleanup + relay-loop.js `elevation→lowest-prio-bonus` reframe) lives as a NOTE *inside* id:da26, not its own ROADMAP line, so D6 reverse-handoff probably won't extract it. dotclaude-skills has unaudited commits → the next `/fables-turn` WILL review it; afterwards CHECK: did reverse-handoff promote the remainder to ROADMAP and an executor/`hard` unit pick it up, or did it fall through? If it fell through, split the remainder into its own `[HARD]` ROADMAP line so the pool sees it. <!-- id:e0c1 -->

- [ ] **Octopus-merge consolidation for independent parallel worktrees finishing close in time** — when the serialized integrator has ≥2 verified worktree branches that are mutually NON-CONFLICTING and completed within a short window, consider a single `git merge --no-ff branchA branchB branchC` (octopus) → ONE integration commit + ONE checkpoint + ONE push, instead of N sequential `--no-ff` merges + N tags + N pushes. Benefits: less RELAY_LOG/tag noise, fewer pushes, faster drain. Decision-call / constraints: (a) the octopus strategy REFUSES on any conflict, so it's only valid for provably-disjoint file sets — the SAME independence test that licensed running them in parallel; (b) loses per-track checkpoint granularity (relay tags per-track today) — a single bad track can't be cherry-excluded post-merge as cleanly, so prefer sequential when any track is unverified/risky; (c) attribution: the combined ckpt message MUST enumerate each track's id + summary. Heuristic: octopus when N≥2 disjoint + each individually gate-verified + close-in-time; fall back to sequential when tracks share files or need independent checkpoints/bisect points. Observed first-hand 2026-06-17 (zkWhale demo-chain): THEME + emoji-svgs + logo were near-independent and finished within minutes — a 3-way octopus would have collapsed 3 merges/tags/pushes into one. (user request 2026-06-17) <!-- id:8691 -->
  - **Field report — zkWhale demo-chain run, 2026-06-17/18 (~20 background agents, directed `/relay --afk`):** Ran ~20 worktree children in waves of 1–3, integrated sequentially (merge → full gate → ckpt-tag → push) ≈20×. Findings: (1) **Conflicts were NOT the bottleneck** — disjoint file-scoping *in the child prompts* prevented nearly all of them; the real tax was the repeated **per-merge integration ceremony**, dominated by re-running the full gate each time (`pnpm test` ~8–15 s + typecheck + lint) plus a push apiece. Octopus's win here is halving *gate-runs + pushes* for co-ready disjoint waves, more than the tag/log noise. (2) **Octopus opportunities genuinely arose ~4–5×** (THEME+emoji-svgs; spinrot+logofix; the two logo rounds; proofglyph+badgesvg) — each ≥2 branches, file-disjoint, individually green, finished within minutes. (3) **The octopus-blockers that actually recurred were a small set of high-churn shared files:** `strings.ts` (almost every UI agent appends copy — distinct keys so git *auto-merged*, but it's the #1 risk), `package.json`/`pnpm-lock.yaml` (new devDeps: vite-plugin-singlefile, jimp, potrace), hot UI files like `ArtifactScreen.tsx` (touched by demo-mount/spinner/emphasis across waves), and the ledgers (`TODO.md`). Plus genuine **dependency chains** forced sequencing regardless of merge strategy (a8c2-wiring after the spinner work; demo-transparent-drop after demo-polish). (4) **Deterministic eligibility predictor that would've worked:** pairwise-intersect `git diff --name-only main..<branch>` across the co-ready candidates; empty intersection ⇒ octopus-safe. (5) **Granularity downside barely bit this workload** — every track was gate-verified *before* merge, so I never needed to bisect/cherry-exclude a track post-integration; a well-formed combined ckpt message preserved the attribution that per-track tags gave. (6) **Recommended impl:** optimistic octopus — `git merge --no-ff b1 b2 …` (git refuses cleanly on conflict, no half-merge), fall back to sequential for the conflicting subset; gate ONCE after the merged result; run `pnpm install` once if any branch touched the manifest (orthogonal to merge strategy but required before the gate). Net: for this run, a file-intersection-gated optimistic octopus would have cut ~5–7 of the ~20 integration cycles (~30%) with negligible downside. (field report 2026-06-18)

- [ ] **Directed `/relay <args>` front-session should spawn children as BACKGROUND agents by default** — user request 2026-06-17. A directed/scoped/`--afk` run (e.g. `/relay . --afk <directive>`) is driven by the strong front session fanning out via the Agent tool (handoff/review C4), NOT the autonomous-pool Workflow. Those Agent spawns should default to `run_in_background: true` so the session feels like the no-arg autonomous pool front-door: work happens in the background, the front session is purely the COORDINATOR (status, serialized integration, checkpoints, summaries) — the only thing surfaced in the front session is coordinator output, not blocking agent transcripts. Today this depends on the operator remembering to background each Agent call; make it the documented default in `references/handoff.md` + `references/review.md` (and the orchestrator invariants §4 "Spawn waves"). Keep foreground only when a child's result is an immediate hard dependency of the very next coordinator step. Connects id:8691 (octopus-merge integration) — together they make a directed run behave like a lightweight pool: background fan-out + consolidated integration. (user request 2026-06-17) <!-- id:1968 -->

- [ ] **Extract the directed-relay worktree boilerplate into a CLI tool** — user request 2026-06-17. A directed/`--afk` `/relay <args>` front session repeats the same bash by hand for every track: `git worktree add ~/.cache/relay/worktrees/<repo>/<track> -b relay/<track> <base>` → `cd … && pnpm install --prefer-offline` (node_modules isn't shared across worktrees) → later, integrate: `git merge --no-ff` → `ckpt-tag.sh` → `git-lock-push.sh --ff-only` → `git worktree remove`/`prune`. Did this ~5× in one session 2026-06-17. Wrap it: `scripts/relay-worktree.sh acquire <repo> <track> [--base main] [--install]` (resolves path + branch name, creates the worktree, provisions deps, optionally takes the `claim.sh` lease) and a teardown/`integrate <repo> <track> -m "<summary>" -l "<model>"` counterpart running the SAME serialized-integrator recipe the pool uses (verify clean → merge --no-ff → ckpt-tag → lock-push --ff-only → prune → release lease). Adequate params: repo, track/branch, base ref, ckpt model label, install-or-not, claim-or-not. COMPOSE the existing `ckpt-tag.sh` / `git-lock-push.sh` / `claim.sh`, don't reimplement. Benefit: fewer hand-typed git calls (each a permission-prompt surface), uniform path/branch conventions, less drift between front-door and pool integrator. Connects id:1968 + id:8691: together these make a directed session a thin scripted pool. (user request 2026-06-17) <!-- id:e79b -->

- [ ] **Pre-scheduled "days-off" → spend the 7-day quota faster on known-idle days (with a reserve floor)** — user request 2026-06-17. The 7-day rolling bucket's whole-window pacing (`RELAY_QUOTA_DECAY_7D` front-loads early, backs off late; `RELAY_QUOTA_THRESHOLD_SEVEN_DAY`/`_SONNET` cap it) assumes the operator might come back any time and want quota left. But the user often KNOWS in advance they won't use Claude Code (e.g. "this weekend I'm away") — on those days the conservative back-off just wastes 7d budget that nobody is competing for. WANTED: a way to **pre-declare idle days/intervals** so during them the pool is allowed to burn the 7d bucket harder (raise the 7d threshold / suppress the decay back-off), while STILL leaving an **adequate reserve** sized to expected usage after the operator returns — never draining to 0 right before they come back. **Design hinge:** the reserve floor is the real parameter — given a known idle interval `[start,end]`, relax the 7d cap *inside* it but keep enough that the window-after-`end` (until the bucket's next reset) still has working budget; the saving is "don't reserve quota for an operator who's provably absent." **Sketch:** a small calendar/intervals source (e.g. `~/.config/fables-turn/days-off.toml` or iCal-ish: `2026-06-20/2026-06-22, reserve=0.20`), read by `quota-stop.sh`'s gate; when `now ∈ an off-interval`, use a relaxed 7d threshold (e.g. burn to `1 - reserve` instead of the decayed cap), reverting automatically outside the interval. **Design qs for a likely `/meeting`:** (a) carrier/format — a TOML intervals file vs a `/relay off <date-range> [--reserve R]` CLI that writes it vs reading the system calendar; (b) how the reserve floor composes with `RELAY_QUOTA_DECAY_7D` (override it inside the interval vs blend) and the 5h bucket (probably untouched — 5h refills fast); (c) per-bucket scope (7d + 7d-Sonnet only) and whether the relaxed cap is absolute or still decays toward the reserve as `end` approaches; (d) auto-expiry + a guard so a stale past-dated off-interval can't silently relax a live run; (e) interaction with the autonomous-campaign id:af30 (the natural consumer — a multi-day unattended campaign over a declared days-off window) and the patient-stop/STOP sentinels (id:d0bc/id:3b1e). Relates to `RELAY_QUOTA_DECAY_7D`, `RELAY_QUOTA_THRESHOLD_SEVEN_DAY[_SONNET]`, `quota-stop.sh`, id:af30 (timed cross-window campaign), id:219b (burnup data to size the reserve empirically). (user request 2026-06-17) <!-- id:2904 -->

- [ ] **Statusline cost should include workflow + subagent token totals (folded in, or a 2nd entry)** — user request 2026-06-17. The Claude Code statusline cost figure appears to count only the main loop, not the tokens spent by spawned `Agent`/Workflow subagents — so a session that fans out many background agents (observed: a directed `/relay --afk` run spawned ~16 background Opus children) badly UNDERcounts true spend. WANTED: the statusline reflects total spend = main loop + all spawned agents + workflows, either (a) folded into the existing `$` cost number, or (b) shown as a second segment (e.g. `$<main> +$<agents>` or `Σ$<total>`). Note the relay already has a live statusline segment (id:15bd: `🔁<round> ✓<done> ⚙<in-flight> Δ$<burn>/h` from RELAY_STATUS.md) and `relay-burn.sh`/`relay-econ.py` already attribute per-agent cost from `profile-run.sh` records — so the data exists; this is about surfacing the AGGREGATE in the always-on statusline, not just during relay runs. Design qs: where the per-agent token usage is readable for the statusline script (agent JSONL transcripts? a running tally file?); main-loop-only vs session-total semantics; how it composes with the existing relay segment so they don't double-count. (user request 2026-06-17) <!-- id:c0bc -->

- [ ] **Per-session cost/token/quota measurement for directed Agent-spawn runs (not just relay-loop workflows)** — surfaced 2026-06-18. `relay-burn.sh`/`relay-econ.py` + the quota-sampling only fire under a relay-loop Workflow (gated on `RELAY_RUN_ID`); a directed `/relay --afk` session that fans out via the **Agent tool** writes NO `quota-samples.jsonl` and isn't captured by `relay-econ.py` (its records come from `profile-run.sh` workflow runs). So a heavy directed session is invisible to the cost/quota tooling. WANTED: a measurement path that works for Agent-spawn sessions. **Reusable recipe proven this session** (works with zero new infra): aggregate `message.usage` (`input_tokens`/`output_tokens`/`cache_read_input_tokens`/`cache_creation_input_tokens` + `message.model`) from (a) each spawned agent's transcript `…/tasks/<id>.output` JSONL **and** (b) the main session JSONL `~/.claude/projects/<proj>/<session-id>.jsonl`; price per-tier (Opus $15/$75 per Mtok in/out, cache-read 0.1×, cache-write 1.25×; Sonnet $3/$15). Wrap as e.g. `scripts/session-cost.sh [session-id]` → per-model table + total + cache-savings line. Pair with id:c0bc (statusline aggregate) — same data source. Also: have a directed run call `relay-burn.sh sample` at start+end so the actual `seven_day`/`seven_day_sonnet` utilization delta IS captured (the only way to get a TRUE % of weekly quota vs an API-$-equivalent estimate). **Seed data point (this session — zkWhale demo-chain, ~20 Opus agents + 1 octopus merge, 2026-06-17/18):** measured **≈ $817 API-equiv** (agents ~$408, main loop ~$409; ~$806 Opus + ~$10 Sonnet); **~395 M tokens processed of which 384 M (97%) was cache-read** at 0.1× — only ~10.5 M fresh, ~1.55 M generated output; **prompt caching saved ~$5 k (~7×** vs no-cache ~$5.8 k); est. **~15–25% of a Max-20× weekly Opus allowance** (≈ 4–6 such sessions/week to drain it). This is the kind of number the tooling should produce automatically. **Per-project breakdown (extension, user request 2026-06-18):** the report must also attribute cost/tokens PER PROJECT/REPO, not just a session total — essential for multi-project sessions like the autonomous `/relay` no-args pool (one run fans agents across many repos). Each spawned agent's repo is knowable from its worktree path (`~/.cache/relay/worktrees/<repo>/<track>`) / cwd / the repo it committed to (and the pool already tags each child with its repo in `relay-events.jsonl` + the claim); group the per-agent `message.usage` by that repo → a per-project cost/token table + the cross-project total. The main-loop (orchestrator) cost is shared overhead — report it as its own line, optionally amortized across the projects it served. Output shape: `session-cost.sh` → per-repo rows + an "orchestrator/shared" row + grand total, so a `/relay` run shows "repo A $X, repo B $Y, … orchestrator $Z". (user request 2026-06-18) <!-- id:6563 -->

## model quality probe

- [ ] **Three-axis cluster extractor** — script that pulls `(turn_index, cumulative_input_tokens, ts, gap_since_prev)` from a session `.jsonl` and marks supplied error turns; run on session `bf9dd9e5` + yesterday's last big `/relay` session. Contract: output a per-error table on all three axes (context depth, elapsed wall-time, idle-gap-before). See `docs/meeting-notes/2026-06-17-0836-opus-degradation-investigation.md` D4. <!-- id:903a -->
- [ ] **[investigation step] Run cold fixed-prompt probe** re-posing incidents #2 (confident-wrong "zkm-* on another machine") and #3 (over-engineered ~/.claude branch-split) against fresh Opus; record pass/fail vs the recorded incident behaviour. See `docs/meeting-notes/2026-06-17-0836-opus-degradation-investigation.md` D1. <!-- id:e3c0 -->
- [ ] **[investigation step] Check Anthropic status / model-version for the 2026-06-16 evening window** (free read — status page + community reports). Record finding in `docs/meeting-notes/2026-06-17-0836-opus-degradation-investigation.md`. <!-- id:241c -->
- [ ] **Seed + close id:dba3** — once id:c345 + id:040a land, run 5 cold seed runs/tier under the pre-registered band (D3: ≥2 items / 2 consecutive gate; tok/s advisory ±30%; committed 2026-06-17-0905 before any data); fill per-tier baselines; then close id:dba3 with the finding written into `docs/meeting-notes/2026-06-17-0836-opus-degradation-investigation.md`. (band formula pre-registered; seeding still gated on id:c345 + id:040a) <!-- id:23e9 -->
- [ ] **[MEETING] Sandboxing: run Claude / `/relay` as a separate OS user** — so auto-mode can't break anything beyond that user's permissions (group setup on `~/src`, write-isolation, polkit interaction). Motivated by the probe's invocation design (see `docs/meeting-notes/2026-06-17-0905-model-probe-tos-and-band.md` D1); broader scope warrants its own design meeting. <!-- id:d0c0 -->

## global CLAUDE.md / output style

- [ ] **Add an output-style rule to global `~/.claude/CLAUDE.md`: no clickbait/teaser prose in working sessions** — owner `/feedback` 2026-06-18: a code session must not emit content-free suspense sentences like *"Both engines agree on the splittable/badge mechanics — but with a critical asymmetry."* — a teaser that asserts something notable exists (a "critical asymmetry") WITHOUT naming it or its evidence on the same line. Diagnosed as Opus over-trained on clickbait register. **Proposed rule** (owner to ratify exact wording — CLAUDE.md is curated): under a reporting/communication heading, "State the finding, never tease it. Don't write a standalone sentence claiming significance ('a critical X', 'the surprising part') without immediately, in the same breath, giving the concrete thing and why. Lead with the fact; every significance-claim carries its evidence inline. No hype sentences before a table/finding." Already captured durably as the toesnail-session feedback memory `no-clickbait-teaser-prose`; this TODO is to promote it into the always-loaded global CLAUDE.md so it isn't recall-dependent. Decide placement (likely under the existing "Reporting work" section) + whether it belongs in global CLAUDE.md vs an output-style doc. <!-- id:6ac6 -->

## Done
placeholder
