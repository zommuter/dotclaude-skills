# TODO

## meeting skill

- [x] **Persona-state write step in `meeting/SKILL.md`** — DONE 2026-06-03: added step 2d (gated on `docs/meeting-notes/persona-state.yml` presence, skip Class 1/2) + `meeting/persona-state.py` helper (deterministic valence, last-5 truncation, affinity running-sum, atomic YAML + `web/persona-state.json` mirror); 8 pytest tests green in `~/src/meeting-rpg/tests/test_persona_state.py`. See `~/src/meeting-rpg/docs/meeting-notes/2026-06-03-1556-todo-classifier.md`. <!-- id:ed25 -->
- [x] **Add `persona-state.py` to `make install`** — DONE 2026-06-04: added to `meeting_FILES`, `meeting_EXEC`, `meeting_ALLOW` in Makefile; switched SKILL.md invocation from `python3 …` to direct shebang (`~/.claude/skills/meeting/persona-state.py`); `make install-allowlist` added 4 missing bare-path entries (the `*`-suffix forms were already hand-added). Contract met: fresh `make install` wires persona-state.py. <!-- id:9460 -->
- [x] **Throwaway proxy spike** — DONE 2026-06-04: `meeting/contrib/llm-proxy.py` (stdlib-only, manual chunked decode, TCP_NODELAY, port 61842). Contract met: multiple SSE turns completed `fully_consumed=true`; `usage` logged with `cache_read/cache_creation/thinking_tokens` breakdown. Three fixes shipped: (1) strip `Accept-Encoding` to prevent compressed forwarding, (2) manual chunked decode to avoid `fp.read1()` blocking forever on keep-alive after terminating chunk, (3) `TCP_NODELAY` to prevent Nagle coalescing on loopback. Abandon-or-promote decision pending. See `docs/meeting-notes/2026-06-03-1452-llm-proxy-token-ctx-and-persona-split.md`. <!-- id:e905 -->
- [ ] **Deferred — agent-persona-separation trigger** — revisit when (a) a logged meeting shows persona convergence weakening a decision, OR (b) meeting-rpg agent/broker infra is consumable by `/meeting`. Natural home: meeting-rpg. See `docs/meeting-notes/2026-06-03-1452-llm-proxy-token-ctx-and-persona-split.md`. <!-- id:36be -->
- [ ] **`/meeting-cross` skill — implementation** (gated on AI-5 backmerge OR 2026-06-28 re-eval). **Architecture resolved 2026-05-28 (D1–D4):** β split + composition — new `/meeting-cross` skill; shared `classify.sh` lifted from canonical; dispatch via Skill-tool invoke of canonical `/meeting <subject>` with `MEETING_ROOT_OVERRIDE` env var; inline dispatch v1 default; ctx-bloat instrumented (250k threshold). Inputs: all `include.toml` projects' TODO.md + 30-day meeting notes; connection detection MVP: path-reference grep + `see ~/src/<other>/...` parsing; `proj` data access TBD. See `docs/meeting-notes/2026-05-28-1138-meeting-cross-architecture.md`. <!-- id:4f01 -->
- [ ] **`/meeting-cross-implementation` session** — at AI-5 backmerge (or 2026-06-28 re-eval): extract `classify.sh` from canonical `/meeting/SKILL.md` no-arg branch + build `/meeting-cross/` (SKILL.md, P2 symlinks, allowlist) + `MEETING_ROOT_OVERRIDE` setup-step-1 change + routing-trail-note shape. Contract: cross-skill invokes canonical via Skill tool against 2-project test set. See `docs/meeting-notes/2026-05-28-1138-meeting-cross-architecture.md`. <!-- id:c3fd -->
- [ ] **2026-06-28 re-eval checkpoint** — if AI-5 backmerge has not yet fired, decide: (a) build /meeting-cross against sibling-topology (eat drift) or (b) extend deferral with new explicit gate. Contract: re-eval recorded in a meeting note. See `docs/meeting-notes/2026-05-28-1138-meeting-cross-architecture.md`. <!-- id:b4fd -->
- [ ] **Instrument first 3 /meeting-cross runs with cost-of.sh** — log combined-session token cost after dispatch; if any >250k tokens, design handover-file mechanism before further cross-meetings. See `docs/meeting-notes/2026-05-28-1138-meeting-cross-architecture.md`. <!-- id:b427 -->
- [ ] **Citation-logging hook design** (citation-rate-triggered): discoveries.md IS being used — audit confirmed 7 citations across 17 notes (~0.4/meeting). Design a Stop hook that reads transcript, matches surfaced entries, appends to `~/.claude/logs/discovery-citations.log`. **Sole gate (revised 2026-06-03):** open `/meeting citation-logging-hook` when an eye-audit finds **>5 surfaced/cited discoveries per meeting on average**. Measurement: count `## Surfaced discoveries` section entries + inline recall references across recent notes, divide by notes scanned. Re-audit checkpoint: 2026-12-01 (or sooner if citations visibly spike). Entry-count trigger (100 entries) removed — payoff-blind proxy fired at 140 entries while citation rate stayed at 0.4/meeting. Current state: 140 entries / 259 lines / ~0.4 citations per meeting. Pre-gate action (standardize `## Surfaced discoveries` in `format.md` template) shipped 2026-06-01. See `docs/meeting-notes/2026-06-01-1331-discoveries-citation-audit.md` and `docs/meeting-notes/2026-06-03-1402-citation-logging-gate-respec.md`. <!-- id:ab70 -->
- [x] **Shared skill-file retrieval helper** — DONE 2026-06-04: `meeting/retrieve-top-k.py` (52 lines, stdlib-only, no `import zkm`); chunks by `--chunk-sep` pattern, POSTs to `EMBED_ENDPOINT`, cosine top-K, emits blocks to stdout; exit 1 on failure (caller falls back to full read). Wired into SKILL.md step 5 for with-subject mode; Makefile + allowlist (8 forms). See `docs/meeting-notes/2026-06-01-1558-user-profile-context-strategy.md`. <!-- id:040c -->
- [x] **`find-todos.sh` — skip git worktree roots** — **RESOLVED 2026-06-03**: pipe results through `.git` file check; worktree-rooted TODO.md files are now silently skipped. N=2: meeting-rpg `wt/` worktrees + any future multi-worktree project.
- [ ] **GH-issue audit + sub-repo discovery in orphan check** (design recorded, build trigger-gated): **Trigger:** open `/meeting gh-issue-audit-impl` on the first open GH issue OR outside PR in the root owner's `<user>/*` namespace. Verified baseline 2026-06-03: 6 zkm plugin repos — 0 open issues, 0 outside PRs. Design sketch (see `docs/meeting-notes/2026-06-03-1430-gh-issue-audit-subrepo-design.md`): (F1/placement) new sibling `gh-orphan-scan.sh` — NOT a flag on orphan-scan.sh (keeps orphan-scan offline/sub-second); (F2) extend `find-todos.sh` `.git`-walker + `git remote get-url origin` owner-match, same-namespace rule, origin only; (F3/allowlist) `gh` called only from inside the script with fixed read-only invocation `gh issue list --repo <r> --state open --json number,title,body`, allowlist the script path only via 8-form generator; (correlation) informational-only read for v1 — id-correlation and issue-write fold into F3 at topology trigger. **Reuse-pointer:** `find-todos.sh` IS the F2 `.git`-walker primitive; F2 = ~3-line extension of it. Also: extend stray-TODO `find` heuristic (D2/D3) to use `.git`-ancestor walker when F1–F3 ships — same primitive. See `~/src/zkm/docs/meeting-notes/2026-05-13-1915-per-plugin-todo-topology.md`. <!-- id:0e3a -->
- [ ] **Revise effort-estimate table in `format.md`** — now that `was_meeting` detection is fixed, re-run `meeting-cost.log` analysis after ≥10 genuinely-detected meeting sessions accumulate; update the v0 table in `format.md` with real data. Blocked on: `was_meeting=true` log entries accumulating post-fix.
- [ ] **Revisit S1+S2 and S3 adoption on 2026-08-13**: check S2 dedup adoption rate + `proj grep` invocation count; retire S3 if zero uses; re-tune S1 threshold if any `TODO.archive.md` >200 lines. (Orphan from `docs/meeting-notes/2026-05-13-1950-global-todo-skill.md`.)
- [ ] **Meeting-note archival skill** (volume-triggered): when any `<root>/docs/meeting-notes/` reaches ≥50 `.md` files, open `/meeting meeting-note-archival`. Design candidate: move notes >3 months old to `<root>/docs/meeting-notes/archive/YYYY-QN/`; orphan-scan skips `archive/**`. Mirrors archive-done.sh shape. See `docs/meeting-notes/2026-05-14-1015-skill-ctx-bloat-audit.md`.
- [ ] **`git-diary-workflow` SKILL.md size audit** (forward-flag, no design yet): currently 166 lines; mandatory after every substantive prompt. Trigger: growth past 200 lines OR a prompt-bloat episode traced to this file. Then open `/meeting git-diary-workflow-trim`. See `docs/meeting-notes/2026-05-14-1015-skill-ctx-bloat-audit.md`.
- [ ] **profile-active.sh flip gate** — flip SKILL.md step 6 to `--filter` when: (a) logged active/full ratio ≤ 0.60 confirmed over ≥5 meetings (check `~/.claude/logs/meeting-profile-active.log`), AND (b) setup-ctx is a material fraction of meeting tokens (per `cost-of.sh`). Current baseline: ratio=0.26 at 656 lines. Contract: both conditions met, entry in meeting note. See `docs/meeting-notes/2026-06-01-1558-user-profile-context-strategy.md`. <!-- id:f8f1 -->
- [ ] **At next helferli meeting**: note whether `Write(docs/meeting-notes/...)` prompt fires; if it does, investigate (likely tilde-vs-absolute mismatch variant). Contract: result noted in next helferli meeting note. Orphan from `docs/meeting-notes/2026-05-14-1739-class1-cross-repo-verify.md:28`.
- [ ] **Caching for orphan-scan.sh** (condition-triggered): `~/.claude/logs/meeting-orphan-scan.log` shows helferli ~1200ms, zkm ~2600ms median — both ≥1s gate. Open `/meeting orphan-scan-caching` when zkm/helferli runtime grows further OR the scans cause perceptible friction. See `docs/meeting-notes/2026-05-14-1803-orphan-scan-accumulation.md` gate-(a) analysis.
- [ ] **Orphan-scan observation window** — re-enabled in ADVISORY mode in both SKILL.md files 2026-05-21. FP ~0 by construction; observation window: N meetings with zero spurious candidates, then drop ADVISORY caveat and treat as authoritative. See `docs/meeting-notes/2026-05-21-0934-orphan-scan-fb-hash-id.md`. <!-- id:f2d4 -->
- [ ] **AI-5 savings gate — run 2 remaining MVP-Live pilots, decide default-on** — gate re-scoped 2026-06-03: broker code is now in canonical `/meeting` (Shape B: lazy broker spec loaded via `broker-mode.md`); this gate only decides whether broker mode becomes the *default*. Measure with `bash ~/.claude/skills/meeting/cost-of.sh <session-id>` vs a comparable canonical meeting; if ≥5k tok/meeting savings confirmed across ≥3 total pilots (1 done 2026-06-01), mark broker mode as recommended default in SKILL.md. Launch: `~/src/meeting-rpg/meeting-rpg <topic>` with `MEETING_LIVE=1`. See `docs/meeting-notes/2026-06-03-1529-meeting-live-backmerge.md` and `docs/meeting-notes/2026-06-01-1349-ai5-live-meeting-pilot-preflight.md`. <!-- id:7b4c -->

- [x] **broker-curl round-trip gate for meeting-live/ deletion** — DONE 2026-06-03: PASS recorded in `docs/meeting-notes/2026-06-03-1529-meeting-live-backmerge.md` (session 8520676a); broken symlink `~/.claude/skills/meeting-live` removed; stale allowlist entry cleaned from settings.json. <!-- id:54ba -->

- [ ] **AI-4 (worktree TODO.md scope)** — after ≥1 live `/meeting` run from a `meeting-rpg` worktree, log whether autonomous upward traversal of TODO.md recurs. If yes, escalate to mechanical guard design (D5 follow-up). See: `docs/meeting-notes/2026-05-20-2105-todo-md-root-resolution-worktrees.md`.

- [ ] **β: flock `append.sh` writes** — wrap `append.sh` write in `flock` so two concurrent calls to the same target never interleave lines (`meeting/append.sh`). Contract: concurrent append.sh calls produce clean separate entries. See `docs/meeting-notes/2026-06-03-1613-parallel-session-state-coordination.md`. <!-- id:9cfb -->
- [ ] **β: `merge=union` for tracked append-logs** — add `.gitattributes` `merge=union` for `personas.md`@dotclaude-skills, `DIARY.md`@claude-diary, MEMORY.md indices. Contract: concurrent-branch appends union-merge with no conflict markers. See `docs/meeting-notes/2026-06-03-1613-parallel-session-state-coordination.md`. <!-- id:fbcd -->
- [ ] **γ: `shard` + `collapse` for persona-state.py** — add `shard` (append session delta to gitignored `persona-events/<session-id>`) and `collapse` (flock'd fold all shards → unified yml/json, GC shards) subcommands; update SKILL.md step 2d. Contract: N concurrent meetings each record delta; final sums = Σ all deltas; no shard files remain after collapse. Zero AI tokens. See `docs/meeting-notes/2026-06-03-1613-parallel-session-state-coordination.md`. <!-- id:5f60 -->
- [ ] **δ: flock'd deterministic merge helper** — `flock`'d merge helper for `TODO.md` (keyed by `<!-- id:XXXX -->`) + `user-profile.md` (keyed by `## section`); apply structured delta rather than blind whole-file rewrite; wire into `todo-update` + `/meeting` end-of-meeting writes. Contract: concurrent sessions editing different items both survive; same-item serializes. See `docs/meeting-notes/2026-06-03-1613-parallel-session-state-coordination.md`. <!-- id:42f4 -->
- [x] **Create `docs/meeting-notes/persona-state.yml` for dotclaude-skills** — DONE 2026-06-04: seeded with standing personas (archie/riku/petra); added to .gitignore; fixed persona-state.py to skip JSON mirror when `web/` absent (no-op for non-meeting-rpg projects). Contract met: step 2d will run at end of next dotclaude-skills meeting. See `docs/meeting-notes/2026-06-03-1613-parallel-session-state-coordination.md`. <!-- id:1178 -->
- [x] **Fix /meeting interactive-protocol self-bug** — DONE 2026-06-04: SKILL.md step 5 already updated in db8e43b (same-turn constraint added); format.md Interactive mode step 2 also updated for consistency (explicit "same response" requirement). See `docs/meeting-notes/2026-06-03-1613-parallel-session-state-coordination.md`. <!-- id:ec72 -->
- [ ] **Design meeting: worktree-per-session for same-repo Class 1 code edits** (D5) — scope, `--no-ff` constraint, lifecycle management. Separate design meeting. See `docs/meeting-notes/2026-06-03-1613-parallel-session-state-coordination.md`. <!-- id:d18d -->
- [ ] **Design meeting: Sonnet/Haiku sub-agent spawning for parallel Class 1 tasks** (D6) — discuss parallelizing impl-ready items across sub-agents. Separate meeting. See `docs/meeting-notes/2026-06-03-1613-parallel-session-state-coordination.md`. <!-- id:cbb5 -->
- [ ] **Investigate: `echo "$EMBED_ENDPOINT"` prompted despite structural parity with `$MEETING_LIVE`/`$MEETING_BROKER_PORT`** — both patterns use simple_expansion; EMBED_ENDPOINT was missing from the global allowlist entirely. Question: are there other meeting-skill env-probes missing from the allowlist? Audit SKILL.md step 5 onward for all `echo "$VAR"` calls and cross-check against `~/.claude/settings.json`. Contract: no meeting-setup step prompts for env-expansion commands.

## Done
