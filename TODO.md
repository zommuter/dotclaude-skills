# TODO

## meeting skill

- [ ] **`/meeting --cross-project` (or new `/meeting-cross` skill)** — design spec in `~/src/project_manager/docs/meeting-notes/2026-05-14-0954-proj-launch-and-meta-meeting.md` (D7). Architecture decision (flag-on-existing vs. separate skill) is the first agenda item of that session. Key concern: cross-project context may bias single-project flow → LLM-induced false execution. Inputs: all `include.toml` projects' TODO.md + 30-day meeting notes. Connection detection MVP: path-reference grep + explicit `see ~/src/<other>/...` parsing. `proj` data access method TBD (direct toml/cache read vs `proj show --json`).
- [ ] **`~/.claude` meetings trigger "allow changing its own settings" prompt** — investigated 2026-05-10-2200 (see `~/.claude/docs/meeting-notes/2026-05-10-2200-settings-prompt-investigation.md`). Prompt text captured verbatim: *"Do you want to create \<file\>? 1. Yes  2. Yes, and allow Claude to edit its own settings for this session  3. No"*. **H3 confirmed** (not H1/H2): the harness detects `~/.claude/` as the config dir and adds option 2 to **any** file write in that tree, not just `settings.json`. **Pre-empted:** `update-config` is a decision skill, not a permission-bypass. **Possible contributing factor:** allowlist entry `Write(~/.claude/docs/meeting-notes/*)` uses tilde but matcher may not expand tilde to absolute path — same class as diary tmp files fix — causing a silent allowlist miss. **Next step:** decide whether to (a) add absolute-path `Write(/home/tobias/.claude/docs/meeting-notes/*)` to test the tilde-miss hypothesis, (b) avoid `~/.claude/` as cwd entirely (meeting topic), or (c) accept option 2 as the intended UX. Open `/meeting` when ready to decide.
- [ ] **Meeting: avoid `~/.claude/` as cwd** — working directly in `~/.claude/` triggers special harness behavior (the "edit own settings" guard) on ANY file write, plus potential tilde/absolute allowlist misses. Is the right fix to (a) always use a different cwd, (b) add absolute-path allowlist entries, or (c) accept it as intended UX? Needs a meeting. **Note (2026-05-11):** meeting notes are now migrated to topic-specific repos (skill notes → dotclaude-skills, diary notes → claude-diary, meta notes remain here); the general cwd question remains open.
- [ ] **Discoveries citation eye-audit** (due **2026-06-05**): re-read `~/.claude/skills/meeting/discoveries.md`; scan meeting notes 2026-05-09 → 2026-06-05 for citations of any entry. If ≥1 citation found: plan citation-logging Stop hook follow-up. If zero citations: treat `(retro)` entries as cull candidates. See `2026-05-08-2058-discoveries-audit.md`.
- [ ] **RAG for discoveries.md** (volume-triggered, not date-triggered): revisit when `discoveries.md` reaches ≥100 entries OR ≥800 lines — open `/meeting RAG-for-discoveries`. BGE-M3 in zkm is the candidate embedder.
- [ ] **GH-issue audit + sub-repo discovery in orphan check** (forward-flag, no design yet): (F1) when auditing orphan action items, query `gh issue list --state open` for the current repo AND any sub-repos (walk `plugins/*/.git` + detect GitHub remotes in same `<user>/*` namespace); (F2) define "sensibly associated repos" — first pass: sub-repos with remotes in the same GitHub user namespace; (F3) once the skill can *read* GH Issues for audit, *writing* issues as action items is a small additional step — folds into the Option 3 topology trigger. See `~/src/zkm/docs/meeting-notes/2026-05-13-1915-per-plugin-todo-topology.md`. Also: extend stray-TODO `find` heuristic (D2/D3 from 2026-05-14-1204-stray-todo-merge.md) to use `.git`-ancestor walker when F1–F3 ships — same primitive. **Note (2026-05-21):** F-B `<!-- id:XXXX -->` is now the canonical meeting↔TODO correlation primitive — any future hash-based-ID mention in this item is satisfied by F-B.
- [ ] **Revise effort-estimate table in `format.md`** — now that `was_meeting` detection is fixed, re-run `meeting-cost.log` analysis after ≥10 genuinely-detected meeting sessions accumulate; update the v0 table in `format.md` with real data. Blocked on: `was_meeting=true` log entries accumulating post-fix.
- [ ] **Revisit S1+S2 and S3 adoption on 2026-08-13**: check S2 dedup adoption rate + `proj grep` invocation count; retire S3 if zero uses; re-tune S1 threshold if any `TODO.archive.md` >200 lines. (Orphan from `docs/meeting-notes/2026-05-13-1950-global-todo-skill.md`.)
- [ ] **Meeting-note archival skill** (volume-triggered): when any `<root>/docs/meeting-notes/` reaches ≥50 `.md` files, open `/meeting meeting-note-archival`. Design candidate: move notes >3 months old to `<root>/docs/meeting-notes/archive/YYYY-QN/`; orphan-scan skips `archive/**`. Mirrors archive-done.sh shape. See `docs/meeting-notes/2026-05-14-1015-skill-ctx-bloat-audit.md`.
- [ ] **`git-diary-workflow` SKILL.md size audit** (forward-flag, no design yet): currently 166 lines; mandatory after every substantive prompt. Trigger: growth past 200 lines OR a prompt-bloat episode traced to this file. Then open `/meeting git-diary-workflow-trim`. See `docs/meeting-notes/2026-05-14-1015-skill-ctx-bloat-audit.md`.
- [ ] **`user-profile.md` ctx strategy** (forward-flag, no design yet): currently 390 lines; loaded every meeting setup. Trigger: exceeds **600 lines** OR meeting setup feels demonstrably slow. Then open `/meeting user-profile-context-strategy`. Candidate approaches: per-meeting topic filtering, RAG, archive low-confidence-untouched entries. See `docs/meeting-notes/2026-05-14-1015-skill-ctx-bloat-audit.md`.
- [ ] **At next helferli meeting**: note whether `Write(docs/meeting-notes/...)` prompt fires; if it does, investigate (likely tilde-vs-absolute mismatch variant). Contract: result noted in next helferli meeting note. Orphan from `docs/meeting-notes/2026-05-14-1739-class1-cross-repo-verify.md:28`.
- [ ] **Caching for orphan-scan.sh** (condition-triggered): `~/.claude/logs/meeting-orphan-scan.log` shows helferli ~1200ms, zkm ~2600ms median — both ≥1s gate. Open `/meeting orphan-scan-caching` when zkm/helferli runtime grows further OR the scans cause perceptible friction. See `docs/meeting-notes/2026-05-14-1803-orphan-scan-accumulation.md` gate-(a) analysis.
- [ ] **Orphan-scan observation window** — re-enabled in ADVISORY mode in both SKILL.md files 2026-05-21. FP ~0 by construction; observation window: N meetings with zero spurious candidates, then drop ADVISORY caveat and treat as authoritative. See `docs/meeting-notes/2026-05-21-0934-orphan-scan-fb-hash-id.md`. <!-- id:f2d4 -->
- [ ] **AI-M4 — Headless pilot (subscribers=0 branch)** — run a second pilot with no renderer attached (or `MEETING_LIVE=0`) to exercise the `subscribers=0` → `AskUserQuestion` fallback and the opt-out env path. Completes γ-branch coverage.
- [ ] **AI-5 — Pilot live meeting + merge trigger** — first pilot ran 2026-05-20 (subject: `/meeting --cross-project`) but was postponed mid-Item-1 due to broker UX gaps (AI-M1–M4). Re-run after AI-4b + AI-M1 + AI-M2 land. On clean success: fold `meeting-live/SKILL.md` deltas into canonical `meeting/SKILL.md`, delete `meeting-live/` sibling, close this TODO. Contract: ≥1 successful live meeting whose chat-side budget (measured via `cost-of.sh`) is ≥5k tokens lower than a comparable canonical-mode meeting. See: `docs/meeting-notes/2026-05-20-1645-meeting-skill-broker-integration.md`.

- [ ] **AI-4 (worktree TODO.md scope)** — after ≥1 live `/meeting` run from a `meeting-rpg` worktree, log whether autonomous upward traversal of TODO.md recurs. If yes, escalate to mechanical guard design (D5 follow-up). See: `docs/meeting-notes/2026-05-20-2105-todo-md-root-resolution-worktrees.md`.

## Done
