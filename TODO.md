# TODO

## meeting skill

- [ ] **`/add-dir ~/src/dotclaude-skills`** — register write-access allowlist entries for the new dotclaude-skills repo so git-diary-workflow Step 1c can commit and push it. Run once from any session. *(deferred again in 2026-05-11 diary-skill-migration meeting — user explicitly asked not to add these patterns)*
- [ ] **Add Write allowlist for project meeting notes** — `Write` to `<project>/docs/meeting-notes/*.md` triggers a permission prompt (observed: zkm, 2026-05-10-1148); no `Write(~/src/*/docs/meeting-notes/*)` entry in `settings.json`. Per 2026-05-11 allowlist-path-resolution discovery, the correct fix is likely a cwd-relative pattern `docs/meeting-notes/*.md` — verify and add. See `~/src/zkm/docs/meeting-notes/2026-05-10-1148-entity-extraction.md` and `~/src/claude-diary/docs/meeting-notes/2026-05-11-1024-allowlist-path-resolution.md`.
- [ ] **`~/.claude` meetings trigger "allow changing its own settings" prompt** — investigated 2026-05-10-2200 (see `~/.claude/docs/meeting-notes/2026-05-10-2200-settings-prompt-investigation.md`). Prompt text captured verbatim: *"Do you want to create \<file\>? 1. Yes  2. Yes, and allow Claude to edit its own settings for this session  3. No"*. **H3 confirmed** (not H1/H2): the harness detects `~/.claude/` as the config dir and adds option 2 to **any** file write in that tree, not just `settings.json`. **Pre-empted:** `update-config` is a decision skill, not a permission-bypass. **Possible contributing factor:** allowlist entry `Write(~/.claude/docs/meeting-notes/*)` uses tilde but matcher may not expand tilde to absolute path — same class as diary tmp files fix — causing a silent allowlist miss. **Next step:** decide whether to (a) add absolute-path `Write(/home/tobias/.claude/docs/meeting-notes/*)` to test the tilde-miss hypothesis, (b) avoid `~/.claude/` as cwd entirely (meeting topic), or (c) accept option 2 as the intended UX. Open `/meeting` when ready to decide.
- [ ] **Meeting: avoid `~/.claude/` as cwd** — working directly in `~/.claude/` triggers special harness behavior (the "edit own settings" guard) on ANY file write, plus potential tilde/absolute allowlist misses. Is the right fix to (a) always use a different cwd, (b) add absolute-path allowlist entries, or (c) accept it as intended UX? Needs a meeting. **Note (2026-05-11):** meeting notes are now migrated to topic-specific repos (skill notes → dotclaude-skills, diary notes → claude-diary, meta notes remain here); the general cwd question remains open.
- [ ] **Meeting: global TODO skill / cross-project task tracking** — `~/.claude/TODO.md` is accumulating items that span projects and skills. A `todo` skill (add/list/close items from any session) would help, but risks reinventing an issue tracker. Warrants a meeting to scope: what's the minimal useful thing vs. "just use GitHub Issues / Linear"? Meeting first, implement after.
- [ ] **Investigate: built-in "update project memory" mechanism** — user is confident a permission-prompt-free memory update path exists beyond plain Write/Edit. Full deferred-tool scan (all ToolSearch queries: memory/project/save/store/remember/note/persist/facts/context/preferences/knowledge) found nothing. Allowlist already covers `Write(~/.claude/projects/*/memory/*.md)` and `Edit(~/.claude/projects/*/memory/*.md)` — so file writes should not prompt. Hypothesis: the prompt the user saw came from the `git -C ~/.claude add/commit` diary-workflow step, not the memory write itself. **Next step:** confirm by testing a Write to a memory path standalone; if it prompts, investigate tilde/absolute mismatch (same class as diary tmp fix). If no dedicated tool surfaces, consider dropping the git-commit step for memory-only changes and relying on the allowlisted file write alone.
- [ ] **Verify cross-repo writes end-to-end**: run `/meeting` from `~/src/helferli` (or similar), trigger a discoveries.md append → confirm no `Update(...)`/Edit prompt fires; at meeting end confirm git-diary-workflow auto-fires and commits both helferli and `~/.claude`. See `~/src/claude-diary/docs/meeting-notes/2026-05-10-1452-cross-repo-writes.md`.
- [ ] **Discoveries citation eye-audit** (due **2026-06-05**): re-read `~/.claude/skills/meeting/discoveries.md`; scan meeting notes 2026-05-09 → 2026-06-05 for citations of any entry. If ≥1 citation found: plan citation-logging Stop hook follow-up. If zero citations: treat `(retro)` entries as cull candidates. See `2026-05-08-2058-discoveries-audit.md`.
- [ ] **RAG for discoveries.md** (volume-triggered, not date-triggered): revisit when `discoveries.md` reaches ≥100 entries OR ≥800 lines — open `/meeting RAG-for-discoveries`. BGE-M3 in zkm is the candidate embedder.
- [ ] **Verify Class 2 format (live test)**: next time a Class 2 item is dispatched, confirm the produced note has `**Mode:** Class 2 planning record` + new section headings; no permission prompts expected.
- [ ] **Whitelist orphan from 2026-05-10-1545**: `[ ] Verify: diary commit at end of this meeting fires without permission prompt` — close if that session's diary did commit cleanly; otherwise investigate. See `~/src/claude-diary/docs/meeting-notes/2026-05-10-1545-heredoc-shell-expansion.md`.

- [ ] **Verify git-diary-workflow end-to-end** after migration: confirm diary-append.sh still appends to `~/src/claude-diary/DIARY.md` (default path unchanged); confirm git-lock-push.sh runs without permission prompt from new install path. See `docs/meeting-notes/2026-05-11-1520-diary-skill-migration.md`.

## Done

- [x] **Prior meeting orphans** (from `2026-05-08-meeting-skill.md`): memory file existed; added missing no-arg default-behaviour decision 2026-05-10. zkm test item ("session 12 zkm-pdf") closed as superseded by 2026-05-10 dispatch validation — verified by user on 2026-05-10.
- [x] **Verify `/meeting` from foreign cwd** (orphan from `2026-05-08-whitelist-skill-permissions.md`): analytical verification 2026-05-10 — `Read(~/.claude/skills/**)` covers all meeting-skill setup reads; `Write(~/.claude/docs/meeting-notes/*)` covers end-of-meeting write; helferli project settings add no restrictions on `~/.claude/` paths. Global tilde-prefixed allowlist entries are cwd-independent. Live test from helferli optional for empirical confirmation.
- [x] **Publish meeting skill → `zommuter/dotclaude-skills`** (from 2026-05-10-1658 meeting): created `~/src/dotclaude-skills/`, two-commit history (gist baseline → live state), pushed to https://github.com/zommuter/dotclaude-skills, P2 symlinks in place, gist comment added, 6 gist-read allowlist entries added, Step 1c added to git-diary-workflow/SKILL.md. See `docs/meeting-notes/2026-05-10-1658-publish-meeting-skill.md`.
- [x] **Migrate .claude meeting notes to topic-specific repos** (2026-05-11): 9 skill notes → `docs/meeting-notes/`, 4 diary notes → `~/src/claude-diary/docs/meeting-notes/`. `meeting-style.md` (Sage-as-standing) replicated to both. Cross-refs updated in TODO.md + discoveries.md. Future venue is cwd-driven. — completed 2026-05-11.
- [x] **Publish git-diary-workflow skill + scripts** (2026-05-11): SKILL.md, git-lock-push.sh, diary-append.sh moved to `git-diary-workflow/`; P2 symlinks in `~/.claude/skills/git-diary-workflow/`; diary-append.sh parameterised with `DIARY_REPO_DIR`; all caller references updated; allowlist updated. See `docs/meeting-notes/2026-05-11-1520-diary-skill-migration.md`.
- [x] **Meeting skill: use `append.sh` for discoveries/personas, never direct Edit** — bolded IMPORTANT note added to SKILL.md end-of-meeting steps (2026-05-10 publish session). Verify: next meeting fires append.sh without prompt.
- [x] **Meeting persona emoji**: added to all standing and ad-hoc personas; position corrected (emoji before name) — verified by user on 2026-05-10.
- [x] **Validate meeting v3 no-arg dispatch**: ran 2026-05-10 — Class 1 dispatched with two-option prompt (no "not yet"); counts were 6/4/0 (vs projected 3/2/0, projection was stale). Dispatch mechanism confirmed. — verified by user on 2026-05-10.
- [x] **Tier A discoveries scan**: read all `~/src/*/docs/meeting-notes/*.md` outside `~/.claude/` and `~/src/claude-diary/claude.ai-export/memories.json`; append cross-project facts to `~/.claude/skills/meeting/discoveries.md`. 8 entries added (7 from meeting notes; memories.json was a single biographical blob with no new technical facts). See `2026-05-08-2004-discoveries-retro-scan.md` — completed 2026-05-08.
- [x] **JSONL discovery pilot**: 2/10 hit rate (20%, below 30% threshold). 3 retro entries added. Tier B bulk scan deferred indefinitely. Completed 2026-05-08.
- [x] **Investigate `Update(...)` permission prompt on discoveries.md**: superseded 2026-05-10 — replaced direct Edit/Write on discoveries.md and personas.md with `~/.claude/skills/meeting/append.sh` Bash calls; allowlist entries added for both `-e` and `-f` invocation forms. Prompt is bypassed regardless of which tool variant triggered it.
- [x] **Class 2 planning record format**: implemented 2026-05-10 — added Class 2 template to `format.md` and wrote step to SKILL.md no-arg dispatch; `**Mode:**` field replaces `**Attendees:**`; `## Plan / ## Implementation findings` replace `## Discussion`; existing notes not migrated. See `docs/meeting-notes/2026-05-10-1623-class2-format.md`.
- [x] **Add meeting-notes Read allowlist entries**: 2026-05-10 — added `Read(~/.claude/docs/meeting-notes/*)`, `Read(~/src/*/docs/meeting-notes/*)`, `Bash(ls ~/src/*/docs/meeting-notes/*.md)` to `settings.json`.
