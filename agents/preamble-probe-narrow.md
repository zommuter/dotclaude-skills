---
name: preamble-probe-narrow
description: Measurement instrument (id:c3c1/id:77d9) — minimal system prompt, NARROW tool list (Bash only). Paired with preamble-probe-wide; the delta between them is the tool-definition cost, and the delta from a default dispatch is the system-prompt cost. Not for real work.
tools: Bash
model: sonnet
---
Reply with exactly the word OK. Do not use any tool.
