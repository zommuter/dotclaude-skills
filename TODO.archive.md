# TODO Archive

## 2026-05
- [x] **Add Write allowlist for project meeting notes** — added `Write(docs/meeting-notes/*.md)` cwd-relative entry to `~/.claude/settings.json` (2026-05-14). Per allowlist-path-resolution discovery: cwd-relative form is the only reliably matched form. See `~/src/claude-diary/docs/meeting-notes/2026-05-11-1024-allowlist-path-resolution.md`.
- [x] **Meeting: global TODO skill / cross-project task tracking** — meeting held 2026-05-13. Decision: no `/todo` skill; lever-first instead. See `docs/meeting-notes/2026-05-13-1950-global-todo-skill.md`.
- [x] **S4 — migrate todo-update to dotclaude-skills** — verified by user 2026-05-14. Symlink in place; SKILL.md + README.md present; skill loads. See `docs/meeting-notes/2026-05-13-1950-global-todo-skill.md`.
- [x] **S1+S2 — archive-done.sh + todo-update SKILL.md amendments** — implemented and verified by synthetic round-trip on 2026-05-14. `archive-done.sh` archives `[x]` entries ≥30 days old to `TODO.archive.md`; Step 2 grep-before-add + Step 4 archive call added to SKILL.md; Makefile updated; allowlist entries added. Idempotent confirmed.
- [x] **Verify cross-repo writes end-to-end** — analytically verified 2026-05-14: (1) `Write(docs/meeting-notes/*.md)` present in global settings.json (cwd-relative, applies to all projects); (2) `append.sh` allowlisted by absolute path (cwd-independent); (3) `helferli/docs/meeting-notes/` exists; (4) git-diary-workflow Steps 1/1b/1c cover project + `~/.claude` + dotclaude-skills. Live-run confirmation (no prompt fires) deferred to next natural helferli meeting. See `~/src/claude-diary/docs/meeting-notes/2026-05-10-1452-cross-repo-writes.md`.
- [x] **Whitelist orphan from 2026-05-10-1545** — closed 2026-05-14: diary-workflow confirmed working end-to-end (AI-8 verified at 16:49); no regression since. See `~/src/claude-diary/docs/meeting-notes/2026-05-10-1545-heredoc-shell-expansion.md`.
- [x] **Meeting-cost.log 10-entry reminder** — closed 2026-05-14: 98 actual meeting sessions confirmed (grep `**Session:**` across all meeting notes). Threshold far exceeded. No /schedule call needed.
- [x] **Fix `was_meeting` detection in `meeting-cost-logger.sh`** — fixed 2026-05-14: replaced `grep -q 'meeting-notes'` with `grep -qE 'Write\(.*meeting-notes.*\.md'`. Then revise the v0 effort-estimate table in `format.md` using real meeting session data. Script at `~/.claude/hooks/meeting-cost-logger.sh`.
- [x] **Implement `orphan-scan.sh`** — done 2026-05-14. Script at `meeting/orphan-scan.sh`; P2 symlink; allowlist entries; SKILL.md updated (both "With a subject" step 2 and no-arg step 1). Live run: 347ms, 32 cand4 (mostly historical archive noise — expected). See `docs/meeting-notes/2026-05-14-1015-skill-ctx-bloat-audit.md`.
- [x] **Amend `discoveries.md` γ-class entry** — closed 2026-05-14: correction already present in `discoveries.md` (line 133: "(corrects 2026-05-14 .claude γ-class entry) /todo-update empirical rate ~0.81/session..."); append.sh was called during the perprompt-cost meeting session. TODO was stale.
- [x] **Add user-profile entry: "proper-sample-first measurement"** — closed 2026-05-14: entry already present in `user-profile.md` (lines 443–451: "challenges low-n samples when large-n is cheaply available"). TODO was stale.
- [x] **S3 — `proj grep` subcommand** — discovered already implemented in `~/src/project_manager/src/project_manager/cli.py:145–168`; matches TODO.md + TODO.archive.md, case-insensitive, `[ ]` default, `--all` flag, `<project>:<line>` output. Closed 2026-05-14 (verified this session). Action item was only in meeting note, not in TODO.md.
- [x] **Verify git-diary-workflow end-to-end** after migration — confirmed by user 2026-05-11. diary-append.sh appends to ~/src/claude-diary/DIARY.md; git-lock-push.sh prompt-free. 1st-run transient "unstaged changes" error was a race condition, not a structural issue.
- [x] **AI-1 — hooks/ verbatim first commit** — done 2026-05-14. Zero content diff; 3 files in commit 82aeeca.
- [x] **AI-2 — sanity-pass `parallel-edit-detector.py`** — done 2026-05-14. All paths use `Path.home()`; no hard-coded UUIDs. Clean.
- [x] **AI-3 — rename notify-hook.sh → notify-hook.linux-x11.sh** — done 2026-05-14. `git mv` in commit 0071a2c; `git log --follow` traces to verbatim commit.
- [x] **AI-4 — add `hooks/README.md`** — done 2026-05-14. Per-hook purpose + prerequisites + full settings.json snippet. Commit 0071a2c.
- [x] **AI-5 — Makefile `install-hooks` target** — done 2026-05-14. 3 `ln -sf` lines; in default `install`. Commit 0071a2c.
- [x] **AI-6 — top-level README Hooks section** — done 2026-05-14. 3-row table + updated Install block. Commit 0071a2c.
- [x] **AI-7 — replace live files with symlinks** — done 2026-05-14. `ls -la` confirms symlinks to dotclaude-skills.
- [x] **AI-8 — post-symlink verification** — done 2026-05-14. `~/.claude/logs/meeting-cost.log` entry at 16:49 (symlink installed 16:48, confirmed via dotclaude-skills project session); `notify-hook.sh` symlink → `notify-hook.linux-x11.sh` confirmed in place.
- [x] **AI-9 — discard historical orphan** (`2026-05-08-meeting-skill.md:71 "Save key decisions to project auto-memory"`) — closed 2026-05-14: superseded by current memory workflow (auto-memory at meeting end).
- [x] **`git-lock-push.sh` first-push upstream bug** — fixed 2026-05-14: `ls-remote` check before pull (skips rebase when remote branch absent); push loop uses `--set-upstream` on first push. See `git-diary-workflow/git-lock-push.sh`.
- [x] **Makefile + README updates for skill install** — `make install` / `make install-<skill>` / `make DEST_DIR=/path install`; `git-diary-workflow/README.md` created; `meeting/README.md` manual block replaced; top-level README updated with new skill row and Install section. Verified via round-trip test and `make status`. — verified by user on 2026-05-11.
- [x] **Prior meeting orphans** (from `2026-05-08-meeting-skill.md`): memory file existed; added missing no-arg default-behaviour decision 2026-05-10. zkm test item ("session 12 zkm-pdf") closed as superseded by 2026-05-10 dispatch validation — verified by user on 2026-05-10.
- [x] **Verify `/meeting` from foreign cwd** (orphan from `2026-05-08-whitelist-skill-permissions.md`): analytical verification 2026-05-10 — `Read(~/.claude/skills/**)` covers all meeting-skill setup reads; `Write(~/.claude/docs/meeting-notes/*)` covers end-of-meeting write; helferli project settings add no restrictions on `~/.claude/` paths. Global tilde-prefixed allowlist entries are cwd-independent. Live test from helferli optional for empirical confirmation.
- [x] **Publish meeting skill → `zommuter/dotclaude-skills`** (from 2026-05-10-1658 meeting): created `~/src/dotclaude-skills/`, two-commit history (gist baseline → live state), pushed to https://github.com/zommuter/dotclaude-skills, P2 symlinks in place, gist comment added, 6 gist-read allowlist entries added, Step 1c added to git-diary-workflow/SKILL.md. See `docs/meeting-notes/2026-05-10-1658-publish-meeting-skill.md`.
- [x] **Migrate .claude meeting notes to topic-specific repos** (2026-05-11): 9 skill notes → `docs/meeting-notes/`, 4 diary notes → `~/src/claude-diary/docs/meeting-notes/`. `meeting-style.md` (Sage-as-standing) replicated to both. Cross-refs updated in TODO.md + discoveries.md. Future venue is cwd-driven. — completed 2026-05-11.
- [x] **Publish git-diary-workflow skill + scripts** (2026-05-11): SKILL.md, git-lock-push.sh, diary-append.sh moved to `git-diary-workflow/`; P2 symlinks in `~/.claude/skills/git-diary-workflow/`; diary-append.sh parameterised with `DIARY_REPO_DIR`; all caller references updated; allowlist updated. See `docs/meeting-notes/2026-05-11-1520-diary-skill-migration.md`.
- [x] **Meeting skill: use `append.sh` for discoveries/personas, never direct Edit** — bolded IMPORTANT note added to SKILL.md end-of-meeting steps (2026-05-10 publish session). Verify: next meeting fires append.sh without prompt.
- [x] **Meeting persona emoji**: added to all standing and ad-hoc personas; position corrected (emoji before name) — verified by user on 2026-05-10.
- [x] **Validate meeting v3 no-arg dispatch**: ran 2026-05-10 — Class 1 dispatched with two-option prompt (no "not yet"); counts were 6/4/0 (vs projected 3/2/0, projection was stale). Dispatch mechanism confirmed. — verified by user on 2026-05-10.
- [x] **Tier A discoveries scan**: read all `~/src/*/docs/meeting-notes/*.md` outside `~/.claude/` and `~/src/claude-diary/claude.ai-export/memories.json`; append cross-project facts to `~/.claude/skills/meeting/discoveries.md`. 8 entries added (7 from meeting notes; memories.json was a single biographical blob with no new technical facts). See `2026-05-08-2004-discoveries-retro-scan.md` — completed 2026-05-08.
- [x] **JSONL discovery pilot**: 2/10 hit rate (20%, below 30% threshold). 3 retro entries added. Tier B bulk scan deferred indefinitely. Completed 2026-05-08.
- [x] **Investigate `Update(...)` permission prompt on discoveries.md**: superseded 2026-05-10 — replaced direct Edit/Write on discoveries.md and personas.md with `~/.claude/skills/meeting/append.sh` Bash calls; allowlist entries added for both `-e` and `-f` invocation forms. Prompt is bypassed regardless of which tool variant triggered it.
- [x] **Class 2 planning record format**: implemented 2026-05-10 — added Class 2 template to `format.md` and wrote step to SKILL.md no-arg dispatch; `**Mode:**` field replaces `**Attendees:**`; `## Plan / ## Implementation findings` replace `## Discussion`; existing notes not migrated. See `docs/meeting-notes/2026-05-10-1623-class2-format.md`.
- [x] **Add meeting-notes Read allowlist entries**: 2026-05-10 — added `Read(~/.claude/docs/meeting-notes/*)`, `Read(~/src/*/docs/meeting-notes/*)`, `Bash(ls ~/src/*/docs/meeting-notes/*.md)` to `settings.json`.
- [x] **Investigate: built-in "update project memory" mechanism** — **Closed 2026-05-14.** No dedicated tool exists (exhaustive ToolSearch confirmed). Probe Write to `/home/tobias/.claude/projects/.../memory/_probe.md` fired **no prompt** — tilde allowlist entry `Write(~/.claude/projects/*/memory/*.md)` matches absolute file_path parameters for the Write tool (tilde-expansion works for Write/Edit, unlike Bash cwd-relative rewriting). Observed prompt was from `git -C ~/.claude add/commit` in git-diary-workflow, not the memory write. Decision about dropping git-commit for memory-only changes deferred (orthogonal; separate investigation if friction continues).
- [x] **Verify Class 2 format (live test)**: confirmed 2026-05-14 — `2026-05-14-1909-orphan-scan-output-cap.md` has `**Mode:** Class 2 planning record`, new section headings (Context/Plan/Implementation findings/Decisions/Action items), no permission prompts fired.
- [x] **Orphan-scan re-evaluation** (after **10** logged invocations in `~/.claude/logs/meeting-orphan-scan.log`): read log; gate (a) median runtime <500ms → no cache, ≥1s → open caching meeting; gate (b) cand5<cand4 in any row → keep 4-word key, cand5==cand4 across all → can raise to 5. Open `/meeting orphan-scan-reeval` only if a gate fires. **Closed 2026-05-14** — gate fired (27 invocations); superseded by meeting `docs/meeting-notes/2026-05-14-1803-orphan-scan-accumulation.md`. Decisions: keep 4-word key; union-read archive; Step 5b process discipline.
- [x] **orphan-scan.sh should not require `bash` prefix** — verified 2026-05-14: shebang `#!/usr/bin/env bash` present, file is `-rwxr-xr-x`. Stale item; closed. Output-cap fix landed separately (see `docs/meeting-notes/2026-05-14-1909-orphan-scan-output-cap.md`).
- [x] **Orphan-scan false-positive noise** — meeting notes often phrase action items differently from their TODO.md counterparts (e.g. full paths vs basename, bold markers shifting word positions), so the 4-word key match fails and already-done items are flagged as orphans every meeting. Root cause: key4 is extracted after `**` stripping but before backtick/path simplification. Options: (a) strip backtick content before key extraction; (b) extract only the first non-punctuation token as a label (e.g. `N9b-1.` or `Sc3.`) and match that against TODO.md; (c) only scan meeting notes newer than the last `[x]`-closure date. **Closed 2026-05-14** — superseded by `docs/meeting-notes/2026-05-14-1803-orphan-scan-accumulation.md`. Root fix: union-read `TODO.md + TODO.archive.md` (D1) + SKILL.md Step 5b process discipline (D2). Options (a)/(b)/(c)/anchor-marker all withdrawn.
- [x] **archive-done.sh: also archive by count, not only age** — implemented 2026-05-14: `git show HEAD:<rel-path>` extracts prior-done set; any `[x]` entry in both prior and current done-sets archived unconditionally to today_ym bucket; age gate stays as fallback. Synthetic round-trip verified.
- [x] **S4 — migrate todo-update to dotclaude-skills**: `dotclaude-skills/todo-update/` has `SKILL.md`, `README.md`, `archive-done.sh`; `~/.claude/skills/todo-update/SKILL.md` symlinked to dotclaude-skills. Closed 2026-05-14.
- [x] **Amend `discoveries.md` γ-class entry** — correction entry already present in discoveries.md (written in same session as meeting note). Closed 2026-05-15.
- [x] **Verify append.sh allowlist consistency** — confirmed 2026-05-15: ran `append.sh -t discoveries -f /dev/null` from helferli cwd; no permission prompt fired. Tilde entries work for Bash commands (literal command-string matching, same as diary-append.sh). No absolute-path variants needed.
- [x] **AI-1 — Verbatim hooks copy to dotclaude-skills**: `hooks/meeting-cost-logger.sh`, `hooks/parallel-edit-detector.py`, `hooks/notify-hook.linux-x11.sh` all present; content matches `~/.claude/hooks/`. Closed 2026-05-14.
- [x] **AI-3 — Rename `hooks/notify-hook.sh → hooks/notify-hook.linux-x11.sh`** via `git mv`. Done — file was already named `.linux-x11.sh` (predates the meeting note). Closed 2026-05-14.
- [x] **AI-4 — hooks/README.md**: one paragraph per hook + settings.json snippet. Present at `hooks/README.md` (14 May). Closed 2026-05-14.
- [x] **AI-5 — Makefile `install-hooks` target**: `mkdir -p ~/.claude/hooks/` + 3 `ln -sf` lines; present and idempotent. Closed 2026-05-14.
- [x] **AI-8 — Post-symlink verification** — confirmed 2026-05-15: `notify-send` 0.8.8 installed; hook chain verified (settings.json → `~/.claude/notify-hook.sh` → `notify-hook.linux-x11.sh`); test notification sent successfully. Fixed `set -e` bug: `wait "$notif_pid" 2>/dev/null` → `|| true` to prevent exit 1 on dismiss-without-action (libnotify 0.8 behavior).
- [x] **Add empty-section prune pass + gate relocation to `archive-done.sh`** — implemented 2026-05-15; see `docs/meeting-notes/2026-05-15-1121-todo-update-prune-empty-sections.md`.
- [x] **Edit `todo-update/SKILL.md` Step 4** — dropped "skip if <50 lines" from prose; gate now single-sourced in script. Same session/meeting.
- [x] **Synthetic round-trip test** — <50-line and ≥50-line fixtures both verified. Same session/meeting.
- [x] **F-A — not adopted** — meeting 2026-05-21 decided F-B subsumes F-A entirely; auto-flip mechanism not needed. See `docs/meeting-notes/2026-05-21-0934-orphan-scan-fb-hash-id.md`.
- [x] **F-B — shipped 2026-05-21** — stable opaque `<!-- id:XXXX -->` token per action item (stored on write, not re-derived); exact-match scan; `append.sh new-id` generator; un-IDed legacy lines skipped (clean cutover after 0-genuine-orphan triage). See `docs/meeting-notes/2026-05-21-0934-orphan-scan-fb-hash-id.md`.
- [x] **AI-1 — Promote broker.py to dotclaude-skills canonical home** — copy `~/src/meeting-rpg/wt/ipc-preflight/broker.py` to `~/src/dotclaude-skills/meeting/broker.py`; add `/status` endpoint returning `{"subscribers": <count>}` (per D4); commit. Then symlink `~/src/meeting-rpg/broker/broker.py → ~/src/dotclaude-skills/meeting/broker.py` (after meeting-rpg's `wt/ipc-preflight` merges to main). Contract: `python3 ~/src/dotclaude-skills/meeting/broker.py $SID` writes broker.json; `curl http://127.0.0.1:<port>/status` returns subscriber count. See: `docs/meeting-notes/2026-05-20-1645-meeting-skill-broker-integration.md`.
- [x] **AI-2 — Create `meeting-live/` WIP sibling skill** — at `~/src/dotclaude-skills/meeting-live/`; copy `meeting/SKILL.md` to `meeting-live/SKILL.md`; symlink `format.md`, `personas.md`, `append.sh`, `cost-of.sh`, `broker.py` from `../meeting/`. Add `~/.claude/skills/meeting-live/` symlink. Contract: `/meeting-live` invokable in Claude Code with all canonical specs intact. See: `docs/meeting-notes/2026-05-20-1645-meeting-skill-broker-integration.md`.
- [x] **AI-3 — Modify `meeting-live/SKILL.md` setup step 7 + γ branch** — insert step 7 (spawn + port + advertise per D3); modify decision-point handling per D4 (poll `/status`, branch on subscribers); ensure `MEETING_LIVE=0` opt-out short-circuits all broker behaviour. Contract: with `MEETING_LIVE=0`, behaviour identical to canonical `/meeting`; with `MEETING_LIVE` unset, broker spawns + advertises + persona lines off-chat + decision branch on subscriber count. See: `docs/meeting-notes/2026-05-20-1645-meeting-skill-broker-integration.md`.
- [x] **AI-4 — Allowlist entries in `~/.claude/settings.json`** — implemented: `Bash(curl -s http://127.0.0.1:*)`, `Bash(curl -N http://127.0.0.1:*)`, `Bash(curl -s -X POST http://127.0.0.1:*)`, `Bash(python3 /home/tobias/.claude/skills/meeting-live/broker.py *)`, `Bash(jq -r * /tmp/meeting-rpg/*/broker.json)`. Verify no permission prompts during pilot. See: `docs/meeting-notes/2026-05-20-1645-meeting-skill-broker-integration.md`.
- [x] **AI-4b — Improve broker curl allowlist** — **TRIGGERED 2026-05-20 pilot**: `curl -s -X POST http://127.0.0.1:33717/event …` fired a permission prompt despite the AI-4 allowlist entry `Bash(curl -s -X POST http://127.0.0.1:*)`. Three-pattern workaround is leaky. Fix: `broker-curl.sh` wrapper script (one allowlist entry covering all call shapes). Prerequisite for AI-5 clean re-run.
- [x] **AI-M1 — Document `POST /response` in SKILL.md broker-endpoints table** — renderer-side answer endpoint is missing from SKILL.md. `POST /response` body `{"answer": "..."}` unblocks `GET /await` (broker.py:49-53). Manual test command: `curl -s -X POST http://127.0.0.1:<port>/response -H 'Content-Type: application/json' -d '{"answer":"<choice>"}'`. Add to the "Broker endpoints" reference table + one-liner example.
- [x] **AI-M2 — Batching-cadence rule in SKILL.md discussion section** — model skipped POSTing persona exchanges during Item 1 (only open header + /question went to broker). Root causes: per-exchange curl cost + AI-4b permission prompt friction. Add explicit rule to SKILL.md: *one `/event` POST per agenda item, batched text, after verbatim transcript is printed locally*. Needed before next AI-5 attempt so renderers receive discussion content.
- [x] **AI-M3 — Debug tail recipe for /events** — raw `curl -N /events` emits `data: <json>` which is unreadable manually. Add a friendly one-liner to SKILL.md or a `events-tail.sh` helper: `curl -N http://127.0.0.1:<port>/events | while read line; do echo "$line" | sed 's/^data: //' | jq -r '.text // .'; done`.
- [x] **Add `new-id` subcommand to `append.sh`** — collision-free 4-hex token absent from union of notes+TODO.md+TODO.archive.md. Shipped 2026-05-21. <!-- id:a1c7 -->
- [x] **Rewrite `orphan-scan.sh` match logic** — exact `<!-- id:XXXX -->` match; un-IDed lines skipped; 4-word/5-word key code removed. Shipped 2026-05-21. <!-- id:b3e9 -->
- [x] **Update `meeting/SKILL.md` Step 5b** — ID minting via `append.sh new-id` required on all mirrored action items. Shipped 2026-05-21. <!-- id:c4f2 -->
- [x] **Mirror Step 5b into `meeting-live/SKILL.md`** — dual-surface, AI-5 merge desync prevented. Shipped 2026-05-21. Also fixed: raw find → find-todos.sh. <!-- id:d8a5 -->
- [x] **One-time legacy triage** — swept 62 un-IDed items; 0 genuine open orphans found (all in archive or tracked differently). Clean cutover confirmed. Done 2026-05-21. <!-- id:e6b1 -->
- [x] **Close F-A + update F1–F3** — done above in this TODO.md. <!-- id:0c93 -->
- [x] **`meeting-live/SKILL.md` fallback `find` needs allowlist or removal** — fixed 2026-05-21: replaced raw `find` with `~/.claude/skills/meeting/find-todos.sh` in `meeting-live/SKILL.md` (same as canonical meeting/SKILL.md).
- [x] **`archive-done.sh` prunes sections with plain bullets as "empty"** — fixed: `has_task` → `has_content` (line 130); sections with any non-blank content (including plain bullets) are no longer pruned. Repro: `meeting-rpg/TODO.md` "Deferred / eliminated" section (2026-05-20).
- [x] **subdirectory TODO scan `find` triggers permission prompt** — root fix: glob characters in `-not -path '*/...'` args are rejected class-wide even when allowlisted. Created `dotclaude-skills/meeting/find-todos.sh` wrapper; symlinked to `~/.claude/skills/meeting/`; both meeting and todo-update SKILL.md updated to call the script; allowlist entry replaced with `Bash(~/.claude/skills/meeting/find-todos.sh)` — verified 2026-05-21.

## 2026-06
- [x] **Transcript-`usage` parser** — **RESOLVED 2026-06-03**: `cost-of.sh` rewritten with `jq -rs` to sum real `input_tokens`/`cache_read_input_tokens`/`cache_creation_input_tokens`/`output_tokens` from `type=="assistant"` turns; `meeting-cost-logger.sh` extended with same jq pass + 4 new CSV columns. See `docs/meeting-notes/2026-06-03-1520-transcript-usage-parser-impl.md`. <!-- id:d0ed -->
- [x] **Document `MEETING_ROOT_OVERRIDE` in canonical SKILL.md setup-step-1** — **RESOLVED 2026-06-01**: added to setup-step-1 noting env-var override path and /meeting-cross usage. See `docs/meeting-notes/2026-05-28-1138-meeting-cross-architecture.md`. <!-- id:8237 -->
- [x] **`~/.claude` meetings trigger "allow changing its own settings" prompt** — **RESOLVED 2026-05-21**: guard is H3-cwd (cwd-based, not file-based). Probe confirmed: editing `~/.claude/` files from a sibling cwd is silent when allowlisted. Convention added to `~/.claude/CLAUDE.md`. See `docs/meeting-notes/2026-05-21-1037-avoid-claude-cwd.md`.
- [x] **Meeting: avoid `~/.claude/` as cwd** — **RESOLVED 2026-05-21**: Branch A — never launch Claude with cwd=`~/.claude/`; edit via tilde/absolute paths from a sibling cwd; use `git -C ~/.claude` for VCS. Convention in `~/.claude/CLAUDE.md`. `settings.json`/`CLAUDE.md` prompts are the guard working correctly (un-allowlisted by design). See `docs/meeting-notes/2026-05-21-1037-avoid-claude-cwd.md`.
- [x] **Discoveries citation eye-audit** — **RESOLVED 2026-06-01**: ≥1 citation found (7+ instances: 2 explicit "## Surfaced discoveries" sections in `2026-05-10-1658-publish-meeting-skill.md` and `2026-05-14-1015-skill-ctx-bloat-audit.md`; 5 inline recall-references in discussion). Retro entries NOT culled. Follow-up: citation-logging hook design (see item below). See `2026-05-08-2058-discoveries-audit.md` and `docs/meeting-notes/2026-06-01-1331-discoveries-citation-audit.md`.
- [x] **`user-profile.md` ctx strategy** — **RESOLVED 2026-06-01**: `profile-active.sh` shipped (log-only/passthrough default; `--filter` behind flip gate). Design: single-file + rule-based filter (eligible=yes AND confidence=med|high); no physical split (drift-aversion). Baseline logged: full=656, active=168, ratio=0.26. See `docs/meeting-notes/2026-06-01-1558-user-profile-context-strategy.md`.
- [x] **Reverse-orphan scan** — **RESOLVED 2026-06-03**: `--reverse` mode added to `orphan-scan.sh`; both SKILL.md paths updated to auto-invoke alongside forward scan. Verified: finds `[x]`/inline ledger gaps; forward scan unchanged; self-scan clean. See `docs/meeting-notes/2026-06-03-0952-reverse-orphan-scan.md`.
- [x] **AI-M4 — Headless pilot (subscribers=0 branch)** — completed 2026-05-21: no-listener run confirmed subscribers=0 → AskUserQuestion; MEETING_LIVE=0 opt-out path verified during broker-topology meeting. Both contract clauses done.
- [x] **Broker global daemon — broker.py** — done 2026-05-21: global fixed-port (64109), /status-probe discrimination, global `/tmp/meeting-rpg/broker.json`, `MEETING_BROKER_IDLE` idle-shutdown. See: `docs/meeting-notes/2026-05-21-1007-meeting-live-broker-global-daemon.md`.
- [x] **Broker global daemon — broker-curl.sh** — done 2026-05-21: session arg added (`<port> <session> <endpoint>`); forwarded as `session` in POST body + `?session=` in GET query. Session routing now active. See: `docs/meeting-notes/2026-05-21-1007-meeting-live-broker-global-daemon.md`.
- [x] **Broker global daemon — meeting-live/SKILL.md** — done 2026-05-21: step-7 replaced with lazy-connect + `MEETING_LIVE=1` opt-in; all broker-curl calls updated to pass session; `MEETING_BROKER_PORT`/`MEETING_BROKER_IDLE` documented; frontmatter updated. See: `docs/meeting-notes/2026-05-21-1007-meeting-live-broker-global-daemon.md`.
- [x] **Broker global daemon — meeting-broker.service + README** — done 2026-05-21: systemd --user unit (`MEETING_BROKER_IDLE=0`); README broker daemon section added with env vars + install snippet. See: `docs/meeting-notes/2026-05-21-1007-meeting-live-broker-global-daemon.md`.
- [x] **AI-M5 — mid-meeting listener toggle pilot** — done 2026-05-21: all 3 probes PASS (A: subscribers=0, B: subscribers=1 + round-trip, C: subscribers=0). Two bugs surfaced in broker.py — see action items below. Live /meeting-live run stays AI-5. See: `docs/meeting-notes/2026-05-21-1908-aim5-listener-toggle-pilot.md`.
- [x] **Fix broker.py: `_ok()` called inside `_lock`** — done 2026-05-26: captured data under lock, moved `_ok()` calls outside lock in `/await` and `/status` GET branches (POST branches were already correct). See: `docs/meeting-notes/2026-05-21-1908-aim5-listener-toggle-pilot.md`. <!-- id:cb79 -->
- [x] **AI-5 pre-flight — three defects found and fixed (2026-06-01)**: (1) `meeting-live/SKILL.md` chat-additive bug → `subscribers>0` now suppresses chat entirely; (2) `broker.py /status` not bridging `"live"` session → fixed; (3) launcher invoked `/meeting` not `/meeting-live` → fixed. See `docs/meeting-notes/2026-06-01-1349-ai5-live-meeting-pilot-preflight.md`.
- [x] **ExitPlanMode-before-end-of-meeting-writes ordering fix** — **RESOLVED 2026-06-03**: reordered end-of-meeting section in `meeting/SKILL.md` and `meeting/format.md`; ExitPlanMode is now step 1, all writes follow. See `~/src/meeting-rpg/docs/meeting-notes/2026-06-03-1502-r4-end-of-meeting-prompts-renderer.md` and `docs/meeting-notes/2026-06-03-1529-meeting-live-backmerge.md`. <!-- id:c762 -->
- [x] **Create CLAUDE.md for fresh projects at meeting end** — **RESOLVED 2026-06-01**: added step 1c to end-of-meeting section in SKILL.md. <!-- id:29ce -->
- [x] **New persona introduction in meeting attendees line** — **RESOLVED 2026-06-01**: added to SKILL.md step 4 (with-subject flow): emit one-sentence lens intro for each "(new)" persona in the opening exchange before the agenda. <!-- id:8365 -->
- [x] **Permission prompts on `find-todos.sh` and `orphan-scan.sh` recurring** — **RESOLVED 2026-06-01**: Root cause was twofold: (1) incomplete path-form × args coverage in hand-maintained allowlist entries; (2) `find-todos.sh`, `orphan-scan.sh`, `broker-curl.sh`, `broker.py` were symlinked by hand outside `make install`. Fix: added `_ALLOW` vars + `tools/allowlist.py` generator; `make install-allowlist` now merges the full 8-form set (4 path forms × with/without args) for all exec scripts idempotently; wired into `make install`; meeting_FILES updated to own all script symlinks. See `docs/meeting-notes/2026-06-01-1221-makefile-allowlist-generator.md`.
- [x] **Permission prompts on `archive-done.sh` and `find-todos.sh`** — **RESOLVED 2026-05-27**: `find-todos.sh` already had absolute-path entries; added `/home/tobias/.claude/skills/todo-update/archive-done.sh` and `/home/tobias/src/dotclaude-skills/todo-update/archive-done.sh` (with and without args) to `~/.claude/settings.json`. Root cause: tilde-vs-absolute mismatch for Bash paths from foreign CWDs. Observed 2026-05-21 during meeting-rpg session.
- [x] **Persona-state write step in `meeting/SKILL.md`** — DONE 2026-06-03: added step 2d (gated on `docs/meeting-notes/persona-state.yml` presence, skip Class 1/2) + `meeting/persona-state.py` helper (deterministic valence, last-5 truncation, affinity running-sum, atomic YAML + `web/persona-state.json` mirror); 8 pytest tests green in `~/src/meeting-rpg/tests/test_persona_state.py`. See `~/src/meeting-rpg/docs/meeting-notes/2026-06-03-1556-todo-classifier.md`. <!-- id:ed25 -->
- [x] **Add `persona-state.py` to `make install`** — DONE 2026-06-04: added to `meeting_FILES`, `meeting_EXEC`, `meeting_ALLOW` in Makefile; switched SKILL.md invocation from `python3 …` to direct shebang (`~/.claude/skills/meeting/persona-state.py`); `make install-allowlist` added 4 missing bare-path entries (the `*`-suffix forms were already hand-added). Contract met: fresh `make install` wires persona-state.py. <!-- id:9460 -->
- [x] **Throwaway proxy spike** — DONE 2026-06-04: `meeting/contrib/llm-proxy.py` (stdlib-only, manual chunked decode, TCP_NODELAY, port 61842). Contract met: multiple SSE turns completed `fully_consumed=true`; `usage` logged with `cache_read/cache_creation/thinking_tokens` breakdown. Three fixes shipped: (1) strip `Accept-Encoding` to prevent compressed forwarding, (2) manual chunked decode to avoid `fp.read1()` blocking forever on keep-alive after terminating chunk, (3) `TCP_NODELAY` to prevent Nagle coalescing on loopback. Abandon-or-promote decision pending. See `docs/meeting-notes/2026-06-03-1452-llm-proxy-token-ctx-and-persona-split.md`. <!-- id:e905 -->
- [x] **Shared skill-file retrieval helper** — DONE 2026-06-04: `meeting/retrieve-top-k.py` (52 lines, stdlib-only, no `import zkm`); chunks by `--chunk-sep` pattern, POSTs to `EMBED_ENDPOINT`, cosine top-K, emits blocks to stdout; exit 1 on failure (caller falls back to full read). Wired into SKILL.md step 5 for with-subject mode; Makefile + allowlist (8 forms). See `docs/meeting-notes/2026-06-01-1558-user-profile-context-strategy.md`. <!-- id:040c -->
- [x] **`find-todos.sh` — skip git worktree roots** — **RESOLVED 2026-06-03**: pipe results through `.git` file check; worktree-rooted TODO.md files are now silently skipped. N=2: meeting-rpg `wt/` worktrees + any future multi-worktree project.
- [x] **broker-curl round-trip gate for meeting-live/ deletion** — DONE 2026-06-03: PASS recorded in `docs/meeting-notes/2026-06-03-1529-meeting-live-backmerge.md` (session 8520676a); broken symlink `~/.claude/skills/meeting-live` removed; stale allowlist entry cleaned from settings.json. <!-- id:54ba -->
- [x] **β: flock `append.sh` writes** — DONE: flock added in commit 5c1ada7 (lines 77–80 in `meeting/append.sh`). Also fixed `(( emitted++ ))` → `(( ++emitted ))` bug in `new-id`/`new-ids` (set -e + arithmetic-0 triggered errexit before `exit 0`). Contract met: concurrent append.sh calls produce clean separate entries; new-id exits 0. <!-- id:9cfb -->
- [x] **β: `merge=union` for tracked append-logs** — DONE 2026-06-04: `.gitattributes` added for `meeting/personas.md` in dotclaude-skills. DIARY.md@claude-diary and MEMORY.md indices still need `.gitattributes` in their respective repos. See `docs/meeting-notes/2026-06-04-1300-haiku-sonnet-builder-pilot.md`. <!-- id:fbcd -->
- [x] **Create `docs/meeting-notes/persona-state.yml` for dotclaude-skills** — DONE 2026-06-04: seeded with standing personas (archie/riku/petra); added to .gitignore; fixed persona-state.py to skip JSON mirror when `web/` absent (no-op for non-meeting-rpg projects). Contract met: step 2d will run at end of next dotclaude-skills meeting. See `docs/meeting-notes/2026-06-03-1613-parallel-session-state-coordination.md`. <!-- id:1178 -->
- [x] **Fix /meeting interactive-protocol self-bug** — DONE 2026-06-04: SKILL.md step 5 already updated in db8e43b (same-turn constraint added); format.md Interactive mode step 2 also updated for consistency (explicit "same response" requirement). See `docs/meeting-notes/2026-06-03-1613-parallel-session-state-coordination.md`. <!-- id:ec72 -->
- [x] **Design meeting: worktree-per-session for same-repo Class 1 code edits** (D5) — DONE 2026-06-04: D5.1–D5.7 decided. Mechanism by topology: worktree for deliberate parallelism; flock+manifest for ad-hoc cross-repo. Plumbing CAS rejected (silent loss). git-lock-push.sh to wrap manifest-scoped stage+commit; git-diary Step 1b/1c to stage from session manifest not status. D5 ratification satisfies D6 build-gate #1. See `docs/meeting-notes/2026-06-04-1144-worktree-per-session-d5.md`. <!-- id:d18d -->
- [x] **Haiku-vs-Sonnet builder pilot** — DONE 2026-06-04. Sonnet-default confirmed: Haiku fails LOGIC-HEAVY (`git add "$manifest_file"` bug); Sonnet correct on all 3. D6 gate #3 satisfied. fbcd + 9ff2 shipped as side effects. See `docs/meeting-notes/2026-06-04-1300-haiku-sonnet-builder-pilot.md`. <!-- id:c0d2 -->
- [x] **broker-mode: apostrophe in jq event/question payload breaks shell quoting** — DONE 2026-06-04: broker-mode.md updated; all 3 call sites (event, question, response) now use `jq -n --arg` pattern; shell-quoting rule block added before Discussion section. Contract met. See `docs/meeting-notes/2026-06-04-1300-haiku-sonnet-builder-pilot.md`. <!-- id:9ff2 -->
- [x] **md-merge.py: delete lock file after flock released** — fixed 2026-06-05: `try/finally` wraps both `update_ids` and `update_sections`; `lock_path.unlink(missing_ok=True)` runs after flock released on success and failure. Contract met.
- [x] **GitHub-aware orphan/past-meeting audit** — built `gh-audit.sh` 2026-06-05: surfaces open issues/PRs after orphan scans (advisory, no-arg + subject audit) and relevance-matched issues/PRs in subject-mode surfaced-discoveries. See `docs/meeting-notes/2026-06-05-1328-gh-audit-build.md`. <!-- id:d9f5 -->
- [x] **`/meeting-cross` skill — implementation** (gated on AI-5 backmerge OR 2026-06-28 re-eval) — **DONE 2026-06-06**: AI-5 backmerge effective (no meeting-live sibling); classify.sh extracted; meeting-cross/ skill built; MEETING_ROOT_OVERRIDE in canonical step 1. See `docs/meeting-notes/2026-05-28-1138-meeting-cross-architecture.md`. <!-- id:4f01 -->
- [x] **`/meeting-cross-implementation` session** — complete 2026-06-06: (1) `meeting/classify.sh` extracted; (2) `meeting-cross/SKILL.md` built + installed; (3) `MEETING_ROOT_OVERRIDE` already in canonical setup-step-1; (4) routing-trail-note shape in meeting-cross SKILL.md. Contract: cross-skill invokes canonical via Skill tool. See `docs/meeting-notes/2026-05-28-1138-meeting-cross-architecture.md`. <!-- id:c3fd -->
- [x] **2026-06-28 re-eval checkpoint** — superseded 2026-06-11: α′ decision made early (meeting `2026-06-11-0950-meeting-cross-as-cross-switch.md`); re-eval no longer needed. <!-- id:b4fd -->
- [x] **classify.sh gate-text check** — advisory `GATED` column shipped 2026-06-11: tail-appended 5th TSV field, case-insensitive grep for gate vocabulary, consumer render notes added. See `docs/meeting-notes/2026-06-11-0824-classify-gate-text-check.md`. <!-- id:1ab6 -->
- [x] **Instrument first 3 /meeting-cross runs with cost-of.sh** — all 3 runs below 250k (run 1: 195,799; run 2: 187,889; run 3: 53,046 tok). No handover-file mechanism needed. See `docs/meeting-notes/2026-06-11-0908-cross-classification.md`. <!-- id:b427 -->
- [x] **Investigate: `echo "$EMBED_ENDPOINT"` prompted despite structural parity with `$MEETING_LIVE`/`$MEETING_BROKER_PORT`** — RESOLVED: `$EMBED_ENDPOINT` added to allowlist; full audit confirmed no other env-probe gaps. <!-- id:0e3a -->
- [x] **Compress `git-diary-workflow/SKILL.md`** in place: collapse Step 1c duplicate block to back-ref of 1b, merge Co-Authored-By explainer, tighten prose in 1b + Gotchas/Merge-Conflicts. Contract: post-trim `wc -c`/4 < 2k tokens; preservation contract met (capture-first session-ID, `<<'EOF'`, never-add-A, never-amend/force, ask-user-on-conflict, one full inline manifest+lock-push in 1b). Done 2026-06-11: 171→150 lines, ~1741 tokens. See `docs/meeting-notes/2026-06-11-0921-git-diary-skillmd-trim.md`. <!-- id:5a5c -->
- [x] **Re-spec `git-diary-workflow` TODO watch-line to token form** — done inline; gate now `~(wc -c)/4 ≈ tokens vs. 2k gate`. See `docs/meeting-notes/2026-06-11-0921-git-diary-skillmd-trim.md`. <!-- id:e57b -->
- [x] **profile-active.sh flip gate** — gate cleared 2026-06-05: ratio 0.26–0.34 (≪ 0.60) over many meetings, setup-ctx ~30% of meeting cache_read tokens. Flipped `profile-active.sh` to `--filter` in `meeting/SKILL.md`. See `docs/meeting-notes/2026-06-05-1200-profile-active-flip-gate.md`. <!-- id:f8f1 -->
- [x] **Orphan-scan observation window** — closed 2026-06-11: 0 spurious FPs in 34 forward-scan runs; forward scan now authoritative. Reverse stays ADVISORY (expected to return in-session completions). Updated both meeting/SKILL.md and meeting-cross/SKILL.md. See `docs/meeting-notes/2026-05-21-0934-orphan-scan-fb-hash-id.md`. <!-- id:f2d4 -->
- [x] **AI-5 savings gate** — confirmed 2026-06-05: ≥5k tok/meeting savings across 3 pilots (e25ec41d hermes session: 490 turns / 87k output vs comparable canonical 110k output). Broker mode marked recommended default in meeting/SKILL.md step 7. <!-- id:7b4c -->
- [x] **γ: `shard` + `collapse` for persona-state.py** — add `shard` (append session delta to gitignored `persona-events/<session-id>`) and `collapse` (flock'd fold all shards → unified yml/json, GC shards) subcommands; update SKILL.md step 2d. Contract: N concurrent meetings each record delta; final sums = Σ all deltas; no shard files remain after collapse. Zero AI tokens. See `docs/meeting-notes/2026-06-03-1613-parallel-session-state-coordination.md`. <!-- id:5f60 -->
- [x] **δ: flock'd deterministic merge helper** — `flock`'d merge helper for `TODO.md` (keyed by `<!-- id:XXXX -->`) + `user-profile.md` (keyed by `## section`); apply structured delta rather than blind whole-file rewrite; wire into `todo-update` + `/meeting` end-of-meeting writes. Contract: concurrent sessions editing different items both survive; same-item serializes. See `docs/meeting-notes/2026-06-03-1613-parallel-session-state-coordination.md`. <!-- id:42f4 -->
- [x] **3e35 + d00e joint session** — implement manifest-scoped stage+commit: (a) `git-lock-push.sh` `-f`/`-m` manifest mode (Sonnet pilot diff in `2026-06-04-1300-haiku-sonnet-builder-pilot.md` is the reference); (b) `git-diary SKILL.md` steps 1b/1c to stage from session manifest. Ship together. Contracts: `3e35` two concurrent sessions commit only own files; `d00e` session with another's uncommitted files present commits only its own. See `docs/meeting-notes/2026-06-04-1144-worktree-per-session-d5.md`. <!-- id:3e35 -->
- [x] **git-diary Step 1b/1c: stage from session manifest, not `git status`** — `git-diary-workflow/SKILL.md` (+ optional Stop-hook to derive manifest from `transcript_path`). Contract: a session with another session's uncommitted files present in the shared tree commits ONLY its own touched files. See `docs/meeting-notes/2026-06-04-1144-worktree-per-session-d5.md` (D5.4). <!-- id:d00e -->
- [x] **Record worktree-dispatch spec + tick D6 build-gate #1** — update D6 TODO line already updated above; additional action: ensure D6 meeting note references this D5 spec. Contract: D6 dispatcher build can cite this note for the worktree lifecycle spec. See `docs/meeting-notes/2026-06-04-1144-worktree-per-session-d5.md` (D5.5, D5.7). <!-- id:14d7 -->
- [x] **Investigate: `echo "$EMBED_ENDPOINT"` prompted despite structural parity with `$MEETING_LIVE`/`$MEETING_BROKER_PORT`** — RESOLVED: all 4 env-probe echo calls confirmed in allowlist; bonus gap found and fixed: `md-merge.py` was missing from script allowlist — added via Makefile 2026-06-05. Contract met.
- [x] **Write `meeting/cross-mode.md` delta-file** — done 2026-06-11: created with project-discovery/classify/dispatch/routing-trail sections; no setup prose. P2-symlinked. See `docs/meeting-notes/2026-06-11-0950-meeting-cross-as-cross-switch.md`. <!-- id:5e5d -->
- [x] **Add `--cross` conditional to canonical `meeting/SKILL.md`** — done 2026-06-11: literal `--cross` check inserted between step 2 and step 2b; reads cross-mode.md and stops. See `docs/meeting-notes/2026-06-11-0950-meeting-cross-as-cross-switch.md`. <!-- id:13f0 -->
- [x] **Shrink `meeting-cross/SKILL.md` to a pure forwarding alias** — done 2026-06-11: 14-line forwarder; setup 1–7 + read cross-mode.md; removal trigger embedded. See `docs/meeting-notes/2026-06-11-0950-meeting-cross-as-cross-switch.md`. <!-- id:18bb -->
- [x] **Cross-project TODO routing** — implemented 2026-06-11: shared inbox at `~/.claude/todo-inbox.md`; `append.sh -t inbox` writes, `inbox-done` resolves; surfaced at meeting setup (step 7b) + `/projects`; no auto-write into any repo's TODO. See `docs/meeting-notes/2026-06-11-1400-cross-project-todo-routing.md`. <!-- id:b30b -->
- [x] **Statusbar: verify it lives in dotclaude-skills** — moved to `statusline/statusline-command.sh`; P2 symlink + Makefile `install-hooks` entry. See `docs/meeting-notes/2026-06-11-1320-statusline-kvcache-countdown.md`.
- [x] **Statusbar: Sonnet quota display** — `Son:XX%→cooldown` added using `seven_day_sonnet` from usage API cache; gracefully absent when null — verified by user 2026-06-11.
- [x] **Statusbar: KV-cache expiry time** — shows `KV:HH:MM` (absolute expiry clock time) / `KV:cold`; green→red gradient; reads `transcript_path` mtime — verified by user 2026-06-11.
- [x] **Fable-harness fix: transcript + AskUserQuestion same-turn pattern broken** — Fable-gated inline-prose protocol added to format.md §Interactive mode; SKILL.md steps 4+5 and broker-mode.md fallback clauses updated. Contract met: Fable uses inline-prose + Decision provenance; Sonnet/Opus/Haiku unchanged. See `docs/meeting-notes/2026-06-12-0749-fable-harness-interactive-mode-fix.md` — implemented 2026-06-12.
- [x] **Statusbar: quota pricing-tier emoji (💸 regular / 🪙 reduced)** — computes peak vs off-peak from weekday 05:00–11:00 PT window (no API field exists); placed after cost on line 1; carries `PRICING_VERIFIED` staleness marker for weekly re-verify — verified by render + branch + staleness tests on 2026-06-11.
- [x] **Statusbar: model-family emoji prefix (Opus 🎼 / Sonnet 🪶 / Haiku 🍃 / Fable 🦊 / Mythos 🐉 / fallback 🤖)** — prepends MODEL_DISPLAY on line 1; substring match tolerates version suffixes + fast variants; editable case arms — verified across all six families on 2026-06-11.
- [x] Run the 3-cell spike: probe real n_ctx; bounded "summarize diff" via curl (B) and `opencode run` (C); smallest agentic "append line + confirm" via opencode (C). Write results table + go/no-go finding. **Finding: B wins (7s curl vs >5min opencode timeout); C-agentic deferred (RAM pressure).** See `docs/meeting-notes/2026-06-11-2300-subagents-local-llama-spike-finding.md`. <!-- id:95e3 -->
- [x] **fables-turn integration defects (2026-06-12 pilot)** — all three fixed: (a) `--ff-only` flag added to `git-lock-push.sh`; (b) `--follow-tags` added to push; (c) `<repo>` → `<repo-path>` doc fix in SKILL.md + conventions.md. See `docs/meeting-notes/2026-06-12-1342-fables-turn-integration-defects.md`. <!-- id:821c -->
- [x] **fables-executor skill** — versioned SKILL.md that executor sessions load directly instead of reading a generated `CLAUDE.md` relay contract. Makes each child self-contained and testable; removes drift risk from the generated file. Open `/meeting fables-executor skill` to design the shape (C2 or C3). <!-- id:fba6 -->
- [x] **Human: work through REVIEW_ME.md judgment boxes** — 8 open boxes (~15 min) covering the 5 still-red specs (3b02 44ba 1ec1 32d6 2520), de9c interpretations, and the expected-red runner semantics — all 8 confirmed by user on 2026-06-12 (boxes ticked; prune at next review turn). <!-- id:60a5 -->
- [x] **broker-mode.md γ-branch table missing Fable caveat** — added `> **Fable note:**` blockquote naming inline-prose replacement and pointing to `format.md §Harness-class gate` — covered by tests (test_fable_caveat.sh) on 2026-06-12
- [x] **Reduce broker-curl ctx overhead in meeting skill** — implemented `say` subcommand in broker-curl.sh (batches lines via stdin, one /event per line) and updated broker-mode.md §Discussion to use one `say` call per agenda item — covered by tests (test_broker_say.sh) on 2026-06-12 <!-- id:3b02 -->
- [x] **Align fables-executor SKILL.md self-report example with de-facto RELAY_LOG format** — done 2026-06-12, riding the natural contract v2 bump as specified: example now uses the `## YYYY-MM-DD — executor (<model-tier>)` heading format that ckpt-tag.sh and every real entry use; content fields unchanged. Found by strong-model audit F13. <!-- id:697e -->
- [x] **Flag Opus-as-Fable-standin checkpoints for re-review when Fable returns** — marker recording shipped (D1–D3): `relay-loop.js` emits `fable-standin` token in Opus review/handoff labels; `ckpt-tag.sh` carries full label into annotated tag message; `tests/test_fable_standin_marker.sh` 8/8 pass. Discovery/priority-surfacing deferred behind 'Fable access restored' gate (id:9821). See `docs/meeting-notes/2026-06-13-0859-fable-standin-checkpoint-marker.md`. <!-- id:0420 -->
- [x] **Self-feeding relay loop (one launch drains the backlog)** — SHIPPED + VERIFIED 2026-06-13. First self-looping run (wf_29c465c4-bad) did **7 rounds in one invocation**, ended on drained (2 consecutive empty discoveries, quotaStopped=false), 8 integrations cycling execute→review→execute across rounds, 0 handbacks, no double-integration. Replaces per-pool orchestrator restarts. relay-loop.js runRound()+outer while; MAX_ROUNDS=30 seatbelt; state/quotaStopped cross-round accumulators; per-round roundCapHit. Prereqs: quota-stop self-refresh + RELAY_QUOTA_DECAY_7D time-decay cap forwarded into the gate. Watchdog (not relauncher) covers abnormal death. <!-- id:60e1 -->
- [x] **Apply 2026-06-13 REVIEW_ME-wave meeting decisions across handed-off repos** — DONE 2026-06-13: review.md test-integrity audit mirrors D1/D2 (commit 9236545); per-repo REVIEW_ME confirmations applied + pushed across romtrans(9cc7a2f), claude-organizer(be7f31b), collaib(ec579b2), rawrora(a4a25fd), zomAI(17b4dc4), recurheb(42c6c2d) per meeting docs/meeting-notes/2026-06-13-1751-handoff-review-me-wave.md. RESIDUALS (tracked elsewhere): rawrora axis-swap(4ad2)/sign(98a0) need on-device USER confirm (left open as flagged guards); romtrans _rebuild_bytes D4 skip-not-splice routed to romtrans inbox (routed:6da4); HARD items (32fc/948d/aaaf/bcb8 adapter-shape/droidclaw) stay [HARD] for dedicated design. <!-- id:3114 -->
- [x] **Two-switch: separate Fable-availability from fallback policy** — DONE 2026-06-14 (relay-loop.js + SKILL.md + tests/test_fable_down_strong_tier.sh, 17/17 green). `-d` defer/demote block now gated on `STRONG_MODEL === 'claude-fable-5'`, so `-d --strong-tier opus` substitutes Opus for review/handoff (marked fable-standin) instead of deferring; `-d` alone still goes executor-only. Triggered by 2026-06-14 Fable outage where plain `/fables-turn` (default fable tier) failed all 18 strong units. See [[relay-fable-down-two-switch]]. <!-- id:5902 -->
- [x] Add `--ff-only` getopts flag to `git-diary-workflow/git-lock-push.sh` — opt-in ff-only pull mode for relay path; no global default change; on divergence: loud failure. See `docs/meeting-notes/2026-06-12-1342-fables-turn-integration-defects.md`. <!-- id:95b0 -->
- [x] Add `--follow-tags` to push line(s) in `git-diary-workflow/git-lock-push.sh` — annotated reachable tags (fable-ckpt-*, vX.Y.Z) auto-push with branch push; no new allowlist entry. See `docs/meeting-notes/2026-06-12-1342-fables-turn-integration-defects.md`. <!-- id:a827 -->
- [x] Update `fables-turn/SKILL.md` invariant 5 (`ckpt-tag.sh <repo-path>` not `<repo>`) and `fables-turn/references/conventions.md` if it echoes the push/tag sequence. Doc-only change. See `docs/meeting-notes/2026-06-12-1342-fables-turn-integration-defects.md`. <!-- id:05cb -->
- [x] Add scratch-repo test `tests/test_git_lock_push_ff_only.sh`: (1) ff-only + unchanged remote → tag survives on-branch; (2) ff-only + diverged remote → loud failure; (3) `--follow-tags` → annotated tag reaches remote. See `docs/meeting-notes/2026-06-12-1342-fables-turn-integration-defects.md`. <!-- id:ed1a -->
- [x] **Design `/meeting` ↔ `fables-*` interaction** — DESIGN DELIVERED 2026-06-15 (`docs/meeting-notes/2026-06-15-0715-meeting-fables-interaction.md`, D1–D9). All three umbrella questions answered: (1) nudge `/fables-turn review <repo>` (surface-only, relay-detection-gated), not inline dispatch — see id:15e9; (2) handoff/review do a mini-handoff on unqualified meeting/manual items (D6, id:7c23) rather than meeting invoking handoff; (3) review reopening `[HARD]` → meeting, plus REVIEW_ME→C3 wiring (id:15d5). Also produced D1/D2 single-id-two-views + cross-ledger guard (id:38df), D7 C1≈executor (id:a21b), D8 Workflow-extraction (id:6ba4, deferred). Build items seeded ledger-neutral below. <!-- id:5ab4 -->
- [x] **`/meeting` surfaces open REVIEW_ME.md entries as a C3 option** — DONE 2026-06-15 (D5): meeting SKILL.md no-arg surfaces open REVIEW_ME boxes as a synthetic Class 3 candidate (relay-detection-gated, surface-only) + end-of-meeting step 2e D5-invariant write-back (ticks the resolved box; keeps any id's checkbox state consistent across TODO/ROADMAP via flock'd md-merge; bookkeeping only, no roadmap re-derivation). Covered by tests/test_meeting_review_me_wiring.sh; make test 20/20. <!-- id:15d5 -->
- [x] **`/meeting` relay-awareness** — DONE 2026-06-15 (D4): `classify.sh` floors any `[HARD]`-tagged TODO item to C3 (never C1/C2 inline); `meeting/SKILL.md` no-arg step 3 prints a relay-detection-gated surface-only nudge to `/fables-turn review <repo>` when ROADMAP.md has open `[ROUTINE]` items. Non-relay repos unaffected. Deferred under id:5ab4: inline dispatch, Fable-vs-Sonnet detection, window fold-in, strong-Agent-spawn. Covered by tests/test_classify_hard_floor.sh; make test 19/19. <!-- id:15e9 -->
- [x] **D1/D2 — single-id-two-views + cross-ledger guard** — DONE 2026-06-15: `orphan-scan.sh --cross-ledger`/`-x` flags ids whose checkbox state disagrees across TODO+ROADMAP; reuse-id instruction added to handoff.md C2, review.md step 5, relay-loop.js review prompt; shared non-union-ledger note in fables-turn SKILL.md guardrails + CLAUDE.md conventions; meeting SKILL.md runs the cross-ledger scan when ROADMAP.md exists. Covered by tests/test_cross_ledger_guard.sh; make test 18/18 green. <!-- id:38df -->
- [x] **D6 — relay reverse-handoff for unqualified meeting/manual items** — DONE 2026-06-15: review.md §5b detects ledger items added since last checkpoint lacking [ROUTINE]/[HARD] (or acceptance/tests) and qualifies+sizes execution-ready ones into ROADMAP (mini-handoff, reusing their id), leaves design-judgment ones as TODO/meeting candidates, skips deferred/gated; handoff.md C2 + relay-loop.js review prompt point at the same triage. Covered by tests/test_reverse_handoff.sh; make test 21/21. <!-- id:7c23 -->
- [x] **D7 — relay-repo C1 dispatch follows the executor contract** — DONE 2026-06-15: meeting SKILL.md no-arg step 5 Class 1 dispatch, when ROADMAP.md exists, follows the fables-executor contract (no test gaming, full suite green, RELAY_LOG self-report) — natural on /opusplan where the post-plan tier is Sonnet; fable-ckpt tag stays the next review's job; non-relay repos unaffected. Covered by tests/test_meeting_c1_executor_contract.sh; make test 22/22. <!-- id:a21b -->
- [x] **`/fables-turn human` — cross-repo human-backlog triage (REVIEW_ME + @manual BDD)** — **HOME DECIDED 2026-06-15: `/fables-turn human`** (renamed from `review_me`; the human is the relay's 3rd actor — execute=Sonnet, review/hard=Opus, **human=you**). Broadened beyond REVIEW_ME to ALSO sweep `@manual` BDD scenarios (run-and-confirm items that need a human, never auto-tickable; once a [HARD] harness lands — id:2f48/71ee/41ef — those scenarios become relay-automatable). Tier-C chewy items route OUT to `/meeting --cross` (downstream, not the sweep itself). **BUILT 2026-06-15** (id:2892, fable-ckpt-20260615-1013): `references/human.md` + SKILL.md `## Human mode` + `scripts/gather-human-backlog.sh` + `tests/test_fables_human.sh`; 24/24 green. Next: the id:d0da [HUMAN]/[MEETING] tags feed this mode's routing. Original spec — new mode. **FIRST RUN executed manually 2026-06-15** (37 open boxes / 19 repos: 1 tier-a auto-resolved [zkWhale 3e27 stale flag, 2ca9497], 23 tier-b surfaced to user, 8 tier-c → /meeting; zelegator skipped dirty) — validates the divide&conquer tiers ahead of building the mode. Grab ALL open `- [ ]` boxes in every relay-managed repo's `REVIEW_ME.md` (the per-repo `/meeting` REVIEW_ME C3 path id:15d5 handles one repo; this is the fleet-wide cousin), then **divide-and-conquer by automatic answerability** so the human only sees what genuinely needs them: a strong-model pass classifies each box into tiers — (a) **auto-answerable** (the box's question has an unambiguous answer derivable from the code/tests/spec — e.g. "is this green test a frozen bug?" where the implementation is plainly correct; the strong model confirms the box and ticks it, logging its reasoning); (b) **batch-decidable** (needs a human but is a quick yes/no — present as one consolidated AskUserQuestion batch); (c) **chewy** (genuine design judgment — route to a `/meeting` REVIEW_ME session, id:15d5). Output: auto-tier resolved + logged, batch-tier as one decision pass, chewy-tier as queued meetings — minimizing human touches. Design questions (chewy, do in a `/meeting`): how the strong model self-assesses "answerable with confidence" without rubber-stamping (anti-gaming: an auto-answer is a claim the next review re-checks, like any executor work); whether auto-answers need a REVIEW_ME audit trail / human-overridable log; aggregation across repos vs per-repo; interaction with `@manual` BDD boxes (never auto-tickable — they need a human to run the scenario). Relates id:15d5 (per-repo C3), id:9821 + the 26+14 REVIEW_ME boxes pending from the 2026-06-12 handoff wave. Requested by user 2026-06-15. <!-- id:72cc -->
- [x] **D8 — Workflow-extraction common-ground analysis** (DEFERRED to its own session; design `2026-06-15-0715-meeting-fables-interaction.md`) — analyze common ground across /meeting end-of-meeting bookkeeping, executor work-discipline, and review write-back; consider extracting parts of /meeting into a Workflow (deterministic, flock'd, worktree-aware). Gate: D1–D7 landed (concrete common ground to factor) + relates id:7b23 (unified-skill question). — **DONE 2026-06-15** (analysis delivered: `docs/meeting-notes/2026-06-15-1005-d8-workflow-extraction.md`): recommend NO Workflow for /meeting (interactive, can't prompt); only genuine extraction = dedupe the single-id contract prose into one cited reference (id:962a); resolves id:7b23 (keep split skills, unify contract text). <!-- id:6ba4 -->
- [x] **Standin re-review detection is masked by later executor checkpoints** — the `standin` flag is re-derived from each repo's LATEST `fable-ckpt-*` tag only (relay-loop.js discovery + `standInRank`). When an executor checkpoint lands on top of a standin review (e.g. project_manager: …-2309 reviewer/fable-standin → …-2352 executor), `standin` reads false and the pending INDEPENDENT Fable re-review (id:9821) goes invisible — project_manager's relay.toml comment ("latest ckpt carries the re-review marker") is now stale/false. Under indefinite Fable absence (id:da26) this queue must stay trackable, never silently lost. Fix: persist a `standin_pending` / `last_standin_ckpt` field in relay.toml, set at standin-integrate time and cleared only by the eventual Fable re-review — i.e. the ONE piece of relay state that SHOULD live in relay.toml precisely because it is NOT reliably re-derivable from git once masked (contrast id:22ef: verdicts stay re-derived). Also: 3 historical manual/pilot checkpoints lack the token (proton-moresync/project_manager …-1359 'fable-down pilot', trAIdBTC …-1628 'reviewer/review') — superseded + harmless; root cause = `ckpt-tag.sh -l` label is free-form/unenforced for manual integrations (consider a warn-if-opus-reviewer-without-fable-standin guard). Surfaced by full standin audit 2026-06-15. — **DONE 2026-06-15 (merged into id:96a8)**: the standin_pending field shipped as relay.toml last_strong_ckpt/strong_model/fable_rechecked, written on strong checkpoints, never cleared by executor checkpoints. <!-- id:e030 -->
- [x] **Long-term Fable-absence operating posture** — user directive 2026-06-15: "assume Fable is gone for a very long time; proceed accordingly — re-reviews deferred until Fable returns, but that must never block work". Honored per-invocation today via `-d --strong-tier opus` (Opus substitutes; standin elevation dormant so deferred re-reviews don't fire; standin repos are NOT excluded — still dispatched for genuine unaudited/routine work, so nothing blocks). Decide the DURABLE form: (a) should `STRONG_TIER=opus` + `-d` become the session default while Fable is out (vs. manual flags each turn), respecting the defer-on-uncertainty default (id:f649) and the deferred Fable auto-probe; (b) guarantee the deferred standin re-review queue (id:9821/id:e030) can grow unbounded without ever gating execute/handoff dispatch or degrading scheduling. **(c) HARD-backlog stall — RATIFIED 2026-06-15 (user): accept Opus doing `[HARD]` work for now.** Live finding 2026-06-15: with `[ROUTINE]` fully drained (0 open across all 32 repos) and every repo already carrying a ROADMAP (so `handoff` never fires), the autonomous pool's only HARD path is the opportunistic one-item handoff-C5 / review-step-6 — which therefore never triggers — so ~46 open `[HARD — strong model]` items (+~14 untagged) stall indefinitely while Fable is out and the pool idles despite ~60 open items. The pool needs a real **HARD-execute verdict**: an Opus-substitute child works a HARD item in its worktree under the same verify-before-merge discipline, ENABLED ONLY when `STRONG_TIER=opus` (never on the Sonnet execute tier), one bounded item at a time, so HARD work progresses without Fable. Scheduling: rank below ROUTINE-execute and review (preserve the anti-gaming window) but above idle; income repos win slot contention as usual. Until it ships, HARD items are driven by explicit strong sessions (`/meeting`→opusplan, or direct Opus per item). **(a)+probe+semantics SHIPPED 2026-06-15 (f64c28b):** STRONG_TIER default=opus; front-door Fable-probe (once/2h cache `~/.config/fables-turn/fable-probe.json`); `fable-standin` reframed to `@fable-optional-recheck` bonus (never "pending"); relay.toml PENDING comments reframed; 22/22 green. **(c) HARD-execute verdict + tested `probe-fable.sh` SHIPPED 2026-06-15 (fable-ckpt-20260615-0948):** `hard` verdict in relay-loop.js — Opus-apex-gated (never Sonnet, deferred under -d/Fable), ranked review>execute>hard>handoff, 23/23 green. REMAINING: deep `-d`/flag-matrix cleanup + relay-loop.js elevation→lowest-prio-bonus reframe. Decision item for the relay-improvement /meeting (id:2909 agenda). <!-- id:da26 -->
- [x] **Rename `fables-*` → `relay`; merge executor as `/relay executor`** (user decision 2026-06-15) — Fable is no longer apex (Opus is), so "fables" is a misnomer. Rename to **`relay`** (matches the existing `relay.toml`/`RELAY_LOG.md`/`relay-loop.js`/`RELAY_STATUS.md` artifacts; model-agnostic). **Merge** `fables-executor` in as `/relay executor` via a **LEAN** entry — cheap Sonnet executors must NOT load the whole orchestrator SKILL.md, so keep the contract a thin reference. Scope: ~53 files (268× `fables-turn`, 113× `fables-executor`), 2 skill dirs (`git mv`), Makefile targets, tests, conventions/references, `docs/fables-relay.md`→`docs/relay.md`, ARCHITECTURE, CLAUDE.md. **Deprecation alias** skills (`fables-turn`→`/relay`, `fables-executor`→`/relay executor`) like meeting-cross so in-flight invocations/pointers/cron don't break. The 33 managed-repo `## Relay contract` pointers **auto-migrate** via review §4 on a contract **vN bump (v2→v3)** — no manual sweep. Leave historical meeting-note prose under the old names. **DONE 2026-06-15** (fable-ckpt-20260615-1045): fables-turn→relay, executor merged as `/relay executor` (lean `relay/references/executor-contract.md`), contract v3, deprecation aliases, 24/24 green; +idempotent-install fix (dir-symlink guard). 33 external pointers auto-migrate on next review (v2<v3). <!-- id:1cb4 -->
- [x] **`relay-ckpt-*` tag prefix + per-checkpoint model tracking for the Fable-bonus stage** (user 2026-06-15) — de-fable the relay's git tags alongside the skill rename (id:1cb4). New checkpoints use `relay-ckpt-YYYYMMDD-HHMM`; **leave existing `fable-ckpt-*` tags as historical bookkeeping** (never rewrite). Changes: (a) `ckpt-tag.sh` emits `relay-ckpt-*`; (b) discovery + standin/latest-tag detection in `relay-loop.js` match **BOTH** prefixes (`git tag -l 'fable-ckpt-*' 'relay-ckpt-*'`) so latest-tag + commit-range logic spans the boundary; (c) tests that grep `fable-ckpt` accept both. **Model tracking (for the deferred Fable "bonus" optional-recheck):** the model is already in the tag annotation label (`reviewer (claude-opus-4-8, ...)` / `executor (sonnet, ...)` / `strong-execute (claude-opus-4-8, ...)`) — keep it, so a future Fable session can find Opus-produced STRONG checkpoints (role in reviewer/handoff/strong-execute, model=opus) to recheck. But a later executor checkpoint masks the latest-tag signal (id:e030), so ALSO persist per-repo `last_strong_ckpt` + `strong_model` + `fable_rechecked` in relay.toml — i.e. MERGE this with id:e030's `standin_pending` into ONE model-tracked Fable-bonus queue. The `@fable-optional-recheck` marker legitimately names the Fable recheck and may keep its name. Sequence AFTER id:1cb4; relay.toml `last_ckpt` values stay `fable-ckpt-*` until each repo's next checkpoint (dual-prefix matching handles the mix). <!-- id:96a8 -->
- [x] **`/relay review` should default to the CURRENT repo, require `--all` for the sweep** [DONE 2026-06-15 — relay/SKILL.md + test_relay_review_default.sh] — for consistency with `/relay executor` (and the safer least-surprise default). Today `/relay review` with no args defaults to `--all` (every confirmed own repo). Flip it: bare `/relay review` reviews the cwd repo only (`git rev-parse --show-toplevel`); `--all` (or an explicit repo-list) opts into the cross-repo sweep. Update relay/SKILL.md invocation block + the review-mode procedure + the `/meeting` no-arg "relay-ready nudge" (which already names the repo explicitly). Mirror the `--all`-required wording the SKILL uses for the default autonomous pool. Requested 2026-06-15. <!-- id:d53c -->
- [x] **`/relay next` — auto-route the next relay action (executor | review | human)** [DONE 2026-06-15 — relay/SKILL.md ## Next mode + test_relay_next.sh] — a quick-decision front door: inspect current state and ACT accordingly — open `[ROUTINE]` → executor; unaudited commits since last checkpoint → review; open `REVIEW_ME`/`@manual`/genuinely-chewy design → human. Optimize for the fastest possible "human-or-not" call: do the autonomous executor/review work WITHOUT asking, and only involve the human when a judgment box truly requires it (when unsure, prefer acting over asking). Builds on the existing executor/review/human modes (`relay/SKILL.md`) + the human-as-3rd-actor design. Requested 2026-06-15. <!-- id:f9ad -->
- [x] **`/relay` (default pool) should regularly report SKIPPED repos with a reason** [DONE 2026-06-15 — RELAY_STATUS "## Skipped (this round)" + discovery skipped[] + test_relay_claim_wiring.sh] — extend the per-round `RELAY_STATUS.md` rollup with a "Skipped" section listing every own repo NOT dispatched this round, each tagged with a one-word reason: `human-required` (open `REVIEW_ME`/`@manual` box), `excluded-by-config` (classification ≠ own / `needs_review`), `dirty-worktree`, `hardware-gated`/`intensive` (id:8d52), `decision-gate` (`[HARD — decision gate]`), `diverged-from-origin` (id:c3f7), `claimed-elsewhere` (id:ebfb), `idle/in-sync`. Today discovery only surfaces dirty/`needs_review` into RELAY_STATUS "Blocked" — there's no comprehensive, regular "here's why each repo is sitting out" view. RELAY_STATUS is rewritten every round, so the rollup is the natural home; discovery already computes most of these signals. Requested 2026-06-15. <!-- id:be62 -->
- [x] **Advanced controlled-force-push solution for fievel** [DONE 2026-06-15 — relay/scripts/force-push.sh (FORCE_PUSH_CONFIRM gate, per-repo guard-lift, --force-with-lease, re-arm trap) + docs/relay.md + test_relay_force_push.sh] — server-side `receive.denyNonFastForwards`+`receive.denyDeletes` were set `true` globally on fievel 2026-06-15 (blocks ALL force/purge pushes — incl. legitimate ones the user occasionally needs). Design a controlled path that keeps *accidental/automated* force-push impossible while allowing a *deliberate human* one: candidates — (a) a wrapped per-session toggle helper (set false → push → restore true under flock); (b) per-repo `denyNonFastForwards=false` exceptions; (c) gerrit/forgejo with an owner force-push permission; (d) a protected `refs/heads/*` + an explicit `refs/force/*` escape namespace. Pick the lightest. Pairs with id:c3f7. Requested 2026-06-15. <!-- id:de51 -->
- [x] **Relay cross-session task reservation / WIP-marking** [DONE 2026-06-15 — claim registry + dispatch lease + executor-honoring + flock'd single-writer; see ROADMAP id:ebfb] — separate sessions (autonomous pool run + manual `/fables-turn` + ad-hoc executor) can currently pick the same repo/item concurrently; within one pool run the serializer protects integration, but nothing reserves *tasks* across sessions. Live evidence 2026-06-12: (1) a parallel session and the reviewer turn collided on the same uncommitted fables-executor contract-v2 edits (absorbed cleanly — but only because both had identical intent); (2) the ~22:47 OOM crash (Gemma 26B in ai-codebench; swap raised 16→32 GB since) killed 5 sessions, 3 mid-work — held worktrees under `~/.cache/fables-turn/worktrees/` were the ONLY durable in-flight signal, which strengthens option (c). (3) Live evidence 2026-06-15: the autonomous pool's Sonnet executor and a concurrent interactive session both worked trAIdBTC `id:fb42` — the integrator's clean-tree guard caught the dirty main and handed back (worktree `fefe0c1` orphaned, no corruption) but the duplicate executor work was wasted; repo/item-level reservation (not just within-run integration serialization) would have prevented it. Candidate mechanisms, design before regular multi-session unattended use (i.e. after the id:1ad7 pilot): (a) flock'd per-repo reservation files under `~/.config/fables-turn/locks/` (git-lock-push.sh pattern); (b) `in_flight = "<runId>"` field in relay.toml written at dispatch, cleared at integration; (c) worktree-existence-as-implicit-lock (`~/.cache/fables-turn/worktrees/<repo>/` entry = reserved — already nearly true, needs the discover step to honor it; doubles as crash-recovery signal). Item-level (not just repo-level) marking would need union-merge-safe RELAY_LOG `WIP:` lines or ROADMAP WIP ticks. Requested by user 2026-06-12. **DESIGN RATIFIED 2026-06-15** as the unifying "task-claim" primitive — this id is the claim/reservation umbrella (worktree-aware discovery + single-writer relay.toml/RELAY_STATUS + per-shard claim registry `~/.config/fables-turn/claims/<key>.json`, item-keyed/repo-enforced, mtime+TTL `claim.sh`); chose mechanism (c) worktree-as-implicit-lock as cheapest-first step 1. ROADMAP id:7b7a/8ac5 retired as dups onto this + id:3558 (enforcement). See `docs/meeting-notes/2026-06-15-1216-relay-dispatch-safety-cluster.md`. <!-- id:ebfb -->
- [x] **Piece 1 — `gaming-scan.sh`** (`relay/scripts/`): mechanical gaming detector extracted from `review.md` §2 — args `<repo-root> <since-tag>`, emits one parseable flag line per detection (deleted test via `git diff --diff-filter=D`, added `skip`/`xfail`/`.only`/`@pytest.mark.skip`, removed `assert`/expectation lines). Red test (`tests/test_gaming_scan.sh`): crafted diffs emit the expected flags; a clean diff emits nothing. `[ROUTINE]`. **DONE 2026-06-15** (closed in ROADMAP; note: needs `make install` to symlink into ~/.claude/skills/relay/scripts/). See `docs/meeting-notes/2026-06-15-1610-adversarial-review-anti-gaming.md` <!-- id:fa05 -->
- [x] **Piece 2 — `review.md` §2 delegate rewrite**: §2 invokes `gaming-scan.sh` for the mechanical pass and retains only the resurrection-check + fixture-special-casing judgment prose; the inlined mechanical one-liners removed (single source of truth). Static-grep test asserts §2 references `gaming-scan.sh` and no longer inlines `--diff-filter=D` / skip-grep one-liners. `[ROUTINE]`. **DONE 2026-06-15** (closed in ROADMAP). See `docs/meeting-notes/2026-06-15-1610-adversarial-review-anti-gaming.md` <!-- id:dfaf -->
- [x] **Piece 4 — supervisor flag-rate logger** in `relay-loop.js` `integrate()`: append to `~/.claude/logs/relay-gaming-flags.log` with repo/run/closed-item-ids/`gaming_flags`/`reopened`/`verified_green`; leave documented escalation-seam comment (future `parallel()` refuter fleet). Red test: fixture report with `gaming_flags` → log line with expected fields. `[ROUTINE]`. **DONE 2026-06-15** (closed in ROADMAP; pool-crashing `new Date()` regression fixed same day with guard `test_relay_no_date_api.sh`). See `docs/meeting-notes/2026-06-15-1610-adversarial-review-anti-gaming.md` <!-- id:3826 -->
- [x] **Workflow profiler for the /relay loop — real numbers, no observation bias** (done 2026-06-15: `relay/scripts/profile-run.sh <runId|wf-id|wf-dir>` + `tests/test_profile_run.sh`; emits concurrency-over-time vs cap, a blocked-vs-`queued-behind-cap` round-boundary verdict, and per-phase/per-model aggregates) — user 2026-06-15: recurring "I think I saw a single Haiku being waited for before another discovery round" suspicion that can't be settled by eyeballing `/workflows`. Build a profiler that parses the run's on-disk journal (`…/subagents/workflows/wf_*/journal.jsonl` has per-agent `started`/`result` keyed by `agentId`) + each `agent-*.jsonl` (usage/timing) + `*.meta.json` into per-agent records: {label, model/tier, start, end, duration_ms, tokens_in/out, phase}. Emit: (a) a **concurrency-over-time** curve (how many agents live at each instant) against the harness cap `min(16, cores-2)`; (b) a **round-boundary analysis** — the gap between round N's last integration and round N+1's first `discover-prelude`, and exactly which agents (and models) occupy it; (c) per-label / per-phase aggregates (count, total/avg duration, total tokens). Tool: `relay/scripts/profile-run.sh <runId|wf-dir>`. **Settles the Haiku question with data**: post-cb50 there is no *logical* await of status writes between rounds (scheduleStatusWrite is fire-and-forget; `await statusTail` only at run END) — BUT a queued Haiku status-write can still occupy 1 of the ~6 cap slots and *delay the prelude's start under cap contention*, which LOOKS like "discovery waiting for a Haiku" without being a logic block. The profiler distinguishes "blocked-on" from "queued-behind-under-cap". Token usage falls out for free (cost-per-round, cost-per-verdict-class). <!-- id:a59e -->
- [x] **Retro-profile recent (or all) relay runs — settle the "again just one Haiku" claim + find 80-20 improvements** (done 2026-06-15: `relay/scripts/profile-runs-batch.sh` + `tests/test_profile_runs_batch.sh` + findings note `docs/relay-profiling-2026-06-15.md`. **Result over 31 runs / 1935 agents: the Haiku-wait claim does NOT reproduce** — 0/41 round boundaries cap-blocked, max inter-round gap 4.1s, median 0.1s; the 15 "single live Haiku at prelude start" cases all had free slots (not blocking). 80-20 finding: pool is under-saturated (mean 14.7% time-at-cap, peak 4.8/~6) → the lever is more parallel work [[id:ca87]], not a bigger cap. Haiku = 46% of agents (status+quota = 1020/1935) — the visual noise; optional wins: 1 status-write/round, longer quota cache.) — user 2026-06-15: now that `profile-run.sh` exists (id:a59e), batch-run it over the most recent relay Workflow runs (or ALL retained runs, for statistics + evolution checkup) to (a) test the recurring "discovery was waiting on a single Haiku" suspicion against real `queued-behind-cap` vs `overlapped-not-capped`/`clean-start` verdicts across many round boundaries — one profiled run isn't enough signal; (b) build aggregate stats (per-run peak/% -time-at-cap, per-phase/per-model token + duration totals, round-gap distribution) and track them over time as the loop evolves (cb50/9ed4/etc. landed recently — did they move the numbers?); (c) surface the highest-impact 80-20 improvement suggestions from the data (where is wall-clock actually lost — cap contention, a slow phase, serialized integration, idle gaps?). **Retention window:** transcripts are pruned at `cleanupPeriodDays` (unset → default 30d; confirmed — oldest on-disk transcript is exactly 2026-05-16, ~30d back), so "all runs" means the last ~30 days; capture/export the aggregate now if longer history matters. Deliverable: a small batch wrapper (loop `profile-run.sh --json` over discovered `wf_*` dirs, fold into a summary table) + a short findings note with ranked suggestions. Builds on [[a59e]] / `relay/scripts/profile-run.sh`. <!-- id:08a3 -->
- [x] **D1: park unmerged orphans on discovery** `[ROUTINE]` — done 2026-06-16 (executor verified done-check: test_relay_orphan_park.sh PASS, make test 55/0; impl was committed 796e275, box ticked in ROADMAP+TODO). See 2026-06-16-0938-relay-orphan-reconcile.md <!-- id:689c -->
- [x] **D2: scripted `/relay reconcile` mode (human-invoked)** `[ROUTINE]` (done 2026-06-16; `relay/scripts/relay-reconcile.sh`, spec green) — enumerate `relay/orphan/*`; per-branch {integrate: verify→`git merge --no-ff`→`ckpt-tag.sh`→`git-lock-push --ff-only` | discard: `git branch -D` | leave}. Reuse ckpt-tag + `--ff-only` (no skipped tag, no race). Test: integrate→`relay-ckpt-*` tag + pushed `--no-ff` merge; discard→ref gone; conflicting orphan→left+surfaced, never half-merged. (see 2026-06-16-0938) <!-- id:3313 -->
- [x] **D3: suppress-redispatch** `[ROUTINE]` (done 2026-06-16; discovery binds parked orphan→item, spec green) — discovery binds `relay/orphan/*`→ROADMAP item via `git show --stat`; if item still OPEN, suppress fresh dispatch + surface one line w/ `relay-burn --run <runId>` cost hint; `/relay reconcile` cost-ordered (fallback commit-count); ambiguous binding→default suppress; no manifest (refs are the registry). Test: open-item orphan NOT dispatched + IS surfaced; closed-item orphan does not suppress. (see 2026-06-16-0938) <!-- id:1f53 -->
- [x] **[HIGH] `/relay human` must also sweep ROADMAP gated-HARD items, not just REVIEW_ME boxes** — user 2026-06-16 ("high priority so we don't miss those anymore"). **Done 2026-06-16** (relay HARD, id:f6c9): `gather-human-backlog.sh` now re-derives a `kind = gated_hard` row per open ROADMAP `[HARD — decision gate]` item or item under a `## Gated`/`do not start`/`deferred` section (the two purely-textual, false-positive-free gates of the id:2d20 EXECUTABLE-HARD test); `box_summary` carries `<item> — gated: <why>` reusing the classifier reason vocabulary. **Decision: re-derive from ROADMAP** (not read live RELAY_STATUS) — freshness-safe even when no pool is live. `references/human.md` §2/§3(c)/return-summary now route `gated_hard` rows as the tier-(c) CHEWY "needs a /meeting" checklist (repo · id · why-gated → `/meeting --cross`, box left open, never auto-ticked). Hermetic tests added to `tests/test_relay_human.sh` (decision-gate + gated-section emit; executable `[HARD — strong model]` + closed `[x]` negative controls). **Gap found live:** the pool surfaces ~14 repos whose only open work is `[HARD — decision gate]` / gated / cross-repo (the `id:2d20` EXECUTABLE-HARD exclusion) into `RELAY_STATUS.md` → Blocked as "HARD backlog is gated — needs a /meeting." But `/relay human` reads ONLY `gather-human-backlog.sh` (open `REVIEW_ME.md` boxes + `@manual` scenarios), so those pool-blocking items are NEVER shown to the human anywhere actionable — they silently stall until someone reads RELAY_STATUS. **Spec:** `/relay human` (and/or `gather-human-backlog.sh`) additionally enumerates open ROADMAP `[HARD — decision gate]` / under-a-Gated-section items across `own` repos and presents them as a tier-(c) CHEWY "needs a /meeting" checklist (route OUT to `/meeting --cross`, box left open), distinct from the REVIEW_ME verdict tiers (a)/(b). Each line: repo · item id · one-line why-gated (reuse the classifier's reason string). Decide: read the live RELAY_STATUS Blocked list vs re-derive from ROADMAP (re-derive is freshness-safe when no pool is live). Relates to id:2d20 (the exclusion that produces them), id:ec9a (`/relay status` shows the same list read-only), `references/human.md`, `gather-human-backlog.sh`. <!-- id:f6c9 -->
- [x] **Cross-session lease LEAKS when a child dies without integrating (release is coupled to the integration path); + no heartbeat means long children can lose their lease mid-work** (done 2026-06-16, strong-execute) — fixes (a)+(b) landed: `runUnit` now calls `releaseLease(unit)` in a per-unit FINALLY path after the child settles with ANY outcome (merged/handback/null/error), run-scoped + idempotent vs. the integrator's retained step-0 release (defense-in-depth), and guarded against the review→execute re-chain steal window (`rechainedSameRepo`); claim.sh gained worktree-anchored liveness (a stale-mtime claim whose recorded `--worktree` still has commits beyond main is LIVE — the converse of id:3ac8 — so a >TTL long child isn't stolen mid-work) plus a `heartbeat` subcommand and a `worktree` shard field, and the work child now passes `--worktree` to acquire. (c) TTL shortening deferred — leave at 1800s now that liveness no longer depends solely on it. Tests: `tests/test_relay_claim_liveness.sh` (heartbeat + worktree-anchored staleness, hermetic git repos) and new id:7570 static checks in `tests/test_relay_loop_structure.sh`. Suite 59/0. — observed live 2026-06-16 (run relay-20260616-112222-29307, round 9): another session was blocked from writing mathematical-writing's TODO/ROADMAP because "a live relay run holds mathematical-writing (mode hard, since 13:18)" — but that hard child was DEAD: worktree `…/mathematical-writing/relay-…-29307-hard` had HEAD==main (0 commits) and zero file activity for 16 min, and there was no integrate/handback event for it. The lease (id:ebfb) stayed held by the ephemeral dead pid 1202111 until its 30-min TTL, needlessly deferring the other session. **Manually released** via `claim.sh release mathematical-writing --run relay-20260616-112222-29307`. (Likely cause of the child death itself: math-writing is Lean/Mathlib — memory-heavy — so an OOM-killed hard child is plausible; cf. [[oom-local-model-session-kills]] — but the LEAK is the bug here regardless of why the child died.) **Root cause (relay-loop.js):** (1) lease release lives INSIDE the integrate agent (L940 step 0); a child that returns null/errors/dies SKIPS integration (L1041/1045 catch → recoverable-handback path), so the release never runs → lease leaks for the full TTL. (2) The claim `pid` is the ephemeral shell that ran `claim.sh acquire`, NOT the agent — so it's useless as a liveness signal (always "dead" within seconds). (3) NO heartbeat: claim mtime is fixed at acquire time and TTL=1800s, so a LEGITIMATELY-long child (>30 min) would have its lease EXPIRE mid-work → another run grabs the repo → double-work/collision (the converse failure, worse than a leak). **Fixes:** (a) immediate — release the lease in a per-unit FINALLY path (after the child settles with ANY outcome: integrate, handback, null, error), not only inside integration; (b) tie claim liveness to the HELD WORKTREE (reuse the id:3ac8 worktree-staleness signal: a claim whose worktree HEAD==main with no recent mtime is stale) and/or HEARTBEAT the claim mtime while the child is alive, so both the leak AND the long-child-expiry directions become correct; (c) optionally shorten TTL once (a)+(b) land. Relates to id:ebfb (lease registry), id:0902 (interactive claim-aware), id:3ac8 (worktree-staleness reap — the analog liveness signal claims should share), id:0547 (same-repo race), id:719e (reliability/waste). <!-- id:7570 -->
- [x] **Cut relay status/overhead cost (id:c3a6, ROADMAP)** — DONE 2026-06-17 (Opus `/meeting`, `docs/meeting-notes/2026-06-17-0721-relay-status-overhead-cost.md`): pinned `discover-shard` to sonnet (was inheriting Opus — the 35%-overhead leak), recorded tier audit as SATURATED, built content-addressed discovery cache (`discover-sig.sh` superset signature + `state.discoverCache` reuse; LLM shard fires only on churn; fail-open). `make test` green (62); TDD `test_discover_sig.sh`/`test_discover_cache.sh`. <!-- id:c3a6 -->
- [x] **Piece 3 — Tier B model canary harness** (`tests/gaming-canary/` + `make gaming-canary`): each fixture is a prepared gamed diff (resurrection-rewrite, fixture-special-casing) + expected verdict + at least one negative control (id:3b02 legitimate resurrection case); harness spawns one review-style agent, asserts `gaming_flags` contains expected id and is empty for controls. Excluded from `run-tests.sh` default sweep. `[HARD — strong model]`. **DONE 2026-06-16** (relay HARD child; ROADMAP done-note has detail; `make gaming-canary` + token-free plumbing guard `tests/test_gaming_canary.sh`). See `docs/meeting-notes/2026-06-15-1610-adversarial-review-anti-gaming.md` <!-- id:414a -->
- [x] **Relay integrator frequency (not tier)** — CLOSED 2026-06-17: both levers exhausted. (1) 'skip no-op' lever does not exist (integrate() early-returns before Sonnet for null/contract_met=false — 119 merges / 8 conflicts / 41 early-returns on 2026-06-16; all spawned integrators had real work). (2) Batching infeasible: per-repo integrations are one-per-round, each branch built on the prior ckpt, median ~100 min apart — never co-ready. Cross-repo batching would reverse the line-465 parallel-per-repo latency fix and worsen conflict recovery (failure atom: wave not repo) to save rounding-error sonnet tokens vs $1317 Opus (76%). Re-open trigger: only if same-repo units become co-ready in one wave (e.g. id:fb75/6e9d multi-execute dispatch). Regression-guarded by tests/test_relay_integrator_noop_guard.sh (checks 7+8). (2026-06-17-0812 meeting) <!-- id:c563 -->
- [x] **[BLOCKING pre-check RESOLVED 2026-06-17] Confirm probe invocation path** — both paths in-bounds; chosen = path A (`claude -p`) as dedicated probe OS user with empty `~/.claude` (harness-free by isolation, subscription path, no per-token cost); path B (API+`--bare`) latent fallback; verdict scoped to subscription path. See `docs/meeting-notes/2026-06-17-0905-model-probe-tos-and-band.md`. <!-- id:2d01 -->
- [x] **Build `tools/model-probe.sh` + `tools/model-probe.battery.jsonl` + log schema** — **DONE 2026-06-17.** 15-item battery (`tools/model-probe.battery.jsonl`); offline subcommands (`grade`, `battery-version`); run mode with `PROBE_MOCK_RESPONSE`/`PROBE_LOG_PATH`/`PROBE_HOME` overrides; config-clean assertion; shape A (probe OS user) + latent shape B (API key). <!-- id:c345 -->
- [x] **Write `tests/test_model_probe.sh`** — **DONE 2026-06-17.** Hermetic, `mktemp -d`, no network; covers grade/battery-version offline, full log-field validation in mock mode, battery-version propagation, dirty-probe-home refusal. 64/64 suite green. <!-- id:040a -->
- [x] **Add `# roadmap:` linkage to `tests/test_model_probe.sh`** once the corresponding ROADMAP item is open. (Placeholder — resolves with id:040a + ROADMAP alignment.) **DONE 2026-06-17** — `# roadmap:040a` header present in `tests/test_model_probe.sh`. <!-- id:6ffe -->
- [x] **Annotate id:de4e with D1 constraints from 2026-06-17 meeting** — update ROADMAP.md id:de4e entry + SEED note (`docs/meeting-notes/2026-06-16-2257-distributed-relay-orchestrator-SEED.md`) with D1 decisions: no-GitHub / peer-or-assume-solo / merge-backstop / quota-ceiling-moot; reject the SEED's ref-CAS-on-GitHub recommendation. Contract: future reader sees the meeting's reframed premise, not the rejected candidate. See `docs/meeting-notes/2026-06-17-0953-k3s-parallelity-coordination-design.md` D1. <!-- id:0b11 -->
- [x] **(DONE 2026-06-17 — TDD, tests green) LIVE REGRESSION: relay state-dir migration (id:10c0) is HALF-DONE → split-brain `~/.config/fables-turn` vs `~/.config/relay`; `/relay human` silently saw an EMPTY backlog** — found 2026-06-17 during a real `/relay human` run. `scripts/gather-human-backlog.sh` defaults `RELAY_TOML=$HOME/.config/relay/relay.toml`, but `relay.toml` still lives ONLY at `~/.config/fables-turn/relay.toml` (NOT migrated). The script read a non-existent file and exited 0 with NO output — the worst failure mode: `/relay human` looked like "no human backlog" when there were **52 open items** (10 REVIEW_ME + 42 gated-HARD). Ran fine once pointed at the real path via `RELAY_TOML=...` override. **Root cause = partial/stalled id:10c0 migration:** `~/.config/relay/` already holds `claims`, `claims.done`, `inject.d`, `inject.done`, `relay-events.jsonl`, `RELAY_STATUS.md`, `fable-probe.json` (so claim.sh/inject.sh/probe-fable.sh/events all use the NEW path), but `relay.toml` was left at the OLD path, and BOTH dirs are REAL directories (neither is a symlink) — `RELAY_STATUS.md` exists in both with different mtimes (live runs split: run 130157 wrote `fables-turn`, run 132749 wrote `relay`). This is precisely the inconsistency id:10c0's design (symlink-old→new + idle guard, `relay/scripts/migrate-state-dirs.sh`) was meant to avoid — it appears a pool hard-execute child began the move (state dirs + script path-defaults flipped to `~/.config/relay/`) without (a) moving `relay.toml` and (b) leaving the back-compat symlink, so the two dirs diverged. **Fix (sequenced):** (1) IMMEDIATE unblock — either move/symlink `relay.toml` to `~/.config/relay/relay.toml` OR have every relay.toml reader fall back old↔new when one path is absent (back-compat read, the safe minimal change); (2) DURABLE — run id:10c0's `migrate-state-dirs.sh` to completion under its idle precondition: reconcile the two divergent dirs (newest-wins per file, esp. the two `RELAY_STATUS.md`), move `relay.toml`, and replace the OLD dir with a permanent symlink→new (the design's safety net), so no reader can split-brain again. **DO NOT hand-mutate state dirs while a pool is live.** Until fixed, any new-path-default relay.toml reader (gather-human-backlog.sh at minimum; audit discover-repos.sh and others) is silently broken. Subsumes/blocks the clean completion of [[id:10c0]]; relates to id:c8b6 (events path), id:15bd (statusline RELAY_STATUS path). **RESOLVED 2026-06-17 (TDD):** the existing `relay/scripts/migrate-state-dirs.sh` was buggy (skipped name-collisions then bailed without symlinking — the half-done cause) AND was never symlinked into `~/.claude` nor run. Rewrote it to RECONCILE collisions (merge `*.jsonl` as a deduped union — no log lost; union dirs; newest-mtime wins for snapshots; drop `*.lock`), honor env overrides for hermetic tests, and exit 3 on the idle-guard refusal. Drove it with `tests/test_migrate_state_dirs.sh` (9 cases, `# roadmap:bbd2`), ran it for real (pool idle): both `~/.{config,cache}/fables-turn` are now symlinks→`relay`, `relay.toml` at the new path, `relay-events.jsonl` merged 347+12→358 (no loss), `gather-human-backlog.sh` returns 48 items on its DEFAULT path (was 0). `make install-relay` linked the script. Full suite 66/66 green. id:10c0 now genuinely complete. <!-- id:bbd2 -->
- [x] **Confirm relay-econ.py before/after for the discover-shard pin (GATE for id:0d31/id:c855/id:2895)** (done 2026-06-18, Opus apex `/relay --afk`) — **CONFIRMED + found+fixed a measurement bug.** Measured by *agent identity* (not the buggy category): discover-shards PRE-pin = **$342.84, 100% Opus** (n=438) → POST-pin = **$80.62, 100% sonnet, $0 Opus** (n=60). The c3a6 Opus leak in discovery is fully eliminated. **DEFECT found+fixed**: `profile-run.sh` PHASE_RULES had a bare `rollup` needle in the `status` rule that matched the shard prompt's `SKIPPED ROLLUP` line (id:be62) *before* the `discover` rule, misfiling ~94% of discovery cost into `status` — so the verification command in this item's old text (trusting `d["cost"]["scaffold"]`) would have read **wrong**. Fixed (removed the `rollup` needle; regression test `tests/test_profile_run_shard_classify.sh`); corrected post-pin attribution: **discover = $86.12 = 47.6% of run cost, 100% sonnet — the #1 line** (was hidden in `status`); `status` is its true $5.90 (haiku writeRelayStatus glue only). **Verification command (corrected):** `python3 relay/scripts/relay-econ.py --json | python3 -c 'import json,sys; d=json.load(sys.stdin); print("scaffold $",d["cost"]["scaffold"],"| by_model:",d["cost_by_model"])'`. **Implication for L1/L2/L3:** scaffold is NOT negligible — it is the dominant overhead — so the levers are well-justified, but the dominant cost is the **sonnet SHARD prompt** ($86), NOT the haiku glue agents L1 originally listed ($5.90). Re-point thinning effort at the shard/prelude prompt. (2026-06-17-0721 meeting; 2026-06-18-0727-relay-skeleton-token-reduction.md) <!-- id:9cb1 -->
- [x] **[skeleton L1] Thin-glue: wrap drift-risky shell-runner agent recipes into named scripts** (done 2026-06-18, covered by tests/test_relay_status_publish.sh) — reframed by user 2026-06-18: the value is NOT cost (haiku glue ~$5.90/run) but RELIABILITY — a long multi-step haiku prompt risks target-drift. Wrapped the ONE genuinely drift-risky glue agent, `writeRelayStatus` (~40-line recipe: path resolve + c34a guard + claim.sh peek→markdown + burnup + conditional event-append) → new deterministic `relay-status-publish.sh`; the haiku prompt collapses to one piped invocation (content + sentinel-framed events on stdin). The other listed agents need NO wrap: `quota`/`release` are already precise one-line script calls; `gaming-log` (4-line append) / `inject-take` are short enough that a wrapper's script+8-allowlist-entry overhead exceeds the gain. Shard-prompt thinning (the high-$ target) is L3, NOT this item, and needs the canary. See 2026-06-18-0727-relay-skeleton-token-reduction.md. <!-- id:0d31 -->
- [x] **[skeleton L2] Push-seed discoverCache from work-agent returns** (done 2026-06-18, covered by tests/test_discover_pushseed.sh) — the LEAD skeleton lever (corrected id:9cb1 measurement: discover-shards are the #1 cost, $86/run, 47.6%, all sonnet). The serialized integrator now recomputes the just-worked repo's discover-sig (AFTER merge+tag+push+toml+worktree-removal, so it matches next round's prelude sig) + open [ROUTINE]/[HARD] counts and returns them (INTEGRATE_SCHEMA postSig/openRoutine/openHard); integrate() seeds an 'idle' cache entry ONLY when provably drained (0 routine AND 0 hard — no EXECUTABLE-HARD judgment, no under-dispatch risk), else deletes the entry. The cache-reuse loop honors idle hits (skip shard, contribute a skipped rollup line, never dispatch). FAIL-OPEN preserved: the seeded sig HITS only when next round recomputes byte-identical (no external change); under-invalidation refused via the drained-only gate. relay-loop.js. See 2026-06-18-0727-relay-skeleton-token-reduction.md. <!-- id:c855 -->
- [x] **[skeleton enabler] shard-canary — behavior safety net for thinning the discover-shard prompt** (built 2026-06-18, covered by tests/test_shard_canary.sh) — the discover-shard prompt is the #1 token line (id:9cb1, ~48% of run cost, all sonnet) and the highest-value remaining lever, but it encodes classifier judgment so thinning is NOT zero-behavior. `tests/shard-canary/` is the golden-corpus harness (mirrors gaming-canary id:414a): fixtures with known verdicts (review/execute/hard/hard-gated/idle/dirty) + `run.sh --prompt-file` to diff baseline-vs-thin and confirm verdicts hold; `make shard-canary` (on-demand, costs tokens); zero-token plumbing guard proves the harness discriminates a wrong verdict. **CALIBRATED 2026-06-18:** `make shard-canary` ran against the real production model (`claude-sonnet-4-6`) — all 6 fixtures passed first-try, no nondeterminism → the baseline is a trustworthy gate. The thinning follow-on is id:11ad (now READY TO START). This is the re-pointed high-ROI half of L1/L3 (id:0d31). <!-- id:3ea3 -->
- [x] **[skeleton] Collapse discover-shard TURN COUNT — classify from one gather call** (done 2026-06-18, validated on shard-canary 6/6 sonnet; tests/test_gather_repo_state.sh + updated guards/tag-scheme tests) — measured driver was TURN COUNT (~121 turns/shard, ~17 git+grep per repo, ~1.9M cache_read = ~92% of shard cost; prompt = 0.7%). Now: `gather-repo-state.sh` emits the full per-repo state (latest_ckpt + commits-since, dirty/porcelain, upstream ahead/behind, worktrees, orphan_refs, toml_block, roadmap) in ONE call; the shard classifies the common case (review/execute/hard/idle/dirty/diverged-detection) from that JSON → ~1 turn/repo instead of ~17 → est ~3x shard-cost cut. Rare MUTATING guards (behind-only ff, worktree reap/park, orphan suppress) stay inline, triggered by gather fields → behavior unchanged. Gated by shard-canary (id:3ea3): the gather prompt passed 6/6 on claude-sonnet-4-6, and the shipped prompt is verdict-line-equivalent to the validated candidate. **Compounds with L2** (L2 cuts how many shards fire; this cuts what each shard costs). NEXT (optional): measure the realized shard-cost drop on the next live pool run via relay-econ. See `docs/meeting-notes/2026-06-18-0727-relay-skeleton-token-reduction.md`. <!-- id:11ad -->
- [x] **[MEETING] Hold a meeting on the 2026-06-18 `/insights` findings — distill lessons learned + further recommendations** — RESOLVED 2026-06-18, see docs/meeting-notes/2026-06-18-1829-insights-findings-triage.md. Verdicts: (b) verify-before-assert→id:f082 CLAUDE.md rule (with id:6ac6); (c) sibling-res→id:35fe one-liner; (d) output-to-file→id:ef77 TODO; (a1) pool-guard→id:11c6 TODO; (a2) dep-cache→folded into id:e79b; pre-commit blocking guard→id:b67e; horizon→ROADMAP direction line; sudo→already done. <!-- id:f1a7 -->
- [x] **Integrator can DESTROY uncommitted edits in a repo's main checkout (data loss)** — fixed by relay 2026-06-18 (clean-tree-gate.sh + integrate-step-1 wiring + git-lock-push autostash refusal; test_integrator_foreign_dirty.sh green). observed 3× on 2026-06-18: while the pool integrated dotclaude-skills, a concurrent uncommitted edit to `relay/scripts/relay-loop.js` vanished (reflog `reset: moving to HEAD`, empty stash list = real loss). The integrate clean-tree check (relay-loop.js ~line 990) is an LLM-agent prompt, not a deterministic gate; the integrator agent most likely force-cleaned the foreign-dirty tree (stash+drop / `checkout --` / `reset --hard`) to do its `--no-ff` merge. `git-lock-push.sh`'s autostash path (line 116) is a second exposure. Forced a `TaskStop` to land an edit safely. Fix: deterministic fail-safe clean-tree gate that DEFERS a repo whose tree carries changes the run didn't create (never force-clean); ban stash/checkout/reset/clean on a main checkout; refuse foreign-dirty on the autostash path. Acute fileable slice of id:3558. ROADMAP `# roadmap:aa93` [HARD]. <!-- id:aa93 -->
- [x] **Discovery shards flailing on the filesystem — no-hunting guard** — fixed 2026-06-18. A discovery shard hitting a repo with parked `orphan_refs` ran `find /home/tobias -name relay.toml` over all of $HOME + cat-ed transcripts + hand-parsed ROADMAPs (36 tool calls vs ~9), pacing the whole barrier round and burning extra Sonnet. Added a NO-FILESYSTEM-HUNTING GUARD to the shard prompt (use the gather JSON's `toml_block`/`roadmap`/`orphan_refs`; never `find` $HOME/cat transcripts; parse the cost-hint runId from the orphan ref basename). Discovery/structure tests green. Follow-up deferred: hoist orphan→ROADMAP binding into the prelude + shard-canary zero-`find` assertion (tracked in ROADMAP). ROADMAP `# roadmap:612f`. <!-- id:612f -->
- [x] **Bug A — relay-loop.js silently drops nested quota-threshold args** — fixed by relay 2026-06-18 (98511a0). a caller passing `args.quotaThresholds = {SEVEN_DAY: 0.70, ...}` is ignored; the script only forwards flat `RELAY_QUOTA_THRESHOLD_SEVEN_DAY[_SONNET]` keys (lines ~901–904). 2026-06-18: a "raise 7d cap to 70%" directive had zero effect twice → the standing `RELAY_QUOTA_DECAY_7D=0.40:0.18` cap governed instead, stopping at ~33.5% Sonnet. Fix: normalize a nested `quotaThresholds` map into flat keys in the args block; SKILL.md front door translates "7d cap X%" → both flat keys. ROADMAP `# roadmap:b841`. <!-- id:b841 -->
- [x] **Bug B — relay quota stop-reason mislabels as `quota-exhausted:unknown`** — fixed by relay 2026-06-18 (d3acf21). bucket attribution (~line 936) uses a hardcoded `pctRemaining <= 10` finder (old 90%-cap assumption), so a stop on a decayed/overridden threshold below 90% utilization names no bucket. Fix: return + use the actually-crossed bucket from `quota-stop.sh`. ROADMAP `# roadmap:2425`. <!-- id:2425 -->
- [x] **Relabel ROADMAP id:de4e — stale "DECISION GATE" heading** — id:de4e (distributed relay orchestrator) reads as "Needs a /meeting (do NOT execute)" but the meeting was already held 2026-06-17 and it is already-decided-DEFERRED on quota economics (GitHub-CAS rejected; cross-machine throughput moot until quota changes). The label contradicts the resolved block beneath it and could mislead a future dispatcher into thinking a decision is owed. Adjust the heading to "DEFERRED (decided 2026-06-17)". Done 2026-06-18 (review): ROADMAP heading + first bullet now read DEFERRED. See `docs/meeting-notes/2026-06-18-1219-cross-gated-hard-triage.md`. <!-- id:9c92 -->
- [x] **[HARD — strong model] Rename relay state dirs `~/.{config,cache}/fables-turn` → relay naming convention** — done by relay (998e8de, green; live dirs now `~/.config/relay/` + `~/.cache/relay/` with back-compat symlinks). legacy "fable's turn" naming; the relay is now Opus-apex/Fable-bonus, so the live state dirs should follow the relay name (`~/.config/relay/` + `~/.cache/relay/`). Scope (measured 2026-06-16): **33 `fables-turn` path refs across 13 executable files** + `.gitignore`, `Makefile`, `.claude/settings.local.json`, and ~25 doc files. **Constraints:** (a) git tags `fable-ckpt-*` stay HISTORICAL — scope is the DIRECTORIES only. (b) live dirs an in-flight pool reads/writes — needs back-compat. (c) run only when NO relay pool is active. **BACK-COMPAT DESIGN RESOLVED 2026-06-17 (Opus apex, via /relay human): symlink-old→new + run-when-idle, symlink kept permanently as the safety net; `relay/scripts/migrate-state-dirs.sh` does idle-precondition guard → mkdir new → mv → replace old dirs with symlinks. UNGATED — now a bounded Opus hard-execute; full acceptance in ROADMAP id:10c0.** <!-- id:10c0 -->
- [x] **Handbacks must DIRECTLY create durable follow-up TODOs (auto-split `[HARD]` seams OR `[MEETING/HUMAN]`) + gate the parent against re-dispatch** — user 2026-06-17. Today a size-out / gated / blocked handback returns a rich REPORT (the zkWhale id:f14f handback even enumerated a clean 3-seam split: signPsbt+OP_RETURN PSBT builder / amount-nonce poll+localStorage / funnel UI state-machine; the dotclaude-skills id:dba3 handback identified the OS-user+meeting gate id:d0c0) — but that judgment **evaporates** into RELAY_STATUS for one run and nothing durable is created. WANTED: on a handback, the integrator (or a cheap follow-up agent) DURABLY records adequate follow-up items in the repo's TODO/ROADMAP under the **same id-ecosystem** (single-id-two-views), choosing per the handback's own classification: **(i) too-large-but-decomposable** → mint child `[HARD — strong model]` seam items from the child's recommended split (each executor/strong-sized) + leave the parent OPEN; **(ii) decision-gated / needs-design** → mint or re-tag a `[HARD — decision gate]` / `[MEETING]` / `[HUMAN]` item so a `/meeting` or `/relay human` picks it up; **(iii)** in BOTH cases ADD THE GATE so the pool stops re-dispatching the parent every round. **Gate partially exists already** (id:2d20: the classifier's EXECUTABLE-HARD test excludes `[HARD — decision gate]` / Gated-section items and surfaces them; id:2d20 option (c) — durable auto-re-tag of a freshly-handed-back HARD to `[HARD — decision gate]` — is the OPEN durable half). This item GENERALIZES id:2d20(c): not just re-tag-to-gate, but **create the right follow-up work** (split-into-seams vs route-to-meeting/human) so the backlog moves forward instead of stalling. **Design forks (likely a `/meeting`, heavy overlap with id:719e/id:2d20/id:194e):** (a) who authors the split — trust the handing-back child's one-shot proposed-seams (add a `proposed_split: [...]` / `route: hard-split|meeting|human` field to the REPORT schema) vs a dedicated splitter agent re-reading the item; (b) where the items are written + dedup so a re-run doesn't mint duplicate seams (idempotency key on parent-id + seam-title; reuse existing ids per single-id-two-views, never mint a duplicate for already-tracked work); (c) routing heuristic hard-split vs meeting/human (size + decision-gated + dependency-blocked signals); (d) how the parent's gate is invalidated when the seams are done / the meeting clears it; (e) trusting a one-shot "too-large" verdict enough to spawn durable items (anti-gaming: the next review re-checks). Builds on id:2d20 (drain + classifier exclusion), feeds id:719e (DMAIC: handback rework→durable forward progress), uses id:194e (failure-reason category as the routing signal) + id:c8b6 (event log). Concrete first targets to validate against: zkWhale id:f14f (3-seam split), dotclaude-skills id:dba3 (gate on id:d0c0); leAIrn2learn id:d61a (4th identical handback 2026-06-19 — manually re-gated `[ROUTINE]`→`[HARD — decision gate]` by hand with the a/b decision spelled out, exactly the case-(ii) auto-gate this item should perform so the pool stops re-dispatching it; the by-hand fix is the proof the automation is overdue). **SHIPPED 2026-06-19** — `relay/scripts/handback-followup.py` (durable auto-gate / auto-split of a handed-back item: decision-gate/human → re-tag parent to `[HARD — decision gate]`; hard-split → gate parent + mint the child's `proposed_split` seams as pickable units; idempotent by id + title-dedup; flock'd `md-merge.py` write + `--ff-only` commit/push) + REPORT-schema fields `route`/`handback_item`/`gate_reason`/`proposed_split` (relay-loop.js + handoff.md/review.md contracts) + fire-and-forget integrator wiring (`durableHandbackFollowup`, non-fatal, Haiku agent — the sandbox can't shell). Covered by `tests/test_handback_followup.sh` (7 cases incl. both idempotency paths + manual-gate respect); suite 76/0, `node --check` clean. Live end-to-end validates on the next pool handback. Deferred refinements (low-pri follow-ups): (d) auto-close the parent when all seams tick; richer `human` route → mint a REVIEW_ME box rather than gating. <!-- id:3801 -->
- [x] **`relay-reconcile.sh --all` — mechanize the cross-repo orphan sweep** — `--all` added: enumerates relay.toml `own` repos via `own_repos()` parser (copied from gather-human-backlog.sh), lists each repo's `relay/orphan/*` branches, surfaces unreadable repos on stderr (never swallows). `--all --integrate` combination rejected. SKILL.md reconcile section updated with canonical cross-repo sweep command. <!-- id:4e14 -->
- [x] **TODO↔ROADMAP seam: mechanize promotion-tracking + derive the count line** [ROUTINE] — the single-id-two-views split is SOUND (TODO=design ledger "why" + portfolio/non-executable items; ROADMAP=lean executor-facing "now"); do NOT merge them. The friction is the hand-done SYNC. Two concrete gaps, both hit this session: **(1) promotion is forgettable** — a TODO item that becomes executable must be hand-copied into ROADMAP with a lane; id:78ff was filed TODO-only and the pool couldn't see it until manually promoted. Add a check (todo-update or orphan-scan) that flags a TODO item carrying an executable lane (`[ROUTINE]`/`[HARD — pool]`) whose `id:` has NO ROADMAP twin → "un-promoted, pool-invisible". **(2) the `Relay: N open ROADMAP items` summary line drifts** — d5e0 (+ project_manager d1dc) are hand-maintained prose the recurring audit (id:401c) keeps re-fixing, yet `proj`/`scan.py` already compute those counts mechanically → DERIVE the count (or drop the prose line, point at `proj relay`). Builds on `orphan-scan.sh --cross-ledger` (existing intra-repo checkbox-drift guard). Relates id:962a (dedupe the contract prose), id:69f4 (cross-PROJECT same-id sync), id:415b (umbrella), and is partly dissolved by id:2840 (the derived-index vision). Acceptance: a check that catches an un-promoted executable TODO item + a derived/removed count line + tests. Promoted to ROADMAP (dogfood the promotion it fixes). **CLOSED 2026-06-22:** gap-1 promotion-tracking + the scope-split `xledger-ok` tooling shipped (`orphan-scan --promotion`/`-x` + `tests/test_ledger_seam.sh`). Residual gap-2 (count-line derive-or-drop) split out to **id:1de1** (gated on the id:2840 derive-vs-defer decision) so this executable unit can close without an open cross-ledger scope-split. <!-- id:d9b0 -->
- [x] **[ROUTINE] Fix the misleading `loop-hint.sh` / step-0a "unattended resilience" nudge** — the nudge currently implies `/loop` gives unattended outage-resilience; correct it to state `/loop`/cron dies WITH the session (resilient only to relay's own early-exit — quota/seatbelt — within a live session, NOT to a session/process kill). Cheap, ship now; independent of the watchdog build. Pairs with id:888a/id:8602. Files: `relay/scripts/loop-hint.sh` + SKILL.md step 0a. From `docs/meeting-notes/2026-06-22-1546-relay-outage-resilience.md`. <!-- id:bde8 -->
- [x] **Auto-handle uv.lock drift in the relay pool** (user request 2026-06-12; RESOLVED 2026-06-22) — pilot run relay-20260612-2304: a parent-zkm version bump (0.14.0) left ` M uv.lock` in plugin checkouts, blocking 2 integrations (zkm-calendar, zkm-eml) + 1 dispatch (zkm-scan); resolution was mechanical (`git add uv.lock && commit "chore: refresh uv.lock"`). Levers, combine as needed: (a) integrator prompt: if porcelain shows ONLY `uv.lock` modified, auto-commit `chore: refresh uv.lock — dependency re-resolution` before the merge instead of aborting; (b) discovery: same uv.lock-only exemption so the repo still dispatches (child regenerates in its worktree); (c) prevention: executor conventions.md mandates `uv run --frozen` / `uv sync --locked` so test runs can't silently mutate the lock — drift then only enters via deliberate dep changes; (d) zkm release flow: bump plugin lockfiles as part of the parent version-bump commit. Guard: the exemption must match uv.lock ONLY — any other dirty path still aborts. **RESOLVED 2026-06-22 (user "fix this once and for all"):** root cause was the *parent zkm's own* uv.lock self-pin going stale on bump (0.15.0 while pyproject=0.16.0) cascading into 15 plugin locks scattered across 0.14/0.15/0.16 (some committed-stale, latent landmines). Fixes shipped: **(d) generator** — `zkm/scripts/relock-plugins.sh` (relock every plugin + `--check`/`--push`/`--install-hook`) + `zkm/scripts/hooks/pre-push` guard that REFUSES a version-bump push while the parent or any plugin lock is stale/uncommitted (tested: blocks stale-bump, allows non-bump + in-sync); all 15 plugins + parent relocked/committed/pushed. **(a)+(b) relay tolerance** — `gather-repo-state.sh` now emits `lock_only_unaudited` + `dirty_lock_only` booleans (conservative: only the unambiguous root `uv.lock`; any other path defeats them; `test_gather_repo_state.sh` cases 9a–9d), and `relay-loop.js` discovery exempts them: a uv.lock-only unaudited commit is audit-exempt (no review), a uv.lock-only dirty tree is committed-in-place then classified (no block, no worktree-merge hazard, no thrash). **(c) `uv ... --frozen` executor mandate was NOT taken** (user did not select it) — optional future hardening; drift can still enter via a deliberate dep change, which is correctly reviewable. <!-- id:bae5 -->
- [x] **`/meeting` deferred ledger write-back: breadcrumb + replay-on-next-invocation + log** — when step 2a's relay-pool claim is refused, the meeting currently DEFERS the `<root>` ledger write-back to a manual `/todo-update` re-apply (decisions stay durable in the note). Meeting 2026-06-22 (af04) ruled this is step 2a *working as designed* (no data loss) — the only gap is that the deferral never auto-completes. Fix: on a refused claim, persist a **generic** `{target_file, helper, payload}` JSON to a gitignored drop path (`<root>/.meeting-deferred-writeback.json`) AND append an event to `~/.claude/logs/meeting-deferred-writeback.log`; add a setup-phase replay check (in `/meeting` setup + `/todo-update`) that applies any pending payload via the named helper (`md-merge.py`/`append.sh`) under a fresh `claim.sh acquire`, then clears the drop file. Payload format generic so it extends if another defer site appears, but WIRED IN only at step 2a (the sole site that defers today — `~/.claude` shared files are flock-safe, no pool there). Test contract: a deferred write-back leaves a replayable payload; the next invocation applies it under a fresh claim and removes the drop file; nothing is applied while the pool still holds the claim. Add `.meeting-deferred-writeback.json` to `.gitignore`. **Also:** add the af04 motivation as a cross-link note on id:3558 and record that the literal worktree-per-meeting option was REJECTED by this meeting (inherits id:ca87's non-unionable-checkbox merge problem; contradicts id:ca87 `/meeting`=same-dir→flock + worktrees-are-code-only). See `docs/meeting-notes/2026-06-22-2139-meeting-worktree-writeback.md`. <!-- id:2c42 -->
- [x] **Margin-aware quota-stop staleness** (incident 2026-06-23 run relay-20260623-070136) — the `--afk` pool false-stopped (`stopReason=quota-stale-cache`) at 7%/38%/25% util vs a 90% cap because `quota-stop.sh`'s staleness fail-safe does an unconditional `exit 2` when the cache is stale + the self-refresh 429s, ignoring the (healthy) last-known reading. Fix in ROADMAP id:1d64: proceed on a stale-but-SAFE reading (every checked bucket < threshold − MARGIN, default 30pts); keep `exit 2` only when near-threshold or the cache is missing. Same false-stop family as the `RELAY_QUOTA_DECAY_7D` warning. Optional follow-up: respect `/tmp/claude-usage-backoff` + reuse the statusline cache. <!-- id:1d64 -->
- [x] **`--afk` pool reports an INTENSIVE-only repo as "idle — no open work" instead of dispatching the INTENSIVE unit** (observed 2026-06-23 `/relay --afk`, run relay-20260623-112409) — ai-codebench has an open `[INTENSIVE — local-llm]` item (id:244b, ~15 model runs pending) and the run set `allowIntensive=true`, yet the discovery shard classified the repo `idle — in sync, no open work` and never emitted an intensive-tagged unit, so `relay-loop.js`'s INTENSIVE partition (L877, `ALLOW_INTENSIVE ? intensiveUnits : intensiveDeferred`) never got a unit to dispatch. **Not a gather bug:** `gather-repo-state.sh --repo ai-codebench` correctly emits `is_finished=false`, the open `[INTENSIVE]` box, and id:244b (verified this session). **Root cause = shard JUDGMENT:** the shard never sees the `--afk`/`allowIntensive` flag (it's a JS-level decision), and the repo's own ROADMAP convention prose says *"a session finding only `[INTENSIVE]` items open reports 'no executor-eligible work' and stops"* — written for generic/remote executors with no GPU. The shard reads that prose literally and reports the repo idle, short-circuiting the intended design (always emit an intensive unit; let the loop decide dispatch by `ALLOW_INTENSIVE`). **Fix direction:** make the discover-shard prompt treat an open `[INTENSIVE — <res>]` item as WORK unconditionally — emit a unit with `intensive` set and NEVER report idle for it — leaving the auto-run-vs-defer decision to `relay-loop.js` (it already surfaces deferred intensive units as skipped when `!ALLOW_INTENSIVE`). The repo-local "reports no executor-eligible work" convention must not override the shard's emit. Add a shard-canary case: INTENSIVE-only open repo → classified as an intensive unit, not idle. Distinct from id:000d (false-dispatch on FINISHED repos — the inverse) and a natural check to fold into the relay-health report id:0907. Directly blocked the user's overnight ai-codebench drain (id:244b/8bea). **Promoted to ROADMAP [ROUTINE] (review Run 47, single-id-two-views) with acceptance + red spec `tests/test_relay_loop_intensive_emit.sh`.** <!-- id:ad74 -->
- [x] **Recurring `[HARD — pool]` audit (id:401c) spins forever when it's the only open item — vacuous self-audit loop burns Opus** — DONE 2026-06-23 (both mechanisms shipped, suite 91/0/0): gather `substantive_unaudited`+`work_sig` primitive (ignores the pool's own `relay: checkpoint` commits) + mechanism 1 shard recurring-audit gate (id:401c marked `relay:recurring-audit`, idled when nothing new to audit) + mechanism 2 deterministic `redispatchGuard` circuit breaker (suppress any non-injected (repo,verdict) dispatched >3× with unchanged work_sig; injected exempt). (observed 2026-06-23 `/relay --afk`, run relay-20260623-172446) — once every genuine `[ROUTINE]`/`[HARD — pool]` item in dotclaude-skills had drained, **id:401c (the RECURRING strong-model audit, which never closes by design) became the only open pool-dispatchable item**, so the `hard` verdict (id:da26) re-selected it EVERY round. Each round audited "commits since last checkpoint" — but the only commit since the prior round WAS that round's own `relay: checkpoint …` commit, so every audit reported *"LEDGER-ONLY clean by vacuity — no findings"* and wrote a fresh checkpoint, which the next round then audited. **~13 consecutive vacuous rounds (audit Run 55→67, ~20:01–21:47, ~9 min each) drove the 5h quota bucket 98%→41% for ZERO output**, and each checkpoint commit changed HEAD → invalidated the discover-sig cache (id:c3a6) → forced a re-classify, so the cache couldn't damp it. **Root cause:** a never-closing recurring HARD item + no guard for "the audit window contains only the pool's own checkpoint commits." **Fix directions:** (a) the `hard` scheduler (or the 401c audit step) must treat an audit window of ONLY relay-checkpoint commits (no real/new code-or-ledger commits) as **nothing-to-audit → idle/skip**, not a dispatch (mirrors the id:000d is_finished demote-guard, generalized to "no NEW auditable work"); (b) a per-item **cooldown / min-interval** on recurring HARD items (e.g. don't re-fire id:401c more than once per N hours or until ≥1 non-ckpt commit lands); (c) make a recurring audit's "open" state CONDITIONAL — it's only dispatchable when there are unaudited *substantive* commits since `last_review`/`last_strong_ckpt`, else it sits dormant. Without this, ANY repo that drains to "only the recurring audit open" will silently spin and burn the apex tier. Caught only because the user asked "stuck or catching up?"; a natural check to fold into the relay-health report (id:0907) AND a strong argument for the exit-reason/idle-detection work (id:8602). Relates id:401c (the recurring audit), id:da26 (`hard` verdict steady-state), id:000d (finished-repo demote guard — the analogue), id:c3a6 (sig cache, defeated by the ckpt-commit churn). <!-- id:365b -->
- [x] **DONE 2026-06-23 (shipped + tested) — First-class per-run priority + exclusion args for the autonomous pool — `--priority <repo[s]>` / `--exclude <repo[s]>` without mutating relay.toml** — `relay-loop.js` reads `args.priorityRepos`/`args.excludeRepos`: EXCLUDE drops repos from the own list before sharding (skipped `excluded for this run (--exclude)`, no relay.toml write); PRIORITY is a per-run within-class ordering bump (above income, below injected + D3 verdict class), reorders the repo's natural unit only (no double-dispatch — the finding). Unknown names LOUD-rejected/surfaced. Logic in pure helper `relay/scripts/pool-args.mjs` (byte-equiv inline copies pinned by `tests/test_relay_pool_args.sh`); SKILL.md front door documents both run-scoped flags. ROADMAP id:d530. — observed 2026-06-23 (`/relay --afk priority on zkm-stt, exclude codebench-ai`): the front door has NO arg for either, so both had to be hand-worked: priority via `inject.sh add zkm-stt` (the raw script — abuses the user-injection mechanism id:baf1, which is really "do this NEXT/once", not "this repo ranks higher all run"), and exclusion via a **temporary `classification = "own"->"excluded"` edit to relay.toml** that must be manually reverted after the run (a destructive mutation of the sticky registry — survives a session-kill mid-run = silent permanent exclusion). Neither is a clean per-invocation knob. **Goal:** `/relay [--afk] --priority <repo|repo,repo> --exclude <repo|repo,repo>` (also accept the natural-language forms the user typed) parsed by the front door and passed as `args.priorityRepos` / `args.excludeRepos` into `relay-loop.js`, scoped to THIS run only, registry untouched. Semantics to design: **priority** = bump those repos to the front of slot contention WITHIN their verdict class (a per-run tier above `income`, but still under the D3 verdict-class order + injected-unit precedence — NOT a verdict override), distinct from `inject` (which forces a one-shot unit); **exclude** = the discovery shards simply never emit a unit for those repos (surface them in `RELAY_STATUS.md` Skipped as "excluded for this run (--exclude)"), no relay.toml write. Reject unknown/unconfirmed repo names LOUDLY (id:415b grammar). **Observed 2026-06-23 (same session):** approximating priority via `inject.sh add zkm-stt` caused a REDUNDANT DOUBLE-DISPATCH — the injected unit ran ahead AND normal discovery still emitted its own zkm-stt execute unit the same round (events log: two `dispatch repo=zkm-stt mode=execute round=1`; the 2nd integrate logged "already pushed (integration completed by previous relay run); worktree already cleaned"). Concrete proof that priority must be an ORDERING bump within the verdict class, NOT a separate injected unit (which `inject` is) — `--priority` must dedupe against / merge with the repo's naturally-discovered unit, never add a second one for the same repo. Relates id:354f (`/relay inject` — the one-shot cousin), id:1ffc (richer income ranking — priority could read the same weight), id:a02c (uniform scope-arg grammar — `--exclude` is the inverse of a repo-list scope), id:11c6 (front-door arg parsing). Natural fold into the scope-arg-grammar work. <!-- id:d530 -->
- [x] **Patient stop (merged → id:c012)** — deduped 2026-06-24 into [[id:c012]] (`/relay stop` graceful drain-then-end); its unique design bits (`--once`, "drop queued units, don't re-discover", `discover-prelude` sentinel check, 2026-06-15 origin) were folded there. Single-id-two-views: one canonical id for this mechanism. <!-- id:3b1e -->
- [x] **Deterministic demote-guard: a `hard` verdict for a repo with NO open `[HARD — pool]` item is surfaced-as-gated, not dispatched** — DONE 2026-06-24 (merged 1cfbee7): gather-repo-state.sh emits `open_hard_pool`; relay-loop.js JS demote-guard (extends id:000d/365b); `tests/test_relay_demote_guard_hard_pool.sh`. Stops the LLM-shard non-determinism that size-out-churned proton-moresync/leAIrn2learn. <!-- id:9973 -->
- [x] **Relay health / sanity check aggregator** — DECIDED 2026-06-24 (`docs/meeting-notes/2026-06-24-1631-relay-doctor-scope.md`): one report-only `relay-doctor.sh` engine; D1 home = standalone script + `/relay health` alias + a `/relay review` sub-step; D2 cadence = on-demand + at-review, NO timer (observe-first); D3 report-only default + `--strict` opt-in (surface to RELAY_STATUS/REVIEW_ME, never hard-block; roadmap-lint LOUD reject preserved); D4 ship cheap-now checks + LIST not-yet-wired ones (e149/c3a6) for honest coverage. DECOMPOSED → id:9bec (engine, DONE) + id:3eb5 (front-door wiring) + id:a883 (--strict + quota-config check). e149-gated checks fold in when id:e149 lands. <!-- id:0907 -->
- [x] **[HARD — pool] `/relay stop` — graceful drain-then-end signal for a running pool (no orphaning)** — built 2026-06-24 (direct strong-turn, not pooled): STOP sentinel checked by the discover-prelude (content = rounds-remaining countdown, consumed on fire) → `stopReason="user-stop"` patient drain (current wave + integration debt drained, queued units dropped, no re-discover); launch-time `--once`/`--after N` round caps; front-door `/relay stop` / `--now` / `--after N` documented in SKILL.md `## Stop mode`. Covered by `tests/test_relay_graceful_stop.sh` (`# roadmap:c012`). Absorbed former id:3b1e. <!-- id:c012 -->
- [x] **relay-doctor front-door wiring** — add a `/relay health` SKILL.md mode (on-demand, cwd/`--all` scope, wraps `relay-doctor.sh`) + a `/relay review` sub-step that runs relay-doctor and surfaces findings as REVIEW_ME boxes (never a hard block). Child of id:0907 (D1). DEP: id:9bec. [ROUTINE] — done 2026-06-24 (closed in ROADMAP at d2a9d98; TODO twin ticked by review) <!-- id:3eb5 -->
- [x] **relay-doctor `--strict` + quota-config sanity check** — add `relay-doctor.sh --strict` (nonzero exit on findings, for a deliberate CI-style gate; default stays report-only) + fold in a quota-config sanity check (RELAY_QUOTA_DECAY_7D direction, threshold bounds). Child of id:0907 (D3/D4). DEP: id:9bec. [ROUTINE] — done 2026-06-24 (closed in ROADMAP at d2a9d98; TODO twin ticked by review) <!-- id:a883 -->
- [x] **`orphan-scan.sh --cross-ledger` false-positive: reports drift when the `<!-- id:XXXX -->` token lines AGREE** — found 2026-06-24 by dogfooding `relay-doctor.sh --all`. CONFIRMED repro (isochrone): `orphan-scan.sh --cross-ledger` reports `id:f4a7 — TODO:[x] ROADMAP:[ ]`, but the actual `<!-- id:f4a7 -->` token line is `- [ ]` in BOTH files and NO `[x]` line anywhere contains the string `f4a7` (the id is also mentioned in prose on 3 sibling child lines `f4a7-A/B/C`, all `[ ]`). Control: `id:601e` in the same repo is reported correctly (genuinely `[ ]` TODO / `[x]` ROADMAP). So the check mis-attributes a checkbox NOT belonging to the id's own token line — likely a substring / multi-mention / adjacent-id confusion in how it pairs the id to a checkbox (it must correlate STRICTLY on the single line bearing `<!-- id:XXXX -->`, exact-token, not a prose/substring match). **Why it matters:** this is the interim TODO↔ROADMAP drift backstop that id:2840 leans on AND a relay-doctor input (id:9bec) — a noisy mix of real + phantom drift erodes trust in both. **Acceptance:** a hermetic test with (a) an id whose token lines agree but whose id is mentioned in prose on other lines → reported CLEAN; (b) a genuine token-line disagreement → reported DRIFT; orphan-scan correlates only the `<!-- id:XXXX -->`-bearing line's checkbox. [ROUTINE] — done 2026-06-24 (fix shipped d2a9d98 + hermetic test; TODO twin ticked by review) <!-- id:9221 -->
- [x] **Gate-detection (id:3801) must commit its main-checkout ROADMAP edits atomically** [HARD — pool] (done 2026-06-24, relay HARD child: `relay/scripts/commit-ledger.sh` + review.md/human.md wiring) — 2026-06-24: found uncommitted id:3801 gate-detection residue (lane migrations + `🚧 GATED` annotations + seam decompositions) in the MAIN checkout of 5 own-repos at once (trAIdBTC/zkm-ner/proton-moresync/puzzle-pwa/zkm-pdf), each tripping the dirty-guard (id:aa93) so every pool run deferred them — a self-perpetuating dirty backlog the review-that-would-commit-it can never reach. Cleaned manually this session. Structural fix: every relay main-checkout ledger write (gate-detection in review.md/human.md, re-derive-roadmap, lane back-fill) must commit atomically per-repo via the flock'd md-merge.py path (id:148b precedent). Complement to id:aa93 (don't DESTROY foreign-dirty) — this stops CREATING the residue. Full spec in ROADMAP.md. <!-- id:2147 -->
- [x] **Workflow-script template-literal lint — catch unescaped backticks before the live Workflow parser** [HARD — pool] — 2026-06-24: id:9973 left backtick-wrapped `` `hard` `` unescaped inside `relay-loop.js`'s `shardPrompt` template literal; `node --check` + `make test` passed but the Workflow parser rejected the script (`Unexpected token (763:1527)`) → `/relay --afk` couldn't launch (fixed ad-hoc 178b8db). Built `relay/scripts/lint-workflow-templates.mjs` (lexer-aware) + `tests/test_workflow_template_lint.sh` wired into `make test`. — done 2026-06-24 (relay HARD child, merged 8b45707; suite green) <!-- id:71f2 -->
- [x] **Un-promoted TODO backlog is invisible to relay routing → repos falsely read as "drained"** [ROUTINE] — DONE 2026-06-25 (ROADMAP id:2dea): `relay/scripts/unpromoted-scan.sh` (lane-tag-agnostic; `promote`/`surface`/`untracked` dispositions, the last catching id-less open checkboxes like truncocraft's favicon) wired into `relay-doctor.sh` (escalates to "needs HANDOFF" only when the ROADMAP is genuinely DRAINED — otherwise informational, so a design-ledger repo isn't flagged) + the `/relay next` route-2a pre-check + handoff.md C2 ("ROADMAP closed ≠ nothing to hand off"); `tests/test_unpromoted_scan.sh` + full suite green. — LIVE evidence 2026-06-25 (truncocraft): `/relay next`/`review`/handoff gauge "is there work?" from OPEN ROADMAP items + unaudited commits ONLY, never from open `TODO.md` items lacking a ROADMAP twin. truncocraft's ROADMAP was fully `[x]`-closed while TODO held 5 open executable items (`b1e4`/`a7d2`/`c3f7`/`a03c` + favicon) — so prior `/relay` runs (and an apex turn) reported the repo DRAINED and the un-promoted backlog sat idle for days, even though promoting TODO→ROADMAP is exactly handoff C2's job. **Second instance** (first: id:78ff, TODO-only HARD-pool the pool couldn't see). The existing d9b0 promotion-tracking note (ROADMAP ~142.1) only catches TODO items *already lane-tagged* `[ROUTINE]`/`[HARD]` — truncocraft's were UNTAGGED raw backlog, so they slip past it; d9b0 was ticked `[x]` but this never shipped as a routing-visible signal. **Build (ROADMAP id:2dea, single-id-two-views):** a mechanical scan listing open TODO ids with no ROADMAP twin (lane-tag-agnostic), wired into `/relay next`/`review`/`relay-doctor.sh` so a closed-ROADMAP/open-TODO repo surfaces "N un-promoted — needs handoff" and `/relay next` routes to handoff, never "drained." Ambiguous-lane items SURFACE for strong-turn triage, never auto-promote. Relates id:2840 (durable home), id:3947 (cross-repo dead-letter sibling), id:9bec (relay-doctor). **Filed as a ROADMAP item, not TODO-only — that IS the lesson.** <!-- id:2dea -->
- [x] **META-lesson (this session): an apex turn judged "handoff = no-op" from ROADMAP closure alone** — DONE 2026-06-25: encoded in `relay/references/handoff.md` C2 (first check is the open TODO backlog via `unpromoted-scan.sh`, not the ROADMAP open-count) and the `/relay next` route-2a ladder. — 2026-06-25, before id:2dea was filed: asked to run handoff+review on truncocraft, the strong turn read the fully-`[x]` ROADMAP and concluded "both modes find an empty repo," missing the 5 open TODO items handoff exists to promote. The human had to point it out twice. Encode in `relay/references/handoff.md` (and the `/relay next` ladder): **"ROADMAP closed" ≠ "nothing to hand off" — handoff C2's first check is the open TODO backlog (incl. untagged lines), not ROADMAP open-count.** Fold into id:2dea's SKILL/reference wording. <!-- id:2dea-ref -->
- [x] `--quota-7d <pct>` / `--quota-5h <pct>` front-door switches for the per-bucket quota cap (ride the id:b841 nested-map plumbing; explicit per-bucket wins over RELAY_QUOTA_DECAY_7D; accept bare-percent or fraction) — documented in relay SKILL.md knob table on 2026-06-25 <!-- id:20bd -->
- [x] PID-anchored claim liveness (`claim.sh` `live_pid`/`--pid` + `acquire-resource.sh --pid` adopt) so a standalone multi-hour drain isn't reaped mid-run; adopted the live `ai-codebench-drain.sh` (PID 23282) into a `resource:local-llm` claim — covered by tests (test_resource_claim_pid.sh) on 2026-06-25 <!-- id:1b11 -->
- [x] **`roadmap-lint.sh` false-positives on the heading-as-item ROADMAP convention** [ROUTINE] — DONE 2026-06-26: `roadmap-lint.sh` + `todo-conformance.sh` now recognize the `## [LANE] Title <!-- id -->` heading-as-item shape — the heading owns the lane+id and its `- [ ] Open`/`- [x] Done` status sub-lines are not flagged as separate items (but a heading-item MISSING its id IS still flagged; positive grammar preserved). Collaib-shaped fixtures added to `tests/test_roadmap_lint.sh` + `tests/test_todo_conformance.sh`; suite green. Surfaced during the cross-repo conformance sweep. <!-- id:c095 -->
- [x] **standalone intensive jobs acquire a relay `resource:<name>` claim** [ROUTINE] — DONE 2026-06-25 (relay HARD child id:da26): `relay/references/resource-claims.md` vocab + `relay/scripts/acquire-resource.sh` wrapper + collision test `tests/test_resource_claim.sh`; composes existing claim.sh (id:ebfb), no new lock. Residual (out-of-repo): prefix the un-versioned local `ai-codebench-drain.sh` run with `acquire-resource.sh local-llm --run drain-$$ --` where that operator file lives. Covered by tests (test_resource_claim.sh). <!-- id:a643 -->
- [x] **Quota gate runs AFTER the discovery fan-out → wasted shards on a round that immediately stops** [ROUTINE] — observed 2026-06-25 (`/relay --afk . --quota-7d 50`): round 1 spent 5 discovery shards / ~94k tokens, THEN quota-stopped (`quota-stale-cache`, 0 units). The stop was CORRECT (id:1d64 margin fail-safe: 6pt headroom < 30pt margin under a stale cache), but the ORDERING burned the discovery wave first. Fix: a cheap quota PRE-GATE at the top of `runRound()` before the discover-prelude/shard fan-out → early-return with 0 shards when over threshold (keep the existing post-discovery gate too). ROADMAP id:5c00. Relates id:1d64 (correct but too-late-in-round). <!-- id:5c00 -->
- [x] **Surface the ROADMAP/TODO `id:` a relay unit is/was working on** — SHIPPED 2026-06-29 (covered by tests): (a) dispatch label appends a known-at-dispatch id (injected/hard) → `execute:zkm-stt id:09a3`; (b) REPORT_SCHEMA `worked_ids` (review falls back to verified-green∪reopened) propagated into the ckpt-tag message, RELAY_STATUS Completed line (`ids=…`), and relay-events integrate event. `tests/test_relay_worked_ids.sh`. Full design in ROADMAP id:de69 + git history. <!-- id:de69 -->
- [x] **Finer-grained `/workflows` phase buckets** — SHIPPED 2026-06-29 (covered by tests): `relay-loop.js` `meta.phases` + agent `phase:` opts split the floods — discover-shard→Classify, write-relay-status→Status, gaming-log/handback-followup→Logging, quota→Quota, release→Leases; Integrate=merge only; Support=injection/heartbeat/auto-reconcile. Display-only. `tests/test_relay_phase_buckets.sh`. Full design in ROADMAP id:7c10 + git history. <!-- id:7c10 -->
- [x] **Fleet-quiescence drain** — SHIPPED 2026-06-29 (covered by tests): `relay/scripts/drain.mjs` (`unitIsSubstantive` — a confirming-only review is not progress; `classifyDrainBacklog` — gated/finished/cb/dirty buckets + `/relay human`·`/meeting` pointer) + byte-identical inline copies in relay-loop.js (dry-detector `produced→substantive`; wind-down summary). Subsumes the cwd self-review footgun. `tests/test_relay_drain.sh` (15 assertions). Full design/diagnosis in ROADMAP id:d58f + the git history. <!-- id:d58f -->
- [x] **Babysitter loop cannot survive a hard session kill** — RESOLVED 2026-06-29 (observe-first watchdog SHIPPED): `tools/relay-watchdog.sh` + `.service`/`.timer` (`make install-relay-watchdog`) — a systemd --user timer that detects a dead loop via the shared run-heartbeat (id:e149) and NOTIFIES once per dead run (PushNotification/custom-cmd→notify-send→logger) + logs an outage-death evidence file; NO `claude -p`, no permission wall. The evidence log is the gate to re-open the deferred heavy path (curated allowlist / dedicated-OS-user, id:2d01). `tests/test_relay_watchdog.sh`. Covered by tests on 2026-06-29. <!-- id:98f0 -->
- [x] **[HARD — hands] Auto-reconcile-on-restart** — SHIPPED 2026-06-29: `relay-reconcile.sh --auto` (+ `--all --auto`) conservative classifier — ledger-only/clean/non-diverged orphan → auto-integrate via the shared --no-ff+ckpt-tag+--ff-only recipe; ANY code diff / dirty tree / empty-or-unrelated diff → parked + surfaced as an open REVIEW_ME.md box (never a weaker bar than a human review). Loop-invoked at startup on a STALE heartbeat (relay-loop.js), then `heartbeat.sh reap`. `tests/test_relay_reconcile_auto.sh`. Covered by tests on 2026-06-29. <!-- id:7809 -->
- [x] **[HARD — hands] Heartbeat liveness on the relay claim/lease (foundation for id:7809 + id:98f0)** — SHIPPED 2026-06-29: `relay/scripts/heartbeat.sh` (run-level liveness marker — beat/stop/status/dead-runs/live-runs/reap; PURE ts+TTL staleness so a left-behind working worktree never masks a dead loop, the key difference from claim.sh liveness) + `tests/test_relay_heartbeat.sh`; beaten each round by the loop prelude, stopped on clean exit (relay-loop.js). Covered by tests on 2026-06-29. <!-- id:e149 -->
- [x] **[HARD — hands] File `CronCreate durable:true` no-op upstream** — FILED 2026-06-29 → https://github.com/anthropics/claude-code/issues/72238 (reproduced on Claude Code 2.1.195: returns `[session-only]`, writes no scheduled_tasks.json, description↔behavior mismatch). Draft `docs/upstream-reports/2026-06-29-croncreate-durable-noop.md`. Verified by user on 2026-06-29. <!-- id:0994 -->
- [x] **Re-lane policy for INTENSIVE items (`pool` vs `hands`)** — 5-condition criterion (automatable done-check / no destructive-without-confirm / no live secret-sudo-device / host-gate-satisfiable / no open judgment) added to `relay/references/hard-lanes.md`; reuse the orthogonal `[INTENSIVE]` axis, NO new lane. Re-laned `ai-codebench` id:244b hands→`[HARD — pool][INTENSIVE][host:zomni]`. done 2026-06-30 (/relay human meeting; `make test` green). See `docs/meeting-notes/2026-06-30-0826-relay-hands-disposition-redesign.md`. <!-- id:db39 -->
- [x] **Author-then-run split disposition for scriptable `[HARD — hands]` items** — split into `[HARD — pool]` author + `[HARD — hands]` run (DEP-linked); pool auto-builds, `/relay human` prints the run command (never auto-executes); documented practice (not a transform) in `relay/references/handoff.md` C2 + `references/human.md` §3. done 2026-06-30 (/relay human meeting). See same note. <!-- id:e175 -->
- [x] **Verbose "you run these" checklist in `/relay human`** — expand `hard_hands` + `manual` items verbatim with `<file>:<line>` editor-jump refs when long; meeting/pool tiers stay terse (`references/human.md` §4 + return summary). done 2026-06-30 (/relay human meeting). See same note. <!-- id:45c6 -->
- [x] **Auto-file inbox dead-letters on every `--all` run** — `scan-routed.sh --apply` reconcile sub-step folded into `SKILL.md` orchestrator invariant 1 (run's `--exclude` + always `truncocraft`; class-A only, class-B prose still surfaced). done 2026-06-30 (/relay human meeting; `test_scan_routed_apply.sh` green). See same note. <!-- id:1d3f -->
- [x] **relay front-door autonomous-pool singleton guard** — before launching the no-arg autonomous pool Workflow, acquire a `pool:autonomous` claim via the existing `claim.sh` (id:ebfb); if a claim is already live, refuse and name the holder (peek). Directed/scoped/`--afk` runs are EXEMPT — this guards only duplicate concurrent no-arg pools; legitimate multi-clauding (54 overlap events, 16% of messages) must be unaffected. Touches relay/SKILL.md front-door step + claim.sh (no new machinery, compose existing). Relates id:1968; does not block on it. Out of scope: blocking any directed/parallel run or building a new lockfile. Source: 2026-06-18 /insights report + meeting id:f1a7; see docs/meeting-notes/2026-06-18-1829-insights-findings-triage.md. — done 2026-06-29 (parallel C1 batch; tests green / landed) <!-- id:11c6 -->
- [x] **Close scoop window (ii): ban `git add -A` in relay integrator** — `relay-loop.js` integrator must use scoped `git add` on its own worktree-merge paths only, never `git add -A`. Contract: a concurrent uncommitted ledger edit (from `/meeting` or `/relay human`) is never captured in a pool checkpoint commit. Ships with or before id:c144 (the lease exemption). See `docs/meeting-notes/2026-06-17-0953-k3s-parallelity-coordination-design.md` D2. — done 2026-06-29 (parallel C1 batch; tests green / landed) <!-- id:debf -->
- [x] **Narrow `hard` lease scope + document the invariant** — update `relay/scripts/claim.sh` header and dispatch-safety reference docs: `hard` lease = code/worktree integration only; ledger-only writes = flock + `orphan-scan --cross-ledger`. Contract: doc change + `make test` green. See `docs/meeting-notes/2026-06-17-0953-k3s-parallelity-coordination-design.md` D2. — done 2026-06-29 (parallel C1 batch; tests green / landed) <!-- id:179e -->
- [x] **Structured executor size-out signal for sized-out `[ROUTINE]` items** — the executor/ROUTINE counterpart of id:2d20's auto-closure: a cheap executor that sizes out a `[ROUTINE]` item only emits soft `friction:`/`BLOCKED:` notes, which the integrator's durable handback follow-up (id:3801) never reads, so the item stays a re-dispatchable `[ROUTINE]`. Fix = make the executor EMIT the structured handback (`contract_met=false`+`handback_item`+`route=hard-split`) so id:3801 re-tags it to `[HARD — decision gate]`. The gate already works; gap is upstream in `executor-contract.md` + the relay-loop.js execute-verdict prompt. Handoff DONE → ROADMAP id:08c0 (+ RED `tests/test_executor_sizeout_signal.sh`), from inbox routed:9a50 (truncocraft). Relates id:2d20 (HARD counterpart), id:3801 (gate), id:8b1f (HARD size-out clean-worktree discipline), id:194e (failure ledger — `size-out` category). <!-- id:08c0 -->
- [x] **Flaky test: `tests/test_relay_claim_liveness.sh` (roadmap:7570) intermittently red under suite load** — surfaced by the id:401c Run 14 audit 2026-06-17: a full `tests/run-tests.sh` showed `65 passed, 1 failed` on this file, then two reruns were clean 66/66; it passes in isolation. Timing/mtime-sensitive (worktree-mtime heartbeat liveness, id:7570) — non-deterministic under load. NOT a feature defect; the test itself is racy. Fix: widen the mtime/TTL margins or stub the clock so the suite is deterministically green. Until then a lone red here is likely a flake — rerun before trusting. Unrelated to id:bbd2/id:10c0. **DONE 2026-06-29**: replaced `$((now-mt)) -ge 2` clock-comparison with before/after mtime snapshot (`mt_before` == `mt_after`); added explicit git-repo verification assertions at setup. Timing dependency structurally removed. 20× consecutive + full-suite green. <!-- id:16e9 -->
- [x] **Flaky test: `tests/test_git_lock_push_slash_branch.sh` intermittently red under suite load** — surfaced by the id:401c Run 15 audit 2026-06-19: a full `tests/run-tests.sh` showed `75 passed, 1 failed` on this file, then it passed in isolation AND on an immediate full-suite rerun (76/0). Same flake CLASS as id:16e9 (real-git fetch/push timing under parallel suite load), NOT new `/tmp` contention from the id:0d31 quota sampler — both `test_quota_sample.sh` and this test isolate their state via `mktemp -d` (+ `QUOTA_CACHE` override). NOT a feature defect. Fix: make the slash-branch test's git fetch/push deterministic (local-only remote / serialize) so the suite is green regardless of run order. Until then a lone red here is likely a flake — rerun before trusting. **DONE 2026-06-29**: root cause was `git-lock-push.sh`'s `ssh-add -l` guard — if no SSH key loaded in agent (CI, expired agent), the script exits 0 without pushing, causing the push assertion to fail. Fixed by injecting a fake `ssh-add` (always exits 0) into the test's PATH; the local file:// remote needs no SSH. Timing dependency structurally removed. 20× consecutive + full-suite (123/0) green. <!-- id:05e8 -->
- [x] **Gate `claim.sh` `is_live` worktree-anchor on the run heartbeat (dead-but-LIVE claim fix)** — meeting 2026-06-29 (`docs/meeting-notes/2026-06-29-1750-dead-but-live-claim-heartbeat-gate.md`, the id:9000 remainder after id:672b). `is_live`'s id:7570 `worktree_working` clause keeps a stale-mtime claim alive whenever its worktree has commits beyond main — but that never detects death, so a dead-but-committed run holds its claim forever. Fix (D1): gate the worktree clause on `heartbeat.sh status <runId>` (id:e149, the pure-staleness run-liveness oracle that exists for exactly this); dead/stale-heartbeat run → reclaimable. (D2) absent-heartbeat → ordinary mtime-TTL fallback (reuse heartbeat's TTL, no new knob). (D3) reclaim frees only the reservation — the orphan worktree is disposed by the existing reconcile (id:a4e9/7809), never deleted (reversible). Test: extend `tests/test_relay_claim_liveness.sh` with stubbed-ts cases (fresh-hb+worktree→live; stale-hb+worktree→reclaimable; absent-hb→mtime-TTL; mtime-fresh→live). Was gated on id:16e9 (now landed). See memory [[relay-claim-liveness-heartbeat-gate]]. <!-- id:33d3 -->
- [x] **/meeting claim-compatibility — advisory claim at SETUP + claim-aware non-conflicting session** — decided 2026-06-24 (meeting note above, D3). /meeting acquires an **advisory repo claim at SETUP** (not just the step-2a write-back); the relay pool already honors claims (`claim.sh peek`) → skips that repo for the meeting's duration; released at end (TTL backstop). Closes the one-sided-lease asymmetry ([[meeting-pool-claim-asymmetry-incident]]). On an EXISTING pool claim at setup, /meeting **informs** the user and **offers a non-conflicting session**: the decide phase needs no claim, so design proceeds; only the ledger **write-back** is **deferred + replayed** (id:2c42) — explicitly NOT worktree-per-meeting (rejected, af04: non-unionable checkboxes). References id:9000 (this is its cheapest half), id:2c42, id:0902/ebfb. — SHIPPED 2026-06-29 (relay handoff→execute→review); RECONCILED with shipped id:c144: ONE setup-time advisory claim + write-back via peek-and-warn under flock (id:148b), id:2c42 = flock-timeout fallback (the 'write-back deferred+replayed' wording above is superseded for the normal case) <!-- id:672b -->
- [x] **Exempt ledger-only writes from the `hard` relay lease** — `/meeting` SKILL.md step 2a + `/relay human` spec: ledger write-backs (`md-merge.py`, `TODO.md`/`ROADMAP.md`/`REVIEW_ME.md`) skip `claim.sh acquire`; change step 2a from DEFER-on-held-lease to peek-and-warn only. `hard` lease narrows to code/worktree integration. Contract: a `/meeting` ledger write-back completes while a multi-repo pool holds the repo `hard` lease, without DEFER. See `docs/meeting-notes/2026-06-17-0953-k3s-parallelity-coordination-design.md` D2. — done 2026-06-29 (parallel C1 batch; tests green / landed) <!-- id:c144 -->
- [x] **Close scoop window (i): atomic ledger write+commit in `md-merge.py`** — `meeting/md-merge.py` commits just the edited ledger file under the same flock (scoped `git add <ledger-file> && git commit`). Contract: after any ledger write-back there is no modified-but-uncommitted ledger file left in the main checkout. Ships with or before id:c144 (the lease exemption). See `docs/meeting-notes/2026-06-17-0953-k3s-parallelity-coordination-design.md` D2. — done 2026-06-29 (parallel C1 batch; tests green / landed) <!-- id:148b -->
- [x] **Add a verify-before-assert rule to global `~/.claude/CLAUDE.md`** — curated rule (owner ratifies wording before landing): under "Reporting work", state that conclusions about code, git history, or file state are only stated after running the command that proves them; never assert "fails to render", "already merged", "file dropped" without first running the check. Land in the same curated edit as id:6ac6 (no-clickbait rule). Out of scope: harness-level "care" prose. Source: 2026-06-18 /insights findings, 8× "Wrong Approach" incidents; see docs/meeting-notes/2026-06-18-1829-insights-findings-triage.md. — done 2026-06-29 (parallel C1 batch; tests green / landed) <!-- id:f082 -->
- [x] **Add a sibling-project resolution line to global `~/.claude/CLAUDE.md`** — curated one-liner near the "Machines" section: "Projects live under ~/src/; resolve bare project names against ~/src/ before asking." Owner ratifies. Out of scope: a programmatic resolver. Source: 2026-06-18 /insights findings; see docs/meeting-notes/2026-06-18-1829-insights-findings-triage.md. — done 2026-06-29 (parallel C1 batch; tests green / landed) <!-- id:35fe -->
- [x] **output-to-file discipline doc** — write an output-style doc or relay-context note (NOT a global always-loaded CLAUDE.md rule) capturing: when output would be long, write it to a file with the Write tool and reply with only a short confirmation + path. Trigger is the narrow 500-output-token capped-config failure mode; re-open to global CLAUDE.md if it recurs outside capped sessions. Source: 2026-06-18 /insights findings; see docs/meeting-notes/2026-06-18-1829-insights-findings-triage.md. — done 2026-06-29 (parallel C1 batch; tests green / landed) <!-- id:ef77 -->
- [x] **Add an output-style rule to global `~/.claude/CLAUDE.md`: no clickbait/teaser prose in working sessions** — owner `/feedback` 2026-06-18: a code session must not emit content-free suspense sentences like *"Both engines agree on the splittable/badge mechanics — but with a critical asymmetry."* — a teaser that asserts something notable exists (a "critical asymmetry") WITHOUT naming it or its evidence on the same line. Diagnosed as Opus over-trained on clickbait register. **Proposed rule** (owner to ratify exact wording — CLAUDE.md is curated): under a reporting/communication heading, "State the finding, never tease it. Don't write a standalone sentence claiming significance ('a critical X', 'the surprising part') without immediately, in the same breath, giving the concrete thing and why. Lead with the fact; every significance-claim carries its evidence inline. No hype sentences before a table/finding." Already captured durably as the toesnail-session feedback memory `no-clickbait-teaser-prose`; this TODO is to promote it into the always-loaded global CLAUDE.md so it isn't recall-dependent. Decide placement (likely under the existing "Reporting work" section) + whether it belongs in global CLAUDE.md vs an output-style doc. — done 2026-06-29 (parallel C1 batch; tests green / landed) <!-- id:6ac6 -->
- [x] **[HARD — meeting] DONE 2026-06-30 (meeting `docs/meeting-notes/2026-06-30-1042-relay-side-zkm-b-topology.md`) — relay-side outcome of zkm B-topology (zkm f98d).** Decomposed the inbox `routed:2649` omnibus → 1 built / 1 routed / 2 verified-noop. **(b) BUILT:** `orphan-scan.sh` forward+reverse token union now plugin-aware (adds `plugins/*/{TODO.md,TODO.archive.md,ROADMAP.md}` when `$ROOT/plugins/` exists; cross-ledger/promotion unchanged) — zkm transitional false-orphans (ac55/b043/c89a/b99e/f40c/8d67) verified no longer flagged. **(c) ROUTED** to project_manager (`proj`/`scan.py` plugin-walk → `routed:c38a`). **(a)** handoff C2 "central ledger by design" = VERIFIED NO-OP (no such assumption exists in `relay/`; `handoff.md:35` is already repo-local; only monorepo machinery is the unrelated host-gate id:43b9). **(d)** 2026-06-26 GH-issues policy = already done zkm-local (`zkm/CLAUDE.md:189` already says "plugin's own TODO"). 69f4 demoted (see id:69f4). <!-- id:d097 -->
- [x] **Loud-fail on a corrupt relay.toml registry** — a duplicate key (concurrent-writer race) made strict `tomllib` throw, so `own_repos()` silently emitted nothing → empty own-repo map → scan-routed mis-reported every routed inbox target as UNRESOLVED (8 phantom 2026-06-30) while relay-doctor looked falsely clean. Fix: registry-parse gate in `scan-routed.sh` (loud stderr + exit 2 on a TOML error) + a first-class `registry_parse_check` in `relay-doctor.sh`. Corruption repaired in `~/.config/fables-turn/relay.toml`. done 2026-06-30 — commit a78ff5b, make test 123/0. <!-- id:2945 -->
- [x] **`ckpt-tag.sh` hard-fails (exit 1) when a repo can't track `.gitattributes`** — observed 2026-06-30 reviewing `kienzler-homepage`, whose `.gitignore` had a `.*` dotfile catch-all that swallowed `.gitattributes`, so ckpt-tag's `git add -- RELAY_LOG.md .gitattributes` aborted the WHOLE checkpoint (no commit, no tag) and left `RELAY_LOG.md` staged. The `RELAY_LOG.md merge=union` attribute is a nicety (matters only for parallel relay merges), not essential — a repo that can't add it should still get its checkpoint. **Fix:** ckpt-tag.sh should degrade gracefully — try to stage `.gitattributes`, but on failure WARN + proceed with `RELAY_LOG.md` + tag only (don't abort). Workaround used 2026-06-30: manual `git commit RELAY_LOG.md` + `git tag -a`, and fixed kienzler's `.gitignore` (negated `!.gitattributes`/`!.dns/`). <!-- id:a7a3 -->
- [x] **The Agent tool's `isolation: worktree` is CWD-repo-relative — wrong for cross-repo relay children** — fixed 2026-06-30. A relay child dispatched for repo X with `isolation: worktree` gets an isolated worktree of the SESSION's current repo, not X, so it silently edits X's MAIN checkout unisolated (observed: a jobAI executor launched with `isolation: worktree` from a dotclaude-skills cwd committed straight to jobAI's real `main` — harmless that time, lease held + verified + pushed, but it defeats isolation). The relay's own pattern is already correct (`relay-loop.js` has the CHILD create an explicit `~/.cache/relay/worktrees/<repo>/<runId>-<verdict>` worktree naming the target); the gap was a manual-dispatch footgun. **Fix shipped:** `relay/SKILL.md` orchestrator-invariant step 4 now explicitly forbids `isolation: worktree` for relay children and points at the explicit-relay-worktree / main-checkout-under-lease patterns. (Spec-driven skill → the contract doc IS the mechanism; no code change needed since relay-loop.js never used the harness param.) <!-- id:c6c8 -->
  - [x] **C3 — Backtest harness** [PRE-flip gate] (DONE 2026-06-30 → `relay/scripts/backtest-verdict.py`, gate run 0 crashes/49) — replay `classify-verdict.sh` over git-reconstructed past ledger states vs logged `relay-events.jsonl` dispatch verdicts; triage EVERY disagreement before flip (script-bug→fix; shard-was-right→new RED fixture). Permanent `make test` regression artifact. Partial-fidelity (only dispatched verdicts logged; ephemeral worktree/claim state not git-recoverable; compare LABELS only). (meeting 2026-06-30-1523) **DOGFOOD PROTOTYPE RUN 2026-06-30** (`classify-verdict.sh` over 49 LIVE own-repo states, compared to last-dispatch verdict; script in scratchpad `backtest_dogfood.py`): **0 crashes/49 (robust).** It also doubled as a prototype of the missing DP1 assembly wrapper (gather → derive `hasRoutine`/`roadmap_actionable_open` from ROADMAP → fold `unpromoted-scan` {promote,surface}). **KEY FINDING:** the meeting-decided `surface>0 → handoff` rule makes **26/49 repos classify `handoff`**, most carrying only untagged design backlog (`surface`, `promote=0`) and `drained(non-actionable)`. This is the case-b/h fix working (backlog no longer hidden as idle/human), BUT empirically confirms the **case-g hazard at scale**: a `surface`-only repo re-emits `handoff` EVERY round unless the forced lane-triage (id:47f1) actually drains surface→tagged. **Refinement for the wrapper build:** distinguish `promote` (directly-promotable, lane-tagged, hiding → strong handoff) from `surface`-only (untagged design epics → needs a CONVERGING triage, not a perpetual re-handoff); else the loud detection re-fires forever (the exact id:4d8e anti-pattern). Corpus spot-checks held: it-infra→review (no phantom hard, case a), ai-codebench→handoff (no false hard, case c), zelegator→review / truncocraft→execute (sane). Two prerequisites this confirmed are MISSING and gate a real backtest: `gather-repo-state.sh` does not yet emit `hasRoutine`/`roadmap_actionable_open`, and there is no assembly wrapper — both are part of the C5 wiring (id:a0b6) / a gather extension. <!-- id:5f93 -->
- [x] **`gather-repo-state.sh` has the same env-var-blob `execve` overflow latent bug** [ROUTINE] (found 2026-06-30 dogfooding id:3f0f) — its `emit()` passes the ROADMAP content (and other fields) to `python3` via ENV VARS (positional→env→python, ~line 48); a single env string over `MAX_ARG_STRLEN` (128KB) breaks `execve` with "Argument list too long". Today it works on real repos because the `roadmap` field is a ~94KB SUBSET (< limit), but a repo with >128KB of open ROADMAP items would crash it (and thus the `classify-repo.sh` wrapper that calls it). Same class as the id:3f0f wrapper bug (fixed via temp files, `tests/test_classify_repo_large.sh`). **Fix:** pass the large blobs to python via stdin or a temp file, not env/argv. **Spec:** a regression test with a >128KB ROADMAP (the original `test_classify_repo_large.sh` fixture that hit gather first) must run clean. Low urgency (no real repo triggers it yet), but it's a latent crash in a core script. <!-- id:07be -->
  - [x] **C2 — RED harness + fixtures (a)–(h-as-a/b/c/d/h)** [DoD] (DONE 2026-06-30) — two-tier (pure-unit JSON→verdict + integration mktemp-repo gather→classify e2e), fixtures seeded from the REAL 2026-06-30 ledger states (truncocraft/zelegator/it-infra/ai-codebench); lint cases c/d assert exit-code+stderr severity not a verdict; each `# roadmap:` → EXPECTED-RED. SHIPPED across `tests/test_classify_verdict.sh` (a/b/h), `tests/test_roadmap_lint_tagprose.sh` (c/d), `tests/test_classify_repo.sh` + `test_classify_repo_large.sh` (integration), `test_classify_verdict_parity.sh` (e424). All green in the 134-test suite. (meeting 2026-06-30-1523) <!-- id:ccd9 -->
  - [x] **(g) Loop-side forced-resolution orchestration** [follow-on] — wire the classifier's `handoff + "N items need triage"` obligation to the inline-triage/decision-queue (C7) so "N surface" can never be swallowed as "handoff complete". (meeting 2026-06-30-1523) **DONE 2026-06-30 (strong turn, dogfood-confirmed):** the live `/relay --afk` shadow surfaced the actual no-op loop — 30 repos classified `handoff`, mostly surface-only backlog the resolution silently dropped, so `unpromoted-scan` re-counted them every round. Fix shipped in `ROADMAP.md` id:47f1: `decision-queue.sh add --source-id`, `unpromoted-scan.sh` excludes tokens with an OPEN queue record (loop-breaker), `handoff.md` documents the file-each-surface step. The classifier was CORRECT (case-b: surface must not be hidden); the bug was the unwired resolution. `tests/test_unpromoted_decision_queue_exclusion.sh` green; suite 136. The confidence-gated inline lane-triage sub-agent remains de31/b444 scope. <!-- id:47f1 -->
- [x] [INBOUND routed:6645 from it-infra relay HARD child relay-20260630-131714-19334] gather-human-backlog.sh emit_hard_lanes() buckets a [HARD — hands] item as hard_pool when its body prose contains the literal backtick `[HARD — pool]` (e.g. a re-lane-criterion sentence). Anchor the lane regex to the tag immediately after the title, or check hands/meeting before pool, or strip prose past the title. Caused it-infra open_hard_pool=2 false-positive → wasted Opus HARD dispatch (ids 9321/c5e9 are genuinely [HARD — hands]). <!-- id:1bbd --> done 2026-06-30 (audit Run 69 cross-ledger fix: shipped+merged in ROADMAP, suite 135 green)
  - [x] **(f896) Log dispatch-time gather-input on the event → exact-replay backtest parity** [follow-on, PRE-flip fidelity] — `relay-events.jsonl` dispatch records carry only `{repo,mode,tier,round}` (the verdict, NOT the input that produced it), so `backtest-verdict.py` compares current-state classification to a STALE last-dispatch verdict → structural divergence (agree=5/50 even on a clean tree). Fix: emit the shard's gather-repo-state JSON (or just its `discover-sig.sh` hash + the shard's own verdict) as event fields at dispatch; then the backtest replays `classify-verdict.sh` on the EXACT input the shard saw → full-fidelity parity for every forward dispatch, state-drift eliminated by construction. Cheap (the shard already has the JSON; one event field, no engine-logic change). **This is the keystone that makes the id:9d2b reframe sound — the "post-run backtest ≡ inline shadow" premise only holds once the input is captured.** Substrate: reuse `discover-sig.sh` (id:c3a6) for the hash. <!-- id:f896 -->
  - [x] **(e8ea) Auto-bucket backtest divergences: state-drift vs true-disagreement** [follow-on] — with id:f896's captured input, `backtest-verdict.py` can label each diverged row mechanically: same-input-different-verdict → RED (real classifier disagreement, the only thing the gate cares about); input-changed-since-dispatch → EXPECTED (drift, auto-explained). Shrinks the human triage from ~43 rows/run to the handful of RED rows. Until f896 lands, a cheaper proxy: flag "new commits since the logged dispatch" as auto-EXPECTED. Turns the id:9d2b gate from manual eyeballing into a mechanical pass/fail. <!-- id:e8ea -->
  - [x] **(1324) Persist + auto-run the shadow accumulation log** [follow-on] — id:9d2b's DoD is "accumulate ≥N explainable comparisons" but `backtest-verdict.py` is stdout-only ("no output files recorded") — there is nowhere for the gate to accrue. Fix: the front door runs `backtest-verdict.py --json` automatically at END of every `/relay` run (post-drain) and appends the summary to a dated `~/.config/relay/shadow-log.jsonl`; the gate reads the last N entries. Makes 9d2b accrue mechanically instead of by remembering to run the backtest by hand. Pairs with e8ea (the persisted summary carries the RED/EXPECTED bucket counts). <!-- id:1324 -->
  - [x] **DP7 — Historical backtest** — `relay/scripts/backtest-historical.py` [--since YYYY-MM-DD] [--limit N] [--json] [--append-log]: iterates `relay-events.jsonl` dispatch events, resolves each to a historical commit via `git rev-list --before` (read-only; NEVER checkout/switch/worktree-add), reconstructs ledger state from `git show C:<file>`, pipes to `classify-verdict.sh` → V', compares to event `mode` V. Partial-fidelity: dirty/diverged=false assumed (self-consistent — dispatch events were demonstrably not blocked); uv.lock + recurring-audit exemptions omitted; historical diverges = CANDIDATE disagreements ("diverge"), not confirmed RED. `tests/test_backtest_historical.sh` (`# roadmap:0e57`) green. FIDELITY PASS 2026-06-30: per-row `classifier_reason`+`classifier_evidence`+`reconstructed` input fields; tag-time filter for ckpt tags; id:9973 recurring-audit gate applied; 3 reconstruction-gap categories (`legacy-lane-vocab` for pre-id:78ff `[HARD — strong model]` vocab, `all-fields-empty` for commit-timestamp-boundary gap, `9973` when gate fires); output reframed to lead with `candidate-classifier-worse` count (target 0); `tests/test_backtest_fidelity.sh` (`# roadmap:0e57`) green. **GUARDRAIL (durable): agreement-with-shard is NOT a quality target and must NEVER become one. When the classifier diverges because the shard was wrong, that divergence is the CORRECT outcome. The 3-bucket output must preserve `classifier-better` rows; do NOT modify `classify-verdict.sh` or add reconstruction hacks to raise agreement.** Real-corpus run (767 events): 0 crashes, 93 candidate-classifier-worse (for human review), 205 reconstruction-gap (legacy-lane-vocab:137 = pre-id:78ff HARD tags; all-fields-empty:73 = commit-timestamp-boundary gap where dispatch-time ckpt missed; 9973:6), 66 classifier-better, 403 matches-shard. <!-- id:0e57 -->
- [x] case-b verdict → `human` + mechanical filing — shipped 2026-06-30 (relay-ckpt-20260630-2341); `classify-verdict.sh` splits handoff/human/idle, new `file-surface-decisions.sh` mechanical filer wired at the human verdict in `relay-loop.js`, `handoff.md` case-g relocated; tests green <!-- id:5eb3 -->
- [x] INTENSIVE verdict-layer fail-safe: flag + invariant + fail-closed assertion (SAFETY) — shipped 2026-06-30 (relay-ckpt-20260630-2341); `classify-verdict.sh` emits clamped `intensive` flag (invariant intensive!=""⇒verdict∈{execute,hard}), fail-closed pre-dispatch guard in `relay-loop.js` runUnit; tests green <!-- id:5ac6 -->
- [x] roadmap-lint case (d): realign to operative-only-on-dispatchable-lanes — shipped 2026-06-30 (relay-ckpt-20260630-2324); case-d accepts INTENSIVE on any recognised lane (lane-less still rejected via grammar), `:80` comment + `hard-lanes.md` doctrine fixed, 297b case-d reconciled; resolves it-infra id:9321 false-positive <!-- id:9062 -->

## 2026-07
- [x] **Relay pool quota gate false-stops on round 1 (82e3)** — FIXED + VERIFIED 2026-07-01 (commits f6a2994, aa1828c; suite 146 green; gate reproduction wf_59dd6986 returned exitCode=0, ran=true, denied=false). Three causes, all resolved: (A) `quota-stop.sh` self-refresh read the OAuth token + curled the usage endpoint on a stale cache — inside the auto-mode pool that credential-egress shape is DENIED, the quota-check agent dies, and `relay-loop.js:1381` labels agent-death `quota-cache-unreadable`. Removed ALL credential logic; the foreground statusline is the sole token owner; a stale cache resolves via the stale-but-safe margin check → burn-rate extrapolation (no network/credential). (B) `--once`/`--after` round cap (`relay-loop.js:1871`) unconditionally set `stopReason='user-stop'`, masking a real quota stop from the same round → now `if (!stopReason)`. (C) the usage API stopped exposing per-model weekly sub-limits on 2026-06-30 (`seven_day_sonnet` et al. null; only `weekly_all`/`seven_day` remains — evidence: quota sampler series ~17h/34 samples), so `check_key seven_day_sonnet` mislabeled the absence as an unreadable cache → sonnet tier now falls back to `seven_day`+`five_hour` (fail-safe only if `seven_day` itself is absent). CORRECTED PREMISE (verified prior session): `/tmp` is SHARED, not a private sandbox — the cache is visible+fresh in the pool; the cause was credential denial + the null sub-bucket, never namespace invisibility. FOLLOW-ON: the classifier-flip gate needs a full UNATTENDED autonomous pool run to accrue (bare `/relay`) — the round-1 gate is now verified passing, so that run is unblocked. (from zkm-whatsapp via relay, routed:82e3) <!-- id:0175 -->
  - [x] **C5 — Flip authority** (DONE 2026-07-01 — flip shipped: shardPrompt deleted, mechanical runner via discover-repo.sh; children 5987/3d61/64b4 + a0b6 all done; suite 151/0; live smoke 6 repos 0 crashes) — classify-verdict PRIMARY + dispatches; shard → AMBIGUOUS-only; delete the general "classify this repo" shard prompt; NO post-flip comparator (validation is entirely pre-flip). (meeting 2026-06-30-1523) **SCOPING FINDING 2026-06-30 (read `relay-loop.js:818-891` shardPrompt closely):** the flip is BIGGER than "swap the verdict source" — the discovery shard does THREE things, only one of which classify-verdict covers: (1) the core VERDICT (review/execute/hard/handoff/idle) ← classify-verdict does this; (2) VERDICT-AFFECTING GUARDS classify-verdict does NOT yet handle — dirty-tree → surfaced (not dispatched), diverged-from-origin → surfaced, `dirty_lock_only`/`lock_only_unaudited` exemptions, behind-origin → ff-merge-then-reclassify; (3) RECONCILIATION ACTIONS (ff-merge, stale-worktree reap, orphan-park, in-place uv.lock commit) + the ~12 per-unit FIELDS (lastCkpt/income/openHard/standin/intensive/work_sig/strongRecheckPending) the loop consumes. So a SAFE flip needs, in order: **(a)** extend `classify-verdict` (+ `classify-repo` to pass the fields through: `dirty`,`dirty_lock_only`,`lock_only_unaudited`,`upstream_ahead_behind`,`has_upstream`,`worktrees`) to reach VERDICT PARITY incl. a disposition for dirty/diverged repos — note this needs a verdict-VOCABULARY decision (a `blocked`/`surfaced` value vs reusing `human`/AMBIGUOUS; touches the DP1 enum, so it's a small contract call, not a blind edit); **(b)** then the relay-loop.js swap — the shard runs `classify-repo.sh` for the verdict, keeps the side-effecting reconciliation guards + emits the per-unit fields from gather, LLM only on AMBIGUOUS. The reconciliation ACTIONS can NEVER move into the side-effect-free classifier — they stay shard-side. **This is careful Workflow-engine work the meeting explicitly gated (the engine crashed the pool 3×, template-literal-lint hazard); do it as a focused supervised step, NOT a tail-of-session rush. Next concrete unit = the verdict-parity extension (a), which is RED-testable with fixtures and touches NO engine code.** **STEP (a) COMPLETE 2026-07-01 (verified, commit pending):** verdict parity is CODE-COMPLETE — every verdict-affecting shard guard is in classify-verdict or subsumed: dirty→`blocked` + diverged→`blocked` + `dirty_lock_only` exemption + behind-only-not-blocked (all id:e424, tested in test_classify_verdict_parity.sh); `lock_only_unaudited`→not-review is SUBSUMED by `substantive_unaudited` (gather sets it False for a pure relock — verified end-to-end: lock-only unaudited ⟹ substantive_unaudited=False ⟹ classify-verdict `idle`, NOT review), now locked in by new assertions in test_gather_repo_state.sh 9a/9b; `worktrees` is a reconciliation input (step b, stays shard-side), NOT verdict parity. classify-repo already passes the full gather JSON through. So NOTHING remains for step (a). **The flip's ONLY remaining work is step (b): the relay-loop.js engine swap** (shard runs classify-repo for the verdict, keeps side-effecting reconciliation guards shard-side, LLM on AMBIGUOUS only) — explicitly GATED as careful supervised engine work (the engine crashed the pool 3×), NOT a tail-of-session edit. With C3 + C4 satisfied (id:9d2b) and step (a) done, step (b) is the sole gate to the flip. **STEP (b) DESIGNED 2026-07-01 (meeting `docs/meeting-notes/2026-07-01-1904-a0b6-step-b-engine-swap.md`):** the Workflow-sandbox constraint (relay-loop.js runs no bash — every git/script goes through an `agent()`) reframes "delete the shard" as "replace the JUDGMENT shard with a MECHANICAL RUNNER agent" that runs scripts + relays JSON. Decomposed into ROADMAP: **(1) id:5987 [ROUTINE]** `reconcile-repo.sh` — the bounded side-effecting git ops (ff-merge/reap/orphan-park/uv.lock-commit) split into a hermetically-tested script (executor-actionable, RED-testable, NO engine code); **(2) a0b6 remainder [HARD — pool]** — the confined `relay-loop.js:820-900` swap (runner = `reconcile-repo.sh`→`classify-repo.sh` per repo, LLM on AMBIGUOUS only), HARD swap / old shardPrompt DELETED / merge+backstop code untouched, **gated on id:5987 green**; **(3) id:b50e [HARD — decision gate]** — delete the 000d/9973/ad74 backstops once the AMBIGUOUS path is proven constrained (365b stays). All four backstops KEPT this session (none fully subsumed while the LLM/AMBIGUOUS path exists). <!-- id:a0b6 -->
- [x] **`unpromoted-scan.sh` promote-regex prose false-match — same bug class as id:4da4, different script** (adopted from inbox routed:85b8, 2026-07-01). DONE 2026-07-01: two mechanisms in `unpromoted-scan.sh` — (1) the canonical `- [ ] Relay: …` status-summary line is mechanically exempted (its `[ROUTINE]`/`[HARD — pool]` tokens are always closed-item PROSE; removes the need for a hand-added lint-ok marker, cf. meeting-rpg id:070c); (2) the promote-vs-surface disposition now uses id:4da4 PRIMARY-LANE anchoring (`primary_lane()` — first recognized lane-tag wins) instead of a substring `grep`, so a `[HARD — meeting]` item that merely mentions `[ROUTINE]` in its prose surfaces (not promote). RED fixtures (h)/(i) added to `tests/test_unpromoted_scan.sh`; suite 151/0; verified the real zkm-eml id:e662 + zkm-claude-ai id:815c no longer mis-fire. The promote-regex matches a literal `[ROUTINE]`/`[HARD — pool]` bracket appearing in PROSE on the canonical `- [ ] Relay: …` summary line → labels it `promote` → wasteful `handoff` dispatch (hit zkm-claude-ai id:815c 2026-07-01; zkm-eml id:e662 still affected). Fix: the scan should SKIP the summary line, OR strip prose/backtick-quoted brackets, OR reuse the **primary-lane** anchoring that fixed the classifier (id:4da4: the item's lane is the FIRST lane-tag on the line, not a substring match). Belongs with the id:4da4 precision family; a RED fixture: a `- [ ] Relay: N open [ROUTINE] items` summary line must NOT be labelled `promote`. <!-- id:ed2e -->
- [x] [ROUTINE] relay-doctor check 9 — main-checkout residue detector (I1/I7; reuse clean-tree-gate.sh; report-only) — spec in ROADMAP.md, see `docs/meeting-notes/2026-07-01-2142-relay-state-machine-invalid-state-detector.md` <!-- id:8018 --> — DONE 2026-07-01 (opus)
- [x] [ROUTINE] relay-doctor check 10 — verdict-invariant replay detector (I2/I4; self-replay classify-repo.sh --emit unit; honest coverage note) — spec in ROADMAP.md, see `docs/meeting-notes/2026-07-01-2142-relay-state-machine-invalid-state-detector.md` <!-- id:188c --> — DONE 2026-07-01 (opus)
- [x] [ROUTINE] relay-doctor check 11 — last_ckpt tag existence detector (I8; rev-parse --verify; + list I5/I9 gated in coverage-gap block) — spec in ROADMAP.md, see `docs/meeting-notes/2026-07-01-2142-relay-state-machine-invalid-state-detector.md` <!-- id:333c --> — DONE 2026-07-01 (opus)
- [x] [HARD — pool] atomic main-write fix for id:3801 handback follow-up (seed invalid-state i; md-merge --commit / commit-ledger; death-simulation test; reconcile out of scope) — spec in ROADMAP.md, see `docs/meeting-notes/2026-07-01-2142-relay-state-machine-invalid-state-detector.md` <!-- id:e5e9 --> — DONE 2026-07-01 (opus)
- [x] **Investigate the 2026-07-01 flip discovery-precision regressions + model the relay as a STATE MACHINE ("what touches what when" / invalid-state detection)** [HARD — meeting] (user 2026-07-01, from the a0b6-flip `/relay --once` smoke). **Two precision bugs the smoke surfaced** — both are the mechanical classifier (now authoritative post-flip) being LESS precise than the old LLM shard on real ledger shapes; caught by the executor's own size-out → clean handbacks (no damage), but wasteful (executor sessions spawned for nothing): **(1) `classify-verdict.sh` gates `execute` on `has_routine`** (ANY `[ROUTINE]` substring) instead of an actionable-routine signal (open, tag-anchored `[ROUTINE]`, `@manual`/human-gated EXCLUDED). `classify-repo.sh` computes `roadmap_actionable_open` but it MIXES routine+pool and isn't used for the execute gate → `yinyang-puzzle` (all `[ROUTINE]` @manual) mis-fired `execute`→handback. Need a distinct `actionable_routine_open` and gate `execute` on it — restores the id:e107 executor-actionable precision the flip dropped. **(2) `[ROUTINE]` detected by substring `"[ROUTINE]" in ln`, not tag-anchored** to the item's own bracket → `leAIrn2learn` matched `[ROUTINE]` inside id:c3f5's INLINE-HISTORY prose (a `[HARD — pool]` item) → phantom `execute` (same class as the id:1bbd HARD-lane prose-match fix; apply the backtick/prose-strip anchoring to the ROUTINE/execute path). **Fix both as executor-actionable RED-spec items** (fixtures: @manual-only repo → NOT execute; prose-`[ROUTINE]` → NOT execute). **BROADER (the reason to link — user-directed):** build a STATE-MACHINE / state-diagram model of the relay to fully grasp **what touches what when** — the artifact × actor × transition matrix (ROADMAP / TODO / REVIEW_ME / relay.toml / worktrees / leases / decision-queue / **main-checkout**, written/read by prelude / discover-run / executor / reviewer / integrator / id:3801 gate-detection / `/meeting`) — and **define validity invariants so 'invalid states' become detectable**. Two invalid states already observed: (i) an **uncommitted main-checkout edit** (loderite id:3801 gate-detection residue stranded dirty — the deeper fix is to make id:3801's main write COMMIT ATOMICALLY, id:148b flock+commit pattern, so it can't strand); (ii) an **`execute` verdict on a repo with no executor-actionable work** (the two bugs above). A formal state model turns both classes into a deterministic invalid-state DETECTOR (a relay-doctor id:9bec extension) instead of ad-hoc per-round discovery. Relate: id:4d8e (the flip umbrella), a0b6 (shipped), id:3801, id:e107, id:1bbd, id:148b, relay-doctor id:9bec. **PART-1 SHIPPED 2026-07-01 (relay-ckpt-20260701-2110):** the precision fixes landed — `classify-repo.sh` derives `actionable_routine_open` (open [ROUTINE], **primary-lane** = first lane-tag beats a fragile backtick-strip on 3.5k-char history lines, NOT @manual/human-gated, NOT dep-blocked "🚧/BLOCKED on"); `classify-verdict.sh` gates `execute` on it (−1 back-compat fallback). All 3 real /relay --once mis-fires verified fixed (yinyang-puzzle @manual→human, leAIrn2learn prose-[ROUTINE]-on-[HARD-pool]→hard, zkm-threema BLOCKED-on-dep→human); suite 151/0; locked at unit + integration tiers. **REMAINING (this item stays open):** the STATE-MACHINE model of "what touches what when" + the invalid-state DETECTOR (loderite id:3801 uncommitted-main residue + execute-on-no-work are the two seed invalid states) — the durable design that turns these ad-hoc precision patches into a systematic invariant check. **PART-2 DESIGNED 2026-07-01 (meeting `docs/meeting-notes/2026-07-01-2142-relay-state-machine-invalid-state-detector.md`):** the state-machine model (artifact×actor×transition matrix + invariants I1–I9, file:line-anchored) is delivered IN that note; the invalid-state detector is decomposed into `relay-doctor.sh` checks + the atomic-write fix, filed as RED-spec ROADMAP items **C1 id:8018 (check 9 main-residue), C2 id:188c (check 10 verdict-invariant replay), C3 id:333c (check 11 last_ckpt-tag), C4 id:e5e9 (handback-followup atomic-commit fix)**. C5 ratified report-only (surface loud, never auto-block; `--strict` opt-in). This item now tracks only those children shipping. **CLOSED 2026-07-01 (fable catch-up review):** all four children shipped in e407d42 and verified — C1 id:8018 / C2 id:188c / C3 id:333c (relay-doctor checks 9/10/11), C4 id:e5e9 (handback-followup atomic write+commit; death-sim test confirms a dying push leaves a clean tree); C5 was a ratified decision (report-only floor), no build. Suite 155/0; ROADMAP children all `[x]`, TODO mirrors archived. Follow-on state-diagram drift-guard stays open as id:a17a; the not-yet-wired invariants I5/I9 stay named in relay-doctor's coverage-gap block (gated on id:e149 / id:b444). done on 2026-07-01 <!-- id:4da4 -->
- [x] **Integrator mislabels a ZERO-COMMIT review branch as "duplicate dispatch" → handback, NO checkpoint → the audit window never closes and the pool re-dispatches strong reviews forever** [HARD — pool] — **done 2026-07-01** (same-night Fable bugfix child, merged + ckpt): relay-loop.js integrate distinguishes clean-empty-review (⇒ `ckpt-tag.sh -c <reviewed-tip>`, new flag) from genuine duplicate; destructive cleanup scoped to own-runId artifacts (id:6e02 hazard); worked_ids string-coercion fixed (`ids:["[","]"]` telemetry bug). Tests test_ckpt_tag_commit_target.sh + test_gather_stale_strong_watermark.sh; suite 158/0/0. (found by the 2026-07-01 fable catch-up review, from the run `relay-20260701-202806-14640` 20:36 handback). **Mechanism (verified against git timestamps + relay-events.jsonl):** the review child branched at the TRUE then-HEAD `33169ee` (20:20:36; next main commit `65ce4ea` landed 20:34:53 — the child did NOT see a stale main), made **zero commits** (a clean review window with no ledger delta is legitimate: commit-ledger.sh no-ops), so its branch tip stayed == its base == an ancestor of the (meanwhile advanced) main. The integrator's `git merge --no-ff` said "Already up to date" and it concluded "**Duplicate dispatch** — no integration action performed": no ckpt tag, no relay.toml update, handback. Consequences: (a) the misleading label masks the real state ("clean review, nothing to integrate"); (b) the reviewed window is never checkpointed, so the NEXT discovery re-classifies the same commits as unaudited and re-spends a strong review child — an unattended pool can loop this indefinitely; (c) tonight it also compounded the relay.toml staleness (id:0a3b). **Fix (the design nuance that makes this pool-lane, not ROUTINE):** the integrator must distinguish `tip == base ∧ zero commits` (clean empty review → **checkpoint at the reviewed tip**, i.e. tag the child's base/tip commit — NOT current main HEAD, which may contain newer commits the review never saw and must not be marked audited) from a genuine duplicate (branch already merged by an earlier integrator in the SAME run). Surface in relay-loop.js integrate() prompt (:1424 area) + a fixture test (zero-commit branch + advanced main → ckpt at branch tip, no handback). Relates: invariant I8/id:333c (ckpt bookkeeping), id:9d2b backtest (re-review waste), the 2026-07-01 events-log evidence. <!-- id:8e3e -->
- [x] **Supervised/manual checkpoints never update relay.toml (`last_ckpt`/`last_strong_ckpt` written ONLY by the pool integrator) → registry lags the tag truth** [ROUTINE] (found by the 2026-07-01 fable catch-up review; observed live: `[repos.dotclaude-skills]` still reads `last_ckpt = relay-ckpt-20260701-1635` while tags 1948/2019/2110 exist — all three were minted by interactive Opus sessions via `ckpt-tag.sh`, which tags + writes RELAY_LOG but never touches relay.toml; relay-loop.js:1426 is the only writer, via `relay-state-write.sh toml-set`). **Impact (verified, honestly bounded):** discovery verdicts are NOT mis-driven — `gather-repo-state.sh:156` derives `latest_ckpt` from `git tag -l` (tags are the source of truth). What drifts: the id:e030 Fable-recheck queue (`last_strong_ckpt`/`fable_rechecked` miss strong checkpoints made outside the pool) and relay-doctor check 11 semantics (it verifies the RECORDED tag exists, silently passing a stale-but-existing value). **Fix:** `ckpt-tag.sh` updates `[repos.<name>]` `last_ckpt` (and `last_strong_ckpt`+`strong_model` when the annotation records a strong model) through the flock'd `relay-state-write.sh toml-set` single-writer, iff the repo has a `[repos.<name>]` block (silently skip unmanaged repos; honor the existing `FABLES_CONFIG` env override for hermetic tests). Red spec: `tests/test_ckpt_tag_toml.sh`. **done 2026-07-01** (Fable bugfix child): flock'd toml-set sync at the ckpt-tag.sh choke-point, spec GREEN; PLUS the upstream consumer hardened — gather-repo-state.sh audit window anchors on the newest strong-labeled tag when newer than the toml watermark (test_gather_stale_strong_watermark.sh), killing the duplicate-strong-dispatch spin at both ends. <!-- id:0a3b -->
- [x] **`git-lock-push.sh` autostash-rebase guard (id:aa93) false-positives on `~/.claude` every push — perpetual runtime churn reads as "concurrent edit"** [ROUTINE] done 2026-07-02 (executor, ROADMAP twin id:dff8): shipped (b)+(c) — porcelain classifies untracked-only churn (proceed with the autostash-rebase) vs any tracked entry (still refuse); refusal wording drops the "(a concurrent edit?)" guess, states facts instead. Option (a) tracked-runtime-churn gitignore stays a separate claude-diary decision, out of scope here. (recurring, observed multiple times incl. 2026-06-30) — pushing to `~/.claude` via `git-lock-push.sh` repeatedly refuses with *"WARNING: working tree is dirty (a concurrent edit?); refusing to autostash-rebase to avoid data loss (id:aa93). Commit or move the changes, then run 'git push' manually."* **Root cause:** `~/.claude` is a worktree of the claude-diary repo whose tree is **perpetually dirty with harness RUNTIME churn** — tracked `.last-cleanup` + `history.jsonl` (rewritten constantly) and untracked `plans/`, `session-env/`, `sessions/`, `tasks/` (NOT gitignored). The dirtiness guard sees this and refuses, even though NONE of it is a concurrent session's WIP (user confirmed 2026-06-30: "there's no concurrent .claude session"). The error's own parenthetical "(a concurrent edit?)" is misleading — it's structural runtime state, not contention. **Effect:** every `~/.claude` push needs the manual plain-push fallback; the guard's "concurrent edit" framing also actively MISLEADS the operator into a wrong diagnosis (filed as a chidiAI anti-bluff datapoint, cf. cf01). **Fix options to weigh:** (a) **gitignore the runtime churn** in `~/.claude` (`.last-cleanup`, `history.jsonl`, `plans/`, `session-env/`, `sessions/`, `tasks/`) so the tree is clean except real edits — but check the `snapshot` commit job + the sessions-worktree archival (claude-diary TODO) don't rely on tracking them; (b) **scope the dirtiness check to the manifest** — in manifest mode `git-lock-push.sh` stages specific files, so the guard should only block when THOSE files (or the rebase) would conflict, ignoring unrelated untracked/runtime churn; (c) reword the warning to drop the false "concurrent edit?" causal guess (state "tree dirty; not autostashing" without inventing a cause — per the mechanize-first / no-confident-bluff rule). Prefer (b)+(c): the guard should reason about the actual commit, not the whole tree, and never assert an unverified cause. Relates id:aa93 (the guard), id:3558 (worktree-per-session), claude-diary sessions-worktree archival. <!-- id:dff8 -->
- [x] **`relay-loop.js` — Fable-produced strong HANDOFF checkpoint queues a bogus self-recheck + hardcoded "Opus stood in for Fable" reason** [ROUTINE] done 2026-07-02 (executor, ROADMAP twin id:6856; TODO twin ticked by review 2026-07-02 — cross-ledger drift fix): `isFableRecheck` dropped the `verdict === 'review'` conjunct; elevation reason no longer hardcodes "Opus stood in for Fable". (found 2026-07-02 by the Fable recheck of relay-ckpt-20260702-0119 — itself the bogus dispatch: empty diff window, `strong_model = "claude-fable-5"`, yet the verdict reason claimed "Opus stood in for Fable"): the id:e030 consume gate `isFableRecheck = SESSION_IS_FABLE && unit.verdict === 'review'` (relay-loop.js:1393) sends a Fable **handoff**/hard strong checkpoint down the else branch → `fable_rechecked = false` → a Fable-rechecks-Fable review next round; and the elevation reason (~:1046) hardcodes the Opus-standin phrasing regardless of the recorded `strong_model`. Fix: drop the `verdict === 'review'` conjunct (any real-Fable strong checkpoint is self-rechecked ⇒ dated watermark), derive the reason wording from `strong_model`. Write-side sibling of id:a42e (read-side standin substring) and id:25aa (tag anchor) — third mechanism in the wasted-re-dispatch family. Red spec `tests/test_fable_recheck_write_side.sh`. ROADMAP twin promoted same day. <!-- id:6856 -->
- [x] **STOP sentinel survived a fired user-stop** [ROUTINE] (observed 2026-07-01 ~23:27: pool run relay-20260701-225242-28925 returned `stopReason: "user-stop"` yet `~/.config/relay/STOP` (mtime 23:05) still existed afterward — the prelude is specced to CONSUME (rm) the sentinel on firing, else it silently stops the NEXT pool too). UPDATE 23:41: the file was GONE before any manual rm — consumption happened, but DELAYED past the workflow's return (observed present ~23:30 post-completion, absent 23:41). Downgraded to OBSERVE: the hazard window is "next pool launched during the lag gets false-stopped"; reproduce the timing in a fixture before building anything. Done 2026-07-02 (executor, ROADMAP id:482d): `relay/scripts/stop-sentinel.sh` collapses check/countdown/consume into one atomic call + timestamped consume log; prelude step 8 delegates to it. <!-- id:482d -->
- [x] **DQ answer-then-re-ask loop fix: `laned` disposition + resolved-record exclusion in `unpromoted-scan.sh`** [ROUTINE] done 2026-07-02 (Fable session, during the 352-entry apex DQ drain): a line whose PRIMARY tag is a human lane ([HARD — meeting]/[HARD — hands]/[HARD — decision gate]) now reports `laned` (verdict-neutral, never filed — filing a lane-triage request for an already-laned line re-asked answered questions); an UNTAGGED line whose lane question is RESOLVED in the decision-queue (parked/not-an-item) is excluded (old "resolved must resurface" rule oscillated the queue). classify-repo counts only promote/surface, so verdicts compose unchanged; specs updated in test_unpromoted_scan.sh (c)/(i) + test_unpromoted_decision_queue_exclusion.sh (3)-(5); suite 164/0/2-expected-red. Drain record + per-verdict rationales: claude-diary docs/dq-triage-2026-07-02/. <!-- id:c06f -->
- [x] **"Effectively-drained" ROADMAP (only `@manual`/human-lane items open) doesn't trigger the handoff-promote / unpromoted-scan route → large TODO backlog reported as "needs human / idle"** [HARD — pool] **DONE 2026-07-02 (handoff C5, ROADMAP twin id:9014):** loop half had already shipped via id:4da4/id:5eb3 (classify-repo.sh actionable counts + classify-verdict case-b split, test-locked); the remaining `/relay next` route-1/2a doc predicate re-keyed on executor-ACTIONABLE with classify-repo.sh cited as authority; drift-guard tests/test_next_actionable_predicate.sh green. (user 2026-06-30, "something's fishy… was there no work left in truncocraft?") — the autonomous pool classifier AND `/relay next` route 2a (id:2dea) only fire the TODO→ROADMAP promote / unpromoted-scan path when ROADMAP has **ZERO** open `- [ ]` items. A ROADMAP whose ONLY open item is `@manual`/human-lane is *not* zero-open, so the route never fires — yet that item can't be executed either, so the repo falls into the "needs a human, ROUTINE all @manual, not dispatched (id:e107)" bucket. **Observed:** truncocraft ROADMAP = 1 open item (id:ab02, `@manual`); TODO = **35 open items**. The `--afk` run reported truncocraft as `ROUTINE backlog is all @manual/human-only — needs a human` and moved on — describing only the single @manual ROADMAP box while **silently hiding the 35-item TODO backlog**. `unpromoted-scan.sh ~/src/truncocraft` shows all 35 as `surface` (untagged design backlog — correctly NOT auto-promotable, but several, e.g. id:556f box-select-start ghost, are concrete and would become `[ROUTINE]` under a handoff-C2 lane triage; the rest are `[HARD — meeting]` design epics). Same class as the 2026-06-25 truncocraft miss ([[unpromoted-scan-and-inbox-layering-2dea]]), one rung up: there a fully-`[x]` ROADMAP hid the backlog; here a lone `@manual` box does. **Fix:** the "drained" predicate for the promote/surface route must mean *no executor-ACTIONABLE open ROADMAP item* (treat `@manual`/human-lane-only as effectively-drained), so it routes to handoff-C2 (or at minimum **SURFACES** "N unpromoted TODO items need triage" in RELAY_STATUS) instead of filing the repo as plain "needs human / idle". Applies to BOTH the relay-loop classifier and `/relay next` route 2a. Keep the anti-guess discipline (id:78ff): still NEVER auto-promote an untagged item with a guessed lane — the fix is SURFACING + triggering a handoff pass, not auto-executing. <!-- id:9014 -->
- [x] **Classifier counts `[HARD — pool]` inside re-lane-criteria PROSE → phantom `hard` unit (false positive)** [ROUTINE] done 2026-07-02 (executor, ROADMAP twin id:fb7f; TODO twin ticked by review 2026-07-02 — cross-ledger drift fix): `gather-repo-state.sh` `open_hard_pool` lane-anchored + `unpromoted-scan.sh` `primary_lane` bold-title-anchored; `tests/test_gather_pool_count_anchor.sh` 6/6 green. (observed 2026-06-30 `/relay --intensive`, it-infra handback) — the discover/classifier's `open_hard_pool` count greps the literal substring `[HARD — pool]` in `ROADMAP.md`, which also matches **re-lane-criteria prose** on items that are explicitly NOT pool-lane — e.g. id:9321 / id:c5e9 read *"flip to `[HARD — pool]` only once …"* while themselves being tagged `[HARD — hands][INTENSIVE — local-llm]` + gated. Result: `open_hard_pool=2` (false), the loop emitted a `hard` verdict for it-infra, dispatched an Opus child, which correctly **pre-start-refused** (no executable pool item; clean worktree, no commit) — wasted a dispatch every intensive/opus run. **Fix:** the lane-tag match must anchor to an item's ACTUAL lane bracket (the `- [ ] … [HARD — pool]` tag on the item line), not a free substring grep that catches the token inside quoted criteria/prose. Likely the same predicate the lane work uses — fix it once in the shared matcher and add a test fixture: a ROADMAP whose only `[HARD — pool]` occurrences are inside back-tick'd re-lane prose must yield `open_hard_pool=0`. Relates id:3c0f (lane-token drift), id:09a3 (roadmap-lint grammar), id:78ff (lane vocab). The handback even suggested this fix verbatim. (Worktree auto-reap id:3ac8 also did NOT fire here — the clean worktree was left for manual prune; possible separate bug, watch for recurrence.) <!-- id:fb7f -->
- [x] **Discovery step inherits the session model (Opus) — pin it cheap** — CLOSED 2026-07-02 (handoff C2, overtaken-by-events): the preferred "remove the model from the hot path" line SHIPPED — the LLM discovery shard was DELETED by the id:a0b6 mechanical-runner swap (ROADMAP a0b6 [x] 2026-07-01, discovery now deterministic reconcile+classify+route), and the residual transport agents are pinned cheap (relay-loop.js:733 `discover-prelude` model:'haiku', :849 `discover-run` model:'haiku' — verified 2026-07-02); the id:3ac8 reap decision moved into deterministic `reconcile-repo.sh`, dissolving the "don't put reaping on haiku" blocker. Original text: relay-loop.js the per-round discovery `agent()` (classifier) has NO `model:` override, so it runs on the session model (Opus on a `--strong-tier opus` run), scanning 30+ repos every round; write-status/quota/integrate are already pinned haiku/sonnet. Classification is purely mechanical (4 deterministic rules over `git tag/log`, ROADMAP `[ROUTINE]` grep, `status --porcelain`, standin grep) with only the "acceptable-dirt" relay.toml-comment check needing any nuance. Cheap interim: add `model: 'sonnet'` (haiku if the acceptable-dirt judgment proves robust) to the discover opts — cuts per-round cost immediately. Full fix = the hybrid mechanical+model discover (id:23fe) / deterministic `discover-repos.sh` emitting DISCOVER_SCHEMA JSON. Do NOT cache verdicts in relay.toml (staleness — git is the source of truth; re-running cheap deterministic discovery each round is both correct and fast). Surfaced 2026-06-15. **DEPRIORITIZED 2026-06-15:** id:3ac8 added a *consequential* `worktree remove --force` decision to the discover agent, so a blind cheap-model pin is now risky (don't put reaping on haiku). Prefer parallel-shard (id:9ed4) + deterministic scanner (id:23fe), which remove the model from the hot path entirely rather than just cheapening it. <!-- id:22ef -->
- [x] [INBOUND routed:f3d0 from zkm relay review 2026-07-02] [ROUTINE] Fix perpetual Fable-recheck re-dispatch: `classify-repo.sh` sets `standin` by naive substring (`"fable-standin" in ckpt_msg`), so a GENUINE Fable recheck tag whose message merely MENTIONS the standin review it audited re-triggers standin; `relay-loop.js` ORs `standin||strongRecheckPending`, so an already-rechecked repo (fable_rechecked set, strongRecheckPending=false) re-elevates idle→review on EVERY Fable pool round (observed: redundant zkm dispatch, run relay-20260701-234115 — empty window, recheck already consumed 2026-07-01). Fix: gate on the durable watermark (`standin && !fable_rechecked`); keep recheck summaries free of the literal token. ROADMAP twin promoted same day. <!-- id:a42e --> done 2026-07-02 (executor)
  - [x] **A1 — `[MECHANICAL]` capability tag** [ROUTINE] — `gather-human-backlog.sh` + `roadmap-lint.sh` accept it; `classify-verdict.sh` emits new `mechanical` verdict (pool-inert; INTENSIVE composes); document in `relay/references/hard-lanes.md`. (meeting 2026-07-02-1924) <!-- id:7616 -->
  - [x] **A2 — recipe manifest schema + drop-dir contract** [ROUTINE] — `~/.config/relay/recipes/{pending,running,done}/`; recipe JSON `{id,repo,cmd,host,est_wall,resource,acceptance_artifact}`; whitelisted (relay-authored), never auto-scanned from ROADMAP. (meeting 2026-07-02-1924) <!-- id:64d3 -->
  - [x] **A3 — mechanical-run daemon** [HARD — pool] — `--user` `.path`-triggered oneshot (model-probe topology), runs pending recipes pending→running→done, writes artifact, drops a review-request consumed via `inject.sh`; `make install-mechanical-daemon`. Gated on A2. (meeting 2026-07-02-1924) <!-- id:b3d0 -->
  - [x] **A4 — `permitted-intensity.json` + `relay-intensity.sh` CLI** [ROUTINE] — graded windows (tea 15m/light, lunch 2h/heavy; `--afk` conservative default); daemon reads it; SUPERSEDE binary `ALLOW_INTENSIVE` with a graded gate + migration. Re-laned [HARD — pool]→[ROUTINE] at handoff (2026-07-02): executor-buildable slice = the config file + CLI + `permits` predicate + hermetic test; the risky relay-loop.js engine wiring (a0b6 template-literal hazard) is a follow-up note, not required for green. (meeting 2026-07-02-1924) <!-- id:e407 -->
  - [x] **A5 — `resource-probe.sh` (check-and-defer)** [ROUTINE] — VRAM/RAM/load + shared `claim.sh` read → check-and-defer arbitration (daemon + pool share one claim registry); never preempt. (meeting 2026-07-02-1924) <!-- id:68dc -->
  - [x] **B1 — target taxonomy → `hard-lanes.md` north star + converter + dual-vocab lint window** [HARD — pool] — deterministic `[HARD — *]`→new-vocab converter; lint accepts BOTH one window then old-vocab → ERROR. Gated on A1. (meeting 2026-07-02-1924) <!-- id:4f02 -->
  - [x] **B2 — lane-readers + references + engine (relay-loop.js) DUAL-ACCEPT the new vocabulary** [HARD — pool] — RE-SCOPED to B2a (parsers `gather-human-backlog.sh`/`classify-repo.sh`/`gather-repo-state.sh` + `references/*`) + B2b (`relay-loop.js` engine). DONE + relay-review-verified 2026-07-02: dual-accept proven BOTH directions (old `[HARD — *]` still buckets/dispatches; new `[HARD]`/`[INPUT — …]` buckets/classifies), no test gamed, suite green. The this-repo-ledger conversion + ~30-test migration + window-CLOSE tail was split out to id:7df1 (GATED). (meeting 2026-07-02-1924) <!-- id:8111 -->
  - [x] **M1 — handoff PRODUCER emits `[MECHANICAL]` + authors the recipe** [ROUTINE] (gap found 2026-07-02 post-build: A1 taught the classifier to RECOGNIZE `[MECHANICAL]`→`mechanical` verdict, but `handoff.md` C2:62 still only tags `[ROUTINE]`/`[HARD]` — the tag is recognized+routed but never PRODUCED and nothing runs it). Teach `handoff.md` C2 to recognize compute-only / no-LLM / benchmark-or-pilot work (leAIrn2learn, pytorch, model-probe-style batteries) → tag `[MECHANICAL]` AND author the A2 recipe manifest (id:64d3) into `~/.config/relay/recipes/pending/`. Missing producer link that makes `[MECHANICAL]` end-to-end; pairs with A3 (id:b3d0) so the daemon has input to run. DEP: A2 (done). <!-- id:9c88 -->
  - [x] **M2 — re-lane DOCTRINE routes compute-only→`[MECHANICAL]` (fix the "`[HARD — hands]` wrongly" producer sites)** [HARD — pool] — now that `[MECHANICAL]` exists, three contract sites still route scriptable/no-human/no-LLM "run" work to `[HARD — hands]` (human): (a) `hard-lanes.md` re-lane policy (~L88–101, the 5-criterion a–e outputs pool-vs-hands) — add a "needs an LLM?" branch: passes a–e + NO LLM ⇒ `[MECHANICAL]` (daemon), passes a–e + LLM ⇒ `[ROUTINE]`/`[HARD — pool]`, fails a–e (needs human) ⇒ `[HARD — hands]`/`[INPUT — access]`; (b) `handoff.md` author-then-run split (L93–106) — route the daemon-runnable "run X" residue to `[MECHANICAL]`, keep only genuinely-human runs (device/sudo/physical/credential) as hands; (c) `human.md` (L158–168/194/287) — EXCLUDE `[MECHANICAL]` from the "you run these" human checklist (daemon-run, not human-run), same author-then-run fix. ORTHOGONAL to B2 (the vocabulary rename) — a routing/doctrine change that must survive B2. DEP: A1 (done). <!-- id:2313 -->
  - [x] **A3-hostgate — daemon must ENFORCE the recipe `host` binding before running** [ROUTINE] (wave-2a review finding 2026-07-02, REVIEW_ME): `mechanical-daemon.sh` reads `est_wall`/`resource`/`cmd`/`artifact` but NEVER reads `.host` nor compares to `$(uname -n)`, even though `recipe-validate.sh` requires a non-empty `host` and `recipe-manifest.md` binds it to `[host:<name>]`. Latent auto-execute safety gap: a recipe bound to zomni would run on any host whose daemon picks it up. Fix: before running, the daemon skips/defers (leave in pending, log) a recipe whose `host` != current host — REUSE the existing `relay/scripts/host-gate.sh` (don't reinvent). Add a test exercising a foreign-host recipe (must NOT run). **Gates the daemon going live on any multi-host setup.** DONE 2026-07-02: mechanical-daemon.sh now calls host-gate.sh (synthesizing `[host:<name>]` from the recipe's `.host` field) before the intensity/resource gate; mismatch defers (stays pending, logged), match proceeds. Tests (4)/(5) in tests/test_mechanical_daemon.sh. <!-- id:9cfa -->
  - [x] **Parser hotfix — anchor `[MECHANICAL]` into the SOLE whitespace-delimited lane reader** [ROUTINE] (DONE 2026-07-03, executor commit 02832a8: added `[MECHANICAL]` to `LANE_TAGS`, derived `is_mechanical` from the anchored `primary`, deleted the L93 substring counter → anchoring alone sufficed, no extra regex; `make test` 174/0) — `relay/scripts/classify-repo.sh` has two disagreeing tag readers: L102 lane derivation is positionally anchored (id:4da4, first recognized `LANE_TAGS` token wins), but `[MECHANICAL]` (L93) is a bare `"[MECHANICAL]" in ln` substring test — NOT in `LANE_TAGS`, skips anchoring — and `open_mechanical>=1` drives the priority-6 `mechanical` verdict. Fix: add `[MECHANICAL]` to `LANE_TAGS`, derive `is_mechanical` from the anchored `primary` tag, DELETE the L93 substring counter (anchored derivation becomes the sole lane reader), whitespace-delimit the token match (a backtick-adjacent tag in prose must not match). RED spec `tests/test_mechanical_lane_anchor.sh`: a `[HARD — pool]` item with a backtick'd `[MECHANICAL]` in its body must NOT emit the `mechanical` verdict. Format-agnostic (works with tag-anywhere) — no ledger churn. (meeting 2026-07-03-0830, `docs/meeting-notes/2026-07-03-0830-todo-lane-tag-format-prior-art.md`) <!-- id:0d58 -->
